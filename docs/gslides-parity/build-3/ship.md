# Ship step, Google Slides parity round three

The ship step's record (`docs/gslides-parity/MILESTONES-3.md` "Ship step"), written 2026-09-14
(PDT) on the shared checkout at `/Users/kevinliu/repos/Turboslide`, `main` at `a1dcc7e` (the other
workflow's round four research and design commit sits on top of `28cb63b`) plus the working tree
of the six builders, the integrator, the fixer round and the verifier's pass 2
(`docs/gslides-parity/VERIFICATION-3.md`; its section 16 is the production table this step
appends). Section numbers refer to SPEC-3 unless prefixed SPEC or SPEC-2; finding numbers are
VERIFICATION-3 section 11's. Every command ran from the repository root; the git write commands
here are the ship step's own (`git add` by explicit path list, two commits, two pushes); no `pnpm
install`, `pnpm add`, `pnpm exec` or `pnpm build` ran outside `scripts/check.mjs`; Docker ran
inside check step 25 only and nothing was pruned; the bearer of `~/.config/turboslide/hosts.json`
was read in code (`verification-2/ship/run-with-token.mjs`) and never printed; the two production
secrets were generated by `openssl rand` inside `verification-3/ship/set-production-env.sh` and
went from its pipe into the vercel CLI, never to a terminal or a file.

The account boundary held: no Marketplace product was installed, no paid resource created, no DNS
or sending domain touched, no service signed up for. Production runs the degraded tiers (section 5).

## 1. Preconditions, as verified here

- `git status` at the start: 440 lines (254 modified, 186 untracked paths), nothing committed since
  `a1dcc7e`; the other workflow's `.github/workflows/check.yml`, `design-4/*/previews/*.png` and
  `verification-4/` untracked beside this round (its `research-4/`, `design-4/`, `SPEC-4.md` and
  `MILESTONES-4.md` are committed in `a1dcc7e`); four scratch decks under `decks/`
  (`e2e-realtime-mu0pdewd`, `e2e-realtime-mu0vkwxf`, `untitled-20260914-46ub`,
  `untitled-20260914-js2x`) left by specs and probes; `scripts/__pycache__/`. None of those ships:
  `verification-3/ship/commit-paths.mjs` builds the path list from `git status --porcelain -uall`
  minus those prefixes and asserts no `.turboslide`, `.vercel`, `dist`, `target`, `node_modules`,
  `.log`, `.env`, `.sqlite`, `.tsbuildinfo` or `.pyc` path is left (section 4 has the count).
- Ports 4321, 3005, 4331 to 4336 and 4344 free at the start; no `.turboslide/e2e.lock`.
- `git grep -n '<<<<<<<'`: no marker outside the prose that names the command (MILESTONES-2,
  MILESTONES-4, MILESTONES, VERIFICATION, the two earlier ship reports).
- `pnpm generate:contracts`: "14 files current" (before the ship step's edits, which touch no action).
- `node_modules/.bin/tsc -b`: exit 0 before and after the edits (0.2 s incremental, 6.0 s after).
- `pnpm format:check` (finding 38): 79 files, 49 the other workflow's untracked `design-4/`,
  `research-4/`, `verification-4/`, and 30 of this round (`apps/studio/src/server/auth/identity.ts`,
  the fixers' 16 probe scripts under `build-3/{b2-fix,b3-fix}/`, `b2.md`, `b3.md`, `b6.md`, and the
  verifier's ten JSON and Markdown outputs under `verification-3/`); `prettier --write` on the 30,
  then `--check` clean. The other workflow's 49 stay as they are.
- The Vercel project: `.vercel/project.json` links `turboslide` (team `kl01s-projects`); `vercel
whoami` answers the account; the stored environments held `TURBOSLIDE_TOKEN` (preview, production)
  and `BLOB_READ_WRITE_TOKEN` alone (VERIFICATION-3 section 12 item 5); `vercel firewall overview`
  read "Firewall: Not configured".
- VERIFICATION-3 section 1 left three severity 3 findings (23, 33, 34) and twelve severity 2
  findings (16, 17, 18, 20, 21, 22, 24, 36, 37, 39, 40, 41, 42, 43, 46) open against this step's
  precondition of severity 1 or recorded deviations. Section 2 says which the ship step closed or
  narrowed; section 6 records the rest as blockers. Kevin asked for no stops, and production stays
  working on the degraded tiers in shadow mode (section 5), which is the condition for shipping with
  a severity 3 recorded.

## 2. Fixed at the ship step

Every edit below is in a seam file or a test; each has a unit test or is covered by an e2e spec of
step 26, and none weakens an existing assertion.

1. Finding 34 (severity 3, narrowed; B2 and B4's files). `apps/studio/src/server/access.ts`: the
   blob tier's record cache trusts an entry for `BLOB_ACCESS_TTL_MS` (5 s) instead of 60 s, because
   the drop bus is process local and another instance's write never reaches it (the file tier keeps
   60 s); `readStoredAccessFresh(deckId)` drops the entry and reads the store, and
   `hostedAccessHooks.load()` uses it, so a share write's `ifMatch` etag is the store's and not a
   copy another instance made stale (the "access.json changed in the Blob store since it was read"
   answer on the second link a rep minted within a minute). `apps/studio/src/start.ts`
   `findShareLink`: a miss through the cache is followed by one pass past it before the 404, so a
   link minted on another instance seconds ago is found (one store read per deck for an unknown
   token, bounded by the deck count). New `packages/store/src/access-cache.test.ts` (5 tests): the
   entry inside the window and the store after it, the 60 s default, drop then read seeing a record
   written behind the cache with the etag a conditional write needs, the cached null, the write
   here dropping a second cache on the same bus. The stale window hosted is now at most 5 s for a
   read and zero for a share write and an exchange miss; F2 (the link hash index and the shared bus)
   stays B2's and Redis closes it (section 6).
2. Finding 36 (severity 2; B6's files). `packages/chrome/src/dialogs/NamePrompt.tsx` takes `modal`
   (default false, the floating card of finding 8); `EditorShell.tsx` renders the dialog slot's
   `namePrompt` (the own chip's Change name row) with `modal`, so a prompt a person opened traps
   focus, sits over the scrim and closes on Esc (SPEC-3 15), while the prompt that fires after a
   typing burst stays a floating card that takes no focus. New
   `packages/chrome/src/__tests__/name-prompt.test.tsx` (2 tests): the floating card has no
   `aria-modal`, leaves focus on the caret and ignores Esc; the dialog has `aria-modal`, the scrim,
   focus inside and Esc calls `closeDialog` and `onNamePrompt(false)`. `packages/chrome` 483 of 483.
3. Finding 40 (severity 2; B6's spec). `apps/studio/e2e/share.spec.ts`: the copying context's
   `storageState()` (its sealed `__Host-ts_id` cookie, the copy's owner under SPEC-3 6.1 and 7.1) is
   kept and every owner context starts from it, so the owner of the tests is the owner of the
   record; rows 2 to 4 can run for the first time. Verified in check step 26 (section 3).
4. Finding 44 (severity 1, wording; B2's route). `apps/studio/src/routes/edit.$deckId.tsx`
   `viewerAdapter`: a refused window API write answers `REFUSALS.viewOnly` and
   `REFUSALS.requestEditAccess` ("You can view this presentation. Request edit access to change
   it.") for a caller without the `write` capability, and "needs Editing mode; switch View > Mode
   to Editing for it" for an editor in Viewing or Commenting mode; the round one sentence with its
   internal noun ("the viewer owner") is gone from this route.
5. Finding 45 (severity 1; B1's, B5's and B3's tests). Per test budgets of 30 s (`{ timeout }`,
   assertions unchanged) on the three property tests of `packages/schema/src/transform.test.ts`
   (10,000 and 1,000 pairs), the sixteen asset rows of `packages/effects/src/parity.test.ts` and
   the 1,000 mark row of `packages/identity/src/marks-render.test.ts`: the four rows that tripped
   the 5 s default in this step's first step 5 run (load average 26 from the run itself) and the
   one the verifier saw twice. Each file passes alone (24, 26 and 9 tests).
6. Finding 38 (severity 2). The 30 files formatted (section 1).

Not fixed here, recorded in section 6: findings 23 and 33 (design changes in B2's room client and
the edit route's write path), the rest of finding 34, and the severity 2 rows that need a builder.

## 3. The chain

`verification-3/ship/run-check-chain.sh` (the verifier's driver with two changes: it starts at
step 4 and its logs are `.txt`) ran `node scripts/check.mjs --from 4` and, after each step that
stopped the chain, again from the next step, so every step from 4 to 28 ran on the fixed tree:
eight segments from 00:50 to 01:55 PDT (`ship/driver.txt`; `check-from-4.txt`, `-6`, `-20`,
`-21`, `-22`, `-26`, `-27`, `-28`), the runner's own dev server on 4321 for the server steps
(stopped by the runner after each segment; 4321 free before and after) and `vite preview` on 4344
for step 27 (the driver's pid file was empty, as the verifier saw; the ship step stopped it by
hand after the chain, `lsof` empty after). Step 3 was not rerun as a step: `pnpm
generate:contracts` answers "14 files current" and the diff it gates is the round's uncommitted
output, committed in section 4. The load average ran between 5 and 26 during the chain (the
vitest run of step 5 and the Playwright steps are most of it; the other workflow's processes the
rest). Two steps were rerun alone after the chain (`ship/rerun-26-and-5.sh`, `check-only-26.txt`,
`check-only-5.txt`) because the ship step's edits to `share.spec.ts` landed after the chain's own
step 26 had started and the budgets of finding 45 were measured against a loaded run.

| Step | Command (abridged)                                                       | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 4    | `tsc -b`                                                                 | ok                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 5    | `pnpm test` (the whole tree's vitest run)                                | FAIL in the chain, 153.4 s: 264 files, 2,826 passed, 4 failed, 3 skipped; the four are 5 s budgets under the run's own load average of 26 (`packages/effects` `parity.test.ts` three asset rows at 5.6 to 12.1 s, `packages/identity` `marks-render.test.ts` the 1,000 mark row at 8.0 s); the transform property test the verifier saw trip passed with its new budget; each file passes alone (26 and 9 tests). Budgets added to those rows (section 2 item 5); the rerun alone is below                                                                                                                                                                                                                                                                                         |
| 6    | `pnpm build && check-client-bundle && --greps`                           | ok, 13.8 s: marker 0 of 45 client files, 1 of 115 server files, node builtins 0 of 33; `innerHTML` 24 call sites in the allowlist, `overwrite: true` 10; `dist/server` 10.7 MB, `dist/client` 4.2 MB                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 7, 8 | the Prototemplate import                                                 | ok, 3.4 s and 0.1 s: 85 slides, 8 sections, 0 html escape blocks, idempotent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 9    | `validate decks/gt-brand`                                                | ok, 3.5 s                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 10   | `render all --theme light,dark`                                          | ok, 20.8 s; 170 records, no page error (11)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 12   | `compare-to-shoot.mjs --max-mismatch 0.005`                              | ok, 53.3 s: 170 pairs, 170 compared, 0 over budget; worst 0.408 percent, mean 0.016 percent (the Perfect export of the GT deck stays pixel identical)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 13   | `sheet all`, both sheets with 85 cells (14)                              | ok, 4.0 s                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 15   | `lint all --json`                                                        | ok, 13.6 s                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 16   | `build --budget 16`                                                      | ok, 7.1 s                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 17   | `playwright test viewer.spec.ts`                                         | ok, 25.8 s                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 18   | `lint --chrome` on `/deck`, `/edit`, `/new`, `/decks` at 1440, 1280, 390 | ok, 109.1 s: 24, 24, 24 and 6 audits, 0 with findings, 0 states unapplied                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 19   | `pnpm format:check`                                                      | FAIL, 24.5 s, on the other workflow's 49 untracked files alone (`SPEC-4.md` 1, `design-4/` 36, `research-4/` 9, `verification-4/` 3); every file of this round passes (finding 38 closed)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 20   | `gslides-parity-audit.mjs`                                               | FAIL, 1,121.0 s: 2,633 pass, 8 fail, 350 skipped (the verifier: 2,635, 10, 350); `verification-3/parity-audit.json`. `effects:scratch title.account.changeName` passes now (finding 36 closed); the eight are `rows:scratch title.account.signIn` (finding 35), `effects:scratch insert.comment` (finding 37's audit state), `effects:textBlock2 format.dropShadow` (finding 16, a deviation), and the five rows of finding 33 (`key.rotate.right15` "revision 193 -> null", then `key.rotate.left1`, `key.nudge.right1`, `key.nudge.down10` "could not select audit-shape" and `lineStart.fillCircle` "baseRevision 193 is stale; the document is at revision 194")                                                                                                               |
| 21   | the 15 round two specs                                                   | FAIL, 820.4 s: 66 tests, 34 passed, 21 failed, 11 did not run (the verifier: 33, 21, 12); the same files and causes as VERIFICATION-3 step 21 (finding 23; VERIFICATION-2 finding 28 stays open)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 22   | the fixture in both modes with `export check`, the flatten report        | ok, 53.6 s: `gslides-light.pptx` 1.32 MiB, `gslides-dark.pptx` 1.34 MiB, `gslides-both.zip` 2.66 MiB, 157 parts and 186 relationships valid in both modes, 28 slides with 28 notes parts; the flatten report perfect, worst decoded mismatch 0.000 percent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 23   | `fonts build --check`                                                    | ok, 47.8 s: 0 stale files, version 4.001+gt.2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 24   | `canvas-fidelity.mjs` over the GT deck and the templates                 | ok, 83.6 s: 171 slides converted, 342 pairs, 0 over budget; worst 0.262 percent (`gt-brand/directions` dark), mean 0.004 percent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 25   | Docker build and `export --mode native --verify` in the image            | FAIL, 7.4 s, in the image build's copy of the context: "write /docs/gslides-parity/design-4/type/previews/home-light.png: no space left on device" (the Docker VM's disk is full, VERIFICATION-3 section 12 item 9); nothing pruned; the container verification did not run, as in both verifier passes                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 26   | the eight round three specs                                              | FAIL in the chain, 611.2 s: 40 tests, 19 passed, 6 failed, 15 did not run (the verifier's counts). `accounts` 7 of 7, `dither` 5 of 5, `security` 4 of 5 run (the upload type mismatch, finding 22), `realtime` 1 of 2 run (the 200 keystroke convergence, finding 39), `presence` 1 of 2 run (the `[data-run]` of finding 24), `comments` 0 of 1 run (finding 37), `versions-by-author` 0 of 1 (finding 24), `share` 1 of 2 run: row 1 passed and row 2, past the owner refusal of finding 40 for the first time, failed on `locator('.pt-viewer')` resolving to two editor skeletons (`pt-viewer is-editor ts-skeleton`) in `openDeck` for the stranger; the spec's five viewer locators now read `.pt-viewer:not(.ts-skeleton)` (section 2 item 3) and the rerun alone is below |
| 27   | `layout-shift-audit.mjs` against `vite preview` on 4344                  | FAIL, 663.4 s: 109 cells, 58 at zero load entries (the verifier: 55), 51 with entries, 69 failures (72), 8 skipped states; `verification-3/layout-shift.json`. By route: `/new` 6 of 6 at zero, `/edit` 11 of 16 (the `notesHidden` variants' `div.sheet` at 0.0134 and the `panel` and `inbox` frame budgets), `/deck` 1 of 30 (`div.pt-bar-r`, max 0.1306 with the outline density), `/embed` 1 of 7 (0.0021), `/decks` 18 of 21 (`li.ts-hm-card`, the footer, the `filter` state), `/decks/trash` 6 of 6, `/present` 7 of 8 and `/print` 8 of 9 (0.0002 each), `?present=1` 0 of 6 (0.0729); the shell unmount and remount during hydration on `/new`, `/edit`, `/present` and `?present=1` as before (finding 21)                                                              |
| 28   | `pnpm audit --prod --audit-level=high`                                   | ok, 1.0 s: 0 advisories at high or above, 0 accepted, 0 open, 0 expired                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

The reruns alone, after the chain (`ship/rerun.txt`, 01:56 to 02:08 PDT, one after the other):

- Step 26 (`check-only-26.txt`, 620.4 s): 40 tests, 20 passed, 6 failed, 14 did not run.
  `share.spec.ts` row 2 passes for the first time (the stranger's four routes, the viewer link on
  `/deck` without notes, `/edit` in Viewing with View only, the commenter link in Commenting; the
  owner context and the viewer locator of section 2 item 3) and row 3 runs for the first time:
  rotate and Stop sharing kill the link and publish answers the player with 200, then the row
  fails on the player page carrying no `x-robots-tag: noindex` header (`share.spec.ts:268`), a
  product fact recorded as finding 48 in section 6; row 4 did not run behind it. The other five
  failures are the chain's (`comments` row 1, `presence` row 2, `realtime` row 2, `security`'s
  upload row, `versions-by-author`), findings 37, 24, 39, 22 and 24.
- Step 5 (`check-only-5.txt`, 83.4 s under a load average of 22): ok, 264 files, 2,830 passed, 3
  skipped, with the budgets of section 2 item 5. Finding 45 is closed for the rows it named; the
  vitest budgets under load item of VERIFICATION-2 section 14 stays open in principle for any
  other row.

The count: 19 of the 25 steps run pass (4, 6 to 18, 22 to 24, 28); step 3 passes once the
contracts are committed (section 4); the six that fail are the same six the verifier's pass 2
recorded (5 under load, 19 on the other workflow's files, 20 on eight rows, 21 on the round two
specs, 25 on Docker's disk, 26 on six round three rows, 27 on the layout shift cells), each with
fewer misses than pass 2 or the same, and each recorded in section 6 or as a deviation. The ship
condition of 16.1 (28 of 28, step 21 green) is not met; the ship proceeds under the orchestrator's
rule because production stays working on the degraded tiers in shadow mode (section 5).

## 4. The commit and the push

Commit `1178478c6d11f651c15200bd3f2fd020694a5c91` on `main`, "Turboslide round three: multiplayer,
presence, comments, share roles, optional accounts, security, layout shift, background dithers",
authored as the repository's local identity (Kevin <kk23907751@gmail.com>) at 02:10:22 PDT with
the body carrying the acceptance summary and the account boundary (what runs on the degraded tiers
and the variables that turn the full tiers on) and the trailer `Co-Authored-By: Claude Fable 5.1
<noreply@anthropic.com>`; 788 paths, 381,776 insertions, 45,308 deletions, staged by the explicit
list `verification-3/ship/commit-paths.mjs` printed (`git status --porcelain -uall` minus the
other workflow's `.github/`, `design-4/`, `verification-4/`, the eight scratch decks specs and
probes left under `decks/` and `scripts/__pycache__/`; 1,251 paths excluded, every one of them in
those sets), and checked after staging for `.github`, `.turboslide`, `.vercel`, `dist`, `target`,
`node_modules`, `__pycache__`, `.log`, `.env`, `.sqlite`, `.tsbuildinfo`, `.pyc`, `research-4`,
`design-4`, `verification-4`, `SPEC-4`, `MILESTONES-4` and `decks/e2e-`, `decks/untitled-` (none).
The commit carries the regenerated contracts (14 files), the fonts (`Inter Fallback`), the fixture
deck's two dither slides, four variants and three threads, `firewall/rules.json`,
`packages/realtime`, `packages/identity`, the six builders' reports, the verifier's record and
evidence (`verification-3/`, the `.log` files staying local under the root `*.log` rule as in
rounds one and two; the JSON, Markdown, PNG, `.mjs` and `.sh` evidence travels), this report and
the ship scripts under `verification-3/ship/`. `git status` after the commit shows no modified
tracked file; the untracked remainder is the other workflow's and the scratch decks. `node
scripts/check.mjs --only 3` after the commit: ok (`ship/check-only-3.txt`; the contracts are
tracked and the diff is empty), which closes the one step the chain could not pass on the
uncommitted tree.

`git fetch origin` read `origin/main` at `a1dcc7e`, the commit's parent; pushed `a1dcc7e..1178478`
to `origin/main` at 02:10:36 to 02:10:40 PDT. The second commit, "Turboslide round three: the
production table", carries VERIFICATION-3.md section 16, this report's sections 5 to 7, the
production screenshots under `verification-3/production-*.jpg`, the walk's production JSON, the
ship evidence under `verification-3/ship/` (the poll, the smoke rows, the firewall answer, the
Vercel record) and the `appliedAt` line of `firewall/rules.json` if the rules were applied.

## 5. Production

VERIFICATION-3.md section 16 is the production table; the facts in one place:

- The deploy: the Vercel Git integration built `dpl_7RFeZg9vZ6TnwB7ZSHuWekekKmtb`
  (`turboslide-g7whhxqxx-kl01s-projects.vercel.app`) from the push, created 02:10:45 PDT, Ready in
  about a minute, aliased to `https://turboslide.vercel.app`, `turboslide-kl01s-projects.vercel.app`
  and `turboslide-git-main-kl01s-projects.vercel.app`, five functions at 107.03 MB in `iad1`
  (`ship/production-vercel.txt`). The poll (`ship/poll-deploy.mjs`, `production-poll.txt`) read the
  round two deploy on three polls (`/new` 24,563 B, `ts-presence` 0) and the new one at 02:12:26,
  106 s after the push (`/new` 29,827 B; `ts-presence` 26, `ts-comment-marker` 9,
  `ts-remote-caret` 6 bytes of markers in its assets).
- The environment (section 1 of `set-production-env.sh`, `production-env.txt`): the six variables
  above, all stored as sensitive by the project's policy; `TURBOSLIDE_MAIL=off` and not `capture`
  because captured mail wants a database or a file the function cannot keep and nothing sends while
  sign in is off; `TURBOSLIDE_PUBLIC_STORE_HOST` read from one listed blob's host through the
  read write token in a subshell (the host is a public URL, the token never printed). A rerun of the
  script rotates the session secret and signs every anonymous principal out of its name, so it runs
  once.
- The smoke: 18 of 18 at 02:13:17 (`production-smoke.txt`); with the bearer 19 of 20 on the first
  run, the batched export's first batch answering 502 from the cold function's Chromium launch
  (`browserType.launch: Target page, context or browser has been closed`), then 20 of 20 with six
  slides and 20 of 20 with the whole deck (236.5 s, `perfect: true`, 16,277,390 B;
  `production-smoke-token.txt`, `-export-retry.txt`, `-export-full.txt`).
- The two browser walk: 18 ok, 13 fail in 205 s (`two-browser-walk-production.json`,
  `production-01` to `-11.jpg`, `ship/production-walk.txt`), the failures the blob tier's
  propagation, finding 33's stale refusals, finding 37's late comment, finding 43's empty version
  rows and finding 34's conflict on the second link (section 16 has each row); the scratch deck
  trashed and removed.
- `share.get` on a deck written after the deploy reports its record (`production-share-get.mjs`,
  `.txt`); a keystroke between two browsers lands in 2.0 to 2.9 s on the blob tier with two of five
  refused as stale (`production-propagation.txt`); B in A's roster in 4.8 s (1.0 s in the walk).
- The WAF: the plan accepted the 23 rules; staged and published in log mode at 02:20 PDT
  (`production-firewall.txt`; `vercel firewall overview` "Enabled, 22 active, 1 inactive"); the
  ids written into `firewall/rules.json` beside `appliedAt`; Bot Protection is Kevin's setting.
- The storage migration: not run on production (section 6, the second Kevin item).
- R0, R1, R3 (shadow), R4 (blob tier), R5's surfaces and R7 are live; R2 waits on the private
  store, R6 on the sending domain, R8 on Kevin's date.

## 6. Blockers recorded for the next fix round, and what waits on Kevin

Every item below is VERIFICATION-3 section 11's or 12's, unchanged in this step apart from the
narrowing of finding 34; the owner is the one named there.

The blockers (a design change, or a builder's file the ship step does not patch):

1. Finding 33 (severity 3, B2, `packages/realtime/client/room-client.ts` and the edit route's
   write path). A page's `describe().state.revision` lags the server after its own acknowledged
   write (the ack updates the client's `revision` variable but the document's `deck.revision`
   moves only on the stream's `checkpoint` event) and after opening a deck that has just been
   written, and a window API write issued in that window carries the stale `baseRevision` and is
   refused ("baseRevision N is stale") or, for the first typing burst of a page whose revision
   lags, lost without a word. S2's "no keystroke lost" fails on that first burst. The fix is a
   design change in the client (the revision based writes and the op stream disagree about what
   the page knows) and is not the ship step's.
2. Finding 23 (severity 3, B2, B5, B6, B4; the round two specs of check step 21). 21 of 66
   round two tests fail and 12 do not run on the fixed tree for the causes VERIFICATION-3 step
   21 lists (`text.splice` where the specs pin `text.replace`, the stale `baseRevision` of
   finding 33, a second `.pt-viewer` mounted, the export batch's 28 pages against the pinned 26,
   the cancel's 403, the robots meta with a nonce, the 44° readout, the filmstrip menu's
   `aria-checked`, the logo drop, the talk track, the Share dialog's `dialog.share.view.copy`).
   VERIFICATION-2 finding 28 stays open; the ship condition of 16.1 (step 21 green) is not met
   and this step ships with it recorded, as round two did.
3. Finding 34's remainder (B2): F2, the link hash index and the drop message on the channel, so
   an instance learns of another's write before its 5 s window ends; Redis (`REDIS_URL`) closes
   it for the record cache as SPEC-3 6.1 specifies.
4. Finding 21 (severity 2, B5 and B6): the layout shift audit at 55 of 109 cells at zero (the
   deck route's toolbar right group, the `notesHidden` editor variant, the `panel` and `inbox`
   driven states, the home page's `filter` state, the presenter's `timerHour`, the presenting
   variant's stage, and the whole shell remounting during hydration on `/edit`, `/new`,
   `/present`).
5. Findings 37 and 39 (severity 2, B2 and B6): a comment reaching the other browser's chrome in
   2.1 to 2.2 s locally against the 2 s bar and over 6.5 s on the blob tier; 200 interleaved
   keystrokes converging in 6.8 s under load against 2 s.
6. Finding 43 (severity 2, B6 and B2): version history shows the window with both marks and 0
   version rows under it. Finding 46 (severity 2, B5 and B4): Upload from computer in the
   Background dialog lands no picture hosted within 45 s while the presigned grant is issued.
   Finding 41 (severity 2, B6 and B5): the strict tooltip audit's 85 `a.pt-orow` rows on
   `/deck/gt-brand`. Finding 42 (severity 2, B6): a marquee from an empty part of the sheet selects
   nothing. Finding 22 (severity 2, B4): the upload type mismatch answering 403 in shadow mode
   where the spec expects 400. Finding 18 (severity 2, B2 and B4): three 500s beside 34 conflicts
   under five concurrent writers on one blob store deck. Finding 20 (severity 2, B2 and B6): the
   own chip keeps the label after the name is given until a reload. Finding 24 (severity 2, B6
   and B2): `presence.spec.ts` row 2 and `versions-by-author.spec.ts` waiting on a `[data-run]`
   the canvas does not draw for the mood slide's heading.
7. Finding 48 (new, severity 2, B4 and B2, the deck route in present mode and
   `apps/studio/src/server/headers.ts`): the published player page (`/deck/<id>?p=<token>`, SPEC-3
   6.4) answers 200 without an `x-robots-tag: noindex` header; `share.spec.ts` row 3 (the first
   run of that row, the step 26 rerun) pins the header at line 268 after rotate, Stop sharing and
   publish passed. The `/s/<token>` exchange and `/new` carry the header (VERIFICATION-3 section 7).
8. Findings 16 and 17 as recorded deviations (the paragraph's Drop shadow section is a schema
   change for B1; the quotas never trip across instances without Redis), 15, 19, 35, 47 as
   severity 1, and the container verification of step 25 (the Docker VM's disk: 98 GB with 0
   available, nothing pruned).

What waits on Kevin (SPEC-3 section 18, `docs/hosting.md` sections 9 to 11, VERIFICATION-3
section 12), none of it an agent's to do under the account boundary:

- The Marketplace installs, two products beside Blob (decision 8): Upstash Redis on the fixed 250
  MB plan from the project's Storage tab (`REDIS_URL`; with it the `redis` realtime tier with
  presence and writes under the 200 ms bar, the shared record cache, the inbox and captured mail
  store, and with `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN` the cross instance
  quotas of finding 17) and Neon Postgres on the free tier (`DATABASE_URL`, `BETTER_AUTH_SECRET`;
  with them accounts and the Sign in row).
- The private Blob store and the storage migration (R2 and R5): `vercel blob create-store
turboslide-private --access private --region iad1 --yes`, `TURBOSLIDE_BLOB_PRIVATE_TOKEN` on the
  preview, `turboslide admin migrate-storage plan`, `copy` and `verify` to the dual read window, a
  day's watch, `cutover` and `delete`; production after the preview verified. The ship step did
  not run it on production: VERIFICATION-3 recorded no preview migration and no rollback flag
  test hosted (the fake's `migrate.test.ts` is the record), which the orchestrator's rule makes
  the condition. Until then every document has a predictable public URL (F8).
- The sending domain and address for Resend (decision 10; `RESEND_API_KEY`,
  `TURBOSLIDE_MAIL_FROM`, and `TURBOSLIDE_MAIL` back from `off`): R6 (sign in by magic link and
  code, invitations, digests) waits on it; until then sign in is GitHub only when
  `GITHUB_CLIENT_ID` and `GITHUB_CLIENT_SECRET` are set (decision 11), which excludes the sales
  audience.
- The R8 date (decision 12; the round's default is one month after R5): the synthesized `open:
editor` default ends and a missing access record becomes a 404 (`TURBOSLIDE_MISSING_RECORD`);
  the CSP from report only to enforce (`TURBOSLIDE_CSP=enforce`) after the two report weeks; the
  WAF rules from log mode to their `enforce` objects, the bypass first, and Bot Protection from
  log to challenge; `TURBOSLIDE_AUTHORIZE=enforce` after the R3 week's denial counts per route
  were reviewed (the 429 rate limit rules may be flipped before the deny and challenge rules
  finish their log week, `docs/security.md` section 3).
- The production domain before passkeys (decision 9, `TURBOSLIDE_PASSKEY_RPID`).
- Docker's disk (step 25), and the two preview entries in `~/.config/turboslide/hosts.json`
  (`turboslide-igavfcg2x`, `turboslide-dniaw7w8f`) to remove when the previews are gone.

## 7. Commands run

| Command                                                                                                                                                                       | Result                                                                                                          |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| `git status --short`, `git grep -n '<<<<<<<'`, `lsof` on 4321, 3005, 4331 to 4336, 4344, `git check-ignore -v`                                                                | 440 lines; no marker outside prose; ports free; `*.log`, `.turboslide`, `decks/*/comments/` ignored             |
| `pnpm generate:contracts` (three times), `node_modules/.bin/tsc -b` (four times)                                                                                              | "14 files current"; exit 0                                                                                      |
| `pnpm format:check`; `prettier --write` on 30 files of this round and the ship step's own edits                                                                               | 79 files listed, 30 of this round; clean after                                                                  |
| `vitest run` in `packages/schema` (transform), `packages/store` (all), `packages/chrome` (all), `apps/studio` (all), `packages/effects` (parity), `packages/identity` (marks) | 24; 122; 483; 166; 26; 9 passed                                                                                 |
| `vercel whoami`, `vercel env ls`, `vercel firewall overview`, `vercel blob list --limit 1` (the read write token in a subshell)                                               | the account; two variables stored; "Not configured"; the public store host                                      |
| `sh verification-3/ship/set-production-env.sh <host>`                                                                                                                         | six production variables set, values never printed (`production-env.txt`)                                       |
| `sh verification-3/ship/run-check-chain.sh` (eight segments from step 4)                                                                                                      | section 3; 4321 and 4344 free after (the leftover `vite preview` killed by hand)                                |
| `sh verification-3/ship/rerun-26-and-5.sh`                                                                                                                                    | step 26: 20 passed, 6 failed, 14 did not run; step 5: ok, 2,830 passed                                          |
| `node verification-3/ship/commit-paths.mjs`; `git add -- <788 paths>`; `git commit -F <message>`; `git fetch origin`; `git push origin main`                                  | `1178478`; `a1dcc7e..1178478` at 02:10:40 PDT                                                                   |
| `node scripts/check.mjs --only 3`                                                                                                                                             | ok after the commit                                                                                             |
| `node verification-3/ship/poll-deploy.mjs https://turboslide.vercel.app 15`                                                                                                   | the round three chrome at 02:12:26, poll 4                                                                      |
| `vercel ls --prod`; `vercel inspect turboslide-g7whhxqxx-kl01s-projects.vercel.app`                                                                                           | Ready, created 02:10:45, aliased to turboslide.vercel.app (`production-vercel.txt`)                             |
| `node scripts/hosted-smoke.mjs --base https://turboslide.vercel.app`                                                                                                          | 18 of 18                                                                                                        |
| `run-with-token.mjs <host> -- node scripts/hosted-smoke.mjs ... --token-env TURBOSLIDE_TOKEN --export-batch` (whole deck, then `--slides 6`, then the whole deck again)       | 19 of 20 (the cold 502); 20 of 20; 20 of 20 in 236.5 s, perfect                                                 |
| `node verification-3/walk/two-browser-walk.mjs --base https://turboslide.vercel.app --out verification-3 --tag production`; `sips` to JPEG                                    | 18 ok, 13 fail in 205 s; 11 screenshots                                                                         |
| `node verification-3/ship/apply-firewall.mjs`                                                                                                                                 | 23 rules staged and published in log mode; the ids into `firewall/rules.json`; `headers.test.ts` 16 of 16 after |
| `run-with-token.mjs <host> -- node verification-3/ship/production-share-get.mjs`                                                                                              | the record on a fresh copy, a viewer link, the copy removed                                                     |
| `run-with-token.mjs <host> -- node verification-3/preview/blob-propagation-probe.mjs https://turboslide.vercel.app 5`                                                         | presence 4.8 s; writes 2.0 to 2.9 s, 3 of 5 landed; both pages at revision 4; the copy removed                  |
| the second commit and push                                                                                                                                                    | the hash in the final report                                                                                    |
