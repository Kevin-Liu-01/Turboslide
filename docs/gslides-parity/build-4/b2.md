# B2 notes, round four: the /home route and its hero

The home builder of the Turboslide round four (`docs/gslides-parity/MILESTONES-4.md`, "B2 The /home route and its hero"), in the shared working tree over `main` at `d5d7f07` (`BASE`) after merge 1, on 2026-09-14. Section numbers refer to `docs/gslides-parity/SPEC-4.md` unless prefixed. Every command ran from `/Users/kevinliu/repos/Turboslide` with Node 24.13.0. No git write command ran; nothing is committed (the ship step commits); no `pnpm install`, no build outside `scripts/check.mjs`. Dev server port 4342 with the tmp store, started for the spec, the chrome lint, the tooltip audit and the shots, stopped before returning. Other builders edited the shared checkout while this server ran (B1's `__root.tsx` and twins, B3's `vite.deploy.config.ts`, `decks.index.tsx`, `-access-page.tsx`, B4's store and filmstrip files, B5's documents), so Vite reloaded the served page several times mid run; every result below is from a run that completed after the reload. Appended across stages.

## 1. Days 2 to 5 (2026-09-14)

### 1.1 What landed

MILESTONES-4 B2 "Delivers" items 1 to 6, on B1's day 1 and day 2 seams (`brand.css` tokens, `TurboslideMark`, `EmptyFigure`, `Progress`'s ramp, `SITE`, the twins under `apps/studio/public/brand/`, `facts.json`) and the integrator's day 0 amendments (the `main` landmark, a root class that is not `.ts-home-page`).

The route and the sheet:

- `apps/studio/src/routes/home.tsx` (new): `createFileRoute('/home')`, no loader, `head()` with the title, the description of 1.6 and `og:url` at `SITE.origin()` (the root route carries the icon set and the card; `HeadContent` keeps one `description` and one `og:title`); the component renders `<main class="ts-product" data-page="home">` with the nine bands, stamps `data-hydrated` from a mount effect once the handlers are attached, and carries the Speculation Rules script of 0.42 inline with the request's CSP nonce (`prerender` `/new` at `moderate`, `prefetch` `/decks` and `/deck/gt-brand`; `SPECULATION_RULES` exported). The two hero twins and the figure twin reach the sheet as custom properties set from `SITE.twins` on `main`, so no CSS names a file. `/home` stays out of `NOINDEX_ROUTES`.
- `apps/studio/src/routes/home.css` (new): one sheet for the page. Colours are `--pt-` tokens alone; sizes the `--ts-` tokens of `brand.css` (the sheet declares none, b1.md R7); every rule 1 px in one of the three roles, grids drawn as a 1 px gap over `--pt-hair-soft` inside one `--pt-hair` border; the hero band 640 px with the twin at `background-size: 1600px 900px`, `image-rendering: pixelated`, cropped, the plate 740 px lower left on `--ts-plate` with a `--pt-hair` edge; `html:has(.ts-product) { scroll-behavior: auto }`; the 1100 and 760 px blocks of P1 4.4 (two then one column; the band 520 px with the plate across the bottom; the four links and the lockup's word hidden under 760); the comparison table in its own `overflow-x: auto` box.

The components under `apps/studio/src/components/home/`:

- `HomeNav.tsx`: the 44 px bar (`--pt-title-h`, 2.2 item 1), the lockup (16 px solid mark, the word at 22 px) as one `Link` to `/home`, Your presentations (`/decks`, a `Link`), Documentation and GitHub (new tab), For agents (`#agents`), the appearance group (`role="group"`, Light and Dark as `aria-pressed` buttons writing `gt-theme` through `applyTheme`, the state from `useTheme` so hydration never mismatches), and the one solid New Presentation as a document navigation to `/new` (2.5).
- `HomeHero.tsx`: the still twin (decorative, `aria-hidden`) behind the plate; the `h1` lockup (`TurboslideMark size={48}` and the word at 66 px as live text), the sentence at `--ts-lead`, the three buttons of 0.22 (New Presentation `<a href="/new">`, Open the GT Deck a `Link` to `/deck/gt-brand` with `preload="intent"`, GitHub in a new tab), the fact line, and the credit line under the band on the page's ground.
- `HomeFacts.tsx`: the numbers strip, six cells, each a figure, a line and a source line in `--pt-mono` (0.20).
- `HomeEditor.tsx`: the editor pair (`01` in dark, `13` in light) above the fold, then the heading, the `06` shot and the three claims of R05 6.3.
- `HomeCards.tsx`: the twelve cards in four rows of three; the shell card pairs `02` with `05` and the viewer card `14` with `15` side by side (0.24), the Charts card carries `03` and `i-presentation-chart-bar` beside its title (0.19 and 0.24 read together), Text, Tables and Quality gates carry `i-bars-3-bottom-left`, `i-table-cells`, `i-check-badge` from the theme sprite through `SpriteIcon.tsx` (the symbol bodies parsed into `<path>` elements once at module load, no `innerHTML`).
- `HomePipeline.tsx`: the dithers band (the lead, the `12` shot with its caption, two claims), the pipeline strip (the continuous frame, the hero frame through the screen at 1:1 as a crop of the same twin the hero shows, the mark at 128 px), and the three specimens: `EmptyFigure` with one New Presentation action, the loading curtain (the figure twin where the sheet will be and the deck's ramp on a 2 px track through `rampEdgeMask` and `shiftOf` from `@turboslide/chrome/Progress`), and the presence chip rendered from `packages/identity` (`markSpec`, `renderMarkBits`, `inkRects` at 24 px for an anonymous principal, monochrome, the label from `labelFor`).
- `HomeSpeed.tsx`: the lead, eight rows (title, two sentences, the figure, the source line) and the closing sentence with its source line.
- `HomeAgents.tsx`: six rows with the command in a hairline box on `--pt-plate`, the bearer sentence and the `deck push` command; the OpenAPI and Skills rows link the committed files on GitHub.
- `HomeCompare.tsx`: the twelve rows with the affiliation sentence; `HomeFooter.tsx`: the four groups, the closing line, the small lockup back to the top.
- `HomeLink.tsx`: every link of the page (a router `Link` with `preload="intent"` for `/decks`, `/decks/trash`, `/home`, `/deck/$deckId` with `?present=1` as typed search; an anchor for `/new`, `#agents` and every external address in a new tab), every one with `tipProps` and a `data-control` id.
- `Shot.tsx`: an `<img>` over `shots.ts` with `src` (the smallest variant), `srcset` (every variant with its width descriptor), `sizes`, `width`, `height`, `decoding="async"`, `loading="lazy"` below the fold, `fetchPriority="high"` on the eager `01` (React writes a `<link rel="preload" as="image" imagesrcset>` for it in the head).
- `copy.ts`: every string (section 1.3 for the rules), `MEASURED` (the measured numbers with their document and date), `HOME_PROPER_NOUNS`, `DEFAULT_VIEW_EXEMPT`, `FORBIDDEN_WORDS`; `facts.ts`: `HomeFacts` and `HOME_FACTS` from `facts-data.ts`; `facts-data.ts`, `shots.ts` and `shots.json`: generated by the build below.
- Tests: `copy.test.ts` (the copy lints of `packages/theme/src/copy.ts`, the default view words over every rep facing band, the five words and the two phrases nowhere, sentence case headings without a period, Title Case buttons, a tooltip name and sentence per control, the counts as functions of the facts and never digits, a source line per figure and per row, the tense rule on the Rust and preload rows, the measured numbers against `verification-4/perf-budget-baseline-2026-09-14.json`), `facts.test.ts` (the copy equals `facts.json` by sha256 and by value), `shots.test.ts` (the sixteen pictures, three widths each, the files present, the module equals the JSON, the small variants' total under the image budget).

The build and the assets:

- `scripts/build-home-assets.ts` (new): copies the fifteen files of `docs/readme/` and the continuous liquid metal frame of the dither proposal (`design-4/dither/assets/hero-frame-continuous.jpg`, the pipeline's first cell) to `apps/studio/public/home/<name>-<hash>.jpg` under content hashed names (sha256 of the bytes, ten hex) with a 1440 px and a 720 px variant each (sharp, JPEG quality 82, mozjpeg), writes `shots.json` and its typed twin `shots.ts`, and copies the ten values the page needs out of `packages/theme/brand/facts.json` into `facts-data.ts` with the facts file's sha256. `--check` asserts every variant's bytes, sha256 and decoded size, the sources' sha256, that the folder holds nothing the manifest does not name, that `shots.ts` equals `shots.json` as data, and that `facts-data.ts` equals `facts.json` as it stands; exit 1 names the first path that differs. The generated modules are written through `node_modules/.bin/prettier --write` so step 19 reads them as any other file.
- `apps/studio/public/home/`: 48 files, 5,362,085 bytes (16 sources, each at its own width, 1440 and 720); the 720 variants total 434,223 bytes, the 1440 variants 1,314,258.
- `apps/studio/e2e/home-page.spec.ts` (new): nine rows (section 1.5).
- The six shots of item 6 under `docs/gslides-parity/build-4/b2-home/`: `home-<width>-<theme>.jpg` (the full page at 720 px wide, JPEG 72) and `home-<width>-<theme>-fold.png` (the first viewport at 1x), from the scratch script `shoot-home.mjs` on 4342 after a full scroll and every picture decoded.

### 1.2 The root class and the landmark, for the integrator (day 0 amendment 6)

- The page's root is `<main class="ts-product">`; `main` is the perf check's landmark and the page never uses `.ts-home-page`. Every class of the page is `ts-product-*`.
- `.ts-product` must join `TURBOSLIDE_CHROME.roots` in `packages/lint/src/chrome.ts` (else the `.ts-product-*` elements are not chrome and only the `.pt-ib` buttons are audited) and the default `readySelector` of `driveShell` in `packages/headless/src/shell.ts` (`.pt-viewer, .ts-studio, .viewer, .ts-home-page, .ts-trash-page`), else `turboslide lint --chrome --url .../home` waits 60 s for a shell that never comes and throws (section 1.5 records the exact text). The page stamps `data-hydrated`, which the driver's `hydrated()` accepts.
- The acceptance line of MILESTONES-4 B2 runs the lint without `--states`, which defaults to `list,grid,book`; the page has no shell states, so the line is `--states ''` as step 18 writes it for `/decks`.

### 1.3 The copy, as written

R05 section 6 rewritten from the ship tree, with these changes recorded:

1. The counts (0.25) are functions of `HomeFacts` and never digits in a string: the six strip figures (four counts, the mismatch percent and the licence from `facts.json`'s `export` and `licence` keys), the Layouts, Objects and Quality gates cards, the second dithers claim, the Contracts row, the agents lead, and the Automation, Layouts and Pictures rows of the comparison. `facts.json` as B1 wrote it carries `{ count, source }` per key and `{ pass, fail, skip, total }` for the audit; the page reads `count` and `total` (3,369 rows). The values today: 169 actions, 146 MCP tools, 153 HTTP paths, 21 layouts, 135 shape presets, 17 materials, 31 check steps, 3,369 audit rows, 0.003 percent, MIT.
2. The default view words (SPEC 12; `FORBIDDEN_DEFAULT_VIEW_WORDS`) are kept out of every string a rep reads. Rewritten for it: "with a light version that is the exact inverse" (not twin), "the GT colour presets" (not palette), "Every slide is checked for overflow, contrast, type sizes and copy" (not grammar rules), "pass before anything ships" (not run), "Free under the MIT licence" (not open source), "one immutable copy per saved version" (not revision), the footer's "The slide rules" for `docs/grammar.md` and "The Rust module" for `docs/native.md`, "The command line, the tools and the API" in the strip's first line (not MCP). Exempt with the reason in `DEFAULT_VIEW_EXEMPT`: the head's description (it is `SITE.description`, the README's first sentence, B1's), the speed band, the agents band, the Automation row and the footer's agents group (they name the transports and the files by name, as Extensions > Agent access does).
3. The five words of 0.26 appear nowhere; "Edge" the browser left the Offline row for it ("in Chrome or in a browser built on Chromium"). "An item Turboslide does not have is in its place" became "An item Turboslide lacks is in its place" because `CONTRAST_PAIR_PATTERN` reads the original as an "X, not Y" pair.
4. The tenses (PP section 4): the Rust row ends "The hosted studio runs the TypeScript stages today."; the preload row says a presentation opened from the list is loading before the click and that leaving the editor for the list is still a document load today; the Contracts row says the committed files fail the check when stale and does not say they are served from the bundle; the OpenAPI row links the committed file and says the hosted studio answers a stub until the generated files ship inside its bundle (R05 8.4; B4's contracts routes are in the tree today but not verified on a deployment).
5. The measured figures and their sources (`MEASURED`): the renders row cites `VERIFICATION-2.md` step 10 (2026-09-12) and `M1-STATUS.md` (2026-09-10); the Rust row `docs/native.md` costs (2026-09-10, Apple M5 Max); the export row `docs/pptx.md` and `docs/hosting.md` section 7 (2026-09-11); the batches row `docs/hosting.md` section 7 (188 to 203 s for the 85 slides as one call, two batches of 60 and 25); the compute row the deploy config; the preload row the verifier's baseline (`decks->edit` 598 ms for the GT deck in the perf-budget run, 544 ms for the one slide scratch deck in the harness, BASELINE.md 3.1, 2026-09-14), not R04's 399 and 723 or R03's 858; the immutable row BASELINE.md 2.2 and 3.6 (16 of 16 thumbnails `HIT`, the twins revalidated in one 300 byte answer); the closing sentence BASELINE.md 2.1, 3.2 and finding 1 (the list's first byte 0.29 to 0.45 s or 4.7 to 7.1 s, the first thumbnail 4.3 s and the later ones 0.13 s, `/new` ready 0.82 s cold, one slide at a time per instance). `copy.test.ts` reads `perf-budget-baseline-2026-09-14.json` and asserts the 598, the 823 and that every `/decks` cold sample sits in one of the two modes.
6. The two cards round three changed (2.2 item 5): The viewer and sharing now names the Share dialog with people by email and a role, Anyone with the link and Publish to the web; Home and files adds Version history by author and comments with an inbox. For B5 to confirm against the README.
7. The comparison's Account, Editing together, Import and Export rows are the ship tree's (optional accounts with a label such as Wax 613; several people edit at once with live carets, pointers, comments, a roster and version history by author; no PowerPoint import; no ODP or SVG). The rows where Turboslide has less stay (section 8 decision 13).
8. Headings are sentence case with `HOME_PROPER_NOUNS` beside `PROPER_NOUNS` (Google Slides, PowerPoint, Paper Shaders, Rust, Vercel, GitHub, Chromium and the rest) and carry no period; the buttons are Title Case; the fact line reads "No account needed to start. Free under the MIT licence, with the code on GitHub. Hosted at turboslide.vercel.app."

### 1.4 Readings and deviations, recorded

1. The `<picture>` pair (0.24, MILESTONES item 2): a `<picture>` element cannot follow `data-theme`, because its `media` queries reach `prefers-color-scheme`, which the product never consults. The editor pair is two `<img>` elements under one `figure.ts-product-picture`, shown and hidden by `:root[data-theme]` in `home.css` (the rules outrank the frame's `img` rule), the dark one eager and the light one lazy, so the hidden picture takes no box and is not fetched; the spec row asserts the swap.
2. `facts.json` in the page (0.25): B1's file carries 141 measured rows beside the counts, and an import of the whole file put about 120 KB into the page's JavaScript on the dev server; the build copies the ten values into `facts-data.ts` with the file's sha256, and both `--check` and `facts.test.ts` fail when the file has moved on, so `facts.json` stays the one source and the page carries ten values. The seam's `measured` rows are not read by the page: the speed rows quote their documents and dates in `MEASURED`, and `copy.test.ts` cross checks the verifier's JSON directly.
3. A 1440 px variant beside the 720 px one SPEC-4 2.4 names, so a full width picture at the rail's 1120 px picks a 40 to 145 KB file at 1x instead of the source's 100 to 330 KB; the source copy stays for a 2x display.
4. The pipeline's first cell is P1's 7.0 second frame (`design-4/dither/assets/hero-frame-continuous.jpg`, copied by the build under `/home/hero-frame-*.jpg`), and the second cell is B1's hero twin (another crop of the picture the hero shows), so the two cells are not the same frame until B1 writes the chosen frame's continuous picture (request R5); the captions say what each cell is.
5. The lockup's word hides under 760 px beside the four links (SPEC-4 2.2 item 1 names the links alone): the row with the word, the two appearance buttons and the solid button measures about 396 px against the 342 px a 390 px viewport leaves inside the gutters; the mark stays and the link keeps its name.
6. The navigation is 44 px (`--pt-title-h`, SPEC-4 2.2 item 1), not the mock's 56.
7. The strip's figure is `white-space: nowrap` at `letter-spacing: -0.03em` in cells with 16 px side padding, because "0.003 %" at 40 px wrapped in a sixth of the rail at 1440 (the first shots showed it).
8. The curtain specimen's 2 px track sits 12 px above the frame's bottom edge: at the edge the lint read the two lines as a double (gap 2), the one finding the first lint run had.
9. The Speculation Rules script is a React `<script type="speculationrules">` with string children and the request's nonce; the served HTML carries the JSON raw (React escapes only `</script`), which the spec asserts. On a same document navigation into `/home` React inserts the element already started, so the browser may not read the rules then; the page is entered as a document, and the rows of 0.42 are about that entry.
10. The tooltip audit's acceptance line names `--only /home`, a flag `scripts/tooltip-audit.mjs` does not have; the audit ran with `--url http://localhost:4342/home`, its form for one page (0 missing). Without `--url` the script walks its default pages and seeds `decks/tooltip-audit` in the checkout's `decks/` folder, which a tmp store server does not see; not run that way from this port.
11. The CSP report log on the dev server shows `script-src` `eval` reports for `/home` (46 in the session): Vite's dev client, not the page (the production build carries neither the client nor an eval); the verifier's run on the node-server build decides.
12. The module graph of `/home` on the dev server reaches `packages/schema/src/shapes/definitions.ts` (661 KB unbundled), `blocks.ts`, `text.ts` and `packages/render/src/dither-runtime.ts` through `@turboslide/viewer/theme` (`viewer/dither.ts` imports the render runtime), which the root route imports for `THEME_BOOT_SCRIPT` on every route; nothing of this page imports the editor, the materials, CodeMirror or the export packages (the spec's shader marker row and the graph smoke agree). The 600 KB row of 0.27 is the node-server build's number (step 31, the integrator) once B3's and B4's diet rows land; the page adds its own components (about 40 KB unbundled), `Tooltip`, `TurboslideMark`, `Progress`, `EmptyFigure` and the identity package's mark renderer.
13. `tsc -b apps/studio` fails today on B3's `apps/studio/src/routes/-access-page.tsx(4,31)` (`Cannot find module '@turboslide/chrome/YouNeedAccess'`, the chrome export awaits the integrator) and `apps/studio/src/server/decks.ts(685,7)` and `(787,30)` (`'cache-control' does not exist in type 'TypedHeaders<ResponseHeaderMap>'`, B3's or B4's in progress header); a `tsc --noEmit` over the project reports no error in a file of this builder.
14. B3's day 2 files already carry what this page needs from them: `vite.deploy.config.ts` prerenders `/home` when `src/routes/home.tsx` exists (`pages: [{ path: '/home' }]`, `prerender.enabled`, `autoStaticPathsDiscovery: false`, `crawlLinks: false`, `failOnError: true`), `'/home/**'` has the immutable header of 1.6, and `new.tsx` and `EditorRoot.tsx` gate the session on `prerenderingchange` (0.42). B1's `__root.tsx` already renders About Turboslide as a `Link to="/home"` (b1.md 1.2 item 8 closed).
15. `home.tsx` and the components import `@turboslide/viewer/theme` (`applyTheme`, `useTheme`), a module the root route already loads on every route, so the appearance group adds no chunk.

### 1.5 Commands run and their results

```
node scripts/build-home-assets.ts                 # 48 files, 5,362,085 bytes under apps/studio/public/home; shots.json, shots.ts and facts-data.ts written
node scripts/build-home-assets.ts --check         # "48 files of 16 pictures match shots.json, shots.ts and their sources; facts-data.ts matches packages/theme/brand/facts.json", exit 0
cd apps/studio && ../../node_modules/.bin/vitest run components/home   # 3 files, 26 tests passed (copy 20, facts 3, shots 4; the baseline JSON row ran, not skipped)
node_modules/.bin/tsc -p apps/studio/tsconfig.json --noEmit --composite false --incremental false   # 3 errors, none in a B2 file (1.4 item 13)
node_modules/.bin/tsc -b apps/studio               # exit 1 on B3's -access-page.tsx (1.4 item 13)
grep -nP '\x{2014}|!' apps/studio/src/components/home/copy.ts   # empty
node_modules/.bin/prettier --check <every B2 file>   # "All matched files use Prettier code style!"
```

The dev server, from `apps/studio`, stopped before returning: `TURBOSLIDE_STORE=tmp TURBOSLIDE_REALTIME=memory TURBOSLIDE_AUTH_DB=<root>/.turboslide/auth-b2-r4.sqlite TURBOSLIDE_MAIL=capture TURBOSLIDE_LOCAL_OPEN=1 TURBOSLIDE_AUTH_RATE_LIMIT=off TURBOSLIDE_SESSION_SECRET=<fake, 46 bytes> TURBOSLIDE_DOWNLOAD_SECRET=<fake, 42 bytes> ../../node_modules/.bin/vite dev --port 4342 --strictPort` (up in 511 ms; `/home` 200 with 144 KB of HTML in dev, the route tree regenerated with `/home`).

| Command                                                                                                                                                                                                                                                                                                   | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `PLAYWRIGHT_BASE_URL=http://localhost:4342 node_modules/.bin/playwright test apps/studio/e2e/home-page.spec.ts` under `.turboslide/e2e.lock`                                                                                                                                                              | 9 passed in 7.5 s, exit 0: the server's HTML carries `main` and the hero sentence (and `<title>Turboslide</title>`, `og:url` ending `/home`, no `noindex`, the rules script); the fifteen README pictures with `width`, `height`, a `720w` candidate, `sizes` and an alt sentence, `01` eager and the rest lazy; the editor pair follows `gt-theme` (the dark shot visible and the light one hidden, then the reverse after Light is pressed, `aria-pressed` on both buttons, `gt-theme` written, `theme-color` `#ffffff`, the hero twin's background reading `/brand/hero-light.png`); the three hero links resolve (`/new` and `/deck/gt-brand` answer 200, GitHub opens in a new tab); nothing wider than 390 at 390 with the four links hidden and the button and the group visible; six figures and eight rows with a source line each and the closing's; the rules script names `/new` at `moderate` and prefetches `/decks` and `/deck/gt-brand`, one script on the page; no script the page loads carries `ShaderMount` or `liquidMetalFragmentShader`; every link and button carries `data-tip`, the `h1` reads Turboslide with the mark, the credit line is under the band |
| the chrome lint through a scratch driver (`lint-home.mjs`: `launchBrowser`, `driveShell` with `readySelector: '.ts-product'`, `auditDocument` with `TURBOSLIDE_CHROME.roots` plus `.ts-product`, no state; the two lines of request R1 applied in the driver alone), 1440, 1280 and 390 in light and dark | 6 audits, 0 with findings, 0 broken: 176 segments at 1440 and 1280, 160 at 390, 0 doubles, 0 junctions, 0 colours outside the roles, 0 self stacks, 0 invisibles. The first run had one double (the curtain's track against its frame's edge, gap 2), fixed as 1.4 item 8 says                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `node scripts/tooltip-audit.mjs --base http://localhost:4342 --url http://localhost:4342/home`                                                                                                                                                                                                            | "0 interactive elements without a tooltip, 0 with a native title only; 0 missing, 0 title only, 0 pages not walked", exit 0                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| the shots (`shoot-home.mjs`, playwright-core over `@turboslide/headless/launch`, 1x, `reducedMotion: reduce`, a full scroll then `document.fonts.ready` and every laid out picture decoded)                                                                                                               | six views: 1440 and 1280 pages 11,036 px tall, 390 pages 19,741 px, horizontal overflow 0 at every width, `--pt-ink` `#f2f2f0` in dark and `#070707` in light, 15 pictures in the layout and 0 broken, `document.fonts.check('500 20px Inter')` true; files under `docs/gslides-parity/build-4/b2-home/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| the module graph smoke (`graph-home.mjs`, 1440, dev server)                                                                                                                                                                                                                                               | 245 script files, 12.4 MB of unbundled source (the dev server's number, not the measurement); no module of the materials, `MaterialMount`, Paper Shaders, CodeMirror, the editor or the export packages; images before the scroll 6 files and 94,940 bytes (`01` at 1440 40,468, the hero twin 13,679, the figure twin 13,873, `06` at 720 25,006, `icon.svg` twice), after a full scroll 19 files and 486,974 bytes, against the rows 500,000 and 2,000,000 of 4.7                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

Recorded after the chain of section 1.5 finished (the acceptance lint line as written, and the perf budget smoke on the dev server): see section 1.6.

### 1.6 The acceptance lint line as written, and the perf smoke

The acceptance line of MILESTONES-4 B2 as written, on one width and theme so the failure is on record (`node apps/cli/bin/turboslide.mjs lint --chrome --url http://localhost:4342/home --widths 1440 --themes light`): the command waits 60 s and dies with `turboslide: page.waitForSelector: Timeout 60000ms exceeded. Call log: waiting for locator('.pt-viewer, .ts-studio, .viewer, .ts-home-page, .ts-trash-page') to be visible`, the default `readySelector` of `packages/headless/src/shell.ts` `driveShell`, which no builder's row names; request R1 adds `.ts-product` to it and to the lint's roots, after which the scratch driver's run above is what the CLI will read (0 findings on six audits). The same line without `--states ''` would then report `list`, `grid` and `book` as unapplied on a page with no shell states (exit 2), so step 18's line carries `--states ''`.

The perf budget as a smoke on the dev server (`node scripts/perf-budget.mjs --base http://localhost:4342 --profile local --only routes --runs 1 --report --json <scratch>/perf-smoke.json`; the dev server measures nothing, AGENTS.md; the measurement is step 31 on the node-server build): the `/home` rows as the check reads them, cold then warm, one sample: TTFB 18 / 14 ms (budget 60 / 40), FCP 88 / 80, LCP 88 / 80 (400 / 200), ready (`main`) 54 / 38 (400 / 200), CLS 0.0000, longest animation frame 0 ms, images before ready 66 KB decoded (488 KB), images after a full scroll 92 KB (1,953 KB; the check's scroll outruns the lazy loads, so the graph smoke's 487 KB after a slow scroll is the larger and truer number), the LCP element `DIV.ts-product-hero-twin` accepted by the flag row ("the plate's text or the twin"), DOM nodes 3,899 (reported, no ceiling on `/home`; the 128 px mark's cell runs, the chip's cells and the twelve cards are most of it). The JavaScript rows fail on every route on the dev server by construction (8,288 KB decoded over the unbundled module graph, `react-dom_client.js` 3,059 KB as the largest file) and say nothing about the build; the 600 KB row of 0.27 gates once B3's and B4's diet rows have merged and the integrator runs step 31 (`--only routes`), whose `/home` number and chunk names are to be recorded here at merge 2.

### 1.7 Requests

R1 (the integrator, `packages/lint/src/chrome.ts` and `packages/headless/src/shell.ts`, merge 2, day 0 amendment 6): `.ts-product` joins `TURBOSLIDE_CHROME.roots` and the default `readySelector` of `driveShell` (`'.pt-viewer, .ts-studio, .viewer, .ts-home-page, .ts-trash-page, .ts-product'`); step 18's `/home` line runs with `--states ''` as `/decks` does, and the Not found page (`/no-such-page`) the same. Verified with the two lines applied in a scratch driver (section 1.5); without them the CLI waits 60 s and throws (section 1.6).

R2 (the integrator, `scripts/check.mjs`): step 26's list gains `apps/studio/e2e/home-page.spec.ts` (SPEC-4 6.1); `node scripts/build-home-assets.ts --check` joins the chain (beside step 29's `build-brand.ts --check`, or step 6's greps), so a stray or stale file under `apps/studio/public/home/` and a stale `facts-data.ts` fail `pnpm check`; the runner's server serves `/home` already (no environment change).

R3 (the integrator, `scripts/tooltip-audit.mjs`): `/home` in the default page list (MILESTONES-4 "Integrator"); `loadPage` may wait for `main[data-hydrated]` beside the shell marks so the page does not sit out the 60 s timeout; the acceptance line's `--only` reads `--url`.

R4 (the integrator, `scripts/hosted-smoke.mjs`): the `/home` row of 2.6: 200, `<main class="ts-product"`, the hero sentence (the apostrophe is `&#x27;` in the HTML), `<script type="speculationrules"`, and `x-vercel-cache: HIT` on the second request.

R5 (B1, `apps/studio/public/brand/` and `packages/theme/brand/site.ts`): the chosen hero frame's continuous picture before the screen (a JPEG at 1600 by 900, `hero-frame.jpg`, with its path on `SITE`), so the pipeline strip's first cell is the frame the second cell screens; until then the cell shows P1's 7.0 second frame from `design-4/dither/assets/`, copied by `build-home-assets.ts` (1.4 item 4). One line in `HomePipeline.tsx` and the build script switches the source.

R6 (B3, confirm): `new.tsx` and `EditorRoot.tsx` gate the session attach and the stream on `document.prerendering` and `prerenderingchange` (0.42), which the tree shows today; the `/new` opened from `/home` after a prerender is B3's `home.spec.ts` or the verifier's row (MILESTONES-4 3.10 names it beside `home-page.spec.ts`; this page's spec asserts the rules script).

R7 (B5, `README.md` "Why it is fast", `docs/performance.md`): the eight rows and the source lines are `SPEED` and `MEASURED` in `apps/studio/src/components/home/copy.ts` (SPEC-4 section 5: the same rows, the same source lines, the same tense rule); the two cards of 1.3 item 6 to confirm against the README's Feature tour.

R8 (the integrator, AGENTS.md at merge 2): the round four ownership line for B2 names `apps/studio/src/components/home/**` (the files of section 1.1 beside the nine components: `HomeLink.tsx`, `HomeEditor.tsx`, `Shot.tsx`, `SpriteIcon.tsx`, `facts.ts`, the generated `facts-data.ts`, `shots.ts`, `shots.json`, the three tests), `scripts/build-home-assets.ts` and `apps/studio/public/home/**` (generated, never edited by hand); and the seam note that `facts-data.ts` is a checked copy of `facts.json`.

### 1.8 Next

The fixer round in this builder's files: the OpenAPI and llms links flip to `/openapi.json` and `/llms.txt` once B4's contracts routes are verified on the preview (2.2 item 8); the preload row's second sentence and the closing sentence follow the verifier's merge 2 numbers (0.26: "replaced by the verifier's post round numbers when they exist"), and the Rust row moves to the present tense only if 0.38's addon ships and `hosted-smoke.mjs` reports `native`; the first pipeline cell switches to B1's continuous frame when R5 lands; `facts-data.ts` is rebuilt (`node scripts/build-home-assets.ts`) after any change to `facts.json`, which `--check` and `facts.test.ts` enforce.
