# Round five day 0 baseline, 2026-09-15

The verifier's record of round four's production deploy before any round five builder started (MILESTONES-5 "Verifier" item 1 and "The order", day 0; SPEC-5 16 is the acceptance this baseline is read against; the orchestrator's ruling 4 makes this file the reference every builder re-baselines a number against). Every number below was taken today, 2026-09-15, from Kevin's machine (Apple M5 Max, macOS 26.4 build 25E246, Node v24.13.0, playwright-core 1.62.1 driving Chrome for Testing 147.0.7727.15 from `chromium-1217` with `--use-gl=angle --use-angle=metal --ignore-gpu-blocklist`, headless, 1440 by 900, load average 3.0 to 4.0 at the start) over a connection `networkQuality -s` rated 846.9 Mbps down, 752.7 Mbps up, 15.5 ms idle latency, against `https://turboslide.vercel.app`. The raw numbers are in `baseline-2026-09-15.json` beside this file, assembled by `build-baseline-json-2026-09-15.mjs` from the five run outputs named in section 10; nothing in it was typed by hand.

Definitions are those of round four's `BASELINE.md` and the header of `scripts/perf-budget.mjs`: TTFB is `responseStart` of the document request from Playwright's request timing; "ready" is `window.turboslide.studio` plus `.pt-viewer[data-settled]` on the editor routes, the studio handle plus `.ts-presenter` on the presenter, `data-settled` on `/deck`, `data-hydrated` on `/decks`, `/decks/trash` and `/home`; cold is a fresh browser context, warm the same context's second load; medians of three with the samples in parentheses where they matter. DOM nodes are CDP `Nodes` after a garbage collection with the live element count beside them. No server was started on this machine for this baseline: every run went to production, the dev server measures nothing (AGENTS.md) and the rules forbid a build outside `scripts/check.mjs`.

## 1. What was measured against

| Target     | Facts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Production | `https://turboslide.vercel.app`, deployment `dpl_3CBG3zfZW7CfewaCaGa2JoWG2imN` (`turboslide-knoksgnju-kl01s-projects.vercel.app`, target production, Ready, created 2026-09-15 10:55:45 UTC, seven seconds after `d5a1be5` was committed at 10:55:38 UTC; read with `vercel inspect`, read only). `d5a1be5` is the round four ship commit named in the task ("Turboslide round five: the handoffs from the hotfix afternoon") and is `origin/main` and GitHub's `main`. The document shell of `/new` is 73,216 bytes and preloads `index-CYGrLim1.js` (the entry, 579,024 bytes decoded), `new-CsXFdKXe.js`, `Frame-Di5h-sQ0.js` and `EditorRoot-CaFtyhvz.js`. The functions are `__server`, `_serverFn/[...]`, `api/decks/[...]/bundle`, `api/decks/bundle` and `api/export/[...]` at 87.24 MB each in `iad1`; every function answer carried `x-vercel-id: sfo1::iad1::…`. The tiers are the ones the task states (`TURBOSLIDE_REALTIME=blob`, `TURBOSLIDE_AUTHORIZE=shadow`, `TURBOSLIDE_MAIL=off`, no `DATABASE_URL`, anonymous principals, one public Blob store, fluid compute); they were not re-read from the Vercel environment. The first request of the four minute old deployment (`/new` at 10:59:13 UTC) answered in 1,581 ms with `x-vercel-cache: MISS`, a cold function; the perf run below started three minutes later.                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| The tree   | `/Users/kevinliu/repos/Turboslide` at `d5a1be5`, clean apart from the untracked files the task names (`.github/`, the mockup PNGs under `design-4/*/previews/`, the hotfix 4 shots). `docs/gslides-parity/build-5/` did not exist when this baseline was taken, so the integrator's `BASE` and line citation map were not read; this lane needed neither. While the parity audit ran (`git status` at 11:32 UTC) the integrator's day 0 seam appeared in the working tree (`packages/schema/src/{assets,blocks,catalog,deck,position,reduce,render,typography,validate}.ts`, `blocks/{dither,table}.ts`, `packages/effects/src/dither.ts` modified; `blocks/{equation,media}.ts`, `fonts.ts`, `motion.ts`, `preferences.ts` and `validate/` new); by 11:50 UTC the seam had spread to `apps/cli/src/store-actions.ts` and a new `apps/cli/src/actions/`, `apps/studio/src/editor/controller.tsx`, `apps/studio/src/server/actions.ts`, the menu model, strings and keys, `packages/export/src/{check,scene/types,scene/extract,pptx/build}.ts` with the new `check/`, `ooxml/{math,media}.ts` and `scene/{equations,media}.ts`, `packages/render/src/{motion,theme-css}.ts`, `packages/realtime/src/{channel,protocol}.ts`, `packages/identity/src/principal.ts`, a new `packages/spelling/`, the generated contracts under `packages/agent/generated/` and the skills' references, `pnpm-lock.yaml`, `pnpm-workspace.yaml` and `tsconfig.json`; none of it touches production or the scripts this lane ran, and this lane edited nothing outside `verification-5/`. No build of `d5a1be5` exists on the tree and none was made: `apps/studio/.output` (2026-09-14 22:49) carries `index-zmcb2DMX.js`, a pre ship build, and `apps/studio/dist` (2026-09-11) holds no assets. |
| The lock   | `.turboslide/e2e.lock` was free at 11:02:02 UTC and was held by this lane's two chains (`run-baseline-chain-1-2026-09-15.sh` for the perf run and the layout shift audit, 11:02:02 to 11:17:09 UTC; `run-baseline-chain-2-2026-09-15.sh` for the parity audit, from 11:17:13 UTC), one browser page at a time, and released by each chain's `rmdir`. Ports 4321 and 4351 to 4357 had no listener before or after.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

Commands run, in order, from `/Users/kevinliu/repos/Turboslide`:

1. `node scripts/check.mjs --list > docs/gslides-parity/verification-5/check-list-2026-09-15.txt` (exit 0, 31 rows; section 3).
2. `networkQuality -s` (11:00 UTC; the header's figures).
3. `TURBOSLIDE_TOKEN=$(node -e "<read hosts.json>") node scripts/hosted-smoke.mjs --base https://turboslide.vercel.app --token-env TURBOSLIDE_TOKEN` (11:01:06 to 11:01:15 UTC; the bearer read from `~/.config/turboslide/hosts.json` in process and passed through the environment only; it appears nowhere in the output or in this record; section 2).
4. `node scripts/perf-budget.mjs --base https://turboslide.vercel.app --profile deployment --runs 3 --report --json docs/gslides-parity/verification-5/perf-budget-production-2026-09-15.json` (11:02:02 to 11:06:01 UTC; 114 of 151 asserted rows met; exit 0 under `--report`; no `--write`, so nothing was created on production; section 4).
5. `node scripts/layout-shift-audit.mjs --base https://turboslide.vercel.app --out docs/gslides-parity/verification-5/layout-shift-production-2026-09-15.json --report` (11:06:01 to 11:17:09 UTC, 667 s; the script accepts a base, so the audit ran against production and not against a node-server build; 109 cells, 71 failure entries in 56 cells, 8 skipped states; section 5).
6. `node scripts/gslides-parity-audit.mjs --base https://turboslide.vercel.app --out docs/gslides-parity/verification-5/parity-audit-production-2026-09-15.json --report` (from 11:17:13 UTC on the scratch deck `untitled-20260915-wes8`, created through `/new`, trashed by the audit's own cleanup and deleted forever by command 8; section 6).
7. `node docs/gslides-parity/verification-5/build-baseline-json-2026-09-15.mjs` (writes `baseline-2026-09-15.json` from the outputs of 1, 3, 4, 5, 6 and 8 and `production-facts-2026-09-15.json`).
8. `deck.info` then `deck.remove` for `untitled-20260915-wes8` over `POST /api/actions/<id>?deck=<id>` with the agent bearer from the hosts file, in process and never printed (11:49:09 UTC; `remove-scratch-deck-2026-09-15.txt`; section 7).

## 2. The hosted smoke

`hosted-smoke.mjs` against production at 11:01 UTC: 37 of 37 rows pass, 6 skipped by name, the same six as round four's production runs (`/s/<token>` exchange, 410 after unpublish, private document 403, twin URL derivability, restricted 404, template copy; each needs a token, a store or a flag this lane does not pass). The rows: `/` 307 to `/new` with `x-robots-tag: noindex` (85 ms); `/new` 200, 73,216 characters, 3 of 3 shell marks, noindex (213 ms); `/deck/gt-brand` 200, 180,467 characters, 0 `notes` keys (469 ms); `/edit/gt-brand` 200, 295,433 characters, 3 of 3 shell marks (681 ms); `/decks` 200 with 26 cards (307 ms); `/decks/trash` 200, 37,830 characters (354 ms); `/print/gt-brand` 200 with 85 pages (341 ms); `/present/gt-brand` 200, 294,225 characters (429 ms); `/home` 200, 95,423 characters, 3 of 3 marks, no noindex meta (17 ms); the cover twin 200 `image/png` 335,538 B; the headers of SPEC-3 8.8 on `/edit`, `/deck` and `/embed` (the embed naming the Prototemplate ancestors); the JSON asset as an attachment with `sandbox`; the cross site `text/plain` POST refused with 415; the unsigned thumbnail 302 to the stored object in shadow mode; the CSP report endpoint 204; the fifteen `cdn` rows `HIT` on the second request at the manifest's bytes (`/favicon.ico` 15,086 B, `/icon.svg` 957, `/apple-touch-icon.png` 753, `/manifest.webmanifest` 971, `/icons/icon-512.png` 460, `/og/turboslide.png` 29,818, the eight `/brand/` twins, `/home` 95,423 B `HIT` on both requests); the thumbnail without `r` a 302 with stamp `277d772f`, source blob; the thumbnail with `r` twice a 302 to the immutable Blob object (`MISS` on both, the redirect is not cached); `effects backend: typescript; glibc 2.34; node v24.19.0 linux`; `deck.info` 1 snapshot at revision 32. The output is `hosted-smoke-production-2026-09-15.txt`.

## 3. The check list

`node scripts/check.mjs --list` prints 31 rows (round four's count; SPEC-5 16.7 adds 32 to 36 this round). By need: 1 to 6, 9 to 11, 13 to 16, 19, 22, 24 and 28 need nothing; 7, 8 and 12 need the Prototemplate deck; 17, 18, 20, 21 and 26 need the server on 4321; 23 needs python; 25 needs docker; 27 needs the preview on 4344; 29 needs the brand assets; 30 needs the Vercel link; 31 needs the node-server build. Step 20 still writes to `docs/gslides-parity/verification-3/parity-audit.json` and step 27 to `verification-3/layout-shift.json`, the paths round three set; this lane passed `--out` on both scripts so neither record was touched. The list is `check-list-2026-09-15.txt`.

## 4. The perf budget run (deployment profile, medians of three, cold then warm)

`perf-budget.mjs --profile deployment --runs 3` at 11:02 to 11:06 UTC: 114 of 151 asserted rows met (round four's production run after the ship step read 119 of 151 on `2ee1db4` at 23:17 PDT on 2026-09-14; its pass 2 run 93 on `02e92c6`; round four's day 0 58 of 121 on `d5d7f07`). Against the ship run, row for row on the same names: 0 rows gained, 5 lost (all `/decks` and `/decks/trash`, section 8 finding 1), 146 kept their verdict; 44 values moved better, 78 worse. The full 190 rows are `perf-budget-production-2026-09-15.json` (`rows` with the verdicts, `routes` with the three samples per route and state, `transitions`, `filmstrip`, `idle`, `twins`, `cdn`, `vitals`); the printed table is the `.log` of the same name.

| Route               | TTFB                                                       | FCP         | LCP (element)                                                                                       | ready                                                      | JS decoded, files, wire    | largest chunk                 | longest frame | CLS             | DOM nodes (live)          |
| ------------------- | ---------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- | -------------------------- | ----------------------------- | ------------- | --------------- | ------------------------- |
| `/` (307 to `/new`) | 119 / 114 (cold 113, 230, 119)                             | 364 / 188   | 440 / 188 (`DIV.ts-curtain`)                                                                        | 487 / 264 (cold 773, 487, 423; warm 261, 264, 319)         | 2,235,742 B, 22, 673,838 B | `index-CYGrLim1.js` 579,024 B | 127 / 0       | 0.0000          | 590 (497)                 |
| `/home`             | 48 / 16                                                    | 164 / 84    | 164 / 84 (`DIV.ts-product-hero-twin`)                                                               | 110 / 39                                                   | 678,814 B, 6, 227,159 B    | the same                      | 76 / 0        | 0.0000          | 1,017 (783)               |
| `/new`              | 135 / 114                                                  | 236 / 188   | 268 / 188 (`DIV.ts-curtain`)                                                                        | 342 / 255 (cold 342, 477, 333)                             | 2,235,742 B, 22, 673,838 B | the same                      | 68 / 0        | 0.0000          | 590 (497)                 |
| `/decks`            | 456 / 2,601 (cold 2,604, 313, 456; warm 2,765, 230, 2,601) | 572 / 2,708 | 1,252 / 2,776 (`P.ts-recent-lead` cold, `IMG` warm; cold 2,728, 1,252, 680; warm 2,936, 368, 2,776) | 610 / 2,733 (cold 2,755, 549, 610; warm 2,895, 329, 2,733) | 2,152,912 B, 23, 645,810 B | the same                      | 87 / 68       | 0.0000          | 513 / 518 (364); 26 cards |
| `/decks/trash`      | 348 / 376 (cold 311, 348, 389; warm 376, 2,032, 368)       | 508 / 464   | 508 / 464 (`P`; warm 464, 2,120, 420)                                                               | 611 / 476 (cold 538, 611, 727; warm 476, 2,132, 439)       | 633,457 B, 7, 210,097 B    | the same                      | 161 / 53      | 0.0000          | 146 (114)                 |
| `/deck/gt-brand`    | 279 / 272 (cold 877, 252, 279)                             | 412 / 376   | 412 / 452 (`IMG`; cold 1,160, 380, 412; warm 468, 452, 352)                                         | 507 / 505 (cold 1,252, 499, 507; warm 526, 505, 418)       | 1,844,701 B, 20, 547,585 B | the same                      | 93 / 0        | 0.0021 / 0.0021 | 2,503 / 2,502 (1,602)     |
| `/edit/gt-brand`    | 582 / 264 (cold 582, 1,181, 319; warm 348, 264, 250)       | 676 / 332   | 988 / 596 (`IMG`; cold 988, 1,756, 756; warm 596, 480, 616)                                         | 935 / 570 (cold 935, 1,704, 704; warm 570, 455, 591)       | 2,238,488 B, 24, 675,775 B | the same                      | 92 / 90       | 0.0000          | 2,134 (1,529)             |
| `/present/gt-brand` | 296 / 367 (cold 374, 296, 292; warm 587, 300, 367)         | 412 / 420   | 412 / 420 (`DIV.ts-presenter-frame`; cold 564, 412, 392; warm 660, 396, 420)                        | 583 / 573 (cold 680, 583, 454; warm 835, 573, 490)         | 1,365,710 B, 11, 396,003 B | the same                      | 69 / 0        | 0.0000          | 368 (303)                 |

The pictures before ready: `/home` 40,468 B cold and 68,020 B warm with 486,017 B after the full scroll; `/deck/gt-brand` 516,149 B in both states; `/edit/gt-brand` 14,830 B cold and 530,022 B warm; the other routes 957 to 14,830 B. The ceilings of the deployment profile (`BUDGETS.deployment` in `scripts/perf-budget.mjs`): `/home` TTFB 150 / 100, LCP and ready 800 / 400, JS 600,000 B; `/` and `/new` TTFB 200 / 150, FCP 400 / 250 (`/new`), LCP and ready 700 / 400, JS 2,000,000 B; `/decks` and `/decks/trash` TTFB 400 / 300, LCP and ready 1,000 / 600, JS 600,000 B; `/deck` TTFB 500 / 400, LCP 600 / 400, ready 800 / 600, JS 1,000,000 B, nodes 1,500; `/edit` TTFB 200 / 150, FCP 400 / 250, LCP and ready 1,200 / 700, JS 2,000,000 B, nodes 1,500; `/present` TTFB 200 / 150, LCP and ready 800 / 500, JS 1,200,000 B; every route the largest chunk 600,000 B, the longest frame 150 ms, CLS 0.05.

The other checks:

| Row                                                                                | Value                                                                                                                                                     | Ceiling                        |
| ---------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ |
| transitions `decks->edit`; `edit->decks`; `trash->decks`; `back`                   | 94; 15; 6; 30 ms, all in page except `back` (wall), all ok                                                                                                | 500; 400; 400; 100             |
| transitions `home->new` (a document navigation after a 1,000 ms hover)             | 302 ms MISS, `activationStart` 0 ("no prerender activation")                                                                                              | 300                            |
| transitions `slideChange` (to `thesis`); `slideshow`; `layoutGrid` (painted frame) | 11; 20; 10 ms, all ok                                                                                                                                     | 50                             |
| filmstrip, three passes of 18 wheel steps over 85 cards                            | first pass longest 25.5 ms ok; steady p95 10.1 and 10.2 ms ok; steady longest 17.0 and 15.1 ms ok; 118.6 to 119.6 fps ok; 0 frames over 33 ms             | 100; 20; 50; floor 50          |
| filmstrip DOM nodes with 85 cards (after GC; live elements)                        | 2,694 (1,821 live) MISS                                                                                                                                   | 1,500                          |
| idle on `/edit/gt-brand`, 60 s after the settle                                    | 2 `/_serverFn/` calls per minute ok; 0 new stream connections (`/api/decks/<id>/stream`) ok; 12 other requests                                            | 4; 2                           |
| twins re-fetched on the second visit of `/deck/gt-brand`                           | 0 of 16 re-fetched, 0 revalidated, ok                                                                                                                     | 0                              |
| the icon set, the card and `/home` on the second request                           | `HIT` on every path (`/favicon.ico`, `/icon.svg`, `/apple-touch-icon.png`, `/manifest.webmanifest`, `/icons/icon-512.png`, `/og/turboslide.png`, `/home`) | `HIT`                          |
| field INP p75 from `/api/vitals`                                                   | not collected (the route is round five's; reserved, no ceiling)                                                                                           | none                           |
| the write path                                                                     | not run (no `--write` against production)                                                                                                                 | 450; 1,000; 50; 16; 500; 2,500 |

The 37 misses, in the classes round four's `VERIFICATION-4.md` section 5.5 named plus the `/decks` mode: the sixteen `js decoded` rows (every route, cold and warm: `/`, `/new` and `/edit` 2,183 to 2,186 KB against 1,953; `/decks` 2,102 against 586; `/deck` 1,801 against 977; `/present` 1,334 against 1,172; `/home` 663 and `/decks/trash` 619 against 586); the five DOM rows (`/deck` 2,503 / 2,502, `/edit` 2,134 / 2,134, the filmstrip 2,694 against 1,500); the `/decks` rows (cold TTFB 456 against 400, cold LCP 1,252 against 1,000, warm TTFB 2,601 against 300, warm LCP 2,776 against 600, warm ready 2,733 against 600); `/decks/trash` cold longest frame 161 against 150 and warm TTFB 376 against 300; `/deck/gt-brand` warm LCP 452 against 400; the `'data-only'` routes' first byte and paint (`/edit` cold TTFB 582 and FCP 676 against 200 and 400, warm 264 and 332 against 150 and 250; `/present` TTFB 296 / 367 against 200 / 150 and warm ready 573 against 500); and `home->new` 302 against 300.

## 5. The layout shift audit

`layout-shift-audit.mjs` against production at 11:06 to 11:17 UTC (667 s, Chrome for Testing 147.0.7727.15, ANGLE Metal): 109 cells, 71 failure entries in 56 cells, 57 cells at zero load entries, 8 skipped states, no cell errored. Round four's last run of the same script against a deployment (the pass 2 preview `ay9oweo3y` at `6679c54` plus the fix round, `verification-4/layout-shift-pass2/layout-shift-preview-2026-09-14.json`) read 66 failures on the same 109 cells and its committed `HEAD` record 67; against the preview run 8 cells carry one more load entry today (`edit 1280 light` 0 to 1, `deck 1440 light outline` 1 to 2, `deck 1440 light listClosedOutline` 1 to 2, `deck 1440 light gridSaved` 1 to 2, `deck 1280 dark gridSaved` 1 to 2, `decks 1280 light sortTitle` 0 to 1, `trash 1440 dark` 0 to 1, `deck 1440 light reduced` 1 to 2), 4 carry fewer (`deck 1280 dark outline` 2 to 1, `embed 1440 light` 2 to 1, `decks 1440 dark sortTitle` 1 to 0, `trash 390 light` 1 to 0) and 97 are the same. Round four recorded that single entry cells vary between runs of the same build (`VERIFICATION-4.md` step 27's row), so the eight gains are the wire's variance until a builder shows a source; none of them names a class the fix round did not know. Of the 71 entries, 63 are load entries, 7 are driven state entries (`edit 1440 light` panel and inbox, `decks 1440 light` filter with the footer among its sources, `present 1440 light` timerHour with the timer button among its sources) and 1 is the print route's anchors moving with scripts blocked (`.ts-print-pages` and the print stage's transform, cell `print 1440 light B`).

| Route                      | Cells | Cells failing | Cells at zero load entries | Load entries | The sources                                                                                                                                                                                |
| -------------------------- | ----- | ------------- | -------------------------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `/new`                     | 6     | 0             | 6                          | 0            |                                                                                                                                                                                            |
| `/edit/gt-brand`           | 16    | 7             | 10                         | 6            | the editor sheet stage (`div.pt-sheet-stage > div.sheet`), the skeleton's menubar titles, the title row's right group; the panel and inbox states                                          |
| `/deck/gt-brand`           | 30    | 29            | 1                          | 36           | the viewer toolbar's right group (`div.pt-toolbar > div.pt-bar-r`, with and without `is-tight-1` and `is-tight-2`), the previous button, the sidebar tree (`nav.pt-tree > section.pt-grp`) |
| `/embed/gt-brand`          | 7     | 6             | 1                          | 6            | the viewer toolbar's right group                                                                                                                                                           |
| `/deck/gt-brand?present=1` | 6     | 6             | 0                          | 12           | the present stage (`div.ts-stagewrap.ts-sheet.is-picture > div.pt-sheet-stage`) and the toolbar                                                                                            |
| `/decks`                   | 21    | 3             | 19                         | 2            | the recent tools row and a card (`sortTitle`, the `F` network variant); the filter state                                                                                                   |
| `/decks/trash`             | 6     | 1             | 5                          | 1            | the trash actions row (`trash 1440 dark`)                                                                                                                                                  |
| `/present/gt-brand`        | 8     | 3             | 7                          | 1            | the presenter counter under the `F` variant; the timerHour state                                                                                                                           |
| `/print/gt-brand`          | 9     | 2             | 8                          | 1            | the print bar's layout label under `F`; the anchors under `B`                                                                                                                              |

SPEC-5 16.5 asks the gate to stay at zero on `/edit`, `/deck`, `/decks`, `/decks/templates`, `/new`, `/present`, `/print`, `/help/training` and `/help/updates`. Today `/new` is at zero on all six cells and the other built routes carry the entries above, round three's recorded state (`VERIFICATION-3.md` finding 26, `VERIFICATION-4.md` finding 24); `/decks/templates`, `/help/training` and `/help/updates` answer 404 on production (section 7) and are not in the matrix. The eight skipped states are the same eight the round four preview run skipped, and seven of them are the audit script's own selectors and hooks rather than production facts (section 8 finding 3): `filmstrip` waits for `[data-menu-item="view.filmstrip"]` where `packages/chrome/src/menus/model.ts` names the row `view.showFilmstrip`; `write` invokes `slide.setNotes`, an id `packages/schema/src/actions.ts` does not define (the slide actions are `slide.update`, `slide.setLayout`, `slide.setBackground` and their siblings); `share` waits for `[data-control="title.share"]` where the title row draws `share.slot` and `share.open`; `follow` waits for `title.presence.more` where the slot draws `presence.more`; `join20` needs `window.__tsPresenceSimulate`, which no file in the tree defines; `printSkipped` clicks `[data-control="print.skipped"]`, a checkbox input the route draws but Playwright's click could not reach in 30 s; `signIn` waits for `[data-menu-item="title.account.signIn"]` for 30 s; `ditherDrag` reports `formatOptions.dither.black.slider` absent, an id the Dither section draws once a dithered picture is selected, so the state's setup did not reach it. The JSON is `layout-shift-production-2026-09-15.json` with the markdown summary beside it.

## 6. The parity audit

`gslides-parity-audit.mjs` against production at 11:17:13 to 11:45:28 UTC (1,695 s, remote mode, commit `d5a1be5` read from the tree, the scratch deck `untitled-20260915-wes8` created through `/new`, the name prompt answered 11 times): 2,574 pass, 22 fail, 445 skipped, 0 page errors; exit 0 under `--report`. Every phase ran to the end, including the object phases (`rows:shape`, `rows:image`, `rows:twoObjects`, the shape, line, image, chart and objects effects, the pickers, the tails) that round four's only deployment run of this script did not reach: the pass 2 preview run of 2026-09-14 (`verification-4/parity-audit-preview-ay9oweo3y-pass2-2026-09-14.json`) read 1,499 / 27 / 340 and stopped on a hover timeout, and the node-server record of the same evening read 2,979 / 6 / 384 (`parity-audit-node-server-pass2-2026-09-14.json`). The default view words phase is 42 of 42 on `/new`, `/edit` and `/decks` (the five words of SPEC-4 0.26 absent) and the tooltip audit in strict mode passes on production; both failed on the round four preview run. The JSON is `parity-audit-production-2026-09-15.json` (`rows` with the evidence per row, `perSection`, `totals` per menu and state); the printed tables are the `.log`.

Rows per menu on the scratch deck (`now` passing of `now` reachable, with the unreachable count; `later` and `omit` all passing): title 39 of 40 (2 unreachable), file 58 of 58, edit 23 of 23, view 78 of 78, insert 69 of 69 (8), format 126 of 130 (66), slide 25 of 25, arrange 17 of 17 (36), tools 37 of 37, extensions 5 of 5, help 9 of 9; the `later` rows 4, 10, 2, 8, 2, 4, 5 and the `omit` rows 6, 6, 2, 7, 8, 6, 4 per menu all present or absent as the model says. The same rows on the text block, shape, image and two object states pass in full (title 20 of 20, format 77 of 77 with 24 unreachable, arrange 24 or 25 of 24 or 25); on `/edit/gt-brand` read only 301 pass, 1 fail, 60 unreachable; `/decks` 1 of 1. Section totals (pass / fail / skip): firstOpen 12 / 0 / 0; defaultViewWords 42 / 0 / 0; rows:scratch 303 / 0 / 59; effects:scratch 137 / 4 / 123; effects:twoSlides 5 / 0 / 1; shortcuts:scratch 28 / 0 / 0; retiredKeys 14 / 0 / 0 on both decks; rows:textBlock 329 / 0 / 34; effects:textBlock 15 / 0 / 3; effects:textBlock2 16 / 1 / 2; pickers:objects 10 / 0 / 0; formatOptions:objects 7 / 0 / 0; rows:shape, rows:image and rows:twoObjects 328 / 0 / 34 each; contextMenu:objects 6 / 1 / 1; effects:shape 23 / 0 / 0; shortcuts:objects 11 / 0 / 0; effects:line 5 / 0 / 0; effects:image 3 / 0 / 0; effects:chart 2 / 0 / 0; effects:objects 5 / 1 / 0; versions 1 / 0 / 1; dither 6 / 0 / 0; roles:scratch 5 / 9 / 0; rows:scratch:viewer 257 / 5 / 59; tooltipAudit 1 / 0 / 0; rows:gt-brand 301 / 1 / 60. The 445 skips are the audit's unreachable rows by construction (224 under a Later or plate parent not drawn in the state, 59 under a Later, plate or per instance roster row, the shape and arrow tiles drawn as plate tiles, and the rows gated by a selection the state does not have).

The 22 failures, in four classes:

1. Fifteen rows are the deployment's shadow authorization (`TURBOSLIDE_AUTHORIZE=shadow`; round four's VERIFICATION-4 finding 23 read the same class on its preview): the nine `roles:scratch` rows (the viewer link landed on `/s/<token>` and the address kept the token; the viewer's `data-edit-mode` is `editing` and `access.generalAccess` reads `restricted` / `viewer` with no grant applied; no View only button; the Mode rows 0 with the Edit menu drawn; 10 handles after a click on a block for the viewer; the commenter link landed on `/s/<token>`; the editor did not settle for the commenter; after Stop sharing the general access link remains with 2 live links) and the five `rows:scratch:viewer` rows (`title.presence.me` and the four `format.chartType.*` rows not rendered for a viewer), plus `share.stop`. They are the tier's behaviour and stay until Kevin switches the authorization to enforce mode (SPEC-5 17 row 32); they are not read as code defects.
2. Three rows are the blob tier's per instance roster (hotfix 2's recorded deviation 2): `title.presence.me` in `effects:scratch` (the roster menu never drew the own row within 4 s), in `rows:scratch:viewer` and on `/edit/gt-brand` (no `data-menu-item` on its path).
3. Three rows are round four's recorded misses, failing on the node-server record too: `insert.comment` ("no comment card opened; snackbar \"\""), `format.dropShadow` on the second text block ("Format options opened; section shadow absent"), the guide context menu (`contextMenu:objects guide` waited 4 s for a visible menu) with `view.guides.delete` ("no Delete guide row in the guide menu").
4. Two rows are this run's own: `insert.chart.line` timed out after 8 s clicking the row inside the chart submenu (the locator resolved; the row passed in all four earlier records with "revision 34 -> 35; blocks 3 -> 4", so this is one occurrence on the deployment and is recorded as unverified until a second run), and `trash.card` ("/decks/trash lists no card for untitled-20260915-wes8"): the audit's cleanup moved the scratch deck to the trash through File > Move to trash, then the trash page it loaded listed no card for it (the blob tier's list lag, the same list as section 8 finding 1), so its Delete forever step did not run. After the audit the deck read `/deck` 404, `/edit` 200 and was on the trash page; section 8 finding 9 records what this lane did about it.

Compared with the round four preview run, seven of its failures do not fail today (`rows:scratch title.presence.me`, `file.download.html`, `file.download.zip`, the default view words row, `formatOptions:objects sections.text`, the `infrastructure objects` row that cut the run short, the tooltip audit) and three fail today that did not fail there (`insert.chart.line`, the guide menu pair, which the preview run never reached).

## 7. Production routes and the deck inventory

One `fetch` per path at 10:59 UTC before any browser run (`production-facts-2026-09-15.json`, `routeProbe`): `/` 307 to `/new` (15 bytes, answered at the edge); `/new` 200, 73,218 bytes (the cold function, 1,581 ms); `/home` 200, 95,423; `/decks` 200, 66,045; `/decks/trash` 200, 37,833; `/deck/gt-brand` 200, 180,469; `/edit/gt-brand` 200, 295,599; `/present/gt-brand` 200, 294,395; `/print/gt-brand` 200, 591,510; `/favicon.ico` 200, 15,086; `/openapi.json` 200, 2,072,829; `/llms.txt` 200, 6,417; `/api/agent` 401 (the bearer rule); `/og/turboslide.png` 200, 29,818; and the three round five routes `/help/training`, `/help/updates` and `/decks/templates` 404 with the 41,801 to 41,849 byte not found page. Every function answer was `x-vercel-cache: MISS` (the pages are `public, max-age=0, must-revalidate`); the headers of SPEC-3 8.8 are on `/new` (`nosniff`, `DENY`, `strict-origin-when-cross-origin`, the permissions policy, HSTS with preload, the nonce based report only CSP naming the public Blob host and `report-uri /api/x/csp/report`).

`/decks` lists 26 cards for the anonymous reader (`data-control="home.open.<id>"`): `gt-brand` and 25 scratch or fixture decks: nine `untitled-*` (`untitled-20260913-tii2`, `untitled-20260914-qbha`, `untitled-20260914-y0mk` and six from today before this lane ran: `untitled-20260915-vgz6`, `-tsv1`, `-1g7z`, `-fatr`, `-blw3`, `-0o93`, `-qfnh`, `-15hi`), seven `editor-depth-0912*`, four `fixture-*` (`fixture-p7-190558`, `fixture-p8-193001`, `fixture-pushed-181201`, `fixture-verifier-0912`), `gt-brand-mu05h5vq` and `bundle-round-trip`; the trash holds `untitled-20260915-7ki1`, `untitled-20260915-xmu5` and `untitled-20260914-ah39`. Round four's day 0 baseline (finding 11) named `untitled-20260913-tii2` and `untitled-20260914-ah39` as decks that were not the verifier's; both are still there. This lane created one deck, the parity audit's scratch deck `untitled-20260915-wes8` (title "Audit scratch", 3 slides, revision 258, 50 snapshots at the end): the audit moved it to the trash at 11:44:44 UTC and its Delete forever step did not run (section 6, class 4), so at 11:49 UTC this lane read it with `deck.info` and deleted it with `deck.remove` (`confirm: true`, `baseRevision` 258) over `/api/actions` with the agent bearer, the path round four's verifier used; afterwards `/deck` and `/edit` answer 404, the trash page no longer lists it and `/decks` shows the same 26 cards as at 11:03 UTC (`remove-scratch-deck-2026-09-15.txt`). Nothing else on production was written by this lane.

## 8. Findings for the round (owner in brackets; the task names the integrator for every production defect)

1. `/decks` is in its slow list mode again on production: the first byte read 2,604 and 2,765 and 2,601 ms in four of six samples and 230 to 456 ms in the other two, so the medians (456 cold, 2,601 warm) miss four rows the ship run met (TTFB 400 / 300, LCP 1,000 / 600, ready 600) and `/decks/trash` shows the same mode once (a 2,032 ms warm sample). Round four's day 0 finding 1 recorded the bimodal blob tier list (0.3 to 0.45 s or 4.7 to 7.1 s) and the ship run's nine samples happened to miss it; this run shows it at 2.6 s. The list is built from the manifests on the Blob store and now holds 26 cards (finding 2), so the two findings share a fix path. [integrator; the `/decks` rows of SPEC-4 4.1, B4's list in round four]
2. Production carries 25 scratch and fixture decks beside `gt-brand` on `/decks` and three in the trash (section 7): the `untitled-*` decks of the probes and audits of 2026-09-13 to 2026-09-15, the seven `editor-depth-*` and four `fixture-*` decks of the round four sessions, `gt-brand-mu05h5vq` and `bundle-round-trip`. Every reader of the product sees them on the home page, they weigh on the `/decks` list of finding 1, and the ship step's rule that a scratch deck is trashed and deleted forever was not followed by whichever runs made them. Removal is through the product's own paths (`deck.trash`, `deck.remove`) after Kevin confirms none is his; this lane removed only its own scratch deck (section 7). [integrator, with Kevin's confirmation; MILESTONES-5 "Integrator" owns the scratch deck cleanup]
3. `scripts/layout-shift-audit.mjs` skips eight driven states on a deployment because seven of its selectors and hooks no longer match the tree (`view.filmstrip`, `slide.setNotes`, `title.share`, `title.presence.more`, `window.__tsPresenceSimulate`, the hidden `print.skipped` input, `title.account.signIn`) and one state's setup does not reach the Dither slider (section 5); the same eight were skipped on round four's preview run. SPEC-5 16.5 adds about twenty rows to this script this round, so the stale eight are best fixed with them; until then the audit's "0 skipped" cannot be read on a deployment. [integrator; `scripts/layout-shift-audit.mjs` is the integrator's file in MILESTONES-5 "Owns"]
4. Eight layout shift cells carry one load entry more than the round four preview run and four carry one fewer on the same 109 cells (section 5); the sources are the ones round three and four recorded (the viewer toolbar's right group, the sheet stage, the sidebar tree, the recent tools row). The gate of SPEC-5 16.5 ("stays at zero" on the nine routes) is met today only on `/new`; the built routes stand at 71 entries, and the round has to decide whether the recorded state of VERIFICATION-3 finding 26 is the ceiling the gate holds or the number the fixer round closes. [integrator for the gate's reading; B4 and B1 for the viewer toolbar and stage sources if the round takes them]
5. The `'data-only'` routes' first byte stays over its ceiling on production, as round four recorded: `/edit/gt-brand` 582 / 264 ms against 200 / 150 with FCP 676 / 332 against 400 / 250 (one cold sample at 1,181 and 1,756 ms LCP), `/present/gt-brand` 296 / 367 against 200 / 150; `home->new` 302 against 300 with no prerender activation; `/deck/gt-brand` warm LCP 452 against 400 (52 ms over where the ship run was 8 over). These are round four's six classes (`VERIFICATION-4.md` 5.5), not new defects; they are the reference the round five budget rows of SPEC-5 16.6 are read against. [integrator; SPEC-5 16.6]
6. Every `js decoded` row is over its ceiling (16 rows, 619 KB to 2,186 KB against 586 to 1,953 KB) with the entry chunk `index-CYGrLim1.js` at 579,024 B under its 600,000 B ceiling on every route; the five DOM rows are over (2,134 to 2,694 against 1,500). SPEC-5 16.6 makes the round five features lazy chunks so `/edit` stays under 2 MB and the largest chunk under 600 KB; today's `/edit` is 2,238,488 B decoded in 24 files, so the round starts 238 KB over the `/edit` ceiling before adding anything. [integrator and every lane that adds client code; SPEC-5 16.6]
7. The perf JSON records the `/api/vitals` row as not collected: the route is round five's (SPEC-5 11 and 16.6) and the check reports it without a ceiling, so no INP number exists for the baseline. [integrator; `api/vitals.ts` and `vitals-report.mjs`]
8. The parity audit's fifteen shadow authorization rows and three per instance roster rows (section 6, classes 1 and 2) are the production tiers, not code: every round five reading of the audit on production carries them until Kevin switches `TURBOSLIDE_AUTHORIZE` to enforce mode and the realtime tier off the blob channel (SPEC-5 17 row 32). The round's parity gate (SPEC-5 16.1, exit 0) is therefore read on the node-server build and on a preview with the enforce tiers, and production is read as 22 minus 18. [integrator, for the reading; Kevin for the switch]
9. The parity audit's cleanup on a deployment can stop after the trash step: the trash page the audit loads right after File > Move to trash listed no card for the scratch deck (the blob tier's list lag), the `trash.card` row failed and Delete forever did not run, leaving `untitled-20260915-wes8` in production's trash. The cleanup should retry the trash listing or delete through the actions API when the card is not drawn. This lane removed the deck itself the way round four's verifier did (`deck.info`, then `deck.remove` over `/api/actions` with the agent bearer; section 7), so production's trash is as this lane found it. [verifier; `scripts/gslides-parity-audit.mjs` is the verifier's file]
10. `insert.chart.line` failed once on production with an 8 s click timeout inside the chart submenu (section 6, class 4) after passing in every earlier record; one occurrence, unverified, to be retried on the day 8 pass before anyone treats it as a defect. [verifier]
11. The three round five routes `/decks/templates`, `/help/training` and `/help/updates` answer 404 on production today (section 7), which is the expected day 0 state and the number a route row of SPEC-5 16.5 and 16.6 starts from. [B3 for the gallery page, B5 for the help routes]

## 9. Budget rows the day 0 run already meets

Production, deployment profile, 114 of 151: every `/home` row (TTFB 48 / 16, LCP 164 / 84, ready 110 / 39, the LCP element the hero twin, the pictures before ready and after the scroll, CLS 0) except its JS decoded; every `/` and `/new` row except JS decoded (TTFB 119 / 114 and 135 / 114, FCP 236 / 188 on `/new`, LCP 440 / 188 and 268 / 188, ready 487 / 264 and 342 / 255); `/decks` cold ready 610 and every `/decks` and `/decks/trash` row not named in section 4's misses (`/decks/trash` cold TTFB 348, LCP 508 / 464, ready 611 / 476); `/deck/gt-brand` TTFB 279 / 272, cold LCP 412, ready 507 / 505, CLS 0.0021, longest frame 93 / 0; `/edit/gt-brand` LCP 988 / 596 and ready 935 / 570 against 1,200 / 700, longest frame 92 / 90; `/present/gt-brand` LCP 412 / 420 and cold ready 583; the entry chunk 579,024 B on every route; every CLS (0.0000 to 0.0021 against 0.05); every longest animation frame except `/decks/trash` cold (161); the four in page transitions and the three painted frame transitions; the filmstrip's four frame rows; the idle rows; the twins row; the seven CDN rows. A ceiling the day 0 run already meets stays a ceiling (SPEC-4 section 4, carried by SPEC-5 16.6).

## 10. Files under `verification-5/`

| File                                                                | What                                                                                                             |
| ------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| `BASELINE.md`                                                       | this record                                                                                                      |
| `baseline-2026-09-15.json`                                          | every number above, assembled from the files below by `build-baseline-json-2026-09-15.mjs`                       |
| `build-baseline-json-2026-09-15.mjs`                                | the assembler (reads only)                                                                                       |
| `production-facts-2026-09-15.json`                                  | the deployment, the commit, the machine, the network, the route probe and the deck inventory of sections 1 and 7 |
| `check-list-2026-09-15.txt`                                         | `check.mjs --list`, 31 rows                                                                                      |
| `hosted-smoke-production-2026-09-15.txt`                            | the hosted smoke table, 37 of 37, with its start and finish stamps                                               |
| `perf-budget-production-2026-09-15.json` and `.log`                 | the perf run's raw numbers with three samples per route and state, and its printed table                         |
| `layout-shift-production-2026-09-15.json`, `.md`, `.log`            | the layout shift audit's cells, entries and skipped states, its markdown summary and its console output          |
| `parity-audit-production-2026-09-15.json` and `.log`                | the parity audit's rows, totals per menu and failures, and its console output                                    |
| `remove-scratch-deck-2026-09-15.txt`                                | the `deck.info`, `deck.remove` and probe lines of the scratch deck's removal (no credential)                     |
| `run-baseline-chain-1-2026-09-15.sh`, `-chain-2-`, and their `.log` | the two lock holding chains and their stamps                                                                     |
