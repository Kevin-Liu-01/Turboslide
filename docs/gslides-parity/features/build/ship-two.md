# The ship step, features round, ship two

The ship step of the features round's ship two (docs/FEATURES.md section 5 the shader library, 6 the lanes, 7 the acceptance; 7.2 the ship rule), 2026-09-25 from 19:45Z (times UTC). Its rules: git write commands on `features/ship-two` alone until the fast forward of `main` and the one push; no `pnpm install`, `pnpm add` or `pnpm exec`; no build outside `scripts/check.mjs`; `round-five/integration` never touched; the hotfix's files (`packages/store/src/pulse.ts`, `packages/realtime/src/blob.ts`, `room.ts`) never edited; no token, cookie or secret printed (the development OIDC credential of `.turboslide/vercel-dev.env`, valid to 2026-09-26 01:57Z, and the agent bearer of `~/.config/turboslide/hosts.json` travel in the environment of the probe commands through a scratch wrapper and appear in no file); nothing on Vercel or GitHub created, changed or deleted beyond the preview deployments this note names and the push of `main`; every scratch deck of this step's runs trashed and deleted forever. The sections: 1 the tree found, 2 the preconditions, 3 the fixes landed by this step, 4 the commits and the rebase, 5 the preview and the two runs of record, 6 the verdict and the parked list, 7 the ship, 8 production, 9 for Kevin.

## 1. The tree the ship step found

The branch `features/ship-two` at `3bd34f8` (thirteen commits over `2be923e`) with the fix round's working copies uncommitted, as the verifier's pass 2 left them (VERIFICATION.md "Features round, ship two, pass 2", F.2): B4's fixer's `apps/studio/e2e/core/lib.ts`, `export.spec.ts`, `shaders.spec.ts`, `scripts/probes/core-walk/areas/shaders.mjs`, `core-walk/index.mjs`, `core-walk/toolkit.mjs`, `docs/gslides-parity/focus/core-matrix.json` and the re rendered `docs/FOCUS.md`; B5's fixer's `packages/viewer/src/MaterialMount.tsx`, `packages/materials/src/controls.ts`, `packages/chrome/src/inspector/shader.tsx`, `shader.css`, `packages/chrome/src/__tests__/shader-section.test.tsx` and the new `packages/viewer/src/__tests__/material-mount-plan.test.ts`; B7's fixer's `apps/studio/vite.deploy.config.ts` with twenty evidence files under `build/b7/`; the four notes' fix round sections; the verifier's two pass sections in `VERIFICATION.md` with 136 pictures untracked under `focus/verification/`; `.github/` untracked as always (never committed). `origin/main` at `da00b5e`, eight commits over `2be923e` (the ship one hotfix's six, the chrome hairline `bb9c28a`, the gate's template rows `da00b5e`); production `dpl_4hTgBSidwHSte78caHrmsdsRYfdq` serving it. No lock folder, no process of the round alive; the vector round's pipeline running its own gates on 4429, on its preview and on production throughout this step (load averages 8 to 30).

The verifier's verdict of pass 2, the input of this step: the tree does not ship as it stands, and the distance is named (F.1, F.10 item 1): two requests of the fix round not landed (R1 of `b7.md` in `packages/materials/src/capture.ts`, R13 of `b5.md` in `packages/chrome/src/FormatOptions.tsx`), four driver contracts of B4's (the slider step's scroll, the preset step's tile, the card read of the auto capture row, the still read of the box aspect row), then the two runs on the rebased preview. Every reading in this note is from a command this step ran or from a run's JSON by row id.

## 2. The preconditions, checked by this step

On the tree of section 1 with the fixes of section 3 landed, before the first commit (19:50Z to 20:00Z), and again after the rebase (section 4):

- `node_modules/.bin/tsc -b` from the root: exit 0 (the e2e files included).
- `node_modules/.bin/vitest run`: `packages/materials` `capture.test.ts` 8 passed (the new case among them); `packages/chrome` 80 files, 750 passed, 2 todo; `apps/studio` `shader-frames.test.ts` and `agent-actions.test.ts` 17 passed, then the whole studio suite after the rebase 560 passed; the scripts project 12 files, 113 passed (twice).
- `node scripts/probes/core-matrix.mjs`: 814 rows validate (586 probe, 222 spec, 6 cost probe; 99 with parks); `node docs/readme/what-works.mjs --check`: README.md current, before and after the rebase.
- `node scripts/check.mjs --only 1,2,3,4` after the rebase: the frozen install, the route tree, `generate:contracts` 14 files current, `tsc -b`, all ok in 14.8 s; `--greps` clean (innerHTML 27 call sites in the allowlist, `overwrite: true` 26; the server bundle 14.5 MB, the client 10.2 MB).
- `git grep -n '<<<<<<<'` outside prose: nothing (one binary preview matches the bytes, not a marker).
- `prettier --check` over every file this step wrote and over the three files the rebase merged (`lib.ts`, `core-matrix.json`, `VERIFICATION.md`): clean.
- The standing reds of the check chain, recorded by the verifier's pass 1 F.2 as predating the round and not rerun here (the production table lists them with their counts): step 20 the parity audit (3,159 pass, 42 fail, 398 skipped), step 21 the older studio suites (18 failed, 14 did not run, 40 passed), step 26 the round three suites (5 failed, 2 skipped, 10 did not run, 37 passed), step 31 the perf budget (110 of 151), step 25 skipped (no Docker daemon), step 27 skipped (no production preview on 4344). Step 5 (the whole workspace in one vitest run) read green in pass 2 (395 files, 4,267 passed).
- The last two preview runs of record before this step are the verifier's pass 2 pair on `turboslide-bnqckjlrt` (the shaders area, the shaders, export and chrome specs and the cost probe, twice, no code change between them) and the integrator's full pair on `turboslide-2lbzoh8o7`; neither is the pair of record for this tree, since the fixes of section 3 change the code and the drivers, and 7.2's rule is a fix, then the preview twice again, then production. The two full runs on the rebased preview (section 5) are the runs of record of the whole matrix.
- The bursts with three companions: the verifier's pass 2 read three runs of 71 calls with 0 failed and no write over 5 s on its preview; this step reads the write path again through the new write probe and the hosted smoke on production (section 8).

## 3. The fixes landed by this step

Each is a request the verifier's pass 2 names as the distance, or a driver contract of its F.5 with the product green by hand; no product behaviour beyond the two requests changed.

1. R1 (`b7.md` section 3; pass 2 F.5 F1, severity 3). `packages/materials/src/capture.ts` `renderMaterialFrames`: the capture context's close in the inner `finally` runs only on a browser that is not the single-process serverless shell (`isSingleProcessBrowser` from `@turboslide/headless/launch`, the guard `openSheetPage` follows; the shell dies on a context close, docs/hosting-chromium.md 3b) and a close error after the frames are read is swallowed. `capture.test.ts` gains the case B7 named: a browser of the test's own flagged with `markSingleProcessBrowser` keeps its capture context open (one more context at the 1600 by 900 viewport) and still answers the 3200 by 1800 frame; an unflagged browser closes it. Without it every hosted render answered the one sentence after 8 to 13 s and `shaders.agent.list-insert-set-render` was red in both runs without `parks`, which 7.2 reads as parking the shader library whole.
2. R13 (`b5.md` section 5; pass 2 F.5 F2). `packages/chrome/src/FormatOptions.tsx`: the import of `isShaderGround`, the panel's `shader` filter (`!many && (a material || a shader ground)`) and the render (`ShaderSection` over the block or the covering picture, the generated rows for the block alone), the three hunks as written.
3. The walk's shaders area, `scripts/probes/core-walk/areas/shaders.mjs` (B4's file; pass 2 F.5 F3, F4, F5): the slider step scrolls the Amplitude control into view before it reads the thumb, the control-sentences step's rule, and presses Cmd+Z only when the release wrote (a failed drag no longer takes the insert and the rows after it, the cascade of pass 2 F.4); the preset step clicks the entry's Chrome tile first, then any tile that is not one of the two black and white kit presets, then any unpressed tile; the kit colours step clicks the Colors row's Primary swatch first, the write a seller makes before the kit's colour matters to the block (a block on a fixed preset takes no kit colour, so the brand write re keyed the frame and moved 2.5 in the run of record), and records the click and its revision.
4. The shaders spec, `apps/studio/e2e/core/shaders.spec.ts` (B4's file; pass 2 F.5 F6 and the box aspect flake): the auto capture row reads the filmstrip card as a screenshot clip of its 16:9 frame (`.ts-card-frame`) at the block's position on the slide, sampled against the frame file's centre over the same patch, instead of the card's first `<img>` (the clone's own frame picture through the assets route, whose redirect to the store's host an anonymous Image in the page cannot decode: 17 reads and no sample in both runs of pass 2); the box aspect row fetches the viewer's still through `fetchPageFile` and samples the bytes (the still read null in one run of two on the same address).

On the checkout's 4416 server (the memory tier, `TURBOSLIDE_STORE=tmp` under a private `TMPDIR`, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, the fixtures, two minted secrets, `vite.no-watch.config.ts`; started 19:57Z, stopped 20:06Z) the walk's shaders area under `.turboslide/e2e.lock` (taken 19:58:08Z with the `until mkdir` loop, released by the script's trap; the deck on the private tmp store) read 12 of 14 rows green: `shaders.panel.slider-live-undo` (Amplitude 3.2 to 5.6, 3.0 percent of pixels changed during the drag with the revision unchanged, one revision on release, Cmd+Z back to 3.2 with the canvas at its rest bytes), `shaders.panel.preset-tiles` (Diamond to Chrome), `shaders.panel.control-sentences` (11 of 11), `shaders.panel.one-home` (the ground's Shader section drawn through R13), `shaders.perf.one-context` and the seven rows the fix round already read green; `shaders.panel.kit-colours` red on the tier's own mechanism alone (the Primary swatch clicked, the block's mean colour moved 76.8, the frame kept its key after 7,166 ms: the client's frame does not land on the memory tier, pass 2 F.5 F8), and the two P1 rows not driven with their reason. The frame rows and the agent row are the preview's to read (the memory tier's frames, and the shell R1 guards, are not on a checkout).

## 4. The commits and the rebase

The fix round's working copies were committed on `features/ship-two` by explicit path lists, one commit per lane in the shape of the integrator's merge, then this step's own (20:00Z to 20:03Z; the hashes are those after the rebase):

| Commit    | What                                                                                                                                                                                                                                                                                                          |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `9b6a8b0` | B4's fix round: `lib.ts`, `export.spec.ts`, `shaders.spec.ts`, `core-walk/areas/shaders.mjs`, `index.mjs`, `toolkit.mjs`, `core-matrix.json`, `docs/FOCUS.md`, `b4.md` (B4's versions of the two driver files, reconstructed from this step's working copies by reversing this step's hunks, prettier clean). |
| `136d828` | B5's fix round: `MaterialMount.tsx`, `controls.ts`, `inspector/shader.tsx`, `shader.css`, `shader-section.test.tsx`, the new `material-mount-plan.test.ts`, `b5.md`.                                                                                                                                          |
| `638ee87` | B7's fix round: `vite.deploy.config.ts`, `b7.md`, the twenty evidence files under `build/b7/` (scanned for tokens first: none).                                                                                                                                                                               |
| `38298ab` | The integrator's fix round section of `integrator.md`.                                                                                                                                                                                                                                                        |
| `3b50043` | The verifier's two passes in `VERIFICATION.md` and the 136 pictures under `focus/verification/` (11 MB).                                                                                                                                                                                                      |
| `f0279e1` | This step's fixes of section 3: `capture.ts`, `capture.test.ts`, `FormatOptions.tsx`, `shaders.mjs`, `shaders.spec.ts`.                                                                                                                                                                                       |
| `573dfe0` | The parked list named after the fixes commit: `docs/gslides-parity/focus/ship-f0279e1.json`, the integrator's empty `ship-e2c6a7a.json` renamed (`git mv`) with its `commit` field and rule updated; `parkedFeatures` and `parkedRows` empty before the runs (7.2), the enforce preview built from this tree. |

The rebase: `git fetch origin` read `origin/main` at `da00b5e` (unchanged since the verifier's read); `git rebase origin/main features/ship-two` replayed the 21 commits over it with one conflict, `docs/gslides-parity/focus/VERIFICATION.md`, both sides appending at the end of the file (the hotfix's production table `88d8e21` and the verifier's two passes); resolved by keeping both in order, the hotfix's table then pass 1 and pass 2, no line of either changed, prettier clean; `lib.ts` and `core-matrix.json` merged clean as the integrator's three way read predicted. The branch is 21 commits over `da00b5e`, HEAD `573dfe0`; `.github/` stays untracked. The checks of section 2 ran again on the rebased tree.

## 5. The preview and the two runs of record

`https://turboslide-bxjtrzqfy-kl01s-projects.vercel.app`, deployed from the repository root at 20:03:56Z with the recorded command (`vercel deploy --yes --archive=tgz -e TURBOSLIDE_AUTHORIZE=enforce -e TURBOSLIDE_ASSIST=fixture -e TURBOSLIDE_LOGO_UPSTREAM=fixture -e TURBOSLIDE_MAIL=off -e TURBOSLIDE_REALTIME=blob -e TURBOSLIDE_PUBLIC_STORE_HOST=ggmycvj7j6224ay5.public.blob.vercel-storage.com -e TURBOSLIDE_SESSION_SECRET=<hex> -e TURBOSLIDE_DOWNLOAD_SECRET=<hex>`, the two secrets minted by `openssl rand -hex 32` into the command and masked from the log), Ready at 20:06:03Z (127 s), the tree `573dfe0`; never `--prod`, no project setting, no env command. Its first reads, before the runs:

- R1 on the deployment: `node docs/gslides-parity/features/build/b7/fix-round-http-render.mjs <preview> <out> gt-brand` with the OIDC header and the bearer at 20:06Z: `shader.render` 200 in 10,451 ms on the cold instance, a 3200 by 1800 PNG of 350,433 bytes, `renderer: chrome-headless-shell 147.0.7727.0, SwiftShader, Google`, `backend: swiftshader`, the render's own 6,956 ms; where every reading of the fix round and of pass 2 answered 500 `browserContext.close` after 9 to 13 s.
- `node scripts/hosted-smoke.mjs --base <preview> --token-env TURBOSLIDE_TOKEN` at 20:06Z: 40 of 40 (the pages, the headers, the enforce rows, the CDN rows, the effects backend, `deck.info` with 1 snapshot at revision 26).

The two runs, one after the other with no code change between them, from `gate-preview.sh` in the scratchpad (the tokens in the environment through `with-tokens.sh`, never printed): `node scripts/probes/core-gate.mjs --base <preview> --out <scratch>/preview-run<n> --parked docs/gslides-parity/focus/ship-f0279e1.json --matrix docs/gslides-parity/features/build/ship-two/preview-run<n>-core-gate.json`, run 1 started 20:06:58Z at load 12.8; the readings in section 6 once both end.

The two runs on `bxjtrzqfy`, read by id from the run JSON (`build/ship-two/preview-run<n>-core-gate.json`, the walk, specs and cost probe files beside them): run 1 from 20:06:58Z to 22:28:03Z (8,463 s; the machine's one minute load average 12.8 at the start, 110 to 132 from 21:55Z as another project's self test suite and two other checkouts' chains shared the machine, read with `uptime` at every poll), 814 rows, 751 passed, 28 failed, 35 not driven (1 manual), 0 no step, retries zero; run 2 from 22:28:03Z to 01:06:27Z (9,503 s; load 155 at the start, 367 at 22:37Z, 100 to 200 through the walk and the specs, 65 to 100 through the cost probe), 743 passed, 32 failed, 39 not driven, 0 no step, retries zero. Both runs exit 1 on the gate's own verdict; neither is a reading of the product alone, and section 6 says what each row's reading is.

## 6. The verdict on the two runs, the parked list and the confirming pair

What the two runs say by id, applying FEATURES.md 7.2 as written and naming the mechanism of every red from the run's JSON, the playwright error contexts and the gate log:

- Red in both runs with the product's own mechanism, one ship two row: `shaders.background.place-answers`. With R1 landed, Place now places the ground on the deployment (15,352 ms in run 1 and 13,506 ms in run 2, "Placing, 14 s" and "Placing, 11 s" read with the seconds, the covering picture `liquid-metal` landed), and the row asks 5 s or the failure sentence: the hosted capture is the function's Chromium (the inflate on a cold instance, the launch, a 3200 by 1800 SwiftShader render of about 7 s, section 5's first read). By the parks rule the row parks `dialog.background.shader`, the Background dialog's Shader row with its Dither toggle and Place. The dialog read no parked set before this step (`dialogs/Background.tsx` was not among the readers of `parked-controls.ts`, so the park would have hidden nothing, judge-design rejection 16): the dialog now reads `isParked('dialog.background.shader', settings)` and draws the whole Shader field only while the id is not parked or the switch is on; `parked-controls.test.ts` pins the surface (no Choose, Dither or Place with the switch off; drawn with it on) and `background-shader.test.tsx` drives its four cases with the switch on, the way a driver drives a parked row. A shader block's insert, section and frame, and `slide.setBackgroundMaterial` on every transport, are untouched.
- Red in both runs with a mechanism outside the product, the ship two row `shaders.frame.auto-capture`: run 1 `locator.count: Target page, context or browser has been closed` before the row's own reads (the runner's browser context gone, the machine at load 110 to 130; the same closure took `brand.export.pdf-logo`, `share.dialog.you-label`, `collab.presence-chips` and `assist.viewer.disabled` in run 1 and `brand.frame.toggles`, `brand.surfaces.viewer-and-show`, `export.download.progress-per-slide`, `versions.restore`, `comments.resolve` and `sync.title.concurrent-both-kept` in run 2, and left their scratch decks on the store, section 6.3); run 2 the frame 12,544 ms after the change against the row's 10 s at load 100, the client capture running in the runner's Chromium on this machine, every other clause green. The mechanical list would park `insert.shader` on it; the list's `notParked` names the row with this reason instead (the product's clauses green in the verifier's pass 2 F.5 F6 and in run 1 up to the closure), and the row is read again by the confirming pair (6.2).
- Red in both runs, the carried and the standing: the features `inbox` (both rows) and `templates` (four rows, the fifth not driven) as at ship one; the rows `view.live-pointers.second-browser` and `versions.show-changes-marks` as at ship one; Kevin's standing `sync.title.concurrent-both-kept` (run 1 the words, run 2 the closed context; never chased); `sync.resend.idempotent` (not driven in run 1 as at ship one, red in run 2 on the same missing origin). `brand.logo.replace-every-slide` in the recorded blob tier class (run 1 the editor load's 90 s, run 2 the footer's picture past 5 s under load). `comments.resolve` (run 1 the thread's resolved count read 0 under a 5 s bound; run 2 the closed context): an unparkable feature's row, both readings the runner's under the load, read again by the pair.
- Red in both runs by the shared store, `decks.file.make-a-copy`: both runs refused the copy with "decks/northwind-renewal-v4-without-notes exists already; pick another name", a live deck of that fixed name written at 20:55:14Z by another pipeline's run of the same spec file on the one Blob store the previews and production share (this step's specs began at 21:06Z) and never removed by it (left alone by this step, another pipeline's). The spec's own name for the copy now carries a four character tag of the run (`apps/studio/e2e/core/decks.spec.ts`, B4's file, a seam), so two pipelines' runs of the file cannot refuse each other; the row's measure (the copy opens in a new tab, has no notes, is listed) is unchanged.
- The cost rows, the store half over its ceiling in both runs while every function ceiling held: `cost.editor-idle.calls` function requests 9.99 and 9.65 a minute against 12; the store simple 48 against 40 in run 1 (the first sample alone, on the instance that opened the deck for the first time during the window: head 35, get 13, the two later samples on the other instance head 5 and 7), and advanced 12 against 11 in run 2 (one instance, put 9 in four samples and 12 in the last, del 2 to 3; simple 26). `cost.two-tabs-idle.calls` one `assets/` listing in both runs (the second instance's first open pull of the deck, the class the integrator's fix round named). The editing row held (59.28 and 71.25 against 75; simple 121 and 127, advanced 75), the show row 0 and 0, `sync.pull.no-listing` passed. Nothing this ship adds rides the idle path (the probe's deck holds no shader; docs/SYNC.md 6.1); the reading of record for the store half is the confirming pair's and production's (6.2, section 8).
- Red once, green in the other run, with the mechanism (the renderer's flaky list, never listed): in the shaders area under run 2's load, `shaders.panel.slider-live-undo` (the drag moved the value 3.2 to 5.6 with 3.0 percent of the box's pixels changed; the release's write not acknowledged within the step's 6 s, so nothing to undo: the step's new guard left the block for the rows after it), `shaders.panel.preset-tiles` (Chrome clicked, the canvas moved 151.8 with 95 percent of pixels changed, read at 785 ms against 500), `shaders.panel.kit-colours` (the Primary swatch, the block's mean colour moved 85.2, the frame re keyed 17,275 ms after the write against 5 s; 4,355 ms and 80.9 in run 1), `shaders.perf.one-context` (the swap 730 ms against 500; 429 ms in run 1), `shaders.frame.box-aspect` (the closed context in run 2; green in run 1), and after `auto-capture`'s red the serial shaders file did not run its later rows in run 2 (`reuse-and-prune`, `one-capturer`, `hidden-pauses`, `editor-frame`, `agent.list-insert-set-render`, all green in run 1), as the export file did not run its rows after `export.download.progress-per-slide`'s closed context (the four shader export rows green in run 1). Elsewhere: `decks.file.open-list-search` (Kevin's standing, 16.2 s in run 1), `decks.home.new-presentation` (the navigation to `/new` past 20 s, run 1), `images.insert.first-on-new-deck` (run 1) and `images.insert.upload-while-pending` (run 2), the recorded asset put timing class; `comments.on-object` (a stale baseRevision, run 1) and `comments.reply` (run 1); `logos.index.refresh-fixture` (run 1) and `logos.picker.search` (328 ms against 300, run 2; ship one's cold index class); `formatting.persistence` (the viewer's page not found under load, run 2); `sync.block.concurrent-same-offset-order` (run 2); `versions.restore`, `brand.frame.toggles`, `brand.surfaces.viewer-and-show`, `export.download.options-dialog` (`net::ERR_ABORTED` on the editor's address) and `export.download.progress-per-slide` (run 2, the closed contexts); `brand.background.enter-keeps-open` (Done's box not read in 30 s, run 2); thirteen brand rows not driven in run 1 alone (the walk's picture setup met `Vercel Blob: Failed to fetch blob: 403 Forbidden` on the just written asset, the store's edge window the hotfix worked around for the logo routes) and green in run 2.
- Not driven in both runs, as expected: the P1 rows not on the build (`shaders.background.add-to-theme`, `shaders.library.glyph-engines-render`, `shaders.frame.scrubber-capture`, `shaders.show.plays-when-on`, `shaders.show.frame-when-off`, ship one's eight, `brand.objects.kit-colours-first`, `fonts.picker.specimen-rows`, `fonts.table.takes-family`, `templates.deck.read-only`), `logos.index.cached-offline` (by design under the fixture upstream, its unit tests named), `assist.quota.429` (no Upstash), `cost.editor-hidden.calls` (headless Chromium reports no hidden page), the manual clipboard row.
- Green in the last run of each origin and in both: every other row, 29 ship two rows among them read green in at least one run (the gallery's stills, sentence, search and chips; the insert in the free rectangle with its words; the section's nine groups and eleven sentences; the slider, the preset tiles, the kit colours, the one home and the one context in run 1; the frame's reuse, the one capturer, the hidden tab's pause, the measurement row 125.0 ms on SwiftShader, `shader.render` through the HTTP transport, the four exports and the report row in run 1; View > Play shaders and the hover mount in both).

### 6.1 The parked list of record before the confirming pair

`docs/gslides-parity/focus/ship-f0279e1.json`, rendered by `build/ship/render-ship-json.mjs --commit f0279e1 --run preview-run1-core-gate.json --run preview-run2-core-gate.json --carry ship-f1afe1e.json --preview <bxjtrzqfy> --built-from 573dfe0` with the exceptions above as `--not-feature` and `--not-row` flags (written into the list's `notParked` with their reasons, never typed): `parkedFeatures` `inbox`, `templates` (carried); `parkedRows` the two carried rows and the eight P1 rows of ship one, and ship two's `shaders.background.place-answers` (`dialog.background.shader`), `shaders.background.add-to-theme` (`dialog.background.shader.addToTheme`), `shaders.library.glyph-engines-render` (`dialog.shader.engine.glyph`) and `shaders.frame.scrubber-capture` (`formatOptions.shader.frame.scrubber`, `formatOptions.shader.frame.capture`); `notParked` `brand`, `fonts`, `assist`, `logos.index.cached-offline`, `shaders.frame.auto-capture`, `shaders.show.plays-when-on`, `shaders.show.frame-when-off`; `leaves` empty. `node scripts/probes/core-matrix.mjs --emit-parked docs/gslides-parity/focus/ship-f0279e1.json` wrote nineteen ids into `packages/chrome/src/parked-controls.ts` (ship one's fourteen and `dialog.background.shader`, `dialog.background.shader.addToTheme`, `dialog.shader.engine.glyph`, `formatOptions.shader.frame.capture`, `formatOptions.shader.frame.scrubber`); `--check` reads it current; `parked-controls.test.ts` pins the nineteen and that `dialog.background.shader.place` is parked while `dialog.shader.tile.paper:liquid-metal`, `formatOptions.shader.amplitude` and `view.playShaders` are not. No `advanced` flag of `menus/model.ts` changes: the parks name no menu row or toolbar control that is not already flagged (the carried `file.versionHistory.showChanges` and the two Live pointers rows), and `view.playShaders` stays unflagged. `docs/FEATURES.md` section 8 carries the ship two paragraph.

### 6.2 Why a confirming pair, and what it is for

The pair on `bxjtrzqfy` ran with the empty list on a machine at a load average of 100 to 367 from other pipelines, and the readings of section 6 name that load as the mechanism of most reds: closed browser contexts, serial spec files stopping after one red, timing bounds of 500 ms, 5 s and 10 s missed by 230 ms to 12 s with every product clause green. Two seams landed after it (the Background dialog's parked read, the copy name's tag) and the parked set is on the build only from this commit, so FEATURES.md 7.2's rule holds as written: a fix landed, the preview runs twice again, then production runs once. The confirming pair is deployed from the tree of section 7 with the same command and read the same way; its two runs are the pair of record for the rows above, the list is re rendered from them if a row reads red in both with the product's mechanism, and its readings are what the release note and README carry.

## 7. The confirming preview and its pair

`https://turboslide-ffc7yeovv-kl01s-projects.vercel.app`, deployed from the repository root at 01:17:06Z on 2026-09-26 with the recorded command of section 5, Ready at 01:19:29Z (143 s), the tree `5f4c535` (section 6.1's commit: the list, the set, the two seams, the tests, FEATURES.md section 8, the ledgers and this note as far as section 6). Its first reads, before the pair: `shader.render` over `gt-brand` through the HTTP transport 200 in 16,827 ms on the cold instance with the 3200 by 1800 PNG of 350,433 bytes (R1 holds on this build too); the hosted smoke 40 of 40 at 01:20Z. The machine's load average had fallen to 30 (one minute) and 69 (fifteen minutes) when run 3 started at 01:20:32Z; the load at every poll is in the ledger lines of `gate-preview-pair2.log` and in 7.1. The two runs: `node scripts/probes/core-gate.mjs --base <ffc7yeovv> --out <scratch>/preview-run<n> --parked docs/gslides-parity/focus/ship-f0279e1.json --matrix docs/gslides-parity/features/build/ship-two/preview-run<n>-core-gate.json` for n 3 and 4, one after the other with no change between them, the tokens in the environment through the scratch wrapper.

### 7.1 The pair's readings

Run 3 from 01:20:32Z to 04:00:36Z (9,604 s; the load average 30 at the start, 90 to 140 from 01:50Z), 814 rows, 744 passed, 48 failed, 22 not driven (1 manual), 0 no step, retries zero, cost rows over their ceiling 0; run 4 from 04:00:36Z to 06:20:53Z (8,416 s; load 72 at the start, 14 to 65 from 04:20Z), 772 passed, 19 failed, 23 not driven, 0 no step, retries zero, cost rows over their ceiling 1 (`cost.two-tabs-idle.calls`, one `assets/` listing). By id from the two run JSONs (`preview-run3-core-gate.json`, `preview-run4-core-gate.json`):

- Red in both runs: the carried `inbox` (three rows), `templates` (three rows; `templates.save.as-template` red once), `view.live-pointers.second-browser`, `versions.show-changes-marks`; `logos.index.refresh-fixture` on the shared store's index (the search for `acme` answered no row, `TypeError` at `logos.spec.ts` 449: the fixture preview read the index every preview and production share on the one Blob store, ship one's rebuilt index of 4,945 brands without the fixture's ten marks; the ship one hotfix's preview run 1 read the same, its runs 2 and 3 green); the four ship two rows below, each with its numbers.
- The four ship two rows red in both runs, and the reruns (7.2): `shaders.panel.one-home` (red on its second clause alone: the Background dialog's options link cannot be read when the row that places the ground, `shaders.background.place-answers`, reads not driven behind the list with the Shader row hidden and the switch off); `shaders.panel.kit-colours` (the six swatches, the Primary swatch's write, the block's mean colour moved 78.2 and 76.8, the frame re keyed 14,718 and 18,075 ms after the kit write against 5 s: the capture runs in the runner's Chromium on a machine at load 57 to 140); `shaders.panel.preset-tiles` (Chrome clicked, the canvas moved 153.3 and 151.4 with 94 and 95 percent of its pixels changed, read at 5,714 and 861 ms against 500 by the step's screenshot loop under the same load); `shaders.perf.one-context` (one canvas on the stage in every state, the second's root one canvas, the first's none with its frame decoded, the swap 3,399 and 613 ms against 500).
- Red once: 21 formatting rows in run 3 alone, one mechanism (after `slide.new` and two `block.insert`s through the window API the tab's next write was refused, "2 changes were not applied. Another change landed first", the client resynced to a document without the slide, `RangeError: No slide "blank-2"`, and every row of the area that selects a box read no run on the stage: the blob tier's per instance read after a write, the recorded class of ship one's table (F9, C3-F5); the area green in run 4 and in runs 1 and 2); `decks.card.download` and `decks.file.open-upload-bundle` (run 3, the bundle ticket's server function under the load); `versions.restore` (run 3, 35 s), `sync.serial.order-and-latency` (run 3, one word 5,408 ms), `sync.block.concurrent-same-offset-order` (run 3), `sync.title.concurrent-both-kept` (run 3; Kevin's standing row, green in run 4), `chrome.split.hover-no-inversion` (run 3), `brand.colors.primary-live` (run 3), `assist.outside-write.snackbar` (run 3), `images.insert.first-on-new-deck` (run 3, the asset put class), `logos.insert.mono-tint` (run 3, the AWS tile under the cloud switch), `shaders.frame.box-aspect` (run 3: the capture after the resize answered a 3200 by 1812 frame for the 600 by 150 box, the earlier aspect's, green in run 4), `shaders.perf.editor-frame` (run 3, 163.1 ms against 150 on SwiftShader; run 4 52.0 ms, the number recorded), `shaders.frame.reuse-and-prune` (run 4, "the 1.2 frame" not within its bound; green in run 3), `share.dialog.co-edit-from-copied-link` (run 4, 5 s), `text.link.detect-url` (run 4: Cmd+Z after the detected link took the link and the last letters of the word with it, `generaltran`; green in run 3 and in runs 1 and 2; on Kevin's list), `surface.cleanup` (run 4: the walk's own deck `untitled-20260926-37w2` read `/edit` 200 and `/deck` 404 after Delete forever, the per instance read; removed by this step's sweep, 7.4), `templates.save.as-template` (run 4), `cost.two-tabs-idle.calls` (run 4, the second instance's first open listing).
- Not driven in both runs: the same set as runs 1 and 2 plus `shaders.background.place-answers` (behind the list: "not on this build: dialog.background.shader" with the switch off, the walk's step of that tree), and `sync.resend.idempotent` (red in run 3, not driven in run 4; the standing row).
- The cost rows: `cost.editor-idle.calls` passed in both (9.99 and 9.65 function requests a minute against 12; simple 39 and 32 against 40; advanced within 11); `cost.editor-editing.calls` 72.60 and 69.28 against 75 (simple 123 and 117, advanced within 85); `cost.two-tabs-idle.calls` `list` 0 then 1 (advanced 18 and 19 against 30); `cost.show.calls` 0 and 0; `sync.pull.no-listing` passed twice. The store half within its ceilings in every reading of the pair but the one listing.
- Green in the last run of the origin, 785 core rows among them, and the ship two rows `shaders.insert.gallery-thumbnails`, `selected-free-rectangle`, `words`, `panel.section-groups`, `panel.slider-live-undo`, `panel.control-sentences`, `frame.auto-capture` (green in both: the frame 1.3 to 2.6 s after the change, `backend: 'client'`, the id `frame-<16 hex of the key>`, 3200 by 1814, the card's clip within 64 of the frame's centre), `frame.one-capturer`, `perf.hidden-pauses`, `agent.list-insert-set-render` (`shader.render` 200 with the 3200 by 1800 PNG in both), the four exports and the report row (green in both), `view.play-setting` and `insert.gallery-hover-live`.

### 7.2 The reruns of the walk's shaders area

The walk's place step reached the parked Shader row with `t.notBuilt` in the pair; the ship step's seam in `scripts/probes/core-walk/areas/shaders.mjs` (B4's file, landed after run 4's walk had ended so the pair kept one driver tree) turns Tools > Advanced tools on when the row is absent and looks again, the toolkit's `reachRow` rule for a dialog control, and the area's end turns the switch off as before. `node scripts/probes/editor-walk-probe.mjs --core --base <ffc7yeovv> --only shaders --parked docs/gslides-parity/focus/ship-f0279e1.json --json build/ship-two/preview-walk-shaders-rerun<n>.json --matrix …-matrix.json` twice in a row on the same deployment, 06:21:13Z (198 s, load 13) and 06:24:27Z (210 s, load 21), the lowest load of this step: 11 and 12 of the 14 rows green. `shaders.panel.one-home` green in both (the Background dialog's link opened Format options at the Shader section for the covering picture; Pictures and materials without a Material section); `shaders.panel.kit-colours` green in both (the six swatches, the block's mean colour moved, the frame re keyed within 5 s); `shaders.panel.preset-tiles` red then green (Chrome, the canvas moved 151 with 93 to 95 percent of its pixels changed; the read past 500 ms in rerun 1, within it in rerun 2); `shaders.background.place-answers` red in both with the switch on, the ground placed in 12,553 and 22,334 ms ("Placing, 10 s", "Placing, 14 s" read with the seconds), the parked row's own reading; `shaders.perf.one-context` red in both on the swap's time alone, 515 and 635 ms against 500 with every plan clause green; the two P1 rows not driven; every other row green. Each rerun's deck trashed and removed by the walk (`/edit` and `/deck` 404).

### 7.3 The list of record

`docs/gslides-parity/focus/ship-f0279e1.json` re rendered from runs 3 and 4 (`--run preview-run3-core-gate.json --run preview-run4-core-gate.json --carry ship-f1afe1e.json --preview <ffc7yeovv> --built-from 5f4c535`): `parkedFeatures` and `parkedRows` as in 6.1, unchanged (the same fourteen rows; `--emit-parked --check` reads the nineteen ids current, so the build the pair drove is the build that ships); `notParked` rewritten from the pair with the reruns' readings: `brand`, `fonts`, `assist`, `logos.index.cached-offline`, `logos.index.refresh-fixture` (the shared store's index), the two show rows, and the four ship two rows above with their numbers: `shaders.panel.one-home`, `shaders.panel.kit-colours` and `shaders.panel.preset-tiles` (green in the last run of the walk's shaders area on this preview), and `shaders.perf.one-context` (red on 15 to 135 ms of a software renderer's shader compile in every reading but run 1's 429 ms and the integrator's 406 ms; the product's plan green everywhere; the first shader item of the round after, Kevin's list). `shaders.frame.auto-capture` leaves the exceptions: green in both runs of the pair. The renderer's own reading of the pair: flaky rows `decks.file.open-upload-bundle`, `logos.insert.mono-tint`, `shaders.frame.box-aspect`, `templates.save.as-template`, `shaders.frame.reuse-and-prune`; one blocking row red in every run, `sync.resend.idempotent` (the standing row of the sync round, no origin in the version list). `docs/FEATURES.md` section 8's ship two paragraph follows the list.

### 7.4 The store after the runs

Every scratch deck of this step's runs is trashed and deleted forever. After runs 1 and 2 the closed browser contexts had left 37 decks live on the shared store (the specs' fixture decks of 21:18Z to 00:32Z and the ids their failed teardowns name), each trashed and removed through the actions API with the bearer and the deck named by `?deck=` (`sweep-after-runs.log`: 37 of 37 answer 404 on `/deck`); the cost probe's twelve decks and the walks' two were torn down by their own steps. After the pair and the reruns: run 4's walk deck `untitled-20260926-37w2` (trashed by the walk, `/edit` 200 on another instance; 404 at the sweep) and the tagged copy `northwind-renewal-v4-without-notes-mb29` (live after the file's teardown; trashed and removed, 404); the specs' fixture decks of runs 3 and 4 were removed by their teardowns (no failed teardown in either run). Left alone: another pipeline's `northwind-renewal-v4-without-notes` of 20:55Z and the decks another pipeline's gate was writing at 06:07Z to 06:29Z while this step's sweep ran (the objects round's, on its preview `turboslide-dxutmn8x9`); the store per lane of the hotfix's item 2 is the cover.

## 8. The ship

`origin/main` had moved to `6c12d0c7` (the vector round, 18 commits) while the confirming pair ran. A rebase of the 22 round commits met conflicts in ten files of its first commit alone, so `origin/main` was merged into `features/ship-two` once (`276b803a`; sixteen files resolved as both rounds' additions, the merge commit's body names each; `tsc -b` exit 0, every package suite green, check steps 1 to 4 and the greps green, prettier clean, the contracts regenerated) and `main` fast forwards to the branch. The list of record was re rendered with `--carry ship-ea332aa.json` (the vector round's list, ship one's carried forward unchanged) and the set re emitted: unchanged, nineteen controls. The combined tree of the two rounds has no gate run of its own before production: the production run of record (section 9) is its first reading, as 7.2's third run.

The ship commit on top of the branch, "Turboslide features round, ship two: the shader library with Glyphfield's controls, the resting frame and its exports, with their rows in the core gate", carries this note through this section; then `git checkout main && git merge --ff-only features/ship-two && git push origin main`.

## 9. Production

`main` at `aa5dab0b` was pushed at 06:55:36Z on 2026-09-26; Vercel built it from the push (`dpl_GG5Vrw5gAosjCQPkypSnkysEiH7E`, `turboslide-i5x81rlhe`, created 06:55:41Z, Build Completed in 47 s, the build log's Cloning line naming `aa5dab0`), live on `https://turboslide.vercel.app` at 06:56:48Z. The readings, each in VERIFICATION.md "Features round, ship two, the production table": the hosted smoke 40 of 40 and the new write probe every step ok at 07:07Z; the production run of record 07:08:03Z to 09:52:57Z (9,894 s; the machine at a load average of 60 to 115 throughout), 856 rows, 798 passed, 32 failed, 26 not driven (1 manual), 0 no step, retries zero, cost rows over their ceiling 3 (the editing row's store half 156 against 140, the two tabs listing, the pull's words), the reds by mechanism in ST.3; the walk's chrome area rerun at 08:33Z with the driver seam for the vector round's icons row (`scripts/probes/core-walk/areas/chrome.mjs` names `insert.shader` where the vector round's list named `insert.material`; the row green); the walk's shaders area rerun at 08:43Z at a load of 18 to 30 (`shaders.perf.one-context` green at 431 ms, `preset-tiles` and `one-home` green, `kit-colours` red by 394 ms, the parked place row red with the switch on at 22.2 s); the svg, sync and images spec files' rerun at 09:54Z (36 rows, 29 passed: the five sync rows red in the run of record green, `images.insert.upload` red twice in the asset put class, the vector round's two svg rows red twice, `svg.sanitize.script-and-handlers` and `sync.structural.concurrent` red once); the twelve pictures at 1440 in both appearances under `focus/verification/production-features-two-*`, the Background dialog's Shader row shot with the switch on. The rows driven only under the fixture upstream are recorded not driven on production with their unit tests named; every scratch deck of the runs is trashed and deleted forever (ST.2, the store row; the sweeps' log in the scratchpad names each id with its 404).

The README's "What works today" is rendered from the matrix by `docs/readme/what-works.mjs` (its check passes; the production run's counts are in the table, never typed), its "Advanced tools" paragraph rewritten from the parked set and the production run, the release note "2026-09-26, the features round, ship two" in `docs/updates.md`, and this note's last sections, committed as "Turboslide features round, ship two: the production table" and pushed. The repository description is unchanged: FEATURES.md gives no sentence under 200 characters for it.

## 10. For Kevin

The list is ST.6 of the production table: the shader library in the default view with one control (Change background's Shader row) behind the switch and the way back; the swap's time and the kit write's re capture as the first shader items of the round after; `text.link.detect-url`'s undo grain; the gate machine's load from other pipelines through every run of this ship, with the reruns' readings beside the rows it touched; the vector round's two svg rows red on production for their owner; the open questions of FEATURES.md section 9 with the defaults used (question 4 the studio field then the house gradients behind the engine interface, P1 and not built; question 5 `motion.play: 'show'` with In the show only as the default and the 60 frame 2x cap, the show's live layer P1 and not built; the rest as ship one left them); the authorization switch, yours and unchanged (shadow); and what only you can do (the Play shaders default in the show once the layer lands, the manual row, the container verify's budget, the Upstash limiter, the version record's origin, a gate machine of its own, the sweep of other pipelines' decks on the shared store).
