# B1, the chrome and the pickers

## Features round, ship one

Builder B1 of the features round, ship one (docs/FEATURES.md 3.5, 4.3, 4.5, 4.6, 4.9, 4.12, 7.2 and 7.3; the lane table of section 6; the rows of 7.1), started 2026-09-22 on the shared checkout at `88b68e7` while the other lanes landed beside it. The dev server port is 4411 (`TURBOSLIDE_STORE=tmp`, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, two fake secrets of 40 characters, `vite.no-watch.config.ts`; stopped before the report). No git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran. No token, cookie or secret was printed. Times are PDT.

This file carries the requests (section 1, written first so the owners can act), the items landed with their files and hunks so a red row's item reverts alone in the fix round (section 2), the rows this lane covers and what happened to each (section 3), the commands run (section 4) and the decisions (section 5).

### 1. Requests

Each names the file, the owner and the one change, written so it can be applied as it stands.

R1 (the integrator, `packages/chrome/src/menus/model.ts`, the `INSERT` menu at 1334 to 1358): the two rows of 4.3 carrying the Logo dialog. `insert.logo` directly under the Image submenu row, in the default view (no `advanced`), with the finder `terms` so Search the menus "logo" answers Logo first and not "Long dash dot" (audit-logos row 12); `insert.image.logo` inside the Image submenu after Upload from computer. B6's R1 adds `format.image.replaceImage.logo` in `replaceImageItems(prefix)` the same way.

```ts
    /* the features round (docs/FEATURES.md 4.3; build/b1.md R1): Insert > Logo in the default view */
    now('insert.logo', 'Logo', dialog('Logo'), {
      turboslide: true,
      icon: 'building-office-2',
      doc: 'A company logo from thesvg.org, or your own',
      terms: 'logo, brand, company, mark',
    }),
```

and inside the Image submenu, after the Upload from computer row:

```ts
        now('insert.image.logo', 'Logo', dialog('Logo'), {
          turboslide: true,
          doc: 'A company logo from thesvg.org, or your own',
          terms: 'logo, brand, company, mark',
        }),
```

With the rows, the two cases of 7.3's `menu-model.test.ts` (B1's by name, the integrator's file): Insert lists Logo under Image in the default view (`isPresent` with `advancedTools: false`), `insert.image.logo` is in the Image submenu, and `finderRows(..., 'logo')` lists Logo first. The row `logos.insert.row` reads red until R1 lands.

R2 (the integrator, `packages/chrome/src/editor-shell.ts` and `EditorShell.tsx`): the dialog id. In `DIALOG_IDS` (959) `'logo'` after `'tailor'` with the comment `/* the features round (docs/FEATURES.md 4.3): the Logo picker */`; in `dialogIdOf` the case `'Logo'` answering `'logo'`. In `EditorShell.tsx`: `const LogoDialog = lazyDialog(() => import('./dialogs/Logo').then((m) => m.LogoDialog));` beside the other lazy dialogs and in `LAZY_DIALOGS`; in `runItem`'s `dialog` branch (1209 to 1222) the id `'logo'` joins `imageByUrl` and `fromThisPresentation` so a Replace image > Logo row passes the block as its target: `if (id === 'imageByUrl' || id === 'fromThisPresentation' || id === 'logo') { ... openDialog({ id, target: pictureTargetOf(item.id, current.slideId, current.selection?.blockId) }) }`; in `dialogNode` the case `case 'logo': return <LogoDialog.Component target={dialog.target} />;`. `LogoDialog({ target?: PictureTarget })` is the component's whole prop surface; a `target` of kind `block` swaps the asset and keeps the box, an `insert` target (the default) places a new picture.

R3 (the integrator, `packages/chrome/src/menus/strings.ts` and `panels/assist-strings.ts`): the words live in this lane's files until strings.ts takes them, each one exported constant and one import to move: `LOGO_DIALOG` in `dialogs/Logo.tsx` (the title, the search field, the groups, Insert and Replace with their docs, Upload, More, Include cloud service icons, the tile docs; the sentences a seller reads come from B6's `LOGO_WORDS` in `logo-model.ts` so the dialog and the server say one thing) and `TAILOR_LOGO` in `dialogs/Tailor.tsx` (Find the <To> logo, its doc, the stored sentence, Storing the logo). `TAILOR.logoFileDoc` in `panels/assist-strings.ts` reads "A PNG, JPEG, WebP or SVG under 20 MB" while the chooser lists the four raster types since this lane (4.5): drop "or SVG".

R4 (B4, the drivers of `logos.picker.*`, `logos.tailor.find-customer-logo` and `fonts.picker.*`): the ids and the readable state the dialog exposes, so a driver reads what happened and not a guess.

- The dialog is `dialog.logo` (`Dialog.tsx`'s card, so `dialog.logo.close` is the X); the field `dialog.logo.search` has the focus on open; the results draw `SEARCH_PAUSE_MS` (200 ms) after the last key plus the route's answer, so the row's 300 ms holds when the route answers within 100 ms.
- `dialog.logo.groups` carries `data-query` (the query the shown results answer) and `data-searching="true"` while a search is in flight: a driver waits for `data-query` to equal what it typed and `data-searching` to be absent, then reads the tiles.
- The groups are `dialog.logo.group.brand`, `dialog.logo.group.recent` and `dialog.logo.group.results`, each with `data-rows`; a tile is `dialog.logo.tile.<slug>` (`aria-selected="true"` and the class `is-active` on the preselected one; `data-slug`), a deck asset's tile `dialog.logo.tile.asset.<assetId>`, the default kit's `dialog.logo.tile.kit`; the halves `dialog.logo.tile.<slug>.paper` and `.ink` carry `data-variant` (`default`, `dark`, `light`, `mono`), `data-tinted="true"` for a mono tinted with the kit's text colour (a `span.ts-logo-mark.is-tinted` with the mark as a CSS mask, not an `<img>`), and `data-plate="true"` when the half draws the plate line ("Best on a dark slide" or "Best on a light slide"); the grounds are inline `background` colours from the kit.
- The licence row `dialog.logo.licence` carries `data-license` (the recorded string) and its tooltip text in `data-tip` ("Recorded on thesvg.org as CC0-1.0"); the link inside it is `dialog.logo.licence.link`. The source sentence is `dialog.logo.source` (B6's `LOGO_WORDS.source`, the failure clause "thesvg.org did not answer at 06:02 UTC" appended from the answer's `lastError`).
- The check is `dialog.logo.everySlide` with its sentence in `dialog.logo.everySlide.line`; it is drawn disabled while the editor handle has no `insertLogoAsset` (B6's R10; section 5), so `logos.insert.every-slide` is not driven until that method lands, never broken.
- The empty state is `dialog.logo.empty` with `dialog.logo.upload`; the cloud switch is `dialog.logo.includeCloud` under the More disclosure; a refused insert reads in `dialog.logo.error`.
- Recent is `localStorage` `turboslide.logos.recent` (twelve rows); the Font dropdown's Recent is `turboslide.fonts.recent` (five ids), the group `toolbar.font.group.recent` after `toolbar.font.group.used`, its foot `toolbar.font.clearRecent`.
- The Tailor slot: `dialog.tailor.logo.find` (a button, `data-slug`, `aria-pressed` once stored) with the pair `dialog.tailor.logo.find.pair` and its halves `dialog.tailor.logo.find.paper` and `.ink`, drawn 300 ms after the To field rests when `GET /api/logo/search?q=<to>&limit=5` answers a row `logoMatchFor` accepts; `dialog.tailor.logo.stored` once the click stored the mark.
- The apostrophes: the seller sentences come from B6's `LOGO_WORDS` and use the typographic apostrophe (U+2019: "Use as this presentation’s logo on every slide", "Your brand kit’s logo is under Your brand", "The brand’s own terms"), while the matrix rows quote the straight one. A driver that compares text should normalize the two, or B6 changes the words; this lane reads B6's so the dialog and the server never differ.

R5 (B6, `logo-model.ts`, `logos.ts`, `routes/api/logo.$.ts`, `Editor.tsx`): the contract this dialog reads, as b6.md R10 states it and as landed here. The search is `GET /api/logo/search?q=<query>&limit=<n>&collection=<brands|all>` from the page (same origin, `credentials: 'same-origin'`, `accept: application/json`, no bearer), answering the `logo.search` output as JSON (`{ logos: LogoSearchRow[], updatedAt, lastError?, progress?, source }`); a non 200 answer's `{ error: { message } }` is the sentence the dialog shows. The insert is `logo.insert` through the shell's dispatch when `isActionId('logo.insert')` is true at run time, else `POST /api/logo/insert?deck=<deckId>` with the action's input as the JSON body (`{ slug, variant?, slideId?, blockId?, everySlide?, baseRevision }`) answering `{ asset: { id, size? }, blockId? }`; `variant` is `chooseVariant(row, deckAppearance(deck)).variant`. After the answer the dialog calls `input.editor.insertLogoAsset(asset, { title, variant?, everySlide?, blockId? })` when the handle carries it (R10), else selects `answer.blockId` when the handler placed the block, else `insertPictureAsset(asset, { blockId? })`. The Tailor slot's default finder is `logoMatchFor(answer.logos, to)` over the same search route and `findCustomerLogo(row, appearance, insert, baseRevision)` over the same insert transport, exactly b6.md R7. b6.md section 1 R2 says the three ids landed in `packages/schema/src/actions.ts`; the tree did not carry them at 06:45 (`grep 'logo\.' packages/schema/src/actions.ts` is empty and the file is unmodified), which is why the dialog checks the id at run time and needs no change either way.

R6 (B2, `packages/chrome/src/font-picker-model.ts`): the one line of 3.1 item 2 landed here as `INTER_RELEASE_TAG = 'v4.1'` in `licenceUrlOf('inter')` (section 2.4), pinned in `__tests__/font-picker-model.test.ts`; your row `fonts.links.licence-v4-1` reads it. The eight `DIRECTORIES` entries for your ids (`ofl/geist`, `ofl/geistmono`, `ofl/instrumentsans`, `ofl/manrope`, `ofl/bricolagegrotesque`, `ofl/schibstedgrotesk`, `ofl/newsreader`, `ofl/fraunces`) landed too, since `Readonly<Record<FontId, string>>` stopped typechecking the moment `FONT_IDS` grew (06:31). The P1 specimen rows (3.5) wait for `assets/<id>/specimen.svg`; nothing of them is in this lane's files yet.

R7 (B3): nothing; `pickers/ColorPlate.tsx` is unchanged.

### 2. The items landed, their files and their hunks

Each item is one commit's worth; the hunks are named so a red row's item reverts alone.

#### 2.1 The parked controls module (FEATURES.md 7.2, 7.3; rows: every `parks` entry on a dialog control)

- `packages/chrome/src/parked-controls.ts` (new): `PARKED_CONTROLS: ReadonlySet<string>` between B4's two markers `/* parked-controls:begin */` and `/* parked-controls:end */` in the exact shape `core-matrix.mjs --emit-parked` writes (`renderParkedSet`: the one comment line and the `new Set<string>([...])` literal), empty now; `isParkedIn(id, set, settings)` (an id is parked when the set holds it or an ancestor of it by the dot, `handle.table.row` covering `handle.table.row.2`, `bar.table` covering `bar.table.<command>`, `formatOptions.shader` covering the section; false for every id while `settings.advancedTools === true`; `null` or `undefined` settings read as the switch off) and `isParked(id, settings)` over the committed set.
- `packages/chrome/src/parked-controls.test.ts` (new, the 7.3 test): the family rule, the sibling that shares a prefix, the switch, the empty set, the committed set read through `vi.importActual` (size 0); the Logo dialog's surface with the module mocked to park `dialog.logo.everySlide` and `dialog.logo.group.brand`: neither drawn with the switch off, both drawn with it on. Two `it.todo` name the surfaces of B3 (Overlay) and B5 (ship two), whose reads and tests are theirs.

#### 2.2 The Logo dialog (FEATURES.md 4.3, 4.6, 4.9, 4.12; rows `logos.picker.search`, `logos.picker.paper-and-ink`, `logos.picker.your-brand`, `logos.picker.empty-state`, `logos.picker.recents`, `logos.picker.licence-words`, `logos.picker.chrome-1280`; the dialog half of `logos.insert.every-slide` and `logos.replace-image.row`)

- `packages/chrome/src/dialogs/Logo.tsx` (new): `LogoDialog({ target? })` over B6's `logo-model.ts` (`chooseVariant` per half, paper as the light appearance and ink as the dark, `licenceSentenceOf`, `licenceTooltipOf`, `licenceLinkOf`, `logoMarkPath`, `kitBackgroundColour`, `kitTextColour`, `LOGO_WORDS`); the transport (`logoSearchUrl`, `searchLogos`, `insertLogo`, section 1 R5); Recent per browser (`readRecent`, `pushRecent`, `recentRowOf`, twelve rows under `turboslide.logos.recent`); the Your brand group (the default kit's logo as its picture pair or, when the kit names none, the GT mark glyph on both grounds; the deck's kit logo when it is a picture; every `role: 'logo'` asset); the results twenty at a time with an `IntersectionObserver` sentinel asking for twenty more up to fifty; the first result preselected in the same render as the results (`active`, derived, so a driver reads the ring and the tiles together), the arrows moving over brand, recent and results, Enter running the dialog's Insert (the card's default button), Escape closing; the check with its sentence, the licence row, the source sentence with the site linked to thesvg.org/legal, the More disclosure with Include cloud service icons; the empty state with Upload (the shell's `uploadPicture` with the target, then the dialog closes) and the kit's sentence when the query is the kit's name (the Your brand group scrolled into view); every `dialog.*` control behind `isParked`. The inserts: a mark of thesvg.org through `insertLogo` then `insertLogoAsset` (section 1 R5); a deck asset through `insertPictureAsset` (the logo size rule is the placement's, 4.4) or `block.set` for a Replace image target; the default kit's picture through `insertPictureFromUrl` of the appearance's file; the GT mark as a `mark` block of 132 by 84 through `insertBlockPlan`. Words: `LOGO_DIALOG` (section 1 R3). Ids: `LOGO_GROUP_CONTROLS` spells the three group ids as literals so `core-matrix.mjs`'s control source validation finds them.
- `packages/chrome/src/dialogs/Logo.css` (new): the 96 px tiles five wide in the 640 px card at 1440 and 1280 (`repeat(auto-fill, 96px)`, gap 8, the body 592 px), each tile two 60 px halves with inline grounds and the mark at up to 56 px (a tinted mono as a CSS mask filled with the kit's text colour; a plate half with the mark small and its line), the title in 12 px under a hairline; the groups scrolling under the field to 348 px with the foot fixed; the foot rows in 12.5 px ink-2 with underlined links; tokens only, radius 0, hairlines from `--pt-hair`, the selected tile in ink.
- `packages/chrome/src/__tests__/logo-dialog.test.tsx` (new): the transport's address and its failure sentences; Recent; the kit name match and the licence words; the dialog over a stubbed route answering four marks (Figma CC0 with guidelines, Vercel with the light and dark pair, GitHub whose default is unreadable on ink, AWS Lambda under CC BY-ND in the aws collection): the focused field, the check off with its sentence and disabled without the handle, the source foot, the results after the pause with Figma preselected, the halves' variants and grounds, the licence row's words and tooltip, the date in the foot, Enter inserting through the transport of the day (`isActionId('logo.insert')` read at test time) and the editor's `insertLogoAsset` with the title and the variant, the pick remembered; the Vercel pair (dark on paper, light on ink), the GitHub mono tinted `#f2f2f0` on ink, the AWS tile absent until Include cloud service icons and then on the plate line with "Free to use unchanged, the brand’s site"; the empty state, Upload and the kit's sentence; Your brand with the GT mark and the deck's logo asset placed through `insertPictureAsset`; the every slide check carried on the insert and to the placement; Recent listed after Your brand, the failure clause, the arrows, the Replace target's `blockId` and the Replace label; a refused insert as one sentence with the dialog open and nothing remembered.

#### 2.3 The Tailor slot (FEATURES.md 4.5; row `logos.tailor.find-customer-logo`, the slot half)

- `packages/chrome/src/dialogs/Tailor.tsx`: `TailorLogoFinder` (`match(name)`, `store(row)`) with `defaultLogoFinder(input)` over `searchLogos`, `logoMatchFor`, `findCustomerLogo` and `insertLogo` (b6.md R7); `TailorDialog({ logoFinder? })` builds the default from the shell's input, a test passes its own, `null` draws no button; the To field rests `FIND_LOGO_PAUSE_MS` (300 ms) before the match; the button `dialog.tailor.logo.find` beside the mark pair (`LogoMarkPair` of Logo.tsx) in the Logo section, `Storing the logo` while it stores, the stored sentence `dialog.tailor.logo.stored`; a stored mark turns Replace the pictures on and Apply names `{ assetId, replaceAlt: from }` in the one `deck.tailor`, the chooser's file path unchanged otherwise; a stored mark of another name is dropped when the To field changes. `PICTURE_TYPES` drops `image/svg+xml` (4.5). Words: `TAILOR_LOGO` (section 1 R3).
- `packages/chrome/src/Dialog.css`: one appended block, `.ts-tailor-find` (a two column grid, the button beside the 96 px pair, the stored sentence under both).
- `packages/chrome/src/__tests__/tailor-dialog.test.tsx`: the two existing cases pass `logoFinder={null}` (no route in jsdom) and the second reads the chooser's four types and no button; a new case passes a finder answering Figma: the button after the pause with the pair's halves, the click storing the mark, the stored sentence, Replace the pictures turned on and Apply's one `deck.tailor` with `logo: { assetId: 'figma', replaceAlt: 'product' }`.

#### 2.4 The Inter licence link (FEATURES.md 3.1 item 2; B2's row `fonts.links.licence-v4-1`)

- `packages/chrome/src/font-picker-model.ts`: `INTER_RELEASE_TAG = 'v4.1'` and `licenceUrlOf('inter')` reading `https://github.com/rsms/inter/blob/v4.1/LICENSE.txt`; the eight `DIRECTORIES` entries of 3.2 (the type needed them once B2's ids landed). Pinned in `__tests__/font-picker-model.test.ts`.

#### 2.5 P1: the font picker deltas (FEATURES.md 3.5; rows `fonts.picker.recent-group`, `fonts.picker.search-category`; audit-fonts 17's tooltip)

Separable from 2.4 by hunk: every line below is under a "features round" comment or in the new test file, so the item moves whole to the round after when the P1 condition fails.

- `packages/chrome/src/font-picker-model.ts`: `filterRows` matches the name, the category label and the id; `FontGroup.id` gains `'recent'` and `groupRows(rows, used, query, brand, recent)` lists the Recent group after Used and before the catalog; `FONTS_RECENT_STORAGE = 'turboslide.fonts.recent'`, `FONTS_RECENT_MAX = 5`, `readRecentFonts`, `pushRecentFont` (never the theme's face), `writeRecentFonts` (an empty list removes the key); the words `recent`, `clearRecent`, `clearRecentDoc`.
- `packages/chrome/src/FontPicker.tsx`: `FontList` takes `recent` and `onClearRecent` and draws Clear recent (`<control>.clearRecent`) at the Recent group's foot; `FontPickerPlate` reads the store on open and writes on every pick; the row tooltip names the face and its category (`Sans serif; Enter picks it`), the licence staying in More fonts.
- `packages/chrome/src/FontPicker.css`: `.ts-font-clear`.
- `packages/chrome/src/__tests__/font-picker-model.test.ts` (new): the licence link, the search over the three fields, the group order and its filtering, the store's five, the order, the theme face, the unknown id, the broken store and Clear recent.

Not landed: the specimen rows (`fonts.picker.specimen-rows`; they wait for B2's `specimen.svg` per family), the P1 Symbol and Wordmark and Color and Mono controls (4.11, `logos.picker.variants`; the appearance rule chooses), `Slideshow.tsx`'s Play shaders row (ship two), `Background.tsx` and `Download.tsx` (no request this ship).

### 3. The rows this lane covers, and what happened

Measured on this lane's dev server (4411, the tmp store) with `build/b1/b1-smoke.mjs` (playwright-core at 1440 by 900, at human speed, one scratch deck from `/new` trashed and removed at the end through the window API's `deck.trash` and `deck.remove`, a 404 on `/edit/<id>` confirmed, under `.turboslide/e2e.lock`; the last table is `build/b1/b1-smoke-run7.json`, 20 of 20 steps ok at 07:04), or read from the unit tests named in section 2. A row not driven is not driven.

| Row                               | What happened                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `logos.insert.row`                | Not driven: the two menu rows are the integrator's (R1) and were not on the tree when this lane returned (07:04); the smoke read `describe()` for `insert.logo` and recorded absent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `logos.picker.search`             | Not driven on a server: the dialog has no menu row and no dialog id until R1 and R2 land, so B4's `logos.mjs` reads it not built on `insert.logo`. The jsdom test drives the same gesture over a stubbed route: "figma" answers after the 200 ms pause with Figma preselected, Enter inserts and closes. B6's search route answered 200 on 4411 at 07:03 (the Tailor row below), so the dialog's search will answer the moment the row and the id land.                                                                                                                                                                                                            |
| `logos.picker.paper-and-ink`      | Not driven on a server (as above). The jsdom test reads the Vercel tile's halves as `dark` on paper and `light` on ink, and the GitHub ink half as the tinted mono; the inserted mark's lit fraction is the server's and the walk's.                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `logos.picker.your-brand`         | Not driven on a server. The jsdom test lists the default kit's mark (the GT glyph on both grounds on a kit without a picture) and a `role: 'logo'` asset first, and places the asset through `insertPictureAsset`; a tmp store with no deployment kit lists the deck's logo assets alone, as B4's row note says.                                                                                                                                                                                                                                                                                                                                                   |
| `logos.picker.empty-state`        | Not driven on a server. The jsdom test reads the empty sentence for "zzqx" with Upload running the shell's chooser, and the kit's sentence for "General Translation".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `logos.picker.recents`            | Not driven on a server (a `core/logos.spec.ts` row, B4's). The jsdom test writes Recent on an insert whose close unmounts the dialog and lists it after Your brand on the next open; a fresh `localStorage` lists none.                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `logos.picker.licence-words`      | Not driven on a server. The jsdom test reads "Figma: Free to use, brand guidelines" with the guidelines link and the tooltip "Recorded on thesvg.org as CC0-1.0", the foot with thesvg.org and "20 September 2026", and no AWS tile until Include cloud service icons is on.                                                                                                                                                                                                                                                                                                                                                                                       |
| `logos.picker.chrome-1280`        | Not driven (a `core/chrome.spec.ts` row, B4's). The grid is `repeat(auto-fill, 96px)` with an 8 px gap in the 640 px card's 592 px body, five tiles at both widths by arithmetic; the dark appearance keeps the halves' grounds because they are inline colours from the kit, not tokens.                                                                                                                                                                                                                                                                                                                                                                          |
| `logos.tailor.find-customer-logo` | The slot half, driven on 4411 (run 7): Tools > Tailor opened, "Figma" typed in To at 60 ms a key, and 1.2 s later the button `dialog.tailor.logo.find` was drawn with the pair, B6's `GET /api/logo/search?q=figma&limit=5` answering 200 on the same server; the click answered the dispatcher's sentence "logo.insert is declared in the action table and lands in P1" in `dialog.tailor.error`, because the window transport runs the three logo ids only once the integrator's one controller line lands (b6.md R3), so nothing was stored. The jsdom test drives the slot with a finder that knows Figma through to Apply's one `deck.tailor`.                |
| `logos.insert.every-slide`        | The dialog half only: the check, off by default, with its sentence; carried as `everySlide: true` on the insert and to `insertLogoAsset`, which B6 landed in `Editor.tsx` at 07:00 (the check is enabled on a handle that carries it and disabled otherwise; the jsdom test covers both).                                                                                                                                                                                                                                                                                                                                                                          |
| `logos.replace-image.row`         | The dialog half only: a `block` target passes `blockId` on the insert and to the placement and the button reads Replace (the jsdom test); the row is B6's (their R1) and the target's plumbing the integrator's (R2).                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `fonts.picker.search-category` P1 | Driven on 4411 (run 7): "mono" in the Font dropdown's search lists the Monospace group alone with Roboto Mono, JetBrains Mono, IBM Plex Mono, Fira Code and Geist Mono; "serif" lists the Serif and Sans serif groups and no Monospace.                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `fonts.picker.recent-group` P1    | Driven on 4411 (run 7): no Recent group before a pick; Roboto picked from Sans serif writes the family (the box computes `Roboto, "Helvetica Neue", Arial, sans-serif`) and `turboslide.fonts.recent` reads `["roboto"]`; the reopened dropdown lists Recent after Used and before Sans serif with Roboto; Clear recent removes the group and the store's key. Runs 2 to 4 read no Recent group after the pick: the store write sat inside a `setRecent` updater and the plate unmounted on the pick before React ran it (section 5); the fix is in 2.5 and the same pattern in the Logo dialog. A new browser context is the spec's (`core/brand.spec.ts`, B4's). |
| `fonts.picker.specimen-rows` P1   | Not landed, not driven (B2's `specimen.svg` files are not on the tree).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `fonts.links.licence-v4-1` (B2's) | The line is in this lane's file (2.4); B2 read the row green on 4412 with it (b2.md section 3) and measures it again after this lane merges.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

### 4. The commands run

- `node_modules/.bin/tsc -p packages/chrome/tsconfig.json --noEmit`: exit 0 at 07:00 on the shared tree with every lane's files of the moment. One reading at 06:42 showed `packages/chrome/src/__tests__/format-options-round-two.test.tsx(376,21): Cannot find name 'ContentSlide'` (B3's file, in progress) and was clean three minutes later; nothing of this lane's.
- `cd packages/chrome && ../../node_modules/.bin/vitest run`: 76 files, 711 tests passed, 2 todo, at 07:02 (the whole chrome suite on the shared tree, B6's `logo-model.test.ts` among them); this lane's four files (`parked-controls.test.ts`, `__tests__/logo-dialog.test.tsx`, `__tests__/tailor-dialog.test.tsx`, `__tests__/font-picker-model.test.ts`: 25 tests, 2 todo) pass alone as well.
- `node_modules/.bin/prettier --write` over this lane's files.
- The dev server on 4411 and `b1-smoke.mjs` under the lock, seven runs: run 1 (06:49) clicked the filmstrip clone of the text box and selected nothing (the locator now reads the stage's sheet, `.ts-stagewrap.ts-editor .pt-slide:not(.is-leaving)`), and its cleanup called `deck.trash` with the wrong input (now `{ id, baseRevision }` and `deck.remove { id, confirm: true, baseRevision }`, the probe's fallback); runs 2 and 3 (06:58, 07:00) read no Recent group after the pick (the updater bug of section 5, fixed at 06:59; run 3 still read the stale module because the no-watch server serves what it transformed first, and the server was restarted at 07:00); run 4 (07:00) passed every font step and timed out on `menu.tools` (the bar title is `menubar.tools`); run 5 (07:02) reached the Tailor dialog, drew the Find the Figma logo button with B6's route answering 200, and failed clicking the clipped check input (the label is what a person clicks); run 6 (07:03) passed 19 of 19; run 7 (07:04) added the Find click and passed 20 of 20, the table in `build/b1/b1-smoke-run7.json`. Every run's scratch deck answered 404 on `/edit/<id>` at the end. Console errors on the page were the dev server's `[Server]` log forwarding alone (the hosting banner lines); none from this lane's modules.

### 5. Decisions and notes

- The dialog reads B6's `logo-model.ts` for every rule two sides share (the appearance rule, the licence classes and sentences, the tint rule, the mark route, the source sentence) and adds no rule of its own; before B6's module landed (06:38) a first draft carried the sentences of 4.3 locally, and it was replaced whole once the module was on the tree, so one module decides for the server and the dialog.
- The search goes over the same origin GET route and not through the shell's dispatch (b6.md R10): the dispatch is the write queue (it waits on `idle()` and runs a server function), a read after every pause does not belong there, and the GET route is cacheable. The insert goes through the action when the schema lists it (true on the tree since 06:55) and through the POST route otherwise, decided at run time with `isActionId`; on the tree of 07:04 the window transport answers the action with "logo.insert is declared in the action table and lands in P1" until the integrator's controller line runs the three ids through `serverSide` (b6.md R3), and the dialog shows that sentence rather than guessing at the route.
- The every slide check is enabled when the editor handle carries `insertLogoAsset` (B6 landed it at 07:00) and drawn disabled with a sentence otherwise: the kit write must be in the insert's one commit (4.4) and only that placement makes it so.
- The default kit's logo tile inserts the theme's `mark` block when the kit names no picture (GT's deployment: `readDefaultKit` passes no `logo`), and the appearance's file of the picture pair through `insertPictureFromUrl` when it does, which stores one neutral twin; a kit source on `logo.insert` would keep the pair and is a request for the round after, not this ship's.
- The first result is preselected in the same render as the results (a derived `active`, not an effect), so a driver that waits for `data-query` reads the ring on the first tile at once; a hovered tile becomes the selection the way the Font list's rows do.
- The pause is 200 ms, not the 300 ms 4.3 names, because the row `logos.picker.search` measures "the results draw within 300 ms of the pause" and the route's answer takes part of that budget.
- A store write never sits inside a React state updater on a surface that closes on the pick: `setRecent((current) => push(current, ...))` queued on the Font plate and on the Logo dialog as they unmounted never ran, so nothing was remembered (runs 2 to 4 of the smoke). Both pickers compute the next list first and write it, and the Logo dialog's test closes by unmounting, the way the shell's `closeDialog` does.
- B2's R4: `licenceUrlOf('inter')` keeps its local `INTER_RELEASE_TAG` rather than importing B2's `INTER_LICENCE_URL` from `@turboslide/fonts/names`, because the chrome depends on the renderer and the schema and not on the fonts package (the module's header says so); the two strings are equal and B2's `inter.test.ts` pins theirs. The integrator may route the import through `@turboslide/render/fonts` if it re exports the constant.
- The words: the seller sentences are B6's `LOGO_WORDS` with the typographic apostrophe; the rows quote the straight one (R4).
- P1 landed in this lane: the font picker's Recent, the search over the category and the id, and the row tooltip (2.5), each a separable hunk; the specimen rows and the picker's variant controls did not.
- B2's R5 (`fonts.table.takes-family`, P1): `takesFamily` still answers false for a table, because the table block's schema carries no `typography` and `renderTable` writes none (the integrator's and B3's hunks in that request); the one line here, `takesFamily` returning true for a table, lands with them and not before, so the dropdown never writes a field the schema refuses.

## Features round, ship one, fix round

The B1 fixer of the features round, ship one (2026-09-22, late evening PDT, into 2026-09-23 UTC; the branch `features/ship-one` at `57da794`; the verifier's pass 1 in `docs/gslides-parity/focus/VERIFICATION.md` "Features round, ship one, pass 1", findings F7 and F8, both severity 2). The dev server ran on 4411 (`TURBOSLIDE_STORE=tmp`, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, two fake secrets of 40 characters, `vite.no-watch.config.ts`; stopped before the report). No git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran; no token, cookie or secret was printed; nothing on Vercel or GitHub was created, changed or deleted; the two scratch decks of the drive and the walk's one answered 404 at the end. Times below are UTC.

### 1. Requests

R6 (the integrator, `packages/chrome/src/useEditorKeys.ts`, `isChromeField` at 47 to 53; the file is in no lane's list of FEATURES.md section 6, so the integrator's): F7, Cmd+Z with the focus on a Format options control does nothing. The verifier named `isChromeControlTarget` (`packages/viewer/src/Editor.tsx`, B3's `CHROME_CONTROL`), but that gate belongs to the stage's own key handler, which never owned `edit.undo`: it returns on `fromControl` without stopping the event, and the document key table runs after it (`keys.ts` `editorKeyAction` names no undo). The one owner of the undo and redo chords is `useEditorKeys`, and its field gate is the mechanism: `isChromeField` answers true for every `INPUT` whatever its `type` (line 49), so the Tabular figures checkbox (`inspector/typography.tsx` 195 to 210, a native `<input type="checkbox">`) counts as a text field and the early return at 197, `if (inField && !(event.altKey && event.ctrlKey) && !(event.metaKey && event.shiftKey)) return;`, swallows Cmd+Z before the key table sees it; Cmd+Shift+Z passes that same clause, so redo works from the checkbox and undo does not. Read first hand on 4411 (section 3, rows 9 to 12 of `b1/b1-fix-drive-run.json`): the row toggled on with the focus on the checkbox, Cmd+Z left `font-variant-numeric: tabular-nums` computed on the block, one click on the block then Cmd+Z computed `normal`, and with the focus put back on the checkbox Cmd+Shift+Z computed `tabular-nums` again. The change, one hunk, so a control that takes no text lets the chords through while a text field keeps the browser's own editing keys:

```ts
/** The input types that take text; a checkbox, a radio, a range or a colour has no editing keys of its own. */
const TEXT_INPUT_TYPES: ReadonlySet<string> = new Set([
  'text',
  'search',
  'url',
  'tel',
  'email',
  'password',
  'number',
  'date',
  'datetime-local',
  'month',
  'time',
  'week',
]);

function isChromeField(target: EventTarget | null): boolean {
  if (!(target instanceof HTMLElement)) return false;
  if (target instanceof HTMLInputElement) return TEXT_INPUT_TYPES.has(target.type);
  if (target.tagName === 'TEXTAREA' || target.tagName === 'SELECT') return true;
  if (target.isContentEditable && target.closest('.ts-sheet, .ts-stage') === null) return true;
  return false;
}
```

With it Cmd+Z from the checkbox reaches `edit.undo` through the key table (the item's predicate decides, as from the stage), which is Google's behaviour from the sidebar; a `<select>` stays a field because its arrow keys are its own. `packages/chrome/src/__tests__/use-editor-keys.test.tsx` (the integrator's) gains one case: a checkbox focused, Cmd+Z runs `edit.undo`, and Cmd+B from a text input still runs nothing. The stage's `isEditableTarget` (`Selection.tsx` 199) and `CHROME_CONTROL` need no change for F7: neither owns the chord. No B1 file changes for F7.

R7 (Kevin; then the schema's, the shell's and the inspector's owners; F8, VERIFICATION.md F.10 item 5): whether the fixed kinds' fields (the cover's heading and lead, a statement's big line) take a `typography` of their own. The default of the product round stands in this fix round (the kit's faces set them, `packages/render/src/theme-css.ts` 192 to 198 writing `--display` and `--text` from the kit), and B1's control now says so (section 2). If Kevin chooses a typography on the fields, the change is `TitleSlide` and `StatementSlide` gaining an optional `typography` per field in `packages/schema/src/deck.ts` 265 to 273 (with `block.set` reaching `/heading` and `/lead`, the paths the canvas conversion keeps), `pseudoBlockOf` in `editor-shell.ts` 1204 carrying it, `ToolbarTail.tsx` 267 reading the pseudo block (today `selectedBlock` walks `slideBlocks` alone, so the size field and the Font control both see no block), and `FormatOptions.tsx` listing the Text section for the field with the Tabular figures row; B1's part is one line, `takesFamily` true for the pseudo block, and the two fixed sentences of section 2 leave. Until then the cover's face is the Brand kit's Display face, one panel away (Slide > Edit theme).

### 2. The item landed, its files and its hunks

F8, the Font control on a fixed kind's field (one item; reverts alone). On a fresh deck's title slide (`kind: 'title'`, `packages/store/src/template-index.test.ts` 152) the heading and the lead reach the text tail as pseudo blocks (`editor-shell.ts` `tailKindOf` over `pseudoBlockOf`) and `FontField` as `block: undefined`, so the control read the theme's face ("Inter") whatever the kit said, its tooltip read the working picker's sentence, and a click opened nothing. Now the control reads the kit's face for the field's role, is disabled, and its tooltip names the kit path.

- `packages/chrome/src/font-picker-model.ts`: the header paragraph on the fixed fields; `FONT_PICKER.fixedDisplayDoc` ("This heading takes the brand kit’s Display face; Slide > Edit theme changes it") and `FONT_PICKER.fixedTextDoc` ("This text takes the brand kit’s Text face; Slide > Edit theme changes it"); `KitFontRole` (`keyof KitFonts`); `fixedFieldRole(field)` (a heading, the cover's and a statement's big line, takes `display`, the lead and a picture kind's plate `text`, the roles `sheet.css` draws through `--display` and the root's `--text`); `fixedFieldFamily(kit, role)` (the kit's face, or null for the theme's when the kit is silent or names `inter`, the shape of `familyOf`); `fixedFieldDoc(role)`. `takesFamily(undefined)` stays false: nothing to write into.
- `packages/chrome/src/FontPicker.tsx` `FontField`: `pseudoBlockOf(slide, selection.blockId)` when the tail passed no block; `role` from it; the `disabledReason` for the tip (`fixedFieldDoc(role)` after the table's sentence); `family` from `fixedFieldFamily(kit, role)` for a fixed field, so the dropdown's words and `data-font` read the kit's face (`fraunces` with the kit's Display face set, `theme` when silent); the read only branch reads the same. The docblock names the mechanism and Kevin's call.
- `packages/chrome/src/__tests__/font-picker-model.test.ts`: `describe('the fixed fields')`, three cases: the roles by block type with `takesFamily(undefined)` false; the kit's face per role and null when silent or `inter`; the sentences naming the face and the menu path with no trailing period.

No new control id (the control is `toolbar.font` as before), no string in `menus/strings.ts` (the two sentences are the picker's words beside `tableDoc`), no `data-control` added, no route, no poll, no store call: the cost rows of docs/SYNC.md 6.1 are untouched.

### 3. The rows this fix touches, and what happened

The hand drive `b1/b1-fix-drive.mjs` on 4411 under the lock, run 4 at 06:56 to 06:58Z, 15 of 15 (`b1/b1-fix-drive-run.json`): the editor booted on `/new` with the title slide; one click on the cover heading, the control read "Inter", `aria-disabled` true, `data-font` theme, the hovered tooltip "Font: This heading takes the brand kit’s Display face; Slide > Edit theme changes it."; a click at its centre opened no plate; `brand.set /fonts/display fraunces` and `/fonts/text geist`, the heading's control read "Fraunces" with `data-font` fraunces and the same sentence while the heading computed `Fraunces, Georgia, "Times New Roman", serif`; the lead's control read "Geist" with the Text sentence; the kit set back; a text block placed and clicked, the control read "Inter", enabled, with the picker's sentence, and its click opened `toolbar.font.plate`; then the F7 reading of R6 (the toggle on from the row's label with the focus on the checkbox, Cmd+Z from it changing nothing, Cmd+Z from the stage computing `normal`, Cmd+Shift+Z from the checkbox computing `tabular-nums` again); no console or page error from this lane's modules (the five console lines were the dev server's `[Server]` log forwarding); the scratch deck 404 at the end. Runs 1 to 3 (06:53 to 06:54Z) were the driver's own faults: Playwright refuses a click on an `aria-disabled` control (run 1; the drive clicks with the mouse at the centre, a person's click), the Tabular figures input sits under its drawn box so the label is what is clicked (run 2, the smoke's run 5 lesson), and `describe().state.revision` follows the commit and lags the optimistic write by more than the drive's pause (run 3 read 5 to 5 with `tabular-nums` computed), so the computed style is the assertion and the revision is information.

The walk, `node scripts/probes/editor-walk-probe.mjs --core --base http://localhost:4411 --only fonts --json b1/fix-walk-fonts.json --matrix b1/fix-walk-fonts.md`, 06:58 to 07:00Z, 109 s, 33 steps, 32 ok, 0 failed, 1 not driven, 0 console errors: `fonts.dropdown.opens`, `fonts.dropdown.apply-selection`, `fonts.dropdown.search`, `fonts.format-menu.row`, `fonts.more-fonts.licence`, `fonts.face.reload-and-show`, `fonts.agent.font-list`, `fonts.links.licence-v4-1`, `fonts.fallback.in-stack`, `fonts.display-features.inter-only`, `fonts.catalog.geist`, `fonts.catalog.six-families` and `fonts.picker.search-category` passed (the thirteen rows the `FontField` change could reach, on text blocks and on the title heading through the window API); `fonts.table.takes-family` not driven as in every ship run (P1, not built: B2's R5 of the ship's section 5); the `decks` rows the core walk always plans passed; the exit code 1 is the whole matrix gate over the 541 rows `--only` did not plan, not a failure of a driven row. `formatting.numerals.tabular-row` (B2's, `parks: formatOptions.typography.numerals`) is not in the fonts area and nothing in this lane changes it; the F7 mechanism was read by the drive, above. No `logos.*` or `shaders.*` row is involved, so nothing is parked by this fix and `parked-controls.ts` is unchanged.

### 4. The commands run

- `node_modules/.bin/tsc -b packages/chrome`: exit 0 (06:49Z); `node_modules/.bin/tsc -p packages/chrome/tsconfig.json --noEmit`: exit 0.
- `cd packages/chrome && ../../node_modules/.bin/vitest run` over this lane's four files (`__tests__/font-picker-model.test.ts`, `parked-controls.test.ts`, `__tests__/logo-dialog.test.tsx`, `__tests__/tailor-dialog.test.tsx`): 28 passed, 2 todo; the whole chrome suite: 76 files, 716 passed, 2 todo (06:51Z).
- `node_modules/.bin/prettier --write` over the three changed source files and the drive; `--check` clean.
- The dev server on 4411 from `apps/studio` with `vite.no-watch.config.ts`, started after the source edits so it served the fixed modules.
- The lock: `mkdir .turboslide/e2e.lock` at 06:53:13Z and 06:53:59Z (runs 1 and 2, released after each); another lane held it from 06:54 to 06:56Z and the contract's `until mkdir` loop took it at 06:56:36Z; runs 3 and 4 and the walk ran under that hold; released at 07:00:04Z.
- No `pnpm` command, no `vite build`, no git write, no deployment.

### 5. Deviations and notes

- F7 is not fixed in this lane's files: its mechanism is `useEditorKeys.ts`, nobody's file, so it is R6 to the integrator with the hunk and the reading. The verifier's file for the fix, `Editor.tsx`, is B3's and is not where the chord dies.
- F8's model is unchanged, as the finding asked (Kevin's call, R7): the fields still take no family and Format options still lists no Text section for them (`FormatOptions.tsx` and `inspector/typography.tsx` are B3's and B2's). What changed is that the control tells the truth: the face the field draws and where it is set. A seller who wants the cover in Fraunces sets the kit's Display face, which also sets every heading, the product round's model.
- The tooltip sentences are the picker's words (`FONT_PICKER`, beside `tableDoc`) with the typographic apostrophe, sentence case, no trailing period; `controlTip` and `tipProps` add the period on the tooltip as for every control.
- The drive's lessons for the next driver: a person's click on a disabled control is `page.mouse.click` at the centre; the Tabular figures label is the click target; `describe().state.revision` lags an optimistic write, read the computed style.
- The walk probe left an empty `b1/shots` folder beside the JSON (no `--shots` was passed); removed.

## Features round, ship two

Builder B1 of the features round, ship two (docs/FEATURES.md 5.3, 5.4, 5.5 the dialog's sentence, 5.6 the setting, 5.10, 7.2 and 7.3; the lane table of section 6; the rows of 7.1), started 2026-09-24 on the shared checkout at `2be923e` while B4, B5 and B7 land beside it. The dev server port is 4411 (`TURBOSLIDE_STORE=tmp`, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, two fake secrets of 40 characters, `vite.no-watch.config.ts`; stopped before the report). No git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran. No token, cookie or secret was printed. Times are PDT.

The sections are the ship one shape: the requests (1, written first), the items landed with their files and hunks (2), the rows and what happened (3), the commands run (4) and the decisions (5).

### 1. Requests

Each names the file, the owner and the one change, written so it can be applied as it stands.

R1 (the integrator, `packages/chrome/src/menus/model.ts`, the `INSERT` menu at 1583 to 1588): the `insert.material` row becomes `insert.shader`, labelled Shader, in the default view (5.4: "the row `insert.material` renamed, `advanced: false` on the ship's branch"), with the finder `terms` so Search the menus "shader" and "animation" answer it. The effect stays `action('block.insert')`, so `insertIntentOf` keeps routing the row to the picker (R2).

```ts
    /* the features round, ship two (docs/FEATURES.md 5.4; build/b1.md R1): Insert > Shader in the
       default view, the Material row renamed and unflagged; the gallery is dialogs/ShaderGallery.tsx */
    now('insert.shader', 'Shader', action('block.insert'), {
      turboslide: true,
      icon: 'cube',
      doc: 'A moving shader in your brand kit’s colours; every export carries its still',
      terms: ['shader', 'material', 'animation', 'gradient', 'motion'],
    }),
```

P1, the same file, under the condition of section 1: `MenuSetting` gains `'playShaders'` (after `'linkDetection'`, with the comment `/* the features round, ship two (docs/FEATURES.md 5.6): View > Play shaders, On, In the show only or Off, per browser */`), and the View menu gains the submenu between Mode and Full screen:

```ts
    /* the features round, ship two (docs/FEATURES.md 5.6; build/b1.md R1): three radios over one
       per browser setting; the editor mount and the show read it, reduced motion forces Off */
    sub(
      'view.playShaders',
      'Play shaders',
      [
        now('view.playShaders.on', 'On', toggle('playShaders', 'on'), {
          doc: 'Shaders move on the slide while you edit and in the show',
        }),
        now('view.playShaders.show', 'In the show only', toggle('playShaders', 'show'), {
          doc: 'Shaders hold their frame while you edit and move in the show',
        }),
        now('view.playShaders.off', 'Off', toggle('playShaders', 'off'), {
          doc: 'Shaders show their frame everywhere',
        }),
      ],
      { turboslide: true, icon: 'cube', dividerBefore: true, terms: ['shader', 'animation', 'motion', 'reduced motion'] },
    ),
```

In `editor-shell.ts`, `STORED_SETTINGS` gains `'playShaders'` and `DEFAULT_SETTINGS` gains `playShaders: 'show'` (the default of question 5); the shell already reports the settings through `onSettingsChange`, so `describe().state.settings.playShaders` reads it the moment the key is stored. The tests of `menus/__tests__/menu-model.test.ts` (7.3 names them as this lane's cases in the integrator's file): the `row('insert', 'now', ['insert.material'])` line (315) reads `insert.shader`; the twenty additions list (1009) and the two parked lists (2473, 2843) drop `insert.material`, and a new case reads `insert.shader` present with `advancedTools: false` where Material was; with the P1 setting, a case reads `view.playShaders` as a submenu of three toggle rows over one setting with `show` checked under `DEFAULT_SETTINGS`.

R2 (the integrator, `packages/chrome/src/editor-shell.ts`, `EditorShell.tsx`, `palette-data.ts` and their tests): the picker's dialog. `DIALOG_IDS` (923): `'insertMaterial'` becomes `'shaderGallery'` with the comment `/* the features round, ship two (docs/FEATURES.md 5.4): the Shader gallery */`; `InsertPicker` (1568) reads `'icon' | 'shader'`; `PICKER_OF` (1596) maps `'insert.shader': 'shader'`; the `case 'insert.material':` of `planFor` (2627) reads `case 'insert.shader':` (B5's shader branch of `insertBlockPlan` lands under it). `EditorShell.tsx`: `PICKER_DIALOG` (146) reads `shader: 'shaderGallery'`; the lazy dialog (187) reads `const ShaderGalleryDialog = lazyDialog(() => import('./dialogs/ShaderGallery').then((m) => m.ShaderGalleryDialog));` in `LAZY_DIALOGS`; `dialogNode` (2122) reads `case 'shaderGallery': return <ShaderGalleryDialog.Component />;`. `palette-data.ts` 206 reads `material: 'insert.shader'` and `__tests__/palette-data.test.ts` 259 follows. Then delete `packages/chrome/src/dialogs/InsertMaterial.tsx`: this lane left it as a four line shim over `ShaderGallery.tsx` so the shared tree compiles until this request lands (section 5). Tests: `__tests__/editor-shell.test.ts` 1000 reads `'insert.shader': { kind: 'picker', picker: 'shader' }`; `__tests__/editor-shell-render.test.tsx` 543 drops `insert.material` from the parked list and 698 to 720 becomes "Insert > Shader opens the gallery; a card writes a material block with the entry's featured preset": no `advancedTools` line, `clickMenuPath(container, 'insert', 'insert.shader')`, `findByRole('dialog', { name: 'Shader' })`, the cards `[data-control^="dialog.shader.tile."]`, the click on the first dispatching `block.insert` with `type: 'material'`, `materialId: 'paper:liquid-metal'` and `preset: 'diamond'`. The parity walk `scripts/probes/editor-walk-probe.mjs` 2080 to 2091 (`insert.material`, `dialog.insertMaterial`) reads `insert.shader`, `dialog.shader` and `dialog.shader.tile.`.

R3 (the integrator, `packages/chrome/src/menus/strings.ts`): the words live in this lane's file until strings.ts takes them, one exported constant and one import to move: `SHADER_GALLERY` in `dialogs/ShaderGallery.tsx` (the title, the sentence, the search field, the category chips, the empty state, the tile docs, the alt `The <name> shader`, the Background row's words Shader, Choose, Place, Placing with the seconds, Shader options, and the one failure sentence of 5.5 `The frame could not be rendered. Try again, or place the shader without a frame`). `DIALOGS.insertMaterial` (524 to 528) leaves with R2; `DIALOGS.background` gains nothing (the Shader row reads `SHADER_GALLERY`). `DITHER.material`, `DITHER.choose`, `DITHER.place` and `DITHER.play` are B5's to keep or drop with `inspector/material.tsx`.

R4 (B5, `packages/materials/src/catalog.ts`, `presets.ts`, `previews/**`, `scripts/build-shader-previews.mjs`, `editor-shell.ts`'s shader branch): the contract the gallery reads. Written at 15:10Z against the tree of the morning; by 15:44Z B5's `category`, `featuredPreset`, `GALLERY_MATERIAL_IDS`, `@turboslide/materials/previews` and the 126 webp files were on the tree and the dialog reads them (section 5), so the first three bullets stand as a record of what was asked and the last two are still open.

- `MaterialEntry.thumbnail?: string`: the 320 by 200 webp of 5.4, in ink and paper at anchor 5500, as a URL the browser loads (a Vite asset URL from `new URL('../previews/<file>', import.meta.url).href`, or the route B7 serves). `MaterialPreset.thumbnail?: string`: the preset's small tile. Until they exist a card draws a plate with no text (`data-thumb="none"`) and the row `shaders.insert.gallery-thumbnails` reads red on the thumbnail clause; nothing in the dialog mounts a canvas to fill the gap (5.6).
- `MaterialEntry.category?: 'fluid' | 'light' | 'metal' | 'gradient' | 'graphic'`: read when present; else the dialog applies Glyphfield's rule over the id, the label and the engine (`shaderLab.ts` `shaderLabCategory`: metal for chrome, metal, glass, prismatic and gem; fluid for fluid, water, warp, swirl, smoke and metaball; light for bloom, ray, line, radiant, ring, border and circuit; gradient for gradient, mesh, noise, orbit and panel; graphic otherwise), which puts liquid metal and gem smoke under Metal, smoke ring, swirl and metaballs under Fluid, god rays under Light, the seven gradients and noises under Gradient and spiral, dithering, waves and dot grid under Graphic. Set the field where the rule reads wrong for a seller.
- `MaterialEntry.featuredPreset?: string`: the preset Insert lands (5.2: the diamond for liquid metal); else the dialog's table (`paper:liquid-metal` → `diamond`, `paper:gem-smoke` → `brand-blue`) and the entry's first preset otherwise. The Preset tiles read `preset.label`, so "Ink and paper" reads as presets.ts spells it; 5.2's "Ink on paper" is a presets.ts label change.
- The block the dialog hands `insertBlockPlan(facts, 'material', make, 'Shader')` is `shaderBlockOf(id, entry, preset)`: `CATALOG.material.make(id)` with `materialId`, `preset` (the featured one, or the clicked tile's), `alt` `The liquid metal shader`, and `motion: { play: 'show' }` written only while `materialBlockSchema.shape` carries `motion` (the runtime probe `schemaHasMotion()`), so your schema change lights the default of question 5 with no change here and a strict validator never sees the key early. Your shader branch of `insertBlockPlan` places that block in the largest free rectangle and keeps its fields; the dialog then calls `input.onSelectBlock?.(blockId)` with the plan's id and closes, so the block is selected (5.4) whichever of us lands first.
- The hover live mount (P1, `dialog.shader.hover`) mounts `mountMaterial(host, { materialId, preset, anchor: 5500 }, { speed: 1, frame: 5500, minPixelRatio: 1 })` through `loadMaterialMount()` of `packages/viewer/src/MaterialMount.tsx`; the kit's colours arrive through your 5.7 palette resolution of the preset, and the 30 frames per second cap is your mount option when `engine.ts` lands (the dialog passes nothing for it today).
- The Background dialog's Add to theme and Apply to all rows for the background shader (P1 item 1 of 5.2): the slot is the Shader field of `dialogs/Background.tsx`; send the action name, the input shape and the words and this lane draws the two buttons beside Place.

R5 (B4, the drivers of `shaders.insert.gallery-thumbnails`, `shaders.insert.gallery-hover-live`, `shaders.view.play-setting` and `shaders.background.place-answers`): the ids and the readable state.

- The dialog is `dialog.shader` (`Dialog.tsx`'s card, so `dialog.shader.close` is the X); the title reads Shader; the sentence is `dialog.shader.sentence` and reads "Previews are in black and white. The shader takes your brand kit’s colours on the slide" (the typographic apostrophe, as the Logo dialog's words); the search field `dialog.shader.search` has the focus on open.
- The chips are `dialog.shader.category.all`, `.fluid`, `.light`, `.metal`, `.gradient`, `.graphic` (`role="radio"`, `aria-checked`); the grid is `dialog.shader.grid` (`role="listbox"`) with `data-count` (the cards drawn), `data-query` (the trimmed query the cards answer) and `data-category`.
- A card is `dialog.shader.tile.<materialId>` (`paper:liquid-metal`; `data-material`, `data-preset` the preset a click inserts, `aria-selected` and `is-active` on the one the arrows moved to and Enter inserts, the first card by default); its thumbnail is `dialog.shader.tile.<materialId>.thumb`, an `<img>` that gains `data-decoded="true"` after `decode()` resolves, or a `<span>` with `data-thumb="none"` while B5's previews are not on the tree; the preset tiles under the name are `dialog.shader.tile.<materialId>.preset.<name>` (`aria-label` the preset's label; a click inserts that preset); the empty state is `dialog.shader.empty`; a refused insert reads in `dialog.shader.error`.
- No canvas is in the dialog unless a card is hovered 400 ms with the P1 hover on: the mount's host is `dialog.shader.hover` (`data-material`), one at a time, disposed on leave; reduced motion and a parked `dialog.shader.hover` draw none. A driver of the thumbnails row keeps the pointer off the cards.
- The Background dialog's Shader row: `dialog.background.shader` (the Choose button, `aria-expanded`; `data-material` and `data-preset` once a tile is picked, the label then reads "Liquid metal, Diamond"); the embedded grid carries the same `dialog.shader.*` ids as the dialog, in a two column compact layout; `dialog.background.shader.dither` (the check); `dialog.background.shader.place` (`data-placing="true"` while it waits, the text `Placing` then `Placing, 3 s` as the seconds pass); a covering material reads in `dialog.background.shader.current` (`data-material`, `data-preset`) as "Liquid metal, Diamond" with the link `dialog.background.shader.options` (Shader options, opening Format options at the Shader section); a failure reads in `dialog.background.error` as one sentence, never a log (`shaderFailureSentence`: a message with a line break, a path, "Browser logs" or over 160 characters is replaced by 5.5's sentence).
- The P1 Slideshow mirror: the Options menu gains `present.options.playShaders` with the rows `present.options.playShaders.on`, `.show`, `.off` (`menuitemradio`, checked from the stored setting; a pick writes `ts-editor-settings` and the editor's View row reads it on its next open). `Menu.tsx` (590 to 591) draws every row with `data-control="menu.<id>"` and `data-menu-item="<id>"`, so the show's rows read `menu.present.options.playShaders.<value>` by control and `present.options.playShaders.<value>` by `data-menu-item`; `shaders.mjs`'s `[data-control^="present.options."]` read (1424) matches neither and `[data-menu-item^="present.options.playShaders"]` matches the three.

R6 (B7, `apps/studio/src/server/actions.ts`, the report module, `download.ts`): two contracts.

- The Place failure: the `RenderError` sentence of 5.5 is spelled in this lane's `SHADER_GALLERY.placeFailed` as `The frame could not be rendered. Try again, or place the shader without a frame`; the Background dialog shows the rejection's message when it is one sentence and this constant otherwise (R5), so the server's sentence reads verbatim when it is the same words. Point your constant at `SHADER_GALLERY.placeFailed` (`@turboslide/chrome/menus/strings` once R3 moves it) or spell it the same.
- The Download dialog's report row (5.5): the dialog reads `input.export.progress.rows?: ReadonlyArray<{ id: string; text: string }>` and draws each as `dialog.download.report.<id>` (`ts-dialog-sentence`) under the progress sentence while the export runs, and on the direct path says each new row once through the snackbar; a row `{ id: 'shaders', text: '2 shaders had no frame; the export waited 8 s for them' }` is what the row `shaders.export.missing-frame-row` reads. `ExportProgress` (`ExportMenu.tsx` 50, the integrator's) gains the optional field; the route fills it from the report module.

R7 (B3): nothing; `pickers/ColorPlate.tsx` is unchanged.

### 2. The items landed, their files and their hunks

Each item is one commit's worth; the hunks are named so a red row's item reverts alone. Every line of this lane's ship two code sits under a comment naming the features round, ship two, or in a new file.

#### 2.1 The Shader gallery (FEATURES.md 5.4, 5.10; rows `shaders.insert.gallery-thumbnails`, and the dialog half of B5's `shaders.insert.selected-free-rectangle` and `shaders.insert.words`)

- `packages/chrome/src/dialogs/ShaderGallery.tsx` (new): `SHADER_GALLERY` (the words, R3); `insertableMaterials()` over B5's `GALLERY_MATERIAL_IDS` (the featured six first, then the catalog's order); `engineOf`, `matchesShaderQuery` (the name, the description and the engine), `shaderGalleryEntries(query, category, engine)` over the catalog's `category`; `shaderEngines` (the P1 engine chips draw only once a second family is available); `featuredPresetOf` (the catalog's `featuredPreset`, else the first preset); `shaderPreviewUrl` over `@turboslide/materials/previews`; `schemaHasMotion()` and `shaderBlockOf(id, entry, preset)` (the catalog's blank with `materialId`, `preset`, the alt "The liquid metal shader", `uniforms` dropped, and `motion: { play: 'show' }` while the schema carries the field, the default of question 5); `shaderWordsOf` ("Liquid metal, Diamond", never an id); `shaderFailureSentence` (one sentence or 5.5's, never a log); `ShaderGalleryGrid({ onPick, compact, hoverLive, autoFocus, control })`: the search field, the six chips, the grid of cards as one `role="option"` element each with the still (`Still`: an `<img>` that gains `data-decoded` after `decode()`, or a plate with no text on an error), the name and the preset tiles inside it (a tile's click stops at the tile), the first card active, the arrows and Enter in the field, the empty state, every `dialog.shader.*` control behind `isParked`; `ShaderGalleryDialog`: the 720 px card titled Shader, the sentence, the grid, a pick through `insertBlockPlan(facts, 'material', shaderBlockOf, 'Shader')` then `onSelectBlock(blockId)` and close, a refusal in `dialog.shader.error`.
- `packages/chrome/src/dialogs/ShaderGallery.css` (new): two 320 px columns in the 672 px body (16 by 32 gaps), the still at 16:10 in a plate ground, the card's hairline frame with the ink ring for the active one, the 48 by 30 preset tiles with the featured one ringed, the compact two fluid columns for the Background dialog; tokens only, radius 0.
- `packages/chrome/src/dialogs/InsertMaterial.tsx`: a four line shim re exporting `ShaderGalleryDialog` as `InsertMaterialDialog` and `insertableMaterials`, so `EditorShell.tsx`'s lazy import and the shared tree compile until R2 deletes the file (section 5).
- `packages/chrome/src/__tests__/shader-gallery.test.tsx` (new): the order, the categories' union, the search ("metal" one card, "meta" two, the engine, a chip and a query together), the engines, the featured preset, the preview file names, the block maker with the strict schema's `safeParse`, the words, the failure sentence; the dialog's title, sentence, focus, chips, seventeen cards as B4's reader takes them, the still and its plate on an error, the preset tiles inside the card, the tooltips, no canvas; the search and the chip narrowing, the empty state; a card's insert with the selection and the close, a preset tile's insert; Enter and the arrows; a refusal; the P1 hover surface on and off; the compact grid under its own `dialog.shader` root.
- `packages/chrome/src/parked-controls.test.ts`: the gallery's surface (7.3): a parked `dialog.shader.category.metal` chip and a parked `dialog.shader.hover` draw nothing with the switch off and draw with it on; the mocked set holds four ids; the B5 todo narrows to the Shader section.

#### 2.2 The Background dialog's Shader row (FEATURES.md 5.4, 5.5; the dialog half of `shaders.background.place-answers`; the link half of B5's `shaders.panel.one-home`)

- `packages/chrome/src/dialogs/Background.tsx`: the imports (`SHADER_GALLERY`, `ShaderGalleryGrid`, `shaderFailureSentence`, `shaderWordsOf`, `ShaderPick`, `FormatSectionId`); the docblock's Shader paragraph and `SHADER_SECTION`; the state `shader`, `shaderDither`, `shaderOpen`, `placing`, `placingSeconds` with its interval; `coveringPreset`; `place()` with the seconds and `shaderFailureSentence`; `openShaderOptions()` (the covering picture selected, Format options at the Shader section); the Shader `DialogField`: the current row `dialog.background.shader.current` with `dialog.background.shader.options`, the Choose button `dialog.background.shader` (`aria-expanded`, `data-material`, `data-preset`, the pick's words), the check `dialog.background.shader.dither`, Place `dialog.background.shader.place` (`data-placing`, "Placing" then "Placing, 3 s"), the embedded `ShaderGalleryGrid compact control="dialog.shader"` while open; the error `dialog.background.error`. The Material row's list, `dialog.background.material.*` and the Material options link left.
- `packages/chrome/src/__tests__/background-shader.test.tsx` (new): Choose opens the compact grid under `dialog.shader` with no canvas, a card picks the featured preset and the button reads "Liquid metal, Diamond", Place is one `slide.setBackgroundMaterial` with the preset, the anchor and the dither and the dialog closes; a preset tile's pick, "Placing" then "Placing, 3 s" under fake timers; a log rejection reads as 5.5's sentence and a one sentence rejection as itself, with Place offered again; a covering shader reads by its words with Shader options opening Format options at `shader`.

#### 2.3 The Download dialog's report rows (FEATURES.md 5.5; the dialog half of B7's `shaders.export.missing-frame-row`; R6)

- `packages/chrome/src/dialogs/Download.tsx`: `ExportReportRow` and `exportReportRows(progress)` (the optional `rows` of the route's progress, each an id in a control's shape and one sentence); the rows drawn as `dialog.download.report.<id>` (`ts-dialog-sentence`, `role="status"`) under the progress sentence while the run is in flight or failed; the direct path says each new row once through the snackbar.
- `packages/chrome/src/__tests__/download-report-rows.test.tsx` (new): the reader over a progress with and without rows and with malformed entries; the direct PDF path says the row once across a rerender; the Download options path draws the row after OK.

#### 2.4 P1: the gallery's hover live mount (FEATURES.md 5.2 item 5; row `shaders.insert.gallery-hover-live`)

Separable by hunk: the `hoverLive` token on `<ShaderGalleryGrid>` in `ShaderGalleryDialog` is the one line the fix round drops when the row is red; the block it enables stays inert without it.

- `packages/chrome/src/dialogs/ShaderGallery.tsx`: `HOVER_LIVE_DELAY_MS`, `reducedMotion()`, `playShadersOff(settings)`; in the grid the `hoverAllowed` read (the prop, `isParked('dialog.shader.hover')`, reduced motion, View > Play shaders Off), `data-hover-live` on the root, the host span in every card's frame (`dialog.shader.hover.<materialId>` while live), `armHover` (400 ms after `pointerenter`, `loadMaterialMount()` then `mountMaterial(host, { materialId, preset, anchor: 5500 }, { speed: 1, frame: 5500, minPixelRatio: 1, palette })` with the deck's `shaderPaletteOf(kit, appearance)`), `disposeHover` (on `pointerleave`, on unmount, when the card list drops the card; the host emptied), one handle at a time with a late mount disposed.
- `packages/chrome/src/dialogs/ShaderGallery.css`: `.ts-shader-hover` over the still, shown while `.is-live`.

#### 2.5 P1: View > Play shaders, the Slideshow's mirror (FEATURES.md 5.6; row `shaders.view.play-setting`)

Not landed in `apps/studio/src/components/Slideshow.tsx`: the rows read and write the `playShaders` `MenuSetting`, which R1 adds to `model.ts` and `editor-shell.ts` (`MenuSetting`, `STORED_SETTINGS`, `DEFAULT_SETTINGS`), and the show's own reading of the setting is B5's `present/ShaderLayer.tsx`; the file does not typecheck with the rows before R1. The hunk, ready to apply the moment R1 lands (one commit with the R1 rows; the row is then driven on a preview):

```tsx
// Slideshow.tsx, the imports: writeStoredSettings joins readStoredSettings and SETTINGS_STORAGE
import { SETTINGS_STORAGE, readStoredSettings, writeStoredSettings } from '@turboslide/chrome/editor-shell';

/* the features round, ship two (docs/FEATURES.md 5.6; build/b1.md 2.5): the show's Options menu
   mirrors View > Play shaders; the words move to menus/strings.ts with R3 */
const PLAY_SHADERS = {
  label: 'Play shaders',
  on: 'On',
  show: 'In the show only',
  off: 'Off',
  doc: 'Whether the shaders on the slides move during the show',
} as const;
type PlayShaders = 'on' | 'show' | 'off';

// optionsItems(state): after the Full screen row and before the pen
    {
      id: 'present.options.playShaders',
      label: PLAY_SHADERS.label,
      status: 'now',
      effect: { kind: 'submenu' },
      icon: 'cube',
      doc: PLAY_SHADERS.doc,
      items: (['on', 'show', 'off'] as const).map((value) => ({
        id: `present.options.playShaders.${value}`,
        label: PLAY_SHADERS[value],
        status: 'now' as const,
        effect: { kind: 'toggle' as const, setting: 'playShaders' as const, value },
      })),
    },

// Slideshow(): beside the advancedTools state, read once on the client and written on a pick
  const [playShaders, setPlayShaders] = useState<PlayShaders>('show');
  useMountEffect(() => {
    try {
      const stored = readStoredSettings(window.localStorage.getItem(SETTINGS_STORAGE));
      setAdvancedTools(stored.advancedTools === true);
      const value = stored.playShaders;
      if (value === 'on' || value === 'show' || value === 'off') setPlayShaders(value);
    } catch {
      // private mode or storage refused: the default view
    }
  });
  const pickPlayShaders = (value: PlayShaders) => {
    setPlayShaders(value);
    try {
      const stored = readStoredSettings(window.localStorage.getItem(SETTINGS_STORAGE));
      window.localStorage.setItem(SETTINGS_STORAGE, writeStoredSettings({ ...stored, playShaders: value }));
    } catch {
      // private mode: the pick holds for the show
    }
  };
  // the context memo: settings: { ...DEFAULT_MENU_CONTEXT.settings, advancedTools, playShaders }
  // onMenuSelect: before the default branch
      case 'present.options.playShaders.on':
      case 'present.options.playShaders.show':
      case 'present.options.playShaders.off':
        pickPlayShaders(item.id.slice('present.options.playShaders.'.length) as PlayShaders);
        break;
```

The rows land as `menuitemradio` through `Menu.tsx`'s `isChecked` over the context's `playShaders`, with `data-control="menu.present.options.playShaders.<value>"` and `data-menu-item="present.options.playShaders.<value>"` (Menu.tsx 590 to 591), which R5's last bullet names for B4's reader. The editor's View row reads the same `ts-editor-settings` record on its next open; `describe().state.settings.playShaders` reports it once R1 puts the key in `STORED_SETTINGS`.

### 3. The rows this lane covers, and what happened

Measured on this lane's dev server (4411, the tmp store, `vite.no-watch.config.ts`) with `build/b1/b1-ship2-smoke.mjs` (playwright-core at 1440 by 900, at human speed, Tools > Advanced tools on through the stored settings so Insert > Material reaches the gallery through the shim until R1, one scratch deck from `/new` trashed and removed at the end through the window API's `deck.trash` and `deck.remove`, a 404 on `/edit/<id>` confirmed, under `.turboslide/e2e.lock`); run 1 (`b1-ship2-smoke-run1.json`, 15:40Z) read 15 of 19 before B5's `packages/materials/previews/` (126 webp, 564 KB) was on the tree and before two smoke defects were fixed (section 5); run 2 (`b1-ship2-smoke-run2.json`, 15:44Z) read 21 of 21. The jsdom tests of section 2 read the rest. A row not driven is not driven.

| Row                                           | What happened                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `shaders.insert.gallery-thumbnails`           | Driven on 4411 (run 2) through Insert > Material with the switch on (the Insert row reads Shader in the default view only after R1): the dialog `dialog.shader` titled Shader, the sentence "Previews are in black and white. The shader takes your brand kit’s colours on the slide" in `dialog.shader.sentence`, the search field focused, 17 cards in the order liquid metal, gem smoke, god rays, mesh gradient, smoke ring, grain gradient then the catalog's, two a row, the first inserting `diamond`; 17 decoded stills 5 ms after the open (B5's previews); 0 canvas in the dialog; "metal" typed at 60 ms a key narrowed to one card, Liquid metal; six chips with All checked first and Metal narrowing to one card. Run 1, before B5's files, read 17 plates with no text and 0 decoded (the clause was red then, as the plate rule of 5.5 intends). |
| `shaders.insert.gallery-hover-live` P1        | Driven on 4411 (run 2): `data-hover-live` on the grid's root; the pointer moved in six steps onto Liquid metal, one canvas within 1.5 s under `dialog.shader.hover.paper:liquid-metal`, one at 400 ms later, at most one while moving to Gem smoke in ten steps, one on it, none 2 s after the pointer left to (40, 40). The colours are the deck's palette through B5's `palette` mount option (the tmp deck has no kit, so the legacy palette); the 30 frames per second cap waits for B5's engine option (R4).                                                                                                                                                                                                                                                                              |
| `shaders.view.play-setting` P1                | Not driven: `view.playShaders` and its `MenuSetting` are R1 (the integrator's file) and were not on the tree at 15:44Z (`describe().state.settings.playShaders` read `null`); the Slideshow mirror is the ready hunk of 2.5 and is not in `Slideshow.tsx`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `shaders.background.place-answers`            | The dialog half, driven on 4411 (run 2): Slide > Change background; the Shader row with Choose and a disabled Place; Choose opened the compact grid under `dialog.shader` with 17 cards and 0 canvas; the Liquid metal card collapsed the grid and the button read "Liquid metal, Diamond" (`data-material`, `data-preset`); Place read "Placing" and the write answered in 818 ms, so no seconds were drawn (the jsdom test reads "Placing, 3 s" at 3.1 s under fake timers) and the dialog closed. A direct `slide.setBackgroundMaterial` probe on the same server (15:47Z) answered in 1618 ms with the covering picture `background` at (0, 0, 1600, 900, z -1) over the asset `liquid-metal`, a material capture of 3200 by 1800 with `backend: 'angle-metal'` (the local Chromium; production's SwiftShader and the 30 s bound are B7's). The failure sentence path (a log rejection reads as 5.5's sentence, never the log) is the jsdom test's.        |
| `shaders.insert.selected-free-rectangle` (B5) | Observed on 4411 (run 2): the card's click closed the dialog and the block landed with `materialId paper:liquid-metal`, `preset diamond`, `alt "The liquid metal shader"`, `motion {"play":"show"}` and `describe().state.blockId` naming it (selected); its `pos` was (560, 314, 480, 272), the sheet's centre over the title slide, because B5's shader branch of `insertBlockPlan` was not on the tree at 15:44Z. The dialog's half (the block's fields and the selection) holds; the placement is B5's.                                                                                                                                                                                                                             |
| `shaders.insert.words` (B5)                   | The dialog's words: the title Shader, the alt "The liquid metal shader", the cards and tiles by the catalog's labels, no `paper:` id drawn; the Insert row reads Material until R1 and the chip, the section title and the sheet label are B3's, B5's and the render package's.                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `shaders.panel.one-home` (B5)                 | The link half (jsdom): Shader options on a covering material selects the picture and opens Format options at the section `shader`; the section itself and the Material section's removal are B5's.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `shaders.export.missing-frame-row` (B7)       | The dialog half (jsdom): a `progress.rows` entry draws as `dialog.download.report.shaders` and the direct path says it once; the row itself is B7's route and report module (R6).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

### 4. The commands run

- `node_modules/.bin/tsc -p packages/chrome/tsconfig.json --noEmit`: exit 0 at 15:22Z, 15:35Z, 15:43Z and 15:46Z on the shared tree with every lane's files of the moment (one earlier reading named a wrong import path in this lane's own test, fixed in the same minute).
- `node_modules/.bin/tsc -b` (the whole tree, 15:24Z): exit 1 with four errors, all in `apps/studio/e2e/core/export.spec.ts` 1991 (`Expression expected`, B4's file, in progress); nothing of this lane's and nothing in `apps/studio/src`.
- `cd packages/chrome && ../../node_modules/.bin/vitest run`: 80 files, 747 tests passed, 2 failed, 2 todo at 15:46Z. The two failures are the integrator's tests that assert the Material dialog and the Material row R2 replaces: `__tests__/editor-shell-render.test.tsx` "Insert > Material lists the catalog; a row writes a material block with that recipe" (it looks for a dialog named Material and `dialog.insertMaterial.pick.*`; the shim now opens the gallery) and `__tests__/materials-sections.test.tsx` "the Material row: Choose lists the catalog, Place is one slide.setBackgroundMaterial with the dither" (it clicks `dialog.background.material.choose`, `.paper:liquid-metal.diamond`, `.dither` and `.place`; the row's ids are now `dialog.background.shader`, the card `dialog.shader.tile.paper:liquid-metal.preset.diamond`, `dialog.background.shader.dither` and `dialog.background.shader.place`, and `background-shader.test.tsx` drives the same write). This lane's five files (`__tests__/shader-gallery.test.tsx`, `__tests__/background-shader.test.tsx`, `__tests__/download-report-rows.test.tsx`, `__tests__/download-dialog.test.tsx`, `parked-controls.test.ts`: 40 tests, 2 todo) pass alone as well.
- The dev server on 4411 (from `apps/studio`, the environment of the header, `--strictPort -c vite.no-watch.config.ts`), restarted once at 15:43Z to pick up the grid's scroll into view; stopped before this report.
- `node docs/gslides-parity/features/build/b1/b1-ship2-smoke.mjs --base http://localhost:4411 --out .../b1-ship2-smoke-run1.json` and `run2.json` under the lock (15:40Z, 15:44Z); the Place probe (a scratchpad script, under the lock, 15:47Z). Every scratch deck was trashed and removed and answered 404.
- No git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran; the untracked `.github/` folder was not touched.

### 5. Decisions and notes

- `InsertMaterial.tsx` is a shim, not yet deleted: deleting it would break `EditorShell.tsx`'s lazy import (the integrator's file) and every lane's `tsc -b`; R2 deletes it in the commit that re points the import. Until R2 the Insert row still reads Material behind the switch and opens the gallery, which is how run 2 reached the dialog.
- The card structure follows B4's reader (`core-walk/areas/shaders.mjs` `cards()`): one `dialog.shader.tile.<materialId>` element per card with the still as its first `<img>`, the name, and the preset tiles inside it, so the tiles are never counted as cards and a click on the card's centre inserts. The embedded grid carries `data-control="dialog.shader"` on its root because B4's Place driver polls `dialog.shader` after Choose; the standalone dialog's card carries it through `Dialog.tsx`.
- The categories, the featured order and the featured preset are read from B5's catalog (`category`, `GALLERY_MATERIAL_IDS`, `featuredPresetOf`), which landed while this lane built; the Glyphfield regex rule this lane wrote first was dropped when the field appeared, because the rule read "dithering" as Light (the word ring). R4's first three bullets are answered by B5's tree; the hover mount's 30 frames per second cap and the Background rows of the P1 background slot stay open.
- `shaderBlockOf` writes `motion.play: 'show'` only while `materialBlockSchema.shape` carries `motion`, so the block validates against the strict schema before and after B5's field; on the tree at 15:44Z the field exists and the inserted block carried it.
- `shaderFailureSentence` guards the Place row against a log whatever the transport answers (a line break, a path, "Browser logs", a stack frame or over 160 characters read as 5.5's sentence); a one sentence server answer reads verbatim, so B7's constant shows as written when it is the same words (R6).
- The Shader options link opens Format options at `shader` through `SHADER_SECTION: FormatSectionId | 'shader'` narrowed at the call; the id joins `FormatSectionId` with B5's `format-sections.ts` change and the cast becomes a no op.
- Run 1's two red rows beyond the thumbnails were this smoke's own: a click by coordinates on a card that sat under the Background dialog's scrolled body (the grid now scrolls itself into view when it opens inside another dialog, and the smoke clicks through the locator as B4's `clickControl` does), and the selection read from `state.selection` where `describe().state` reports `blockId`.
- The 404 flood in run 1's console (about 500 lines) was the stills' requests before B5's files existed; run 2 read none from this lane's modules.
- The Recent group, the arrows and the first card preselected follow the Logo dialog's grammar (ship one); no Recent group exists for shaders because the catalog is seventeen cards on one screen and a half.
