# The ship step, features round, ship one

The ship step's note of the features round, ship one (2026-09-23, times in UTC), in the shape of the sync and costs round's `docs/gslides-parity/sync/build/ship.md`. The binding specification is `docs/FEATURES.md` (section 7.2 is the ship rule); `docs/PRODUCT.md`, `docs/SYNC.md`, `docs/RETURN.md`, `docs/FOCUS.md` and `docs/gslides-parity/focus/AMENDMENTS.md` A1 stand under it, A1 rules 1 and 3 amended for tables alone by FEATURES.md 2.1. The verifier's two passes (`docs/gslides-parity/focus/VERIFICATION.md`, "Features round, ship one, pass 1" and "pass 2") and the six lane notes and the integrator's note beside this one were read as claims; every number below was read first hand from a command this step ran or from a run's JSON by row id, and a row nobody drove is not driven, never passed. This step is the one agent of its stage allowed git write commands, on the branch `features/ship-one` and then `main`. No `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran outside `scripts/check.mjs`; `pnpm generate:contracts` ran once. The round five branch was never checked out, merged or cherry picked. Nothing on Vercel or GitHub was created, changed or deleted apart from the preview deployments named here and the pushes to `main`; the two preview secrets came from `openssl rand -hex 32` inside the deploy command and every output line naming them was dropped. No token, cookie or secret is printed here or in any file of this step, with one deviation recorded in section 10.

Sections: 1 the tree the ship step found, 2 the preconditions and what they read, 3 the fixes landed by this step, 4 the previews and the runs of record, 5 the rows by run and the verdict, 6 the parked list, 7 the commits, the push and the deployment, 8 the production run, 9 the pictures, 10 deviations and findings, 11 for Kevin, 12 the run ledger.

## 1. The tree the ship step found

The branch `features/ship-one` at `57da794` (twelve commits over `88b68e7`: the six lanes, prettier, the integrator's day 0 requests and seams, the memory tier gate's seams, the parked list before and after the fixes commit, the integrator's note with the pictures and ledgers) with the fix round's work uncommitted as the verifier's pass 2 found it: 22 modified paths (B3's paste item in `packages/viewer`, B1's fixed field Font control in `packages/chrome`, B6's model wait, title rule, `since` search and fixture files in `apps/studio/src/server` and `packages/chrome/src/logo-model.ts`, the integrator's Docker context in `.dockerignore`, `docker/` and `scripts/check.mjs`, the four notes and pass 2's section of VERIFICATION.md) and the untracked evidence (the pass's 136 pictures, B1's drive and walk files, B3's run JSON, the integrator's step 25 log) beside the untracked `.github/` folder, which never enters a commit. The verifier's verdict (pass 2, F.1): the tree does not ship as it stands under FEATURES.md 7.2, because the fix round's eight cross lane requests (R6, R9, R13, R14, R15, R16, R17, R18, each with its hunk written in `b1.md`, `b3.md` or `b6.md`) were addressed to the integrator, B1 and B4 and none was landed (the integrator's fixer found no request open at 06:50Z; the lanes wrote theirs after). Read by this step by id from the pass's run JSON: red in both preview runs of pass 2 among the ship one rows `logos.insert.every-slide` (R15), `logos.agent.search-insert` (R16; no `parks`, so by the letter it parks `logos` whole), `logos.insert.mono-tint` (the driver's `maxRedirects: 0` against the blob tier's 302, pass 1 F10) and `tables.paste.into-cell-spreads` (the driver's last line, R9); red once with a mechanism outside the two pre forgiven classes `logos.index.refresh-fixture` (pass 2 F.5 finding 3: the refresh's takedown decided from a stale per instance copy).

## 2. The preconditions, checked by the ship step

The task's preconditions, each read by this step on the tree of section 3 (the fixes landed), with the command and what it read:

| Precondition                                                                                                                                                              | Read                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `node scripts/check.mjs` green apart from step 3's uncommitted contracts and the standing reds                                                                            | Run from step 2 in chunks (step 1 is `pnpm install --frozen-lockfile`, which this pipeline never runs): steps 2 to 18 ok (12:24Z to 12:29Z: 5 the whole suite in 53.6 s, 12 the compare to the shoot 170 pairs at 0.5 percent with the worst 0.408, 17 the viewer spec, 18 the chrome lint over the six routes in 90.6 s); 19 red once on this step's own unformatted shooter, then ok after `prettier --write` (35.8 s, "README.md is current"); the later steps in section 12 with the standing reds named (20 the parity audit, 21 the older suites, 26 the round three suites, 31 the perf budget, as VERIFICATION.md records them since the sync round; 25 the container verify's per cell budget under LibreOffice, the round two family, with the disk failure gone since the fix round) |
| `pnpm generate:contracts` current                                                                                                                                         | Run once after R18 (`logo.search` gains `since`): 5 of 14 files written (`cli.json`, `mcp-tools.json`, `openapi.json`, `llms-full.txt`, `skills/turboslide-api/references/actions.md`), committed in `f1afe1e`; check step 3 ok after                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `git grep -n '<<<<<<<'` empty outside prose                                                                                                                               | Empty over the sources (one binary picture of the design round matches the bytes, not a marker)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| The last two preview runs green apart from the parked rows and the recorded flakes                                                                                        | Not true of the verifier's runs (section 1); true or not of this step's two runs on the preview built from the fixes (section 5), which are the runs of record of this ship                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| The burst with three companions clean                                                                                                                                     | Pass 2's three bursts on the verifier's preview (71 calls each, 0 failed, no write over 5 s, no 429, no 503); run again by this step on its own preview (section 4)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| The round branch carrying only this ship's files                                                                                                                          | `git diff --stat main...features/ship-one` over `.github`, `decks/untitled-*`, `decks/e2e-*`, `scripts/__pycache__`, `.turboslide`, `.vercel`: empty; `docs/gslides-parity/verification-3/{layout-shift.json,layout-shift.md,parity-audit.json}` unchanged against `main` and restored from `HEAD` after the check chain's step 20 rewrote the audit's JSON                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| The objects and Inter file rows red in both runs reverted by the fix round                                                                                                | None was reverted and none needed it: the one objects row red in both runs (`tables.paste.into-cell-spreads`) failed on its driver's last assertion while the product wrote the grid the row describes (pass 2 F.5 finding 5), and its item (B3's paste, FEATURES.md 2.3 item 5) was uncommitted; the driver's line is R9, landed by this step (section 3), and the row's two readings on the preview of record are in section 5. No `fonts.*` row of an Inter file read red                                                                                                                                                                                                                                                                                                                    |
| The logo index snapshot rebuilt and committed (added by the hotfix of 2026-09-24, build/hotfix.md section 9; a precondition of the ships after this one, not of this one) | `node scripts/build-logo-index-snapshot.mjs --store <public store host>` writes `apps/studio/src/server/logo-index.snapshot.json` from the store's real build, or from `icons.json` through the builder when the store holds a fixture's, and `--check` reads the committed file back; the file is committed with the ship                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

## 3. The fixes landed by this step

FEATURES.md 7.2 stops the ship on a core row red in both preview runs unless the fix is small and in a seam, and the eight requests of the fix round were small, written and unlanded. This step applied them as written in the lane notes, on the round branch, as one commit whose body names every hunk (`f1afe1e`, section 7), with two seam changes of its own, and then ran the preview twice again as the rule asks ("a fix lands, the preview runs twice again, then production runs once").

| Request                                                                                        | File and hunk                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Row                                                              |
| ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| R6 (b1.md; pass 1 F7)                                                                          | `packages/chrome/src/useEditorKeys.ts`: `isChromeField` counts an `<input>` as a field by its `type` (`TEXT_INPUT_TYPES`), so a checkbox, a radio or a range lets Cmd+Z through to `edit.undo`; `__tests__/use-editor-keys.test.tsx` gains the case (a checkbox focused, Cmd+Z runs `edit.undo` and is prevented; Cmd+B from a text input still runs nothing)                                                                                                                                                                                           | the F7 hand drive; `formatting.numerals.tabular-row` unchanged   |
| R9 (b3.md)                                                                                     | `apps/studio/e2e/core/documents.spec.ts` line 270: the untouched row reads `['a1', 'b1', 'c1', '']`, the empty cell of the column the paste added, the schema's one cell per column                                                                                                                                                                                                                                                                                                                                                                     | `tables.paste.into-cell-spreads`                                 |
| R13 (b6.md; pass 1 F2)                                                                         | `apps/studio/src/editor/controller.tsx`: `logo.insert` registers with `announce: true`, so the window transport's answer waits until the record has come back over the channel (a resync at once on the blob tier), and `failureSentence` names the logo ("The logo could not be added: …")                                                                                                                                                                                                                                                             | `logos.tailor.find-customer-logo` as a seller does it            |
| R14 (b6.md; pass 1 F2)                                                                         | `packages/chrome/src/dialogs/Tailor.tsx`: `defaultLogoFinder` takes `documentNow` and passes `findCustomerLogo`'s wait over the live document, answering "The … logo is stored but has not reached this presentation yet; try again in a moment" after the bound; `TailorDialog` keeps a ref of the document; the sheet's error line loses a refusal's action id (`refusalSentence`)                                                                                                                                                                    | the same                                                         |
| R15 (b6.md; pass 1 F1)                                                                         | `packages/render/src/blocks/context.ts` `titleMark`; `slide.ts` `titleMarkOf` (the canvas title's mark block from the grammar record's main slot and the kit's mark record); `blocks/misc.ts` `renderMark` draws the kit's picture into that block's box as `img.mark.mark-picture.mark-block`, nothing for `none`, the glyph otherwise and for a picture whose asset the deck lacks; `block-css.ts` the `img.mark-block` rule; `__tests__/title-mark.test.ts` (four cases)                                                                             | `logos.insert.every-slide`                                       |
| R16 (b6.md; pass 1 F5)                                                                         | `packages/mcp/src/http.ts`: `resume` rebuilds a session another instance opened on the POST that lands here, under the same id, from the request itself (the deck in the query, the author in its header, the key from the bearer), feeding the transport its own initialize first; the per key session cap holds on a resume as on initialize (429 with `retry-after`); a GET or a DELETE of an unknown session stays 404; `http.test.ts` gains the two instance case and the cap case, and its unknown session case moves to a GET and a missing deck | `logos.agent.search-insert`                                      |
| R17 (b6.md)                                                                                    | `apps/studio/e2e/core/logos.spec.ts`: `searchHttp` takes `since` and the takedown read of `logos.index.refresh-fixture` names the refresh's `builtAt`; `scripts/probes/core-walk/areas/logos.mjs`: the walk's Tailor step drops the wait for the revision before Apply, the seller's gesture                                                                                                                                                                                                                                                            | `logos.index.refresh-fixture`, `logos.tailor.find-customer-logo` |
| R18 (b6.md)                                                                                    | `packages/schema/src/actions.ts`: `logo.search` gains `since` ("A refresh's builtAt: the answer comes from an index at least as new when the store holds one") and the CLI usage `--since <builtAt>`; `pnpm generate:contracts`                                                                                                                                                                                                                                                                                                                         | the same                                                         |
| Pass 1 F10 (B4's)                                                                              | `scripts/probes/core-walk/areas/logos.mjs`: the walk's read of the stored source file follows the blob tier's 302 instead of stopping at it                                                                                                                                                                                                                                                                                                                                                                                                             | `logos.insert.mono-tint`                                         |
| Pass 2 F.5 finding 3 (B6's)                                                                    | `apps/studio/src/server/logos.ts` `refresh()`: `await revalidate()` in place of `await index()`, so every instance heads the store's index version and adopts its index before the manifest is asked, and a stale copy no longer re lists a slug another instance's refresh dropped                                                                                                                                                                                                                                                                     | `logos.index.refresh-fixture`                                    |
| The outage row's driver (this step's, after two red readings on the outage preview; section 5) | `apps/studio/e2e/core/logos.spec.ts` `logos.index.cached-offline`: the row provokes the outage's failure the way the daily cron would (one refresh, whose manifest fetch fails and writes the previous index with `lastError` and a new `builtAt`) and reads the search with that `builtAt` as `since`, so the answering instance adopts the index whichever it is; the dialog's foot is read after up to three searches and the reading records how many it took                                                                                       | `logos.index.cached-offline`                                     |

Gates on the tree after the fixes, before the first commit: `node_modules/.bin/tsc -b` clean (8.9 s); `node_modules/.bin/vitest run` at the root 384 files, 4,174 passed, 3 skipped, 2 todo (51 s); the named tests of FEATURES.md 7.3 per package (`packages/mcp` `http.test.ts` 9, `packages/render` `title-mark.test.ts`, `theme-css.test.ts`, `canvas.test.ts` 91, `packages/chrome` the keys, Tailor, logo model, parked controls, menu model and font picker files 108 with 2 todo, `apps/studio` `logos.test.ts` and `no-action-id-snackbar.test.ts` 34, `packages/schema` 583, `packages/agent` 260, the scripts project 110); `node scripts/check.mjs --greps` ok; `node docs/readme/what-works.mjs --check` current; `prettier --check` clean over every changed file (the walk table `build/b1/fix-walk-fonts.md` formatted, which clears check step 19's red of pass 2).

## 4. The previews and the runs of record

Written by the ship step's continuation, part A (the preview runs of record), which took over at 12:50Z after the ship agent's forced return. The rules of the header hold for this part too; its one git write is the commit of 4.5, on `features/ship-one`, never `main`, no push.

### 4.1 The tree at the continuation

The branch `features/ship-one` at `0843164`, fourteen commits over `88b68e7` (the lanes, `f1afe1e` the fix round's requests landed by the ship step, `0843164` the parked list named `docs/gslides-parity/focus/ship-f1afe1e.json`, empty before the runs); `main` untouched at `88b68e7` and production serving it. Uncommitted: `apps/studio/e2e/core/logos.spec.ts` (the ship agent's changed `logos.index.cached-offline` driver, 4.3), this note's sections 1 to 3, `docs/gslides-parity/features/build/ship/render-ship-json.mjs` and `shoot-production.mjs`, the four outage ledgers `docs/gslides-parity/focus/verification/features-one-ship-gate-down-run1.json` to `run4.json`; the untracked `.github/` folder, never committed. `docs/gslides-parity/verification-3/parity-audit.json` unchanged against `HEAD` (the pipeline restored it after the check chain's step 20). `.turboslide/e2e.lock` absent; no Playwright, walk probe or gate process alive (`pgrep`), two `ugrep` filters of the ship agent's log pipes aside. The OIDC token in `.turboslide/vercel-dev.env` valid to 2026-09-24T00:49Z. The two previews of the ship agent answer (307 on `/`, the redirect to the pages): `https://turboslide-nt7ym1egb-kl01s-projects.vercel.app` (enforce, `TURBOSLIDE_LOGO_UPSTREAM=fixture`, built from `0843164`, deployed 12:20Z to 12:27Z) and `https://turboslide-qs5ps0cte-kl01s-projects.vercel.app` (enforce, `TURBOSLIDE_LOGO_UPSTREAM=down`, deployed 12:27Z to 12:34Z).

### 4.2 The sweep of the interrupted runs' decks

`deck.list` with the trash through production at 12:55Z (the bearer in the environment, never printed): 71 decks, 55 live and 16 trashed; of 2026-09-23 one, `untitled-20260923-hq0l` "Acme renewal deck v3" (the interrupted walk's scratch deck, live at revision 408, last written 12:48:56Z); the specs' decks of the day gone by their teardowns. `template.list`: `blank`, `gt-brand` and four leftovers of the logos and brand specs' interrupted runs, `core-spec-template-mudyfjmf`, `-mudha5lo`, `-mudhcbhg`, `-mudyqdcx`; the default `blank`. `template.delete` through production at 13:00Z: two answered 200 and two answered 500 with the store's "Vercel Blob: Failed to fetch blob: 403 Forbidden" (the public store's edge on a just written file, the parked templates feature's recorded mechanism, `packages/store/src/blob-templates.ts`); a second call for the two read 404 with the index listing `gt-brand, blank` alone, so the first call had removed them and the 500 came from the index read after it. Templates at 13:01Z: `blank`, `gt-brand`, the default `blank`. The walk's deck: `deck.info`, `deck.trash` and `deck.remove` through production answered 500 on the same 403 for the deck's manifest (three tries over 13:00Z to 13:07Z; production's `/edit/<id>` and `/deck/<id>` read 500 too), while `deck.info` through either preview answered 200 at revision 408, so the deck stood in the store and production's instances met the edge's 403 window on its last write; trashed and deleted forever through the fixture preview at 13:08Z (`deck.trash` 200, `deck.remove` 200), after which `/edit/untitled-20260923-hq0l` answers 404 on the preview and on production and `/deck/untitled-20260923-hq0l` 404 on the preview. Nothing of the earlier rounds' 70 decks or `gt-brand` was touched.

### 4.3 The outage row's driver, `logos.index.cached-offline`

The four outage ledgers on `qs5ps0cte` (`features-one-ship-gate-down-run1.json` to `run4.json`, `--only specs --spec logos`, 88 to 113 s each, `retries` 0; 7 rows: 4 passed, 2 failed, 1 not driven in every run): `logos.picker.recents`, `logos.route.mark-headers`, `logos.index.refresh-dry-run` and `logos.agent.search-insert` passed four times; `logos.index.refresh-fixture` not driven by its rule (the index reads `down`, not the fixture); `logos.cache.open-licence-only` failed four times on the AWS insert reading 500 "thesvg.org did not answer; try again in a minute", which is the outage doing what the deployment asks and not a reading of record for that row (its readings of record are the fixture preview's, 4.7); `logos.index.cached-offline` failed four times, twice under each driver. Read by annotation from `specs.json`:

- Runs 1 and 2, the verifier's driver (12:36Z, 12:38Z): `logo.search` over HTTP answered 200 with the one Figma row from the cache and `updatedAt` 11:50:51Z, `lastError` null, so "the failure is recorded" failed before the insert and the foot were reached. The failure the foot names lives in the memory of the instance that met it (`apps/studio/src/server/logos.ts` `memoryError`, "the failure the instance saw since the index was written") and the shared index carries `lastError` from a failed refresh alone; on a fresh deployment of several instances the search lands on one that has fetched nothing yet (the integrator's finding 15).
- Runs 3 and 4, the changed driver (12:46Z, 12:47Z): one `POST /api/logo/refresh` first, the cron's action, whose manifest fetch met the outage and answered 200 with `lastError` `{ status 0, "thesvg.org did not answer" }` and a new `builtAt` (12:46:00Z, 12:47:43Z), then the search with that `builtAt` as `since`, up to five reads 2.5 s apart. Run 3: every read answered `updatedAt` 11:50:51Z and `lastError` null, so the instance that answered adopted no index at that build within 10 s (`indexSince` heads the store's version at most once per 2 s per instance and adopts the store's index when the version moved; the version it read did not) and the row failed on the same line. Run 4: the search answered `lastError` at 12:46:38Z, the memory of the AWS fetch run 3's `logos.cache.open-licence-only` had failed on that instance a minute before, not the refresh's 12:47:43Z, with `updatedAt` still 11:50:51Z; the Figma insert passed (`figma-2`, 200); the dialog's foot after four searches read "Logos from thesvg.org, updated 23 September 2026. Brand marks belong to their owners; use them to name the brand, not to imply endorsement" with no failure sentence, and the row failed on "the foot names the failure".

The decision: the changed driver does not read the product truthfully. It takes an action the seller never takes (the refresh) to provoke the sentence, loops the seller's search to catch an instance that remembers a failure, and still reads whichever instance answers; its one green claim beyond the verifier's (the insert) the verifier's driver reads too. The verifier's driver is restored (`git checkout -- apps/studio/e2e/core/logos.spec.ts` at 12:58Z; the changed hunk kept beside this part's scratch files as `cached-offline-driver-change.diff`, not in the tree), and the row is recorded as FEATURES.md 7.2 reads it: not driven by design on the deployment of record (the fixture preview, where the spec skips it with the unit test named, `apps/studio/src/server/logos.test.ts`, the cache during an outage; the precedent of `assist.quota.429`), read on the outage preview twice more with the verifier's driver (4.8) and recorded there as what it reads, never as passed. The per instance failure memory (finding 15) is B6's item in the round after: a failed mark fetch records `lastError` on the shared index, throttled like `persistSoon`, or the foot reads the refresh route's answer. The product's claim of FEATURES.md 4.9 that the outage runs read green in all four: `logo.search` answers from the cache during the outage (200, the Figma row) and a cached open licence mark inserts (run 4; runs 1 to 3 never reached the insert behind the failed assertion).

### 4.4 The banner after the tab's own server side write, and the fix

The ship agent's seller visible finding (severity 2, no row): on the blob tier the announce path's resync after `asset.add` and `logo.insert` showed the banner "Revision rN arrived from outside this editor and is shown. Reload" and cleared the undo history. Read at the code: `apps/studio/src/editor/controller.tsx` `serverSide(id, { announce: true })` runs the action on the server, and when the write has not come back yet on the blob tier it calls `room.resync()` at once instead of waiting for the channel's one second poll; `onResync` reads the reloaded document's revision against `latest().serverRevision`, the revision this tab acknowledged, and `resyncBroughtUnseen(fresh, known)` (`resync-history.ts`) read true because the tab had acknowledged nothing above its last write while the store had committed the action's revision, so the history and its clocks were cleared and `showExternal` named the tab's own write. The precedent in the same file: `version.restore` publishes `serverRevision: result.revision` before `room.resync(result.revision)`, and the assist accept path publishes `Math.max(serverRevision, answer.revision)` after its server side write.

The fix, one guarded change in the announce branch: before `room.resync()`, the tab counts the answer's `revision` as acknowledged (`publish({ serverRevision })` when it is a finite number above the known one), through the new pure helper `acknowledgeAnswered(known, answered)` in `resync-history.ts`, so a reload landing at the answered revision brings nothing unseen (the history stays, no banner) and one landing above it (another writer's entry with it) still clears and shows the banner. The memory tier's path (no resync; the stream brings the entry) is untouched, and `landed()` reads the document's own revision, so the wait is unchanged. `resync-history.test.ts` gains four cases: the own write answered r9 with the reload at r9 (no banner), another writer's entry with the reload at r10 (banner), an answer without a revision (`undefined`, `null`, a string, `NaN`) changes nothing, an answer at or below the known revision never lowers it. Gates at 13:00Z: `node_modules/.bin/tsc -b` clean (3.5 s); `apps/studio` `vitest run` 71 files, 530 passed (7.3 s); `packages/realtime` `vitest run` 10 files, 136 passed (10.7 s); `prettier --check` clean over the three files. The change is read on the deployment of record by the walk's `images.*` and `logos.insert.*` rows (every picture insert on the blob tier passes this branch) and by `banner-probe.mjs` (4.6).

### 4.5 The commit

`2b1d851` on `features/ship-one` at 13:03Z, "Turboslide features round, ship one: the last driver, the outage readings and the ship note", by an explicit path list: `apps/studio/src/editor/controller.tsx`, `resync-history.ts`, `resync-history.test.ts` (4.4), this note as far as 4.4, `docs/gslides-parity/features/build/ship/render-ship-json.mjs` and `shoot-production.mjs`, the four outage ledgers `features-one-ship-gate-down-run1.json` to `run4.json`; `apps/studio/e2e/core/logos.spec.ts` carries no change after 4.3 and is not in it; never `.github/`, never `parity-audit.json`, no file carrying a token or cookie (the ten files scanned for token shapes before the add). Fifteen commits over `88b68e7`; `main` untouched; no push. The sections after this one are written after the commit and stay for the ship step's part B.

### 4.6 The deployment of record and its first reads

A source file changed in 4.4, so a fresh enforce fixture preview was deployed from the committed tree `2b1d851` with the recorded command (`vercel deploy --yes --archive=tgz` from the repository root, `TURBOSLIDE_AUTHORIZE=enforce`, `TURBOSLIDE_ASSIST=fixture`, `TURBOSLIDE_LOGO_UPSTREAM=fixture`, `TURBOSLIDE_MAIL=off`, `TURBOSLIDE_REALTIME=blob`, production's public store host, the two secrets minted inside the command and dropped from the log; never `--prod`, no project setting, no env command), 13:03:46Z to 13:07:19Z: `https://turboslide-6yg7b2f01-kl01s-projects.vercel.app` (`dpl_B4kPjQeR6m8SMkQMCif1aZuWeQMK`, behind Vercel Authentication, the one Blob store production uses). It is the deployment of record of this ship; `nt7ym1egb` (built from `0843164`, one interrupted run) and `qs5ps0cte` (the outage preview, `TURBOSLIDE_LOGO_UPSTREAM=down`, also from `0843164`; the spec and the docs are the only difference from `2b1d851` besides 4.4, which touches no logo route, so its outage readings of 4.8 stand for the ship) keep their readings as what they were.

| Read                                                     | Command and time                                                                                              | Result                                                                                                                                                                                                                                    |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The pages                                                | `GET /` and `/new` with the OIDC header (13:07Z)                                                              | 307 and 200 (the editor page, 5.3 s on the cold function)                                                                                                                                                                                 |
| The hosted smoke                                         | `node scripts/hosted-smoke.mjs --base <preview> --token-env TURBOSLIDE_TOKEN` (13:07:50Z, 18 s)               | 40 of 40 passed                                                                                                                                                                                                                           |
| The new write probe                                      | `node scripts/probes/new-write-probe.mjs --base <preview>` (13:08:08Z, 43 s)                                  | all steps ok on the blob tier; its deck `untitled-20260923-1cgn`'s successor trashed and deleted forever by the probe                                                                                                                     |
| The banner probe, asset.add then logo.insert             | `banner-probe.mjs --base <preview>` (13:08:51Z; the scratch deck `untitled-20260923-rpyd`, gone, `/edit` 404) | after `asset.add` (answered in 1,183 ms) the banner "Revision r2 arrived from outside this editor and is shown. Reload" at revision 2; after `logo.insert` (1,052 ms) revision 3 and no new banner (the r2 banner still on its 8 s timer) |
| The banner probe, asset.add alone with the answer's keys | `banner-probe-2.mjs` (13:10Z; `untitled-20260923-efno`, gone)                                                 | the answer's keys `id, role, alt, twins, size, scale, source, inline`, no `revision`; the banner at 1,014 ms with revision and serverRevision 2 together, standing at 6 s                                                                 |
| The banner probe, logo.insert alone                      | `banner-probe-3.mjs` (13:11Z; `untitled-20260923-n9zl`, gone)                                                 | the answer's keys `asset, revision, variant`, `revision` 2; revision and serverRevision 2 at 757 ms, no banner in 16 samples over 4 s and none at 6 s                                                                                     |

So the fix of 4.4 holds where the answer names its revision: `logo.insert` (the seller's Insert > Logo, Tailor's Find the customer's logo and the agent's insert) no longer shows the banner or clears the history on the blob tier. It is inert for `asset.add` (a picture uploaded or captured through the window API and the dither), whose answer is the asset record alone (`packages/materials/src/actions.ts` `commitAssetsAtHead`; the output schema in `packages/schema/src/actions.ts`, the integrator's file), so `acknowledgeAnswered(known, undefined)` changes nothing and the tab's own resync still lands one revision above what it acknowledged. The rest is Kevin's list and the round after, larger than this part's one guarded change: either `asset.add`'s answer carries `revision` (a field in the integrator's schema file and the generated contracts) or the announce branch flags its own resync with the asset ids it asked for and `onResync` reads a reload at exactly one revision above the acknowledged one that holds every one of them as the tab's own. The gate's rows on the deployment of record read the change through every picture insert of the walk and the specs (4.7).

### 4.7 The two gate runs of record on `6yg7b2f01`

`node scripts/probes/core-gate.mjs --base https://turboslide-6yg7b2f01-kl01s-projects.vercel.app --out <scratch>/ship/gate-preview-a-run<n> --parked docs/gslides-parity/focus/ship-f1afe1e.json --matrix docs/gslides-parity/focus/verification/features-one-ship-preview-run<n>.json`, both tokens in the environment through `with-tokens.mjs` (never printed), each as a detached process whose log was read at most once every ten minutes; no code change between the runs, no other load on the deployment from this machine beside the outage preview's own spec runs, which ran after both (4.8). The default driver set (the walk probe, the core specs and the cost probe with its three minute windows). The store was swept between the runs (the four `core-spec-template-*` leftovers of run 1's brand and logos specs deleted at 15:21Z; no deck of the day remained, the walk's and the specs' teardowns having removed theirs); run 2's decks spec left its four fixtures (`northwind-renewal-v4-without-notes`, `import-bundle-source-muebktjs`, `untitled-20260923-e9tv` "Northwind renewal", `untitled-20260923-s72s` "Thumbnail deck") and one template, all trashed and deleted forever at 00:52Z with `/edit` 404 for each; `deck.list` with the trash through production at 00:58Z read 70 decks (54 live, 16 trashed), none of 2026-09-23 or 2026-09-24, the earlier rounds' 70 untouched, and `template.list` read `blank`, `gt-brand` with the default `blank`. The ledger copies are `docs/gslides-parity/focus/verification/features-one-ship-preview-run1.json` and `run2.json`; the walk tables, the Playwright reports with the failed rows' traces and pictures, and the cost probe's JSON sit under the scratch folders of the runs.

| Run | Started, length    | Rows                                                                                                                                                                                     | Walk                                                                                                                                         | Specs                                                       | Cost probe                                                                                                        |
| --- | ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| 1   | 13:12:41Z, 7,652 s | 785: 749 passed, 19 failed, 17 not driven (1 manual), 0 no step; `retries` 0; cost rows over their ceiling 1 (`cost.two-tabs-idle.calls`); verdict failed under the empty committed list | 572 matrix rows: 555 passed, 6 failed, 11 not driven; 622 steps, 3,054 s, 83 console lines (every one a 404 of a trashed deck's card render) | 190 passed, 12 failed, 5 skipped by their own rule; 3,384 s | 6 rows: 4 passed, 1 failed (the measurement row above), 1 not driven; 1,214 s; 7 instances answered `sync.status` |
| 2   | 15:21:55Z, 8,196 s | 785: 751 passed, 18 failed, 16 not driven (1 manual), 0 no step; `retries` 0; cost rows over their ceiling 0; verdict failed                                                             | 572 matrix rows: 555 passed, 6 failed, 11 not driven; 622 steps, 3,218 s, 168 console lines (the same 404s)                                  | 191 passed, 12 failed, 5 skipped; 3,697 s                   | 6 rows: 5 passed, 1 not driven; 1,281 s; 4 instances                                                              |

The ship one rows read green in both runs, by area, from the ledgers by id: every `tables.*`, `charts.*`, `diagrams.*`, `shapes.*`, `wordart.*` and `arrange.*` row that is on the build (`tables.paste.into-cell-spreads`, red in both of the verifier's runs, passed twice on R9's line); every `fonts.*` row of the Inter files, the fallback, the display features and the eight families (`fonts.export.editable-names-face` red once, 5.2); every `logos.*` row on the build but `logos.picker.search` once and `logos.index.refresh-fixture` once (5.2): `logos.insert.every-slide` (R15; the title slot draws the mark, a new slide's footer draws it, Cmd+Z removes it whole, the check off in a fresh dialog), `logos.agent.search-insert` (R16; the CLI, HTTP and the MCP session on a second instance), `logos.insert.mono-tint` (the F10 driver; Stripe's mono tinted to the kit's text colour on ink, AWS's CC BY-ND mark unmodified), `logos.tailor.find-customer-logo` (R13, R14, R17), `logos.picker.recents`, `logos.route.mark-headers`, `logos.index.refresh-dry-run`, `logos.cache.open-licence-only`, `logos.replace-image.row`, `logos.insert.one-click-asset`, `logos.insert.logo-size`. The P1 rows whose controls ship one does not build read not driven with the control id and the FEATURES.md item named, in both runs: `arrange.group.tail-text-controls`, `tables.seam.row-drag`, `tables.edge.add-row-column`, `tables.heads.select-row-column`, `tables.bar.row-column-buttons`, `wordart.tail.fill-outline`, `brand.objects.kit-colours-first`, `fonts.picker.specimen-rows`, `fonts.table.takes-family`, `logos.picker.variants`, `logos.kit.find-a-logo`; not driven by their rule in both: `logos.index.cached-offline` (the fixture preview; 4.3), `assist.quota.429` (no Upstash), `sync.resend.idempotent` (deployment N's record carries no origin), `cost.editor-hidden.calls` (headless Chromium never reads hidden), `text.clipboard.paste-without-formatting` (the manual checklist's).

### 4.8 The outage preview, runs 5 and 6

`node scripts/probes/core-gate.mjs --base https://turboslide-qs5ps0cte-kl01s-projects.vercel.app --only specs --spec logos --parked docs/gslides-parity/focus/ship-f1afe1e.json --matrix docs/gslides-parity/focus/verification/features-one-ship-gate-down-run<n>.json`, the verifier's driver (4.3), after both gate runs, with the OIDC token refreshed at 00:50Z (`vercel env pull .turboslide/vercel-dev.env --environment development --yes`; the earlier token expired at 00:49Z; the new one expires 2026-09-24T12:50Z). Run 5 (00:53:00Z, 98 s): 7 rows, 4 passed, 2 failed, 1 not driven; `logos.index.cached-offline` failed on "the failure is recorded" (the search answered 200 with the Figma row, `updatedAt` 16:55:13Z and `lastError` null: a fresh instance with no failure in its memory, finding 15 as in runs 1 to 4). Run 6 (00:55Z, 83 s): 7 rows, 5 passed, 1 failed, 1 not driven; `logos.index.cached-offline` passed: the search answered `lastError` at 00:53:56Z (the memory of the AWS fetch run 5's open licence row had failed on that instance), the Figma insert 200 (`figma-2`), the foot "Logos from thesvg.org, updated 23 September 2026. Brand marks belong to their owners; use them to name the brand, not to imply endorsement. thesvg.org did not answer at 00:53 UTC". Across the six outage runs the row read red five times and green once, each reading decided by which instance answered and whether it had met the outage, which is finding 15 measured; the row's reading of record is the deployment of record's, not driven by design with the unit test named (4.3). `logos.cache.open-licence-only` failed in all six outage runs on the AWS insert answering 500 "thesvg.org did not answer; try again in a minute", the outage refusing the fetch of a mark the cache may not hold (a CC BY-ND mark is never cached), which is the product doing what FEATURES.md 4.9 says and not a reading of record for that row (its readings of record, 4.7, passed twice). `logos.index.refresh-fixture` not driven there by its rule in every run. The spec's decks were removed by its teardown (the store check of 4.7 at 00:58Z read none).

## 5. The rows by run and the verdict

### 5.1 Red in both runs

By FEATURES.md 7.2 and the rule this part was given (a core row of an earlier round red in both runs is a blocker unless it is one of Kevin's two standing rows), with the mechanism read from the runs' JSON, traces and pictures:

| Row                               | Feature, round            | Run 1                                                                                                                               | Run 2                        | Mechanism                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Consequence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --------------------------------- | ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `sync.title.concurrent-both-kept` | sync, the sync round      | failed (round 2 after Escape: A and B read the same interleaved title, "within 5 s false, after 3 s more false")                    | the same                     | Kevin's standing row                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | recorded as standing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `brand.logo.remove`               | brand, the product round  | failed: record `mark.kind none`, footer none, show entered with no footer logo and no slide mark, but "title slide mark drawn true" | the same                     | R15 (`packages/render/src/blocks/misc.ts` `renderMark`) emits, for a kit whose mark is `none`, an empty `<div class="mark-block is-none" data-slot="mark" aria-hidden>` that keeps the block's box, and the walk's editor read counts any `.mark, [data-slot="mark"]` with a client rect as drawn (`scripts/probes/core-walk/areas/brand.mjs` 367 to 369) while its show read requires children and reads no mark. The product draws nothing; the driver reads the placeholder. The row was never read on a tree with R15 before these runs (R15 landed after the verifier's pass 2 and the ship agent's run stopped at row 224)                                                                                                       | a blocker by the rule as written; the seam is one line in the walk's brand area (the editor read requires content, `mark.children.length > 0` or an `img`/`svg`, as the show read does) or, in the render, no element for `none`; a driver change, so the deployment stands and the brand area reruns twice                                                                                                                                                                                                            |
| `assist.rewrite.card-accept-undo` | assist, the product round | failed "the body changed" (7.5 s)                                                                                                   | the same (7.5 s)             | the card came in 365 ms and Accept's `POST /api/assist` answered 200 in 558 ms (run 2's trace, 16:15:41.6Z), but the accept's record reached the tab's stream only at 16:15:48.6Z (`stream?since=4`), after the controller's 5 s wait for the entry (`controller.tsx`, the assist accept path: "the stream brings the entry back within a moment") and after the spec's read; the write landed on the assist route's instance and the tab's instance announced it at its next tick. Green in both of the sync round's preview runs and on production; no commit of this round touches `packages/realtime`, `packages/store` or the controller's assist path (the diff against `88b68e7` is the logo route and a file store test there) | a blocker by the rule; the seam is the blob channel's announce cadence when the deck's writer sits on another instance (`packages/realtime/src/blob.ts`, the quiet tick `HOSTED_POLL_QUIET_MS` and the active window), or the controller's accept path resyncing at once on the blob tier when the entry has not come back, the pattern of the announce branch; the row carries `parks` (`panel.assist.starter.shorter`), so RETURN.md rule 2's letter would park the starter instead, a decision for part B and Kevin |
| `sync.viewer.live-updates`        | sync, the sync round      | failed: C read the words in 5,073, 5,077, 3,064 ms                                                                                  | failed: 5,162, 577, 1,104 ms | the first word (and run 1's second) lands 73 to 162 ms past the 5 s bound and the later words in 0.6 to 3 s: C's instance holds no writer, so it learns of A's write at its quiet tick and moves to the active cadence after; the sync round read 529 to 4,073 ms on its preview and 1,047 ms on production. The same cadence as the assist row's and `share.dialog.co-edit-from-copied-link`'s 17.8 s (5.2)                                                                                                                                                                                                                                                                                                                           | a blocker by the rule (an unparkable feature, red in both); the seam is the same quiet tick, or a viewer's instance polling at the active cadence while the presence store names an editor on the deck                                                                                                                                                                                                                                                                                                                 |

### 5.2 Red once

| Row                                                            | Red in | Mechanism                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Class                                                                                                                                                                                                                                                                                                                                              |
| -------------------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `decks.card.move-to-trash-undo`                                | 1      | 60 s test timeout while the card page navigated `/decks` to `/decks/trash` and waited for the snackbar's Undo (the trace's call log)                                                                                                                                                                                                                                                                                                                                                                              | the listing page's tail on the blob tier (the sync round's `decks.trash.delete-forever-button`, pass 2 F13); recorded, not chased                                                                                                                                                                                                                  |
| `decks.editor.move-to-trash`                                   | 1      | File > Move to trash; `/decks` not reached in 20 s                                                                                                                                                                                                                                                                                                                                                                                                                                                                | the same class; recorded                                                                                                                                                                                                                                                                                                                           |
| `decks.nav.back-forward`                                       | 1      | `page.waitForFunction` 90 s across `/home`, `/decks`, `/edit`                                                                                                                                                                                                                                                                                                                                                                                                                                                     | the editor and listing load tail (pass 1 F16, pass 2 F9); recorded                                                                                                                                                                                                                                                                                 |
| `decks.new.first-write`                                        | 2      | the first write of the walk's fresh draft: stored true, revision 2, the save word stayed "Saving…"                                                                                                                                                                                                                                                                                                                                                                                                                | the blob tier's acknowledgement at the run's start; recorded                                                                                                                                                                                                                                                                                       |
| `decks.title.save-words`                                       | 2      | the second edit read "Couldn't save, retrying"                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | a write refused one revision low by another instance's mirror and re-sent, the return round's measured class; recorded                                                                                                                                                                                                                             |
| `images.insert.first-on-new-deck`                              | 2      | 90 s timeout with the page still at "Loading the editor" (the error context's snapshot)                                                                                                                                                                                                                                                                                                                                                                                                                           | the editor load tail on the blob tier (pass 2 F9's 90 s stall); recorded                                                                                                                                                                                                                                                                           |
| `share.dialog.co-edit-from-copied-link`                        | 2      | the first browser read the second's six words 17,792 ms after they were typed (the bound 10 s)                                                                                                                                                                                                                                                                                                                                                                                                                    | the blob tier's cross instance delivery (the return round's `collab.*` class); recorded, and the same cadence as 5.1's two rows                                                                                                                                                                                                                    |
| `brand.export.pdf-logo`                                        | 1      | the spec's `asset.add` with `baseRevision` 19 refused by the page's own base check: "the document is at revision 20", the previous test's write acknowledged between the spec's state read and its write                                                                                                                                                                                                                                                                                                          | the blob tier's acknowledgement timing; recorded                                                                                                                                                                                                                                                                                                   |
| `fonts.export.editable-names-face`                             | 1      | the export's POST answered 200 in 5.3 s (14:34:26Z) and the job's status was polled every 20 s for 90 s with no download event (the trace's network)                                                                                                                                                                                                                                                                                                                                                              | the export job's tail on the deployment (the product round's export readings, the sync round's `export.png.current-slide`); recorded                                                                                                                                                                                                               |
| `logos.index.refresh-fixture`                                  | 2      | refresh 1 answered 10 icons, fetched 1, dropped none (the index before it lacked `northwind` since run 1's takedown, so the fixture's manifest listed it again and the refresh re fetched it); refresh 2 answered dropped none: it ran on an instance whose held copy still lacked the slug, so the fixture's manifest listed all ten and nothing was dropped (`fixtureUpstream` decides from the answering instance's held index; `revalidate()` heads the store's version and the version had not moved for it) | the store's version head lagging the write, the CDN lag class; recorded (finding 20 is its neighbour)                                                                                                                                                                                                                                              |
| `cost.two-tabs-idle.calls` (measure)                           | 1      | `list` 1 in the window, the folder `assets` (the second tab's open on its instance; advanced 19 against 30); run 2 `list` 0, advanced 18                                                                                                                                                                                                                                                                                                                                                                          | a measurement row: recorded, never holding the ship; the same reading as the sync round's preview                                                                                                                                                                                                                                                  |
| `logos.picker.search`                                          | 1      | the one result tile drew 338 ms after the last key against the 300 ms bound (the dialog waits 200 ms after the last key before it fetches, `dialogs/Logo.tsx` 327 and 652, so the round trip had 100 ms); Figma preselected, Enter inserted it, the dialog closed; run 2 within the bound. The foot named "thesvg.org did not answer at 12:47 UTC": the shared index carried the outage preview's refresh of 4.3 (one `system/logo-index.json` for every deployment of the store)                                 | not one of the two recorded classes: flaky under FOCUS.md 6.2, a blocker until a rerun after a named code change reads it green in both runs. The change named and not made: the dialog fetches on the keystroke with the last answer winning, or its pause drops under 200 ms, so the 300 ms budget is not spent 200 ms before the request leaves |
| `text.link.slide-target`                                       | 2      | after Insert link > Slides in this presentation > Slide 3 the slide's JSON carried no `link.slide` 200 ms later, and no ops POST of the trace carries a link (16:57:37Z to 16:58:11Z): the menu path wrote nothing                                                                                                                                                                                                                                                                                                | not one of the two classes: flaky under 6.2, a blocker until a rerun after a named code change; no product change is named, because the write never left the page and the trace does not say whether the submenu's row took the click                                                                                                              |
| `templates.card.rename-and-delete`, `templates.deck.read-only` | 1, 2   | the parked templates feature                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | carried park                                                                                                                                                                                                                                                                                                                                       |

### 5.3 The carried parked list and the not driven rows

The previous ship's list (`ship-7ea3db9.json`) read as before: `inbox` red in both runs on `inbox.bell-panel-toggle`, `inbox.settings-persist` and `inbox.notification-arrives`; `templates` red in both on `templates.default.use-for-new` and `templates.gallery.page` (`templates.card.rename-and-delete` once, `templates.deck.read-only` not driven then failed); `view.live-pointers.second-browser` and `versions.show-changes-marks` red in both; `collab.roster.go-to-slide` passed in both runs and leaves the list by the render's rule. The not driven rows are 4.7's list; `decks.file.open-list-search`, Kevin's other standing row, passed in both runs.

### 5.4 The cost rows beside their ceilings

| Row                         | Run 1                                                       | Run 2                | Ceiling            | The sync round's production reading |
| --------------------------- | ----------------------------------------------------------- | -------------------- | ------------------ | ----------------------------------- |
| `cost.editor-idle.calls`    | function 9.66 a minute; store simple 31, advanced 10        | 9.65; 30, 10         | 12; 40, 11         | 9.66; 28, 9                         |
| `cost.editor-editing.calls` | 69.92 (210 in 3 min); simple 120, advanced 75               | 72.92 (219); 137, 75 | 75; 140, 85        | 68.60                               |
| `cost.two-tabs-idle.calls`  | advanced 19; `list` 1 (`assets`, the open)                  | 18; `list` 0         | 30; 0              | `list` 0 in the window, advanced 15 |
| `cost.show.calls`           | 0 function requests in the window; the page's store calls 0 | the same             | 0; 0               | 0                                   |
| `cost.editor-hidden.calls`  | not driven by its rule                                      | the same             | 8                  | not driven                          |
| `sync.pull.no-listing`      | passed                                                      | passed               | `lists.versions` 0 | passed                              |

Every cost row holds under its ceiling in both runs but the one `assets` listing of run 1, the reading the previous ship recorded too. The editing row's function requests read 69.92 and 72.92 against production's 68.60 at the last ship (the verifier's 68.60 and the preview's 68.93 then): 4 to 13 more requests over a three minute window, under the ceiling, and its simple store calls 137 in run 2 against 140; part B's production run reads these again and the rise, if it holds there, is Kevin's list.

### 5.5 The verdict of this part

The features of ship one read green on the deployment of record: every objects row on the build in both runs, every fonts row but one export tail read once, every logos row on the build but the picker's 338 ms once and the fixture takedown once. No objects item, no Inter file and no logos feature is red in both runs, so no revert and no logos park follows from the rule. Three core rows of earlier rounds are red in both runs and are blockers by the rule as this part was given it: `brand.logo.remove` (the driver's read of R15's placeholder, the product drawing nothing), `assist.rewrite.card-accept-undo` and `sync.viewer.live-updates` (the blob channel's cross instance cadence at the bound, the realtime code untouched by this round); `sync.title.concurrent-both-kept` is standing. Two rows red once fall outside the two recorded classes (`logos.picker.search`, `text.link.slide-target`) and are flaky under FOCUS.md 6.2 until a rerun after a named code change; the rest of the red once rows are the blob tier's timing and are recorded. The decision on the blockers is Kevin's and part B's; this part made no further change and stopped here.

## 6. The parked list

Rendered by the rule from the two runs, with the exceptions the ship rule makes over the gate's mechanical `wouldParkRows`:

- `parkedFeatures`: `inbox`, `templates` (carried; red in both runs again). Not `brand`, `fonts` or `assist`, which the gate's `wouldPark` names: `brand`'s and `assist`'s red rows are earlier rounds' core rows (5.1, blockers, never parks of a feature in the default view), and `brand`'s and `fonts`' other entries are P1 rows not on the build.
- `parkedRows`: carried `view.live-pointers.second-browser` (`view.livePointers.mine`, `view.livePointers.collaborators`) and `versions.show-changes-marks` (`file.versionHistory.showChanges`); `collab.roster.go-to-slide` leaves the list (green in both runs). The P1 rows of this round whose controls ship one does not build, listed so the gate reads them behind the list while their ids draw nothing: `arrange.group.tail-text-controls` (`toolbar.group.text`), `tables.seam.row-drag` (`handle.table.row`), `tables.edge.add-row-column` (`handle.table.add.column`, `handle.table.add.row`), `tables.heads.select-row-column` (`handle.table.head.column`, `handle.table.head.row`), `tables.bar.row-column-buttons` (`bar.table`), `wordart.tail.fill-outline` (`toolbar.wordart.outline`), `logos.picker.variants` (`dialog.logo.kind.wordmark`, `dialog.logo.tone.mono`), `logos.kit.find-a-logo` (`panel.brand.logo.find`).
- Not parked though the gate's `wouldParkRows` names it: `logos.index.cached-offline` (`insert.logo`), not driven by design on the deployment of record (4.3, 4.8); parking `insert.logo` would hide Insert > Logo for a row the fixture preview cannot drive. `assist.rewrite.card-accept-undo` (`panel.assist.starter.shorter`) is a blocker in 5.1, not a park, unless Kevin takes RETURN.md rule 2's letter.
- Reverts: none.

## 7. The fix part: the three seams and the picker fetch

Written by the ship step's continuation, part B's fix part, which took over at 01:10Z on 2026-09-24 after part A stopped on the ship rule (5.5). The rules of the header hold; the git writes of this part are the commit of 7.7 on `features/ship-one` (never `main`, no push). The tree it found: the branch at `2b1d851`, fifteen commits over `88b68e7`, `main` untouched and production serving it; uncommitted, this note's sections 4.5 to 6 and the four ledgers `features-one-ship-preview-run1.json`, `run2.json`, `features-one-ship-gate-down-run5.json` and `run6.json`; the untracked `.github/` folder, never committed; `parity-audit.json` unchanged against `HEAD`; no Playwright, walk or gate process alive (two `ugrep` filters of earlier log pipes aside); the OIDC token valid to 2026-09-24T12:50Z. Every reading below is first hand: the two runs' traces were unzipped and their network logs read by request, the deployment's runtime logs were read with `vercel logs -d <deployment> --since --until --json` (a read; nothing on Vercel changed), and every gate ran on this machine.

### 7.1 `brand.logo.remove`: the walk's read

The walk's editor read in `scripts/probes/core-walk/areas/brand.mjs` (the `brand.logo.remove` step) counted any `.mark, [data-slot="mark"]` element with a client rect as a drawn mark. It now counts a mark as drawn only when the element carries content: it is an `img` or an `svg` itself (the kit's picture is the `img.mark.mark-picture[data-slot="mark"]` of `render/slide.ts` and `renderMark`, the default glyph an `svg.mark`), or it holds an `img` or `svg`, the way the same step's show read requires children. The empty `div.mark-block.is-none[data-slot="mark"]` R15 emits for a kit whose mark is `none` reads as not drawn.

The render is left as it is, on purpose. `renderMark` (`packages/render/src/blocks/misc.ts`) emits the empty element so the canvas title's mark block keeps its box: the editor finds a block by its `[data-block]` element (`packages/viewer/src/Selection.tsx` for the click and the Tab walk, `canvas-measure.ts` and `render/measure-dom.ts` for the boxes, `Editor.tsx` 2256 and 2341 waiting for every wanted id's element), so a `none` kit rendered as no element would leave the mark block in the document with nothing to select, measure or move until Use the default logo brings the glyph back, against the standing rule that anything on the sheet can be dragged. The show and the exports read the slot by content already: the show's stage draws nothing for an empty `div`, `packages/export/src/scene/extract.ts` tags `img.mark-picture[data-slot="mark"]` alone and `kit-logos.ts` reads the kit's `mark.kind`, so no surface but the walk's editor read took the placeholder for a logo. `packages/render/src/__tests__/title-mark.test.ts` pins the empty block for `none` (its case "draws nothing for none, as an empty mark block in the box"); a render change would have been a change to a green surface for a driver's misread. This is a driver change, so the deployment of record could have stood for this row alone; the resync and pulse changes below rebuild it anyway.

### 7.2 The tab's own server side write on another instance: the resync at once

The mechanism of `assist.rewrite.card-accept-undo` (5.1): Accept's `POST /api/assist` answered 200 in 558 ms on one instance and the record reached the tab's stream about 7 s later, after `acceptAssist`'s 5 s wait for the entry (`controller.tsx`), because the tab's stream instance had no op of its own and ticked at the quiet cadence. The same shape sat in `slide.import` (a 5 s wait, then a reload) and, without the wait, in the announce branch part A built for `asset.add`, `asset.dither`, `material.capture` and `logo.insert` (a resync at once on the blob tier).

The change: one helper in `apps/studio/src/editor/controller.tsx`, `settleOwnWrite(revision, landed)`, that every window transport write which runs on the server and answers goes through. It waits `OWN_WRITE_STREAM_WAIT_MS` (250 ms) for the stream to bring the entry, the time a commit on the tab's own instance takes; past it, on the blob tier, it counts the answered revision as acknowledged (part A's `acknowledgeAnswered`, so the reload lands at a revision the tab asked for, the undo history stays and no banner names the tab's own write as external) and calls `room.resync()` at once; then it waits for the entry up to `OWN_WRITE_LANDED_MAX_MS` (15 s). The memory tier keeps the wait alone (its follower streams the write within milliseconds; a reload there would cost a read for nothing). The rule is the pure `resyncsForOwnWrite({ landed, tier, waitedMs })` in `resync-history.ts` beside the two constants. The paths on it: the announce branch of `serverSide` (`asset.add`, `asset.dither`, `material.capture`, `logo.insert`; the 250 ms wait replaces the resync at once, so a commit on the tab's own instance no longer costs a reload), `acceptAssist` (the 5 s wait replaced; the history entry is pushed after the settle as before, and `ownAssistNotes` keeps the follower's banner off the assistant's own entry), and the `slide.import` handler (the 5 s wait and the unconditional reload replaced; the reload stays as the last resort when the entry never came, and the private `awaitRevision`, which nothing else called, is gone). `version.restore` (`commitServerFirst`) already publishes the answered revision and resyncs at once with `atLeast`, so it is untouched. `deck.tailor` is not a server side write: its handler plans in the page and commits through the room client (`apps/cli/src/store-actions.ts` `deckTailor` over `storeDeps`), so its entries are the tab's own ops and acknowledged by id; the server side write Tailor makes is Find the customer's logo, `logo.insert`, which is on the helper.

The B2 note's acknowledgement rule (`docs/gslides-parity/sync/build/b2.md` section 1: the room client settles a pending op by the record's `covers` and its own id, `settledOps` drops a repeated echo, a resync's `since` answer names the op ids the records above it carry) is untouched: the helper publishes the controller's `serverRevision` alone, only on the blob tier and only before a reload it asks for, as part A's announce branch did; the room client's acknowledged position moves as before. `asset.add`'s answer still carries no revision (`packages/materials/src/actions.ts` `commitAssetsAtHead`; the output schema in `packages/schema/src/actions.ts`), so its reload still lands one revision above what the tab acknowledged and its banner stays on Kevin's list (4.6).

`resync-history.test.ts` gains four cases: the reload on the blob tier once the wait ran out without the entry, no reload while the wait runs, never for an entry that landed or on the memory tier, and the bound pinned under a second and above zero with the landed bound above it.

### 7.3 `sync.viewer.live-updates`: the roster at the stream open

The mechanism, read from the two runs' traces (`gate-preview-a-run<n>/playwright/core-sync-sync-viewer-live-*/trace.zip`, the `0-trace.network` and `1-trace.network` request logs; times from each trace's first request):

| Run | A's stream, A's first presence | C's `/edit` load, stream, first presence | A's first word (ops POST) | Read at C                                                                           | A's second and third words |
| --- | ------------------------------ | ---------------------------------------- | ------------------------- | ----------------------------------------------------------------------------------- | -------------------------- |
| 1   | 3,216 ms; 3,872 ms             | 6,265 ms; 7,042 ms; 7,157 ms             | 7,868 ms (458 ms)         | not within 5 s (5,073 ms); the second not within 5 s (5,077 ms); the third 3,064 ms | 13,541 ms, 19,223 ms       |
| 2   | 4,335 ms; 5,086 ms             | 8,252 ms; 9,098 ms; 9,228 ms             | 10,184 ms (875 ms)        | not within 5 s (5,162 ms); the second 577 ms; the third 1,104 ms                    | 16,450 ms, 17,747 ms       |

In both runs C's stream opened on an instance that then read nothing for about ten seconds: run 1's first two words were read together at the quiet tick (the second typed 5 s after the first was read late, and read at the same tick), and run 2's second word arrived in 577 ms because its POST landed on C's own instance (`append` announces the records committed elsewhere first, C3S-F8, so C read both words at once). The deployment's runtime logs for both windows (14:55Z to 15:01Z, 17:12Z to 17:18Z; `--level error`, which carries every `turboslide` line) hold no `presence: reading` and no `blob: polling ... failed` line, so no read failed loudly. A's row had been pushed 3 to 4 s before C's stream opened, so the roster read at C's first tick should have named A and the tick should have been the active one; the one path by which that read names nobody without an error is `packages/store/src/presence-store.ts` `readRemote`: when `client.head` names the record but `provenGet` answers null, because the public store's edge answers 403 on a just written file for a while (C3S-F4; the SDK's `get` throws, `provenGet` swallows it and tries the copy under the version and the url past the CDN, all on the same edge), the read was taken for "no record" and stored as a proven empty roster. A fresh viewer's instance reads a record written seconds earlier, so it read nobody, `nextTickMs` counted the tab alone and the poll went quiet for 10 s; A's write moved the pulse and the next tick read everything. The same path also announced a `leave` for every row the instance had announced, a chip flap this part did not chase but the change removes.

Three changes, each small:

- `packages/store/src/presence-store.ts`: `readRemote` keeps the rows the instance holds and marks the copy unproven (`version: null`, `proven: false`, `sawRecord: true`) when the head names a record whose body no read answered, instead of an empty proven roster; `confirm` (the pulse did not move) does not count an unproven copy as fresh, so the next roster question reads again once the copy is older than `PRESENCE_POLL_MS`; the type gains `proven(deckId)`, whether the roster this instance holds is a reading of the record or of its absence. A push over an unproven copy already waited for a proven base (`pushNow`, b4.md C3T-R1), so an instance in the edge's window no longer tries to create over the record either.
- `packages/store/src/pulse.ts`: `PollTickInput` gains `rosterKnown`; `pollTickMs` answers the active tick while it is false, so a tab whose company is unknown is never counted alone; absent, the rule reads as before. `pulse.test.ts` gains the case (unknown active, known alone quiet, absent as before, company and an op active either way, under `POLL_CALLS_PER_MINUTE_MAX`).
- `packages/realtime/src/blob.ts`: `nextTickMs` passes `rosterKnown: shared.presence.proven(deckId)`; and a stream that opens while the deck's pulse poll already runs on the instance wakes it (`DeckState.wake`, set by `startPulsePoll`): one read of the presence record now (`presence.poll`, the one extra store call of a stream open) and the tick re-armed from the fresh roster when it is sooner than the armed one. The new stream is company (`listeners - 1`), so a poll at the quiet pace returns to the active pace at the open instead of at its next quiet tick, up to 10 s away; a tick in flight arms the next wait from the same roster itself. Nothing periodic is added; the first stream on an instance already reads the record at its first tick (`readMoved`).

The costs: an idle tab alone still ticks at 10 s once its instance has read the record or its absence; while the edge withholds a fresh record's body, the instance ticks at 2 s and reads the record again at most every 5 s until a read proves it, bounded by the window; a stream opening on an instance that already polls costs one presence head (and a get when the record moved). `POLL_CALLS_PER_MINUTE_MAX` is pinned as before, and 8.1's cost rows read the ceilings again on the new preview.

`blob.test.ts` gains two cases on the fake store: a second stream opening while the poll is at the quiet pace (600 ms) reads the record once and brings the 20 ms tick back within 200 ms, where the quiet tick would have made none; and a first read of a record whose body and copy the fake refuses with the edge's 403 leaves the copy unproven and the tick at 20 ms rather than the quiet 600 ms an empty roster would read as, and the next read proves it and lists the colleague (the setup exposes the shared presence per instance as `presenceOf`). `presence-store.test.ts` gains the case: the record there but unreadable keeps the row it held and announces no leave, `confirm` does not freshen the unproven copy, the roster question reads again once stale and proves it, and a set through a third instance in the window pushes nothing and says "no read proved the record; the push waits".

A finding for Kevin, not changed here: `blob.ts` counts a roster row as the instance's own when this instance's presence route set it (`ownClients`), on the assumption that a tab's presence POSTs land where its stream is; the platform routes each POST on its own, so an editor's row can read as own on an instance where only a viewer's stream is open, and that instance would count the viewer alone. Two rows of company where one is misread is one edge; the fix is the stream telling the channel its client id at `subscribe` so own means a stream here, a change to `room.ts` and the channel contract for the round after.

### 7.4 `logos.picker.search`: the fetch on the keystroke

`packages/chrome/src/dialogs/Logo.tsx` searched 200 ms after the last key (`SEARCH_PAUSE_MS`), so the row's 300 ms from the last key had 100 ms left for the round trip and read 338 ms once (5.2). The dialog now fetches on the keystroke: the effect bumps `searchSeq` and calls `searchLogos` at once; the answers land in any order and only the newest query's applies, so a slower earlier answer never draws over a later one; a cleared field bumps the sequence too, so an answer in flight for the cleared query is dropped (before, the timer was cleared but a request already sent could still land). `SEARCH_PAUSE_MS` and the `pause` ref are gone. The search route answers from the instance's held index, so the extra requests of a typed word are cheap; the cost rows do not measure the picker. `logo-dialog.test.tsx` gains the case, over a route whose answers the test releases by hand: three keys make three requests with none held back, the last query's answer drawn first draws its tile and ends the search, the two earlier answers landing late change nothing, and a cleared field drops the answer in flight; the results case is renamed to the keystroke.

### 7.5 Gates at 01:29Z to 01:32Z

| Command                                                                                      | Result                                                                                                                |
| -------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `node_modules/.bin/tsc -b`                                                                   | clean (a first pass read the channel's `presence` type in the new blob test and the dead `awaitRevision`; both fixed) |
| `apps/studio`: `vitest run`                                                                  | 71 files, 534 passed                                                                                                  |
| `packages/realtime`: `vitest run`                                                            | 10 files, 138 passed                                                                                                  |
| `packages/store`: `vitest run`                                                               | 20 files, 255 passed, 1 skipped (as before)                                                                           |
| `packages/chrome`: `vitest run`                                                              | 76 files, 721 passed, 2 todo (as before)                                                                              |
| root: `vitest run scripts/probes/core-walk.test.mjs core-gate.test.mjs core-matrix.test.mjs` | 3 files, 34 passed                                                                                                    |
| `node_modules/.bin/prettier --write` over the twelve changed source and test files           | three test files reformatted, the rest unchanged                                                                      |
| `node scripts/check.mjs --greps`                                                             | clean (27 `innerHTML` call sites and 25 `overwrite: true` in the allowlist)                                           |
| `git status -- docs/gslides-parity/verification-3/parity-audit.json`                         | unchanged                                                                                                             |

### 7.6 What this part did not change

The render's empty mark block (7.1); `deck.tailor` (7.2); the own row rule of `blob.ts` (7.3); the outage row's driver (4.3); `asset.add`'s answer (4.6); the spec text of FEATURES.md 4.3, which still reads "results 300 ms after the pause" (the row's bound is from the last key and stands; the pause is gone).

### 7.7 The commit

`db7b673` on `features/ship-one` at 01:33Z, "Turboslide features round, ship one: the mark read, the own write resync, the roster at the stream open, the picker fetch", with the trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`, by an explicit path list of seventeen files: the twelve sources and tests of 7.1 to 7.4 (`scripts/probes/core-walk/areas/brand.mjs`; `apps/studio/src/editor/resync-history.ts`, `resync-history.test.ts`, `controller.tsx`; `packages/store/src/pulse.ts`, `pulse.test.ts`, `presence-store.ts`, `presence-store.test.ts`; `packages/realtime/src/blob.ts`, `blob.test.ts`; `packages/chrome/src/dialogs/Logo.tsx`, `packages/chrome/src/__tests__/logo-dialog.test.tsx`), this note as far as 7.6, and part A's four ledgers (`features-one-ship-preview-run1.json`, `run2.json`, `features-one-ship-gate-down-run5.json`, `run6.json`). The seventeen paths were scanned for token and cookie shapes before the add (none); never `.github/`, never `parity-audit.json`. Sixteen commits over `88b68e7`; `main` untouched at `88b68e7`; no push. The sections after this one are written after the commit and stay uncommitted for part B's commit.

## 8. The deployment of record of the fix part and its first reads

A fresh enforce fixture preview from the committed tree `db7b673` with the recorded command (`vercel deploy --yes --archive=tgz` from the repository root, `TURBOSLIDE_AUTHORIZE=enforce`, `TURBOSLIDE_ASSIST=fixture`, `TURBOSLIDE_LOGO_UPSTREAM=fixture`, `TURBOSLIDE_MAIL=off`, `TURBOSLIDE_REALTIME=blob`, production's public store host, the two secrets minted inside the command and dropped from the log; never `--prod`, no project setting, no env command), 01:34:35Z to 01:37:40Z: `https://turboslide-98in7yfd4-kl01s-projects.vercel.app` (`dpl_ZbFJpT2HdRtzZHCvbDto7FpnbFXG`, behind Vercel Authentication, the one Blob store production uses). It replaces `6yg7b2f01` as the deployment of record of this ship; `6yg7b2f01`'s two runs (4.7) keep their readings as what they were, and the outage preview `qs5ps0cte`'s readings of 4.8 stand (`logos.index.cached-offline` is not driven by design on the deployment of record, 4.3; no logo route changed in this part).

| Read                                  | Command and time                                                                                                | Result                                                                                                                                                                                                                             |
| ------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The pages                             | `GET /` and `/new` with the OIDC header (01:37Z)                                                                | 307 to `/new` in 265 ms; 200 in 4,252 ms on the cold function                                                                                                                                                                      |
| The hosted smoke                      | `node scripts/hosted-smoke.mjs --base <preview> --token-env TURBOSLIDE_TOKEN` (01:38:03Z, 18 s)                 | 40 of 40 passed                                                                                                                                                                                                                    |
| The new write probe                   | `node scripts/probes/new-write-probe.mjs --base <preview>` (01:38:21Z, 51 s)                                    | all steps ok on the blob tier (the first write after opening acknowledged 14 to 15, rejects 0; the immediate keystroke reached the document); its deck `untitled-20260924-8bvf` trashed and deleted forever by the probe           |
| The banner probe, `logo.insert` alone | `banner-probe-3.mjs --base <preview>` (01:39:30Z; the scratch deck `untitled-20260924-fgox`, gone, `/edit` 404) | the answer's keys `asset, revision, variant`, `revision` 2, answered in 1,368 ms; revision and serverRevision 2 together at 1,260 ms; no banner in 16 samples over 4 s and none at 6 s; one console 404 of the removed deck's card |

Both tokens came from `with-tokens.mjs` (never printed). The store after the probes: no deck of 2026-09-24 but the ones the probes removed (8bvf, fgox; both `/edit` 404), read again in 9.1 before the gate's runs.

## 9. The diagnostic run on `98in7yfd4`, the assist over-report and the fix

The gate ran once on the deployment of record `98in7yfd4` (`db7b673`) as run 3 before run 4 started (01:39:59Z, 7,927 s, `--matrix features-one-ship-preview-run3.json`; the ledger of that run is kept aside as `<scratch>/fixpart/diagnostic-run-db7b673.json`, since run3.json and run4.json of record are rewritten on the fixed deployment below). It read 785 rows, 755 passed, 14 failed, 16 not driven, `retries` 0, no cost row over its ceiling. Read by id against the matrix and the run's traces:

- The three seams of 7.1, 7.3 and 7.4 read green: `brand.logo.remove` passed (the walk's content read; R15's placeholder no longer counts as a mark), `logos.picker.search` passed (the fetch on the keystroke; within 300 ms in the run), and `sync.viewer.live-updates` passed (the unparkable blocker of 5.1; C read A's words within 5 s three of three, the roster known at the stream open and the tick active). None of the three is in the run's not-passed list.
- `assist.rewrite.card-accept-undo` failed again and `assist.notes.draft`, green in both of part A's runs, failed too. Both go through `acceptAssist`, and the trace showed the mechanism the resync seam introduced: the accept's `POST /api/assist` answered 200, `settleOwnWrite` published `serverRevision` at the answered revision about 250 ms later, and the spec's poll of `state.revision` (which folds `serverRevision`) passed at once while the document body still read the pre accept text; the spec read the body in that gap and failed "the body changed". The revision was reported ahead of the document. Part A's own version had the same shape one wait later (it published `serverRevision` at the end of the 5 s wait, before the stream brought the entry at 7 s), which is why the row was red in part A's runs; the resync seam moved the publish earlier and so also broke `assist.notes.draft`, whose stream had arrived inside part A's 5 s wait.
- The rest of the not-passed rows are 5.1 to 5.3's classes: `sync.title.concurrent-both-kept` (Kevin's standing row), `decks.editor.move-to-trash` (the blob listing tail, red once), `brand.logo.replace-every-slide` (the picture arriving on the footer past 5 s, the blob delivery class, red once here after two greens in part A), the carried parked features (`inbox`, `templates`, `view.live-pointers.second-browser`, `versions.show-changes-marks`) and the P1 rows not on the build, and the not driven rows by their rule.

The fix, deferring the acknowledgement into the resync instead of publishing it ahead of the document (`apps/studio/src/editor/controller.tsx`, `resync-history.ts` unchanged): a new controller field `ownAnsweredRevision` holds the highest revision a server side write of this tab answered; `settleOwnWrite` folds the answered revision into it before it resyncs (`acknowledgeAnswered`), carries the revision to `room.resync(revision)` so the read force syncs the store to at least it (`liveAtLeast`, write.ts) and the reload brings the write itself, and no longer publishes `serverRevision`; `onResync` reads `resyncBroughtUnseen(fresh, Math.max(latest().serverRevision, ownAnsweredRevision))`, so a reload landing at or below a revision the tab's own write answered keeps the undo history and shows no banner, while another writer's higher revision still clears and shows it. The reported revision now moves with the document, so a driver that reads the revision then the body reads them consistent. The announce branch (`asset.add`, `asset.dither`, `material.capture`, `logo.insert`), the assist accept and `slide.import` all go through the one helper; `asset.add`'s answer carries no revision, so it resyncs with no bound and its banner stays Kevin's list (4.6). `node_modules/.bin/tsc -b` clean; `apps/studio` `vitest run src/editor` 14 files 90 passed; `resync-history.test.ts` 12 passed; prettier clean over the file.

### 9.1 The commit of the fix

A second commit on `features/ship-one` after `db7b673`, "Turboslide features round, ship one: the assist accept revision moves with the document, not ahead of it", with the trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`, by an explicit path list: `apps/studio/src/editor/controller.tsx` and this note. Scanned for token and cookie shapes before the add (none); never `.github/`, never `parity-audit.json`. Seventeen commits over `88b68e7`; `main` untouched; no push.

## 10. The deployment of record and the two gate runs

A fresh enforce fixture preview from the fix commit with `deploy-preview.sh fixture` (the recorded command; never `--prod`), the deployment of record of this ship; `98in7yfd4` and its diagnostic run keep their readings as what they were. The two gate runs on it with no code change between them (`run-gate-a.sh`, `--parked ship-f1afe1e.json`, `--matrix features-one-ship-preview-run3.json` and `run4.json`), each detached and polled, the store swept between and after. The hosted smoke, the new write probe and `banner-probe-3.mjs` ran against it first.

### 10.1 The two gate runs of record

`node scripts/probes/core-gate.mjs --base https://turboslide-2kbrkgcbc-kl01s-projects.vercel.app --parked docs/gslides-parity/focus/ship-f1afe1e.json --matrix docs/gslides-parity/focus/verification/features-one-ship-preview-run<n>.json`, both tokens through `with-tokens.mjs`, each detached and polled at most every ten minutes, no code change between the runs, the store swept between (run 3's four decks spec fixtures, one leftover `core-spec-template-*` of the brand and logos specs, and the `gt-brand-*` copy of the GT brand deck card spec, all trashed and deleted forever; the two earlier `gt-brand-*` copies of 2026-09-13 and 2026-09-15 are among the 70 and left) and after (run 4's six 2026-09-24 decks removed, the store read 70 decks with `blank`, `gt-brand` and the default `blank`, none of 2026-09-24). The default driver set. The ledgers of record are `docs/gslides-parity/focus/verification/features-one-ship-preview-run3.json` and `run4.json`; the walk tables, the Playwright reports with the failed rows' traces and the cost probe JSON are under the scratch run folders.

| Run | Started, length    | Rows                                                                                                                              | Walk                                                 | Specs                           | Cost probe                                                              |
| --- | ------------------ | --------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- | ------------------------------- | ----------------------------------------------------------------------- |
| 3   | 04:03:23Z, 7,288 s | 785: 757 passed, 11 failed, 17 not driven (1 manual), 0 no step; retries 0; cost rows over ceiling 1 (`cost.two-tabs-idle.calls`) | 572 matrix rows: 558 passed, 4 failed, 11 not driven | 196 passed, 6 failed, 5 skipped | 6 rows: 5 passed, 1 failed (the measure row), 0 not driven; 7 instances |
| 4   | 06:07:33Z, 7,215 s | 785: 757 passed, 11 failed, 17 not driven (1 manual), 0 no step; retries 0; cost rows over ceiling 0                              | 572 matrix rows: 558 passed, 3 failed, 11 not driven | 194 passed, 8 failed, 5 skipped | 6 rows: 5 passed, 0 failed, 1 not driven; 4 instances                   |

The four seams of this part read green in both runs, by id from the ledgers: `brand.logo.remove` (7.1, the walk's content read), `sync.viewer.live-updates` (7.3, the unparkable blocker of 5.1, C read A's words within 5 s three of three both runs), `assist.rewrite.card-accept-undo` and `assist.notes.draft` (7.2 and 9, the revision moves with the document). `logos.picker.search` (7.4) read green in both. Every objects, fonts and logos row on the build read green in both, as in 4.7.

### 10.2 The rows not passed in both runs, classified

By FEATURES.md 7.2, with the carried list of `ship-7ea3db9.json` (`inbox`, `templates` parked; `view.live-pointers.second-browser`, `versions.show-changes-marks` parked rows) read against the runs, and the mechanism from the runs' JSON and traces:

- Red in both runs, all carried or standing, none a blocker: `inbox.bell-panel-toggle`, `inbox.settings-persist`, `inbox.notification-arrives` (carried park `inbox`); `templates.default.use-for-new`, `templates.gallery.page` (carried park `templates`); `view.live-pointers.second-browser`, `versions.show-changes-marks` (carried parked rows); `sync.title.concurrent-both-kept` (Kevin's standing row, passed run 3 and failed run 4, either way not chased).
- Red once, all the two recorded classes of 7.2, recorded and not chased: `decks.editor.move-to-trash` (failed run 3, the listing page's tail on the blob tier), `images.insert.upload-while-pending` (failed run 3, the blob acknowledgement timing, b3.md R21), `brand.appearance.default` (failed run 4, the editor load's 90 s stall, pass 2 F9's class), `share.dialog.co-edit-from-copied-link` (failed run 4, the blob cross instance delivery, the return round's `collab.*` class), `logos.index.refresh-fixture` (failed run 3, the store version head lagging the write, the CDN lag class of 5.2), `logos.picker.chrome-1280` (failed run 4 on its afterAll teardown "every deck of this file is torn down", a blob edge lag on a deck removal; the row's own assertion passed both runs, the annotation reading nine tiles five per row with no horizontal scroll on both grounds at 1280 by 800, so the picker's layout is not the failure), `cost.two-tabs-idle.calls` (the measure row, the one `assets` listing of run 3, never holds the ship).
- Not driven in both runs by their rule: the P1 rows this ship does not build (`tables.seam.row-drag`, `tables.edge.add-row-column`, `tables.heads.select-row-column`, `tables.bar.row-column-buttons`, `arrange.group.tail-text-controls`, `wordart.tail.fill-outline`, `brand.objects.kit-colours-first`, `fonts.picker.specimen-rows`, `fonts.table.takes-family`, `logos.picker.variants`, `logos.kit.find-a-logo`, `templates.deck.read-only`) with their control ids; `logos.index.cached-offline` (the fixture preview, not the outage; the unit test `apps/studio/src/server/logos.test.ts` named); `assist.quota.429` (no Upstash); `sync.resend.idempotent` (deployment N carries no origin); `cost.editor-hidden.calls` (headless Chromium reports no hidden page); `text.clipboard.paste-without-formatting` (the manual checklist). `collab.roster.go-to-slide` and `decks.file.open-list-search` (Kevin's other standing row) passed in both runs.

### 10.3 The cost rows beside their ceilings

| Row                         | Run 3                                                | Run 4                | Ceiling            | The last ship's production reading |
| --------------------------- | ---------------------------------------------------- | -------------------- | ------------------ | ---------------------------------- |
| `cost.editor-idle.calls`    | function 9.65 a minute; store simple 30, advanced 10 | 9.66; 27, 9          | 12; 40, 11         | 9.66; 28, 9                        |
| `cost.editor-editing.calls` | 69.60 (209 in 3 min); simple 127, advanced 75        | 67.95 (204); 130, 74 | 75; 140, 85        | 68.60                              |
| `cost.two-tabs-idle.calls`  | advanced 19; `list` 1 (`assets`, the open)           | 15; `list` 0         | 30; 0              | `list` 0, advanced 15              |
| `cost.show.calls`           | 0 function requests; the page's store calls 0        | the same             | 0; 0               | 0                                  |
| `cost.editor-hidden.calls`  | not driven by its rule                               | the same             | 8                  | not driven                         |
| `sync.pull.no-listing`      | passed                                               | passed               | `lists.versions` 0 | passed                             |

Every cost row holds under its ceiling in both runs but the one `assets` listing of run 3 (the measure row `cost.two-tabs-idle.calls`, the reading the last ship recorded too, `list` 0 in run 4). The editing row's function requests read 69.60 and 67.95 against production's 68.60 at the last ship, within the same band as 5.4's reading; this part added one store read per stream open (7.3, the roster wake) and one head per unproven read while the edge withholds a fresh presence record, neither periodic, and the idle and editing rows hold. Part B's production run reads these again.

## 11. The verdict of this part

Every feature of ship one reads green on the deployment of record in both runs: every objects row on the build, every fonts row, every logos row on the build. The three seams that were blockers in 5.1 are resolved and read green in both runs: `brand.logo.remove` (the walk's content read), `assist.rewrite.card-accept-undo` (the revision moves with the document) and `sync.viewer.live-updates` (the roster known at the stream open, the tick active while the deck's company is unknown), and `assist.notes.draft`, which the first fix attempt regressed, reads green too. The picker's `logos.picker.search`, flaky in part A's runs, reads green in both after the fetch on the keystroke. No row is red in both runs but the carried parks, the carried parked rows and Kevin's standing `sync.title.concurrent-both-kept`; no row red once falls outside the two recorded classes; no objects, fonts or logos feature is parked or reverted by the rule. No blocker stops the ship. The parks decision list of 6 stands unchanged: no feature and no row is added to it by this part, and none leaves it beyond `collab.roster.go-to-slide`, which part A already removed and which passed in both runs here. This part made the four seams, ran the two gate runs of record, and stops here; the production run is part B's.

## 12. Part B: the parked list, the confirming run and the ship

Written by the ship step's continuation, part B, which took over at 08:20Z on 2026-09-24 after the fix part's two gate runs of record (10.1) and its verdict (11). The rules of the header hold: the git writes of this part are the commits of 12.1 and 12.4 on `features/ship-one`, the fast forward of `main` to the branch and the one push of `main`; no `pnpm install`, `add` or `exec`, no build outside `scripts/check.mjs`, `round-five/integration` never touched, no token, cookie or secret printed. The tree it found: the branch at `3a1e339`, eighteen commits over `88b68e7`, clean but the untracked `.github/` (never committed); `main` at `88b68e7`, `origin/main` the same, production `dpl_5s5zTWuu4zphkMT1kaaQeHArjdX7` (`turboslide-cr0xfpuii`) serving it; no Playwright, walk or gate process alive; the OIDC token in `.turboslide/vercel-dev.env` valid to 12:50Z, refreshed at 08:21Z with `vercel env pull .turboslide/vercel-dev.env --environment development --yes` (valid to 20:21Z) so a run crossing midday would not meet a 401.

### 12.1 The parked list rendered, the set written, the flag lifted

`docs/gslides-parity/focus/ship-f1afe1e.json` is rendered by `docs/gslides-parity/features/build/ship/render-ship-json.mjs` from the two runs of record (`features-one-ship-preview-run3.json`, `run4.json`) with `--carry ship-7ea3db9.json`. The renderer grew two things this part so the list stays computed and never typed: a row of a feature the list parks whole is not listed a second time in `parkedRows` (the feature's flag hides its controls already; `templates.gallery.page` and `templates.default.use-for-new` under `templates`), and the ship rule's exceptions over the gate's mechanical `wouldPark` are flags (`--not-feature <name>=<why>`, `--not-row <id>=<why>`) written into the list's `notParked` with their reasons; a carried row green in every run is written into `leaves`. The list, exactly part A's classification of 6 and the fix part's verdict of 11:

- `parkedFeatures`: `inbox`, `templates` (carried; red in both runs again).
- `parkedRows`, ten: the carried `versions.show-changes-marks` (`file.versionHistory.showChanges`) and `view.live-pointers.second-browser` (`view.livePointers.mine`, `view.livePointers.collaborators`); the eight P1 rows whose controls ship one does not build, not driven in both runs, `arrange.group.tail-text-controls` (`toolbar.group.text`), `logos.kit.find-a-logo` (`panel.brand.logo.find`), `logos.picker.variants` (`dialog.logo.kind.wordmark`, `dialog.logo.tone.mono`), `tables.bar.row-column-buttons` (`bar.table`), `tables.edge.add-row-column` (`handle.table.add.column`, `handle.table.add.row`), `tables.heads.select-row-column` (`handle.table.head.column`, `handle.table.head.row`), `tables.seam.row-drag` (`handle.table.row`), `wordart.tail.fill-outline` (`toolbar.wordart.outline`).
- `notParked`: `brand` (`brand.logo.remove` green in both runs; `brand.objects.kit-colours-first` a P1 row not on the build; `brand.appearance.default` red once, run 4, the editor load's stall), `fonts` (its entries P1 rows not on the build; every fonts row on the build green in both), `assist` (`assist.rewrite.card-accept-undo` and `assist.notes.draft` green in both; `assist.quota.429` not driven with no Upstash, PRODUCT.md 8.2's exemption), and the row `logos.index.cached-offline` (`insert.logo`; not driven by design on the fixture preview, 4.3 and 4.8, the unit test `apps/studio/src/server/logos.test.ts` named).
- `leaves`: `collab.roster.go-to-slide` (`title.presence.goTo`), green in both runs.
- The renderer's own reading of the runs, printed: flaky rows `logos.index.refresh-fixture` (run 3) and `logos.picker.chrome-1280` (run 4), no flaky feature, `sync.resend.idempotent` the one blocking row red in both runs (not driven; deployment N carries no origin, 10.2), no revert.

`node scripts/probes/core-matrix.mjs --emit-parked docs/gslides-parity/focus/ship-f1afe1e.json` wrote fourteen ids into `packages/chrome/src/parked-controls.ts` between its markers (`bar.table`, `dialog.logo.kind.wordmark`, `dialog.logo.tone.mono`, `file.versionHistory.showChanges`, `handle.table.add.column`, `handle.table.add.row`, `handle.table.head.column`, `handle.table.head.row`, `handle.table.row`, `panel.brand.logo.find`, `toolbar.group.text`, `toolbar.wordart.outline`, `view.livePointers.collaborators`, `view.livePointers.mine`); `--check` reads it current. The `advanced` flags of `menus/model.ts`: `file.versionHistory.showChanges` and the two Live pointers rows already carried theirs (the carried list); the eight P1 controls exist in no source (`git grep` over `packages/chrome/src` and `apps/studio/src` finds them in `parked-controls.ts` and `core-matrix.mjs`'s `DECLARED_CONTROL_IDS` alone), so no flag is set for them and the set is what hides their ids once they land; `title.presence.goTo` loses its flag, since its row left the list, with the comment naming the ship. The tests follow: `parked-controls.test.ts` pins the committed set to the fourteen ids and the family rule over them (`handle.table.row.1`, `bar.table.insertRowBelow` parked while the switch is off, drawn while it is on; `dialog.logo.search`, `bar.chart.editData` and `title.presence.goTo` not parked); `menu-model.test.ts` moves `title.presence.goTo` from "keeps the flag on every row RETURN.md leaves parked" to its own test in the default view; `presence-slot.test.tsx` reads the Go to slide word in the default view in both roster tests; `RosterMenu.tsx`'s docblock and `apps/studio/e2e/presence.spec.ts`'s comments name the change (that spec asserts the word behind the switch, which still holds). `docs/FEATURES.md` section 8 opens with what this ship parks, the reason and the way back for each; the README's "Advanced tools" paragraph names the list as shipped and drops Go to slide from the title row's line. Checks at 08:24Z: `node_modules/.bin/tsc -b` clean; `packages/chrome` `vitest run` 76 files, 722 passed, 2 todo; `scripts/probes` `core-matrix.test.mjs`, `core-gate.test.mjs`, `core-walk.test.mjs` 34 passed; `node scripts/check.mjs --greps` clean (27 `innerHTML` call sites in the allowlist); `node docs/readme/what-works.mjs --check` current; prettier clean over the eleven files; `git grep '<<<<<<<'` empty outside a committed PNG; `parity-audit.json` untouched (no check chain ran). The eleven paths were scanned for token and cookie shapes before the add (the one hex hit is the Inter italic's sha256 in FEATURES.md 3.1). The commit: `e2b6b03` on `features/ship-one` at 08:25Z, "Turboslide features round, ship one: the parked list rendered from the two runs", with the trailer, nineteen commits over `88b68e7`; `main` untouched; no push.

### 12.2 The confirming preview, run 5 and the rerun

The parks commit touched two sources (`parked-controls.ts`, `model.ts`), so the deployment of record no longer matched the tree and one more enforce fixture preview was deployed from `e2b6b03` with the recorded command (`deploy-preview.sh fixture`; never `--prod`, no project setting, no env command), 08:25:29Z to 08:28:22Z: `https://turboslide-dzz447n7t-kl01s-projects.vercel.app` (`dpl_274sbw5g1jZABkiRcHNeiYddcKtL`, behind Vercel Authentication, the one Blob store production uses). The hosted smoke read 40 of 40 at 08:29Z; the store read 70 decks (54 live, 16 trashed), none of 2026-09-24. The gate ran once on it as run 5 (`run-gate-b.sh`: `--parked docs/gslides-parity/focus/ship-f1afe1e.json`, the rendered list, `--matrix features-one-ship-preview-run5.json`, both tokens through `with-tokens.mjs`), 08:29:19Z, 7,898 s: 785 rows, 753 passed, 15 failed, 17 not driven (1 manual), 0 no step; `retries` 0; cost rows over their ceiling 0. The walk read 572 matrix rows, 558 passed, 3 failed, 11 not driven (622 steps, 3,059 s; the same 558 as runs 3 and 4); the specs 190 passed, 12 failed, 5 skipped (3,587 s); the cost probe 6 rows, 5 passed, 1 not driven (1,251 s, eight instances answering).

What the run confirms. The parked controls are hidden and the switch shows them: `surface.advanced.off-by-default`, `surface.advanced.on-shows-parked`, `surface.advanced.remembered`, `surface.parked-blocks-render`, `surface.parked-shortcut-unbound` and `surface.parked-block-core-rows` passed, as did the other two surface rows. `collab.roster.go-to-slide` passed with its flag lifted. Every objects row on the build read green (tables 43 rows but the four P1 rows not driven, charts 29, diagrams 11, shapes 24, arrange 80 but the P1 row, word art 6 but the P1 row, formatting 26), every fonts row on the build (21 but the two P1 rows), every brand row but the P1 row, and every logos row on the build but two red once (below).

The rows not passed in run 5, by id against runs 3 and 4 (10.2) and the rerun below:

- Red in both runs of record and again here, carried or standing, none a blocker: `inbox.bell-panel-toggle`, `inbox.settings-persist`, `inbox.notification-arrives` (the parked `inbox`); `templates.gallery.page`, `templates.default.use-for-new` and, red once, `templates.card.rename-and-delete` (the deleted template still listed 20 s later, the templates' documented listing mechanism; the parked `templates`); `view.live-pointers.second-browser`, `versions.show-changes-marks` (the parked rows). `sync.title.concurrent-both-kept`, Kevin's standing row, passed.
- Red once in run 5 and green in runs 3 and 4, read again green on the rerun, each with the mechanism from its trace: `decks.list.read` (the Recent row of `/decks` was absent within the row's 5 s; the listing answered in 4,954 ms on the blob tier and the row is drawn from this browser's `ts-recent` cookie less the deck marked trashed; the listing tail class of 7.2), `decks.card.open-in-new-tab` (the deck's `/edit` answered 200 at 09:32:28 and 404 at 09:32:37 on another instance, 500 once before the row at 09:31:29; the new tab's editor did not expose the studio within the row's 60 s; the blob tier's per instance read of an existing deck, the family of VERIFICATION.md F9 and C3-F5), `export.png.current-slide` (the hosted `render.slide` answered "render: turboslide exited 2 without a JSON result; page.goto: net::ERR_FILE_NOT_FOUND at file:///tmp/turboslide-render-w6ihio/deck-light.html#s/title", so no tab opened; VERIFICATION.md C3T-F4, the hosted render's own file missing on a cold instance; red once in the sync round's ship too and green on its rerun), `sync.reload.same-document` (C, the stranger through the `/s/` link, read 404 on `/deck/<id>`, `/edit/<id>` and `/api/access/<id>` 5 s after A created the deck on another instance and never joined; the cross instance read of a just created deck, the collab class, the same row flaky in the sync round's own ship), `logos.picker.chrome-1280` (its afterAll teardown alone, as in run 4; the row's own assertions passed), `logos.index.refresh-fixture` (as in run 3: a refresh after the fixture dropped the slug did not report the drop, the fixture manifest decided from the answering instance's held copy).
- Red in both readings on this deployment: `images.insert.upload-while-pending`. Run 5: the tab threw `RangeError: No slide "title-2"` at the second picture's insert, after its own resync following the first `asset.add` (the document read at 09:56:23.42, between the `asset.add` answer and the first insert at rev 4) brought a document without the slide added at rev 2 two seconds earlier, the store's head lagging on the answering instance; the rerun: the second picture landed past the 2 by 5 s bound (1 of 2 at 9.5 s). Both are the blob acknowledgement timing class (b3.md R21, the row's mechanism in run 3), run 5's through the `asset.add` seam of 4.6 (its answer carries no revision, so the resync reads with no bound); green in run 4 and on production at the sync round's ship. No path leads from the parks commit to the upload. Recorded here with its mechanism, and the first item of Kevin's list for the round after: `asset.add`'s answer carries `revision`, or the announce branch flags its own resync with the asset ids it asked for.
- Not driven by their rule, as in 10.2: the twelve P1 rows, `logos.index.cached-offline`, `assist.quota.429`, `sync.resend.idempotent`, `cost.editor-hidden.calls`, `text.clipboard.paste-without-formatting`.

The rerun once of the spec files with a row red once (the sync round's pattern), `run-rerun-b.sh` (`--only specs --spec decks,export,sync,images,logos,chrome`, `--matrix features-one-ship-preview-run5-rerun.json`), 10:45:01Z, 2,142 s, after the store was swept of run 5's seven leftovers: 121 rows, 116 passed, 3 failed, 2 not driven, `retries` 0. Failed: `templates.gallery.page` (parked), `images.insert.upload-while-pending` (above) and `sync.serial.order-and-latency` (one word of thirty, ` a30`, at 5,254 ms against 5,000; the sync round's own recorded flaky row, 7.3 s once on its preview; green in run 5). Not driven: `sync.resend.idempotent`, `logos.index.cached-offline`, by their rule. `decks.list.read`, `decks.card.open-in-new-tab`, `export.png.current-slide`, `sync.reload.same-document`, `logos.picker.chrome-1280`, `logos.index.refresh-fixture` and `brand.appearance.default` passed.

The cost rows in run 5, beside their ceilings and run 4: `cost.editor-idle.calls` function 9.64 a minute (12; run 4 9.66), store simple 29 (40), advanced 9 (11); `cost.editor-editing.calls` 70.60 (75; run 4 67.95, production 68.60 at the last ship), simple 121 (140), advanced 69 (85); `cost.two-tabs-idle.calls` advanced 18 (30), `list` 0 (0); `cost.show.calls` 0 function requests and 0 store calls of the page's own; `cost.editor-hidden.calls` not driven by its rule; `sync.pull.no-listing` passed (`versions` listings 0). Every cost row holds under its ceiling; the editing row stays in the band of 10.3.

The store after the rerun: the five leftovers of the rerun's specs trashed and deleted forever through the API (`remove-deck.mjs`), 70 decks (54 live, 16 trashed), templates `blank` and `gt-brand`, none of 2026-09-24. The scratch folders `gate-preview-b-run5/` and `gate-preview-b-run5-rerun/` hold the walk tables, the Playwright reports with the failed rows' traces and the cost probe JSON.

The verdict of this part. The parked controls are hidden and the switch shows them; no unparked row of ship one is red; every row red once in run 5 read green on the rerun with a mechanism in the classes 7.2 and the earlier ships record, but `images.insert.upload-while-pending`, red in both readings of this deployment and red once on the deployment of record, whose mechanism is the recorded acknowledgement timing class through the `asset.add` seam already on Kevin's list. By 7.2 the flakiness check is the two runs of record on the deployment of record (10.1, 11), and this run changes none of their readings; the ship proceeds with the confirming run's readings on the record.

### 12.3 The acceptance summary, as shipped

- The deployment of record: `https://turboslide-2kbrkgcbc-kl01s-projects.vercel.app` (`dpl_7maP4WQhoyWgh9S2eez68RRxr9os`, enforce, `TURBOSLIDE_LOGO_UPSTREAM=fixture`, built from `0fc0cfa`); run 3 at 04:03:23Z, 7,288 s, and run 4 at 06:07:33Z, 7,215 s, no code change between: each 785 rows, 757 passed, 11 failed, 17 not driven (1 manual), 0 no step, `retries` 0; cost rows over ceiling 1 in run 3 (the measure row's one `assets` listing) and 0 in run 4 (10.1). The outage preview `qs5ps0cte` for `logos.index.cached-offline` (4.8). The confirming preview `dzz447n7t` built from the parks commit `e2b6b03`: run 5 and its rerun (12.2).
- The parked list as shipped, `docs/gslides-parity/focus/ship-f1afe1e.json` (12.1): `parkedFeatures` `inbox`, `templates`; `parkedRows` `arrange.group.tail-text-controls`, `logos.kit.find-a-logo`, `logos.picker.variants`, `tables.bar.row-column-buttons`, `tables.edge.add-row-column`, `tables.heads.select-row-column`, `tables.seam.row-drag`, `versions.show-changes-marks`, `view.live-pointers.second-browser`, `wordart.tail.fill-outline`; `notParked` `brand`, `fonts`, `assist`, `logos.index.cached-offline`; `leaves` `collab.roster.go-to-slide`; no revert. The fourteen ids in `parked-controls.ts`; `title.presence.goTo` in the default view.
- The flaky rows with their mechanisms, red once across the runs of record and the confirming run: `decks.editor.move-to-trash` (run 3, the listing tail), `images.insert.upload-while-pending` (run 3 and both readings of `dzz447n7t`, the acknowledgement timing through the `asset.add` seam), `brand.appearance.default` (run 4, the editor load's stall), `share.dialog.co-edit-from-copied-link` (run 4, the cross instance delivery), `logos.index.refresh-fixture` (runs 3 and 5, the fixture manifest from the answering instance), `logos.picker.chrome-1280` (runs 4 and 5, the teardown's removal lag), `decks.list.read`, `decks.card.open-in-new-tab`, `export.png.current-slide`, `sync.reload.same-document`, `sync.serial.order-and-latency` (12.2), `templates.card.rename-and-delete` (run 5, the parked feature), `cost.two-tabs-idle.calls` (run 3, the measure row).
- The standing rows: `sync.title.concurrent-both-kept` (Kevin's; red in run 4, green in runs 3 and 5), `decks.file.open-list-search` (green in every run).
- Kevin's list from this ship: `asset.add`'s answer carries no revision (4.6, 12.2); `blob.ts` counts a roster row as this instance's own by the presence route that set it (7.3); `docs/FEATURES.md` 4.3 still reads "results 300 ms after the pause" while the dialog fetches on the keystroke (7.4); the hosted render's `ERR_FILE_NOT_FOUND` on a cold instance (C3T-F4, seen again in run 5).

### 12.4 The commit, the merge and the push

The commit on `features/ship-one` after `e2b6b03`, "Turboslide features round, ship one: tables, charts and diagrams edited in place, Inter 4.1 with eight more families, and a logo picker over thesvg.org, with their rows in the core gate", with the trailer, by an explicit path list of this note and the two ledgers `features-one-ship-preview-run5.json` and `features-one-ship-preview-run5-rerun.json` (scanned for token and cookie shapes; never `.github/`, never `parity-audit.json`). Then `main` fast forwarded to the branch (`git checkout main && git merge --ff-only features/ship-one`, `origin/main` still at `88b68e7` when fetched at 08:37Z), twenty commits over `88b68e7`, and pushed to `origin/main` once; the production deployment Vercel built from the push was read with `vercel inspect https://turboslide.vercel.app` until it was Ready, and its id is in part B's report. The production run of 7.2 (the core gate once on production under this list, after production's logo index is rebuilt with the bearer) is the next step's; the release note in `docs/updates.md` follows it, as 7.2 says.

## 13. Part C: production, the index rebuilt and the run of record

Written by the ship step's continuation, part C, which took over at 11:32Z on 2026-09-24 after part B's push (12.4). The rules of the header hold: the git writes of this part are the one commit of 14.4 on `main` and its push; no `pnpm install`, `add` or `exec`, no build outside `scripts/check.mjs`, `round-five/integration` never touched, no token, cookie or secret printed, nothing on Vercel created, changed or deleted (the production deployment is Vercel's own build of the push; this part deployed nothing). The tree it found: `main` at `9d21394`, `origin/main` and `features/ship-one` the same, clean but the untracked `.github/` (never committed); no Playwright, walk or gate process alive; the OIDC token valid to 20:21Z and not needed, since production is not behind Vercel Authentication (every command took the bearer alone through the scratch `with-tokens.mjs --no-oidc`). The task's section numbers for this part (8 and 9) predate parts A and B's renumbering; this note continues as 13 and 14.

### 13.1 The deployment

`vercel inspect https://turboslide.vercel.app` at 11:32Z: `dpl_441AUoH4Dmo4DBsKWsVbmBCRJp65` (`turboslide-nf5u22l4f-kl01s-projects.vercel.app`), target production, Ready, created 11:25:51Z, aliased to `turboslide.vercel.app`, `turboslide-kl01s-projects.vercel.app` and `turboslide-git-main-kl01s-projects.vercel.app`; its build log (`--logs`) opens on `Cloning github.com/Kevin-Liu-01/Turboslide (Branch: main, Commit: 9d21394)` at 11:25:52Z, Build Completed at 11:26:51Z, Deployment completed at 11:27:26Z. Production serves the commit. The deployment before the push, `dpl_5s5zTWuu4zphkMT1kaaQeHArjdX7` (`88b68e7`), is the rollback.

### 13.2 The logo index rebuilt from thesvg.org, and the coverage number

The shared Blob store's `system/logo-index.json` is one file for every deployment, and the fixture previews of parts A and B had written their ten mark index over it. The dry run first, `POST /api/logo/refresh { dryRun: true }` with the bearer (11:33Z, 200 in 457 ms): `credential: agent`, `upstream: network`, and the store's index still the fixture's (9 icons, 8 brands, 11 cached marks, 1 unavailable, `builtAt` 11:12:53Z). Then the refresh, `POST /api/logo/refresh {}` (11:33:25Z, 200 in 43.8 s, inside the route's 240 s budget): 7,422 icons, 4,945 brands, 4,939 marks fetched for their two flags, 10 cached marks, 3 unavailable, `dropped` `acme` and `gradientco` (the fixture's two invented slugs; the other eight fixture slugs are real brands and stay), `builtAt` 11:34:09.575Z. Every instance answered searches from the rebuilt index by 11:35:20Z (`indexed 4945`, `updatedAt` 11:34:09Z, Salesforce and Slack found).

The coverage list of FEATURES.md 4.1, `node scripts/probes/logo-coverage.mjs --base https://turboslide.vercel.app`, ran twice. The first read at 11:35Z answered 83 of 146 with 63 missing: it raced instances that still held the fixture's nine icon index (the file's header records the first answer's `updatedAt` 11:12:53Z, and Salesforce, Slack and Notion read missing), so it is not the number and is kept in the scratch folder alone. The second read at 11:36Z, with every instance on the rebuilt index: 119 of 146 names matched, 27 missing, 0 searches not answering 200, written to `docs/gslides-parity/features/build/logo-coverage-list.txt` and committed beside this note as 4.1 says. The missing names: ServiceNow, Lokalise, Phrase, Smartling, Weglot, Lilt, Outreach, ZoomInfo, Apollo, PandaDoc, Klaviyo, Segment, Ramp, Deel, Remote, Microsoft, Deloitte, McKinsey, Philips, Zscaler, ServiceTitan, Toast, Shopify Plus, Hygraph, GTM, General Translation and Locadex. Judge-seller's read of the registry on 2026-09-20 matched 122 by title, slug and alias; the six present there and missing here (Segment, Ramp, Deel, Remote, Microsoft, Philips) are names the search answers with another brand first, which 4.1 counts as missing and never as a near match (Microsoft's row, for one, ranks behind the brands whose title starts with the word). The picker's Your brand group lists the kit's logo first either way, and the two missing company marks are the one time submission of FEATURES.md 9, question 3.

### 13.3 The run of record on production

`node scripts/probes/core-gate.mjs --base https://turboslide.vercel.app --out <scratch>/gate-production-run1 --shots --parked docs/gslides-parity/focus/ship-f1afe1e.json --matrix docs/gslides-parity/focus/verification/gate-production-features-one.json`, detached, the bearer in its environment, 11:36:29Z to 14:10:34Z, 9,244 s (not the 90 minutes the task names; the preview runs took 7,200 to 7,900 s and production's specs took 4,627 s alone): 785 rows, 747 passed, 20 failed, 18 not driven (1 manual), 0 no step; `retries` zero; cost rows over their ceiling 0; the gate's verdict failed by the six rows of 13.4 and the not driven `sync.resend.idempotent`. The walk (11:36:30Z, 3,248 s): 572 matrix rows, 556 passed, 5 failed, 11 not driven, 622 steps, 5 console errors (resource loads answering 503, 404, 404, 502 and 502, none on a row). The specs (12:30:37Z, 4,627 s): 186 passed, 15 failed, 6 skipped (`logos.index.cached-offline` and `logos.index.refresh-fixture` in the fixture project, `assist.quota.429`, `sync.resend.idempotent`, `fonts.picker.specimen-rows`, `templates.deck.read-only`), 0 flaky. The cost probe (1,369 s): 6 rows, 5 passed, 1 not driven, eleven instances answering `sync.status`. The ledger is `docs/gslides-parity/focus/verification/gate-production-features-one.json`.

The rows not passed, by their class:

- Carried parks, red as the list expects: `inbox.bell-panel-toggle`, `inbox.settings-persist`, `inbox.notification-arrives`; `templates.gallery.page`, `templates.save.as-template`, `templates.card.rename-and-delete`, `templates.default.use-for-new` (`templates.deck.read-only` not driven); `view.live-pointers.second-browser`; `versions.show-changes-marks`.
- The environment's: `assist.rewrite.card-accept-undo`, `assist.notes.draft`, `assist.free-ask.fallback-sentence`, `assist.mark.chip-and-history`, `assist.agent.propose-accept` (`assist.propose` answers 500 "The assistant is not set up on this Turboslide yet" without `ANTHROPIC_API_KEY`); `assist.quota.429` not driven without Upstash.
- Not driven by their rule: the twelve P1 rows whose controls are not on the build (the eight parked rows and `brand.objects.kit-colours-first`, `fonts.picker.specimen-rows`, `fonts.table.takes-family`, `templates.deck.read-only`); `logos.index.cached-offline` and `logos.index.refresh-fixture` (the index reads network on production; `apps/studio/src/server/logos.test.ts` named, FEATURES.md 7.2); `sync.resend.idempotent` (no `origin` in the version list); `cost.editor-hidden.calls` (headless Chromium reports no hidden page); `text.clipboard.paste-without-formatting` (manual).
- Red once in the run of record, of the default view: `brand.logo.replace-every-slide` (the editor shell did not settle in 90 s on the freshly created deck, the return round's editor load class, VERIFICATION.md F9 and C3-F5), `images.insert.upload-while-pending` (1 of 2 pictures within 9.5 s, the blob acknowledgement timing through the `asset.add` seam, 12.2), `collab.edit-from-second-browser`, `collab.slide-added-appears` and `collab.presence-chips` (the second browser bound across instances, the return round's `collab.*` class, S.3), and `logos.picker.paper-and-ink` (13.4).
- Kevin's standing rows: `sync.title.concurrent-both-kept` and `decks.file.open-list-search` both passed.

The cost rows, beside their ceilings and the sync round's production reading: `cost.editor-idle.calls` 9.66 function requests a minute (12; the same 9.66), store simple 31 (40) with 3 of them the probe's own heads, advanced 9 (11), one instance; `cost.editor-editing.calls` 68.53 (75; 68.56 at the sync ship), simple 80 (140), advanced 51 (85), two instances; `cost.two-tabs-idle.calls` advanced 9 (30; 15 at the sync ship), `list` 0; `cost.show.calls` 0 function requests, the store's one head at the first sample the probe's own; `sync.pull.no-listing` passed; `cost.editor-hidden.calls` not driven by its rule. No cost row rose. The two large deck measures: the 85 slide PDF in 17.8 s, the editable PowerPoint with embedded fonts in 191.2 s.

### 13.4 The blocker: the logo routes after a write of the index

`logos.picker.paper-and-ink` read red in the run of record (12:23:47Z) and again in the walk's `--only logos` rerun (14:12:18Z, 215 s; 28 passed, 2 failed, 2 not driven), with the same reading both times: the Vercel tile's ink half lit 0.00 percent on `#070707`, its paper half 38.19 percent with 0.00 percent white on `#ffffff`, the mark inserted on the light deck lit 50.12 percent. Red in both production readings, it is a blocker for a hotfix by the rule of the task, named here with its seam and never hidden; the parked list is not changed by a production reading.

What the tile does. Read whole on production at 14:19Z with a read only probe (open `/new`, Insert > Logo, type vercel, read the tile): both halves draw, `data-variant` `light` on the paper half (thesvg.org's `light.svg`, a black triangle) and `dark` on the ink half (`dark.svg`, a white triangle), each `<img>` complete at 256 by 222 with no filter, blend or mask, the picture of the tile in the scratch folder. The walk's own read (`halfLit` in `scripts/probes/core-walk/areas/logos.mjs`: scroll the half into view, 250 ms, a CSS scale clip less a 4 px inset, the pixels whose luminance differs from the half's background by more than 40 steps) replicated on that drawn tile twice reads the ink half 37.88 percent lit and the paper half 38.19 percent, the walk's paper number exactly. So the walk's 0.00 is a broken image: the ink half's request for `/api/logo/mark/vercel/dark.svg` was answered 500 both times.

Why the mark route answers 500. Measured directly on production between 12:38Z and 12:53Z: the store's `system/logo-index.json` was written at 12:38:06Z (its `last-modified`; a discovery persisted by an instance); at 12:39:04Z and 12:39:35Z every `GET /api/logo/search` answered 500 on two instances each with `{"error":{"name":"Error","status":500,"message":"Vercel Blob: Failed to fetch blob: 403 Forbidden"}}`; from 12:40:05Z the answers were 200 with one more 500 at 12:41:40Z and two of three at 12:45:43Z, and 200 throughout from 12:52:40Z. `GET /api/logo/mark/turborepo/light.svg` and `default.svg` answered the same 500 five times in a row at 12:41Z (`action: logo.mark`) while the store's public host answered the pathname 404 and thesvg.org the file 200. The runtime log read at 12:43Z carried 16 of 105 lines naming the refusal (the templates pull skipped, a room's polling failed, the SDK's `get` stack at `@vercel/blob`), 9 of 100 at 13:02Z and 0 of 100 at 14:19Z. The path in the code: `createLogoService.load()` (`apps/studio/src/server/logos.ts`) reads the index through `deps.store.readIndex()`, whose `client.get(LOGO_INDEX_PATH)` (`packages/store/src/blob-vercel.ts`) throws on any status but 200 and 404; the public store's edge answers 403 on a just written pathname for a while (the product round's templates mechanism, `isStoreBusy` in `packages/store/src/pulse.ts`, VERIFICATION.md C3S-F4), and the throw reaches every logo route through `index()` as the framework's 500, while `revalidate()` catches the same read and keeps the held copy. A cold instance, one that holds no index yet, fails every logo request for the two to three minutes the edge refuses the file; a warm instance keeps answering. On production the index is rewritten whole (3.6 MB) by `persistSoon` after every discovery a search makes (a first fetch of an open licence mark caches it and records the cache entry, a 404 marks a variant unavailable), at most once a minute per instance, so the logos rows of the walk open the window themselves: `logos.picker.search` two steps earlier inserts Figma and its tiles cache marks. On the fixture preview the ten marks were cached at the first load and the index never changed during the logos area, which is why the row passed there five times, and why the preview cannot be the deployment that reads this seam.

The hotfix, named for the round after this note (the code is on `main`): `load()` treats a busy store as the templates pull does (`isStoreBusy`) and answers from the instance's disk mirror (`blobLogoStore`'s `mirrorDir`, written on every read) or, with no copy yet, 503 with `retry-after` and the one sentence, so the tile half and the search retry instead of drawing nothing; and a discovery is persisted to a small side file instead of the index, so the index's pathname is written by the refresh alone. `logos.picker.search`, green in the run of record and red once in the rerun (the one tile 530 ms after the last key against 300, on a cold instance whose first request parses the 3.6 MB index; the searches measured 400 to 800 ms cold and 30 to 90 ms warm), is the same seam's smaller cost and is recorded flaky with it. The driver's read stands: the row measures what a seller sees.

### 13.5 The reruns once, the two probes and the pictures

The spec files with a row red once were rerun once with the committed list, `node scripts/probes/core-gate.mjs --base https://turboslide.vercel.app --only specs --spec brand,images,share --parked docs/gslides-parity/focus/ship-f1afe1e.json --matrix docs/gslides-parity/focus/verification/production-features-one-specs-rerun.json` (14:20:18Z to 14:37:45Z, 1,047 s): 56 rows, 42 passed, 8 failed, 6 not driven, `retries` zero. `images.insert.upload-while-pending`, `collab.edit-from-second-browser` and `collab.presence-chips` passed, and are recorded flaky in their recorded classes (the acknowledgement timing through the `asset.add` seam; the second browser bound). `collab.slide-added-appears` failed again (4 slides not 5 within 5 s, in the first of its three rounds both times): red in both production readings, it is the second blocker for a hotfix, named with the seam to read first, the pulse cadence across instances on the blob tier (the owner's tab learns of another instance's write at its pulse, which ticks at the quiet 10 s cadence when the owner's instance saw no op of its own in 30 s and its per instance roster holds no other client; 7.2, 7.3 and 12.3), so a slide added by a browser on another instance reaches the owner 5 to 10 s later; the two sibling rows read the same bound and passed on the rerun, so the cadence is met some of the time. Red once in the rerun and green in the run of record: `images.replace.drop-on-picture` and `images.background.upload-picture`, the product round's recorded asset put timing class. The parked rows read red as their parks expect (`templates.card.rename-and-delete`, `templates.default.use-for-new`, `templates.deck.read-only`, `view.live-pointers.second-browser`, `inbox.notification-arrives`). `brand.logo.replace-every-slide` was not driven: `brand.spec.ts` runs in serial and `templates.deck.read-only`, skipped in the run of record, ran here and waited 30 s on an address, so the file's remaining rows were skipped, the brand row and five fonts rows among them. One more rerun of `brand` alone (`--spec brand`, 14:40:37Z, 63 s, `production-features-one-specs-rerun-brand.json`: 11 rows, 0 passed, 1 failed, 10 not driven) stopped at `templates.save.as-template` the same way; the brand row is red once in the run of record with the editor load class as its mechanism and no rerun reading, recorded so. The walk's rerun is in 13.4.

`node scripts/probes/new-write-probe.mjs --base https://turboslide.vercel.app` (14:20Z, 62 s): all steps ok, the deck `untitled-20260924-ffbo` trashed and deleted forever by the probe. `node scripts/hosted-smoke.mjs --base https://turboslide.vercel.app --token-env TURBOSLIDE_TOKEN` (14:21Z, 16 s): 40 of 40. `node docs/gslides-parity/features/build/ship/shoot-production.mjs --base https://turboslide.vercel.app` (14:40:55Z to 14:41:46Z, 51 s): 19 pictures at 1440 by 900 under `docs/gslides-parity/focus/verification/production-features-one-<surface>-1440-<appearance>.jpg` (the Insert menu with its Logo row, the Logo dialog with Figma found, the mark inserted beside the brand's mark in the light appearance, Replace image > Logo on the picture's menu, Tailor's Find the logo, the table after one click, the selected chart with Edit data, the Chart data grid, the Font dropdown and More fonts, in both appearances), the record `production-features-one-views.json`, the scratch deck `untitled-20260924-xobx` trashed and deleted forever by the script with `/edit` answering 404. The script's own read of the table after one click said no cell was editing in either appearance; the picture shows the table selected with the pressed cell framed and the text tail drawn, the matrix rows `tables.cell.click-places-caret` and `tables.cell.click-then-type` passed on production in the run of record, and the picture is the record, not the script's boolean.

### 13.6 The store after the runs

`deck.list` with the trash through production with the bearer read 77 decks (60 live, 17 trashed) at 14:42Z, seven of them of 2026-09-24 and left by the run of record's specs: `untitled-20260924-3qhk`, `untitled-20260924-bzx3` (the thumbnail deck), `untitled-20260924-jcyi` and `northwind-renewal-v4-without-notes` (the export fixtures), `import-bundle-source-mufjbsgp`, `gt-brand-mufja8ja` (the large deck's copy, already in the trash) and `untitled-20260924-7z5d` (the brand spec deck). Each was trashed and deleted forever through the API (`remove-deck.mjs`: `trash 200, remove 200; /edit answers 404`); the reruns', the probes' and the shoot's decks were removed by their own teardowns. The store after: 70 decks (54 live, 16 trashed), read the same three times at 14:45Z, what part B left (12.2). A listing one minute earlier answered 15 decks and none of today's: one instance's partial read during the store's refusals (13.4's window), not the store, and the three following reads agree on 70. The templates: the specs left six `core-spec-template-*`, one of them set as the default for new presentations. `template.setDefault { id: 'blank' }` answered 200 and every instance lists the default `blank`; `template.delete` removed `mufbo6iw`, `mufikitv` and `mufmf8cb` (`removed: true`), and for `mufbs9aw`, `mufivi4u` and `mufmcqwg` answered 500 with the store's 403 on a current instance and then 404 from every instance's mirror ("the index lists gt-brand, blank") for eight rounds over four minutes, while the store's own `templates/index.json` (last written 14:50:31Z) still lists the three and their `template.json` files answer 200. That inconsistency is the parked templates feature's own mechanism (PRODUCT.md section 9), it is invisible in the default view (the gallery is behind the switch and the default is Blank), and the three folders are on Kevin's list for the store sweep the API cannot make from a mirror that does not hold them.

## 14. The verdict of the ship, as read on production

### 14.1 The verdict

Ship one is on production at `9d21394` and every feature of the ship is in the default view: every objects row on the build green (tables 39 of 43 with the four P1 rows not driven, charts 29 of 29, diagrams 11 of 11, shapes 24 of 24, word art 5 of 6, arrange 79 of 80, formatting 26 of 26), every fonts row on the build green (19 of 21), every logos row on the build green but one (17 of 22), the parked controls hidden and shown by the switch (surface 8 of 8), the cost rows under their ceilings and none raised. The production run does not read every driven row green: six rows of the default view read red once, three of them green on the rerun once (`images.insert.upload-while-pending`, `collab.edit-from-second-browser`, `collab.presence-chips`), one not reached by either rerun (`brand.logo.replace-every-slide`), and three more red once in the reruns alone (`images.replace.drop-on-picture`, `images.background.upload-picture`, `logos.picker.search`), each with a mechanism the earlier ships record; and two rows read red in both production readings and are the blockers named for a hotfix with their seams, never hidden: `logos.picker.paper-and-ink` (13.4, the logo routes on a cold instance after a write of the index, with the picker's search on a cold instance beside it) and `collab.slide-added-appears` (13.5, the pulse cadence across instances on the blob tier). The parked list of 12.1 stands unchanged; nothing is added to it by a production reading and nothing is reverted.

### 14.2 The documents of the table

This section and 13; VERIFICATION.md "Features round, ship one, the production table (2026-09-23)" (SF.1 to SF.6, the sync round's shape); the release note "2026-09-23, the features round, ship one" in `docs/updates.md`; the README's "Advanced tools" paragraph with the production sentence and its "What works today" section regenerated by `node docs/readme/what-works.mjs` (the check chain's rule renders it from the matrix's `today`, so the section did not change; the production readings are in the ledgers and this table); the ledgers `gate-production-features-one.json`, `production-features-one-walk-rerun.json` and `production-features-one-specs-rerun.json`, the pictures `production-features-one-*.jpg` and `production-features-one-views.json` under `docs/gslides-parity/focus/verification/`; the coverage list `docs/gslides-parity/features/build/logo-coverage-list.txt`. `gh repo edit --description` was not run: FEATURES.md gives no new sentence for the repository, and the topics are unchanged. `docs/FEATURES.md` is not touched by this part.

### 14.3 Kevin's list from this part

1. The hotfix of 13.4 (the logo routes on a cold instance after a write of the index; the index rewritten whole after every discovery).
2. The second hotfix of 13.5 (`collab.slide-added-appears`: the pulse cadence across instances on the blob tier, the roster the cadence reads).
3. The three `core-spec-template-*` folders left in the store's `templates/index.json` that no instance's mirror holds (13.6), for the store sweep.
4. `CRON_SECRET` on production and the daily Vercel cron for `POST /api/logo/refresh` (FEATURES.md 4.2); until then the index is the build of 2026-09-24 made by this step's one POST with the bearer.
5. The rotation of the agent bearer, which has now also rebuilt the index.
6. The thesvg.org submission of General Translation's and Locadex's marks (FEATURES.md 9, question 3).
7. The items part B left (12.3): `asset.add`'s answer without a revision, `blob.ts`'s roster row rule, FEATURES.md 4.3's "300 ms after the pause" against the fetch on the keystroke, the hosted render's `ERR_FILE_NOT_FOUND` on a cold instance; and the sync round's items unchanged (the enforce switch, the key, the hosting decisions, the version record's `origin` in `version.list`).

### 14.4 The commit and the push

The commit on `main`, "Turboslide features round, ship one: the production table", with the trailer, by an explicit path list: `README.md`, `docs/updates.md`, this note, `docs/gslides-parity/focus/VERIFICATION.md`, the production ledger `gate-production-features-one.json`, the rerun ledgers `production-features-one-walk-rerun.json`, `production-features-one-specs-rerun.json` and `production-features-one-specs-rerun-brand.json`, the nineteen pictures and `production-features-one-views.json` under `docs/gslides-parity/focus/verification/`, and the coverage list `docs/gslides-parity/features/build/logo-coverage-list.txt` (FEATURES.md 4.1 commits it beside the ship note); never `.github/`, never `parity-audit.json` (untouched: no check chain ran in this part). Prettier ran over the files it formats; `node docs/readme/what-works.mjs --check` read the README current. The paths were scanned for token and cookie shapes before the add. Then `git push origin main`; the hash is in part C's report. Vercel's build of the push touches no source (notes, ledgers and pictures alone), so production keeps serving the code of `9d21394`.
