# B6, logos and the server routes

## Features round, ship one

Builder B6 of the features round, ship one (docs/FEATURES.md section 4, the lane table of section 6, the rows of 7.1 and the named tests of 7.3), on the shared checkout at `88b68e7`, 2026-09-22. The dev server port is 4416, started with the fixture upstream and stopped before the return. This file carries the requests to the other lanes and the integrator (section 1, written first, day 0), the items landed with their files and hunks so a red row's item reverts alone (section 2), the rows covered with what happened on the dev server (section 3), the tests (section 4) and the notes and decisions (section 5). Nothing here ran a git write command, `pnpm install`, `pnpm build` or `pnpm generate:contracts`.

### 1. Requests

Each is a change in a file another lane or the integrator owns; the hunk is written so it can be applied as it stands. R3 and R4 landed during the build (B7's `agent-actions.ts` and `actions.ts` carry them on the checkout); the rest stand.

R1 (the integrator, `packages/chrome/src/menus/model.ts`): the row `format.image.replaceImage.logo` in `replaceImageItems(prefix)`, after the By URL row, and the two Insert rows B1 asked for carry the Logo dialog:

```ts
    now(`${prefix}.logo`, 'Logo', dialog('Logo'), {
      turboslide: true,
      doc: 'A company logo from thesvg.org in the same box',
      terms: 'logo, brand, company, mark',
    }),
```

The dialog title `Logo` maps to the dialog id B1 registers (`dialogIdOf('Logo')` in `editor-shell.ts`, B1's request); the Replace image target is the selected block, the way the By URL row's `PictureTarget` is built (`pictureTargetOf`, `editor-shell.ts`). Row `logos.replace-image.row`. Not on the checkout as this is written (`model.ts` is unchanged), so the row's menu gesture is not driven; its server path is (section 3).

R2 (the integrator, the header of `packages/schema/src/actions.ts`): the three ids `logo.search`, `logo.insert` and `logo.refresh` in `ACTION_IDS` after `font.list` and before the round three block so `GS3_ACTION_IDS` keeps its slice, and the three `action({...})` entries after `font.list`. B6 landed both (section 2, item 7) because the handlers and their tests do not typecheck without the ids; the integrator may move the lines and keeps the entries' shapes. Two counts follow at the merge: `packages/schema/src/actions-gs3.test.ts` line 169 pins `ACTION_IDS` at 183 and reads 186 now (the test is the integrator's to bump to 186 with the words "and three logo rows in the features round"), and `pnpm generate:contracts` rewrites `packages/agent/generated/*` (the four `packages/agent` tests that read the generated files fail until it runs: `skills.test.ts` "actions.md names every action" and "browser-api.md names every window action", `contracts.test.ts` "are committed and current" and "give every action a row").

R3 (B7, `apps/studio/src/server/agent-actions.ts`): landed by B7 on the checkout as `SERVER_SIDE_WINDOW_ACTIONS_F1` (filtered through `isActionId`) on both lists. The controller (`apps/studio/src/editor/controller.tsx` 3609, the integrator's) runs the three through `serverSide(id)` the way it runs `SERVER_SIDE_WINDOW_ACTIONS_P1`; if it iterates the sublists by name, one line, `for (const id of SERVER_SIDE_WINDOW_ACTIONS_F1) serverSide(id);`, is still needed for the window transport. Until it lands the dialog reaches the same handlers through the same origin route `POST /api/logo/insert?deck=<id>` and `GET /api/logo/search` of `routes/api/logo.$.ts` (B1's `insertLogo` already does both).

R4 (B7, `apps/studio/src/server/actions.ts`): landed by B7 as `registerLogoActionsLazily` in `deckDispatcher`, loading `./logos` on the first call; `/api/actions/logo.search`, `/api/actions/logo.insert` and `deck_logo_search` answered 200 on the dev server (section 3).

R5 (B7, `apps/studio/src/server/flags.ts`): the variable name `TURBOSLIDE_LOGO_UPSTREAM` with its three values `fixture`, `down` and unset; B6 reads it in `logo-index.ts` (`LOGO_UPSTREAM_ENV`, `logoUpstreamMode(env)`) and B7 may re export the constant from there.

R6 (B3, `packages/viewer/src/Selection.tsx` `displayName` at 382 to 397): the chip word `Logo` for a `shot` or `picture` whose asset carries `role: 'logo'`; the function takes the deck's assets (or the caller passes the role), one branch before the `DISPLAY_NAMES` read. Row `logos.insert.logo-size` reads the chip.

R7 (B1, `packages/chrome/src/dialogs/Tailor.tsx`): the button "Find the <To> logo" (`dialog.tailor.logo.find`, the words `LOGO_WORDS.findLogo(to)`) beside the chooser, drawn once `logoMatchFor(rows, to)` of `@turboslide/chrome/logo-model` answers a row for the To field (the rows from `GET /api/logo/search?q=<to>&limit=5`, debounced with the field); its click runs `findCustomerLogo(row, appearance, insert, baseRevision)` of the same module, which stores the mark through `logo.insert` with no slide and no kit write and answers the asset id, which the dialog then names in `deck.tailor { logo: { assetId, replaceAlt: from } }` in the one Tailor commit as the chooser's file does today. The two choosers drop `image/svg+xml` from their accept lists until the svg intake lands (4.5). Row `logos.tailor.find-customer-logo`: not driven (the button is not on the checkout).

R8 (the integrator, `packages/chrome/package.json`, `apps/studio/package.json`): the export line `"./logo-model": "./src/logo-model.ts"` landed by B6 in the chrome's package.json because the studio server imports the shared rules from it (B1's dialog imports it relatively); `apps/studio/package.json` should gain `"sharp": "catalog:"`, because `logos.ts` and `logo-index.ts` import sharp directly (the file resolves today through the root's hoisted copy, and the deploy build externalizes sharp already, `externalSharp()`); the integrator installs once.

R9 (B4, `docs/gslides-parity/focus/core-matrix.json`): the rows landed. One note on `logos.insert.one-click-asset`: on the blob tier a logo's twins are served from the public store's host like every picture's, so "the sheet's img src is same origin" holds on a checkout and on the tmp store and reads "never a thesvg.org or jsDelivr address" on the blob tier.

R10 (B1, `packages/chrome/src/dialogs/Logo.tsx`): landed by B1 against this contract: the rows through `GET /api/logo/search?q=&limit=&kind=&collection=` (the `logo.search` answer), a tile's two halves from `/api/logo/mark/<slug>/<variant>.svg` where the variant is `chooseVariant(row, appearance, options)`, the insert through `input.dispatch('logo.insert', ...)` (or the route until R3's controller line) followed by `input.editor.insertLogoAsset(answer.asset, { title, variant, everySlide, blockId })`, which places the picture at the logo size and, with `everySlide`, writes the kit's three slots in the same commit and says "The <Title> logo is on every slide" with Undo; the foot sentence is `LOGO_WORDS.source(facts)` over the search answer's `updatedAt` and `lastError`.

R11 (the integrator or the CLI's owner, `apps/cli/src/cli.ts` and a new `apps/cli/src/commands/logo.ts`): the CLI's command table is hand written per group (`commands/fonts.ts`, `commands/brand.ts`), so `turboslide logo search figma`, `turboslide logo insert figma --slide <id> --every-slide --kit --block <id>` and `turboslide logo refresh --dry-run` need one `logo(ctx)` command in the shape of `brand.ts`: the three subcommands parse their flags with `flagString` and `flagBoolean` and run the action through `runAction` (which takes `--to <studio>` to the hosted instance over `POST /api/actions/<id>` with the saved credential); on a checkout without `--to` the local dispatcher has no handler and answers `NotImplementedError`, which is right, because a checkout has no index (the index is the deployment's). Measured on the checkout: `turboslide logo search figma --to http://localhost:4416` answers `unknown command "logo"`. Row `logos.agent.search-insert` is driven over HTTP and the same origin route (section 3); its CLI clause waits for this command.

R12 (B1, `packages/chrome/src/dialogs/Logo.tsx`, `editor-shell.ts` `DialogRequest`): a `kit` target for the P1 button of 4.5 (`panel.brand.logo.find`, `LOGO_WORDS.findAKit` and `findAKitDoc` in `logo-model.ts`): the dialog opened with `{ id: 'logo', kit: true }` inserts through `logo.insert { slug, variant, kit: true, baseRevision }` (the handler writes `/mark`, `/footer/logo` and `/footer/assetId` and places no block, section 2 item 5) and says `LOGO_WORDS.everySlide(title)` with Undo. B6 did not land the button in `ThemesPanel.tsx` (section 5, P1): without the target the button would insert a picture instead of setting the kit, a half landed item.

### 2. Items landed, with their files and hunks

Each is one item of docs/FEATURES.md section 4; the hunks are named so a red row's item reverts alone.

1. The `logo` source kind (4.4; `packages/schema/src/assets.ts`): `LogoAssetSource` in the `AssetSource` union (the block after `{ kind: 'file' }`) and its `z.strictObject` in `assetSourceSchema` (the entry after `kind: 'file'`). Additive; `assetSchema.safeParse` of a logo asset is pinned in `logos.test.ts`. Row `logos.insert.one-click-asset`.

2. The shared rules (4.2, 4.3, 4.4, 4.6; `packages/chrome/src/logo-model.ts`, new, and `logo-model.test.ts`, new; `packages/chrome/package.json` one export line): `LogoRow` and the index facts, the search ranking (`rankLogos`, `normalizeLogoText`, `matchTier`, `logoMatchFor`), the licence classes and sentences (`licenceClassOf`, `licenceSentenceOf`, `licenceTooltipOf`, `isOpenLicence`, `tintAllowed`), the appearance rule (`chooseVariant`), the Mono decision (`monoOffered`), the kit colours (`kitTextColour`, `kitBackgroundColour`), the size rules (`logoInsertSize`, `logoBoxIn`, `logoRasterSize`, `insertKindOf`), the words (`LOGO_WORDS`, `logoIndexDate`, `licenceLinkOf`, `logoMarkPath`, `logoAssetId`) and the Tailor handler (`findCustomerLogo`). 19 tests.

3. The sanitizer (4.7; `apps/studio/src/server/logo-sanitize.ts`, new): a strict XML reader (`parseSvg`: the declaration, comments, processing instructions and a plain DOCTYPE skipped, a DOCTYPE with an internal subset refused, CDATA as text, a bare `&` or an unknown entity, an unquoted or repeated attribute, a mismatched or unclosed element, a second root or content after it refused), the allowlist walk (`sanitizeTree`, `KEPT_ELEMENTS`, `KEPT_ATTRIBUTES`, the `on*`, `href`, `url(`, `style` and `<style>` rules, `draws` when a drop changes the look), `sanitizeLogoSvg` (the 256 KB cap before parsing, the viewBox from width and height, the size), `tintLogoSvg` (every fill and stroke that is not none in attributes, style attributes and stylesheets, plus a root fill and color), `withAttribution`, `attributionOf`, `descOf`, `sniffSvg`, `LogoBrokenError` and `LogoTooLargeError`. B7's svg intake (`actions.ts` `setStudioIntakePolicy`) imports `sanitizeLogoSvg` from here.

4. The fixture upstream (4.9; `apps/studio/src/server/logo-fixtures/manifest.ts` and `marks.ts`, new): ten marks, eight of them the files thesvg.org served on 2026-09-22 through the jsDelivr mirror verbatim (Figma, Vercel, GitHub, Stripe, Anthropic, OpenAI, Amazon EC2) and three of Turboslide's own (Acme: a Trademark licence, a stylesheet, an explicit fill mono, a wordmark path answering 404; Gradientco: a community mark whose mono is a gradient fill; Northwind: the slug dropped from the manifest once the index holds it). `FIXTURE_DROPPED_SLUG`, `FIXTURE_MISSING_PATH`.

5. The index and its refresh (4.2, 4.9; `apps/studio/src/server/logo-index.ts`, new): the upstreams (`networkUpstream` over the capture allowlist's own `pinnedFetch` and `resolvePinned` with `LOGO_HOSTS`, the User-Agent, 10 s, 256 KB, three redirects; `fixtureUpstream`; `downUpstream`), the row shape (`rowOfIcon`, `rowsOfManifest`: variant paths from the manifest alone, a record without a default path or with registry.json's key list is no row), the index file (`LogoIndex`, `parseLogoIndex`, `logoIndexBytes`, `cachedMarkKey`), the stores (`diskLogoStore` under `<state>/logos/`, `blobLogoStore` under `system/logo-index.json` and `system/logos/<slug>/<variant>.svg` with `contentType` and a week of `cacheControlMaxAge` and no other metadata, `memoryLogoStore` for the tests), the reads measure (`litFraction`, `readsFlagsOf`, `sharpRasterizer`, `densityFor`: 96 px, 0.2 percent over 40 luminance steps), `mergeLogoRows`, `wantsFlags`, `measureRow` and `refreshLogoIndex` (the manifest once, the merge, the takedown of dropped slugs with their cached files, the eviction of cached marks older than a week or no longer open or available, the reads pass at a concurrency of 8 inside the 240 s budget, the partial file with `progress`, `updatedAt` at the previous complete build, `lastError` on a failed upstream, the dry run's counts with no fetch).

6. The service, the cache rule, the insert and the handlers (4.2, 4.4, 4.9, 4.11; `apps/studio/src/server/logos.ts`, new): `createLogoService` (the index held per instance, read once, a `head` at most once an hour on a request, the fixture mode's inline first build, the search, `mark()` with the cache rule of 4.2 (an open licence mark written to the store with the attribution `<desc>` and served from there; every other mark fetched at each call, kept in this instance's memory ten minutes and never written), a 404 or a broken file marking the variant unavailable and persisting at most once a minute per instance, an upstream failure recorded for the foot), `logoService()` (the deployment's: the disk store on a checkout and the tmp store, the Blob store hosted; the upstream by `TURBOSLIDE_LOGO_UPSTREAM`), `refreshCredential` (the agent bearer or `CRON_SECRET`, `timingSafeEqual`), `kitLogoMutations` (the three kit writes of Use on every slide, on the server), `rasterizeLogoPng`, `logoPlacementOn` (the product round's free rectangle rule of `apps/studio/src/editor/place-insert.ts` with the logo size inside it), `logoInsert` (4.4 whole: the fetch through the service, the gradient mono not offered, the twins from the light and dark pair or the tint per appearance or one neutral file, the digest named `putAsset` files, the asset record, `slide.toCanvas` through the dispatcher when the slide is not a canvas, the block at the logo size, the swap of `blockId`'s asset, the kit's slots for `everySlide` and `kit`, one write against the store's head with the additive record's retry) and `registerLogoActions`.

7. The three actions (4.11; `packages/schema/src/actions.ts`): the ids in `ACTION_IDS` after `font.list`; `logoRowSchema`, `logoIndexErrorSchema` and `logoIndexProgressSchema` after `noWindow`; the entries `logo.search` (deck less read, every transport, `deck_logo_search`, `turboslide logo search <query>`), `logo.insert` (mutates, every transport, `deck_logo_insert`, joins the two lists of `agent-actions.ts` through B7's R3) and `logo.refresh` (a read of the index, `cli`, `mcp` and `http` and never the window transport, `deck_logo_refresh`, `turboslide logo refresh --dry-run`) after `font.list`. See R2 for the counts.

8. The route (4.2, 4.7, 4.11; `apps/studio/src/routes/api/logo.$.ts`, new; `apps/studio/src/routeTree.gen.ts` regenerated with `tsr generate`, a gitignored file): `GET /api/logo/mark/<slug>/<variant>.svg` with `image/svg+xml`, `nosniff`, `cross-origin-resource-policy: same-site`, `content-security-policy: sandbox; default-src 'none'` and `public, max-age=3600`; `GET /api/logo/search?q=&limit=&kind=&collection=`; `POST /api/logo/refresh { dryRun? }` under the two credentials; `POST /api/logo/insert?deck=<id>` for the page (the same origin session, `authorize()` for `write`, the readOnly switch, the draft created first as the asset actions create it) and for an agent (the bearer), through the deck dispatcher's `logo.insert` when it is registered and the handler directly otherwise; an upstream failure is 503 with the sentence and `retry-after`.

9. The cron (4.2; `apps/studio/vercel.json`): `crons: [{ path: '/api/logo/refresh', schedule: '0 6 * * *' }]`. `CRON_SECRET` is a project variable Kevin sets beside the others; this file never prints it.

10. The editor's placement (4.4; `packages/viewer/src/Editor.tsx`): `insertLogoAsset(asset, { title, variant?, everySlide?, blockId? })` on `EditorHandle` (after `insertPictureAsset`), its implementation after `insertPictureAsset` (the logo size rule repeated in the viewer because the viewer cannot import the chrome, the free area through `pictureInsertArea`, one `commitCanvas` with the block insert, the placeholders it takes the place of and the kit's three writes through `brandWriteMutation`, the selection on the new block, the snackbar `The <title> logo is on every slide` with Undo and nothing on a plain insert, the `blockId` swap with the box kept), the wiring in the handle object, and the imports `brandWriteMutation` and `PICTURE_MARGIN`. B1's dialog calls it.

11. The kit plan over an asset (4.4; `packages/chrome/src/brand/UseOnEverySlide.tsx`): `useOnEverySlidePlanForAsset(document, assetId)`, which `useOnEverySlidePlan` now delegates to, for the dialog's `everySlide` and the P1 Find a logo.

12. The notices (4.6; `THIRD_PARTY_NOTICES.md`): the section "thesvg.org (MIT code; brand marks under their recorded licences)" before "Everything else": the code's licence, the marks' owners, the recorded licence per mark and the seller's sentences, what the store redistributes (open licences alone, the attribution desc, the takedown), the untinted CC BY-ND marks, the fixture's eight real files.

13. The tests (7.3): `apps/studio/src/server/logos.test.ts` (25 tests) and `packages/chrome/src/logo-model.test.ts` (19 tests); section 4.

Not landed: the P1 Find a logo button of 4.5 (`ThemesPanel.tsx`), section 5.

### 3. The rows and what happened on the dev server

The server: `apps/studio`, `TURBOSLIDE_STORE=tmp TURBOSLIDE_OVERLAY_DIR=<scratch>/overlay TURBOSLIDE_REALTIME=memory TURBOSLIDE_LOCAL_OPEN=1 TURBOSLIDE_LOGO_UPSTREAM=fixture` (then `down`), a fake `TURBOSLIDE_TOKEN` and a fake `CRON_SECRET` so the credential rows could be driven, the two secrets at 32 characters or more, `node_modules/.bin/vite dev -c vite.no-watch.config.ts --port 4416 --strictPort`, stopped before the return. The drive scripts (`drive-routes.mjs`, `drive-offline.mjs`) are B6's scratch, not matrix drivers; the walk and the specs are B4's. The scratch decks were made from `deck.create` and trashed and deleted forever at the end (each answered 404 on `/deck/<id>`). The clean run of 2026-09-22 13:58 UTC (the fixture upstream, then the down upstream over the same overlay):

| Row                               | What happened                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Verdict here                                                                                                                                                 |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `logos.route.mark-headers`        | `GET /api/logo/mark/figma/default.svg`: 200, `image/svg+xml; charset=utf-8`, `nosniff`, `cross-origin-resource-policy: same-site`, `content-security-policy: sandbox; default-src 'none'`, `cache-control: public, max-age=3600`, 1,158 bytes, no `<script`, no `on*` attribute, no external `href`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | passed on the route; the fixture cases are the unit tests                                                                                                    |
| `logos.index.refresh-dry-run`     | `POST /api/logo/refresh { dryRun: true }` with no credential 401; with the bearer 200 in 6 ms with `icons`, `brands`, `cachedMarks`, `unavailable`, `updatedAt`, `lastError`, `progress`, `upstream`; with `Bearer <CRON_SECRET>` 200 and `credential: cron`; a wrong secret 401; no upstream fetch (the fixture upstream's manifest is in process and the counts came from the held index)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | passed                                                                                                                                                       |
| `logos.index.refresh-fixture`     | Northwind listed and its default cached (200); `acme/wordmark.svg` 404 with "Acme has no wordmark on thesvg.org" and the row's `unavailable.wordmark` `{ status: 404, reason: 'missing' }`; the refresh answered `dropped: ["northwind"]`, `cachedMarks` 2 to 1; Northwind then unlisted and its mark 404; the acme mark stayed unavailable across the refresh                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | passed on the fixture; not driven on production by design (4.9)                                                                                              |
| `logos.index.cached-offline`      | The down server over the fixture run's overlay: the dry run read `upstream: down`; the refresh kept the 9 icons and recorded `lastError { status: 0, message: 'thesvg.org did not answer' }`; `logo.search` answered Figma first from the cache with `updatedAt` and `lastError`, so the foot reads the date and "thesvg.org did not answer at 13:58 UTC"; the cached Figma mark served 200 with the attribution desc; the uncached Stripe mark 503 with the sentence; the Figma insert landed the asset and the block; the Stripe insert answered 503 with the sentence "thesvg.org did not answer; try again in a minute" and `retry-after` (re driven at 14:02 UTC after the route's last hunk; the first run read 500 through the agent surface's error mapping, which `/api/actions/logo.insert` still answers for an upstream failure because `errorStatus` knows no 503 class) | passed on the fixture; not driven on production by design (4.9)                                                                                              |
| `logos.cache.open-licence-only`   | After the Figma (CC0) mark the dry run's `cachedMarks` held it and the file carried "Figma logo, from thesvg.org under CC0-1.0; the mark belongs to its owner" as its `<desc>`; the AWS mark (CC BY-ND 2.0) served 200 with `#ED7100` intact and `cachedMarks` unchanged                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | passed                                                                                                                                                       |
| `logos.agent.search-insert`       | `POST /api/actions/logo.search` 200 (Figma first, "Free to use"); `GET /api/logo/search?q=figma` 200; `POST /api/actions/logo.insert { slug, slideId, baseRevision }` with the bearer 200 with the asset (`role: logo`, `source.kind: logo`, `provider: thesvg`, `scale: 3`, `sourceFile`), `blockId: logo` on the blank deck's title slide (converted through `slide.toCanvas` by the render worker), `deck.info` counting 4 then 5 assets; the CLI clause `turboslide logo search figma` answers `unknown command "logo"` (R11); MCP not driven here (the same dispatcher answers `deck_logo_search`, the tool list is generated at the merge)                                                                                                                                                                                                                                      | passed over HTTP and the route; the CLI and MCP clauses not driven                                                                                           |
| `logos.insert.one-click-asset`    | The server path: the record carries no thesvg.org or jsDelivr address, the twins are `assets/<id>.<digest>.png` served from `/decks/<id>/assets/` on this store, `role: logo`, `source.kind: logo`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | the server path passed; the one click in the dialog is B1's and B4's to drive                                                                                |
| `logos.insert.logo-size`          | The server path: the Figma symbol landed at `{ x: 832, y: 370, w: 108, h: 160, z: 3 }` inside the body slot, over no text run; the editor's `insertLogoAsset` places by the same rule                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | the server path passed; the chip word is R6 (B3), the dialog gesture B1's and B4's                                                                           |
| `logos.insert.mono-tint`          | The server path on a dark deck: GitHub took its `light` variant (the pair exists, so no tint is needed); Figma asked for as `mono` recorded `tint { light: '#070707', dark: '#f2f2f0' }`, its `sourceFile` served 200 with every fill `#f2f2f0` and no `#000` left; the AWS mark inserted with no tint under `CC-BY-ND-2.0`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | the server path passed; the row's GitHub clause reads the pair, not a tinted mono, because GitHub has `light` and `dark` files (4.3's rule prefers the pair) |
| `logos.insert.every-slide`        | The server path: `everySlide: true` wrote `brand.mark { kind: 'picture', assetId: 'stripe' }` and `brand.footer { logo: 'picture', assetId: 'stripe' }` in the insert's one write                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | the server path passed; the check's words, the snackbar and Cmd+Z are the dialog's and the editor's (`insertLogoAsset`), B1's and B4's to drive              |
| `logos.replace-image.row`         | The server path: `blockId` swapped the block's asset from figma to openai with its box kept                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | the server path passed; the menu row is R1, not on the checkout                                                                                              |
| `logos.tailor.find-customer-logo` | Not driven: the button is R7 (B1); the handler `findCustomerLogo` is in `logo-model.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | not driven                                                                                                                                                   |
| `logos.kit.find-a-logo` (P1)      | Not landed (section 5)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | not driven                                                                                                                                                   |

The search against the real index, read only: `node -e` over the fetched `icons.json` of 2026-09-22 (3,284,459 bytes, 7,417 records, the collections brands 4,681, community 259, auth-badges 860, aws 739, azure 626, gcp 214, k8s 38, the 57 licence strings) through `rowsOfManifest` and `rankLogos`: "figma" answers Figma first; the 57 strings all map to one of the five sentences (`logo-model.test.ts` pins the list). No refresh was run against the network from the dev server; the network upstream's fetch path is exercised by the tests through an injected fetch only where it is pure, and the first production build is the ship step's `POST /api/logo/refresh` (the bound of 4.2: about 4,940 marks at a concurrency of 8 inside 240 s, resumed by the next invocation when it stops).

### 4. The tests

- `apps/studio/src/server/logos.test.ts`, 25 passed: the sanitizer's fixtures (script, foreignObject, onload, image and an external href removed with the drops recorded; filter with feGaussianBlur and pattern kept and rendered by sharp from a buffer with no base URL to a lit raster; text dropped with its subtree and the variant marked unavailable through `measureRow`; `style="fill:url(http://…)"` removed whole and `fill="url(#g1)"` kept; a stylesheet that imports dropped and one that draws kept; a 300 KB file refused before parsing with the sentence naming the cap; seven broken files including the entity case and a DOCTYPE with an internal subset answering "This logo's file is broken on thesvg.org"; the viewBox from width and height; the attribution desc added to a cached file, first and once; every fixture mark passing unchanged; the tint over attributes, style attributes, stylesheets and the root, leaving `none`), the index (variant paths from icons.json alone, the merge keeping flags while the default path stands and forgetting an unavailable mark whose path moved, the reads flags of Vercel, Figma and Anthropic with sharp), the refresh (the ten mark build with the 404 discovered at the fetch, the takedown of Northwind with its cached file at the second refresh, the previous file kept with `lastError` on a down upstream and the search answering from the cache, the budget stop with `progress` and the resume, the dry run with no fetch and no write, the cache rule for CC0 against CC BY-ND, the ranking, the four credential cases), the insert (the asset with its source and the block at the logo size in one write with the version note; the tint per appearance and the untinted CC BY-ND mark and the paired twins; `everySlide`, `kit` and the `blockId` swap; the gradient mono not offered; the unknown slug, the missing variant and the second id), the placement, the sharp PNG, and the three actions through a dispatcher in the table's shapes.
- `packages/chrome/src/logo-model.test.ts`, 19 passed: the 57 licence strings mapped with "The brand's own terms" as the fallback, the cache and tint rules per licence, the appearance rule on paper and on ink for the fixture marks, the unavailable variant skipped, the gradient mono not offered, the kit's text colour, the ranking, the aliases and categories, the collection switch, the limit and the wordmark kind, the Tailor match, the size rules, the words and the ids.
- The suites of the packages touched: `packages/viewer` 412 passed; `packages/schema` 582 passed and 1 failed, `actions-gs3.test.ts` pinning `ACTION_IDS` at 183 (now 186; R2); `packages/agent` 4 failed, the generated contracts (R2). `node_modules/.bin/tsc -b apps/studio` passes but for B1's in progress `packages/chrome/src/__tests__/logo-dialog.test.tsx` and B3's `format-options-round-two.test.tsx`, neither B6's.

### 5. Notes and decisions

- The licence classes (4.6, question 2): open is CC0, MIT, Apache, BSD, ISC and Unlicense (the store cache); credit is CC BY and CC BY-SA; unchanged is CC BY-ND; copyleft is MPL, GPL, LGPL and AGPL; everything else, a non commercial clause (`CC-BY-NC-*`), `PD`, `TODO`, brand-use, Trademark, Fair Use, Proprietary, Custom, Unknown and the free text sentences, is the brand's own terms. The tint is on for open, credit and copyleft (each allows a derivative) and off for unchanged and own; `PD` is not cached because the spec's list is explicit. Kevin may widen either list in one function each (`licenceClassOf`, `tintAllowed`).
- The appearance rule prefers a brand's own light and dark files over a tint, so GitHub on a dark deck inserts its `light` file, never a tinted mono; the row `logos.insert.mono-tint` names GitHub's mono, which the rule reaches only for a mark without the pair (Stripe, Figma). A tinted mono is a light and dark pair of twins, each in the kit's text colour of its appearance, and its `sourceFile` carries the deck's current appearance's colour.
- A non open mark's `sourceFile` carries the attribution `<desc>` too (4.7 "the deck's sourceFile carries it too"); "unmodified" for CC BY-ND means no tint and no other change to what the file draws.
- The search route is open to any caller who can reach the server (a read of public data, the same rows a browser can read from thesvg.org); the refresh takes the two credentials; the insert takes the page's session under `authorize()` or the bearer.
- In fixture mode a fresh instance builds its index inline on the first read (ten marks), so the preview's spec project needs no refresh call first; in network mode nothing builds until `POST /api/logo/refresh` runs (the cron, the ship step or a marketer's `turboslide logo refresh` once R11 lands), and the search answers `updatedAt: null` until then, the foot reading "Logos from thesvg.org" with no date. A lane driving the picker on a network dev server runs `curl -X POST -H 'Authorization: Bearer <token>' http://localhost:<port>/api/logo/refresh` once (about a minute for the reads pass on a fast connection; the budget resumes otherwise).
- The store budget (docs/SYNC.md 4, 6.1): the index is one `get` per instance on the first read and one `head` at most an hour on a request; a discovery (an unavailable variant, a newly cached open mark) persists at most once a minute per instance; a tile of a cached open mark is one `get`; an insert is two or three `put`s of asset files plus one record write; nothing here polls or starts a timer. The refresh's own writes are the cron's once a day.
- The tmp store's state folder lives under the overlay (`TURBOSLIDE_OVERLAY_DIR`, `/tmp/turboslide` by default, shared by every tmp dev server on the machine); B6's server ran on its own overlay so no other lane's overlay changed.
- The insert on a slide that is not a canvas converts it through `slide.toCanvas` first (a separate write, as `slide.setBackgroundPicture` does), so an agent's insert on a fresh deck's title slide is two revisions; the editor's path (`insertLogoAsset`) is one commit.
- The size rule is repeated in `Editor.tsx` because the viewer cannot import the chrome (the one way direction of the packages); the numbers are the model's and both are tested.
- P1 (4.5, `logos.kit.find-a-logo`): not landed. The condition of section 1 (the lane's P0 rows green on its own preview run) cannot be read on the checkout, and the button needs B1's kit target (R12) to do what its words say; landing it without the target would insert a picture instead of setting the kit. The handler side exists (`logo.insert { kit: true }`), tested in `logos.test.ts`.
- What is not driven here and why: the dialog's gestures (B1's dialog and B4's drivers; the menu rows are R1), the chip word (R6), the Tailor button (R7), the CLI clause (R11), MCP (the tool list is generated at the merge), and the rows on production (the ship's runs).

## Features round, ship one, fix round

The B6 fixer on the branch `features/ship-one` at `57da794`, 2026-09-23 (UTC). The findings are the verifier's pass 1 (docs/gslides-parity/focus/VERIFICATION.md "Features round, ship one, pass 1", F.5 findings 1 to 5 as the task named them). What follows: the mechanism read first hand for each finding (section 1), the changes inside B6's files with their hunks (section 2), the requests to the other lanes and the integrator under the fix round's rule (section 3), the tests (section 4), the runs and what happened (section 5) and the deviations (section 6). No git write command, no `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran. The dev server was `apps/studio` on 4416 (`TURBOSLIDE_STORE=tmp`, its own overlay under the scratchpad, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, `TURBOSLIDE_LOGO_UPSTREAM=fixture`, a fake bearer and a fake `CRON_SECRET`, both secrets at 32 characters or more, `vite dev -c vite.no-watch.config.ts --port 4416 --strictPort`), stopped before the return. Every scratch deck made on the preview of record (`https://turboslide-e4sw7h7ay-kl01s-projects.vercel.app`, the integrator's, deployed from `44c2cc0`) was trashed and deleted forever through the actions API with the owner bearer and read back as `deck.info` 404: `b6-fixer-probe` (a first run of the probe exited before its teardown; removed at 06:47Z), `b6-fixer-probe-qqcld`, `untitled-20260923-cj7f`, `untitled-20260923-ebsn`. No token, cookie or secret was printed.

### 1. The mechanisms, read first hand

- F2, Tailor's Find the customer's logo on the blob tier. The verifier's reading of the mechanism (an asset only write dropped by the room) is not what happens: `logo.insert` with no slide lands in the store on the blob tier. Read over the actions API with the bearer on the preview at 06:47Z (`b6-tailor-probe.mjs`, the scratchpad): `deck.create` (revision 0, 4 assets), `logo.insert { slug: 'figma' }` answered revision 1 with the asset `figma` in 770 ms, four `deck.info` reads on four instances answered revision 1 and 5 assets, `sync.status` revision 1, and `deck.tailor { logo: { assetId: 'figma', replaceAlt: 'Acme' } }` answered 200. The refusal is the page's: `deck.tailor` runs in the page over the tab's document (`apps/studio/src/editor/controller.tsx` 3785, `on('deck.tailor', ...)`), and `logo.insert` is a server side window action registered through `serverSide(id)` without `announce` (controller.tsx 3639), so its answer returns as soon as the server wrote and nothing waits for the record to come back over the channel; on the blob tier the tab learns of a direct store write through the channel's head poll and a resync, seconds later, while the memory tier's `roomBackedStore` streams the write within milliseconds (which is why 4419 read green). Read through the page's own transport on the preview at 06:5xZ (`b6-tailor-page-probe.mjs`: `/new`, the heading typed so the deck exists and its room is attached, the tab reading `tier blob connected true` at revision 1): `logo.insert` through the window API answered revision 2 at 7.7 s while the tab stood at revision 1; `deck.tailor` at once was refused with `deck.tailor: no asset "figma" in this presentation`; the tab reached revision 2 at 15.4 s; the same `deck.tailor` then answered `replacements: 1, slideIds: ['title'], revision 3`. On a fresh `/new` draft with no room yet (`untitled-20260923-cj7f`) the first write creates the deck and the tab adopts it, so the same sequence passed there, which is not the seller's case. The picker's own path passes because `insertLogoAsset` waits for the asset in the tab's document (`waitFor`, `ASSET_WAIT_MS`) before it places the block; the walk's Tailor step passes because it waits for the revision before Apply (`logos.mjs` 1221), which a seller does not.
- F4, the refresh and another instance's search. As the verifier read it: the instance that answers the search holds its index for up to an hour (`INDEX_REVALIDATE_MS`), and nothing in a search asked the store whether a refresh had moved the version. Two more readings from the code: the mark route on a stale instance would have served, and cached again, the file of a taken down slug (a first fetch of an open licence mark never asked the store's version either, and the fixture upstream served the dropped slug's file whatever the index said), and a fresh instance's `refresh()` ran the manifest before it held any index, so the fixture's takedown (decided from the held index) needed a second refresh on that instance.
- F3, the title placement. The editor's path places through `pictureInsertArea` (packages/viewer/src/picture-place.ts): the title's empty lead is a `paragraph` with no letters, which the picture rule reads as the layout's empty body placeholder, and a one line placeholder "stands for its column of the body" from the content box's top to its bottom, so the area was the whole content box, the logo was centred in it at {746, 370, 108, 160}, over "renewal for", and the lead placeholder was removed in the same write (`replaces`). The server's path (`logoPlacementOn`) reads the free rectangles of the body under the head band; the title's h1 sits below the head band limit (sheet height over 3), so the body is the whole content box there too.
- F1, the canvas title's mark block. As the verifier read it: `renderMark` (packages/render/src/blocks/misc.ts) draws `<use href="#gt-mark">` for every `mark` block, and the block that `toCanvas` makes of the title's mark slot (packages/schema/src/canvas.ts 282 to 287) is a `mark` block, so once the title slide is a canvas the kit's picture is not read for it while the footer (stage.ts) follows the kit. Not B6's file; request R15.
- F5, MCP on a multi instance deployment. As the verifier and the integrator read it: `createMcpHttpHandler` keeps `entries = new Map` per instance and answers 404 for a session id it did not hand out. Not B6's file; request R16.

### 2. Changes inside B6's files

Each is one hunk or a few in one file, named so it reverts alone.

1. F2, `packages/chrome/src/logo-model.ts`: `findCustomerLogo` takes an optional fifth argument `wait: FindLogoLanded` (`landed(assetId)`, `timeoutMs` 15 s, `intervalMs` 100 ms, an injectable `sleep`) and answers `{ assetId, variant, landed }` only once the deck's document holds the asset, or after the bound with `landed: false`; without `wait` it answers as before (`landed: true`). The Tailor dialog's handler passes `landed` over the live document by R14; the controller's `announce` by R13 makes the store's own answer wait, which fixes the picker's and the P1 kit button's callers too.
2. F4, `apps/studio/src/server/logos.ts`: `SEARCH_REVALIDATE_MS` (2 s); `revalidate()` on the service (one head, the store's index when the version moved), which `index()` now calls at the hour; `search(query, options, { since })`: a search that names a refresh's `builtAt` the held copy is older than (ISO strings compared as strings) asks the store's version now, at most once per 2 s per instance (`revalidateSoon`), and answers the adopted index; `mark()`: a first fetch of an open licence mark on this instance (nothing cached for it in the held index) runs the same throttled head first, so a slug a refresh took down elsewhere answers 404 instead of a fresh copy written to the store again; a cached open mark whose file is gone from the store (`readMark` null) reads the index again, unthrottled, and answers 404 for a slug that left or its variant; `refresh()` reads the held index before the manifest, so a fresh instance's first refresh drops what the store's index holds and the fixture lacks; `registerLogoActions` passes `since` through when the table's input carries it (R18). `apps/studio/src/routes/api/logo.$.ts`: `GET /api/logo/search` reads `since` from the query string beside the table's input and passes it to the service. `apps/studio/src/server/logo-index.ts`: `fixtureUpstream.mark` answers 404 for the dropped slug's files once a built index no longer holds the slug (the takedown removes the files too), and serves them while the index holds it or before the first build.
3. F3, `packages/chrome/src/logo-model.ts`: `LOGO_TITLE_GAP` (40), `logoTitleArea(kind, objects, content)` (a converted title's mark slot row: from the mark block's right edge plus the gap to the content box's right edge minus the gap, at the mark's height, the left edge moved past any object already on the row; null when the slide is not a converted title, has no mark block or the row is full) and `logoBoxAtStart(area, size)` (the logo scaled down to the row, never up, at the area's left edge, centred on its height). `apps/studio/src/server/logos.ts` `logoPlacementOn`: the title rule first (`grammarRecordOf(slide)?.kind === 'title'`), the free rectangle rule after. `packages/viewer/src/Editor.tsx` `insertLogoAsset`: the same rule repeated (the viewer cannot import the chrome) before `pictureInsertArea`, with `replaces: []` on a title, so the title's placeholders stay; imports `grammarRecordOf` and `BODY_SLOT`.
4. The tests, section 4.

### 3. Requests, fix round

R13 (the integrator, `apps/studio/src/editor/controller.tsx` 3636 to 3639): `logo.insert` announces like `asset.add`, so the window transport's answer waits until the record has come back over the channel (a resync at once on the blob tier, b3.md R21's rule) and every caller reads a document that holds the asset (Tailor's Apply, the dialog's placement, the P1 kit button):

```ts
/* the logo picker's ids of the features round (docs/FEATURES.md 4.11; build/b6.md R3): the
     index and the store live on the server, so the search, the insert and the refresh run there.
     `logo.insert` writes this deck (the asset record, the kit's slots), so it announces as
     `asset.add` does: the answer waits until the write has come back over the channel (a resync
     at once on the blob tier), and Tailor's Apply, which plans in the page over the tab's
     document, finds the asset it names (the fix round; VERIFICATION.md pass 1 F2) */
for (const id of SERVER_SIDE_WINDOW_ACTIONS_F1)
  serverSide(id, id === 'logo.insert' ? { announce: true } : {});
```

and in `failureSentence`, before `default`: `case 'logo.insert': return \`The logo could not be added: ${reason}\`;`(the reason is a sentence, never an id; the dialog draws its own error line too, so the integrator may keep or drop the snackbar). Row`logos.tailor.find-customer-logo`; with it the walk's wait before Apply (R17) can go.

R14 (B1, `packages/chrome/src/dialogs/Tailor.tsx`, `defaultLogoFinder` and `TailorDialog`; B6 owns the handler's rules in logo-model.ts): the finder passes the live document to `findCustomerLogo`'s wait and reads `landed`:

```ts
export function defaultLogoFinder(input: {
  deckId: string;
  revision: number;
  dispatch: EditorShellInput['dispatch'];
  document: DeckDocument;
  /** the document as it stands when asked, so the wait for the stored mark reads the live one */
  documentNow?: () => DeckDocument;
}): TailorLogoFinder {
  const current = input.documentNow ?? (() => input.document);
  return {
    match: async (name) => { ... as today ... },
    store: async (row) => {
      const stored = await findCustomerLogo(
        row,
        deckAppearance(input.document.deck),
        (request) =>
          insertLogo(request, { dispatch: input.dispatch, deckId: input.deckId }).then(
            (answer) => ({ asset: { id: answer.asset.id } }),
          ),
        input.revision,
        { landed: (assetId) => current().deck.assets[assetId] !== undefined },
      );
      if (stored === null) throw new Error(`${row.title} has no variant that reads on this deck`);
      if (!stored.landed)
        throw new Error(
          `The ${row.title} logo is stored but has not reached this presentation yet; try again in a moment`,
        );
      return { assetId: stored.assetId };
    },
  };
}
```

In `TailorDialog`: `const documentRef = useRef(input.document); documentRef.current = input.document;` and `documentNow: () => documentRef.current` in the `defaultLogoFinder` call (the `useMemo` keeps its dependencies). The error line: a refusal's message loses its action id before it is shown (`err.message.replace(/^[a-z]+\.[a-zA-Z.]+: /, '')`), so the sheet never reads `deck.tailor:` (the copy rule; the verifier's F2 "the dialog also shows an action id and a slug"). Row `logos.tailor.find-customer-logo`.

R15 (the integrator, `packages/render`): the canvas title's mark block follows the brand kit as the slot did before the conversion (the verifier's F1; row `logos.insert.every-slide`, which by 7.2 parks `dialog.logo.everySlide` until this lands). Three files, one idea: the slide's context names the block that is the title's mark slot and the kit's mark record; `renderMark` draws the kit's picture into that block's box, nothing for `none`, the GT glyph otherwise; every other `mark` block (the brand deck's specimens) renders as today.

`packages/render/src/blocks/context.ts`, on `BlockContext`:

```ts
  /**
   * The title slide's mark slot once the slide is a canvas (schema/canvas.ts toCanvas makes a
   * `mark` block of it): that block's id and the brand kit's mark record, so the block draws what
   * the slot drew (docs/PRODUCT.md 4.1, 4.4: the kit's picture, nothing for none, the GT glyph
   * for default). Absent on every other slide, where a `mark` block is the brand deck's specimen.
   */
  titleMark?: { blockId: string; mark: BrandKit['mark'] | undefined };
```

`packages/render/src/slide.ts`, in `renderSlide`'s `ctx` (imports `grammarRecordOf` from `@turboslide/schema/canvas`):

```ts
    ...(titleMarkOf(deck, slide) !== undefined ? { titleMark: titleMarkOf(deck, slide) } : {}),
```

```ts
/** The canvas title's mark slot: the first block of the record's main slot and the kit's mark record (the fix round, F1). */
function titleMarkOf(deck: Deck, slide: Slide): BlockContext['titleMark'] {
  const record = grammarRecordOf(slide);
  if (record === null || record.kind !== 'title') return undefined;
  const blockId = record.slots?.main?.[0];
  return blockId === undefined ? undefined : { blockId, mark: deck.brand?.mark };
}
```

`packages/render/src/blocks/misc.ts`, `renderMark` (imports `classes` from `../html.ts` and `attrs`, `twinAttrs` beside `markSvg`):

```ts
export function renderMark(block: BlockOf<'mark'>, ctx: BlockContext): string {
  const attributes = rootAttrs(block, ctx, {});
  const extra: Record<string, string> = {};
  for (const [name, value] of Object.entries(attributes))
    if (value !== undefined) extra[name] = value;
  // the canvas title's mark slot follows the brand kit as the slot did before the conversion
  // (slide.ts titleMarkSlot): the kit's picture fitted into the block's box, nothing for none, the
  // GT glyph for default and for a picture whose asset the deck lacks
  const slot = ctx.titleMark;
  if (slot !== undefined && slot.blockId === block.id && block.pos !== undefined) {
    const kind = slot.mark?.kind ?? 'default';
    if (kind === 'none')
      return `<div${dataAttrs({ ...extra, class: classes(extra['class'], 'mark-block', 'is-none') ?? 'mark-block is-none', 'data-slot': 'mark' })} aria-hidden="true"></div>`;
    const assetId = slot.mark?.assetId;
    const image = kind === 'picture' && assetId !== undefined ? ctx.image(assetId) : undefined;
    if (image !== undefined)
      return `<img${dataAttrs({ ...extra, class: classes(extra['class'], 'mark', 'mark-picture', 'mark-block') ?? 'mark mark-picture mark-block', 'data-slot': 'mark' })} src="${escapeAttr(image.src)}"${attrs(twinAttrs(image))} alt="${escapeAttr(image.alt)}"${dataAttrs(raster(ctx, block.id, 'mark', true))}>`;
  }
  return markSvg(ctx, block.id, block.w, block.h, extra, block.pos === undefined);
}
```

`packages/render/src/block-css.ts`, beside the `svg.mark-block` rule: `.ts-sheet .free > img.mark-block, .ts-sheet .free > .link > img.mark-block { display: block; width: 100%; height: 100%; object-fit: contain; object-position: left center; }`. The walk's driver reads `img.mark, .mark img, img[data-slot="mark"]` for the title slot (logos.mjs 1049), which this markup satisfies; the exporter shoots the element by its `data-rid` as it shoots the glyph today. A test in `packages/render` (theme-css.test.ts's neighbour): a converted title with `brand.mark { kind: 'picture', assetId }` renders the block as `img.mark-picture` with the asset's src, with `kind: 'none'` as an empty `.mark-block.is-none`, and a `mark` block of a content slide as `<use href="#gt-mark">` unchanged.

R16 (the integrator, the MCP transport's owner; `packages/mcp/src/http.ts`): a session another instance opened is rebuilt on the instance the request lands on, under the same id, from the request itself (the verifier's F5; row `logos.agent.search-insert`, which has no `parks` and by 7.2 as written parks `logos` whole while red). The session id is opaque and every fact `createServer` binds comes from the request (the deck in the query, the author in its header, the key from the bearer), so nothing is lost but this instance's count for the key. The SDK's transport answers a request only after its own `initialize` (`validateSession`: "Server not initialized"), so one is fed to it first with the client's protocol version and its answer discarded. A GET (the notification stream) and a DELETE are not resumed: the client opens the stream again after a 404, and a DELETE of an unknown session names one that is closed here already.

```ts
/**
 * A session another instance opened (a deployment of several instances: `initialize` answered
 * there and this POST landed here): rebuilt under the same id from the request, which carries
 * everything `createServer` binds. The transport answers only after its own initialize, so one
 * is fed to it first and its answer discarded. POST alone; null leaves the 404 as it was.
 */
const resume = async (
  request: Request,
  sessionId: string,
  key: KeyBinding | null,
): Promise<Entry | null> => {
  if (request.method !== 'POST') return null;
  let created: CreatedSession;
  try {
    created = await options.createServer(request, sessionId, key);
  } catch {
    return null;
  }
  const transport = new WebStandardStreamableHTTPServerTransport({
    sessionIdGenerator: () => sessionId,
    ...(options.enableJsonResponse !== undefined
      ? { enableJsonResponse: options.enableJsonResponse }
      : {}),
    onsessionclosed: (closed) => {
      const entry = entries.get(closed);
      if (entry) void closeEntry(entry);
    },
  });
  const entry: Entry = {
    session: {
      id: sessionId,
      createdAt: stamp(),
      lastSeenAt: stamp(),
      facts: { ...(created.facts ?? {}), resumed: true },
      ...(key !== null ? { key } : {}),
    },
    transport,
    server: created.server,
  };
  entries.set(sessionId, entry);
  transport.onclose = () => {
    if (entries.get(sessionId) === entry) void closeEntry(entry);
  };
  await created.server.connect(transport);
  const protocolVersion = request.headers.get('mcp-protocol-version') ?? '2025-03-26';
  const handshake = new Request(request.url, {
    method: 'POST',
    headers: {
      'content-type': 'application/json',
      accept: 'application/json, text/event-stream',
    },
    body: JSON.stringify({
      jsonrpc: '2.0',
      id: 0,
      method: 'initialize',
      params: { protocolVersion, capabilities: {}, clientInfo: { name: 'resumed', version: '0' } },
    }),
  });
  const answer = await transport.handleRequest(handshake);
  await answer.body?.cancel().catch(() => undefined);
  log(`mcp http: session ${sessionId} resumed here (${JSON.stringify(entry.session.facts)})`);
  return entry;
};
```

and in `handle`, where the entry is looked up:

```ts
      if (sessionId) {
        let entry = entries.get(sessionId);
        if (!entry) entry = (await resume(request, sessionId, key)) ?? undefined;
        if (!entry)
          return rpcError(404, -32001, `Unknown MCP session ${sessionId}; initialize again`);
```

A test in `packages/mcp/src/http.test.ts`: two handlers over one `createServer` factory and one `now`; `initialize` on the first answers a session id; `tools/list` on the second with that id answers 200 and the tool list; `sessions()` of the second lists it with `resumed: true`; a GET with the id on the second still answers 404. The alternative the integrator's finding 16 names, a shared session table on the studio's KV (`sessions.ts` `redisSessionDirectory`), cannot carry the SDK's transport object across instances, so it would still rebuild the transport here; this hunk is that rebuild without the table.

R17 (B4, `apps/studio/e2e/core/logos.spec.ts` and `scripts/probes/core-walk/areas/logos.mjs`): two driver changes. The spec's `searchHttp` takes the refresh's build and the takedown read names it, so the row measures the same day promise through the mechanism the product offers and not the hour long hold of one instance (`Counts` gains `builtAt?: string | null`):

```ts
async function searchHttp(query: string, since?: string | null): Promise<{ ... }> {
  const res = await page.request.get(
    `/api/logo/search?q=${encodeURIComponent(query)}&limit=10${since ? `&since=${encodeURIComponent(since)}` : ''}`,
    { headers: extraHTTPHeaders, maxRedirects: 0 },
  );
```

and in `logos.index.refresh-fixture`, after the takedown: `const after = await searchHttp('northwind', (second ?? first).body?.builtAt ?? null);` (the CLI reads the same way: `turboslide logo refresh` answers `builtAt`, `turboslide logo search <query> --since <builtAt>` once R18 lands). The walk's Tailor step (`logos.mjs` 1219 to 1222) drops the `waitRevision(revStore + 1, 15_000)` before Apply and keeps `t.settled()`, so the row measures the seller's gesture, Apply as soon as the stored line reads; with R13 the store's answer itself waits, and without R13 the row reads red on the blob tier as the verifier read by hand. Rows `logos.index.refresh-fixture`, `logos.tailor.find-customer-logo`.

R18 (the integrator, `packages/schema/src/actions.ts`, the `logo.search` entry B6 landed): the input gains `since: z.string().optional().describe('A refresh’s builtAt: the answer comes from an index at least as new when the store holds one')` and the CLI usage `--since <builtAt>`; the handler in `server/logos.ts` passes it already; `pnpm generate:contracts` at the merge. Until it lands the same origin route carries `since` and the action's strict input refuses it.

R19 (B4 and Kevin, `docs/gslides-parity/focus/core-matrix.json`): `logos.agent.search-insert` carries no `parks` while FEATURES.md 4.12 says every `logos.*` row does; the row guards no control (the agent transports), so by 7.2 as written its red parks `logos` whole (the verifier's F.10 item 1). Whether the row takes an empty `parks` (its red then parks nothing and stays a red core row) or stays without is Kevin's call; this note records that the MCP half's mechanism is the transport's (R16), older than the ship and shared by every tool.

### 4. The tests

- `apps/studio/src/server/logos.test.ts` (28 passed, three new): "adopts another instance's refresh when a search names its build, and answers 404 for a taken down slug on a stale copy" (three services over one memory store and one clock: A builds and caches Northwind, B and C read the index, the clock moves a minute, A's refresh drops Northwind; B's plain search still lists it (the hour long hold is the design), B's search with `since` the refresh's `builtAt` lists nothing twice and B's mark route answers 404 unknown; a `since` the store never wrote costs one head and answers the held copy; C, which never searched and holds the cached entry, asks for the mark, meets the missing file, reads the index again and answers 404 without fetching); "refuses the dropped slug's file once a built index no longer holds it, and a fresh instance's refresh reads the store's index first" (the fixture serves the file before the first build and while the index holds the slug, a fresh service over a store holding it drops it on its first refresh, and the file answers 404 after); "places a logo beside the brand's mark on a converted title slide, never over the heading's words" (`toCanvas` of the TITLE fixture with an empty lead at the stage's boxes: a symbol at [309, 370, 57, 84], a wordmark at [309, 380, 320, 64], a second logo at [406, 370, 57, 84], and the general rule clear of the heading when the mark block is gone).
- `packages/chrome/src/logo-model.test.ts` (22 passed, three new): "answers once the deck's document holds the stored mark, and says when it never arrived" (`findCustomerLogo` looks three times at the interval, answers `landed: false` at the bound, and answers as before without a wait); "puts the logo beside the mark on a converted title slide and leaves every other slide to the general rule" (`logoTitleArea`: [309, 370, 1114, 84] on the title, null for a content slide, no record or no mark block, the left edge past an object on the row, a background picture ignored, a full row null); "scales the logo to the row and sits it at the area's left edge, centred on the row's height" (`logoBoxAtStart`).
- `node_modules/.bin/tsc -b` over the tree: clean (5 s) after every change.

### 5. The runs and what happened

The preview of record, the probes of section 1 (06:47Z to 06:59Z): the bearer probe's `logo.insert` landing on the blob tier (revision 0 to 1, assets 4 to 5, `deck.tailor` 200), the page probe's refusal at once and its success 7.7 s later, every scratch deck removed (`deck.info` 404 read back). Nothing on the preview changed but the scratch decks.

The dev server, 4416, the fixture upstream (07:04Z to 07:06Z, `b6-title-probe.mjs` and curl):

| Row or step                                                                                               | What happened                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The canvas title step (F3), the editor's path                                                             | `/new`, the title slide current; Insert > Logo, "figma", Enter: the slide became a canvas with its mark block at {137, 307.28, 132, 84}, the heading at {137, 439.28, 1326, 90}, the lead placeholder at {137, 555.03, 901, 38} kept, and the Figma mark landed at {309, 307, 57, 84}: beside the GT mark on its row, 40 px from it, scaled to the row's height, clear of the heading and the lead (the picture `b6-title-canvas-light-1440.png` in the scratchpad: GT, Figma and Stripe on one row over "Click to add title" and "Click to add text")                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| The canvas title step (F3), the server's path                                                             | `logo.insert { slug: 'stripe', slideId }` through the window API on the same slide: the block `logo-2` at {406, 307, 201, 84}, to the right of the Figma mark by 40 px on the mark's row (Stripe's default is wider than tall, so 201 by 84 at the row's height)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `logos.index.refresh-fixture`, the route (F4)                                                             | `GET /api/logo/search?q=figma&since=2099-01-01T00:00:00.000Z` 200 with Figma first (the bogus build costs one head and answers the held copy); `POST /api/logo/refresh` with the bearer: 9 icons, `dropped: ["northwind"]`, `cachedMarks` 2; `GET /api/logo/search?q=northwind` right after: `logos: []`, `indexed` 8; `GET /api/logo/mark/northwind/default.svg` 404 after 200 before the refresh. One instance here: the two instance reading is the unit test's                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| The walk, `editor-walk-probe.mjs --core --only logos` (07:05Z to 07:08Z, under the repository's e2e lock) | 33 steps, 31 ok, 2 not driven, 0 failed, 0 console errors in 170 s: every driven `logos.*` row of the walk passed on this tree (`logos.insert.row`, `picker.search` at 161 ms, `picker.paper-and-ink`, `picker.your-brand`, `picker.empty-state`, `picker.licence-words`, `insert.one-click-asset`, `insert.logo-size` at 467,414 108 by 160 centred in p1's column with the chip Logo, `insert.mono-tint` with Stripe's fills at the kit's text colour and AWS untinted, `insert.every-slide` on an untouched title slide with the title slot drawing the mark, `tailor.find-customer-logo` with the picture swapped and "Prepared for Figma" in one revision, `replace-image.row`, `intake.url-sentence`); `logos.picker.variants` and `logos.kit.find-a-logo` not driven (the P1 controls are not on the build, as before). The scratch deck `untitled-20260923-ka36` was removed by the walk (`/edit` and `/deck` 404). The gate exit is 1 because `--only logos` leaves every other area's rows without a step, the expected reading of a narrowed run |
| The gate's specs, `core-gate.mjs --only specs --spec logos --lock <scratch>` (07:08Z, 26 s)               | The first run read `logos.index.refresh-dry-run` and `logos.agent.search-insert` red and the two fixture rows not driven, all on this server's own configuration and not the product: the server ran with a fake `TURBOSLIDE_TOKEN` (the recipe of the build's curl drives), so the refresh route wanted the bearer the localhost spec does not send (`agentBearer` answers null on localhost) and the CLI met the same 401 (exit 2). The server was started again as the task names it (no bearer, no cron secret, a fresh overlay) and the specs run again; that run's reading is the row below                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| The gate's specs again, the server as the task names it (07:10Z to 07:11Z, 23 s)                          | 7 rows: 6 passed, 0 failed, 1 not driven: `logos.picker.recents`, `logos.route.mark-headers`, `logos.index.refresh-dry-run`, `logos.index.refresh-fixture` (the takedown read on the one instance, the search without `since` since R17 is B4's), `logos.cache.open-licence-only` and `logos.agent.search-insert` (the CLI, HTTP and MCP halves on one instance) passed; `logos.index.cached-offline` not driven by design (the fixture upstream, not the outage; the unit test covers it). The gate's own exit is 1 for that not driven row, the reading of every fixture run                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

### 6. Deviations and notes

- The parks rule as the fix round names it. Three of the four rows red in both preview runs have their product fix outside B6's files, so they stand as requests until the integrator applies them: `logos.insert.every-slide` (R15, the render's mark block; parks `dialog.logo.everySlide` while red), `logos.agent.search-insert` (R16, the MCP transport; no `parks`, so by 7.2 as written its red parks `logos` whole; Kevin's F.10 item 1, R19) and `logos.tailor.find-customer-logo`'s hand drive defect (R13 and R14; the row itself passes because its driver waits, R17). `logos.index.refresh-fixture`'s product half is inside B6's files and landed (section 2 item 2); its driver's half (the search that names the refresh's build) is B4's, R17; on a multi instance preview the row stays red until R17 lands, and then measures the same day promise through the mechanism the product offers. No control was parked by this lane: `parked-controls.ts`'s set is the ship step's, from the reruns.
- F2's mechanism is not the one the verifier named. The asset only write lands in the store on the blob tier (read over the API and through the page, section 1); what fails is the page's `deck.tailor` planning over a tab document that has not received the record yet, because `logo.insert`'s window action does not announce. The fix is the controller's (R13), one line, with the model's wait (section 2 item 1, landed) as the dialog's own guard through R14; room.ts and blob-store.ts, which the finding names, need no change.
- The store budget (docs/SYNC.md 4, 6.1). The changes add no timer and no listing: one `head` on the system index per instance at most every 2 s, and only on a search that names a newer build than the instance holds or on the first fetch of an open licence mark this instance has nothing cached for (a first tile draw of a mark, once per mark per instance lifetime); a cached mark's tile stays one `get`; the missing file case (a taken down mark) is one unthrottled `head` and one `get` of the index. Nothing rides the editor's idle or editing paths, so the cost rows' ceilings are untouched; the hour long hold of the index (`INDEX_REVALIDATE_MS`) stands as FEATURES.md 4.2 wrote it.
- The fixture upstream's files. Before this round the fixture served the dropped slug's file whatever the index said; now the files answer 404 once a built index no longer holds the slug, as thesvg.org's do after a removal, and serve before the first build and while the index holds it. On the fixture's cycle (the manifest re lists the slug once the index lacks it) a refresh after the takedown re adds the slug with an unavailable default, which the picker never shows, and the refresh after that drops it again; the spec's two refresh reading is unchanged.
- The title rule against FEATURES.md 4.4's letter ("centred in the body slot's free area ... at the slot's centre otherwise"): the title layout has no body slot, so the rule of 4.4 had no area to name there; the mark slot's row is the verifier's suggested place ("in the mark slot's free area") and the seller's co branded cover (question 10's default, the customer's mark as a placed picture). The title's placeholders are never removed by a logo insert; on every other slide the picture rule stands.
- Two probes and a driver of this round live in the scratchpad, not the tree: `b6-tailor-probe.mjs` (the API reading), `b6-tailor-page-probe.mjs` (the page reading), `b6-title-probe.mjs` (the title placement on the dev server) and the picture `b6-title-canvas-light-1440.png`; the verifier shoots the tree's pictures.
- Formatting: `prettier --write` ran over the files this round touched (`logos.test.ts`, `logo.$.ts`, this note); `tsc -b` over the tree is clean.
