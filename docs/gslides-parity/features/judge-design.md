# Judge design: the features round

The design and engineering judgment of `docs/FEATURES.md` (the synthesizer's draft of 2026-09-20 12:40), read against the four audits under `docs/gslides-parity/features/` (`audit-objects.md`, `audit-fonts.md`, `audit-logos.md`, `audit-shaders.md`), `docs/PRODUCT.md` sections 1, 3.7, 4.1, 4.2, 7, 8.1 and 8.2, `docs/RETURN.md` section 1, `docs/FOCUS.md` 6.1 and 6.2, `docs/gslides-parity/focus/AMENDMENTS.md` A1, `docs/gslides-parity/SPEC-5.md` 0.3 and 0.10, and the shared checkout with the product round's uncommitted tree. Every file the lane table names was checked for existence on the checkout; the counts of section 7.1 were recounted from the table; the mechanisms the document leans on were read in the source files named below. This judgment touched no source file and ran no git write command. The lens is design and engineering: does every proposal have a file, an acceptance row and a driver; are the lanes disjoint with a file table; is the two day size honest; does the shader block obey the renderer's resting rule and the export paths; does the logo route sanitize and cache; are the licences recorded correctly; does the acceptance keep the gate a safety net.

## Verdict

Revise. The document is complete in the ways that are hardest to get right: every item of sections 2 to 5 has a file, a behaviour, a `data-control` id, an undo rule and one or more rows; the 89 rows of 7.1 recount as 89 (33 broken, 56 not driven, 63 on the probe, 26 on the specs, as the document says); the P0, P1 and later counts and the effort sums recount from the audits' tables (58, 19, 9 and one, 111 points); the eight font families' bytes sum to 1,764,484; every file the lanes name exists on the checkout except the ones marked new; the fonts' licences are recorded the way the OFL asks (the licence text beside the files, no Reserved Font Name, a notices paragraph, an image specimen that is not font software); the Inter verdict is right in every particular; the decisions of section 1 (the `material` kind kept with `material.*` aliases, the A1 amendment for tables alone, the free rectangle helper reused, the client frame first with the hosted job as the fallback) are the right ones and are placed before the lanes where two lanes build against them. It is rejected on eighteen points: the two day size is asserted and not cut; the lane table is not disjoint in four files and one row has no owner; the daily refresh, the fallback index and the HTML export have no driver or no owner as written; the shader still breaks the resting rule for a box that is not 16:9; the capture path has no storage rule on a store the focus round put on a budget; the gate loses a preview run for a schedule reason and pre forgives two of the product's own failure mechanisms; the `parks` field is written on controls the switch cannot hide.

## Rejections

1. Section 1 (Sizing): the document names two days, measures its P0 at 111 effort points against a cut that "held about 30 friction items" and cuts nothing, handing the cut to the end of day two ("a P0 item not landed by the end of day two moves to the round after whole"); a size that its own paragraph says is above the cut is not a size, and the product round's two days are still being built, so the calibration number does not exist yet either.
2. Section 5.1 and section 6 (B5): the chip word lives in `packages/viewer/src/Selection.tsx` line 355 (`material: 'Picture'`), a B3 file, and the document cites `packages/viewer/src/model.ts` 355 (the audit's error, copied), which holds no chip; B5 needs the word Shader there and B6 needs the chip Logo there (4.4), and the shared file table does not list `Selection.tsx`.
3. Section 6: the row `brand.objects.kit-colours-first` is B1's while its files are B3's (`inspector/table.tsx` 92 to 135 and `inspector/chart.tsx` 430 to 460, audit-objects 19) and the shared table names neither; the row `shaders.insert.words` is B1's while the chip (`Selection.tsx`, B3), the section title (`inspector/shader.tsx`, B5), the alt (`editor-shell.ts` 1750, B5) and the sheet label (`render/src/blocks/material.ts` 30 to 34, B5) are other lanes' files, so the document's own rule "no row is owned by two lanes" does not hold.
4. Section 3.1 item 2 and section 6: `packages/chrome/src/font-picker-model.ts` 260 (`licenceUrlOf`) is B1's file, is changed by B2 for B2's row `fonts.links.licence-v4-1`, and is absent from the shared file table.
5. Section 6: the merge orders contradict each other; the integrator's order is B4, B2, B3, B5, B6, B1, B7, while the shared table says `packages/schema/src/assets.ts` merges B6 then B5 and `dialogs/Tailor.tsx` merges B1 then B6.
6. Section 7.1: the row `shaders.background.add-to-theme` (audit-shaders 11) appears in no lane's row list (B5's list is `shaders.panel.*`, `frame.*`, `perf.*`, `show.*`, `view.play-setting`, `insert.selected-free-rectangle`, `agent.*`, `library.*`; B7's is `shaders.background.place-answers` and `shaders.export.*`), so nobody builds it and nobody drives it.
7. Section 4.2: the daily refresh has no driver as written; a Vercel cron sends `Authorization: Bearer <CRON_SECRET>` and nothing else, the document says the cron calls `POST /api/logo/refresh` "with the agent bearer", `apps/studio/vercel.json` has no `crons` entry, and no file under `apps/studio/src/server` reads a `CRON_SECRET`.
8. Section 4.2: the fallback to thesvg.org's `registry.json` cannot build an index the picker inserts from, because `registry.json` carries variant keys and not file paths (audit-logos 13: `openai/wordmarkLight.svg` answers 404) and the same paragraph forbids composing an address from a key, so the fallback index would have no `default` path for any new mark.
9. Section 5.5: the still is rendered at 1600 by 900 and cover cropped to the box while the live mount renders at the box's own aspect (`block-css.ts` 81 to 83: `.material` at `aspect-ratio: 16 / 9` unless `height` sets `auto`, the img at `object-fit: cover`, the canvas filling the box; `catalog.ts` 41 to 117: the `object` family fits the shape to its canvas), so a shader with `height` set or with the new `fit: 'contain'` (5.1) rests differently from how it plays, and SPEC-5 0.3's resting rule requires every still surface to read what the editor shows at rest.
10. Section 5.5: "the standalone motion script mounts the shader over" the HTML export names round five's `__tsMotion` (SPEC-5 line 816), which has no match on the checkout outside `docs/`, and the web page export that does exist (`file.download.html`, `model.ts` 989, built in `apps/studio/src/server/download.ts` 637 to 697) is owned by no lane in section 6.
11. Section 5.5: a capture runs `asset.add` of a 3200 by 1800 PNG 800 ms after every recipe commit with no dedupe and no orphan rule, on the one public blob store the focus round put on a budget; the store prunes unreferenced snapshots (`blob-store.ts` 895, `snapshots.ts` 28) and never assets, so a seller's slider session leaves one multi megabyte file per release behind.
12. Section 5.5: `recipeKey` on the checkout is sha256 over materialId, uniforms, size, timeMs and backend (`capture.ts` line 3), so the hosted fallback frame of 5.5 can never equal a client key and "a frame whose recipeKey differs from the block's is stale" re captures the same recipe on every fallback or never marks a real change stale, depending on which key the block stores.
13. Section 7.2: the gate is cut to one preview run "taken here to fit the two days"; RETURN.md section 1 rule 1 and FOCUS.md 6.2 define the flakiness check as two runs on one deployment with no code change between them, a preview and production are two deployments with two blob tiers and two cold start profiles, and the document's own sentence names the schedule, not a measurement, as the reason.
14. Section 7.2: the not chased classes pre forgive two of the product's own mechanisms before any run measured them: "a render worker capture past its 30 s bound on `shaders.background.place-answers`" is the defect audit-shaders 1 measured (93 s and a command line in the dialog), and "a WebGL context loss on a `shaders.*` row" is the renderer's own failure on the runner; PRODUCT.md 8.2's classes are a third party's latency or a blob tier timing the return round's runs recorded.
15. Section 4.7: the sanitizer defines no rule for an element outside its allowlist (`filter` and the `fe*` primitives, `pattern`, `symbol`, `text` and `tspan` are absent; the sample carried one filter, one mask and 24 gradients, audit-logos section 1) and removes `@import` and `url(` from `<style>` elements but not from `style` attributes, so the file sharp rasterizes is undefined for a mark with a filter or an inline `url(` and 7.3's fixture list does not name those cases.
16. Sections 4.12, 5.10 and 7.1: `parks` names handles (`handle.table.row`, `handle.table.add.*`, `bar.table`), dialog controls (`dialog.logo.*`) and panel controls (`formatOptions.shader.*`), but the switch hides menu rows and toolbar controls through `advanced` and Format options sections through `advancedTools` (`FormatOptions.tsx` 129 to 130) and nothing else; `Overlay.tsx` and the dialogs read no setting, and B4 adding the files to `CONTROL_SOURCE_PATHS` validates an id (`isKnownControl`) without parking it, so a red row's `parks` on those controls hides nothing.
17. Sections 4.2, 4.6 and 4.10: the sanitized SVGs cached under `system/logos/<slug>/<variant>.svg` sit in the public blob store ("public URLs the browser reads directly", `blob-store.ts` line 4), so Turboslide redistributes every mark a seller touched, CC BY-ND, Proprietary, Trademark and Unknown marks included, at a bare URL without the attribution sentence of 4.6, for a week past thesvg's 24 hour takedown; 4.10 records only the deck's copy as the seller's act and nothing records the product's.
18. Section 7.1: `logos.index.daily-refresh` drives `POST /api/logo/refresh` on production in every gate run, a full rebuild over jsDelivr and thesvg.org (7,417 marks, a source with a soft 100 GB a month limit the audit recorded), and its "a variant that answers 404 upstream is marked unavailable" clause needs an upstream that answers 404 on cue, which production does not have.

## Additions

1. The cut, named now. Move to P1 behind the day two condition: audit-objects 8 and 9 (the row seams, the "+" edges and the heads; 6 points), audit-fonts 9 and 10 (the specimens and Recent; 3 points), audit-logos 5 and 7 (the Symbol, Wordmark, Color and Mono controls, with the appearance rule of 4.3 choosing the variant on its own; the kit's Find a logo; 4 points), audit-shaders 6, 10 and 17 and the hover live mounts of 5.4 (the Frame scrubber, the show playing a shader, View > Play shaders; the gallery ships its still tiles; about 7 points). That leaves about 91 points, still above the cut, so the document must also choose between two ships in one round (the first: objects, fonts and the logo picker's core, about 52 points; the second: the shader library, about 39 points; each with its own parked list and its own two preview runs) or a stated size of three to four pipeline days with Kevin told so in section 9. Either way the sentence "two days" leaves unless the P0 tier fits it.
2. The lane table fixed in one pass: `packages/viewer/src/Selection.tsx` (B3 owns; B5's `material: 'Shader'` word and B6's `logo` chip land by request), `inspector/table.tsx` and `inspector/chart.tsx` (B3 owns; `brand.objects.kit-colours-first` moves to B3, or its plate rows land by request with B1 keeping `ColorPlate.tsx`), `font-picker-model.ts` (B1 owns; B2's `licenceUrlOf` line lands by request), `apps/studio/src/server/download.ts` (B7 owns; the web page carries the frame and no live shader this round), `apps/studio/e2e/core/documents.spec.ts` in B4's spec list (the two paste rows name it), an owner for `shaders.background.add-to-theme` (B5, with `Background.tsx`'s Add to theme row by request to B1), `shaders.insert.words` owned by B5 whole with B1's dialog title measured under `shaders.insert.gallery-thumbnails`, and one merge order that the shared table follows (B6 before B5 on `assets.ts` and B1 before B6 on `Tailor.tsx` change the integrator's order or the table changes).
3. The capture in a shared editor. Name which client captures when two editors hold the deck: the editor that made the recipe commit schedules the capture and a follower never does; a follower that receives a `block.set /asset` from outside draws the frame; two captures of one recipe key resolve to one asset. Name the frame write's revision rule beside `baseRevision` so the system write never conflicts with the seller's next commit.
4. The client capture's pixel path, named from `capture.ts` 222 to 244: `contextAttributes: { preserveDrawingBuffer: true, antialias: false }`, `minPixelRatio: 2`, `devicePixelsSupported` cleared after the first `ResizeObserver` callback so a 1x display reaches 3200 by 1800, `setFrame(anchor)`, then `toBlob` in the same frame; and the canvas sized to the block's box aspect within the 3200 by 1800 cap (the long side 3200) so the still equals the editor at rest, or the material box pinned to 16:9 with `fit` and `height` dropped from the seller's controls.
5. `recipeKey` for staleness: the block stores a key over materialId, preset, the resolved uniforms, the anchor and the palette colours, without size and without backend; the asset keeps `capture.ts`'s key beside its backend and renderer; the staleness test compares the block's key with the asset's recipe key alone. `capture.test.ts` (7.3) gains the case "a client frame and a hosted frame of one recipe are both fresh".
6. The storage rule for frames: the frame asset's id derives from the block's recipe key so a repeated recipe reuses the asset and a slider dragged back to its value writes nothing; a superseded frame of the same block is removed at the next checkpoint or by the store's prune (name the function in `packages/store`), or its growth is recorded as accepted with a measured number per capture (the liquid metal diamond at 3200 by 1800 as PNG, measured on the checkout's capture fixture, never typed); the debounce is 800 ms after the last change and no capture runs while a slider is held.
7. The refresh's driver and its bound: a `crons` entry in `apps/studio/vercel.json` (`0 6 * * *`), the route accepting `CRON_SECRET` beside the agent bearer with a unit test in `logos.test.ts` for both and for neither (401); the first build's bound (7,417 fetches: the concurrency, a time budget under the function's maximum duration, resumable across invocations with the partial file written and `updatedAt` held); the fallback to `registry.json` dropped, or limited to refreshing title, aliases and licence for slugs whose paths the previous file already holds.
8. The refresh row on production: `logos.index.daily-refresh` drives `POST /api/logo/refresh { dryRun: true }` answering the cache's counts and `updatedAt` without a fetch, or moves to the preview with `TURBOSLIDE_LOGO_UPSTREAM` pointing at a fixture that answers 404 for one variant; the 404 marking stays a named unit test in `logos.test.ts` either way, and the production row is recorded not driven with the test named, the pattern of `logos.index.cached-offline`.
9. The sanitizer's full rules in 4.7: the attribute rule (an allowlist, or the removal list extended with `style` values containing `url(` or `expression(`), the disallowed element rule (the element and its subtree dropped and `source.sanitized: { removed: ['filter'] }` recorded on the asset, or the variant marked unavailable when a `filter`, `pattern` or `mask` was dropped because the look changed), `filter` and the `fe*` primitives and `pattern` added to the allowlist if librsvg renders them from a buffer with no base URL, and 7.3's fixture list extended with a `<filter>`, a `<pattern>`, a `<text>` and a `style="fill:url(http://…)"` case.
10. The `parks` mechanism for a control that is not a menu row or a toolbar control: a `PARKED_CONTROLS` set the chrome reads (compiled from `ship-<commit>.json` at build, or the `advancedTools` setting read by the surface that draws the control), named per surface with its file: `Overlay.tsx` for the handles and the bar, `dialogs/Logo.tsx` and `ShaderGallery.tsx` for the dialog controls, `inspector/shader.tsx` for the section's controls; B4's `CONTROL_SOURCE_PATHS` change stays as validation. Without it, `parks` on those rows must be removed and the rows fall under the feature rule.
11. The cache and the takedown: a slug absent from `icons.json` at a refresh leaves the index and its cached SVGs the same day; the cached SVGs live under a path the blob host does not serve publicly (the function's own cache, or a store prefix the assets route fronts with the headers of 4.7), or the public cache is recorded in 4.10 as a redistribution by the product with the attribution sentence written into the store object's metadata and the cache limited to marks under an open licence (CC0, MIT, Apache, BSD, ISC, Unlicense), with the rest fetched per insert and never cached.
12. The gate kept: two runs on the enforce preview and one on production, as RETURN.md section 1 rule 1 and PRODUCT.md 8.2 read; the not chased classes limited to the third party upstreams (thesvg.org and jsDelivr on a `logos.*` row, the mechanism read from the cache's `lastError`) and the return round's measured blob tier timing; the render worker bound and the WebGL context loss removed from the list, because `shaders.background.place-answers` exists to measure the first and the renderer string in the run's JSON records the second as a failure with its mechanism.
13. The mono tint's rule for a file whose paths carry explicit fills (the root `fill` does not reach a `fill="#000"` on a path): every fill attribute and `fill:` declaration in a mono variant is rewritten to the kit's text colour, or the variant is not offered as Mono; `logo-model.test.ts` gains a fixture with explicit fills.
14. Question 9 for Kevin in section 9: the two ships or the longer round, with the default the document chooses and the cut list of addition 1 beside it.

## Notes

- What checks out on the checkout: `CHROME_CONTROL` at `Editor.tsx` 746 to 760 and the panel root at `FormatOptions.tsx` 630 to 631 (`control="panel.formatOptions"`, class `ts-fo`, not `ts-inspector`), so rank 1 of 2.2 names the right mechanism; `THREE_X_KINDS` at `extract.ts` 57 is `['icon', 'mark']`; `typographyDeclarations` is in `packages/schema/src/typography.ts` 184, a B2 file; `licenceUrl` in `summary.ts` 86 and `licenceUrlOf` in `font-picker-model.ts` 260; `insert.material` at `model.ts` 1544 and `MenuSetting` at 250 to 283; `GT_PALETTE` at `presets.ts` 13 to 24; `SERVER_SIDE_WINDOW_ACTIONS` and `DRAFT_CREATING_ACTIONS` in `agent-actions.ts` (composed from the GS2, GS3 and P1 lists at 46 to 149, the draft list at 181); the CSP `img-src 'self' data: blob: <store>` at `headers.ts` 208, so `/api/logo/mark/...` draws in the dialog; the agent bearer is a hard 401 in `auth.ts` separate from `authorize()`'s shadow mode, so the 401 clause of `logos.index.daily-refresh` holds on production.
- The matrix file supports every field the rows use (`parks` on 47 rows, `measure` on 2, `manual` on 2, `setup` on 5, 698 rows today), `CORE_FEATURES` and `CORE_SPEC_DRIVERS` are where the document says, and `ship-b828afa.json` has the `parkedFeatures` and `parkedRows` shape the document reuses.
- Paper's `ShaderMount` takes WebGL context attributes through `mountMaterial`'s `contextAttributes` (`mount.ts` 31 to 32) and the capture page already asks for `preserveDrawingBuffer`, so the client capture of 5.5 is buildable; the document should say so in the terms of addition 4 rather than leave the lane to rediscover `devicePixelsSupported`.
- The fonts' licence recording is right: eight OFL 1.1 families with no Reserved Font Name at the pinned commit (`candidates.md`), the licence file beside each folder, the notices paragraph, the woff2 conversion as a Modified Version that keeps no reserved name, the SVG specimen as a document and not font software, the v4.1 tag replacing the v4.001 tag that does not exist. The Inter italic finding (a 4.0 file under a 4.1 label, `THIRD_PARTY_NOTICES.md` 12 to 16) is exact.
- The logos' licence recording on the asset (`source.license` verbatim, `source.url`, `source.guidelines`, the dialog's foot and row, the cloud collections behind a switch, the mono tint off for CC BY-ND) is right for the deck's copy; rejection 17 is about the product's cache, not the asset.
- The shader section's licences are right: Paper at `THIRD_PARTY_NOTICES.md` 167 (Apache 2.0 with NOTICE), Glyphfield at 447 (MIT, Kevin Liu), Prototemplate's studio field as Kevin's own code; the credit line per asset carries the source.
- The resting rule is obeyed everywhere but the aspect case of rejection 9: the renderer draws the frame or a plate with no text, the editor and the show are layers over the rendered node, the exporters read the frame, and the missing frame is a report row and never a label. The 800 ms schedule and the system write outside the undo stack are the right shape once additions 3, 5 and 6 pin the sharing, the key and the storage.
- Section 7.2's rule for a red objects row ("reverts that item's one commit in the fix round") reads as PRODUCT.md 8.2 does once the fix round is understood to sit between the preview runs and the production run; the sentence should say so in one clause so a reader does not take it as a revert after the ship.
- The audit-shaders citation `viewer/model.ts 355` is the audit's error (the file is `Selection.tsx`); the synthesizer copied it into 5.1 and the lane table, which is how rejection 2 arose. Every other line number spot checked matched the checkout within the drift the audits declared.
- Section 9's questions are the right ones and the defaults are the ones the rows are written against; question 8 (the second click caret against Google's one click) is the one Kevin is most likely to overturn, and its rows (`tables.cell.second-click-caret`, `tables.range.drag-from-selected`) are written so that a reversal changes two interaction texts and no file list.
