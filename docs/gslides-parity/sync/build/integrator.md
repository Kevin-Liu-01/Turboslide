# The sync and costs round, the integrator

The integrator's note of the sync and costs round (2026-09-21, times in UTC unless marked PDT), in the shape of the product round's build notes. The binding specification is `docs/SYNC.md` (section 5 the lanes and the shared file table with its merge order, section 6 the acceptance); `docs/HOSTING-MOVE.md` section 9 is the pipeline's part of the move; `docs/PRODUCT.md`, `docs/RETURN.md`, `docs/FOCUS.md` and `AMENDMENTS.md` A1 stand under them. The six lane notes beside this one (`b1.md` to `b6.md`) are read as claims; every number below was read first hand from a command this integrator ran, and a row nobody drove is not driven, never passed. The round was built on the shared checkout at `b22ffa0` plus the continuation agent's commit `c4ad8b1` (the product round's production table, which landed at 21:36Z while this integrator worked; its files were never edited here before it). No git write command ran; no `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran outside `scripts/check.mjs`; the blob tier locally was `blob-fake.ts` alone; nothing on Vercel or GitHub was created, changed or deleted apart from the one preview deployment this pipeline names (`vercel deploy --yes --archive=tgz` from the repository root, never `--prod`, never a project setting, never an env or store command); no token, cookie or secret is printed here or in any file this integrator wrote. The dev server was port 4418 from `apps/studio` (`TURBOSLIDE_STORE=tmp` under a private `TMPDIR` in the scratchpad, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, `TURBOSLIDE_ASSIST=fixture`, two 48 character secrets from `openssl rand -hex 24`, `vite.no-watch.config.ts`; started 21:26Z, stopped at the return). The branch `round-five/integration` was never checked out, merged or read.

## Sync round

### 0. The merge as it happened

SYNC.md 5.1 gives the merge order B4, B3, B1, B2, B5, then B6, and the shared file table names who owns which function where two lanes touched one file. On this shared checkout every lane's files were already on the tree when the integrator started, so the order was read as a check rather than applied: the shared files were read against the table (`protocol.ts` carries B1's `covers` and B2's two heartbeat constants and nothing else of B2's; `mutations.ts` carries B1's field target and `Write.origin`, read by B2's `textBurstMutation`; `store.ts` and `versions.ts` carry B3's tolerant parser, read structurally by B1's `blob.ts`; `write.ts` carries B3's `since` answer, read by B2's `controller.tsx`; `controller.tsx` is B2's whole and `deck.$deckId.tsx` B5's whole; `actions.ts` of the schema got its two entries from the integrator below and `pnpm generate:contracts` ran once), and nothing of any lane was reverted. `node_modules/.bin/tsc -b` read exit 0 on the tree as the lanes left it (21:22Z) before any integrator edit.

### 1. Requests answered

Every request under a lane note's "Sync round" heading that named the integrator, and the two seams no lane owned. The owner's request id is kept.

| Request                 | What was asked                                                                                                                                                                                                                                                   | What landed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B3 R1 (and B4's third)  | `packages/schema/src/actions.ts`: `syncStatusSchema.storeCalls` and `deck.info` `output.counts.records`, `.holes`, both optional; `pnpm generate:contracts` once                                                                                                 | Landed word for word from b3.md R1 (the `storeCalls` strict object after `connected`; `records` and `holes` after `snapshots`). `pnpm generate:contracts` wrote 7 of 14 files (`mcp-tools.json`, `describe.json`, `openapi.json`, `manifest.json`, `llms.txt`, `llms-full.txt`, `skills/turboslide-studio/references/browser-api.md`); `rules.json`, `cli.json`, `docs/grammar.md` and the lint fixture index did not move, so B1's R8 (b) holds: nothing of the write schema changed.                                                                                                                                                                                                                                                                                                            |
| B3 R2                   | `apps/studio/src/server/actions.ts` `deckDispatcher`: `registerStoreStatusActions(dispatcher, { deckId, store: deckStore, tier: realtimeTier() })` after the record actions                                                                                      | Landed after `registerRecordActionsFor(...)`, which is itself after `registerReadActions(...)`, so the one call replaces both the CLI's `sync.status` placeholder and the reader's `deck.info`; `registerStoreStatusActions` imported from `./agent-actions`, `realtimeTier` added to the existing `./room` import. `/api/actions/sync.status` on 4418 answers the store's view (`tier: 'memory'`, `storeCalls` zeros with the instance id) and `deck.info` carries `counts.records` and `counts.holes`; on the preview the cost probe read `storeCalls` from the blob tier (section 4).                                                                                                                                                                                                          |
| B3 R4, B1 R9            | `apps/studio/src/routes/api/decks.$deckId.stream.ts`: `flushCardThumb(deckId)` in the stream's one close path after the slot's release                                                                                                                           | Landed inside `createStreamCloser`'s callback after `slot.release()` and the presence leave, as a dynamic import of `../../server/card-thumb` (the spelling B3 gave, `write.ts`'s precedent), so the renderer stays out of the route module's graph; the promise is caught so a close never throws.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| B1 R10                  | `apps/studio/src/server/export-jobs.ts`: the module level `import { provenGet, putWithCopy } from '@turboslide/store/access-store'` reached the browser through `download.ts` and killed `/new` and `/edit/<id>` with "Module node:crypto has been externalized" | The three functions that touch a `BlobClient` (`writeExportJob`, `readExportJob`, `pruneExportJobs`) moved whole to a new server only module `apps/studio/src/server/export-jobs-store.ts` (its header says why); `export-jobs.ts` keeps the pure half and imports nothing from the store; `export-batch.ts` and `export-jobs.test.ts` import the three from the new module. The proof rule of SYNC.md 3.5 is unchanged (the same `putWithCopy` and `provenGet` calls). `scripts/check.mjs`'s `OVERWRITE_ALLOW` names the new file instead of the old one (the `overwrite: true` call site moved with the function; step 6's greps read clean). Verified by `scripts/probes/new-write-probe.mjs` against 4418 (all steps ok, 21:28Z) and by both gates' `decks.new.draft` rows: the editor loads. |
| B2 R2 (optional)        | `apps/studio/src/editor/convert-first.ts` `slideToConvertFor`: read past a text run on a slide field                                                                                                                                                             | `isSlideFieldTextRun(slide, mutation)` exported (a `text.splice`, `text.mark` or `text.replace` whose `blockId` is a field of the slide's kind and whose `path` is the field's pointer, through the schema's `slideFieldOf` and `slideFieldPath`) and read past inside `slideToConvertFor`; `convert-first.test.ts` gains the case (a heading splice at `/heading` and a `big` mark at `/big` name nothing; a heading mark at `/text` still converts). The controller's own filter (B2's, `commit`) stays and is now redundant, never wrong.                                                                                                                                                                                                                                                      |
| B2 R3                   | `typing-key.ts` and `auto-title.ts`: the comments describe the old `slide.set` burst form                                                                                                                                                                        | Both headers rewritten to the text run form: a field's burst is a `text.splice` keyed `<slideId>/<field><path>`; the `slide.set` branch stays for the whole value writes (the Escape commit, a markup change, an agent) and its comment says so; `auto-title.ts` records that the reducer's `followTitle` derives the title from a heading burst and this function names the rename for the whole value writes alone. No code path changed.                                                                                                                                                                                                                                                                                                                                                       |
| B5 R1                   | `apps/cli/e2e/mcp-http.mjs` line 386: open `/deck/${DECK}?agent=1`                                                                                                                                                                                               | Landed with a comment saying the flag is the rule (SYNC.md 3.10, question 3's default), not a test convenience.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| B5 R2 (B4 forwarded it) | `apps/studio/e2e/window-api.spec.ts` 365 to 400 and `scripts/perf-budget.mjs` 206 and 286: the idle ceiling of four server function responses in 60 s superseded by the unheld 20 s poll                                                                         | The spec's ceiling reads 6 with the comment naming the cadence (`floor(60 / 20) + 1 = 4` polls plus the editor's two own calls) and SYNC.md 6.1 `cost.editor-idle.calls` as the row of record; the gaps assertion stands. `perf-budget.mjs` `idle.serverFnPerMinute` reads 6 in both budgets with the same comment.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| B5 R3                   | The sentences that describe when a page attaches: `manifest.ts` line 66, `describe.ts` line 112, SPEC-4 3.8, `sessions-polling.md` section 5                                                                                                                     | `viewTools` names the editor while visible, a `/deck`, `/present` or `/embed` page opened with `?agent=1` while visible, and the 10 s detach with its 404; the viewer owner's `when` says the window API is on every viewer page and the session needs the flag (both regenerated into `manifest.json`, `describe.json`, `llms*.txt` and `browser-api.md` by `pnpm generate:contracts`). `docs/gslides-parity/SPEC-4.md` 3.8 gains a paragraph; `docs/sessions-polling.md` gains section 5 (the five rules as built, the measured numbers of b5.md section 5, the round after's item R7, why the push channel did not ship).                                                                                                                                                                      |
| B5 R5, R7               | Facts for the ship note and the round after                                                                                                                                                                                                                      | Carried into section 7 below.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| B4, B5 R9, B6 R5        | `.prettierignore`: the lanes' run JSON under `docs/gslides-parity/sync/build/*/**/*.json` and the hosting checks' `docs/gslides-parity/sync/hosting/**/*.json`                                                                                                   | Both globs landed, with the same rule for the run JSON of the discovery audits committed at `0b4f459` (`sync/audit-*/**/*.json`, `features/audit-*/**/*.json`) and of the product round's lanes (`product/audit-*/**/*.json`, `product/build/*/**/*.json`); the notes and scripts beside them stay under the formatter (section 2, the format gate).                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| B4 (README)             | Re-render `README.md` after the continuation agent's commit                                                                                                                                                                                                      | Done at 21:38Z, after `c4ad8b1` landed: `node docs/readme/what-works.mjs && node_modules/.bin/prettier --write README.md` (3 insertions, 2 deletions between the markers: the sync and cost features join the block). `node docs/readme/what-works.mjs --check` reads "README.md is current"; `docs/readme/what-works.test.mjs` 8 of 8. Nothing else of README.md changed; the release note and the address change stay the ship step's (SYNC.md 5.1 shared file table).                                                                                                                                                                                                                                                                                                                          |
| B6 R1                   | The drain script of HOSTING-MOVE.md section 5 step 5                                                                                                                                                                                                             | `scripts/probes/presence-drain.mjs` with `presence-drain.test.mjs` (7 cases over `blob-fake.ts`): one folded listing of `decks/`, a `head` of each deck's manifest (a folder without one is a removed deck's leftovers and is skipped), the roster read through `provenGet` and `parsePresenceRecord`, a row live when `expiresAt` is ahead and no tombstone at or after it, one line per read, exit 0 at zero; `--wait` reads again every minute up to `--until <minutes>`, `--json <out>` writes the readings and the deck ids with a live row. `BLOB_READ_WRITE_TOKEN` from the environment (sourced in a subshell), never printed. It has never run against a real store, as b6.md's scripts have not.                                                                                        |
| B6 R2                   | `--etags <file>` on `docs/gslides-parity/sync/audit-hosting/scripts/blob-sizing.mjs`                                                                                                                                                                             | Landed: the pathname and etag of every `decks/<id>/deck.json` the listing returned, sorted by pathname, to a separate file; the summary and stdout keep counts and bytes alone (the header says the etags file names deck ids). Syntax checked; not run (a real store token).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| B6 R4 (optional)        | Fact 4 (whether the WAF redirect keeps the request's path) into HOSTING-MOVE.md 2.2 and section 5 steps 5 and 7                                                                                                                                                  | Landed in the three places, with the branch: if the scratch project's rule drops the path, the grace hour alternative is not taken and steps 7 and 8 run back to back.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| B6 R7 (optional)        | The literal `turboslide deck info --to <url> <id>` in HOSTING-MOVE.md section 7's acceptance and section 10 item 3 names a command the CLI does not have                                                                                                         | Both read `turboslide share get <id> --to https://turboslide.vercel.app` now, and item 3 names `scripts/hosting-check.mjs` as the command that reads the unauthenticated rows and prints the authenticated call.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| B6 R3                   | The Playwright rehearsal of the flip (two browsers, `page.route` answering the ops POST and the stream with a 307 to a second origin)                                                                                                                            | Not built this round (section 8); the ship note states the loss from HOSTING-MOVE.md 1.7 as b6.md R3 asks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| B6 R6                   | The destination literal in the two redirect files, the README and `hosting-check.mjs` replaced by the team's alias                                                                                                                                               | The cutover commit's, once the alias exists (section 7).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| B1 R8, B3 R8, B2 R4     | Records, no action                                                                                                                                                                                                                                               | Read and confirmed: `generate:contracts` moved nothing of the write schema; `Write.origin`, `StoreWrite` and `writeOriginSchema` agree in shape (the one definition of B1's R4 is left for the round after, since both compile and the tests pass); the client applies a resend's synthesized entries once (B2's `room-client.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| B4 to B3, B5 R8 to B4   | `sync.status`'s own reads in `storeCalls`; the cost probe's `freshDeck` on a draft                                                                                                                                                                               | Left to the run: B3's R7 (b) gives the cost (one `deck.json` head per sample past the 750 ms window) and the ceilings carry it; the gate's cost probe rows in section 4 are the reading. On the merged tree the probe's title typing lands as a `text.splice` on the heading (B2's client, B1's reducer), so the draft moves to revision 1 and the stream opens (the idle row's `connected` check of b5.md R8 read true on both origins, section 4).                                                                                                                                                                                                                                                                                                                                              |

### 2. The gates on the tree

Run from the repository root between 21:22Z and 23:45Z, in this order, on the tree as merged plus the integrator's edits of section 1. The machine carried the continuation agent's production gate until about 21:20Z and then this integrator's own three runs together (the local gate, the preview gate and the check chain's steps; the one minute load read 6 to 15 through them, recorded where a result could move with it).

| Gate                                                                       | Command                                                                                                                                                                                    | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The tree as the lanes left it                                              | `node_modules/.bin/tsc -b`                                                                                                                                                                 | exit 0 (21:22Z), before any integrator edit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| The contracts                                                              | `pnpm generate:contracts`                                                                                                                                                                  | wrote 7 of 14 files (section 1, B3 R1); the write schema unchanged. Check step 3 (`git diff --exit-code` over the generated files) fails as written on the uncommitted tree, as every merge of the last four rounds recorded, and passes by regeneration: a second `pnpm generate:contracts` changes nothing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Types                                                                      | `node_modules/.bin/tsc -b`                                                                                                                                                                 | exit 0 (21:31Z, after every edit of section 1); check step 4 `pnpm exec tsc -b` ok in 3.9 s                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Unit tests per package (the named tests of SYNC.md 6.4 among them)         | `node_modules/.bin/vitest run` from each package; `--project scripts` from the root                                                                                                        | `packages/schema` 25 files, 580 passed (`transform.test.ts`, `reduce.test.ts`, `mutations.test.ts`); `packages/realtime` 10 files, 134 passed (`room-client.test.ts` 45, `blob.test.ts`, `protocol.test.ts`); `packages/store` 20 files, 247 passed (`hosted.test.ts`, `pulse.test.ts`, `versions.test.ts`, `blob-store.test.ts`); `packages/viewer` 42 files, 405 passed; `packages/agent` 12 files, 257 passed; `apps/studio` 69 files, 485 passed (`blob-admission.test.ts` 27, `export-jobs.test.ts` against the split module, `useStudioSession.test.ts` 13, `agent-actions.test.ts`, `card-thumb.test.ts`, `write.test.ts`, `convert-first.test.ts` with the new case); `apps/cli` 23 files, 142 passed, 1 skipped; the `scripts` project 10 files, 91 passed after the README re-render (90 of 91 before it, the one red the README block; `sync-cost-probe.test.mjs` 14, `core-matrix.test.mjs`, `core-gate.test.mjs`, `hosting-check.test.mjs` 13, `blob-copy.test.mjs` 12, `presence-drain.test.mjs` 7). Check step 5 `pnpm test`: 374 files, 3998 tests, 1 failed on the README block at 21:37Z; ok in 63.3 s at 21:43Z after the re-render                                                                                                                                                                                                                                                                                                                       |
| The source greps                                                           | `node scripts/check.mjs --greps`                                                                                                                                                           | one red at 21:27Z (`export-jobs-store.ts:43: overwrite: true outside the allowlist`, the call site that moved with the function); clean after the allowlist entry moved with it: innerHTML 27 call sites in the allowlist, overwrite: true 23 in the allowlist                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| The README block                                                           | `node docs/readme/what-works.mjs --check`                                                                                                                                                  | red until 21:38Z ("README.md is not what docs/readme/what-works.mjs renders"; README.md was the continuation agent's uncommitted file until `c4ad8b1`); "README.md is current" after the re-render                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| The format gate                                                            | `node_modules/.bin/prettier --check .`; check step 19                                                                                                                                      | every file of the round formatted (the lanes' and the integrator's); the whole tree read 158 reds at the start, all committed evidence and notes of the discovery and product rounds (`docs/gslides-parity/sync/audit-*`, `features/audit-*`, `product/audit-*`, `product/build`), which the product round's ship.md recorded as the standing red of step 19. Resolved here: the run JSON under `.prettierignore` (section 1), the notes and scripts by `prettier --write` (72 files, whitespace alone). Step 19 red at 21:38Z (48 files, before the README re-render and the write), ok in 48.1 s at 21:41Z                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| The check chain, the steps that need no server or browser                  | `node scripts/check.mjs --only <n>` for 1, 2, 3, 4, 5, 6, 9, 15, 16, 19, 22, 23, 24, 28, 29, 30 (21:36Z to 21:41Z)                                                                         | 1 ok (install, already up to date), 2 ok (routes), 3 FAIL by design (the uncommitted generated files; passes by regeneration), 4 ok, 5 red then ok (above), 6 ok in 5.9 s (the build, the client bundle check, the greps), 9 ok (85 slides, 0 errors, 46 warnings), 15 ok (187 findings, 0 at severity 3), 16 ok (the brand deck build, 45 twins pass through), 19 red then ok (above), 22 ok in 38.2 s (the fixture exports, both modes, the flatten report perfect), 23 ok in 42.3 s (fonts build check), 24 ok in 72.4 s (the canvas fidelity gate), 28 ok (0 advisories at high), 29 ok in 5.7 s (the brand, home assets, definitions and the wasm record rebuilt from the crate: 4 outputs match; the Linux addon pending as before), 30 ok in 12.2 s (the Vercel build and its 40 of 40 output assertions)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| The check chain, the steps that need Chrome or the runner's server on 4321 | `node scripts/check.mjs --only <n>` for 7, 8, 10 to 14, 17, 18, 31, 26, 20, 21, after the local gate released the machine                                                                  | 7 ok (the import: 85 slides, 8 sections, 0 escapes), 8 ok, 10 ok in 14.1 s (170 renders), 11 ok, 12 ok in 50.5 s (the compare to the shoot), 13 ok, 14 ok, 17 ok in 15.2 s (the viewer spec), 18 ok in 93.5 s (the chrome lint over the six routes), 31 red (`perf-budget.mjs`: 113 of 151 budgets met; the misses are the size budgets standing since round four, the largest client chunk 1047 KB against 586 KB and the routes' decoded JS 2307 KB against 1953 KB, plus timing budgets read under this run's load; the product round's ship recorded 127 of 151 with the same size misses; the client bundle check of step 6 passes its own ceilings), 26 red (the older two browser suites: 43 passed, 4 failed, the same four standing files the product round's ship names: `home-page`, `presence`, `share`, `versions-by-author`), 20 red (the parity audit: 38 rows over the round's chrome against the product round's 37; the one new row is `retiredKeys key.retired.?: changed dialogs: 0 -> 1` on both the scratch deck and gt-brand, the retired `?` key opening a dialog, recorded for the product round's chrome owner), 21 red (the older studio suites: 41 passed, 17 failed, 14 did not run; the product round's ship read 19 of 72 red there). Steps 25 (Docker) and 27 (nothing on 4344) skipped by the runner; step 32 not run as a chain step (section 6). The chain's server steps ran on 4321 with the file store and left nothing under `decks/` |
| The new write probe on the checkout                                        | `node scripts/probes/new-write-probe.mjs --base http://localhost:4418` under `.turboslide/e2e.lock` (21:28:05Z to 21:28:48Z)                                                               | all steps ok: the draft on `/new`, the double click session, 6 edits saved and 12 sequential edits each acknowledged at exactly the next revision, the reload at the revision the tab had, the second tab as one other participant, Move to trash and Delete forever. The first proof that B1's R10 (the editor's client) loads on this tree                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| The core gate on the checkout                                              | `node scripts/probes/core-gate.mjs --base http://localhost:4418 --out <scratch>/gate-4418 --lock <scratch>/e2e-4418.lock --parked docs/gslides-parity/focus/ship-b22ffa0.json` (21:32:15Z) | section 4                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| The hosted smoke on the preview                                            | `node scripts/hosted-smoke.mjs <preview> --token-env TURBOSLIDE_TOKEN` with `VERCEL_OIDC_TOKEN` in the environment (21:36:55Z)                                                             | 40 of 40 passed (`/new` 200 in 3.8 s cold, `/edit/gt-brand` 200, `/api/agent` 401 without the bearer, the enforce rows 403, `deck.info snapshots` 1 snapshot at revision 26, the effects backend typescript on glibc 2.34); exit 0                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| The new write probe on the preview                                         | `node scripts/probes/new-write-probe.mjs --base <preview>` (21:37:13Z to 21:37:53Z)                                                                                                        | all steps ok on the blob tier; the deck `untitled-20260921-968b` trashed and deleted forever by the probe. One note the probe printed: the per instance roster on the blob tier makes the cross tab count depend on the instance (`BLOB_TIER_NOTICE`: first tab others 0, second tab others 1), the known reading of that row there                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| The store status actions on both tiers                                     | `POST /api/actions/sync.status?deck=gt-brand` and `deck.info` on 4418 (no bearer, localhost) and on the preview (the bearer and the OIDC header)                                           | 4418: `tier memory`, `storeCalls { head 0, get 0, put 0, list 0, del 0, windowMs 60000, instance d2bb4884 }`, `counts.records 7`, `counts.holes 0`; the preview: `tier blob`, revision 26, `storeCalls { head 1, ... instance 5bb85328 }` (the one head the sample itself cost, b3.md R7 b), `counts.snapshots 1`, `counts.records 8`, `counts.holes 0`. B3's R1 and R2 answer on HTTP                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| The core gate on the preview                                               | `node scripts/probes/core-gate.mjs --base <preview> --out <scratch>/preview/gate --parked docs/gslides-parity/focus/ship-b22ffa0.json` with the two tokens in the environment (21:37:53Z)  | section 4                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| The sync ordering probe on the preview                                     | `node scripts/probes/sync-ordering-probe.mjs --base <preview> --out <scratch>/preview/ordering --tier blob`, after the gate                                                                | not driven on the enforce preview: the probe's setup joins C (a stranger) on the plain `/edit/<id>` and waits for the studio API, which a stranger never reaches under enforce (the same mechanism as finding 9); it ended on that 90 s timeout at 96 s with the phases `create` and `access` recorded, 0 anomalies and its deck `untitled-20260921-x4v4` trashed and deleted forever (`gone: true`). The probe was written for production's shadow mode (the audit's runs 2 to 4 ran there) and had never run against an enforce origin (b4.md section 3); B4's driver needs the link address for C, as the spec's `joinC` does. Its memory tier form was not run either (the local gate's spec rows cover the same phases there)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

### 3. The preview

The deployment of record is `https://turboslide-bkwnt59my-kl01s-projects.vercel.app`, deployed from the repository root at 21:32:22Z to 21:35:46Z with the command VERIFICATION.md's product round tables record, plus the assist fixture: `vercel deploy --yes --archive=tgz -e TURBOSLIDE_AUTHORIZE=enforce -e TURBOSLIDE_ASSIST=fixture -e TURBOSLIDE_MAIL=off -e TURBOSLIDE_REALTIME=blob -e TURBOSLIDE_PUBLIC_STORE_HOST=ggmycvj7j6224ay5.public.blob.vercel-storage.com -e TURBOSLIDE_SESSION_SECRET=<openssl rand -hex 32> -e TURBOSLIDE_DOWNLOAD_SECRET=<openssl rand -hex 32>`; never `--prod`, no project setting, the two secrets generated inside the command and never printed, the deploy's output filtered so no `-e` value reached the log. The public store host was read from production's CSP (`img-src` and `connect-src` name it). `vercel inspect` (a read): target preview, status Ready, created 21:34:16Z; 329.1 MB uploaded, the build cache restored from the previous deployment, `vercel build` on 4 cores. It sits behind Vercel Authentication (`/home` 200 with `x-vercel-trusted-oidc-idp-token` from `.turboslide/vercel-dev.env`, whose token expires 2026-09-22T02:47Z and was not refreshed) and serves enforce mode on the blob tier of the one store production uses. The tree it carries: no file under `apps/` or `packages/` was written after the upload began (`find -newermt`), so the preview is the tree of section 1 byte for byte; the files written after it are this note, the two hosting scripts of B6 R1 and R2, their test, and the continuation agent's commit's files.

### 4. The core gate, two runs

#### 4.1 The checkout, port 4418 (the memory tier, the tmp store)

`node scripts/probes/core-gate.mjs --base http://localhost:4418 --out <scratch>/gate-4418 --lock <scratch>/e2e-4418.lock --parked docs/gslides-parity/focus/ship-b22ffa0.json`, started 21:32:15Z, 4864 s, the one minute load 6 to 15 (the preview gate and the check chain's steps ran beside it). 714 rows: 698 passed, 12 failed, 4 not driven (1 manual), 0 no step; verdict failed under the committed parked list; retries zero; cost rows over their ceiling 0; exit 1. The walk: 518 rows, 558 steps, 2349 s; the specs: 190 tests, 179 passed, 9 failed, 2 skipped by their own rule, 1501 s; the cost probe: 6 rows at 3 minutes, exit 0.

The rows red, by class:

- The committed parked list (never blockers): `versions.show-changes-marks` (no marks with Show changes on, the product round's reading), `view.live-pointers.second-browser` (the pointer not drawn within 10 s), `inbox.bell-panel-toggle` (the panel stays open after the second click) and `templates.gallery.page` (the gallery's list not found in 6 s); `templates.deck.read-only` not driven behind its parked feature.
- Inherited memory tier readings of the product round, red in its own local runs the same way (`product-p1-gate-local.json`, `product-p2-gate-local.json` on 4419) and green on production and on every preview: `assist.tailor.agent-deck-tailor` (`deck.tailor` over HTTP answers `replacements: 0` on the tmp store while the revision moves and the snackbar names the change; the fixture assist's tailor finds nothing to replace on this store) and `assist.quota.429` not driven (no Upstash on a checkout). Not this round's.
- `text.clipboard.paste-without-formatting` manual and `cost.editor-hidden.calls` not driven by its own rule (below).
- The seven `sync.*` rows the run of record read red, every one of them a driver seam of the new spec, which no lane had run before this merge (b4.md: the lock was another lane's for the whole window): see 4.3. After the spec fixes the rerun of `core/sync.spec.ts` on 4418 (21:55:56Z to 22:05:39Z, under `.turboslide/e2e.lock`) read 7 passed, 1 failed, 2 skipped by their own rule.

The 16 rows of SYNC.md 6.1 on the memory tier, from the rerun and the cost probe of the run of record:

| Row                                       | 4418                                                                                                                                                                                                                                                                                                                    |
| ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `sync.serial.order-and-latency`           | passed in the rerun: latency mean 192 ms, max 250 ms over 30 words, 30 POSTs each answered 200 at the next revision (the run of record read 2.9 s per word: the spec registered each token for B's sampler only after the checkpoint's revision moved, 4.3)                                                             |
| `sync.title.concurrent-both-kept`         | passed (both runs); the title travelled as `text.splice` (the field as a text run, 3.4)                                                                                                                                                                                                                                 |
| `sync.title.offline-both-kept`            | passed (both runs); converged 3.9 s after the reconnect, A and B "Sync title offline ob1 ob2 ob3 oa1", `text.splice` on the wire                                                                                                                                                                                        |
| `sync.block.concurrent-same-offset-order` | passed in the rerun: 7 ops POSTs, no repair splice, final "pa3pb3pb2pa2pa1pb1end" in both browsers over three rounds                                                                                                                                                                                                    |
| `sync.structural.concurrent`              | failed in both runs: part one passes (two drags, one position in both browsers within 3 s); part two reads the slide gone in both browsers (5.4 s including B's menu) and no reject card on A: A's drag released after B's delete posts nothing, so no loser exists to read the sentence (4.3, a finding for B1 and B4) |
| `sync.block.offline-replay-converges`     | passed in the rerun: converged 3.9 s after the reconnect, A's splice offset 18 first and 30 on the admitted POST (the transform past B's three words), A "Start of the block ob1 ob2 ob3 oa1"                                                                                                                           |
| `sync.viewer.live-updates`                | not driven on this origin by the spec's rule after the fix: C reads owner under shadow authorization (a dev server, production today), so no viewer floor exists; the preview is where C is a viewer                                                                                                                    |
| `sync.reload.same-document`               | passed (both runs): reloads 1010, 965, 1000 ms, revisions 5, 5, 5 against the live 5                                                                                                                                                                                                                                    |
| `sync.undo.after-remote`                  | passed in the rerun (three of three)                                                                                                                                                                                                                                                                                    |
| `sync.resend.idempotent`                  | not driven by its own rule: the dropped answer's POST landed once (status 200, one POST carrying the op ids, one record after, the word once in both browsers) and the record carries no `origin` on deployment N (`RECORD_ORIGIN_WRITES` false, B3), the known mechanism of 6.2                                        |
| `sync.pull.no-listing`                    | passed: 10 commits from B landed in A, 12 ops POSTs answered 200; the store half reads zero on the tmp store as 6.3 says                                                                                                                                                                                                |
| `cost.editor-idle.calls`                  | passed: 10 function requests a minute (ceiling 12; 30 in 3 minutes, 9 of them the poll), the tab connected; store half zero                                                                                                                                                                                             |
| `cost.editor-hidden.calls`                | not driven: `document.visibilityState` read visible after the second page was brought to the front (headless Chromium reports no hidden page); the visible tab's counts recorded, 10 a minute                                                                                                                           |
| `cost.editor-editing.calls`               | passed: 69 function requests a minute (ceiling 75; 207 in 3 minutes, 9 polls); store half zero                                                                                                                                                                                                                          |
| `cost.two-tabs-idle.calls`                | passed: both tabs connected and in each other's roster, 19.7 function requests a minute over the two; store half zero                                                                                                                                                                                                   |
| `cost.show.calls`                         | passed: 0 function requests in the 3 minute window after the load (ceiling 0), no poll                                                                                                                                                                                                                                  |

The store status actions answered through the run: one instance id (`d2bb4884`) on every sample, every count zero on the tmp store.

#### 4.2 The enforce preview (the blob tier, the one store production uses)

`node scripts/probes/core-gate.mjs --base https://turboslide-bkwnt59my-kl01s-projects.vercel.app --out <scratch>/preview/gate --parked docs/gslides-parity/focus/ship-b22ffa0.json` with `VERCEL_OIDC_TOKEN` and the bearer in the environment, started 21:37:53Z, 6906 s, beside the local gate and the check chain (the one minute load 6 to 15). 714 rows: 688 passed, 21 failed, 5 not driven (1 manual), 0 no step; verdict failed under the committed parked list; retries zero; cost rows over their ceiling 3 (`sync.pull.no-listing`, `cost.editor-idle.calls`, `cost.show.calls`); exit 1. The walk: 518 rows, 558 steps, 2660 s; the specs: 190 tests, 173 passed, 14 failed, 3 skipped by their own rule, 3167 s; the cost probe: 6 rows at 3 minutes, seven instance ids answered `sync.status` across the rows, exit 1. Every scratch deck of the walk, the specs and the probe was torn down by its file (the probe's rows read `gone: true`; the store check of section 6 confirms nothing of today remains).

The rows red, by class:

- The committed parked list: `versions.show-changes-marks`, `view.live-pointers.second-browser`, the three inbox rows (`inbox.bell-panel-toggle`, `inbox.settings-persist`, `inbox.notification-arrives`), `templates.gallery.page`, `templates.default.use-for-new`; `templates.deck.read-only` not driven behind its feature.
- The product round's readings on a preview, as its ship recorded them: `assist.rewrite.card-accept-undo` and `assist.notes.draft` ("the body changed", "the slide carries the notes": the fixture assist's rows the product round's previews read red too), `assist.quota.429` not driven (the instance facts name no limiter), `brand.background.enter-keeps-open` (the verifier's pass 2 read it red on another read; here Done's bounding box timed out at 30 s).
- Load or blob tier timing, red here and green on 4418 and on production's run of record (`c4ad8b1`): `decks.list.open-title` and `decks.card.menu-open-escape` (60 s test timeouts on `/decks` while two gates and the check chain shared the machine), `brand.logo.replace-every-slide` (the studio API not read in 90 s). The recorded and not chased class of SYNC.md 6.2; a rerun of `core/decks.spec.ts` and `core/brand.spec.ts` on the preview is the ship step's if it wants the reading clean.
- The sync and cost rows: the table below.

The 16 rows of SYNC.md 6.1 on the blob tier, from the run of record (the spec as loaded before the fixes of 4.3) and the cost probe:

| Row                                       | The preview                                                                                                                                                                                                                                                                                                                             |
| ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `sync.serial.order-and-latency`           | passed: latency mean 1662 ms, max 2868 ms over 30 words (bound 5000 ms), 30 POSTs each answered 200 at the next revision                                                                                                                                                                                                                |
| `sync.title.concurrent-both-kept`         | failed: round 1 both words in both browsers after 5 s and before 8 s; round 3 `... ca3 cb3 d db3a3` in both browsers, B's word inside A's (finding 1); the title travelled as `text.splice`                                                                                                                                             |
| `sync.title.offline-both-kept`            | passed: converged 4924 ms after the reconnect, A and B "Sync title offline ob1 ob2 ob3 oa1", `text.splice` on the wire                                                                                                                                                                                                                  |
| `sync.block.concurrent-same-offset-order` | failed: round 1 B's word reached A after 3 s (A `pa1end`, B `pa1pb1end` at 3 s); rounds 2 and 3 clean, no repair splice, final `pa3pb3pb2pa2pa1pb1end` in both (finding 10)                                                                                                                                                             |
| `sync.structural.concurrent`              | failed: part one two drags of one block within 200 ms left A `{x 423, y 400}` and B `{x 203, y 574}` at 3 s (finding 2); part two not reached                                                                                                                                                                                           |
| `sync.block.offline-replay-converges`     | passed: converged 5907 ms after the reconnect, A's splice offset 18 first and 30 on the admitted POST, A "Start of the block ob1 ob2 ob3 oa1"                                                                                                                                                                                           |
| `sync.viewer.live-updates`                | failed for the driver: C on the plain `/edit/<id>` never reached `window.turboslide.studio` in 90 s under enforce (finding 9)                                                                                                                                                                                                           |
| `sync.reload.same-document`               | failed for the same driver reason (C's page)                                                                                                                                                                                                                                                                                            |
| `sync.undo.after-remote`                  | passed (three of three)                                                                                                                                                                                                                                                                                                                 |
| `sync.resend.idempotent`                  | not driven by its own rule: the dropped answer's POST landed once (status 200, one POST carrying the op ids, one record after, the word once in both browsers); the record carries no `origin` on deployment N                                                                                                                          |
| `sync.pull.no-listing`                    | failed (a P0 row): 10 commits from B landed in A, 12 ops POSTs answered 200; `storeCalls` over the window: `list` 2 against 0, `get` 26 against 10 commits, `head` 118, `put` 40; two instances answered `sync.status` (findings 4, 6, 7)                                                                                               |
| `cost.editor-idle.calls`                  | failed on the store half (measure: true, over its ceiling on the preview, so it holds the ship by 6.2): function requests 9.64 a minute against 12 (29 in 3 minutes, 9 of them the poll); store simple 34 a minute against 40; store advanced 18 a minute against 11 (put 18, del 5, list 0; finding 5); one instance answered          |
| `cost.editor-hidden.calls`                | not driven by its rule (`visibilityState` never read hidden); the visible tab's counts recorded: function requests 9.65 a minute, store simple 33 and advanced 18 a minute, two instances                                                                                                                                               |
| `cost.editor-editing.calls`               | passed: function requests 69.24 a minute against 75 (208 in 3 minutes, 9 polls); store simple 127 a minute against 140 (head 117, get 10); store advanced 75 a minute against 85 (put 72, list 3, del 5); one instance                                                                                                                  |
| `cost.two-tabs-idle.calls`                | passed: `list` 0 against 0; store advanced 20 a minute against 30 (put 20, del 6); simple 14 (head 12, get 2); both tabs connected and in each other's roster; function requests 19.3 a minute over the two tabs                                                                                                                        |
| `cost.show.calls`                         | failed on the store half (measure: true, over its ceiling on the preview): function requests 0 in the window against 0 (the page's half, B5, holds); `storeCalls` at the first sample 12 against 0 (head 9, put 2, list 1: the show page's own load and the probe's `deck.info` inside the first 60 s window; finding 14); one instance |

The store status actions on the blob tier answered with `storeCalls` and an instance id on every sample; the pull row's two ids and the probe's seven across the rows are the routing SYNC.md 6.3 foresaw.

The rerun of `core/sync.spec.ts` against the preview after the fixes of 4.3: `PLAYWRIGHT_BASE_URL=<preview> node_modules/.bin/playwright test apps/studio/e2e/core/sync.spec.ts` (23:35:08Z to 23:44:43Z): 4 passed, 5 failed, 1 skipped. `sync.serial.order-and-latency` passed (latency mean 793 ms, max 2776 ms); `sync.title.offline-both-kept` passed (converged 5.9 s after the reconnect); `sync.block.offline-replay-converges` passed (6.9 s, the offset 18 to 30); `sync.undo.after-remote` passed. `sync.title.concurrent-both-kept` failed again on the same mechanism (round 1 B's word not in A within 5 s; round 3 `... ca3 cb3 da db33`, B's word inside A's), `sync.structural.concurrent` part one read the same two positions (A `{423, 400}`, B `{203, 574}`), `sync.block.concurrent-same-offset-order` missed the 3 s bound once (round 2, B's word in A after 3 s; the final text agrees), and the two C rows read the driver's 90 s wait. `sync.resend.idempotent` skipped by its rule with a reading worth the ship note: the dropped answer's op was posted twice and the log holds two records after it (the run of record read one), the duplicate the record's origin is there to answer in deployment N plus 1; the word read once in both browsers. So on the blob tier two findings reproduce in two runs (1 and 2), and the rest is the driver or the bound.

#### 4.3 The new spec's seams, found by the runs and closed at the merge

`apps/studio/e2e/core/sync.spec.ts` had never run before this merge (b4.md section 3: the checkout's lock was another lane's for the whole window; b1.md and b2.md the same). The run of record on 4418 read seven of its ten rows red, and a probe of the integrator's (a temporary spec beside it, deleted after its one run at 21:53Z) read the mechanisms first hand:

1. `openRun` double clicked the run's centre, where `document.elementFromPoint` read `button.ts-handle.is-selected`, the move handle of the heading `newDeck` had typed and left selected; the block placed under it by `bodyBlock` never opened a session (four rows: the block order, the offline replay, undo and the resend). `openRun` now clears a selection while no session is open (Escape) and, when the double click opened nothing, makes one more from the element itself; `bodyBlock` clears both browsers' selections after the placement.
2. `sync.serial.order-and-latency` registered each token for B's sampler only after polling A's revision past its previous value; the revision is the checkpoint's and moved 2.5 to 3.2 s after the op, while B's DOM showed the word at 470 ms and B's seq moved at 470 ms (the probe), so the row measured its own poll: 2.9 s per word against the memory tier's 1 s bound. The token is registered before the poll now; the rerun read 192 ms mean, 250 ms max.
3. `sync.structural.concurrent` part two polled B's stage for a block on a slide B had not switched to (the stage draws the current slide alone), then started A's drag 120 ms after B's Delete slide, whose `selectBlock` click waited for a block the delete had removed until the test's 300 s timeout. Both browsers now switch to the slide before its block is read, and A holds the block mid drag while B's delete lands and releases once B's filmstrip has lost the slide, so A's write is the one that meets the deleted slide; the row runs in 18 s. What it then reads is the finding below (no reject card on A).
4. `sync.viewer.live-updates` asserted C's role is `viewer`; under shadow authorization (a dev server, production today) every visitor reads `owner`, so no viewer floor exists to drive the row on. The row is not driven with that sentence where C reads `owner`, and still fails on any other role that is not `viewer`.

The preview's run of record loaded the spec before the fixes (its rows print the old line numbers), so its sync rows read the driver seams on the blob tier as well; the rerun of the spec against the preview after the fixes is 4.2's second reading.

### 5. Findings by owner

Findings a run of this merge produced, by the owner SYNC.md 5.1 names; each with the run that read it. A finding is not a fix: nothing of a lane's was changed for it here beyond the spec seams of 4.3.

**B1 (the realtime core)**

1. `sync.title.concurrent-both-kept` on the blob tier (the preview's run of record): round 3 read `... ca3 cb3 d db3a3` in both browsers, B's word `db3` landed inside A's `da3` (between `d` and `a3`), so both browsers agree and the seller reads a colleague's word inside their own. A's word travels as two bursts; B's insert at the same offset was placed between them (the tie of two inserts at one offset, or the transform of A's second burst past B's insert), on the blob tier alone (three rounds of three clean on the memory tier in both runs). Round 1 of the same run read both words in both browsers after 5 s and before 8 s. Severity: the row is a P0 row (the words are kept; the order within a word is not). The rerun of 4.2 is the second reading.
2. `sync.structural.concurrent` part one on the blob tier (the preview's run of record): two drags of one block within 200 ms left A at `{x 423, y 400}` and B at `{x 203, y 574}` at 3 s, each browser holding its own gesture; the memory tier read one position (`{203, 574}`) in both runs. Invariant 2 outside text is what this row falsifies (SYNC.md 6.1); the mechanism (a refused second `block.set /pos`, a rebased one, or a late echo) is not read from the run's JSON and needs the wire.
3. `sync.structural.concurrent` part two on both tiers: A's drag released after B's Delete slide posts nothing (the block left A's stage with the slide), so no loser exists and no reject card shows; the row's third check (the loser reads the reducer's sentence) cannot pass with a drag as the loser's gesture. With B4: either the row's interaction names a write that reaches the server (a `block.set` through the window API after the delete), or the reducer's sentence for a write against a deleted slide is read by a different row.
4. `sync.pull.no-listing` on the preview: `storeCalls.list` read 2 in the window against a ceiling of 0, with two instances answering `sync.status`. The two lists are either the prune at a stream's close (`pruneAtClose`, B1's landing of b3.md R3: a stream that reaches its lifetime inside the window closes and lists `snapshots/` once per instance) or `deck.info`'s `counts.snapshots` list (b3.md R7 c). With B3: the row's ceiling and the prune rule of SYNC.md 3.6 disagree whenever a stream closes inside a measured window.

**B3 (the store tier)**

5. `cost.editor-idle.calls` on the preview: `storeCalls.advanced` 18 a minute against the ceiling of 11 (put 18, del 5, list 0 in the worst 60 s window; simple 34 against 40; function requests 9.64 a minute against 12). An idle tab pushes presence at the quiet cadence (6 a minute) and each push costs three puts (the record, its immutable copy under `putWithCopy`, and the pulse), so the store half of the row is 18 where SYNC.md 6.1's model expected about 7 (11 at the 1.5 ceiling). The function half passes; the store half is over its ceiling on the preview, and a measurement row over its ceiling on the preview holds the ship (6.2). With B2 (the cadence is B2's, the puts per push B3's) and Kevin (question 4: the ceiling's model against the store cost as built; the round after's presence copy rename and roster from the pulse's copy are the items that cut the puts).
6. `sync.pull.no-listing` on the preview: `get` 26 against 10 commits where the row's text reads "one snapshot or record read per commit"; the reader as built reads the snapshot and the record of every remote commit (b3.md section 1: 4 heads and 2 gets per remote commit), so 2 per commit plus the pull's own reads is the model's number and the row's sentence undercounts it. With B4 (the ceiling's wording).
7. Two instances answered `sync.status` inside one 3 minute window on the preview (`instances 2` on the pull row), so the store half of every cost row on a deployment is the maximum of two instances' windows, as 6.3 foresaw; the ceilings carry it only where the sampled instance is the one that did the work.

**B4 (the matrix and the drivers)**

8. The four seams of 4.3, closed at the merge.
9. `joinC` under enforce (the preview's run of record): C on the plain `/edit/<id>` never reached `window.turboslide.studio` in 90 s, so `sync.viewer.live-updates` and `sync.reload.same-document` read red for the driver, not the product. Under enforce a fresh principal on the plain editor address lands outside the editor (the product round's `share.stranger-cannot-edit`; VERIFICATION.md's note on `fonts.budget.no-load-before-ready`); C must open the deck's link address (the `share.get` record's link with its token) or `/deck/<id>` where the viewer's window API is registered.
10. `sync.block.concurrent-same-offset-order` round 1 on the preview: B's word reached A after the row's 3 s (A `pa1end`, B `pa1pb1end` at 3 s; both browsers agree on `pa3pb3pb2pa2pa1pb1end` at the end); the blob tier's propagation read 1.7 s mean and 2.9 s max on the serial row of the same run, so a 3 s bound on that tier is inside the noise. The recorded and not chased class of 6.2 names 5 s for a word; the row's bound may want the tier split the serial row has.
11. `sync.resend.idempotent` skips on the record's `origin` as designed; on deployment N it will read not driven on every origin until N plus 1 writes the origin.
12. `cost.show.calls` on the preview: the store half read 12 calls at the first sample (head 9, put 2, list 1) against a ceiling of 0. The first sample sits 60 s into the window and the counters' window is the last 60 s, so the show page's own load (the SSR's reads of the deck) and the probe's `deck.info` before the window (the `list` of `counts.snapshots`, b3.md R7 c) are inside it; the function half (0 requests, B5's fix) holds. The row's store half needs the first sample past the load's 60 s, or the load's reads named; the store cost of a show load itself (9 heads) is the round after's published show snapshot (SYNC.md 5.2, 4.4 item 7).

**B5 (the pages)**

12. `cost.show.calls` and the function half of the idle and editing rows read under their ceilings on both origins (4.2); `cost.editor-hidden.calls` is not driven on any Playwright base by its rule (the verifier's hand row).

**Inherited, not this round's**

13. `assist.tailor.agent-deck-tailor` red on the memory tier and green on the blob tier, as in the product round's own runs; `assist.quota.429` not driven without Upstash; the parked features and rows of `ship-b22ffa0.json`; `brand.background.enter-keeps-open` red on the preview's walk (the verifier's pass 2 read it red on another read too); the check chain's standing reds of section 2.

### 6. Deviations

What did not run, or ran otherwise than SYNC.md 5.1 and 6.2 describe, with the reason.

- The merge order was read, not applied (section 0): the lanes built on one shared checkout and every file was already in place; the integrator committed nothing (no git write command is the pipeline's rule for this role), so the round's branch, the ship's `ship-<commit>.json` and the two deployments N and N plus 1 are the ship step's.
- Check step 3 fails as written on the uncommitted tree (the generated files differ from `HEAD`) and passes by regeneration, as every merge since round three recorded.
- Check step 25 (Docker) and step 27 (nothing answers on 4344) are skipped by the runner's own rule; step 32 (the core gate on 4321) was not run as a chain step: the same gate ran on this integrator's server on 4418 under a scratch lock (section 4), and a second 90 minute run of the same tree on 4321 would have told nothing new inside the window. Step 32 as a chain step therefore reads not run here; the runs of section 4 are the round's gate readings.
- The check chain ran as `--only <n>` per step (sections 2): the runner stops at the first red step, and steps 3 and, until the README re-render, 5 and 19 were red by their known mechanisms, so the chain as one command would have stopped at step 3.
- The four two person rows by hand at human speed on production (SYNC.md 5.1, the verifier's) and the production run of the gate wait for the ship; nothing moved to production this pass.
- `cost.editor-hidden.calls` is not driven on either origin by its own rule (`visibilityState` never reads hidden under Playwright, b5.md and b4.md); the verifier drives it by hand on the preview (section 8).
- The Playwright rehearsal of the flip (b6.md R3) was not built (section 8).
- The two gates and the check chain's steps ran together on one machine (the load in section 2 and 4); AGENTS.md's one browser rule was kept for the checkout (the local gate held its lock, the write probe the repository's), and the preview gate drove a remote origin, as the verifier's passes did.
- `.turboslide/e2e.lock`: taken 21:28:05Z to 21:28:48Z by the write probe, 21:53:42Z to 21:54:09Z by the integrator's temporary probe spec (`apps/studio/e2e/core/zz-int-probe.spec.ts`, written for one run and deleted after it), 21:55:56Z to 22:05:39Z and 22:08:22Z to 22:08:41Z by the two sync spec reruns; the local gate took its own scratch lock (`--lock`); nothing holds a lock at the return.
- The store after the runs: `deck.list` with the trash over HTTP on the preview at 23:46Z listed 75 decks, five of them made today, all between 22:29Z and 22:36Z inside the preview gate's spec run (the decks spec's fixtures `Northwind renewal v2` (trashed by its file), `Northwind renewal v4 without notes`, `Import bundle source mubtoubr`, `Northwind renewal`, `Thumbnail deck`: the two 60 s timeouts of `decks.list.open-title` and `decks.card.menu-open-escape` under the machine's load ended their tests before their teardown). Each was trashed and deleted forever through `deck.trash` and `deck.remove` on the actions API at 23:47Z and answers 404 to `deck.info`; the second listing reads none of today. Every other scratch deck of the walks, the specs and the probes was torn down by its own file (the walks' decks, the cost probe's `gone: true` rows, the ordering probe's `x4v4`).
- The dev server on 4418 was stopped at 23:45Z; the tmp store under the scratchpad died with it; no scratch deck ever touched `decks/` of the checkout (the chain's own server steps run on their own store and left none).

### 7. For the ship note

Facts the ship note carries, gathered from the lanes' requests and this merge; the ship step writes the note, this section is its source.

1. Two deployments in order (SYNC.md 3.2, 5.1): deployment N is this tree with `RECORD_ORIGIN_WRITES = false` in `packages/store/src/blob-store.ts` (the tolerant parser: a record with `origin` parses, nothing writes it); N plus 1 flips the constant to `true` and is one commit after N. N plus 1 goes out only once N is the production deployment an Instant Rollback would land on, and the note records that a rollback of N plus 1 lands on N and never earlier. Until N plus 1 is production the row `sync.resend.idempotent` reads a known mechanism (the resend is caught by the write path's origin check alone, which reads the records above the base for a `Write.origin` no record stores yet; b1.md decision 8, b3.md section 4).
2. The hosting move has not happened: production is the one deployment on the store, so the two commits follow each other on it (HOSTING-MOVE.md 2.6). Nothing on Vercel was created or changed by this round; the enforce preview of section 3 is the one deployment this pipeline made.
3. The parked list production runs under is the product round's `docs/gslides-parity/focus/ship-b22ffa0.json` (the features inbox and templates; the rows `view.live-pointers.second-browser`, `versions.show-changes-marks`, `collab.roster.go-to-slide`), committed at `c4ad8b1`; both gate runs of section 4 read their verdict under it. The ship step commits the ship's own `ship-<commit>.json` before its runs (SYNC.md 5.1); the two new features `sync` and `cost` are unparkable (`UNPARKABLE_FEATURES`), so a red row of theirs blocks the ship and never parks.
4. The agents' line (b5.md R5): a viewer or show page reached by an agent must be opened with `?agent=1` and kept visible; `deck_goto_slide` is listed only while such a page or an editor is visible; a page hidden for 10 s detaches and answers 404 until shown. `DEFAULT_COMMAND_TIMEOUT_MS` is 30 s (was 15), so a command to a page that is attached but not answering waits up to 30 s before its 504.
5. The deploy window (b5.md R5): a tab on the older deployment keeps asking a 25 s hold, is clamped to 0 by the new server and paces itself with its own 6 s pause (about 10 polls a minute of a few milliseconds each) until it reloads; a tab on the new deployment against an older instance is not a case (the alias moves whole).
6. The chip bound on the blob tier (b1.md R7): a tab alone reads a colleague's arrival on its next quiet tick (10 s), so the appearance half of `collab.presence-chips` sits at its 10 s bound there; `HOSTED_POLL_QUIET_MS` in `pulse.ts` is the one number if a production run reads it red (8 s keeps 7.5 calls a minute).
7. `README.md` and `docs/updates.md` (SYNC.md 5.1 shared file table): the release note and the address change of HOSTING-MOVE.md are the cutover commit's; the README's what works block was re-rendered by this integrator after `c4ad8b1` (section 1) and needs nothing more until the matrix moves.
8. The destination literal `https://turboslide-general-translation.vercel.app` stands in `hosting/redirect/vercel.json`, `hosting/redirect/index.html`, `hosting/redirect/README.md`, `scripts/hosting-check.mjs` (`DEFAULT_TO`) and `RUNBOOK.md` until the team project names its alias (b6.md R6); the cutover commit replaces it and passes `--to <alias>` to the check.
9. The Playwright rehearsal of the flip was not driven (section 8), so the ship note states the loss from HOSTING-MOVE.md 1.7 by the code reading: a tab left open through the flip reads offline and loses what it typed after the flip at its reload, which is why the drain (`scripts/probes/presence-drain.mjs`) waits for every roster row to expire before step 7.
10. Check step 19 was red on 158 committed evidence and note files of the discovery and product rounds when this round started (the product round's ship.md section 251 recorded it); it is green on this tree (section 2), by the `.prettierignore` rules for the run JSON and a `prettier --write` over the notes and scripts. The `git diff` of those files is whitespace alone.

### 8. What stays for later

- The Playwright rehearsal of the flip (HOSTING-MOVE.md section 5 step 5, section 9; b6.md R3): two browsers holding pending edits, `page.route` answering the ops POST and the stream with a 307 to a second local origin, the title rows and the reload recorded. Not built: the integrator's window went to the seams and the runs; the shape is in b6.md R3 and the ship note carries the code reading of 1.7 until it is driven.
- One definition of the write's origin (b1.md R4): `StoreWrite` becomes `Write` and the store's `WriteOrigin` a re-export of the schema's, and the structural reads in `blob.ts` (`recordOrigin`, `replayedRecordOf`) lose their casts. Both compile and every test passes as they stand; a cleanup for the round after's B3 and B1.
- The P1 set of SYNC.md 5.2, none of it built this round as instructed: the lost claim in its corrected form, `versions/head.json`, the published show snapshot, the `/decks` index record, the presence copy names and the sweep, the idle stream close and the paused tab, the 60 s access trust, `flags.json`, the picture twin and the font catalog.
- The closed tab's lingering session (b5.md R7): a `pagehide` beacon to the detach function or `DEFAULT_STALE_MS` at 35 s, so a closed tab's session stops taking the next `deck_goto_slide` for up to 45 s plus the 30 s command timeout.
- The tab hide as a card thumbnail trigger (b3.md R5, declined this round): the stream's close and the 30 s rest cover the seller who closes the tab or stops typing.
- `cost.editor-hidden.calls` by a real browser hide: a covered tab reads `visible` under Playwright in every way B5 and B4 tried (b5.md blockers, b4.md finding 2), so the row reads not driven with that reason on every Playwright base; the verifier drives it by hand on the preview (open the editor, switch tabs, read `/api/agent?deck=` at 15 s for no session, switch back for one session within a second). The product's hidden path is covered by the unit test, B5's synthetic `visibilitychange` run and a real hidden page in the desktop app's Browser pane (b5.md section 5).
- The remaining unformatted files outside this round's folders were formatted by this integrator (section 7 item 10); the rule for the next round is the one the focus round wrote: run JSON under an ignore glob, notes under the formatter, at the lane's return.
