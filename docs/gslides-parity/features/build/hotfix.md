# Features round, ship one: the hotfix (2026-09-24)

Written by the hotfix agent, part A, as it went. The state at the start, read at 08:05 PDT (15:05Z): ship one of the features round on `origin/main` (`9d21394` the ship, `2be923e` the production table), production `https://turboslide.vercel.app` serving `9d21394` (`dpl_441AUoH4Dmo4DBsKWsVbmBCRJp65`) under shadow authorization with no `ANTHROPIC_API_KEY`, the parked list `docs/gslides-parity/focus/ship-f1afe1e.json`. The production run of record (`gate-production-features-one.json`: 785 rows, 747 passed, 20 failed, 18 not driven) read two rows red in both production readings, each with its mechanism in `ship.md` 13.4 and 13.5 and the production table of `VERIFICATION.md`: `logos.picker.paper-and-ink` (the logo routes answer 500 on a cold instance while the public store's edge refuses the just written index, and every discovery rewrites the 3.6 MB index) and `collab.slide-added-appears` (the pulse's cadence reads its company from a per instance heuristic, so an editor's instance can count itself alone while another editor works on another instance). This note records what was done for each, with the checks, the preview, the two gate runs and the push.

The rules of the task hold throughout: every command runs from the worktree `/Users/kevinliu/repos/Turboslide-hotfix`; the shared checkout `/Users/kevinliu/repos/Turboslide` is ship two's and is never touched; no `pnpm install` beyond the one frozen install of step 1, no `pnpm add` or `pnpm exec`, no build outside `scripts/check.mjs`; the branch `round-five/integration` is never checked out, merged or cherry picked; no token, cookie or secret is printed; nothing on Vercel is created, changed or deleted apart from the preview deployment this note names; Kevin's standing rows `sync.title.concurrent-both-kept` and `decks.file.open-list-search` are never chased.

## 1. The worktree prepared

- `git -C /Users/kevinliu/repos/Turboslide-hotfix fetch origin` at 15:06Z; `origin/main` at `2be923e9d68b65c251172acb04c2157fc065704a`, the same as the shared checkout's `main`. The worktree stood detached at `9293df4` (the ring hotfix of 2026-09-20) with its own `node_modules` from that day's install.
- `git checkout --detach origin/main`: `HEAD is now at 2be923e`. The tree is clean.
- `pnpm install --frozen-lockfile --prefer-offline` inside the worktree: "Already up to date", 185 ms; `git diff --stat 9293df4 2be923e -- pnpm-lock.yaml` is empty, so the lockfile did not move between the two commits and the worktree's own `node_modules` serves the detached tree (`node_modules` is a folder, not a link).
- `/Users/kevinliu/repos/Turboslide/.turboslide/vercel-dev.env` copied to `/Users/kevinliu/repos/Turboslide-hotfix/.turboslide/vercel-dev.env` (1,374 bytes, never printed; `.turboslide/` and `.vercel/project.json` are ignored, `git check-ignore` confirms both). `.vercel/project.json` names the project `turboslide`.
- The first `node_modules/.bin/tsc -b` failed with eleven errors in the route files (`/api/assist`, `/api/logo/$`, `/decks/templates`, `/fonts/$` "not assignable to keyof FileRoutesByPath"): `apps/studio/src/routeTree.gen.ts` is generated and ignored (`.gitignore` line 17) and the worktree's copy was the one Vite wrote at `9293df4`. `node_modules/.bin/tsr generate` from `apps/studio` regenerated it against the routes of `2be923e` (it differs from the shared checkout's copy only by the `Register` block the Vite plugin appends). Then `node_modules/.bin/tsc -b` exits 0, clean, at 15:12Z, before any change.

## 2. Seam one: the logo routes and the store's busy window

The mechanism, from ship.md 13.4: `createLogoService.load()` read `system/logo-index.json` through `client.get`, which throws "Vercel Blob: Failed to fetch blob: 403 Forbidden" on a pathname the public store's edge does not serve yet for two to three minutes after a write (`isStoreBusy` in `packages/store/src/pulse.ts` names the class), and the throw reached every logo route (search, mark, insert) as the framework's 500 while `revalidate()` caught the same read; `persistSoon` rewrote the 3.6 MB index after every discovery a search made (a cached mark, an unavailable variant), so a seller's searches opened the window themselves. What landed, in `apps/studio/src/server/logo-index.ts`, `apps/studio/src/server/logos.ts` and `apps/studio/src/routes/api/logo.$.ts`; nothing in `packages/store` needed to change (its `isStoreBusy` and blob-fake's `failNextGet` were what the seam needed):

- The index's pathname is written by the refresh alone. A discovery goes to the small side record `system/logo-discoveries.json` (`LogoDiscoveries`: `cached` keyed as the index keys its cached marks, `unavailable` by slug and variant, `at`), held per instance in `pending` and persisted at most once a minute per instance (the throttle `persistSoon` always had), merged over the record as read then (`mergeLogoDiscoveries`, the later mark wins). Every instance folds the record into the index it loads (`applyLogoDiscoveries`, at `load()`), so a mark another instance cached is read from the store and fetched once, as before; the refresh folds the record and the caller's own pending discoveries into the previous index before the manifest is merged, writes the file, then clears the record (best effort, a refused put leaves entries the next fold skips). `lastError` stays per instance (`memoryError`, the foot of 4.9), which FEATURES.md 4.9 allows; it never wrote the index. The refresh's `dryRun` reads the record and clears nothing; its counts read the held index as before.
- `load()` tolerates a busy store: a read `isStoreBusy` classes falls back to `readMirror()`, the copy this instance holds on disk from its last read or write (the Blob store's mirror folder, written on every read and write; the disk store's own file), with the version left unknown so the next revalidation asks the store again and never takes the copy for the store's version (an earlier draft put the fallback inside `blobLogoStore.readIndex`, which would have let `revalidate()` stamp a stale copy with the new version; moved). With no copy `load()` throws `LogoStoreBusyError` (`status` 503, `retryAfterS` 5, the sentence "The logo index is not readable right now; try again in a few seconds") and the next request loads again. `revalidate()` and the mark route's re-reads kept their catches and serve the held copy.
- The four routes answer a store refusal as 503 with `retry-after: 5`, `code: store_busy` and the sentence (`storeBusy()` in the route file, over `isLogoStoreBusy`: the typed error, or the store's own error thrown under a read such as the refresh's first `readIndex` on a cold instance), never `errorResponse`'s 500.
- The mark route: a cached open mark whose file the edge withholds (`readMark` throws the busy class) is answered from the upstream for that call, sanitized, and nothing is written or recorded, since the store holds the file (`withheld`); a `readMark` that answers null keeps its meaning of "gone" (a takedown elsewhere) and its revalidation. Before this the busy read was swallowed as null, the mark was fetched and written again with `overwrite`, and the write reopened the edge's window on the file for every other instance.
- The refresh adopts the index it wrote from `refreshLogoIndex`'s `onWritten` instead of reading the just written file back through the edge (one 3.6 MB get fewer per refresh, and no read in the window it opens); the fixture's inline build at a fresh instance's load does the same.
- The picker's search already parses the index once per instance (`held`); the cold 530 ms of `logos.picker.search` is a cold instance's first get and parse of the 3.6 MB file, and no cheaper copy is at hand on a cold function instance (its `/tmp` mirror is empty), so nothing changed there; recorded in section 8 for Kevin.

`apps/studio/src/server/logos.test.ts` gains the describe "the store's busy window and the discoveries record (build/hotfix.md 2)" over `blobLogoStore(memoryBlobClient(), <mirror>)` with `failNextGet` and the SDK's 403 sentence: a search during the window answers from the held copy and never takes it for the store's version, then adopts the build once served; a cold instance with no copy answers `LogoStoreBusyError` (503, 5 s) and loads on the next request, an instance whose disk holds its last read serves it, and a mark during the window answers the mark; a cached mark whose file the edge withholds is answered from the upstream with no put; a discovery writes the side record (one put, a few hundred bytes, under a quarter of the index) and never the index, the second discovery inside the minute stays pending, and an instance loading after it reads the cached mark from the record and searches without reading it again; the refresh folds the record and the pending discoveries into the index (one get of the previous index, none after its put), clears the record, and a dry run clears nothing. The test "adopts another instance's refresh when a search names its build" now asserts the discovery in the record and `store.writes` at 1, where it asserted the index rewritten.

## 3. Seam two: the pulse cadence across instances

The mechanism, from ship.md 7.3 and 13.5: the pulse's tick is 2 s while an op landed on this instance in the last 30 s or the deck has company, 10 s otherwise (`pollTickMs`); `blob.ts` counted a roster row as this instance's own when this instance's presence route had set it (`ownClients`), on the assumption that a tab's presence POSTs land where its stream is, but the platform routes each POST on its own, so an editor's row set through the instance holding only the other editor's stream read as that instance's own, the instance counted itself alone and ticked at 10 s, and a slide added on another instance reached the owner 5 to 10 s later. What landed:

- `packages/realtime/src/channel.ts`: `SubscribeOptions` gains `clientId`, the server issued id of the tab whose stream the subscription is. `apps/studio/src/routes/api/decks.$deckId.stream.ts` passes it (the id is minted before the subscription); `apps/studio/src/server/room.ts` needed no change, its own subscription is `passive`. The memory and redis tiers ignore the option.
- `packages/store/src/pulse.ts` gains `pollCompany({ roster, streams, listeners })`, node free and pinned by `pulse.test.ts`: a roster row is this instance's own exactly when a stream of that client id is open here, every other row is company whichever instance's presence route set it, and a second own row or a second stream here is company as before; a stream that names no id counts through `listeners` alone and claims no row. `packages/realtime/src/blob.ts` replaces `ownClients` with `streams` (client id to the count of its open streams here, kept by `subscribe` and its release), `nextTickMs` reads `others` from `pollCompany` over the shared presence copy, and `presence.set` and `leave` stop the bookkeeping. `PRESENCE_EXPIRY_MS` leaves the module's imports.
- `nudge`: a presence POST of a tab whose stream is not here landing on this instance means the roster copy just gained a row of company, so a running poll re-arms its tick from the copy with no store call of its own (`presence.set` calls it when `streams` lacks the id; a poll already at the active pace re-arms nothing sooner). Without it the first blob test below read 7 heads where the active pace makes 20: the quiet poll counted the colleague only at its next quiet tick, up to 10 s away, the same gap ship one's `wake` closed for a stream opening. The roster question costs a store read only when the copy is older than the presence poll interval, so at most one per return from the quiet pace.
- The costs: an editor alone still ticks at 10 s when quiet (its one row is its stream's own by id, wherever its POST landed); `POLL_CALLS_PER_MINUTE_MAX` holds, one call per tick at either pace; the idle cost row's ceiling of 12 function requests a minute is the client's cadence and does not move. The preview's cost rows read the ceilings again in section 6.

Tests: `pulse.test.ts` gains "reads the deck's company from the roster and the open streams, own exact by client id" (two editors on two instances both active; one editor alone quiet; the own row exact by id: a row this instance's route set for a tab whose stream is elsewhere is company, a stream whose row has not landed claims no row; a second tab by row or by stream; the two paces under the budget). `blob.test.ts` gains three cases on the fake store over two channels: two editors on two instances with each row set through its own instance tick at the active pace on both, measured one instance at a time (8 or more heads in 400 ms at 20 ms a tick, the quiet pace 400 ms); one editor alone whose presence POST landed on the other instance ticks at the quiet pace (2 to 5 heads in 600 ms at 200 ms); a row this instance's route set for a tab whose stream is elsewhere is company and the quiet poll returns to the active pace at once through `nudge`.

### 3.1 The checks

At 15:27Z, from the worktree: `node_modules/.bin/tsc -b` exits 0; `vitest run` in `apps/studio` 71 files, 539 passed; `packages/store` 20 files, 256 passed, 1 skipped; `packages/realtime` 10 files, 141 passed; `node scripts/check.mjs --greps` clean (27 `innerHTML` call sites and 26 `overwrite: true` in the allowlist; the discoveries record's put is in `logo-index.ts`, an allowlisted file); `prettier --write` over the ten changed sources and tests and this note, then `--check` clean. Nothing in `packages/chrome` or the client changed: the dialog's tile draws what the mark route answers, and a 503 in the refresh's window on a cold instance leaves the half empty for that draw, recorded in section 8.

### 3.2 The local smoke

The worktree's dev server on 4419 (`TURBOSLIDE_STORE=tmp`, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, `TURBOSLIDE_LOGO_UPSTREAM=fixture`, the two secrets minted in the command; `apps/studio/node_modules/.bin/vite`, since the root `node_modules/.bin` carries no `vite`), 15:33Z to 15:40Z, stopped after. The routes: `GET /api/logo/search?q=figma` 200 with Figma first; `GET /api/logo/mark/vercel/dark.svg` 200 twice, `x-turboslide-logo-cached: 1`; `GET /api/logo/mark/acme/wordmark.svg` 404 "Acme has no wordmark on thesvg.org" (the fixture's 404 variant, a discovery); `POST /api/logo/refresh { dryRun: true }` 200 (9 icons, 8 brands, 11 cached marks, 1 unavailable); `GET /api/logo/search?q=acme` 200. The core walk's logos area against it under the worktree's `.turboslide/e2e.lock` (`node scripts/probes/editor-walk-probe.mjs --core --base http://localhost:4419 --only logos`, 172 s): 33 steps, 31 ok, 0 failed, 2 not driven (`logos.picker.variants`, `logos.kit.find-a-logo`, the P1 rows behind their parks); `logos.picker.paper-and-ink` read the ink half lit 37.88 percent on `#070707`, the paper half 38.19 percent with 0.00 percent white, the inserted mark 50.12 percent on the light deck; 0 console errors; the scratch deck trashed and deleted by the walk's finally block (`/edit` 404). The run's exit 1 is the narrowed run's 540 "no step" rows of the other areas, what `--only` produces; the table is in the scratch folder `hotfix-a/local-walk-logos.json`.

## 4. The commit

The branch `hotfix/features-one` from the detached head `2be923e`, the commit by an explicit path list (the eight sources, the three tests, this note; never `.github/`, never `apps/studio/src/routeTree.gen.ts`, which is ignored, never a file carrying a token: the paths were scanned for token, JWT and bearer shapes and the hits were comment rulers and a commit hash), authored as the repository's identity, with the message file written to the scratch folder and the trailer. No push yet.

## 5. The preview, its first reads and the hand drive

### 5.1 The deployment

The recorded command of the features round (`deploy-preview.sh`'s body, run with the worktree as the working directory through the scratch `hotfix-a/deploy-preview-hotfix.sh`: `vercel deploy --yes --archive=tgz` from the worktree's root with `TURBOSLIDE_AUTHORIZE=enforce`, `TURBOSLIDE_ASSIST=fixture`, `TURBOSLIDE_LOGO_UPSTREAM=fixture`, `TURBOSLIDE_MAIL=off`, `TURBOSLIDE_REALTIME=blob`, `TURBOSLIDE_PUBLIC_STORE_HOST`, the two secrets minted inside the command; never `--prod`, no project setting, no env command), 15:37:15Z to 15:41Z from `90c54df`: `https://turboslide-qcw93z6vi-kl01s-projects.vercel.app`, `dpl_FJCwkYhpFaG5ByjhJEweU45EuxDi`, target preview, Ready, created 15:38:51Z, behind Vercel Authentication, the one Blob store production uses.

### 5.2 The first reads

- `node scripts/hosted-smoke.mjs --base <preview> --token-env TURBOSLIDE_TOKEN` through `with-tokens.mjs`, 15:42Z: 40 of 40 (the enforce rows read as enforce: the thumbnail without its grant 403; `deck.info` 1 snapshot at revision 26; the effects backend typescript).
- `scripts/probes/new-write-probe.mjs` against the preview, 15:42Z: all steps ok (the twelve edits each acknowledged at the next revision, the reload, the second tab listing only the other, the trash and the delete forever of `untitled-20260924-oymc`). The probe's `createRequire` names the shared checkout's `package.json` for Playwright, which this hotfix must not use, so it ran from an untracked copy inside the worktree's `scripts/probes/` with the path pointed at the worktree, deleted after (the tree clean again).
- The store through the preview with the bearer at 15:45Z: `deck.list` with the trash, none of 2026-09-24 left (the probe's and the hand drive's decks removed; the count is in 5.4).

### 5.3 The hand drive of the two rows

`hotfix-a/hand-drive.mjs` through `with-tokens.mjs`, 15:43:23Z to 15:43:57Z, headless Chromium from the worktree's Playwright, on the deck `untitled-20260924-vpbx` it created from `/new` (trashed and deleted forever through the API after, `/edit` 404).

- Two browsers, the same person's cookies in two contexts (the sync round's B), the second landing `/edit` at 200 on its first load. A slide added by a click on New slide in one browser, the other read by `slide.list` and the filmstrip's cards every 100 ms: round 1, B to A 423 ms, A to B 2,172 ms; round 2, B to A 723 ms, A to B 2,191 ms; round 3, B to A 2,278 ms, A to B 2,160 ms. Six of six within 5 s; four of the six at the active tick's 2 s and two under a second (the add's POST landed on the reader's own instance, whose append announces at once). At round 1 A's roster did not yet list B (`others` 0) and B's listed A; by round 2 both listed each other, and at the end each saw the other's id. `sync.status` through the bearer, six calls, answered from two instances (`8794a82f`, `f42a69f1`); the instances the two streams landed on cannot be read from the product (the actions route takes the bearer alone, and a request from a browser context lands where the platform sends it), so the population is recorded as such and never as the streams' instances.
- The picker after a discovery. The shared store's `system/logo-index.json` is production's real index since part C's rebuild (4,945 brands, `last-modified` 14:20:04 GMT), and the preview's upstream is the ten mark fixture, so every mark outside the ten answers 404 from the upstream on this deployment: the mark route's 404s for `nextdotjs`, `vercel-blob`, `pi` and the rest are the fixture's condition, and each one is a discovery (an unavailable variant), the case the seam is about. `GET /api/logo/mark/acme/wordmark.svg` 404 "No logo named acme" (the fixture's invented slug is not in the real index); `GET /api/logo/search?q=vercel` 200; then Insert > Logo, "vercel" typed at 70 ms a key (6 tiles: Vercel drawn on both grounds, paper `light` and ink `dark`; the kit tile with no mark on this deck; v0 paper drawn and ink 404; the three others 404 on both), then "a" (21 tiles, the fixture's `ada` drawn on paper, its ink the tinted mask; the rest 404). In A's network log 40 `/api/logo` answers: 12 at 200, 28 at 404, none at or above 500 and no 503. The store after: the index's `last-modified` and etag unchanged (14:20:04 GMT), `system/logo-discoveries.json` 200, 382 bytes, `last-modified` 15:43:57 GMT: the discoveries went to the record and the index's pathname was not written, so no window opened. "Every tile drawn on both grounds" cannot be read on this deployment for marks the fixture does not serve; the reading over the fixture's marks alone is repeated after run 1 (5.5).

## 6. The two gate runs on the preview

Each run: `node scripts/probes/core-gate.mjs --base <preview> --out <scratch>/hotfix-a/gate-preview-run<n> --shots --parked docs/gslides-parity/focus/ship-f1afe1e.json --matrix docs/gslides-parity/focus/verification/features-one-hotfix-preview-run<n>.json`, from the worktree, both tokens through `with-tokens.mjs`, detached (`hotfix-a/run-gate.sh`), its log polled at most every ten minutes; no code change between the runs; the store swept of the run's decks between and after through the bearer (`deck.list` with the trash, `remove-deck.mjs`; only today's decks, never the earlier rounds' 70 or `gt-brand`).

### 6.1 The first attempt at run 1, stopped

Started 15:44:49Z. The walk's browser vanished at 388 s: step 127 (the setup slide for the text boxes) read ok and step 128, `text.textbox.insert-click-type`, observed "page.evaluate: Target page, context or browser has been closed", as did every step after it, so the walk ended at 15:51:17Z with 121 rows passed, 7 failed, 444 not driven, 34 console errors (all "Failed to load resource: 404"), and the cleanup could not trash its deck. The walk recorded no stall (`stalls: []`, no retired deck); the machine had 71 percent of its 128 GB free with no jetsam or memorystatus line in the system log; the unified log for 08:50:45 to 08:52:00 PDT holds no Chromium crash, no exit and no kill; ship two's lanes run their own browsers on the same machine (41 to 46 Chromium processes at the time). The cause is not known and nothing in the hotfix touches the client. The specs had run 37 of 207 when the run was stopped at 16:00:21Z (the process tree of this run alone, found from the wrapper's pid, SIGTERM; nothing of another lane); the log, the walk table and the partial ledger are kept beside the run as `gate-run1-stopped-1551Z.log`, `gate-preview-run1-stopped-1551Z/` and `features-one-hotfix-preview-run1-stopped.json` in the scratch folder, never under `docs/`. The store after: five decks of today (the walk's `untitled-20260924-uahv`, the export fixtures `untitled-20260924-ii0i`, `northwind-renewal-v4-without-notes` and `copy-of-northwind-renewal-v4`, the large deck's copy `gt-brand-mufpx99e`) trashed and deleted forever through the API (`trash 200, remove 200; /edit answers 404` each); `deck.list` with the trash then read 70 decks (54 live, 16 trashed), what part B and part C left, none of today. Run 1 restarted at 16:02:21Z from the same tree and deployment.

### 6.2 The state read by the gate part (19:25Z)

The first agent's process ended in the network outage of about 16:53Z, and its scratch folder was gone when this part started (the machine's scratch is wiped; the four helpers were written again from this note's descriptions: `with-tokens.mjs`, `remove-deck.mjs`, `deploy-preview-hotfix.sh`, `run-gate.sh`, and `act.mjs` for one action call). The worktree read as expected: the branch `hotfix/features-one` at `90c54df`, this note modified, nothing else; `.turboslide/vercel-dev.env` in the worktree the same bytes and mtime as the main checkout's copy (12:24 PDT; the token's `exp` decoded, never printed, 2026-09-25T07:24Z). The machine held 211 processes named for Chromium at 19:25Z, 77 of them the person's own Google Chrome; ship two's lanes were done and only its integrator and verifier ran browsers.

- Production read again (`vercel inspect https://turboslide.vercel.app`, read only, 19:36Z): the alias serves `dpl_2UjC4LUq928FvsYFZi4m7U3Ek87w` (`turboslide-1iakke242`, target production, created 07:59:09 PDT, the Git deployment of `2be923e`, the production table commit, whose sources are `9d21394`'s), not `dpl_441AUoH4Dmo4DBsKWsVbmBCRJp65` (`nf5u22l4f`, 8 hours old in `vercel ls`, the deployment of `9d21394` the pipeline read at 08:05 PDT before the table's push had built). `origin/main` after `git fetch` is still `2be923e`; ship two has not shipped.
- The preview `qcw93z6vi` still answered: the hosted smoke through `with-tokens.mjs --origin <preview>` at 19:28Z read 40 of 40 (the enforce rows as enforce, `deck.info` 1 snapshot at revision 26, the effects backend typescript), so no new deployment was made.
- The store swept: `deck.list` with the trash read 71 decks (55 live, 16 trashed), one of today, `untitled-20260924-aari` (created 16:02:24Z, the walk deck of the restarted run 1 that the outage killed, revision 561). Trashed and deleted forever through the bearer (`trash 200, remove 200; /edit answers 404`); the list then read 70 decks (54 live, 16 trashed), what part B and part C left, none of today. The templates facet listed four `core-spec-template-<id>` rows of today beside `blank` and `gt-brand` (the ids decode to 09:24Z, 12:42Z, 14:20Z and 15:52Z; the last is the stopped first attempt's, the others the day's earlier runs on the shared store); `template.delete` with `confirm: true` removed two (200) and answered 404 "No template under decks/templates" for `mufmcqwg` and `mufpn7hx` (index rows whose folders were already gone), and the list after read `blank` and `gt-brand` alone.

### 6.3 Run 1

Started 19:30:15Z from the worktree (`run-gate.sh 1`, pid 45811, `--out hotfix-a/gate-preview-run1`, `--matrix docs/gslides-parity/focus/verification/features-one-hotfix-preview-run1.json`), the walk on `untitled-20260924-melp`; polled every ten minutes.

The walk ended at 20:22Z (3,113 s, 618 steps): 572 matrix rows, 479 passed, 41 failed, 52 not driven, 0 no step; 248 console errors (131 resource loads answering 404, 87 answering 503, 30 answering 409); no stall, no retired deck; the cleanup trashed and deleted its deck (`/edit` 404). The two hotfix rows read green: `logos.picker.paper-and-ink` (the ink half lit 37.90 percent on `#070707`, the paper half 38.19 percent with 0.00 percent white, the inserted mark 50.12 percent on the light deck) and `logos.picker.search` (one tile 110 ms after the last key, Figma inserted, the foot naming 24 September 2026). The 41 failures and 41 of the 52 not driven rows are one window: from step 36 (`slides.delete.two-selected-key-undo`, 19:34:20Z) the walk's tab could not land its writes (step 61 at 19:39:54Z read the save stuck on "Couldn't save, retrying" with the reject card "12 changes were not applied. Another change landed first", the console's 409s, and the editor loaded again; step 36's reload met the prompt "14 unsaved changes from this browser", applied), the setup slide `split-1` of the layout rows was among the changes not applied, so every `slides.layout.*` row through step 89 read "no filmstrip card split-1" or "No slide split-1", `slides.notes.per-slide`, `slides.context.empty-canvas` and `slides.numbers.apply` the same, and the setup steps of the formatting rows (26) and the brand rows (15) failed on the same state ("setup failed: a slide for the formatting rows"); from step 101 (19:42Z) every row landed and the areas after (text from step 106, pictures, arrange, shapes, lines, view, logos, chrome, cleanup) read as ship one's runs did. The 503s are the ops route's transient answer for a store refusal (`decks.$deckId.ops.ts`: a 429 or a 5xx from the store, its deadline, or the edge answering 403 on a fresh deck's file, VERIFICATION C3S-F4) and the 409s its base check after the resend; nothing in the hotfix reaches that route (the logo routes, `pulse.ts`, `blob.ts`'s cadence, the stream route's client id; the stream's slot cap is `streamCounters`, untouched; the discoveries record's read at `load()` is caught). The eleven other not driven rows are the ten P1 rows behind their parks and the manual clipboard row. Run 1's walk is a reading of that window on the shared store; run 2 decides the rows (a row passes when it passes the last run of each origin), and a runtime log tail of the preview (`vercel logs --json`, read only, to the scratch folder) was started at 20:39Z to name the routes and statuses of any window during run 2.

The window, read from the preview's runtime log (`vercel logs <preview> --since --until --limit 5000 --json`, read only, fetched a minute at a time for 19:31Z to 19:44Z into the scratch folder; 540 distinct entries): from 19:32:55Z to 19:35Z every `POST /api/decks/untitled-20260924-melp/ops` answered 503 (16 to 17 a minute, the tab's resend cadence), each carrying the room's line "read at revision 22 after 6 syncs; the caller asked for 23" (then 24, then 25): the instance answering the tab's writes read the deck's files at revision 22 through the public store's edge and stayed there while the tab's base moved to 25, so the base check could not verify the base and the route answered its transient 503 (`storeRefusalOf`, the mirror's pull on the base check, VERIFICATION C3S-F4); "polling <deck> failed: Vercel Blob: Failed to fetch blob: 403 Forbidden" once at 19:34:20Z and once at 19:37Z on the poll's read; at 19:36Z to 19:37Z the resends met 409 (13 and 12: the base had moved past the tab's, the reject card's "Another change landed first"); at 19:38Z 19 more 503s and then 200s (7, then 24 a minute), and from 19:39Z every write landed. No instance booted inside the window (the boot lines of new instances sit at 19:42Z, after it); the presence route answered 200 throughout and the stream route 200 at its opens. The mechanism is the public store's edge serving the deck's files stale to one instance for about five minutes after another instance's writes, the CDN lag class ship one's note records (5.2 `logos.index.refresh-fixture`, the return round's measured blob tier timing): one of the two recorded classes of FEATURES.md 7.2, and outside anything the hotfix touches (the mirror's pull and the base check are `packages/store` and `server/room.ts`, unchanged). The templates line "templates/index.json still read an older copy after the CDN lag budget; the pull is skipped" (52 entries from 19:31:36Z) is the same edge on the index this part's sweep wrote at 19:30:13Z, and it is harmless by its own sentence (the instance's mirror stands).

The specs (20:22:09Z, 4,172 s): 187 passed, 15 failed, 5 skipped, 0 flaky, `retries` 0. `collab.slide-added-appears`, the second hotfix row, passed, as did its siblings `collab.edit-from-second-browser` and `collab.presence-chips`. The fifteen, with the reading from the report (`spec-fails.mjs` over `specs.json`): the parked feature `templates` on five rows (`templates.save.as-template`: the saved template not listed under Your organisation, the templates index the store's edge served stale, the line "templates/index.json still read an older copy after the CDN lag budget" in the runtime log; `templates.save.same-name-replaces`, `templates.card.rename-and-delete`, `templates.default.use-for-new`, `templates.gallery.page`), the parked rows `view.live-pointers.second-browser` and `inbox.notification-arrives` (the parked feature `inbox`), Kevin's standing `sync.title.concurrent-both-kept` (round 3 after Escape interleaved in both browsers; never chased); and seven rows red on the blob tier's timing: `export.download.progress-per-slide` (no download event in 90 s) and `export.download.large-deck-pptx` (no file after 600 s; the export route untouched by the hotfix, the export job's tail ship one's 5.2 records on `fonts.export.editable-names-face`), `logos.index.refresh-fixture` (refresh 1 answered 200 in 958 ms and rebuilt the shared index to the fixture's ten marks at 21:09:51Z, then the `acme` search seconds later was answered by an instance still holding the 4,945 brand build, so the row's `acme` was undefined and the spec threw its own `TypeError` on `JSON.stringify(row).slice` at logos.spec.ts 449; the store's version head lagging the write, the class ship one's 5.2 records on this very row, and a driver defect for Kevin's list), `logos.cache.open-licence-only` (the Figma insert 200 and cachedMarks 19 to 19 with the mark cached before, then `searchHttp('aws')` answered the real build's `aws` row by one instance and `logo.insert` "No logo named aws on thesvg.org" by another holding the fixture build: the same head lag in the minute after row 127's refresh), `sync.viewer.live-updates` (C joined through the /s/ link and `/edit` did not reach `window.turboslide.studio` in 90 s: the editor load's stall on the blob tier, pass 2 F9's class, ship one's 5.2 on `images.insert.first-on-new-deck`; the cadence was never reached), `share.dialog.co-edit-from-copied-link` (the copied `/s/` address answered 404 in the second browser: the share record just written not yet readable through the edge on the answering instance) and `fonts.preload.italic-on-edit-only` (its own assertion passed, `/decks` one preload and `/edit` two, and the file's `afterAll` failed on the removal of `untitled-20260924-oc8y` not answering 404 in time, the teardown's removal lag of ship one's `logos.picker.chrome-1280`; Playwright charges the file's teardown to its last row). The five skipped are ship one's: `assist.quota.429`, `templates.deck.read-only`, `fonts.picker.specimen-rows`, `logos.index.cached-offline`, `sync.resend.idempotent`. The five "Target page, context or browser has been closed" lines of the log are the brand and decks files' teardowns after their contexts closed, not the walk's browser, which ran to its cleanup. The cost probe started at 21:31:41Z.

The cost probe (21:31:41Z, 1,220 s): 6 rows, 4 passed, 1 failed, 1 not driven, five instances answering. `cost.editor-idle.calls` function 9.65 a minute (ceiling 12), store simple 28 (40), advanced 9 (11); `cost.editor-editing.calls` function 67.78 a minute (ceiling 75; 204 in 3.01 min), store advanced 74 (85), and store simple 155 a minute over its ceiling 140: the samples read head 113 to 115 a minute on the instance holding the tab's stream and its writes (the poll's 2 s tick, the ops route's base checks, the presence route) with get 0 to 6 there, and the last sample, answered by a second instance, head 57 with get 40 (its mirror's pulls of the log the tab's writes landed on the first instance made); the probe sums the highest head and the highest get across the samples (b3.md R7 b), so the 155 is the two instance editing reading, the measure row over by 15 on the second instance's gets, none of them the roster read the hotfix added (that read is a get on the streaming instance, which read 0 to 6). `cost.two-tabs-idle.calls` advanced 18 (30), `list` 0; `cost.show.calls` 0 function requests and the page's store calls 0; `cost.editor-hidden.calls` not driven by its rule; `sync.pull.no-listing` passed (`lists.versions` 0). Every cost row is `measure: true` and never holds the ship; the function counts read no rise against ship one's production reading (idle 9.66, editing 68.53).

The gate's ledger, `features-one-hotfix-preview-run1.json` (8,501 s, 19:30:15Z to 21:52Z): 785 rows, 670 passed, 57 failed, 58 not driven (1 manual), 0 no step; `retries` zero; verdict failed. By FEATURES.md 7.2, read against production's run of record: the two hotfix rows green (`logos.picker.paper-and-ink`, `collab.slide-added-appears`); red in both readings and carried or standing: the parked feature `inbox` (3), the parked feature `templates` (5), the parked rows `view.live-pointers.second-browser` and `versions.show-changes-marks`, Kevin's standing `sync.title.concurrent-both-kept`; red in this run and not on production, all the blob tier's timing: the 41 walk rows and the 41 setup skipped rows of the 19:32Z to 19:39Z window above, `share.dialog.co-edit-from-copied-link`, `sync.viewer.live-updates`, `fonts.preload.italic-on-edit-only`, `export.download.progress-per-slide`, `export.download.large-deck-pptx`, `logos.index.refresh-fixture` and `logos.cache.open-licence-only` (the head lag after the fixture refresh; not driven on production by design), and the measure row `cost.editor-editing.calls`; not driven by design: the ten P1 rows behind their parks, `logos.index.cached-offline`, `assist.quota.429`, `sync.resend.idempotent`, `cost.editor-hidden.calls`, `templates.deck.read-only`, `fonts.picker.specimen-rows` and the manual clipboard row (`brand.objects.kit-colours-first` and `slides.layout.subtitle-prompt` were setup skipped and not on this build). `decks.file.open-list-search`, Kevin's other standing row, passed; the assist rows passed against the fixture. Run 2 decides every row red once here.

The store after run 1 (21:57Z): `deck.list` with the trash read 78 decks (61 live, 17 trashed); eight of today removed through the bearer (`untitled-20260924-ocb4`, `-14l8`, `-oc8y`, `-504y`, `import-bundle-source-mufzv9r8`, `northwind-renewal-v4-without-notes`: trash 200 or already, remove 200, `/edit` 404 each), and two of today refused by `deck.trash` with 400 "is not a deck manifest: /assets/frame-091c1b413302364f/source/backend Invalid option: expected one of angle-metal | swiftshader": `untitled-20260924-hjpo` (created 20:46:16Z) and `untitled-20260924-o4bv` (created 21:53:11Z) carry a shader material frame asset (`paper:liquid-metal`) of ship two's schema, written to the shared store by ship two's runs and unreadable by this deployment's schema; they are another lane's decks and were left alone. The list then read 72 decks (56 live, 16 trashed), the baseline 70 and those two. The templates facet: `core-spec-template-mufmcqwg` and `-mufpn7hx` (the index rows of 19:30Z) deleted at 200 this time, and `-mufzbgvj` and `-mufzdoah` (run 1's own, 20:23Z and 20:24Z) answered 500 "Vercel Blob: Failed to fetch blob: 403 Forbidden" on their delete (the edge's window on the index the two deletes before them had just written); retried after run 2.

### 6.4 Run 2

Started 21:57:57Z from the same tree and deployment (`run-gate.sh 2`, pid 35148, `--matrix docs/gslides-parity/focus/verification/features-one-hotfix-preview-run2.json`); polled every ten minutes.

#### 6.4.1 The first attempt at run 2, stopped

Started 21:57:57Z. The walk's first deck `untitled-20260924-hmy0` was created at 21:57:58Z and its first twelve rows landed; at step 13 (`decks.edit.reload-keeps-slide`, 21:59:38Z) the window API's `slide.new` did not answer within 60 s, the walk's stall rule retired the deck ("Saving…", pending 1, revision 6 against the server's 6) and made `untitled-20260924-6yac`, whose rows 18 to 41 landed and whose setup `slide.remove` at 22:02:09Z stalled the same way; the fresh deck after that, `untitled-20260924-jih6`, stalled on its first `slide.new` at 22:03:33Z and the walk stopped: 618 steps, 39 ok, 7 failed, 529 not driven (`decks.file.open-list-search`, Kevin's standing row, read 56 decks listed after 1,122 ms and failed on the filter; `decks.file.import-slides-deck`'s setup copy answered "Vercel Blob: Failed to fetch blob: 403 Forbidden"), the cleanup trashing the three decks through the actions API (`/edit` and `/deck` 404 each). The runtime log for 21:57Z to 22:07Z (324 distinct entries): `GET /api/decks/<deck>/stream` answered 500 six times at 21:58Z, six at 21:59Z, nine at 22:02Z and four at 22:03Z, each with the stack "Error: Vercel Blob: Failed to fetch blob: 403 Forbidden at get (@vercel/blob)" from the room's open (the stream route throws a store refusal, `decks.$deckId.stream.ts` 95 to 97, unchanged since before the return round: C3S-F4 gave the ops and presence routes their 503 and not the stream); `POST /api/decks/<deck>/ops` answered 503 twelve times at 22:01Z and twice at 22:02Z with the room's "read at revision N after N syncs; the caller asked for N"; the hosting lines "the fresh deck index was not written for <deck>: 403; the folder listing catches it up" (21:59:51Z), "the listing's read of <deck> met 403; the deck is left out of this listing" (8) and "warm: <deck> light: 403" (6) name the same condition: the public store's edge refusing a just created deck's files to every instance but the one that wrote them, for minutes, so a tab whose stream open or write landed elsewhere got no acknowledgement and the walk's stall rule made another fresh deck into the same window. The mechanism is the return round's C3S-F4 class on fresh decks, and `git diff 2be923e 90c54df` touches neither the stream route's error path (its one hunk passes the client id to `subscribe`), `isStoreBusy`, `server/room.ts` nor `packages/store`'s mirror. A walk that stalls out at its start is not a reading, so this attempt was stopped at 22:17:51Z (the process tree of this run alone from the wrapper's pid, SIGTERM then SIGKILL for the Playwright browsers that stayed; the specs had run 30 of 207) and its log and folder are kept beside the run as `gate-run2-stopped-2203Z.log` and `gate-preview-run2-stopped-2203Z/`, never under `docs/`. The store after: `untitled-20260924-svo8` and `-a3ub` (the stopped specs' decks) trashed and deleted forever (`/edit` 404); two more decks of ship two's schema (`untitled-20260924-bvd5` 22:14:08Z, `-j5q7` 22:15:24Z, each with a shader frame asset, `deck.trash` 400 "is not a deck manifest") left alone beside `hjpo` and `o4bv`; 75 decks (59 live, 16 trashed): the baseline 70, ship two's four, and one more of today that a listing a minute later showed beside three others created within seconds of the restarted walk's deck (`untitled-20260924-1d2h`, `-kyc2`, `-8r9h`, none of them this run's): ship two's verifier runs its own gate against the same store at this hour, so from here the sweeps name the decks this run's walk table, spec report and cost probe made, never a created-on-today rule (`svo8` and `a3ub` were removed by that rule at 22:18Z on the strength of their times inside the stopped specs' window, the decks file's rows; recorded as such). The templates facet was not touched before the restart.

#### 6.4.2 Run 2, started again

Started 22:18:13Z from the same tree and deployment (`run-gate.sh 2`, pid 45158); polled every ten minutes.

The walk of the restarted run 2 ended at 23:11Z (622 steps): 572 matrix rows, 604 steps ok, 7 failed, 11 not driven, no stall, 161 console errors (every one a resource load answering 404, the fixture's condition for marks outside its ten and the render route's 404s); the cleanup trashed and deleted its deck `untitled-20260924-2316` and the import copy `import-source-20260924-2316`. The seven: `decks.file.open-list-search` (Kevin's standing row; 62 decks listed after 6,915 ms with another lane's deck first), `versions.show-changes-marks`, `inbox.bell-panel-toggle` and `inbox.settings-persist` (the parked row and the parked feature), `brand.background.enter-keeps-open` (the deck's return read `locator.boundingBox` 30 s; green on production, not driven in run 1's window), `assist.outside-write.snackbar` (the agent's `text.replaceAll` answered 409 "baseRevision 1025 is stale" against the seller's own edit that had just landed, so no snackbar; green in run 1 and on production), and `logos.picker.paper-and-ink`, the first hotfix row, red: at 23:04:48Z to 23:05:08Z the tile's ink half read lit 0.00 percent on `#070707` while the paper half read 38.19 percent with 0.00 white and the inserted mark 50.12 percent on the light deck, the reading production's run of record gave twice (SF.3). `collab.slide-added-appears`, the second hotfix row, is a spec row and is read below.

### 6.5 The ink half by hand, and what it measures

The walk's `halfLit` reads a tile's half 250 ms after it is scrolled into view (`core-walk/areas/logos.mjs` 350 to 375) and waits for no image, so a half whose `<img>` has not answered by then reads 0.00. The platform's runtime log listing drops most `/api/logo` entries (the searches and the two marks of step 562 are absent from a listing of 23:04:30Z to 23:05:15Z that holds 49 other entries), so the mark route's answer to the ink half at 23:04:5xZ cannot be read from it; the mark route carries `cache-control: public, max-age=3600` and the paper half's `light.svg` was a CDN hit by then while the dark variant was not, so the ink half was the one of the two that reached a function. The scratch `ink-drive.mjs` (headless Chromium through the worktree's `playwright-core`, the OIDC header, routing on so the browser cache is off, a `/new` draft that never landed in the store) then drove the row's first half by hand twelve times from 23:21:19Z, recording every `/api/logo` answer and the ink `<img>`'s state at +250 ms to +4 s after the tiles drew: the first two searches (23:21:22Z and 23:21:39Z) met **503 with `retry-after: 5` on every keystroke's request** (twelve answers, `x-vercel-cache: MISS`), the dialog showed the empty state (`dialog.logo.empty` with the route's sentence in place of the "No logo named" line, `Logo.tsx` 971 to 972; `dialog.logo.error` unset; the foot without its date) and no tile drew in 12 s; from 23:21:56Z every search answered 200 (within 30 to 90 ms of the last key), both marks answered 200 (CDN hits, `x-turboslide-logo-cached: 1`) and the ink `<img>` was complete at 256 by 222 by +250 ms in each of the ten searches after. An earlier attempt of the same drive at 23:19:05Z to 23:19:55Z read four searches with no tile in 12 s, and the runtime log for 23:19Z to 23:22Z lists thirteen `503 /api/logo/search` (23:19:40.7Z to 23:19:41.1Z, 23:19:57.7Z to 23:19:58.1Z, 23:20:26.0Z) beside 35 at 200. The 503 is the hotfix's own answer (`storeBusy()` in `logo.$.ts` over `LogoStoreBusyError`): `load()` met a refused read of `system/logo-index.json` and the instance held no disk copy. The index on the public host reads `last-modified` 23:11:38 GMT with the etag and the bytes of the fixture build of 21:09:51Z (7,355 bytes, ten icons, `builtAt` 2026-09-24T21:09:51.721Z): the pathname was rewritten with the same content at 23:11:38Z, after this run's walk had passed the logos area and before its specs reached their logos file, by a refresh this deployment did not make (ship two's runs use the same store and the same ten mark fixture; its decks with shader frame assets were written at 20:46Z, 21:53Z, 22:14Z and 22:15Z), and the public store's edge then refused the pathname to instances of this deployment for minutes, so every instance without a copy answered 503 for the length of the window (25 consecutive answers over 23:19:40Z to 23:21:40Z in the log and the drive together), what the hotfix turned the 500 of SF.3 into and not a served answer. The same condition on an instance answering the dark variant at 23:04:5xZ is the reading of the row; whether that answer was the 503 or a 404 from a copy without the variant cannot be read from the listing, and the row is failed by its measurement either way.

The seam, stated for the next hotfix and never changed here (no code change is allowed between the runs, and the row must read green in both): `createLogoService.load()` has no copy to serve on a cold instance while the edge refuses the index's pathname, and the window follows every write of that pathname, on production the daily refresh and on the shared store any deployment's fixture refresh; a copy that never passes through the edge is needed (the previous build kept under its own pathname and named by a small pointer the refresh flips only after the new file is served, or the build's snapshot bundled with the function), and the dialog's search should retry on `retry-after` before it shows the empty state. On the shared store the fixture previews of every lane rewrite one index, so a preview run's logos rows measure other lanes' refreshes as well as their own; the runs of record of a logos hotfix need a store no other run writes to during them, or a per deployment index pathname under `TURBOSLIDE_LOGO_UPSTREAM=fixture`.

The specs of the restarted run 2 (23:12:05Z, 3,991 s): 192 passed, 11 failed, 4 skipped, 0 flaky, `retries` 0. `collab.slide-added-appears`, the second hotfix row, passed again (12.3 s), with `collab.edit-from-second-browser` (15.0 s); the two logos rows red in run 1 (`logos.index.refresh-fixture`, `logos.cache.open-licence-only`) passed, as did the two export rows, `sync.viewer.live-updates`, `share.dialog.co-edit-from-copied-link` and `fonts.preload.italic-on-edit-only`. The eleven: the parked feature `templates` on three rows (`templates.card.rename-and-delete`, `templates.default.use-for-new`, `templates.gallery.page`; `templates.save.as-template` and `-same-name-replaces` passed this time), the parked rows `view.live-pointers.second-browser` and `inbox.notification-arrives`, Kevin's standing `sync.title.concurrent-both-kept` (round 2 interleaved), and six rows on the blob tier's timing, each green in run 1: `brand.logo.replace-every-slide` (the footer's replaced picture not drawn in 8 s, the asset put's timing, b3.md R21; its file's teardown then failed on `untitled-20260924-sf6o` not answering 404 in time), `fonts.picker.specimen-rows` (its editor did not load in 90 s, the load stall class; the row skips as P1 when the editor loads), `decks.trash.delete-forever-cancel` (`apiRequestContext.get` read `ETIMEDOUT` on `/edit/<id>` from this machine, a network tail), `collab.presence-chips` (the join half passed, both tabs showing the other within 1,304 ms; the leave half failed, the second tab's chip still in the owner's roster 35 s after its tab closed, with two rows of tabs that had left 17 s and 4.6 s before the row already in that roster: the presence record's expiry through the edge, the return round's `collab.*` class; red once on production's run of record too and green on its rerun) and `versions.restore` (the second browser's text not landed within 35 s, the second browser bound of PRODUCT.md, the row's own sentence). The four skipped are ship one's (`assist.quota.429`, `templates.deck.read-only`, `logos.index.cached-offline`, `sync.resend.idempotent`). The cost probe started at 00:17Z.

The cost probe of run 2 (00:18:36Z to 00:38:47Z, five instances): 4 passed, 1 failed, 1 not driven. `cost.editor-idle.calls` function 9.65 a minute (ceiling 12), store simple 32 (40), advanced 10 (11); `cost.editor-editing.calls` function **76.24 a minute over its ceiling 75** (229 requests in 3 min: the tab's presence POSTs 181 at 60.26 a minute against run 1's 159 at 52.83, its ops POSTs 39 against 36, the server function 9 in both; no stream reopen, no failed request), store simple 135 (140), advanced 70 (85); `cost.two-tabs-idle.calls` advanced 18 (30), `list` 0; `cost.show.calls` 0 function requests and the page's store calls 0; `cost.editor-hidden.calls` not driven by its rule; `sync.pull.no-listing` passed (`lists.versions` 0, three `assets` listings). The editing row's function count is raised in run 2 against ship one's production reading of 68.53 and run 1's 67.78, the whole rise in the tab's presence POSTs; the presence route ignores the channel's `set` answer (`decks.$deckId.presence.ts` 118 to 125 answer `ok`, `hueSlot` and `role` from the roster entry) and the hotfix changed nothing the client reads, so the cadence is the tab's own (`PRESENCE_HEARTBEAT_MS` 5,000 and the caret batches of 80 ms under the probe's typing), and the reading is recorded as a measure row over its ceiling once, not attributed.

The gate's ledger, `features-one-hotfix-preview-run2.json` (8,433 s, 22:18:13Z to 00:38:54Z): 785 rows, 750 passed, 19 failed, 16 not driven (1 manual), 0 no step; `retries` zero; verdict failed. The store after the run (00:45Z): `deck.list` with the trash read 91 decks; of the ids this run's walk table, spec report and cost probe name, one was still there (`untitled-20260924-sf6o`, the brand file's teardown failure) and was trashed and deleted forever (`/edit` 404); the walk's two and the cost probe's six were gone by their own cleanups. The list then read 90 decks (71 live, 19 trashed): the baseline 70 and twenty of 24 and 25 September that no artefact of these runs names, created between 20:46Z and 00:44Z while ship two's gate ran on the same store (its own walk, spec and export fixture decks, among them a `northwind-renewal-v4-without-notes` at 23:35:36Z, the fixed id both lanes' export specs use), left alone under the rule of 6.4.1. The templates facet: `core-spec-template-mufzbgvj` and `-mufzdoah` deleted at 200, six more of this run's specs answering 500 "Vercel Blob: Failed to fetch blob: 403 Forbidden" at 00:45Z and the list reading `blank` and `gt-brand` alone a minute later (the folders were gone; the index read after each delete met the edge).

## 7. The two runs classified, and the verdict

By FEATURES.md 7.2 over the two ledgers, read against production's run of record. A row passes when it passes the last run of its origin; run 2 is the last preview run.

- The two hotfix rows. `collab.slide-added-appears`: passed in run 1 and run 2 (12.3 s in run 2), with `collab.edit-from-second-browser` beside it, against failed twice on production; the second seam holds on the preview. `logos.picker.paper-and-ink`: **passed in run 1 (the ink half lit 37.90 percent) and failed in run 2 (0.00 percent)**, the reading production gave twice; the mechanism, measured by hand in 6.5, is the hotfix's own 503 on every logo route of an instance without a copy while the public store's edge refuses the index's pathname after a write of it (the 500 of SF.3 turned into a 503 the tile cannot draw), and the pathname was rewritten during this run by another lane's fixture refresh on the shared store. The row does not read green in both runs.
- Red in both runs, carried or standing, none a blocker: the parked feature `inbox` (`inbox.bell-panel-toggle`, `inbox.settings-persist`, `inbox.notification-arrives`), the parked feature `templates` (`templates.gallery.page`, `templates.card.rename-and-delete`, `templates.default.use-for-new`; `templates.save.as-template` and `-same-name-replaces` red in run 1 alone), the parked rows `view.live-pointers.second-browser` and `versions.show-changes-marks`, Kevin's standing `sync.title.concurrent-both-kept` (never chased); `cost.editor-editing.calls`, a measure row, over a different ceiling in each run (store simple 155 in run 1, function 76.24 in run 2).
- Red once, the blob tier's timing (the return round's measured class and the CDN lag on written pathnames), recorded and not chased: in run 1 the 41 walk rows and 41 setup skipped rows of the 19:32Z window (6.3), `share.dialog.co-edit-from-copied-link`, `sync.viewer.live-updates`, `fonts.preload.italic-on-edit-only`, `export.download.progress-per-slide`, `export.download.large-deck-pptx`, `logos.index.refresh-fixture`, `logos.cache.open-licence-only`; in run 2 `decks.trash.delete-forever-cancel` (a read timeout from this machine), `collab.presence-chips` (the leave half), `versions.restore`, `brand.logo.replace-every-slide`, `brand.background.enter-keeps-open`, `assist.outside-write.snackbar` (the agent's 409 on a base the seller had just moved), `fonts.picker.specimen-rows` (its editor did not load in 90 s), and Kevin's standing `decks.file.open-list-search` (another lane's deck listed first).
- Not driven by design in both runs: the ten P1 rows behind their parks, `logos.index.cached-offline`, `assist.quota.429`, `sync.resend.idempotent`, `cost.editor-hidden.calls`, `templates.deck.read-only`, the manual clipboard row; `fonts.picker.specimen-rows` not driven in run 1 and failed in run 2 as above.
- The cost rows beside their ceilings: idle function 9.65 and 9.65 (ceiling 12; production 9.66), editing function 67.78 and 76.24 (75; production 68.53, raised in run 2 by the tab's presence POSTs), idle store simple 28 and 32 (40), advanced 9 and 10 (11), editing store simple 155 and 135 (140), advanced 74 and 70 (85), two tabs advanced 18 and 18 (30) with `list` 0 and 0, the show 0 and 0.

The verdict: the hotfix stops here and nothing is pushed. `logos.picker.paper-and-ink` is red once on the preview with a mechanism inside the routes the hotfix changed, and the rule this part was given requires both hotfix rows green in both runs; a row red once whose mechanism is not one of the two recorded classes is flaky under FOCUS.md 6.2 until a rerun after a named code change reads it green in both runs. The named change is 6.5's seam (a copy of the index that never passes through the edge, and the dialog retrying on `retry-after`), and the runs of record for it need a store no other lane's fixture refresh writes to during them. The second seam (`collab.slide-added-appears`, the pulse's roster across instances) read green twice and is ready to ship with the first once the first holds. The branch `hotfix/features-one` stays in the worktree at its two commits (90c54df and this note's), unpushed; `origin/main` stays at `2be923e` and production keeps serving `dpl_2UjC4LUq928FvsYFZi4m7U3Ek87w`, whose logo routes answer 500 in the window the hotfix would have made a 503. The production index is still the real build of part C on production's reading of 11:34Z, rewritten on the shared store by the fixture runs since (`builtAt` 21:09:51Z, ten icons at 00:4xZ): production's picker serves ten marks until `POST /api/logo/refresh` through the bearer rebuilds it, which this part did not run because the push did not happen and the rebuild belongs to the part that runs production's gate (the state ship one's part C found and fixed the same way).

## 8. For Kevin

1. The first hotfix row is not fixed by a 503: a cold instance with no disk copy answers 503 to every logo route for the length of the edge's window after any write of `system/logo-index.json`, and the picker shows "The logo index is not readable right now" in its empty state while the tile's ink half stays blank (6.5). The seam is a copy that does not come through the edge (the previous build under its own pathname behind a pointer the refresh flips after the new file is served, or the build's snapshot in the function), plus a retry in the dialog on `retry-after`; until then production's picker has the window after each daily refresh.
2. The shared Blob store carries one `system/logo-index.json` for every deployment, and every lane's fixture preview rewrites it (`logos.index.refresh-fixture`): the two runs of a logos hotfix cannot be read while another lane's gate runs, and two gates on one store also collide on fixed fixture deck ids (`northwind-renewal-v4-without-notes`) and each other's scratch decks. A per deployment index pathname under `TURBOSLIDE_LOGO_UPSTREAM=fixture`, or a store per lane, is needed before the next logos ship.
3. The stream route throws a store refusal as 500 (`decks.$deckId.stream.ts` 95 to 97) where the ops and presence routes answer 503 with `retry-after` (C3S-F4): on a fresh deck whose files the edge refuses to other instances, the tab has no stream, its writes are not acknowledged and the walk's stall rule made three fresh decks into the same window (6.4.1). The same 503 mapping belongs in the stream route.
4. The public store's edge served stale metadata and bodies for `system/*` files for hours in this session (a HEAD of the index at 23:13Z read `last-modified` 14:20 GMT and `age` 31,893 s while the body carried the 21:09Z build), and run 1's window read the deck at revision 22 for five minutes on one instance while its base moved to 25 (6.3): the CDN lag class is longer than the two to three minutes the notes assume.
5. The walk's `halfLit` reads a tile's half 250 ms after it scrolls into view and waits for no image (`core-walk/areas/logos.mjs` 350 to 375), so the row measures the mark route's latency as much as its answer; `logos.spec.ts` 449 throws its own `TypeError` when `acme` is missing from the search (`JSON.stringify(undefined).slice`). Both are the drivers lane's.
6. Ship two's runs wrote four decks with a shader frame asset of its schema to the shared store (`hjpo`, `o4bv`, `bvd5`, `j5q7`), which this deployment's `deck.trash` and `deck.info` refuse with 400 "is not a deck manifest"; they are ship two's to remove.
7. The editing cost row read 76.24 function requests a minute in run 2 (the tab's presence POSTs 181 in 3 min against 159), over the ceiling of 75 for the first time in any run; the cadence is the tab's own and not the hotfix's, and the row is a measure row, but the ceiling is one edit per 5 s from the presence batches and the number is worth a look when the shader library's editing rows land.
8. The production deployment id the pipeline recorded at 08:05 PDT (`dpl_441AUoH4Dmo4DBsKWsVbmBCRJp65`) was replaced at 07:59 PDT by the Git deployment of the production table commit (`dpl_2UjC4LUq928FvsYFZi4m7U3Ek87w`, `turboslide-1iakke242`); the sources are `9d21394`'s.

## 9. The snapshot part: a copy of the index that never passes through the edge

Written by the hotfix agent, the snapshot part, as it went, from 01:00Z on 2026-09-25 (18:00 PDT on 2026-09-24). The state read at the start: the worktree on `hotfix/features-one` at `6152b0a`, clean; `origin/main` at `bb9c28a` (the chrome fix after the production table; no file of this hotfix); production serving `bb9c28a`'s Git deployment under shadow authorization with no `ANTHROPIC_API_KEY`; the shared store's `system/logo-index.json` holding the fixture's ten marks (`builtAt` 2026-09-25T00:02:29Z, 7,696 bytes, twelve cached marks; the CDN's `last-modified` still 23:11:38 GMT with `age` 6,522 s while the body carried the later build, the stale metadata class of 8.4); the OIDC copies in the worktree and the main checkout identical (12:24 PDT, expiring 2026-09-25T07:24Z); no lock folder, port 4419 free. `docs/VECTOR.md` is not on `origin/main`, so the classification of section 12 reads FEATURES.md 7.2 with the rules the task gave. The rules of this note's head hold throughout.

### 9.1 The seam, and what landed

The mechanism of 6.5: `createLogoService.load()` had nothing to serve on a cold instance while the public store's edge refused the index's pathname, and the window follows every write of it (production's daily refresh; on the shared store every lane's fixture refresh), so the first hotfix's 503 with `retry-after` stood for the whole window on every logo route of an instance without a copy. What landed, in `apps/studio/src/server/logo-index.ts`, `logos.ts`, the new `logo-index-snapshot.ts`, `packages/chrome/src/logo-model.ts` and `dialogs/Logo.tsx`, with `scripts/build-logo-index-snapshot.mjs` and the committed `apps/studio/src/server/logo-index.snapshot.json`; nothing in `packages/store`, the route file `logo.$.ts` (it passes the search answer through as before), the schema or the generated contracts changed:

- The bundled snapshot. `logo-index-snapshot.ts` reads `logo-index.snapshot.json` as text through Vite's `?raw` import (the pattern `contracts.ts` uses for `describe.json`), parses it once per process on the first call and never at module load, strips `lastError`, and answers a fresh copy on every call so a holder's discoveries never write into the module's copy; `logoService()` binds it as the service's `snapshot`. The plain file weighs 3,649,286 bytes, under the 4 MB line the task set, so the gzip path was not taken (the script refuses a file over the line with the size named, so the choice is a decision at the next ship and never a surprise). `.prettierignore` names the file: one line of canonical JSON the formatter would reflow into twice the bytes.
- The load order, in `load()`: the copy this instance holds in memory (as before); else its disk mirror, which now carries the store version it was read or written at (`readMirror` answers `{ index, version }`; the Blob store's mirror folder keeps `index.version` beside `index.json`, written from the get's entry and the put's entry, the strong etag `head`, `list` and `put` share), so one head decides: at the store's version the mirror stands with no get of the 3.6 MB file, with the head refused it stands with the version unknown, and with the version moved the store's copy is read when the store answers and the mirror stands otherwise; else the store's copy when the store answers, one get and one head as before; else the snapshot, with its `builtAt` recorded on the answer; and with none of those the typed `LogoStoreBusyError` as before, which only a build without the file can reach. A store that holds no index yet (before its first refresh) serves the snapshot too, and builds and writes the fixture's inline under that upstream as before (4.9).
- `snapshotAt`. `LogoIndexFacts` (chrome, shared with the server) gains the optional field; the service's `facts()` sets it while the held copy is the snapshot's, `search()` carries it on its answer, and `LogoService.snapshotAt()` reads it. The `logo.search` action's output is a strict object of the table and names no snapshot, and the dispatcher validates outputs (`dispatch.ts` 88), so the handler in `registerLogoActions` strips the field from the action's answer and the route's JSON alone carries it to the dialog (`serveSearch` passes the service's answer through). The generated contracts therefore did not move and `generate:contracts` was not run.
- The revalidation. An instance on the snapshot asks the store again at most every `SNAPSHOT_REVALIDATE_MS` (30 s) on a request, in place of the hourly `INDEX_REVALIDATE_MS`, and `revalidate()`, the refresh's `onWritten` adoption and `adopt()` clear `snapshotAt` when the store's copy or the refresh's index replaces it. A refresh that lands writes the new index and the instance's held copy at once, as the first hotfix made it.
- The fixture upstream. Under `TURBOSLIDE_LOGO_UPSTREAM=fixture` the snapshot is the ten marks built in memory through `refreshLogoIndex` over `memoryLogoStore()` once per instance (the deployment's rasterizer, the fixture's marks, no network), never written to the store, so a cold fixture instance in the window opens no window of its own on the shared index; its `builtAt` is left null so a search that names a refresh's build (`since`) always asks the store, and `logos.index.refresh-fixture`'s driver already tolerates a first refresh that reports the drop (`logos.spec.ts` 462 to 474).
- The mark route. Unchanged in the route file: on a snapshot held, a mark the snapshot lists as cached whose file the store withholds is answered from the upstream with no write (the first hotfix's `withheld`), a mark it does not list is fetched, sanitized and written, and a mark the upstream does not serve answers the upstream's 503 with its sentence; the store's own 503 stays for the refresh, which needs the store itself (`refreshLogoIndex` reads the previous index through it).
- The picker. `searchLogos` throws a `LogoRouteError` carrying the status and, for a 503, the seconds its `retry-after` named (bounded to 10, 5 when absent); the search effect asks once more after that many seconds with the last results still drawn and the search still on, drops the retry when a later keystroke moved the sequence, and reads the empty state with the route's sentence only when the retry fails too. `LOGO_WORDS.source` reads "Logos from thesvg.org as of <date>" while `snapshotAt` is set, the sentence the walk's foot check accepts (`logos.mjs` 732 to 734: thesvg.org and a date), and the dialog carries the field from the route's answer to its facts.
- The script. `node scripts/build-logo-index-snapshot.mjs --store <public store host>` reads the store's index through its public host and writes it when it is a real build (a thousand icons or more, `updatedAt` set, no `progress`); when the store holds none or a fixture's it builds from thesvg.org's `icons.json` through `refreshLogoIndex` over the network upstream and sharp, the same reads pass and flags the refresh makes, in rounds until no `progress` remains; `--upstream` forces the build, `--check` reads the committed file back with its facts, `--dry` proves the builder imports. The builder is loaded through Vite's module runner from `apps/studio` (`createServer` with no config file and `createServerModuleRunner` over the ssr environment), because the server sources use extensionless relative imports Node's type stripping cannot follow and no `tsx` or `vite-node` binary is installed; `sharp` is externalized as the app's own config keeps it.

### 9.2 The snapshot built

Run at 01:03Z with `--store ggmycvj7j6224ay5.public.blob.vercel-storage.com`: the store held the fixture's ten marks (the line "the store holds no real build (...): the ten mark fixture of a fixture preview, or a partial build); building from icons.json"), so the snapshot was built from thesvg.org's `icons.json` directly through the builder, as the task said to do and to say. One round: 7,422 icons, 4,686 in the brands collection, 4,942 rows flagged (the brand and community rows `isBrandCollection` counts), no cached marks, `updatedAt` and `builtAt` 2026-09-25T01:15:23.308Z, no `lastError`, no `progress`; written at 01:15Z, 3,649,286 bytes. The line about the rebuild at each ship is in `docs/FEATURES.md` 4.2, the index section (the task named 4.4, which is the insert), and a row of the preconditions table of `ship.md` section 2, marked as added by this hotfix for the ships after.

### 9.3 The checks

- `node_modules/.bin/tsc -b` exits 0 at 01:12Z before the tests were written and again at 01:19Z after them.
- `apps/studio/src/server/logos.test.ts` gains, in the busy window describe, the bundled snapshot on a cold instance with no copy (the store refused: the answer from the snapshot with its `builtAt` as `snapshotAt`, the foot's sentence, the shorter interval, the adoption on the next revalidation with the field gone); the snapshot on a store that holds no index yet and the typed 503 for a build with no snapshot; the fixture's ten marks built in memory with no put and `builtAt` null, then adopted by `since` after another instance's refresh took the dropped slug down (the other instance's fixture upstream bound to its held index, as `logoService()` binds it); a refresh replacing the held snapshot from its own hand; the mark route on a snapshot (the fixture serving and writing, the down upstream answering its own 503 and no put while the store withholds a cached file); the mirror served at the store's version with one head and no get, read when the version moved, kept with the version unknown when the head is refused and then replaced at the revalidation; the action's answer without the field beside the service's with it. The first hotfix's "answers 503 with a retry ... on a cold instance" case became "answers the fixture's ten marks from memory ... (the typed 503 stays for a build with no snapshot)", since that is what the cold fixture instance does now, and its mirror half asserts the zero gets. `logo-index-snapshot.test.ts` (new) reads the committed file: a complete build of thousands of marks with its flags, under 4 MB, `builtAt` and `updatedAt` strings, no `lastError`, the Vercel and Figma rows, and a fresh copy per call.
- `packages/chrome/src/__tests__/logo-dialog.test.tsx` gains the retry (200, then a 503 with `retry-after: 1` on the next query: the Figma tile stays drawn, the search stays on, no empty state, one retry lands within the second and draws; then a 503 twice: the empty state with the route's sentence and no tile, five fetches in all) and the foot naming the snapshot's date with no "updated".
- `vitest run` at 01:17Z to 01:18Z: `apps/studio` 72 files, 548 passed (539 before, and the nine new); `packages/chrome` 76 files, 724 passed and 2 todo. `packages/store` and `packages/realtime` were not touched by this part.
- `prettier --write` over the changed sources, tests, the script and the two documents; `node scripts/check.mjs --greps` clean (27 `innerHTML` call sites and 26 `overwrite: true` in the allowlist, as before; the mirror's version file is a plain write).
- The local smoke, the worktree's dev server on 4419 (`TURBOSLIDE_STORE=tmp`, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, `TURBOSLIDE_LOGO_UPSTREAM=fixture`, the two secrets minted in the command), 01:20Z to 01:22Z, stopped after: `GET /api/logo/search?q=figma` 200 with Figma first, `GET /api/logo/mark/vercel/dark.svg` 200 `image/svg+xml`, `POST /api/logo/refresh { dryRun: true }` 200 (10 icons, upstream fixture); the server booted with the 3.6 MB `?raw` import on the logo module's graph, which is what the smoke was for.

### 9.4 The commit

`a90fd7d` on `hotfix/features-one`, by an explicit path list (the three sources and the new module, the snapshot file, the script, the three tests, `.prettierignore`, `docs/FEATURES.md`, `ship.md`, this note; never `.github/`, never a file carrying a token: the paths were scanned for JWT, bearer and long token shapes and the hits were comment rulers; the snapshot file holds no token shape and no store host), authored as the repository's identity, the message file written to the scratch folder, with the trailer. `ship.md`'s diff is the one row and the table's columns as prettier re aligned them. The tree clean after.

## 10. The preview of the snapshot part, its first reads and the hand drives

### 10.1 The deployment

The recorded command (`deploy-preview-hotfix.sh`, the worktree as the working directory: `vercel deploy --yes --archive=tgz` with `TURBOSLIDE_MAIL=off`, `TURBOSLIDE_AUTHORIZE=enforce`, `TURBOSLIDE_ASSIST=fixture`, `TURBOSLIDE_LOGO_UPSTREAM=fixture`, `TURBOSLIDE_REALTIME=blob`, `TURBOSLIDE_PUBLIC_STORE_HOST`, the two secrets minted inside the command; never `--prod`, no project setting, no env command), 01:22Z to 01:25Z from `a90fd7d`: `https://turboslide-aegtdhyfy-kl01s-projects.vercel.app`, `dpl_EDKu2FprEu5i5fAUhKaNbX8T6ryQ`, target preview, Ready, behind Vercel Authentication, the one Blob store production uses.

### 10.2 The first reads

- `node scripts/hosted-smoke.mjs --base <preview> --token-env TURBOSLIDE_TOKEN` through `with-tokens.mjs`, 01:26Z: 40 of 40 (the enforce rows as enforce; `deck.info` 1 snapshot at revision 26; the effects backend typescript; `/decks` 71 cards).
- `scripts/probes/new-write-probe.mjs` against the preview, 01:26Z to 01:27Z, from an untracked copy inside the worktree's `scripts/probes/` with its `createRequire` path pointed at the worktree (the committed file names the shared checkout's `package.json`), deleted after with the tree clean: all steps ok (the twelve edits each acknowledged at the next revision, the reload, the second tab listing only the other, the trash and the delete forever of `untitled-20260925-ccvr`).
- The logo routes on the preview at 01:27Z (a scratch reader through `with-tokens.mjs`): four `GET /api/logo/search?q=vercel` at 200 in 107 to 334 ms, one row, `indexed` 9, `updatedAt` 2026-09-25T00:02:29Z and no `snapshotAt` (the store answered, so the held copy was the store's fixture build); `GET /api/logo/mark/vercel/dark.svg` 200 in 211 ms, `x-turboslide-logo-cached: 1`; the dry run 200 (10 icons, 12 cached marks, upstream fixture, credential agent).

### 10.3 The two rows by hand, three times each

Both drives are scratch scripts under `hotfix-b/` (`collab-drive.mjs`, `ink-drive.mjs`), headless Chromium from the worktree's `playwright-core` with the OIDC header, through `with-tokens.mjs`; every deck they made was trashed and deleted forever through the bearer as `remove-deck.mjs` does it (the listed revision, `deck.trash`, `deck.remove` with `confirm`, then `/edit` read and answering 404), and the note says so per deck. A first attempt of each script met the window API's `slide.new` wanting its `layout` and `baseRevision`, and a first cleanup passed no `baseRevision` (400 both); the draft is landed by a click on New slide and the cleanup follows the removal script, and `untitled-20260925-gx7c` of the first collab drive was removed through `remove-deck.mjs` after (trash 200, remove 200, `/edit` 404). No instance header exists on any logo route; the `x-vercel-id` tail's first token repeats across requests one instance answers (`pg8v5` and `f5b9h` in two attempts a minute apart), so the ink drive counts instances by it and the count is recorded as that reading.

- `collab.slide-added-appears`, three drives, each on a deck of its own (`untitled-20260925-gx7c` 01:28Z, `-cq6a` 01:30Z, `-o73z` 01:30Z): context A opens `/new` and lands the draft with New slide; context B, the same person's cookies (the sync round's B), opens `/edit/<id>` and landed 200 on its first load each time (427 to 460 ms); then three rounds in both directions, New slide clicked in one context and the other read by `slide.list` and the filmstrip's cards every 100 ms. The seconds: drive 1, B to A 2.49, A to B 0.42, B to A 1.24, A to B 2.58, B to A 0.41, A to B 1.34; drive 2, 2.28, 0.52, 1.24, 1.44, 1.86, 1.24; drive 3, 3.00, 2.06, 2.48, 1.85, 0.62, 2.06. Eighteen of eighteen within 5 s, the slowest 3.00 s, twelve of them at the active tick's 2 s or under; each context's roster listed the other by the end (`others` 1 on both). The instances the two streams landed on cannot be read from the product, as 5.3 recorded.
- `logos.picker.paper-and-ink`, three rounds (01:30:57Z, 01:31:14Z, 01:31:31Z), each opened by a refresh through `POST /api/logo/refresh` with the bearer (200 in 275 to 667 ms; the fixture's build written to the shared index each time, `builtAt` 01:30:57Z, 01:31:14Z and 01:31:31Z, the takedown of `northwind` reported in rounds 1 and 3 and the slug re listed in round 2, the fixture's alternation), so the public store's edge had the pathname's window open under every attempt; then, in a fresh browser context each time (routing on, so the browser cache is off; the draft landed with New slide, a deck of its own per attempt), Insert > Logo and "vercel" typed at 70 ms a key, until two instances had answered the search route, with every `/api/logo` answer recorded and the Vercel tile's two imgs read at +250 to +4,000 ms. Six attempts (two per round, `untitled-20260925-g0m9`, `-sf1k`, `-hblh`, `-hpf0`, `-yz04`, `-slc5`): the three tiles drew 23 to 78 ms after the last key in every attempt; 48 answers of `/api/logo` in all, every one 200 (the six searches and the two marks of each attempt), no 404, no 503, no `retry-after`, nothing at or above 500; the ink img complete at 256 by 222 and the paper img the same in every attempt at every sample; no empty state and no error line; the foot "Logos from thesvg.org, updated 25 September 2026" in every attempt (the store's copy answered every search, so no attempt met an instance holding the snapshot and none named it); five, five, three, five, five and two distinct instances answered the six attempts' searches. Read against 6.5, where two searches in a row met twelve 503s on a cold instance in the window: no attempt of these six met the window's refusal on any instance, so the reading is the row green by hand with the window open behind it, and the snapshot path itself is exercised by the unit tests of 9.3 and not by these drives.

### 10.4 The gate on the preview

Run 3 started 01:32:34Z from the worktree (`run-gate.sh 3`, pid 67344, `--out hotfix-b/gate-preview-run3`, `--shots`, `--parked docs/gslides-parity/focus/ship-f1afe1e.json`, `--matrix docs/gslides-parity/focus/verification/features-one-hotfix-preview-run3.json`), both tokens through `with-tokens.mjs`, detached, its log polled at most every ten minutes; one run, as the task set for the day.

## 11. Run 3 on the preview, read and classified

### 11.1 The run

The walk ended at 02:23Z (3,072 s, 622 steps): 572 matrix rows, 556 passed, 5 failed, 11 not driven, 0 no step; 115 console errors (resource loads answering 404: the fixture's condition for marks outside its ten and the render route's 404s); no stall, no retired deck; the cleanup row trashed and deleted its deck `untitled-20260925-mb0w` and the import copy (`/edit` and `/deck` 404). The two hotfix rows read green: `logos.picker.paper-and-ink` (step 562: the ink half lit 37.88 percent on `#070707`, the paper half 38.19 percent with 0.00 percent white, the inserted mark 50.12 percent on the light deck) and `logos.picker.search` (one tile 111 ms after the last key, Figma inserted, the foot "Logos from thesvg.org, updated 25 September 2026"). The five: Kevin's standing `decks.file.open-list-search` (72 decks listed after 9,148 ms with another lane's deck first), the parked row `versions.show-changes-marks`, the parked feature `inbox` (`inbox.bell-panel-toggle`, `inbox.settings-persist`), and `decks.editor.move-to-trash` (step 621, `page.waitForURL` 20 s: the address did not move to `/decks` after the trash; green in run 2 and on production; the trash's write and the listing after it on the blob tier, a deck route this hotfix does not touch).

The specs (02:23:47Z, 3,816 s): 192 passed, 10 failed, 5 skipped, 0 flaky, `retries` 0. `collab.slide-added-appears`, the second hotfix row, passed (spec 173), with `collab.edit-from-second-browser` and `collab.presence-chips`; every logos spec on the build passed (`logos.picker.recents`, `logos.index.refresh-dry-run`, `logos.index.refresh-fixture`, `logos.picker.chrome-1280`, `logos.cache.open-licence-only`); the ten, from `specs.json`: the parked feature `templates` on three rows (`templates.card.rename-and-delete`, `templates.default.use-for-new`, `templates.gallery.page`), the parked rows `view.live-pointers.second-browser` and `inbox.notification-arrives`, and five on the shared store and the blob tier's timing, each green in run 2 and on production: `decks.file.make-a-copy` (the copy dialog "decks/northwind-renewal-v4-without-notes exists already; pick another name": the fixed fixture id both lanes' export specs use was in the store from another lane's run at that minute, the collision 8.2 named; the id is not in the store now), `decks.trash.editor-undo-snackbar` (the snackbar with Undo drew after 127 ms and the card did not return to the listing within 10 s of Undo: the listing through the edge), `images.insert.upload` (the uploaded picture not counted on the slide within 5 s: the asset put's timing, b3.md R21), `share.dialog.co-edit-from-copied-link` (the first browser read the six words 11,354 ms after they were typed against the bound of 10,000: the second browser bound of PRODUCT.md) and `sync.reload.same-document` (the third browser's `/edit` did not reach `window.turboslide.studio` in 90 s after joining through the `/s/` link: the editor load's stall on the blob tier, pass 2 F9's class). The five skipped are ship one's (`assist.quota.429`, `templates.deck.read-only`, `fonts.picker.specimen-rows`, `logos.index.cached-offline`, `sync.resend.idempotent`).

The cost probe (03:28Z to 03:58Z, five states): 3 passed, 2 failed, 1 not driven. `cost.two-tabs-idle.calls` passed (advanced 18 against 30, `list` 0), `cost.show.calls` passed (0 function requests, the page's store calls 0), `sync.pull.no-listing` passed (`lists.versions` 0, one `assets` listing), `cost.editor-hidden.calls` not driven by its rule. The two failed on "a tab lost its stream during the window" (`connected` false), with their function counts under the ceilings: `cost.editor-idle.calls` 10.32 a minute (ceiling 12; 31 in 3 min; the store's simple 39 and advanced 10 over the window against 40 and 11) and `cost.editor-editing.calls` 15.32 (ceiling 75; 46 in 3 min). Their mechanisms, from the probes' request lists and the preview's runtime log fetched a minute at a time (read only, `hotfix-b/runtime-cost-windows.jsonl`):

- The idle row's deck `untitled-20260925-m8kl` was pushed at 03:28:49Z ("pushed 10 files in 293 ms"), its stream opened at 03:28:50Z and held 186 s, its presence POSTs answered 200 every 5 to 10 s and its ops 200; at 03:31:55Z the presence route logged "the deck is gone; its presence is dropped", at 03:32:00Z the stream's poll "stopped, the deck is gone: No deck untitled-20260925-m8kl in the Blob store" and at 03:32:01Z a stream open "watching failed: No deck ... in the Blob store", so the tab's stream closed (the save state read "Reconnecting…"), the last `sync.status` sample answered 404 and `/edit/<id>` 404 at 03:32:02Z. The store answered "no deck" for a deck it had held three minutes before, on the instances of three routes at once: either an actor outside this run removed it (another lane's gate was writing the shared store at that hour, its walk decks `untitled-20260925-06me` created 03:36Z and `-v3i5` 04:08Z still updating at 04:14Z, and a created-on-today sweep is the hazard 6.4.1 named) or the store's not-found on a fresh deck's manifest; the probe's teardown found the deck gone and it is not listed now. The room's "gone" path and the presence push are `packages/realtime` and `packages/store` code neither part of this hotfix touched (part A changed the poll's cadence and the roster's own-row rule, not the manifest read), and runs 1 and 2 on the same realtime code read the row connected.
- The editing row's tab never had a room: its deck `untitled-20260925-lwhi` was never pushed (no push line, no stream, presence or ops request in the whole window, `sync.status` 404 every 30 s, the deck facts reading tier memory), while its 49 draft writes through the server function `_serverFn/a98ce574…` every 5 s each answered 200 and the draft's revision reached 49; the same function pushed the idle row's deck on its first call fifteen minutes earlier and the write probe's at 01:26Z, and every other deck of this run landed. The draft's first write path (`createStoredDeck`, the server functions) is untouched by both parts of this hotfix; the reading is one occurrence in the five cost probe runs on this hotfix's previews, recorded for Kevin's list (section 13, item 9), and the row's function count is the draft's writes alone, not a reading of the editing cost.

The gate's ledger, `features-one-hotfix-preview-run3.json` (8,897 s, 01:32:34Z to 04:00:51Z): 785 rows, 751 passed, 17 failed, 17 not driven (1 manual), 0 no step; `retries` zero; the gate's own verdict failed on the parked rows and the rows above.

### 11.2 The store after the run

`deck.list` with the trash at 04:15Z read 75 decks (59 live, 16 trashed): none of the sixteen ids this run's walk table, spec report and cost probe name (`untitled-20260925-mb0w`, `import-source-20260925-mb0w`, `-y9nz`, `-0dzj`, `-2vw4`, `-4xio`, `-h1g3`, `-me3e`, `-u4gk`, `-wnu9`, `-yvcu`, `-z5z4`, `-m8kl`, `-lwhi`, `gt-brand-mugcx3pg`, `northwind-renewal-v4-without-notes`) was there, every one removed by its driver's own cleanup, so no deck sweep was made; the five decks of 2026-09-25 listed (`-v8t9` 00:09Z, `-pc8u` 00:25Z, `-3gb4` 03:17Z, `-06me` 03:36Z, `-v3i5` 04:08Z) are other lanes' and were left alone under the rule of 6.4.1. The templates facet listed ten: `blank`, `gt-brand`, the six `core-spec-template-*` rows of part A's run 2 (22:08Z to 23:21Z on 2026-09-24, whose deletes met the edge at 00:45Z and whose index rows were listed again), this run's `core-spec-template-mugc8fi9` (02:24Z, the row `templates.card.rename-and-delete` names) and `-mugax174` (01:47Z, named by this run's report only inside the gallery listing's annotation and by no driver of this run, so another lane's and left alone). `template.delete` with `confirm: true` on the seven: two at 200, five at 500 "Vercel Blob: Failed to fetch blob: 403 Forbidden" (the edge's window on the index the first two had just rewritten, the reading of 6.4.2), and the list a minute later read `blank`, `gt-brand` and `mugax174` alone.

### 11.3 The rows classified

By FEATURES.md 7.2 with the rules the task gave (`docs/VECTOR.md` is not on `origin/main`), over run 3 read against run 2 and production's run of record:

- The two hotfix rows, green in the run and in the three hand drives each: `logos.picker.paper-and-ink` (the run's ink half 37.88 percent; by hand six attempts over three refresh windows with 48 logo answers at 200, every tile drawn on both grounds, no 503 and no empty state; against 0.00 percent twice on production and once in run 2) and `collab.slide-added-appears` (passed in the run; by hand eighteen of eighteen within 5 s, the slowest 3.00 s; against failed twice on production).
- Red with a mechanism this hotfix could have caused (the logo routes, the snapshot, the pulse, presence, the stream's client id): none. Every logo route answer of the run and the drives read 200 or the fixture's 404, no `retry-after` was seen, and no row's reason names the pulse, presence or the stream's open; the two cost rows' stream loss is the store answering "no deck" and a draft never landing (11.1), outside both parts' files.
- Red in both readings, carried or standing, none a blocker: the parked feature `inbox` (3), the parked feature `templates` (3), the parked rows `view.live-pointers.second-browser` and `versions.show-changes-marks`, and Kevin's standing `decks.file.open-list-search` (another lane's deck listed first, never chased); Kevin's other standing row, `sync.title.concurrent-both-kept`, passed this run.
- Red once, recorded and not chased: the blob tier's timing (`decks.editor.move-to-trash`, `decks.trash.editor-undo-snackbar`, `images.insert.upload`, `share.dialog.co-edit-from-copied-link`, `sync.reload.same-document`) and the shared store's collision on the fixed fixture id (`decks.file.make-a-copy`); the two cost measure rows (`cost.editor-idle.calls`, `cost.editor-editing.calls`) red on the stream loss with their function counts under the ceilings, measure rows that never hold the ship.
- Not driven by design, as in ship one's table: the twelve P1 rows behind their parks (`tables.seam.row-drag`, `tables.edge.add-row-column`, `tables.heads.select-row-column`, `tables.bar.row-column-buttons`, `brand.objects.kit-colours-first`, `arrange.group.tail-text-controls`, `wordart.tail.fill-outline`, `fonts.picker.specimen-rows`, `fonts.table.takes-family`, `logos.picker.variants`, `logos.kit.find-a-logo`, `templates.deck.read-only`), `logos.index.cached-offline` (the fixture preview), `assist.quota.429`, `sync.resend.idempotent`, `cost.editor-hidden.calls` and the manual clipboard row.
- The cost rows beside their ceilings: idle function 10.32 (ceiling 12; run 2 9.65, production 9.66), store simple 39 (40) and advanced 10 (11); editing function 15.32 (75; run 2 76.24, production 68.53; the draft's writes alone, 11.1), store 0 (no room); two tabs advanced 18 (30) with `list` 0; the show 0 function requests and 0 store calls; `sync.pull.no-listing` `lists.versions` 0.

The environment's rows read as ship one's table: the assist rows against the fixture passed, `assist.quota.429` not driven, the enforce only share halves as enforce.

## 12. The verdict of this part

Nothing stops: the two hotfix rows read green in the one run and in the three hand drives each, and no red row of the default view has a mechanism on the paths this hotfix touched. The rest of this part: the note and the ledger committed, the branch rebased on `origin/main` (`bb9c28a`: four files, `core-matrix.json`, `MenuBar.css`, `Overlay.css` and the chrome walk area, none of them a file of this branch, so the rebase changes no source beyond what each commit carries and no further preview is needed by the task's rule), the checks after the rebase, the push, production polled until the deployment of the push is Ready, and the hosted smoke on production. The production index still holds the fixture's ten marks (the fixture refreshes of every lane's previews, 9.2): production's picker serves ten marks until `POST /api/logo/refresh` through the bearer rebuilds it, which the production part runs before its gate, as ship one's part C did; a cold production instance in the window after that rebuild serves the bundled snapshot of 01:15Z in place of the 503.

## 13. For Kevin, from the snapshot part

9. The editing cost row's tab wrote 49 draft commits at 5 s through `_serverFn/a98ce574…` for three minutes and its deck never landed in the store (no push, no room, `sync.status` 404 throughout), while the same function pushed the idle row's deck on its first call fifteen minutes earlier and the write probe's an hour before: one occurrence in five cost probe runs on this hotfix's previews, on the draft's first write path, which this hotfix does not touch; a driver that reads the address after the first write (`/edit/<id>`, as the write probe does) would name the moment it happens.
10. The idle cost row's deck was answered "no deck in the Blob store" by three routes at once three minutes after its push, while another lane's gate wrote the shared store (11.1): with two gates on one store, a created-on-today sweep by either removes the other's fresh decks mid-row; a store per lane, or sweeps by the run's own artefact ids alone (the rule 6.4.1 set for this hotfix), is the only cover until then.
11. `template.delete` answers 500 "Failed to fetch blob: 403 Forbidden" for every delete after the first two rewrite the templates index (11.2, 6.4.2): the same edge window the logo index had, on `decks/templates/templates.json`; the delete removes the folder and the 500 is the index read after it. The index route's mapping of a store refusal to 503 with `retry-after` (C3S-F4) belongs on the template actions too.
12. The bundled snapshot is 3.65 MB of the function bundle and is parsed once per instance on the first refused read of the index, never at start; the file grows with thesvg.org (7,422 icons today against the 4,945 brand marks of ship one's coverage number, since the snapshot carries every collection the index does) and the script refuses a plain file over 4 MB, at which point the gzip path (a `.json.gz` the module inflates) is the decision at that ship.
