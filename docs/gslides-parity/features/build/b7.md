# B7, the server and the exports

## Features round, ship one

Builder B7 of the features round, ship one (docs/FEATURES.md sections 3.4, 4.7, 4.8 and 4.11, the lane table of section 6, the rows of 7.1), on the shared checkout at `88b68e7` on 2026-09-22 while the other lanes landed beside it (the tree carried B2's fonts, B3's editor, B4's matrix, B1's dialog and B6's routes as this lane worked; every file below is B7's alone). The dev server was port 4417 (`TURBOSLIDE_STORE=tmp` under a private `TMPDIR` in the scratchpad, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, two 48 character secrets from `openssl rand -hex 24`; started once with `TURBOSLIDE_SVG_RASTER` unset and once with it set to `1`), stopped before the return. Playwright (playwright-core, Chrome for Testing 1217) ran against 4417 under `.turboslide/e2e.lock` (taken with the `until mkdir` loop, released by the runner's trap; no lock is held at the return). No git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran. Every scratch deck was made on the tmp store of 4417, trashed and removed through the window API at the end of its drive, and left with the store when the server stopped; nothing was created on a deployment. No token, cookie or secret was printed. The temporary diagnostic test this lane wrote under `packages/export/src/scene/` was removed before the return.

### 1. The items, their files and their hunks

Each item is one commit's worth; a red row's item reverts alone. The hunks are named by the symbol they add or change.

| Item                                                        | Files and hunks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Rows                                                                                                         | Unit tests                                                                                                                                                                                                                                                                                                           |
| ----------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 4.8 the logo role at 3x in the exports                      | `packages/export/src/scene/extract.ts`: `THREE_X_KINDS` gains `logo`; the header comment; `tagKitLogoElements` (new: the kit's footer and title slot pictures tagged as `mark` rasters with rids past `tagRasterElements`'s, the two images' decode awaited first); in `extractScenes`, `kitLogo` and the flatten `shotScales` (`[2, 3]` when the kit draws a picture logo), the kit tags after `tagRasterElements` on every page, `logoBlocks` per slide, the filter that drops an undrawn kit raster, the flatten `wantRasters` (the wordmark and the kit logos), `scaleKind` before `rasterScaleFor`; the re export of the three readers. `packages/export/src/scene/kit-logos.ts` (new, schema imports alone): `KIT_LOGO_BLOCK_IDS`, `kitHasPictureLogo`, `logoBlockIds`. `packages/export/src/pptx/build.ts`: the import and the flatten overlay loop after the sheet raster (the kit logo rasters placed at their boxes through `addRaster`, the wordmark on the master being the precedent) | `logos.export.pdf-pptx-crisp`                                                                                | `packages/export/src/scene/kit-logos.test.ts` (new, 5 tests): `logo` is a 3x kind under auto and the forced policies and the 1x types hold; `logoBlockIds` reads picture and shot blocks through composites and never a missing asset; an opener's plate; `kitHasPictureLogo` on the six kit shapes; the two kit ids |
| 4.7 the seller's svg sentence                               | `apps/studio/src/server/upload.ts`: `UPLOAD_REASONS.notSvg` ("SVG files are not accepted yet. Export the logo as a PNG and upload that"), `isSvgRefusal`, `uploadFailureReason` mapping `not_svg`; the header comment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `logos.intake.svg-sentence` (the server half; the chrome half is request R2)                                 | `upload.test.ts` "names the svg sentence of the features round as a whole sentence, and reads the intake's line"                                                                                                                                                                                                     |
| 4.7, 4.9 the two deployment variables                       | `apps/studio/src/server/flags.ts`: `LOGO_UPSTREAM_ENV`, `LogoUpstreamMode`, `logoUpstreamMode`, `SVG_RASTER_ENV`, `svgRasterOn` (read from the environment, never from the flag reader)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | the `logos.index.*` rows read the first through B6's `logo-index.ts`; `logos.intake.svg-sentence` the second | `flags.test.ts` "the features round variables" (3 tests, one pinning that `logo-index.ts` agrees on the name and the three values)                                                                                                                                                                                   |
| 4.11 the logo ids on the window transport (B6's R3)         | `apps/studio/src/server/agent-actions.ts`: `SERVER_SIDE_WINDOW_ACTIONS_F1_IDS` and `SERVER_SIDE_WINDOW_ACTIONS_F1` (filtered through `isActionId`, the pattern of the pending list, so the file typechecked before B6's schema entries landed and carries all three now), appended to `SERVER_SIDE_WINDOW_ACTIONS`; `logo.insert` in `DRAFT_CREATING_ACTIONS`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `logos.agent.search-insert`, `logos.insert.one-click-asset` (B6's rows; this is their transport)             | `agent-actions.test.ts` "carries the logo picker's three ids once the action table names them"                                                                                                                                                                                                                       |
| 4.11 the logo handlers on the deck dispatcher (B6's R4)     | `apps/studio/src/server/actions.ts`: `LOGO_ACTION_IDS`, `registerLogoActionsLazily` (B6's `registerLogoActions` over a dispatcher loaded on the first call, the way the materials package loads, because `logos.ts` imports sharp and the store client at its top), the call after `registerAssistActions`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | the same                                                                                                     | none of its own; `tsc -b apps/studio` and the export drive's `asset.add` path through the same dispatcher                                                                                                                                                                                                            |
| 4.7 P1 the URL intake's sentence and the User-Agent         | `packages/headless/src/capture/shared.ts`: `ALLOWLIST_SENTENCE`, `AllowHostOptions.allowPaths`, `assertAllowedHost` (the seller's sentence when paths are off, the `--allow` line when on), `fetchAllowed` passing `allowPaths`, `turboslideUserAgent`, the `user-agent` header on `pinnedFetch`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `logos.intake.url-sentence`                                                                                  | `shared.test.ts` "the features round (docs/FEATURES.md 4.7)" (3 tests) and the pin at "the process policy is the default the studio sets once" moved to the sentence                                                                                                                                                 |
| 4.7 P1 the hosted svg branch behind `TURBOSLIDE_SVG_RASTER` | `packages/headless/src/capture/shared.ts`: `IntakePolicy.svgRaster`, `SvgRasterPolicy`, `svgRasterPolicy`, `imageInfo` keeping an svg hosted under the policy (the refusal line unchanged). `packages/headless/src/capture/intake.ts`: `AssetIntakeOptions.svgRaster`, `SVG_RASTER_BOX`, `SVG_RASTER_SCALE`, `svgRasterSize`, `rasterizeSvg` (sharp at a density that draws at the target, `fit: 'fill'` on the computed box), the svg branch of `addAsset` (the sanitized source kept, one PNG twin at scale 3, the drop named in the warnings), the header comment. `apps/studio/src/server/actions.ts`: `setStudioIntakePolicy` carrying `svgRaster` from `svgRasterOn()` with B6's `sanitizeLogoSvg` (a pure import, `logo-sanitize.ts`)                                                                                                                                                                                                                                                       | `logos.intake.svg-sentence` (the raster half)                                                                | `intake-svg.test.ts` (new, 4 tests): the fit and the 3x size; hosted with the policy one PNG twin at 792 by 198 with the source kept and the `<script>` drop named; hosted without the policy the line it always had; a checkout keeps the svg bytes                                                                 |
| 3.4 the fonts' export path                                  | nothing to change: `pptx/fonts-map.ts` reads `FONT_NAMES` whole (`CATALOG_NAMES`) and the PDF embeds what the browser drew, so B2's eight `FONT_NAMES` entries (on the tree) are the whole export change; the eight folders sit at `packages/fonts/assets/<id>/*.woff2` beside `LICENSE`, which `PACKAGES_PATTERN`'s `fonts/assets/*/*` in `apps/studio/vite.deploy.config.ts` already names. The `packages` server asset group's size is measured on the first preview deploy by the integrator and never typed here                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `fonts.export.editable-names-face`, `fonts.export.pdf-face` (B2's rows, extended by Geist and Fraunces)      | none                                                                                                                                                                                                                                                                                                                 |

Not this ship: the `shader.frame` handler, the 30 s bound and the `RenderError` sentence in `actions.ts`, `download.ts`'s web page frame and the report's missing frame row are ship two's items of this lane (docs/FEATURES.md 5.5, 5.8) and were not touched.

### 2. What the drives measured on 4417

The drives are two playwright-core scripts in this lane's scratchpad (`drive-export.mjs`, `drive-intake.mjs`), each over the window API of a fresh `/new` draft; the JSON records sit beside their files there. Times in UTC.

**`logos.export.pdf-pptx-crisp` (14:17Z, the raster flag unset; 14:19Z again with it set, the same numbers).** A 396 by 252 PNG added as `role: 'logo'` (a logo insert's 3x twins, stood in for by the spec's PNG since B6's `logo.insert` fetches the network), `brand.set /mark { kind: 'picture' }`, `/footer/logo 'picture'` and `/footer/assetId`, then the toolbar's New slide and a `picture` block of that asset at 160 by 100 on the second slide (a block insert on the title slide converts it to a canvas, which the first run measured: its mark became a block shot at 2x, the drive's artefact and not the export's). The editor drew the footer picture and the title slot picture and no GT wordmark. The files, each downloaded through File > Download > Download options and read from the download's bytes:

- The PDF (22,194 bytes, 1.6 s): two image objects, 396 by 252 and 396 by 248, the title slot's twin and the picture block's; the first page carries the mark.
- The Perfect PowerPoint (45,183 bytes, 2.3 s): slide 1 the sheet at 3200 by 1800 and `ts:title#title-logo:2` at 396 by 252 in a 132 by 84 box (this lane's overlay, the 3x page opened for the flatten export because the kit draws a picture logo); slide 2 the sheet alone.
- The Editable text PowerPoint (21,770 bytes, 2.3 s): slide 1 `ts:title#title-logo:2` at 396 by 252 in a 132 by 84 box; slide 2 `ts:split-1#acme-mark:1` at 480 by 300 in a 160 by 100 box, the logo role shot at 3x (`THREE_X_KINDS`).

The footer's box is in none of the three files, and the row is not passed on this tree. The mechanism, measured with a temporary test that rendered the same deck with `renderDeck` and read the document: `frameBandHtml` (`packages/render/src/stage.ts` 168 to 174) writes the band's picture as `<div class="wordmark pos-bottom-left is-picture">` while the theme sheet hides `.wordmark:not(.ts-kit-wordmark)` whenever the kit draws the footer (`packages/render/src/theme-css.ts` 212 to 218, `kitDrawsFooter`); the viewer's `Frame.tsx` 52 adds `ts-kit-wordmark` and the editor shows the footer picture, and the export's document and the print document (the PDF) hide theirs, so a kit with a picture footer logo has no footer logo in any export today. This lane's tag found the image and measured it 0 by 0 (`width: auto` on a hidden element), and the raster left the scene. With the class added to the document string alone, the same test measured the footer at [72, 864, 28.28, 18] on the title slide and the content slide, shot it at 3x to 87 by 54 (the snapped 29 by 18 box) and the title slot to 396 by 252: the export path is ready and waits on request R1. `brand.export.pdf-logo` reads the title slot alone and is unaffected.

**`logos.intake.url-sentence` (14:01Z, 14:19Z).** `asset.add { url: 'https://thesvg.org/icons/figma/default.svg' }` from the page answered "Pictures can be fetched from these sites only: generaltranslation.com, prototemplate.com, glyphfield.com and Wikimedia Commons. Upload the file instead", no `--allow`; `asset.add` with `https://upload.wikimedia.org/wikipedia/commons/4/47/PNG_transparency_demonstration_1.png` answered an asset of 800 by 600 (the pinned fetch now sends `User-Agent: Turboslide/0.0.0 (+https://turboslide.vercel.app)`; audit-logos 16 measured 403 without it). Both halves of the row's interaction passed at the window API; the Image by URL dialog's own line is request R2.

**`logos.intake.svg-sentence` (14:01Z with the flag unset, 14:19Z with `TURBOSLIDE_SVG_RASTER=1`).** Flag unset: `asset.add` with an svg data URL answered the API's line "svg is not accepted here; send png, jpeg, webp or gif" (unchanged, as 4.7 asks); the snackbar the row reads is the viewer's mapping of that line (request R2), so the row's sentence half is not passed on this tree. Flag set: the same svg (a 200 by 50 viewBox carrying a `<script>`) landed as `{ size: [792, 198], scale: 3, twins: { neutral: 'assets/acme-svg.png' }, sourceFile: 'assets/acme-svg.source.svg' }`, 3x of its 264 by 66 fit, through the studio's dispatcher and B6's sanitizer; the row's raster half is on the tree behind the flag (docs/FEATURES.md section 8: the flag drops when the row has passed on production). The chooser's accept list is B1's and B6's (4.5).

No production deck was created or read; the production rows of this lane are the integrator's runs.

### 3. Commands and their results

- `node_modules/.bin/tsc -b packages/export`, `packages/headless`, `apps/cli`: clean. `tsc -b apps/studio`: clean for every file of this lane; the four errors it prints are B4's in progress `apps/studio/e2e/core/logos.spec.ts` (296 `dryRun` is not on `Counts`; 455 `headers` is possibly null, twice; 467 a `Record<string, string> | null` where `undefined` is wanted), not this lane's.
- Vitest, per package: `packages/export` `kit-logos.test.ts` 5 passed, `export-pptx.test.ts`, `report.test.ts`, `batch/merge.test.ts` 28 passed; `packages/headless` `src/capture` 3 files 28 passed (`shared.test.ts` with the three added and the moved pin, `intake-svg.test.ts` 4, `capture.test.ts`); `apps/studio` `flags.test.ts` 10, `upload.test.ts` 3, `agent-actions.test.ts` 6 passed. The browser test `extract.test.ts` was not run by this lane (it takes the fixture deck through the same `extractScenes`; the export drive covered the path on 4417).
- The two drives, section 2.

### 4. Requests

R1 (the integrator, `packages/render/src/stage.ts` `frameBandHtml`, 168 to 174; the file is in no lane's list): the band's div carries the class the viewer's Frame gives it, in both kit forms, so the theme sheet's `.wordmark:not(.ts-kit-wordmark) { display: none }` (`theme-css.ts` 217) no longer hides the footer logo in the export's document and the print document:

```ts
if (band.logo.kind === 'default')
  logo = `<div class="wordmark ts-kit-wordmark ${positionClass(band.logo.position)}" aria-hidden="true"><svg width="28" height="18" fill="currentColor"><use href="#gt-mark"/></svg></div>`;
else if (band.logo.kind === 'picture')
  logo = `<div class="wordmark ts-kit-wordmark ${positionClass(band.logo.position)} is-picture" aria-hidden="true" style="width:${band.logo.w}px"><img src="${escapeAttr(band.logo.src)}" width="${band.logo.w}" height="${band.logo.h}" alt=""></div>`;
```

`stage.test.ts` or `theme-css.test.ts` pins that a kit band's markup carries `ts-kit-wordmark`. Without it `logos.export.pdf-pptx-crisp` stays red on its footer half and every export of a kit with a picture footer logo lacks the footer logo (a defect older than this round: `brand.logo.use-on-every-slide` writes the footer slot and the PDF and the PowerPoints drop it). This lane's extractor and builder are ready for the class and change nothing else when it lands.

R2 (B3, `packages/viewer/src/Editor.tsx` `insertPictureFile` 3302 to 3390 and the By URL catch at 3446 to 3460, with `packages/viewer/src/picture-place.ts` `uploadFailureOf` and `uploadFailureSentence`, the integrator's file): the two sentences of 4.7 reach the seller as they stand. For `logos.intake.svg-sentence`: when `asset.add` refuses with the line `isSvgRefusal` reads (`apps/studio/src/server/upload.ts`: `/svg is not accepted here/`), the outcome's sentence is `UPLOAD_REASONS.notSvg` whole ("SVG files are not accepted yet. Export the logo as a PNG and upload that"), not "The picture could not be uploaded: the upload did not finish", which is what `uploadFailureOf` maps it to today (`did-not-finish`, the fallthrough); the same for the presigned route's `reason` when its `code` is `not_svg`. For `logos.intake.url-sentence`: when the server's message starts with "Pictures can be fetched from these sites only", the Image by URL dialog shows that sentence and not "the page could not read it and the server may not fetch from this host" (the `/allowlist|not a public address|private address/` branch no longer matches, because the sentence carries none of those words). One line each; the sentences are constants B3 may import from `@turboslide/headless/capture/shared` (`ALLOWLIST_SENTENCE`) and the studio's `upload.ts` (`UPLOAD_REASONS.notSvg`), or repeat by request in `packages/chrome/src/menus/strings.ts`.

R3 (B6, `apps/studio/src/server/logo-index.ts` 40 to 51): `LOGO_UPSTREAM_ENV`, `LogoUpstreamMode` and `logoUpstreamMode` are defined in `flags.ts` now (B6's R5); `logo-index.ts` may import them from `./flags` and drop its copy. Until then `flags.test.ts` pins that the two agree on the name and the three values, and nothing breaks either way.

R4 (B4, `docs/gslides-parity/focus/core-matrix.json` and the drivers): the three rows exist in the matrix; no driver names them yet on the tree (`grep url-sentence|svg-sentence|pdf-pptx-crisp` over `apps/studio/e2e/core` and `scripts/probes/core-walk` answers nothing). What the drives of section 2 read is what a driver reads: for `logos.export.pdf-pptx-crisp` the Perfect file's `ts:<slide>#title-logo:<n>` object at 396 by 252 and, once R1 lands, `#footer-logo:<n>` at 84 by 54 or more on every slide, the Editable file's the same plus a logo role block's PNG at 3x of its box; for the svg row `UPLOAD_REASONS.notSvg` in the snackbar once R2 lands, and with the preview's `TURBOSLIDE_SVG_RASTER=1` a `scale: 3` asset with a `.png` twin and a `.source.svg`.

R5 (the integrator, information): B2's note names `packages/headless/src/sheet.ts` 135, a fifth Inter stack without the fallback face, as "the integrator's or B7's file"; `sheet.ts` is not in this lane's list (B7 owns `capture/intake.ts` and `capture/shared.ts` in that package) and was not touched.

### 5. The P1 condition and the budget

The two P1 items (the URL sentence with the User-Agent; the svg branch behind the flag) landed beside the P0 items because their rows are `images` rows (unparkable, B4's note on each) and the integrator decides at the ship whether they count; each is one commit's worth and reverts alone (section 1). Neither adds a store call, a timer or a poll: the svg branch runs inside the `asset.add` write that exists, the User-Agent is a header on a fetch that exists, and the 3x page a flatten export opens for a kit with a picture logo is one more page in the export's own function call, never in the editor.

## Features round, ship two

Builder B7 of the features round, ship two (docs/FEATURES.md 5.5, 5.8, the lane table of section 6, the rows of 7.1, the named tests of 7.3 that fall to this lane), on the shared checkout at `2be923e` on 2026-09-24 while B1, B4 and B5 landed beside it (the tree carried B5's schema fields, `recipe-key.ts`, `frames.ts`, the materials `shaderFrame`, B1's gallery and Download rows and B4's matrix rows and drivers as this lane worked; every file below is B7's alone, and no file of another lane was edited). The dev server was port 4417 (`TURBOSLIDE_STORE=tmp` under a private `TMPDIR` in the scratchpad, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, `TURBOSLIDE_AGENT_LOG=1`, two 48 character secrets from `openssl rand -hex 24`), stopped before the return with no listener left on the port. playwright-core (Chromium 151.0.7922.34 from the repository root) ran against 4417 under `.turboslide/e2e.lock` (taken with the `until mkdir` loop, released after every run; no lock is held at the return). No git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran. Every scratch deck was made on the tmp store of 4417 and trashed and removed through the window API at the end of its drive (the one draft an earlier run left when its removal met a stale base was removed by the next run); nothing was created on a deployment. No token, cookie or secret was printed. Times are UTC.

### 1. The items, their files and their hunks

Each item is one commit's worth; a red row's item reverts alone. The hunks are named by the symbol they add or change.

| Item                                                    | Files and hunks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Rows                                                                                                                                         | Unit tests                                                                                                                                                                                                                                                                                                         |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 5.5 the hosted capture's 30 s bound and its sentence     | `apps/studio/src/server/shader-frames.ts` (new): `RENDER_ERROR_SENTENCE` ("The frame could not be rendered. Try again, or place the shader without a frame"), `CAPTURE_BOUND_MS` (30,000), `RenderError` (status 500, the message the sentence, `reason` for the server log), `isCallerRefusal`, `boundedCapture` (through the render worker's local queue as a `render` job so one browser runs at a time behind a render or export, the caller waiting the bound at most while the job runs on; a plain race without a queue; a caller's own refusal passes through). `apps/studio/src/server/actions.ts`: `CAPTURE_ACTION_IDS` (`material.capture`, `shader.capture`, `slide.setBackgroundMaterial`, `slide.setBackgroundShader`, filtered through `isActionId`), `captureQueue` (`localQueue(clientPaths(process.env))`), `boundedHostedCapture`; `registerAssetActionsLazily` runs a capture id under it; `registerRecordActionsFor`'s `captureMaterial` dep (the record actions' background picture) under it too | `shaders.background.place-answers` (the server half; the dialog's Placing state and sentence are B1's)                                        | `shader-frames.test.ts` "the hosted capture's bound" (4 tests: the sentence and the bound, the race past the bound, a browser failure as the sentence with a `ConflictError` and a `TypeError` passing through, the queue path with done, failed, conflict and a slow job left to finish) |
| 5.8 the shader ids on the studio's dispatcher and the window transport | `actions.ts`: `SHADER_ASSET_ACTION_IDS` (`shader.list`, `shader.insert`, `shader.set`, `shader.frame`, `shader.capture`, `shader.render`, `slide.setBackgroundShader`, the strings filtered through `isActionId`, the F1 pattern), registered over the lazily loaded materials dispatcher beside `ASSET_ACTION_IDS` (B5's `registerAssetActions` handlers, one implementation on every transport; `shader.frame`'s presigned upload consumed after the write as `asset.add`'s is); the `placeInsert` dep of `assetDispatcherLoader` (B5's R5, the studio half: `placeInsert(slide, 'material', size).pos` from `../editor/place-insert`, wired only while `INSERT_MIN_SIZE` carries `material`, so the file typechecks and answers before the integrator's R5 lands and an agent's `shader.insert` takes the gallery's rectangle after). `apps/studio/src/server/agent-actions.ts`: `SERVER_SIDE_WINDOW_ACTIONS_F2_IDS`, `SERVER_SIDE_WINDOW_ACTIONS_F2` (filtered), `SHADER_DRAFT_CREATING_IDS`, appended to `SERVER_SIDE_WINDOW_ACTIONS` and `DRAFT_CREATING_ACTIONS` | `shaders.agent.list-insert-set-render`, `shaders.frame.*` (B5's rows; this is their hosted registration and transport)                       | `agent-actions.test.ts` "carries the shader library's ids once the action table names them" (the filter's pattern: empty before the schema entries land, every id on both lists after, the writes creating a draft's deck)                                                                          |
| 5.5 the orphan prune behind the response                 | `shader-frames.ts`: `FRAME_PRUNE_EVERY_MS` (60,000), `pruneDue` (per deck per instance, a table seam for tests), `runBehindResponse` (`waitUntil`, the thumbs route's pattern), `frameFileLister` (the Blob prefix listing with upload times on a deployment, this instance's `assets/` folder with modification times on a checkout or the tmp store), `FramePruneDeps`, `scheduleFramePrune` (B5's `pruneFrameAssets` over the listing, behind the response, once a minute). `actions.ts`: `FRAME_WRITE_IDS` (`shader.frame`, `shader.capture`); `registerAssetActionsLazily` takes the frames deps and schedules the prune after a frame write; `deckDispatcher` builds the deps (`deckStore`, `frameFileLister(deckId, store.dir, exportBlobClient)`) | `shaders.frame.reuse-and-prune` (B5's row; the prune half)                                                                                   | `shader-frames.test.ts` "the orphan prune behind the response" (3 tests: the minute's throttle per deck, the folder listing, an unreferenced frame older than the grace removed behind the response with the referenced one kept and a second write within the minute scheduling nothing)  |
| 5.5 the export's wait and the report row                 | `shader-frames.ts`: `materialBlocksOf`, `pendingShaderBlocks` (B5's `frameIsStale` over `shaderPaletteOfDeck`), `SHADER_FRAME_WAIT_MS` (10,000), `SHADER_FRAME_WAIT_TICK_MS` (2,000), `ShaderFrameWait`, `waitForShaderFrames`. `apps/studio/src/server/download.ts`: `awaitShaderFrames` (the room flushed first, then the head read every tick while a shader of the exported slides has no fresh frame), `shaderWaits`, `rememberShaderWait`, `reportWithShaderWait`, `ExportReportRowEntry`, `shaderRowsOf`; the wait before the folder sync in `startExportFn` (remembered by the worker's job id), `syncExportFn` (remembered by the page's progress id while the call runs, the row in the answered report), `startBatchedExportFn` (the plan answers `rows`; `StartBatchedExportAnswer`), `runBuildFn`; the row on `pollExportFn`'s answers while the wait is remembered and in the report once done (`ExportPoll` gains `rows?`); `mergeExportFn`'s report; `BatchedExportProgress` gains `rows?` and `runBatchedExport` carries the plan's rows on every step; the header comment. `packages/export/src/report.ts`: `SHADER_REPORT_PREFIX` (`shaders: `), `shaderFrameSentence`, `shaderReportRow`, `shaderRowOf`, `withShaderRow` | `shaders.export.missing-frame-row`                                                                                                           | `shader-frames.test.ts` "the export's wait for a frame" (3 tests: pending, fresh, stale by a missing key and by a moved recipe; one read and no wait when fresh; the ticks until the frame lands with the count and the time, the bound with five ticks and out); `report.test.ts` "the shader frame row" (2 tests: the sentence forms, the replace and read back) |
| 5.5 the exporters draw the frame                         | `packages/export/src/scene/shaders.ts` (new, schema imports alone): `SceneShader`, `ShaderScene` (`Scene & { shaders? }`, so `scene/types.ts`, no lane's file, is untouched), `shadersOf`, `materialBlocksOf`, `frameAssetOf`, `sceneShadersOf` (the recipe, the record, the file under the deck's folder, `missing`, `stale` by a missing `frameKey`), `pendingShadersOf`. `packages/export/src/scene/extract.ts`: the header comment, `ExtractResult.scenes: ShaderScene[]`, `measureShaderBoxes` (the `.material` element's box off the 1x page, snapped), the scenes' `shaders` after `enrichScene`. `packages/export/src/pptx/build.ts`: the header comment, `addShaderFrames` (the frame file's own bytes at the frame box as `ts:<slide>#<block>`, no rid suffix, the recipe JSON as `descr`, opaque; the `shader:` residual line), its call after the kit logo overlay in flatten and after the raster loop in native, the `shaders:` row from `pendingShadersOf` before the write. `packages/export/src/pdf/build.ts`: the header comment, the `shaders:` row over the exported slides (the frame's pixels reach the page through the print document's own `<img>`, nothing drawn here) | `shaders.export.pdf-frame`, `shaders.export.pptx-frame`, `shaders.export.html-frame`                                                          | `scene/shaders.test.ts` (new, 4 tests): the material blocks through composites; the recipe, the record, the file and the two readings; the pending list once per shader over two themes; `addShaderFrames` read back out of the package (the picture `ts:s1#fresh`, the recipe in descr, the box in EMU, one media part of the frame's bytes, a shader with no frame or no box left alone) |
| 5.4 the previews in the function's asset group (B5's R11) | `apps/studio/vite.deploy.config.ts`: `PACKAGES_PATTERN` gains `materials/previews/*` with its comment (B5 measured 125 files, 136,512 bytes; the group's size is read on the first preview deploy, never typed here)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `shaders.insert.gallery-thumbnails` (B1's row, when a still is served from the workspace)                                                    | none                                                                                                                                                                                                                                                                                                               |

Not this ship: `upload.ts`, `flags.ts`, `packages/headless/src/capture/intake.ts` and `shared.ts` stand as ship one left them; nothing in section 5 names them. The web page export (`file.download.html`, `runBuildFn`) needed no change beyond the wait: the CLI's `renderStandaloneFile` inlines the renderer's `<img class="material-frame">` (a continuous tone frame as a JPEG under the 16 MB budget, `inlineAssets`) and mounts nothing, since the live mount is the editor's alone (`data-live` on a live render only); the drive read the built page with no canvas, no `data-live` and no label.

One reconciliation the tree forced (section 5 below): B5 landed `shaderFrame` in `packages/materials/src/actions.ts` (the write of 5.5 with the presigned `upload` path a frame over the body cap needs, registered by `registerAssetActions`) while this lane wrote the same write in `shader-frames.ts`. SPEC 7.1 wants one implementation per action, so this lane's copy of the write was removed before the return and the studio reaches B5's through the lazily loaded asset dispatcher (the deps carry `readUpload` and the hosted store's `putAsset`); what stays this lane's is what the lane table names around it: the bound, the prune behind the response and the wait. The one rule B5's write does not carry is request R2.

### 2. What the drives measured on 4417

The drive is `docs/gslides-parity/features/build/b7/drive-shader-exports.mjs` (playwright-core over the window API of a fresh `/new` draft; the JSON records beside it are the last run's, `drive-shader-exports-4417.json` and the four reports). Eight runs between 15:46Z and 16:07Z, the first seven fixing the drive's own readings (a single pixel against a 6.7x downscale, a folder entry read as a file, the page's document read before the server's write had reached it, a PNG regex over a JPEG data URI); the run of record is the eighth (16:06Z to 16:07Z), every row passed, the deck `untitled-20260924-s6ku` trashed and removed.

The setup: `block.insert` of a `material` block (`paper:liquid-metal`, alt "The liquid metal shader", `pos` 900,300 480 by 272) on the title slide of the draft, which converted it to a canvas; the frame by the hosted capture, `material.capture { anchors: [5500], id: 'sh1-frame', role: 'frame' }` through the bound and the queue, answered in 1.7 to 2.3 s across the runs (`angle-metal`, 3200 by 1800); `block.set /asset`. The action table on this tree refuses `frameKey` on `material.capture`'s input (B5's schema entry, R3), so the frame carries no key and is stale by that absence (recipe-key.ts `frameIsStale`): every export below waited and wrote the row. That is the tree's state, recorded, not the design: with B5's entry the hosted fallback records the key (`capture.ts` takes `request.frameKey`) and a fresh frame waits for nothing (the unit tests read that path over a store).

- `shaders.export.pdf-frame`: the PDF (738,148 bytes, 9.9 s with the 8 s wait) holds one image object of 3200 by 1800 (`pdfimages -list`); the page rasterized to 1600 by 900 (`pdftoppm -scale-to-x 1600 -scale-to-y 900`) has a mean colour over the block's box of 167,167,170 against the frame's 166,166,169 (one channel level apart); `pdftotext` finds no "not captured". Passed.
- `shaders.export.pptx-frame`, Editable text (10.1 s) and Perfect (10.6 s): the slide part carries `<p:pic>` named `ts:title#sh1` with `descr="{"materialId":"paper:liquid-metal"}"` and a media part of 3200 by 1800; the report's residual reads `shader: title#sh1 travels as its frame (3200 by 1800) at the block's box with the recipe in descr; the frame predates its recipe and is drawn as it is (docs/FEATURES.md 5.5)`. Passed in both modes.
- `shaders.export.html-frame`: `build.run` (750,894 bytes, 8.8 s with the wait); the page carries `<div class="material" role="img" aria-label="The liquid metal shader"><img class="material-frame" src="data:image/jpeg;base64,…">` decoding to 3200 by 1800 with a mean colour of 166,166,169 (the frame's own), no `<canvas`, no `data-live`, no "not captured". Passed.
- `shaders.export.missing-frame-row`: `block.set /anchor 7000` then `export.run pdf` at once; the report's residual carries `shaders: 1 shader had no frame; the export waited 8 s for it` (the server log: `shaders: 1 frame(s) pending at export time: title#sh1`, `waited 8011 ms; 1 frame(s) still pending`), the file carries the frame. Passed. The wait reads 8 s and not 10 because a tick that would pass the bound is not taken (four 2 s ticks plus the reads); the sentence rounds to whole seconds. The dialog's row (`progress.rows`, B1's reading) is on the poll's answer from this ship and reaches the dialog once the controller copies it (R1).
- `shaders.background.place-answers` (the window API half): `slide.setBackgroundMaterial { slideIds: ['b7-ground'], materialId: 'paper:gem-smoke', anchor: 5500 }` on a statement slide answered in 1.8 to 2.7 s through the bound and the queue, and the slide read back as a canvas with the covering picture `background` at the back naming `gem-smoke`. The dialog's Placing state and its sentence are B1's; the sentence path of the bound is the unit tests' (a browser failure and a job past the bound both answer the sentence, never a log).

No production or preview deck was created or read; the production rows of this lane are the integrator's runs. `core-gate.mjs --only specs` was not run by this lane: B4's `core/export.spec.ts` gained the four export rows during this lane's work with a syntax error mid edit (`tsc` at 1790 and 1991), and this lane drives against the checkout's server with its own script until B4's file compiles; the drivers' readings were read and match section 4's R5.

### 3. Commands and their results

- `node_modules/.bin/tsc -b packages/export`: clean. `tsc -b apps/studio`: clean for every file of this lane; the errors it prints are B4's in progress `apps/studio/e2e/core/export.spec.ts` (1790, 1801, 1991: a syntax error mid edit) and B5's in progress `packages/store/src/frames.test.ts` (88, 105) and `packages/materials/src/shader-actions.test.ts` (269, 319), not this lane's.
- Vitest, per package: `packages/export` whole, 21 files 154 passed (52.6 s; `report.test.ts` with the two added, `scene/shaders.test.ts` 4, the browser tests included); `apps/studio` whole, 72 files 552 passed (8.4 s) before the reconciliation, then `shader-frames.test.ts` 10 and `agent-actions.test.ts` 7 passed after it.
- The drive, section 2.

### 4. Requests

R1 (the integrator, `apps/studio/src/editor/controller.tsx` and `packages/chrome/src/ExportMenu.tsx` `ExportProgress`, both in no lane's list; B1's R6 names the same field): the dialog's row of `shaders.export.missing-frame-row` reaches the dialog through `input.export.progress.rows` (B1's `exportReportRows`), and the controller builds `progress` field by field, so the row this lane puts on the answers needs one copy per site. `ExportProgress` (ExportMenu.tsx 50) gains `rows?: ReadonlyArray<{ id: string; text: string }>`. In the controller: the sync path's poll (1029 to 1031) reads `answer.rows` beside `answer.line` and hands both to the publish (`onLine` becomes `(line, rows)` or the publish takes `{ label, line, ...(answer.rows ? { rows: answer.rows } : {}) }`); the queued path's publish (about 3235) adds `...(poll.rows !== undefined ? { rows: poll.rows } : {})`; the batched path's publish (about 3185) adds `...(progress.rows !== undefined ? { rows: progress.rows } : {})`. The types are `ExportPoll.rows` and `BatchedExportProgress.rows` in `server/download.ts`. Without this the row is in the report's residual only (the card after the export) and B4's driver, which reads the dialog while the export runs, reads no row.

R2 (B5, `packages/materials/src/actions.ts` `commitFrame`): the storage rule's last clause (5.5: "two clients capturing one key resolve to one asset ... a `BlobExistsError` on the put is success"). `deps.store.putAsset(relative, png, 'image/png')` throws `AssetExistsError` when the name is held with other bytes, which two GPUs rendering one key produce (the pixels differ, the key does not), and the write then fails for the second client. The change: catch `AssetExistsError` and `isBlobExistsError` (packages/store `store.ts`, `blob-store.ts`) around the put, keep the first writer's file and write the record and the block as when `existed` is true. This lane's removed copy of the write carried that branch (`shader-frames.test.ts` in its earlier form read it: the second client's write named the first file's bytes); the case is yours to pin in `shader-actions.test.ts`. Second, the schema entry of `material.capture` gains `frameKey` (your `actions-shader.patch.ts`, R1 to the integrator) so the hosted fallback and the CLI record a fresh key; on this tree the input refused it and every frame the fallback made was stale by absence (section 2).

R3 (the integrator, `packages/schema/src/actions.ts`): B5's R1, the `shader.*` entries including `shader.frame`. Until it lands `isActionId` answers false for every shader id, `SHADER_ASSET_ACTION_IDS`, `FRAME_WRITE_IDS` and `SERVER_SIDE_WINDOW_ACTIONS_F2` are empty, and this lane's registrations answer nothing for them by design; `agent-actions.test.ts` and `shader-frames.test.ts` pass on both trees.

R4 (B1, `packages/chrome/src/dialogs/ShaderGallery.tsx` `SHADER_GALLERY.placeFailed`; or the integrator, `packages/chrome/src/menus/strings.ts`): the one sentence lives twice, `RENDER_ERROR_SENTENCE` in `server/shader-frames.ts` (the server's error message, which the window transport carries as `error.message` and the dialog shows) and `placeFailed` in the gallery module; they are equal byte for byte today and `shader-frames.test.ts` pins the literal. The one constant belongs in `strings.ts` (`SHADER.placeFailed`) with both sides importing it; until then a change to either breaks the pin, which is the point.

R5 (B4, `apps/studio/e2e/core/export.spec.ts`, information): what the rows read on this tree. `shaders.export.pptx-frame`: the picture is `ts:<slide>#<block>` with no rid suffix, `descr` is the block's recipe as JSON (`materialRecipeOf`), the media PNG is the frame file's own bytes (the long side 3200), in both modes. `shaders.export.pdf-frame`: the frame reaches the page through the print document's `<img>`, so the PDF's largest image is the frame at its stored size and a stale frame is drawn as it is. `shaders.export.html-frame`: the build inlines the frame as `data:image/jpeg` (the CLI's budget encoder), so a reading that wants a PNG data URI fails on the encoding alone; the pixels are the frame's. `shaders.export.missing-frame-row`: the row's text is `shaderFrameSentence` ("1 shader had no frame; the export waited 8 s for it"), on the poll's `rows` while the export runs (after R1 in the dialog) and as `shaders: <sentence>` in the report's residual; the wait reads 8 s when nothing lands. `shaders.background.place-answers`: through the bound a hosted capture on a checkout answers in about 2 s and a failure reads the one sentence; on a deployment the row's 5 s is the client capture's (B1 and B5) and the hosted path is the fallback.

R6 (B5, information): the studio wraps `shader.capture` and `slide.setBackgroundShader` in the same bound as `material.capture` and schedules the orphan prune after `shader.capture` and `shader.frame`; `shaderCapture`'s note that the bound is the studio's holds. `slide.setBackgroundShader` answers 501 until the P1 slot's handler lands, as your note says.

### 5. The P1 condition and the budget

No P1 item of this lane exists in ship two (the two P1 rows of the lane are ship one's `logos.intake.*`). The budget (docs/SYNC.md 4.5, 6.1): the export's wait reads the head at most six times per export, none when no frame is pending, spaced two seconds apart, and lists nothing; the orphan prune is one `assets/` listing per deck per instance per minute at most, behind a frame write's response and never in it, so a slider burst of frame writes costs one listing; the bound adds no call (the capture's own browser and store writes stay the materials package's); the exporters read files the folder sync already pulled. Nothing here adds a timer or a poll, and the frame's pixels travel inside the export's own function call.
