{
  "schemaVersion": 1,
  "id": "cli",
  "kind": "content",
  "layout": {
    "type": "cols",
    "ratio": "5/7",
    "gap": 72,
    "align": "center"
  },
  "slots": {
    "left": [
      {
        "id": "h",
        "type": "heading",
        "level": "h2",
        "text": "The CLI"
      },
      {
        "id": "p1",
        "type": "paragraph",
        "text": "The CLI is the gt package, the same name as the library family. Four commands and one component take a project from a fresh install to a production build in every configured locale. The same CLI also translates JSON, YAML, Markdown, and MDX files in projects that do not use the libraries.",
        "measure": 56
      },
      {
        "id": "code",
        "ext": {
          "import": {
            "style": "margin-top:8px"
          }
        },
        "type": "panel",
        "code": "npm i gt-next\nnpm i -D gt\n\nnpx gt init\n\nnpx gt translate"
      }
    ],
    "right": [
      {
        "id": "rows",
        "type": "rows",
        "key": 180,
        "tight": true,
        "items": [
          {
            "key": "Install",
            "icon": {
              "name": "cube"
            },
            "value": "gt-next runs in the app and gt is the CLI, installed as a dev dependency."
          },
          {
            "key": "Configure",
            "icon": {
              "name": "cog-6-tooth"
            },
            "value": "The setup wizard detects the framework, writes gt.config.json, and can create the credentials."
          },
          {
            "key": "Wrap",
            "icon": {
              "name": "code-bracket"
            },
            "value": "Text is wrapped in T where it is written. Var marks the values that are left as they are."
          },
          {
            "key": "Translate",
            "icon": {
              "name": "language"
            },
            "value": "The translate command writes every configured locale into the project, ready to build."
          },
          {
            "key": "Build",
            "icon": {
              "name": "rocket-launch"
            },
            "value": "In CI the same command runs before the build, so every locale ships with the deploy."
          }
        ]
      }
    ]
  }
}
