# B7 The server rows

The note of builder B7 of the return round (2026-09-18, times in UTC; the drive ran past midnight UTC on the 19th). Owns the files `docs/RETURN.md` section 6 names for B7 server (`apps/studio/src/routes/api/render.$slideId.ts`, `apps/studio/src/server/download.ts`, `tokens.ts`), plus `apps/studio/src/server/**` and `packages/store/src/**` for the render token, the persisted notification level and the deck name at the create, and the three flaky rows' requests the stream fix round two's fixers left for B7 (`docs/gslides-parity/focus/build/b4.md` "Cycle 3 stream fix round two fix round", C3T-R1 and C3T-R2). The dev server was port 4407 (`TURBOSLIDE_STORE=tmp` under a private `TMPDIR` in the scratchpad, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, two 48 character secrets from `openssl rand -hex 24`, and `TURBOSLIDE_TOKEN` set to a random value so the render route's bearer rule holds as on production; `vite.no-watch.config.ts`, `--strictPort`), stopped before the return. No git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran. Playwright ran against 4407 under `.turboslide/e2e.lock` (taken with the `until mkdir` loop, released with `rmdir` after each run); the core gate's localhost run took a scratch lock folder with `--lock`. Every scratch deck was trashed and deleted forever through the product and answered 404 on `/edit/<id>`. No token, cookie or secret was printed. Every uncommitted fix of the stream fix round two's pipeline on the tree stands untouched (`comments-store.ts`, `share.spec.ts`, `room.ts`'s `admitOnBlob`, `images.spec.ts`, `InlineText.tsx`, the render worker queue, the mcp test budget, `.prettierignore`, `versions-by-author.spec.ts`); the edits below are additions beside them.

## Return round

### 1. The render grant: `export.jpg.current-slide` and `export.png.current-slide` (RETURN.md 2.19)

The mechanism, read on `1d3ba31` and on the tree: `file.download.jpg` and `file.download.png` run `render.slide` (`editor-shell.ts` 2314 to 2326: `{ slideIds: [facts.slideId], scale: 2, format: 'jpg' }` and `{ slideIds, scale: 2 }`), the controller forwards it to the `renderSlideImages` server function (`apps/studio/src/server/render.ts`), which answers `images[]` as `/api/render/<slide>?deck=..&theme=..&scale=..&revision=..` urls, and `EditorShell.tsx` opens `images[0]` with `window.open(first, '_blank', 'noopener')` (the shipped 648 to 654). The tab carries no header, and the render route requires `Authorization: Bearer <TURBOSLIDE_TOKEN>` for a full size render when the variable is set, which production and the preview have had since 2026-09-11, so the tab answered 401 `bearer token required` (audit-surface rows 27, 28; audit-export rows 22, 23).

The fix, in the three files section 6 names for B7 and `server/render.ts`:

- `apps/studio/src/server/tokens.ts`: the render grant beside the thumbnail grant. `RENDER_GRANT_TTL_MS` (10 minutes), `RENDER_GRANT_QUERY` (`g`), `RenderGrantTarget` (deck id, slide id, theme, scale, format png or jpg), `signRenderGrant(target, now)` (`<exp>.<hmac>` over `deck|slide|theme|scale|format|exp` under the download secret with its own purpose word), `verifyRenderGrant(target, grant, now)` (true for exactly that picture inside the expiry; the ids must be slugs), `renderFileName(target)` (`<deck id>-<slide id>.<png|jpg>`, the deck id naming the file as the PowerPoint `<deck id>-<theme>.pptx` and the bundle `<deck id>-<revision>.zip` are named). `TYPES` gains `.jpg` so the download route's table is complete. The docblock names the round.
- `apps/studio/src/server/render.ts`: `renderSlideImages` mints a grant per picture after its `authorizeRequest(read)` passed and appends it to every url it answers, in `records[].image` and `images[]` alike (`imageUrl` gains the grant argument; `tokens.ts` is loaded inside the handler as the other security modules are, for the DeckViewer client stubs). The Tools > Advanced > Render this slide row and the agent's `invoke('render.slide')` get the same urls; a bearer still passes the route first.
- `apps/studio/src/routes/api/render.$slideId.ts`: `grantTargetOf(url, slideId)` reads the full size picture a grant can name (null for a thumbnail, the JSON variant, or ids that are not slugs); a verified grant stands for the bearer (`unauthorized(request, url, granted)`) and for the route's `authorize(read)` (the grant was minted after that decision passed for the caller, the thumbnail grant's own rule); a granted request is a download: `wantsJson` is false whatever the Accept header, the response carries `content-disposition: attachment; filename="<deck id>-<slide id>.<ext>"`, `cache-control: private, no-store` and `x-content-type-options: nosniff` (the download route's own headers); a bearer request is unchanged (`private, max-age=60`, no disposition). A grant never opens the JSON variant or a thumbnail: the target it signs is the full size picture alone.

No client file changed: `EditorShell.tsx`'s `window.open(images[0])` opens a url that now downloads (section 6 gives B7 nothing in that file; request B7-R2 below names the better trigger). The blob tier budget is untouched: a granted request is one render per click of the row, no timer, no store poll; on another instance than the one that rendered, the route renders again from the store's copy (`ensureDeckAssets`), bounded by the grant's ten minutes and its fixed parameters.

Measured on 4407 with `TURBOSLIDE_TOKEN` set (the probe `b7-render-download-probe.mjs` in the scratchpad, headless Chromium 1440 by 900, the product's own controls; three runs at 00:57Z, 00:58Z and 01:00Z on the 19th, each on a fresh deck from `/new` trashed and deleted forever, 404 read):

| Step                                                                                         | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| -------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `GET /api/render/opener-brand?deck=gt-brand&theme=light&scale=1` with no header and no grant | 401 `bearer token required` (the rule of the route stands)                                                                                                                                                                                                                                                                                                                                                                                                                    |
| the same with a forged `g=9999999999999.000…`                                                | 401                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| the same with the bearer                                                                     | 200 `image/png` 160 794 B in 6.4 s, `cache-control: private, max-age=60`, no disposition (unchanged)                                                                                                                                                                                                                                                                                                                                                                          |
| `?w=160` with no header                                                                      | 200 `image/png` (the thumbnail rule unchanged)                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Tools > Advanced tools on, File > Download > PNG image                                       | the tab opens `/api/render/title?deck=<id>&theme=light&scale=2&revision=1&g=<grant>`; Chromium turns the navigation into a download and the tab closes; the file `<id>-title.png` (55 825 B, the PNG magic) arrives in 531 to 614 ms (a cache hit after the JPEG row's render); the download event fires on the editor page, not on a popup                                                                                                                                   |
| a second GET of that url from the page's own request context                                 | 200 `image/png`, `content-disposition: attachment; filename="<id>-title.png"`, `cache-control: private, no-store`, `x-content-type-options: nosniff`                                                                                                                                                                                                                                                                                                                          |
| the same url without `g`                                                                     | 401                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| the same url with `format=json`                                                              | 401 (the grant never opens the JSON variant)                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| the same url with the other theme                                                            | 401 (the grant names one theme)                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| File > Download > JPEG image                                                                 | a file arrives (5.98 s, 6.57 s and 27.1 s over the three runs, the last with the gate's specs rendering beside it), but it is the PNG: the tab's url carries no `format=jpg` because `apps/studio/src/editor/controller.tsx` 3087 to 3094 forwards `slideIds`, `themes` and `scale` and drops `format` (request B7-R1). The audit's own urls for row 27 read the same, `…&scale=2&revision=6` with no format, so the JPEG row has answered a PNG url since before the parking |

So `export.png.current-slide` passes on this server end to end and `export.jpg.current-slide` delivers a PNG file until B7-R1 lands in the controller; the server answers a JPEG for a `format=jpg` url (the `format` validator and the worker's `jpg` format are in place, `render.ts` 76 to 84), which the unit test of the grant covers and the row will measure once the input reaches it.

### 2. C3T-R1: a read that proved nothing schedules the one push at the floor

`packages/store/src/presence-store.ts` `pushNow`: before, the unproven branch (`!d.remote.proven`, the copy under the head's version not readable yet) logged "no read proved the record; the push waits" and returned; `pushIfDue` schedules a timer only when `pushNow` throws, so a pending leave waited for the next `set`, `leave` or timer of that deck on that instance, which a closed tab that was the instance's only client of the deck never sends, and the row lived out its 30 s on every other instance (C3T-F1's runs 1 and 3, the chip leaving 28 s and 19 s after the navigation). Now the branch sets `d.lastPushAt` and calls `scheduleAt(deckId, d, now + spacing)`, the lost race's own shape three lines below: one attempt per floor, a head and a proven get each, no puts until a read proves the body, no loop inside one push. The budget stands: presence writes on a change alone at the 5 s floor, and a read that proves nothing costs what the lost race's retry costs.

Pinned by the test "schedules one push at the floor when no read proved the record, so a closed tab's leave lands once the copy is readable without another event of the deck (b4.md C3T-R1)": two instances over the fake, the store's `get` lagging (`holdGet`) and the copies hidden; the leave on the instance whose copy is behind reads once (two heads, `readRemote`'s and `provenGet`'s), writes nothing, logs the one line; the copies return, the floor passes, and the scheduled push alone lands the tombstone (one record put), which the other instance's poll announces as `leave`.

### 3. C3T-R2: the tombstone keeps the beacon's clock

The wire already orders the two: the leave beacon posts `clock: presenceClock + 1` (`packages/realtime/client/room-client.ts` 1432), above every set of that tab, and the route dropped it (`leave(deckId, state.clientId)`). Now:

- `presence-store.ts`: `SharedPresence.leave(deckId, clientId, clock?)`; the record's `left` rows gain an optional `clock` (`PresenceTombstone = { at, clock? }`; absent in a record an earlier build wrote, so the schema reads both), `parsePresenceRecord` and `presenceRecordBytes` carry it; `buried(row, left)` reads a tombstone at or after the row's time or one whose clock is at or above the row's; `set` refuses a state whose clock is at or below the tombstone's for that id (the pending leave's, the record's, or the higher of the two: `tombstoneClock`) the way it refuses an older clock, so the pending leave is never replaced and the row never lands, on this instance (the route's set handler running after `?leave=1` while `roster()` took its head and get) or pushed from another (the tombstone read at the push); a set above the clock (a tab that came back) lands. A merged tombstone keeps the later time and the higher clock.
- `apps/studio/src/server/room.ts`: `leavePresence(room, clientId, clock?, shared)` hands the clock to the blob tier's shared roster (kept in the module state as `presence`, `sharedRoster()`), and calls the channel's own `leave` on the memory and redis tiers and for the clockless leaves (the stream's close, `retireClients`). `packages/realtime/src/channel.ts` `PresenceChannel.leave` and `blob.ts` 556 are the integrator's files this round, so the clock travels through `room.ts` for now; request B7-R3 names the two line change after which `leavePresence` is one channel call.
- `apps/studio/src/routes/api/decks.$deckId.presence.ts`: `?leave=1` calls `leavePresence(room, state.clientId, state.clock)`.
- Beside the two requests, one rule the same-instance test needed and the store lacked: `merged` (the roster) and `reconcile` (the announcements) now skip a remote row whose client has a leave pending here (`leftPending`: a clockless pending leave hides the row; a clocked one hides rows at or below its clock). Before, after a leave on instance a with the floor holding its push, a's own `roster()` listed the client from the record's copy of its row for up to 5 s, and a poll on the pulse in that window re-announced the left client as `presence` to a's streams until the push landed. A leave this instance accepted is a fact for this instance from the moment it landed.

Pinned by three tests: "never lands a set posted before the tab's leave beacon on the same instance: a set at or below the tombstone's clock is refused and the pending leave stays" (set 3, leave with clock 5, set 4 under a 20 s floor: the roster reads empty, the record after the flush has no row and `{ at, clock: 5 }`; set 6 lands), "keeps the beacon's clock in the record's tombstone, so a set with a lower clock pushed from another instance after the leave never lands and one above it does" (a leaves with clock 5, b sets 4 with a later `at` and flushes: no row, the tombstone keeps its clock, b's listeners see `leave`, both rosters read empty; b sets 6 and it lands on both), and `apps/studio/src/server/presence-leave.test.ts` (`leavePresence` hands the clock to the shared roster and the channel its leave otherwise). The existing tombstone test's assertion reads the new shape (`{ at: leftAt }`).

The driver's half of C3T-F1 is the b4 fixer's `share.spec.ts` (on the tree); `collab.presence-chips` on the blob tier is the row these two fixes answer, and it is measured on the enforce preview by the merge owner's two runs, not here (4407 runs the memory tier, where the row passed before).

### 4. The items with nothing to build

- The persisted notification level (`inbox.settings-persist`): RETURN.md 2.16 and section 6 keep the inbox parked this round and its fixes with it ("The inbox's persisted notification level waits with the inbox"). Nothing written.
- The deck's name at the create (RETURN.md 2.18, `decks.name.follows-heading`): the server derives no name. `createStoredDeck` takes `{ name, from, id? }` from the client (`apps/studio/src/server/decks.ts` 275; `packages/store/src/templates.ts` 81 to 86) and the truncation is the client's `autoTitleMutations` on the first committed heading write (`controller.tsx` 6 and 2018, B2's). Nothing for the server.
- B5's table and chart export paths: `docs/gslides-parity/return/build/` held no note of another lane at 01:05Z, so no request reached this lane; the export paths B5's rows name (`tables.export.pdf`, `.pptx-editable`, `charts.export.pdf`, `.pptx-native`) run through the export job and `signDownload` unchanged.
- The Plain Text download (audit-surface row 26; RETURN.md section 8 parks it): read for its mechanism on 4407, not fixed. `export.text` is a client action (`controller.tsx` 3152, `deckText(snapshot.document, input)`; no server call) answering `{ text, slides, bytes }`, and `EditorShell.tsx` makes a blob anchor from `result.text`; on 4407 the row downloaded `<id>.txt` (15 characters, the title) in 607 ms in headless Chromium. The production failure's mechanism was not seen here; the row stays parked as section 8 says.
- Make a copy > Selected slides (audit-surface row 23; section 8): the server's `deck.copy` answers the record (`actions.ts` 759 to 762, `decks.copy(rest, baseRevision)`) and the audit read `GET /edit/<copy>` 200, so the copy is made; the tab is the dialog's (`packages/chrome/src/dialogs/MakeCopy.tsx` 31 to 52 opens a blank tab on the click and points it at the copy when the answer arrives). Not driven here; nothing server side to fix from what the audit saw.

### 5. The matrix rows this lane covers

- `export.jpg.current-slide`, `export.png.current-slide` (driver `core/export.spec.ts`, B4's; `parks: ["file.download.jpg"]`, `["file.download.png"]`): the server's half is above; the PNG row passes end to end on 4407 through the product's controls; the JPEG row needs B7-R1 in the controller to deliver a JPEG.
- `collab.presence-chips` (the product's half of C3T-F1, `core/share.spec.ts`): the two store fixes above; measured on the blob tier by the merge owner's preview runs.

### 6. Commands and results

| Time (UTC)       | Command                                                                                                                                                                                                                                                                    | Result                                                                                                                                                                                                        |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 00:50Z           | `cd packages/store && ../../node_modules/.bin/vitest run src/presence-store.test.ts`                                                                                                                                                                                       | 15 passed, 2 failed on the first draft of the new tests (the head count: `provenGet` makes its own head, so a read is two; the same-instance test's 60 s floor outlived the 30 s row): both the tests', fixed |
| 00:52Z           | `cd packages/store && ../../node_modules/.bin/vitest run`                                                                                                                                                                                                                  | 15 files, 196 passed after the fixes (the file: 17 passed)                                                                                                                                                    |
| 00:52Z           | `cd apps/studio && TURBOSLIDE_SESSION_SECRET=<40 chars> ../../node_modules/.bin/vitest run`                                                                                                                                                                                | 58 files, 366 passed (`tokens.test.ts` 10, `presence-leave.test.ts` 1 among them)                                                                                                                             |
| 00:53Z           | `node_modules/.bin/tsc -b packages/store`, `tsc -b packages/realtime`                                                                                                                                                                                                      | exit 0 both (the realtime channel still calls `leave(deckId, clientId)`, which the optional clock allows)                                                                                                     |
| 00:53Z           | `node_modules/.bin/tsc -b packages/store apps/studio`                                                                                                                                                                                                                      | blocked: `packages/chrome/src/menus/model.ts` (B1's, mid edit) fails with 15 `TS1109: Expression expected` at 1143 to 2140 and `-b` stops before the studio                                                   |
| 00:53Z, 01:02Z   | `node_modules/.bin/tsc -p apps/studio/tsconfig.json --noEmit`                                                                                                                                                                                                              | exit 0, no error (the studio against the referenced packages' built declarations; the workaround while B1's file is in progress)                                                                              |
| 00:54Z           | `node_modules/.bin/prettier --check` on the nine files this lane edited                                                                                                                                                                                                    | clean                                                                                                                                                                                                         |
| 00:55Z           | the dev server on 4407 (`vite dev --port 4407 --strictPort -c vite.no-watch.config.ts`, the env above with `TURBOSLIDE_TOKEN`)                                                                                                                                             | up in 1.2 s; `/new` 200 in 7.2 s (the first load)                                                                                                                                                             |
| 00:56Z           | the four `curl` readings of the route on `gt-brand`                                                                                                                                                                                                                        | 401, 401, 200 with the bearer, 200 for `?w=160` (table above)                                                                                                                                                 |
| 00:57Z to 01:00Z | `node <scratchpad>/b7-render-download-probe.mjs --base http://localhost:4407` under `.turboslide/e2e.lock` (three runs; the first two read the tab through the response event, which a download never emits, and were rewritten to read `download.url()` and a second GET) | run 3: 13 steps, 2 failed, both the JPEG row's format (the client's dropped `format`, B7-R1); every server side reading passed; the deck trashed, deleted forever, 404                                        |

### 7. Requests (Return round)

- **B7-R1 (B2 or the integrator; `apps/studio/src/editor/controller.tsx` 3087 to 3094, the `render.slide` handler).** The handler forwards `slideIds`, `themes` and `scale` and drops `format`, so `file.download.jpg`'s `{ format: 'jpg' }` (`editor-shell.ts` 2317) never reaches `renderSlideImages` and the JPEG row downloads a PNG named `.png` (measured on 4407 three times; the audit's urls for row 27 carried no `format=jpg` either). The change is one spread in the handler, `...(input.format !== undefined ? { format: input.format } : {})`, and `format?: 'png' | 'jpg'` on its input type; the server's `format` validator and the worker's JPEG at quality 92 are in place and the grant names the format, so nothing else moves. Without it `export.jpg.current-slide` delivers a PNG file and cannot pass as written ("a JPEG file of the current slide").
- **B7-R2 (B1; `packages/chrome/src/EditorShell.tsx`, the `render.slide` branch of the plan runner, the shipped 648 to 654).** `window.open(first, '_blank', 'noopener')` after the awaited render is a popup opened outside the click's activation: Chromium allows it while the transient activation lasts (about 5 s from the click) and blocks it after, and the render took 6 s on a cold worker on 4407 and 27 s under load (a cold serverless Chromium is in that range), so a seller can see nothing happen. The url now downloads, so the branch is better as the anchor the web page download uses (`controller.tsx` 921 to 929 `triggerDownload`: an anchor with `download=""` appended, clicked, removed), which needs no popup and takes the server's file name from `content-disposition`. Measured on 4407 with the tab: Chromium turns the tab's navigation into a download, closes the tab, and fires the download event on the editor page; with the anchor the same event fires with no tab at all. Keep the snackbar fallback for a url the pattern does not match.
- **B7-R3 (the integrator; `packages/realtime/src/channel.ts` 242 and `packages/realtime/src/blob.ts` 556).** `PresenceChannel.leave` gains the optional clock, `leave: (deckId: string, clientId: string, clock?: number) => Promise<void>`, and the blob channel passes it through, `leave: (deckId, clientId, clock) => deps.shared!.presence.leave(deckId, clientId, clock)`; `memory.ts` and `redis.ts` need no change (an optional parameter they ignore). Then `room.ts` `leavePresence` collapses to `room.channel.presence.leave(room.deckId, clientId, clock)` and `sharedRoster()` and the module state's `presence` leave with it; `presence-leave.test.ts` keeps its clockless and memory tier rows. The store's `leave(deckId, clientId, clock?)` already takes it.
- **B7-R4 (B4; `apps/studio/e2e/core/export.spec.ts`, the two rows).** What the driver meets on the tree: the rows sit behind Tools > Advanced tools until B1 lifts `advanced` from `file.download.jpg` and `.png` (`model.ts` 909 to 915), so the test turns the switch on as `surface.spec.ts` 75 to 79 does; the download event fires on the editor page (the popup that turns into a download is attributed to its opener), so `lib.ts`'s `download(page, () => menuPath(...))` reads it as it reads the PDF's; the file is `<deck id>-<slide id>.png` (the JPEG row's `.jpg` once B7-R1 lands); the render url is `download.url()` (a navigation that becomes a download emits no `response` event), and a second `page.request.get(url)` inside ten minutes reads the headers (`content-disposition`, `private, no-store`, `nosniff`); the url without `g` answers 401, so a negative reading is one request away. The first render of a fresh deck took 6 s alone and 27 s beside the gate's own renders on this machine; the row's 30 s bound holds on production's warm worker (the audit's 401 tabs answered at once), and a row that reads the second download of the same slide reads the worker's cache in under a second.
- **B7-R5 (the integrator; `apps/studio/src/server/export-sync.ts` 110 `jobFileUrl`, read with `apps/studio/src/routes/api/export.$deckId.ts` 156 to 166), an observation with its mechanism.** On a hosted store without a Blob client (the lanes' `TURBOSLIDE_STORE=tmp` servers) the synchronous export answers its downloads as `/api/export/<deckId>?job=<id>&file=<name>`, and that route requires the bearer on every request while `TURBOSLIDE_TOKEN` is set, the editor's anchor download included (the route's docblock says the editor never fetches it; `jobFileUrl` is the fetch). On this lane's 4407 with the token set, every export download row of the gate failed as `download.path: canceled` inside 4 to 9 s: the job ran (the log shows each file written), the anchor asked the route, the route answered 401 (`curl` of the `?job=&file=` form: 401 without the header, 404 "no job" with it), and Chromium reports a refused attachment as a cancelled download. Production is not affected (its copies are stored and served from the Blob store, `stored: true`), and no audit saw it, so nothing was written; the polled path already signs its files for the download route (`export-batch.ts` 668 `deps.sign(job.id, name)`), so the sync path's `jobFileUrl` taking the same `downloadUrl(signDownloadToken({ k: 'job', j, n }))` would serve a tokened instance without a Blob store. Until then a lane's server that sets `TURBOSLIDE_TOKEN` cannot measure the export rows (section 9).
- **B7-R6 (B4 with B1; `scripts/probes/new-write-probe.mjs` 132), an observation.** The probe's roster step clicks `[data-control="presence.more"]`; B1's uncommitted `presence.css` gives `.ts-presence-more.is-empty` `opacity: 0; pointer-events: none` (RETURN.md 4.3, the empty `+N` box no longer drawn), so on a deck with nobody else present the click waits 30 s on a visible, enabled, stable element that takes no pointer and the probe stops at its step 19 before its own cleanup. Read twice on 4407 (01:22Z beside the gate, 01:33Z on the quiet server), 18 steps ok each time up to it; the probe's roster read wants another door to the roster while the chip is empty (the row's own `presence.slot` press, or the window API's `presence` facts) and its cleanup wants a `finally`.
- **Observations for the integrator, no request:** the Plain Text row works on 4407 (a `.txt` with the title in 607 ms) and its production failure's mechanism was not seen here; Make a copy > Selected slides is the dialog's tab (`MakeCopy.tsx` 31 to 52) and the server's copy answers, as the audit read; both stay in section 8.

### 8. Deviations and hazards recorded

- `tsc -b apps/studio` cannot run while B1's `packages/chrome/src/menus/model.ts` is mid edit (15 `TS1109` at 00:53Z); the studio was typechecked with `tsc -p apps/studio/tsconfig.json --noEmit` against the referenced packages' built declarations, exit 0 at 00:53Z and 01:02Z. `tsc -b packages/store` and `tsc -b packages/realtime` pass.
- `parsePresenceRecord`'s `left` map now holds `{ at, clock? }` rather than the time alone; the one existing assertion moved with it. The b4 fixer's reading probe `docs/gslides-parity/focus/build/b4-presence-probe.mts` prints a tombstone as `<id>@<at>` from that map and will print `[object Object]` there until its template reads `.at`; it asserts nothing and is B4's file, not edited.
- The `leftPending` rule in `merged` and `reconcile` (section 3) is one rule beyond the two requests' text; it is what makes a leave visible on its own instance during the 5 s floor and what the same-instance test reads, and it is pinned by that test.
- The gate's `--only specs --spec export,decks` run on 4407 (section 9) reads B4's matrix in progress: rows B4 added with `core/decks.spec.ts` and `core/export.spec.ts` as their driver (`decks.file.template-gallery`, `export.jpg.current-slide` and the others of RETURN.md section 5) have no test yet, so `coverage()` in `lib.ts` 37 to 44 registers a failing "no test drives the matrix row" for each. Those reds are the matrix ahead of its drivers, not the server; the rows of the shipped matrix are read row by row in section 9.
- The dev server ran the memory tier: the C3T fixes are blob tier code and are measured here by their unit tests over the fake store; the deployment's measurement is the merge owner's two preview runs of `collab.presence-chips`.

### 9. The gate and the probes on 4407

| Time (UTC)       | Run                                                                                                                                                                                                           | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 00:59Z to 01:12Z | `node scripts/probes/core-gate.mjs --base http://localhost:4407 --only specs --spec export,decks --out <scratch> --matrix <scratch> --lock <scratch> --allow-scratch`, the server with `TURBOSLIDE_TOKEN` set | 55 rows: 32 passed, 23 failed, 0 not driven, 0 no step; retries zero; 774 s; verdict failed. The 32 passed are every decks row of the shipped matrix that has a test (`decks.home.*`, `decks.list.*`, `decks.card.*` with `make-a-copy` and `open-in-new-tab`, `decks.file.make-a-copy`, `decks.trash.*`, `decks.access.*`, `decks.notfound.page`, `decks.nav.back-forward`) and `export.pdf.notes-honest`. The 23 failed split in two: 10 are every row that downloads a file through the export dialog or the print page (`export.pdf.file`, `.include-skipped`, `export.pptx.perfect`, `.editable`, `.notes-and-skipped`, `images.export.pdf-with-picture`, `shapes.export.pdf`, `.pptx`, `export.print.download-pdf-follows-preview`), each `download.path: canceled` in 4 to 9 s with the job's file written in the server log: the export route's bearer rule refusing the anchor's request on a tmp store with the token set (B7-R5; not the product on production, not this lane's edits, which touch no export path); 13 are rows B4 added to the matrix this evening whose spec has no test yet (`decks.file.template-gallery`, `.open-upload-bundle`, `.import-slides-bundle`, `help.improve-link`, `export.zip.bundle`, `export.html.web-page`, `export.jpg.current-slide`, `export.png.current-slide`, `lines.connector.export-pptx`, `tables.export.*`, `charts.export.*`, `wordart.export.pdf`), each "no test in the spec drives the matrix row" from `lib.ts` `coverage()` in 0 to 2 ms |
| 01:19Z           | the server restarted without `TURBOSLIDE_TOKEN` (the same store, the same secrets' shape), `/new` 200 in 6.3 s                                                                                                | the render route open, as on a checkout; the grant urls still answered (the grant is appended whatever the rule)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 01:20Z to 01:29Z | the same gate command on the server without the token                                                                                                                                                         | 55 rows: 40 passed, 15 failed, 0 not driven, 0 no step; retries zero; 523 s. Every export dialog and print row of the shipped matrix but one passed: `export.pdf.file`, `.include-skipped`, `.notes-honest`, `export.pptx.perfect`, `.editable`, `.notes-and-skipped`, `images.export.pdf-with-picture`, `shapes.export.pdf`, `.pptx`, and every decks row that has a test (31). The 15 failed: the 14 rows of B4's matrix ahead of its drivers (the list of run 1 less the export dialog rows) and `export.print.download-pdf-follows-preview`, `broken` in the matrix today, red here on the print page's count (`print.pages` `data-count` read 2 where the row expects 3 with the skipped slide included), a print page reading and no server path. So the export path of the tree, `signDownload` and the download route included, passes on this server once the token is not set, and the 10 reds of run 1 were the token's (B7-R5)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 01:22Z, 01:33Z   | `node scripts/probes/new-write-probe.mjs --base http://localhost:4407` under `.turboslide/e2e.lock`, twice (beside the gate, then on the quiet server)                                                        | 18 steps ok each time (the double click on the placeholder, the first write creating the deck and moving the address, twelve sequential edits each acknowledged at the next revision, no stale message, no 404 on the draft thumbnail, Duplicate slide at the next revision, the slide switch keeping the editor, the client id and the one stream, the reload at the tab's revision), then step 19 stopped at `locator.click: Timeout 30000ms` on `presence.more` (B7-R6: B1's `is-empty` chip takes no pointer); the steps after it, the second tab and the cleanup among them, did not run. The two decks the probe left in this lane's private tmp store (`untitled-20260919-q9b4`, `-yk8g`) were removed from that folder at 01:35Z; the store is the scratchpad's, not a deployment's                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 01:34Z           | the server stopped                                                                                                                                                                                            | 4407 free; no scratch lock folder left; `.turboslide/e2e.lock` released after each of this lane's runs (the lock present at the return is another lane's, taken at 01:34Z); nothing under the repository's `decks/` from this lane                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

## Return round fix round

The B7 fixer's note of the return round fix round (2026-09-19, times in UTC), against `docs/gslides-parity/focus/VERIFICATION.md` "Return round, pass 1" R.5, the four findings the task named for this lane: R1-F1 (`collab.presence-chips`, severity 3 here: a red row of an unparkable feature in both gate runs), R1-F3 (`decks.file.open-list-search` flaky), R1-F4 (`decks.file.import-slides-deck` flaky, a copy left per red) and R1-F13 (burst run 2, write 55 answered `stale` while the store took it). The files edited are `packages/store/src/presence-store.ts`, `packages/store/src/blob-store.ts` and their tests `presence-store.test.ts` and `hosted.test.ts`; nothing else. Every uncommitted fix of the stream fix round two pipeline stands (`comments-store.ts`, `share.spec.ts`, `room.ts`, `images.spec.ts`, `InlineText.tsx`, the render worker queue, the mcp test budget, `.prettierignore`, `versions-by-author.spec.ts`). No git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran; the dev server was 4407 (`TURBOSLIDE_STORE=tmp` under a private `TMPDIR` in the scratchpad, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, two 48 character secrets and a `TURBOSLIDE_TOKEN` from `openssl rand`, `vite.no-watch.config.ts`, `--strictPort`), stopped before the return; Playwright and the walk ran against it under `.turboslide/e2e.lock` (0 waits, taken 16:01:32Z, released 16:02:47Z). Every scratch deck this lane made on the preview was trashed and removed through the window API and answered 404 on `/edit/<id>` with nothing left under its prefix (six decks: `untitled-20260919-rnjz`, `-b46f`, `-5ch0`, `-hnst`, `-wgl9`, `-n2cz`). No token, cookie or secret was printed.

### 1. R1-F1, `collab.presence-chips`: what the preview measures and what the store did wrong

Read first hand on the enforce preview `turboslide-hkne1i4o2` with the scratchpad script `presence-probe.mjs` (two or three Playwright contexts with the preview header, the owner's roster through `describe().state.presence.others`, the record `decks/<id>/.turboslide/presence.json` read proven through the immutable copy under the head's etag, `vercel logs` for the request log), five runs between 15:19Z and 15:34Z:

1. The leave beacon never reaches a preview behind Vercel Authentication. The beacon is a keepalive `POST /presence?leave=1` the room client sends at `pagehide` (`room-client.ts` `stop`, `controller.tsx` `postPresence`). Playwright's `extraHTTPHeaders` carries `x-vercel-trusted-oidc-idp-token` on every request of a live page, a keepalive fetch included (a keepalive `POST /presence` with the body `{}` from the live second tab answered the app's 400 `invalid`), and not on a request issued as the page unloads: in four runs no tombstone reached the record within 42 to 57 s of the navigation to `about:blank`, and a keepalive presence set with clock 777 registered on `pagehide` beside the app's beacon never landed either (run 5: the second tab's row stayed at clock 1 in the record 5, 10 and 20 s after the navigation, proven reads), while the same set from the live page had landed within a second. The cookies a context holds on the preview (`__vdpl`, the app's `__Host-ts_id`) do not pass the wall: a `POST /presence?leave=1` with both cookies and no header answered 401 "Protected deployment". Production has no wall, and on the memory tier (4407, 4409, check 32) the row passes. So on a preview the row measures the wall, not the leave, the same class as R1-F5's four downloads; the verifier's two runs cannot decide it and a production run does. The spec's `before` reading needs no change: the row already waits for the owner's tab to show the second tab's chip before the tab leaves, and judges that chip alone.
2. The product's half, fixed here: when the beacon is lost, the row should still leave at the record's 30 s, and it did not when the collaborator's set had landed on the owner's stream instance. `presence-store.ts` `reconcile` dropped an expired local row without a `leave` (`if (alive(local, t)) publish leave` guarded it), and `confirm` (the pulse did not move) never reconciled, so an expiry was announced only when the pulse next moved and, for a local row, never. Runs 3, 4 and 5: the owner's tab listed the second tab 76 s after it left, its row long gone from the record (the record held only the owner's row at +42 s); run 2, where the set had landed on another instance, dropped it at 26 s through the announced path. This is C3T-F1's "the chip leaves at the record's row life, not at the leave" with the row life itself not announced. Now an expired local row announces `leave` to this instance's listeners unless the record holds a live row of the same client from another instance (its heartbeats moved there; that row is announced in its place), whatever the read proved, and `confirm` runs `reconcile` on the tick with no store call, so an expiry is announced within the 2 s tick on a quiet deck too. The budget stands: no read or write was added to the poll or the push.
3. On production (the beacon lands) the leave's path was read and is unchanged: a leave on the owner's stream instance publishes at once; a leave on another instance pushes its tombstone at the floor, the pulse moves, the owner's poll reads it, `buried` drops the local row and the leave is published, about 7 s.

Pinned by two rows in `presence-store.test.ts`: "announces the leave of its own row that ran out its life, on the tick when the pulse stands still and on the poll (R1-F1)" (one instance, the owner heartbeating and the collaborator silent; `confirm` at 30 s announces the one leave with no store call; the poll path on a second instance the same) and "keeps a client whose heartbeats moved to another instance: its expired row here is replaced by the record's live row and no leave is announced".

### 2. R1-F3 and R1-F4, the blob tier's listing: 552 heads for 65 decks

Measured against the shared store through the store library alone (`listing-timing.mts`, reads only, the collection over a scratch overlay): the listing made 1 `folders`, 552 `head` and 64 `get` calls for 48 live decks, 16.4 s cold and 13.5 to 13.9 s warm from this machine. `folders('decks/')` answered 554 folders for 65 decks: 489 folders without a manifest, the leftovers of removed decks, holding 3,381 blobs, all of them the presence state (`.turboslide/presence.json` 425, `.turboslide/presence/<md5>.json` 1,609, `.turboslide/pulse.json` 366) and a handful of `.thumbs/` files. Their source is `remove()`: it deletes the manifest, lists the prefix and deletes what the listing shows, and the listing lags the store by up to a minute (the note on `pull`), while the presence record, its copies and the pulse are refreshed every few seconds by any tab open on the deck, so the files written in the last minute were never in the listing; an instance with a cached room whose tab heartbeated after the removal wrote the record and the pulse back as well. Every listing on every instance then headed each of those folders, four at a time, which is the 4.5 to 5.5 s the integrator read at best and the 26 to 30 s tail under load; run 1's "0 decks listed after 30028 ms" is one deck's read past its 10 s deadline failing the whole listing.

The fixes, `blob-store.ts`:

- `cardOf`: the listing keeps each deck's card by the manifest etag it was proven from (`listed`), so a warm listing costs one head per deck and no body; a card from an unproven body (the CDN's state before the last push) is not kept, so the next listing tries the proof again as before; `manifestAtHead` replaces `currentManifest` and reports the proof.
- The listing's calls run under their own deadline `BLOB_LISTING_READ_TIMEOUT_MS` (4 s), and a store answer of "not now" for one deck (`isStoreBusy`: a 429, a 5xx, the deadline) answers the card proven last, or leaves that deck out of the one listing with a log line; the whole listing no longer fails on one deck.
- `phantoms`: a folder without a manifest is not headed again for `LISTING_PHANTOM_TTL_MS` (5 min) on the instance; `create` and `copy` forget the id at once, so a deck made under a leftover's id lists here at once and elsewhere within five minutes.
- `remove()` deletes the presence record, the pulse and the current record's copy (named by the record's version, read before the record goes) by name beside the listed files, and lists the copies folder on its own; `listed` forgets the deck.
- `presence-store.ts` `pushNow`: a record this instance knew that is gone (`sawRecord`, the head null and proven) costs one manifest head; when the manifest is gone too the pending rows are dropped, the miss is remembered for `PRESENCE_GONE_MEMORY_MS` (60 s) and the heartbeats of a tab that has not noticed cost nothing until then; a record that vanished alone with the deck there pushes as before. The first push of a deck's life is untouched (no manifest head), so the budget of the tests and the poll stands.

The store itself: the 489 leftover folders were swept on 2026-09-19 at about 15:58Z with the scratchpad script `phantom-sweep.mts` (a dry run first at about 15:52Z: 490 folders, 2,390 state blobs, 1 skipped): under each `decks/<id>/` folder whose `deck.json` head answered null, read again right before the delete, and holding nothing but `.turboslide/**` and `.thumbs/**` (a create pushes its bodies before its manifest, `pushDeckDir`, so a folder with any other file is skipped), every blob was deleted: 495 folders, 2,409 blobs, in 80 s; `folders('decks/')` answers 67 now (66 decks and one skipped folder, section 5). The listing measured after: 1 `folders`, 67 `head`, 66 `get`, 2.9 s cold; 66 `head`, 0 `get`, 1.7 to 1.8 s warm from this machine, which is a Vercel instance's about one second warm. Deviation 1 records the sweep.

Pinned in `hosted.test.ts`: "lists from one head per deck once a card is proven, reads a body for a deck whose etag moved alone, and lists on through one deck the store refuses (R1-F3, R1-F4)" and "removes the presence record, its copies and the pulse with the deck although the listing lags, and heads a folder without a manifest once in five minutes (R1-F3)"; in `presence-store.test.ts`: "writes nothing back under a removed deck (R1-F3)".

### 3. R1-F13, write 55 answered `stale` while the store took it: read, not fixed here

`room-client.ts` and the ops route are not this lane's files; the reading is a request (section 4). The evidence is the tab's alone (`verification/return-p1-burst-run2.json` row `write 55`: 1,282 ms, `r 63 -> 64`, server 64, pending 0, `ConflictError: The change was not accepted (stale)` from `controller.tsx` 1997, the window API's `write` settling with `rejected`; the run's console holds one CSP line). The client marks an op `stale` in four places (`fold` when the op no longer applies, `rebasePast`, a remote op's `transformPast` answering null, the answer's `rejected`), and the write landed (the revision moved, the run ended at 72 with 23 slides like runs 1 and 3). Two mechanisms fit the client's view; the 1,282 ms rules out a resend after a 503 (a `retry-after` of at least 1 s plus the resync would take over 2.3 s):

- The store echo of the tab's own write reaching the tab before the POST's answer, from the pulse poll of a stream instance that is not the write's, and `settleOwnEcho` not recognising it: the echo carries `clientId: 'store'` and the record's mutations, the match is `entry.rev >= row.base && row.folded === folded` with both sides `JSON.stringify`; a record whose mutation bytes differ from the client's fold (the server's zod parse of the ops post rebuilds the mutation objects in schema key order, `landCandidate` answers `placed.mutations`) fails the match, the echo is taken as another author's op, the pending `slide.insert` is transformed past its own copy, `transformPast` answers null and the op is returned `stale`; the answer then finds the op settled and `held` keeps the document right. A `canonicalJson` on both sides of the match (or a match on `rev` and the record's author when the fold differs) closes it.
- The server's retry after a lost race that was not lost (`admitOps`, the `BLOB_APPEND_RETRIES` loop): a re-placement on the head that already holds the slide, `landCandidate` refusing it as `invalid`, `blobRefusal(63, 64, invalid)` answering `resync`, the client resyncing to 64 and `fold` returning the pending op `stale`. Less likely on the timing and with no concurrent writer in the burst.

### 4. Requests (Return round fix round)

- **The integrator (the ship note, RETURN.md section 2 by request, the parked list).** (a) `collab.presence-chips` measures the authentication wall on a preview (section 1 item 1) and is decided on production or on a deployment without the wall, the way R1-F5's four downloads are; no row of this lane re-parks, `share` stays unparkable, and the product half is fixed on the tree. On a preview the row's outcome after the fix depends on the second tab's heartbeat phase (the row expires 25 to 30 s after the navigation and the tick announces it within 2 s), so it is not a measurement there either way. (b) A fresh enforce preview from this tree measures `decks.file.open-list-search` and `decks.file.import-slides-deck` against the swept store with the listing cache; on this machine the listing went from 13.5 to 16.4 s to 1.7 to 2.9 s. (c) `presence-store.test.ts` and `hosted.test.ts` are this lane's tests; the realtime package's `blob.test.ts` (drives `confirm` through the pulse tick) and the studio's `presence-leave`, `blob-admission`, `live-read` and `retire-clients` tests were run and pass.
- **B4 (`scripts/probes/core-walk/areas/decks.mjs`, the import row).** The setup copy's removal sits after the pick, so a `waitControl('dialog.importSlides.deck.<copy>', 10_000)` that times out leaves the copy on the store (R1-F4's `import-source-20260919-cd2a` and `-8ciz`); the trash and remove of the copy should run in a `finally` around everything after the copy is made. The pick's bound: the copy is made through this instance's window API and lists at once on that instance (the mirror join), but a picker whose listing lands on another instance sees it only once `folders('decks/')` shows the folder, up to a minute later; a second open of the dialog after 5 s, or the bound raised toward the listing's lag, would make the row read the product and not the store's listing.
- **The room client's owner (`packages/realtime/client/room-client.ts` `settleOwnEcho`, `foldOf`) with the stream fix round two lanes.** Section 3: a unit test in `room-client.test.ts` where the store echo of the tab's own POST arrives before the answer with the record's mutations in another key order (the server's zod parse) would show whether `row.folded === folded` holds; if it does not, compare through `canonicalJson` on both sides. The second mechanism (the server's retry loop) is the integrator's or B7's to read in `room.ts` `admitOps` with a server log of such a write.
- **The verifier.** `decks/untitled-20260919-cd2a/` (the verifier's run 1 walk deck) holds `access.json` and its assets with no `deck.json`: the hand removal of 13:37Z deleted the manifest and the listing lag hid the rest. It is not a deck the product lists and it is not this lane's to delete; the sweep skipped it and named it.

### 5. Deviations and hazards recorded

1. A write to the shared Blob store outside any deck: the sweep of section 2 deleted 2,409 blobs under 495 `decks/<id>/` folders that held no manifest and nothing but presence state and thumbnails, with the manifest head read again before each delete. The preview and production share the store, so the sweep reaches both; no deck, trashed or live, was touched (a trashed deck keeps its manifest).
2. `LISTING_PHANTOM_TTL_MS`: a deck made under a leftover folder's id on another instance lists there within five minutes, not on the next load; on the instance that made it, at once. Deck ids from `/new` are dated random slugs, so the case is a named deck made again under a name a removed deck had.
3. The presence copies an instance wrote are deleted by that instance once they are a minute old; an instance frozen or gone before then leaves them (1,609 of the 3,381 stray blobs were copies). Under a live deck they cost a listing nothing (the listing heads the manifest alone) and `pull` lists them with the prefix; a sweep of copies older than the record's life is for a later round, not added here (a `list` per push would be a read on the push path).
4. `pushNow` heads the manifest once when a record this instance knew is gone: one read per removed deck per instance, none on the first push or the poll path.
5. The three `chrome.*` and `inbox.*` misses of the burst evidence are untouched; the burst JSON's `consoleLines` holds one line (the CSP notice), so R1-F13's mechanism is read from the client's row alone (section 3).
6. The matrix rows run here: `collab.presence-chips` on 4407 (memory tier) passed in 5.3 s; the walk `--core --only decks` on 4407 drove 19 steps, 19 ok (`decks.file.open-list-search` "2 decks listed after 1 ms", `decks.file.import-slides-deck` "copy import-source-20260919-y7w5 offered 2 slides; slides 2 -> 4; the copy answers 404"); the exit 1 is the run's "no step" for the other areas of a `--only` run. Neither drives the blob tier, whose evidence is the store tests and the listing measurement of section 2.

### 6. Commands and results

| When (UTC)       | Command                                                                                                                                                                                                                                            | Result                                                                                                                                                                                                                                                                      |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 15:19Z to 15:34Z | `node <scratchpad>/presence-probe.mjs https://turboslide-hkne1i4o2-kl01s-projects.vercel.app all` (three runs), `... wall2` (one run), plus `vercel logs -d <preview> --since 15m -j`                                                              | Section 1: no tombstone within 42 to 57 s in any run, the keepalive set at `pagehide` never landed, the owner's tab listed the second tab 76 s after it left when its set had landed on the owner's instance; a header-less request with the context's cookies answered 401 |
| 15:38Z           | `node_modules/.bin/vitest run src/presence-store.test.ts` (packages/store), `tsc -p packages/store/tsconfig.json --noEmit`                                                                                                                         | 19 passed after the R1-F1 fix; exit 0                                                                                                                                                                                                                                       |
| 15:41Z           | `node_modules/.bin/vitest run` (packages/store), `node_modules/.bin/tsc -b`                                                                                                                                                                        | 199 passed; exit 0 (the listing cache)                                                                                                                                                                                                                                      |
| 15:49Z           | `node <scratchpad>/listing-timing.mts` (the store library over the shared store, reads only)                                                                                                                                                       | before the sweep: cold 48 decks 16,378 ms (1 folders, 552 head, 64 get); warm 13,867, 13,549, 13,694 ms (552 head, 0 get)                                                                                                                                                   |
| 15:51Z           | `node <scratchpad>/phantoms.mts` (reads only)                                                                                                                                                                                                      | 554 folders, 65 real decks, 489 without a manifest, 3,381 blobs: presence records 425, copies 1,609, pulses 366, a few thumbnails                                                                                                                                           |
| 15:52Z           | `node <scratchpad>/phantom-sweep.mts` (dry run)                                                                                                                                                                                                    | 557 folders, 66 decks, 490 leftover folders holding 2,390 state blobs, 1 skipped (`untitled-20260919-cd2a`, `access.json` and assets)                                                                                                                                       |
| 15:55Z           | `node_modules/.bin/vitest run` (packages/store) after the removal sweep, the phantom memory and the gone deck guard; two of this lane's new rows red on their own setup (a heartbeat inside the half life, a copy the held listing could not show) | fixed in the tests (the heartbeat at 16 s, the current copy named by the record's md5 and an older one the listing shows); 87 passed in the two files at 15:57Z                                                                                                             |
| 15:58Z           | `node <scratchpad>/phantom-sweep.mts --apply`                                                                                                                                                                                                      | 495 folders, 2,409 blobs deleted in 80 s, 1 skipped; `folders('decks/')` answers 67                                                                                                                                                                                         |
| 15:59Z           | `node_modules/.bin/vitest run` (packages/realtime); `vitest run src/server/presence-leave.test.ts src/server/blob-admission.test.ts src/server/live-read.test.ts src/server/retire-clients.test.ts` (apps/studio)                                  | 116 passed; 26 passed                                                                                                                                                                                                                                                       |
| 16:00Z           | `node <scratchpad>/listing-timing.mts`                                                                                                                                                                                                             | after the sweep and the cache: cold 50 decks 2,870 ms (1 folders, 67 head, 66 get); warm 2,146 ms (66 head, 2 get: two etags moved), 1,764 and 1,748 ms (66 head, 0 get)                                                                                                    |
| 16:01:32Z        | the lock taken (0 waits); `PLAYWRIGHT_BASE_URL=http://localhost:4407 node_modules/.bin/playwright test apps/studio/e2e/core/share.spec.ts -g "collab.presence-chips" --reporter=list`                                                              | 1 passed in 5.3 s (22.5 s with the file's setup)                                                                                                                                                                                                                            |
| 16:01:55Z        | `node scripts/probes/editor-walk-probe.mjs --core --base http://localhost:4407 --only decks --json <scratchpad>/walk-decks-4407.json`; the lock released 16:02:47Z                                                                                 | 19 steps, 19 ok, 0 failed, 0 not driven, 52 s; the two rows of this lane green (deviation 6); exit 1 on the other areas' "no step" of a `--only` run                                                                                                                        |
| 16:03Z           | `node_modules/.bin/tsc -b`; `node_modules/.bin/prettier --check` on the four edited files; `git status --short -- packages/store/src/`                                                                                                             | exit 0; "All matched files use Prettier code style!"; the store's changes are this lane's four files plus the stream fix round two's `comments-store.ts` and its test, untouched                                                                                            |

### 7. Environment at the return

- The dev server on 4407 (pid 22219) was stopped at 16:03Z; nothing listens on 4407. `.turboslide/e2e.lock` is absent. `decks/` holds `fixture`, `gt-brand` and `templates`.
- The shared Blob store lists 66 decks and one skipped leftover folder (section 4, the verifier); every scratch deck of this lane is gone (404 on `/edit/<id>`, 0 blobs under the prefix, read after each run).
- The preview OIDC token in `.turboslide/vercel-dev.env` (expires 2026-09-19T20:26Z) was read into the probes' processes and never printed; the Blob token in the same file was read by the timing, diagnostic and sweep scripts the same way. The agent bearer of `~/.config/turboslide/hosts.json` was not used.
- The scratch of this lane is in the session's scratchpad (`presence-probe.mjs`, `listing-timing.mts`, `phantoms.mts`, `phantom-sweep.mts`, the logs), not in the repository.
