# B5, the matrix and the drivers

## Vector round

Builder B5 of the vector round (docs/VECTOR.md section 6; the "Owns" list of 5.1), on the shared worktree `/Users/kevinliu/repos/Turboslide-vector` at `2be923e` on 2026-09-24 while B1 to B4 landed beside it. The dev server was port 4425 (`TURBOSLIDE_STORE=tmp` under a private `TMPDIR` in the scratchpad, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, `TURBOSLIDE_ASSIST=fixture`, `TURBOSLIDE_LOGO_UPSTREAM=fixture`, two fake secrets of 48 characters, `vite.no-watch.config.ts`; restarted before a run so it served the tree as it stood, since the no-watch config never reloads another lane's landed file; stopped before the return). The walk probe and Playwright ran against 4425 under `.turboslide/e2e.lock` (taken with `until mkdir`, released after each run; no lock is held at the return). No git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran. Every scratch deck was made on the tmp store of 4425 and trashed and deleted forever by the walk's cleanup or the specs' teardown; nothing was created on a deployment. No token, cookie or secret was printed. Times are PDT.

This file carries the requests (section 1, written first), the items landed with their files and hunks (section 2), the rows this lane covers and what each run read (section 3), the commands run (section 4) and the decisions and the corrections to VECTOR.md 6.1's texts (section 5).

### 1. Requests

R1 (the integrator, `packages/chrome/src/menus/model.ts`): nothing beyond B1's R1. Every `parks` id of the new rows is a literal of `model.ts` or `toolbar-tails.ts` today (`insert.shape.gallery`, `.arrows`, `.callouts`, `.equation`, `toolbar.changeShape`, `format.changeShape`, `format.image.maskImage`) or one of the six ids VECTOR.md 4.8 declares, which `DECLARED_CONTROL_IDS` holds from day 0, so the matrix validates on the tree the lanes started from and after B1's rows land.

R2 (the integrator, `scripts/probes/editor-walk-probe.mjs` 1462 and 1936, and `apps/studio/e2e/objects.spec.ts` 283 to 291; B1's R4 first bullet names them): the parity walk and the objects spec hover `insert.shape.shapes`, the container B1's R1 removes. Neither file is this lane's (the "Owns" list names `core-walk/**` and `e2e/core/**`); the change is the one B1 wrote: `['insert.shape', 'insert.shape.shapes.rectangle']` for the named row, or `['insert.shape', 'insert.shape.gallery', 'insert.shape.gallery.pick.rect']` through the gallery, and the container click dropped in the objects spec. The core walk's own driver of `shapes.insert.named-rows` tolerates both trees (section 2.3).

R3 (B2, `packages/schema/src/shapes.test.ts`): `node_modules/.bin/tsc -b` stops at the schema package on B2's test, which still imports `DRAWN_PRESET_IDS`, `isDrawnPreset` and `roundRectRadius` (B4's note reads the same at 18:15Z and 18:26Z); this lane typechecks its spec files with `tsc -p apps/studio/tsconfig.json --noEmit`, which is clean, so nothing here waits on it. Named so the integrator's `tsc -b` at the merge reads it as B2's.

R4 (docs/VECTOR.md 6.1, the specification agent or the integrator at the ship note): five row texts of 6.1 state a number the tree contradicts; the matrix carries the tree's facts (section 5) and the document's table should follow: the Shapes plate holds 99 tiles with 94 distinct glyphs (not 100 and 95), ECMA `star5` puts the inner radius at `adj / 50000` of the outer (60 percent at Adjust 30, not 30), `cloudCallout` draws its cloud with arcs (no cubic), `curvedRightArrow` shades with `darkenLess` at 0.15 (not `darken` at 0.3), and the three named Insert > Shape rows place the legacy kinds drawn as `rect` and `ellipse` elements, so the pinned three paths are read on the preset tiles of the Shapes plate.

R5 (B4 or the integrator, `apps/cli/src/assets.ts` `inlineAssets` 119 on and `packages/render/src/slide.ts`'s standalone resolver): the standalone build inlines "every twin of every asset as a data URI, keyed by the twin path renderStandalone looks up"; a vector asset's file is `asset.vector.neutral` (or its light and dark files), not a twin, so the page's `<img src="assets/<id>.svg">` stays a relative path and the downloaded html draws nothing for the picture (`svg.export.web-page` red: one img source `assets/svg-picture-asset.svg`, no data URI). The hunk: `inlineAssets` walks `vectorOf(asset)`'s files beside the twins (the `.svg` branch of `inlineAssetFile` already writes `image/svg+xml`), so the standalone page's resolver finds the vector path in `uris`. VECTOR.md 4.6 read "No change" for the web page; the tree says otherwise.

R6 (the integrator, `packages/render/src/stage.ts` 92 to 101, B4's R7 restated because a row of this lane reads it): the frame band's `bandAssetResolver` reads `asset.twins` and never the vector, so the kit's footer logo travels as the PNG twin in the print document and the PDF page holds an image XObject for it (`logos.export.svgblip`'s last claim red: 2 image XObjects on the title page). Two lines: `vectorOf(asset) ?? asset.twins`.

R7 (the integrator, `packages/chrome/src/menus/model.ts` and `editor-shell.ts`, B1's R1.5, R1.6 and R2 restated because a row of this lane reads them): with the crop row's `disabledReason` and the `rasterPictureSelected` predicate absent, Format > Image > Crop image stays enabled on an svg picture while the toolbar's Crop speaks the sentence; `svg.render.picture-gestures` reads the menu row's state and stays red on it.

### 2. What landed

Each item is one commit's worth; the files are this lane's alone.

2.1 The matrix (`docs/gslides-parity/focus/core-matrix.json`): the 43 rows of VECTOR.md 6.1 in the file's row shape, with the interaction texts as the table gives them (five corrected to the tree's facts, section 5), `today` as the tree and the audits show on 2026-09-24 (a row of a control that does not exist is not driven with the document's reading as its evidence; a behaviour production shows red is broken with the audit's severity), `parks` as the table's last column (the gallery rows name their category row alone; every `svg` row names one or more of the six ids of 4.8; `shapes.geometry.pinned-three`, `.text-rect`, `shapes.icons.named-rows` and `shapes.insert.named-rows` carry none), `setup` on the rows that make a window API write before the driven steps (the labelled shapes, the three geometry shapes, the hexagon, the picture, the svg picture, the logo insert, the icon rows' objects), and `note` naming the round, the VECTOR.md section and the lane. `shapes.insert.named-rows` extended to the seven rows (its driver reads the seven ids in order). `logos.intake.svg-sentence` retired with its sentence (4.7); `svg.import.upload` measures the upload. The `$comment` extended. The matrix holds 827 rows (785 − 1 + 43): 595 probe, 226 spec, 6 cost probe. `docs/FOCUS.md` re-rendered (`node docs/gslides-parity/focus/render-focus.mjs`, then prettier) and the README section "What works today" (`node docs/readme/what-works.mjs`, then prettier; `--check` is green).

2.2 The module (`scripts/probes/core-matrix.mjs`, `core-matrix.d.mts`): `CORE_FEATURES` gains `svg` (parkable); `AREA_FEATURE` gains `menus: 'chrome'` (an icon row is the chrome's, unparkable); `ROW_FEATURE` drops the retired row; `CORE_SPEC_DRIVERS` gains `core/svg.spec.ts`; `DECLARED_CONTROL_IDS` gains the six ids of 4.8 (`intake.svg.upload`, `.paste`, `.drop`, `.url`, `picture.svg.copy`, `export.svg.vector`); the header names the round; the declarations follow (`CoreFeature`, `CoreSpecDriver`). `core-matrix.test.mjs`: the counts it pins (827 rows, 8 logos spec rows, 23 logos rows, ship one's 70 rows with 20 P1 after the retirement, `ROW_FEATURE` with two entries), the partial run expectation narrowed to the parkable features that have a row without `parks` (svg has none by design, 6.1), and a new describe for the round: the feature, the area, the spec driver, the 44 rows carrying the note (43 plus the extended one), every svg row's `parks` among the six ids, a `menus` row read as the chrome's and refused as a feature, a red icon row blocking and a red svg or gallery row parking its controls alone, the six declared ids known, and `--emit-parked` over a fixture run with the paste rows red writing `intake.svg.paste` into the parked set.

2.3 The walk (`scripts/probes/core-walk/areas/shapes.mjs`, `chrome.mjs`): `shapes.mjs` declares the 20 new probe rows and exports `parsePath` (an absolute path string's straight vertices, curve and arc end points and command letters, on the half pixel grid `fmt` writes); the vector block after `shapes.label.centred-default` imports `packages/schema/src/shapes.ts` under Node's type stripping and compares every drawn `d` with `shapePath` at the box less the stroke (`drawnSize`), reads the paths of a block with their attributes and computed paint (`pathsOf`, the `data-shape` and `data-adjust` attributes on the svg root or a descendant, `data-shade` on an overlay), the distinct geometry paths without the overlays (`geometryPaths`), a star's inner radius against the outer ellipse along each inner vertex's direction (`starRatio`, the ECMA `a / 50000` reading of b2.md request 3), the plates of a category (`openGrid`, `tilesOf`), a Format options field typed once its section is open and the panel's write has settled (`typeField`, `openPanelFor`), an insert from a grid tile by a click or a drag (`insertFromGrid`), Cmd+Z polled to an object count (`undoTo`), the switch turned on through `reachRow` while the galleries are parked and off again at the block's end (`advancedBack`), and a hexagon and a picture placed as setup writes for the sites, Change shape and Mask image rows. `shapes.insert.named-rows`'s driver reads the seven ids, the three before the four in the menu and in the toolbar Shape button's dropdown, and the container of the tree before the round when it is drawn. `chrome.mjs` declares the three `menus.*` rows and, before its closing `advancedBack`, reads every menu row's icon (`iconsUnder`: the svg in `.ts-menu-ic` with its viewBox, size, joined path, fill and stroke attributes, and any `use` or `img`), walks a menubar menu two levels down (`readMenu`), an object's right click menu one level down (`readContext`) and a toolbar dropdown (`readDropdown`), with a rectangle, a chart and a picture placed as setup writes for the Format rows their selection enables and removed after.

2.4 The specs (`apps/studio/e2e/core/`): `svg.spec.ts` (new): the twelve svg rows of the intake, the sheet, the gestures, the copy and the sanitizer; `export.spec.ts`: the three geometry export rows and the four svg export rows on identities of their own (`geometryOwner`, `svgOwner`: a deck built as setup writes, five downloads each at most); `logos.spec.ts`: `logos.export.svgblip`; `images.spec.ts`: the retired row's test and coverage entry removed; `lib.ts`: `fixtureBytes`, `svgFixture`, `dataUrlOf`, `pasteFile`, `pasteText`, `copyFromStage`, `dropFileAt`, `assetOf` (the asset record through `slide.get`'s `assets`), `placeSvgPicture` (the svg as a data URL through `asset.add { file }`, then a `shot` block, the upload path's kind), `pictureImg`, `snackbarText`, `fetchBytes`. The fixtures under `apps/studio/e2e/fixtures/`: `mark.svg` (a rect and a circle at 96 by 64, 489 bytes), `unsafe.svg` (a script, an onload, a foreignObject, an outside image), `data-image.svg` (a 1 by 1 PNG data URI image), `broken.svg` (an unclosed rect); the 2.1 MB file is built in memory.

2.5 The renderers: `docs/gslides-parity/focus/render-focus.mjs` gains the `svg` heading and its README paragraph; `docs/readme/what-works-data.mjs` the same paragraph verbatim (the test pins the two).

### 3. The rows and what each run read

Every reading is of the tree as it stood when the server was started for that run (section 4 names the times); the other lanes' files landed while these ran, so a later run can differ from an earlier one for their reasons. A row nobody drove is not driven, never passed.

3.1 The shapes area (the walk, `--only shapes`, three smokes; the third at 19:05 on the tree with B2's interpreter, B1's picker and icons module, B3's viewer and render files): 57 of the area's 42 declared rows plus the decks area's passed, 2 failed. Green: every geometry row (`shapes.geometry.shapes.hexagon-sheet`, `.star5-adjust`, `.pie-arc`, `.multipath-can`, `.flowchart-own-space`, `shapes.geometry.arrows.right-arrow`, `.curved-right`, `shapes.geometry.callouts.wedge-rect`, `.cloud`, `shapes.geometry.equation.plus-divide`, `shapes.geometry.pinned-three`, `shapes.geometry.sites`, `shapes.geometry.resize-keeps-adjust`; every drawn `d` equal to `shapePath` at the box less the stroke, the hexagon's six vertices, the pie's arc from the right edge to the top with a vertex at the centre, the can's three geometry paths in four elements with the lid's stroke none, the flowchart document inside its box with its curve, the right arrow's seven vertices and its shaft at 79.5 of 159, the curved arrow's four paths with arcs and the ink overlay at 0.15, the callout's one vertex outside the box at 69.5,179 for 69.7,178.9 wanted, the cloud's eleven arcs and three tail paths below and left, the plus's twelve vertices with the arm at 37.4, the divide's bar and two dots, the rectangle's box, the rounded rectangle's four arcs of 26.5 and the Corner field at 50 giving 79.5, the ellipse tile drawn as an ellipse element of 119.5 by 79.5, the star's inner radius at 60 percent with Adjust 30 and 38.2 at the default and the same 60 percent after the resize to 600 by 600 with `adjust` [30000, 105146, 110557] kept, the line's end on the hexagon's upper right site with six site marks and `connect.end.site` 5 and the rectangle's eight), the four grids (`shapes.insert.grid-shapes` 99 tiles with 94 distinct glyphs and the hexagon placed by Enter on the focused grid; `.grid-arrows` 26 of 26 with the left right arrow's two tips; `.grid-callouts` 4 of 4 with the oval callout's pointer; `.grid-equation` 6 of 6 with Not equal placed), `shapes.change-shape.plate` (135 tiles with the hexagon ringed from the toolbar and from the right click menu, Octagon written with eight vertices and `adjust` null, each Cmd+Z back to the hexagon) and `shapes.mask-image.plate` (the ellipse written, `data-mask` on the shot's crop child with a clip path of arcs, the Crop arrow opening `format.image.maskImage.pick.ellipse`, Reset image clearing it). Red: `shapes.insert.named-rows` (the three rows still under the `insert.shape.shapes` container and no category row in the default view: the integrator's `model.ts` rows of B1's R1 were not on the tree) and `shapes.icons.named-rows` (the seven rows drew an empty icon slot: the same). The galleries were reached with Tools > Advanced tools on (the rows still carry `advanced` on this tree) and the switch was turned off again at the block's end.

3.2 The chrome area (the walk, `--only chrome`, two smokes; the second at 19:15): `menus.icons.insert-rows` red (13 of 39 named rows draw a glyph; the 26 without draw an empty slot: the integrator's `model.ts` icons of B1's R1.2 not on the tree), `menus.icons.format-rows` red (4 of 24 Format rows and none of the four context rows draw a glyph, the same mechanism; the Replace image submenu's four rows read with the picture selected), `menus.icons.one-family` red on its line kinds half alone (over the ten menus and the three toolbar dropdowns 262 rows were read, 61 with an icon, none off the family; the eight line kinds draw no glyph yet). The area's own rows read as before (one red, `chrome.field.boundary-3-1`, a broken row of the return round).

3.3 The svg spec (`core/svg.spec.ts`, 19:00 on the tree with B3's intake, viewer, clipboard and render files): 11 of 12 passed. `svg.import.upload` (the picture in 388 ms at 688 by 458, the img `assets/mark.svg`, the asset `kind: 'svg'`, `vector.neutral` `assets/mark.svg`, the twin `assets/mark.png`, scale 3), `.paste-file`, `.paste-markup` (the prolog form and the html form each a vector picture, "hello <svg>" landing text and no picture), `.drop` (the box holding 1000, 600; the PNG replaced in 118 ms with its box kept), `.url` (`/api/logo/mark/figma/default.svg` in 378 ms, alt "default"), `svg.render.vector-at-zoom` (the svg at 100 and 200 percent, no PNG request of the asset, the show and the viewer drawing the same file), `svg.copy.markup` (`text/plain` the markup, `text/html` the envelope comment, Cmd+V a second block 16 px right and down on the same asset), the four sanitizer rows (`removed` ["script", "foreignObject", "image"]; the data URI image kept; the 2,202,010 byte file refused in 386 ms with the cap sentence; the broken file refused in 438 ms with its sentence). Red: `svg.render.picture-gestures` on two of its nine checks in that run, the border weight (the driver's selector, fixed since: the tail's list rows are `menu.toolbar.borderWeight.weight-<n>`) and Drop shadow (a context only row, driven through the right click menu since), and on the crop check: the toolbar Crop's click said the sentence (B1's `ToolbarTail.tsx`) while the Format > Image > Crop image row was not disabled (the `rasterPictureSelected` predicate and the row's `disabledReason` are the integrator's `model.ts`, B1's R1.5 and R1.6, and `editor-shell.ts`, R2: not on the tree). The move from inside (80 by 40), the se handle with Shift (the aspect 1.502 kept), the rotation (15), the mask (the ellipse clip path on the shot's crop child), the PNG drop (the asset swapped, the box kept) and every undo read green.

3.4 The export and logos rows (`core/export.spec.ts` and `core/logos.spec.ts` over the round's rows, 19:20 on the tree with B4's builder): `shapes.geometry.text-rect` green (the rounded rectangle's layer 7.80 px in, the arrow's on the shaft at 40 and 40; the file's `lIns` and `rIns` 0 for both, the vertical pair carrying the exporter's first baseline shift of 4.48 px on both shapes, section 5 item 11), `shapes.geometry.export.pptx-prst-avlst` green (`prst="hexagon"` with an empty list, `star5` with `adj` 30000 first then `hf` and `vf`, `wedgeRectCallout` with `adj1` −20833 and `adj2` 62500, in 3.8 s), `shapes.geometry.export.raster-modes` green (the Perfect file in 4.8 s, `perfect` true, three pages at 0.000; the PDF in 2.6 s, `passed` true, three pages; no verify entries on this machine, where the PDF gate is the page count), `svg.export.pdf-vector` green (2 pages, 0 image XObjects, `passed` true), `svg.export.pptx-svgblip` green (`ppt/slides/slide2.xml` carries the ext with `asvg:svgBlip` to `../media/ts-split-1-svg-picture-1.svg`, 250 bytes opening with `<svg xmlns=`, beside `../media/image-2-1.png`; the content type default present). Red or not driven: `svg.export.pptx-fallback` (the first half green: the blip a PNG whose IHDR width is the block's 480 times 3; the second half not driven, `export.run` refusing the key `svgVector`: the integrator's header field, B4's R1, not on the tree; the row reads not driven with that reason), `svg.export.web-page` (the html's one `<img>` source is `assets/svg-picture-asset.svg`, a relative path, and the page carries no data URI at all: the standalone build inlines twin paths alone, request R5), `logos.export.svgblip` (the deck's asset count grew by one; the Editable text file's `ts:title#footer-logo:1` and `#title-logo:2` each carry `asvg:svgBlip` to their `.svg` part beside a PNG of 39 by 54 and 171 by 255; the Perfect file's two logo objects the same; the PDF page holds 2 image XObjects, the footer band's PNG twin and its mask, which is B4's R7 to the integrator: `stage.ts` `bandAssetResolver` reads `asset.twins` and never the vector).

3.5 The gate over the four spec files (`--only specs --spec svg,export,logos,images`, 19:24 to 19:46, 1331 s): 75 rows judged, 70 passed, 3 failed, 2 not driven, 0 no step; retries zero; the verdict red on `logos.index.cached-offline` (not driven: the fixture upstream, not the outage; a ship one row of the logos spec, exempt by ship one's rule), `logos.export.svgblip` (failed on the driver's object filter, which took the GT sprite mark `ts:title#mark:1` for a logo object once the spec's earlier rows had filled the deck; fixed since to the names of R5 of b4.md), `svg.render.picture-gestures` (failed on the frame read, which looked at the shot's crop child while an unframed shot draws its frame on the img, and on Drop shadow, which a picture's right click menu does not carry: both the driver's, fixed since; and on the crop row's state, R7), `svg.export.pptx-fallback` (not driven, the header field) and `svg.export.web-page` (failed, R5). The other 16 rows of the round the four files carry passed; every row of `images.spec.ts` passed with the retired row gone. The table is under the scratchpad (`runs/gate-specs.json`, `gate-specs/core-matrix.md`); the tmp store listed `gt-brand` and `templates` alone after the run.

3.6 The fourth shapes walk (19:52, after the keyboard route landed): 57 passed, the same 2 failed; `shapes.insert.grid-shapes` read "Enter on the focused grid (opened by ArrowRight)", the plate reached by ArrowDown to the Shape row, ArrowRight, ArrowDown to the Shapes row and ArrowRight, and the row now passes on that route alone.

3.7 The three rows the gate read red for the driver's reasons, run again at 19:57 (`svg.spec.ts` and `logos.spec.ts` over `svg.import.upload`, `svg.render.picture-gestures` and `logos.export.svgblip`): `svg.render.picture-gestures` now reads eight of its nine checks green (the move, the Shift resize, the rotation, the mask, Border weight 2 written and drawn as 2px on the img, Drop shadow through the Format options section written as `{ color: 'ink', opacity: 0.3, angle: 45, distance: 8, blur: 12 }` and drawn, the PNG drop, every undo) and stays red on the crop row's state alone (R7: the toolbar's Crop says the sentence, the menu row is not disabled on this tree); `logos.export.svgblip` reads its Editable text half and its Perfect half green (`ts:title#footer-logo:1` and `#title-logo:2` each with `asvg:svgBlip` beside a PNG in both files) and stays red on the PDF alone (2 image XObjects, the footer band's PNG twin, R6). Both are the tree's, not the drivers'; the rows read them as such. The third chrome walk (20:03): the same four red rows, the shape's right click menu now read for both context only rows (`format.changeShape` and `format.dropShadow`, each an empty icon slot on this tree) and the chart's for its two; its cleanup answered 404 on its own.

### 4. Commands

The runs, each against 4425 (the server restarted at 18:45, 19:20 and 19:24 on the tree as it stood), their files under the scratchpad's `vector/b5/runs/`:

- The walk, `--only shapes`: 18:31 (47 passed, 12 failed: the driver's first reading, section 5), 18:47 (53 and 6), 19:05 (57 and 2). `--only chrome`: 19:08 (48 and 4), 19:15 (48 and 4, the Format menu read with the picture kept selected and the three dropdowns through their arrows).
- `core/svg.spec.ts` at 19:00: 11 passed, 1 failed (1.8 min). `core/export.spec.ts` and `core/logos.spec.ts` over the round's rows (`-g "shapes.geometry|svg.export|logos.export.svgblip"`): 19:12 (1 passed, 7 failed: the driver's first reading, section 5) and 19:20 (5 passed, 3 failed).
- The gate, `--only specs --spec svg,export,logos,images --lock <scratchpad>/gate-lock-specs`, 19:24 to 19:46 (section 3.5). The walk `--only shapes` again at 19:52 (section 3.6), then `core/svg.spec.ts` and `core/logos.spec.ts` over the three rows the gate read red for the driver's reasons, and `--only chrome` again with the Drop shadow row read from the shape's right click menu (section 3.7).
- The full walk (`--only probe`) did not run here: the two areas this lane changed were walked four and three times and the other areas' drivers are untouched; the one shared change, the quiet cleanup of `index.mjs`, runs in a `--only` run and for a retired deck alone. The whole matrix is the integrator's gate on the preview (VECTOR.md 6.2).
- `node_modules/.bin/vitest run --project scripts`: 12 files, 113 tests green after the changes (the matrix module, the walk's coverage of the probe rows, the README section).
- `node_modules/.bin/tsc -p apps/studio/tsconfig.json --noEmit`: clean on the e2e files (the root `tsc -b` stops on B2's `shapes.test.ts`, R3).
- The walk: `node scripts/probes/editor-walk-probe.mjs --core --base http://localhost:4425 --only shapes --json … --matrix … --shots …` under the lock.
- The specs: `PLAYWRIGHT_BASE_URL=http://localhost:4425 node_modules/.bin/playwright test apps/studio/e2e/core/<file>.spec.ts --reporter=list,json` under the lock.
- The gate: `node scripts/probes/core-gate.mjs --base http://localhost:4425 --only specs --spec svg,export,logos,images --lock <scratch>/gate.lock --out <scratch>/gate` (a scratch lock, never the checkout's).

### 5. Decisions and the corrections to 6.1's texts

1. The Shapes plate: the table's shapes category holds 99 presets (`SHAPE_PRESETS`), five of them flowchart presets that share a plain shape's outline at the defaults (`flowChartProcess` with `rect`, `flowChartAlternateProcess` with `roundRect`, `flowChartConnector` with `ellipse`, `flowChartExtract` with `triangle`, `flowChartDecision` with `diamond`), so 94 glyphs are distinct. The row reads the counts from the schema so it follows the table.
2. The star: ECMA `star5`'s inner radius is `iwd2 = swd2 × a / 50000` (b2.md 2.7 and request 3), 60 percent of the outer at Adjust 30 and 38.2 percent at the default 19098, measured along each inner vertex's direction against the outer ellipse (`swd2` by `shd2` about `svc`); the row's text and the driver read that, and the path's byte equality with `shapePath` at the written adjust beside it.
3. The can and the curved arrow: 2.3's shade overlay is a fourth `<path>` marked `data-shade` that repeats the base's `d`; the drivers join the geometry paths without it. `curvedRightArrow` shades with `darkenLess` (the ink overlay at 0.15); `bevel` alone carries `darken`.
4. The cloud: `cloudCallout`'s cloud is eleven `arcTo` commands; the row reads arcs, not cubics.
5. The pinned three: the named rows insert `shape: 'rectangle'`, `'rounded'` and `'ellipse'`, which `primitives.ts` draws as `rect` and `ellipse` elements (`shapes.insert.rounded-click`'s rx 8, works on production), so the row reads the presets' paths on the `rect`, `roundRect` and `ellipse` tiles of the Shapes plate, with the switch on while the plate is parked; the row keeps no `parks` as 6.1 says, since `reachRow` drives it either way.
6. The sites: the line's end is dropped on the hexagon's upper right site read from `sites('hexagon', …)` (its top right vertex), so the snap has a site under the pointer; the first drive dropped it on the edge's middle, 6 site marks showed and no attachment was written.
7. `shapes.change-shape.plate` picks Octagon from the toolbar plate, undoes, then picks it again from the right click menu's plate and undoes, so both plates write and both undos restore the hexagon.
8. `svg.import.paste-markup`'s third paste ("hello <svg>") asserts no picture landed and that whatever landed is not a picture; the text box the product may make from a plain paste is FEATURES.md 2.3 item 5's, not this row's.
9. `svg.export.pptx-fallback`'s second half reads the `svgVector: false` file from the address the window API's report names, else through the sync export route with the deployment's bearer (none on localhost), else skips with the reason.
10. `svg.export.web-page` tells the picture's PNG twin from the kit's pictures by its aspect (the fixture's 3 by 2); a deployment whose kit inlines other PNGs still reads right.
11. `shapes.geometry.text-rect`'s file half: `addShapeText` writes the sides net of the preset's rectangle exactly, and moves the exporter's first baseline shift from the top inset (clamped at zero) to the bottom (a margin the box cannot move, `text.ts`), so the driver reads the sides within 1.5 px, the top at most the net top, the bottom at least the net bottom, and the same shift on both shapes (4.48 px for the deck's face and size); the first reading summed the pair against zero and read the shift as a defect.
12. The svg picture the export rows place is a `shot` block through `asset.add { file }` (the upload's own kind and path, b3.md 2); the Editable file names it `ts:<slideId>#<blockId>:<rid>` and the driver finds the object by that name (b4.md R5), since the title slide's kit logo objects carry blips of their own.
13. The walk's quiet cleanup (a `--only` run's deck, and every deck a stall retired) settles the last write first, retries a refused trash or removal with the revision the refusal names ("baseRevision 90 is stale; … is at revision 91": the tab's `deck.info` lags the server by a checkpoint after the area's last write, so a fresh read of the tab answered the same stale number) and prints what was refused; the four shapes smokes had left their decks behind on the tmp store with the refusal swallowed (each removed through the product's route before the return: `deck.trash` then `deck.remove`, each 200, then 404 on `/edit`; the chrome smokes' cleanups answered 404 on their own).
14. The tile counts, the star's ratio, the shade overlay, the cloud's arcs and the ellipse element (items 1 to 5) were the driver's first reading against the tree; each row's text in the matrix now says what the tree does, and the driver reads it.
