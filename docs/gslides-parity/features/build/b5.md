# B5, the shader library

## Features round, ship two

Builder B5 of the features round, ship two (docs/FEATURES.md section 5 whole, the lane table of section 6, the rows of 7.1 and the named unit tests of 7.3), on the shared checkout at `2be923e` on 2026-09-24 while B1 (the gallery, `dialogs/ShaderGallery.tsx`, the Background dialog's Shader row), B4 (`core-matrix.json`'s 29 `shaders.*` rows, `core-walk/areas/shaders.mjs`, `core/shaders.spec.ts`) and B7 (`apps/studio/src/server/shader-frames.ts`, the registration in `server/actions.ts` and `agent-actions.ts`, the exporters) landed their files beside this lane; every file in section 1 is this lane's alone, and the four files outside the lane's list that this lane changed are named in section 1 with the reason. The dev server was port 4415 (`TURBOSLIDE_STORE=tmp` under a private `TMPDIR` in the scratchpad, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, two fake secrets of 44 characters, `vite.no-watch.config.ts`), stopped before the return. No git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran; binaries ran as `node_modules/.bin/<tool>` and `node <script>`. No Playwright test ran on the checkout's servers; the one browser drive of section 3 was this lane's own playwright-core script against 4415 (one page, no lock needed by AGENTS.md's rule because no Playwright test ran; the machine's one browser rule was kept). Every scratch deck of the drive lived on the private tmp store, was trashed and removed forever through the window API, and answered 404. Nothing was created on a deployment. No token, cookie or secret was printed. Times are PDT.

The Glyphfield clone the audit made (`/private/tmp/claude-501/.../scratchpad/features/Glyphfield`, at `bda9e0c`) was read for `src/lib/liveMaterials.ts`, `agentCatalog.ts`, `paperShaderControls.ts` and `shaderLab.ts`; nothing of it was copied into the checkout beyond the control model rewritten against Turboslide's uniform names (THIRD_PARTY_NOTICES.md, section 1 item 5.9).

### 1. The items, their files and their hunks

Each item is one commit's worth, in the rank of docs/FEATURES.md section 5, so a red row's item reverts alone in the fix round. The hunks are named by the symbol they add or change.

| Item                                                             | Files and hunks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Rows                                                                                                                                                                                     | Unit tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 5.1 the document shape                                           | `packages/schema/src/blocks/material.ts`: the header comment; `MATERIAL_MOTION_PLAYS`, `MaterialMotion`, `MaterialControls`, `MATERIAL_CONTROL_NAMES`, `MaterialPalette`, `MATERIAL_PALETTES`; `MaterialBlock.motion?`, `.controls?`, `.palette?`; `materialMotionSchema`, `materialControlsSchema` (the eleven ranges); the three annotated fields of `materialBlockSchema` (json, json, select). `packages/schema/src/assets.ts`: the material source's `size` widened to `[number, number]`, `backend` gains `'client'`, `frameKey?`, the schema beside it. `fit` is not added (5.1). `migrate` is the identity; nothing else in the schema changed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | every `shaders.*` row reads the record; `shaders.frame.auto-capture` reads `backend` and `frameKey`                                                                                      | `packages/schema` 583 tests pass unchanged (a material block with every new field absent validates through the fixtures; `materialControlsSchema` refuses a value outside its range in `shader-actions.test.ts`); `packages/lint` fixtures unchanged                                                                                                                                                                                                                                                          |
| 5.2 the controls ported from Glyphfield                          | `packages/materials/src/controls.ts` (new): `SHADER_CONTROLS` (the eleven with Glyphfield's ranges, the group and the sentence), `DEFAULT_CONTROLS`, `controlsWithDefaults`, `clampControl`, `controlsAtDefault`, `controlMapOf` (the per entry mapping from the group key lists onto the entry's `u_*` names; Brightness names the colour uniforms where no `u_brightness` exists), `controlApplies`, `controlFactor` (Glyphfield's), `applyControls` (the mapped keys alone, every value clamped to its spec), `uniformsWithControls`, `speedOf`, `scaleHexColor`. `packages/materials/src/shader-writes.ts` (new, browser safe): `shaderBlockOf` (the featured preset, `motion.play: 'show'`, the controls with their resolved uniforms beside them, the alt "The liquid metal shader"), `shaderSetMutations` (a preset write clears `/controls` and `/uniforms`; a controls write writes `/controls` and `/uniforms` together; raw Advanced overrides of unmapped keys survive), `shaderInsertPosition`, `requireMaterialBlock`, `entryForBlock`, `SHADER_INSERT_SIZE`. `packages/materials/src/catalog.ts`: `SHADER_CATEGORIES`, `ShaderCategory`, `FEATURED_MATERIAL_IDS`, `GALLERY_MATERIAL_IDS`, `MaterialEntry.category`, `.still`, `.featuredPreset` (the diamond for liquid metal, the Primary smoke for gem smoke), `entryWithPalette`, `requireMaterialFor`, `featuredPresetOf`, `categoryLabel`, `listMaterials(filter, palette)`; the seventeen entries gain their category and the four stills their flag | `shaders.panel.slider-live-undo`, `shaders.panel.preset-tiles`, `shaders.insert.selected-free-rectangle` (the block's fields)                                                            | `controls.test.ts` (new, 6 tests; the 7.3 test): the eleven names in order with a sentence each; every entry maps the controls onto uniforms inside their ranges over every preset; a preset's authored uniforms hold until a control moves; the record stores the controls and the resolved uniforms both; the kit palette by role; the frame key's inputs                                                                                                                                                   |
| 5.7 the kit palette feeding the shader                           | `packages/materials/src/presets.ts`: `ShaderPalette` (the six roles plus `figure`), `LEGACY_SHADER_PALETTE` (today's pixels for a deck without a kit), `shaderPaletteOf(kit, appearance)`, `shaderPaletteOfDeck(deck)`, `PALETTE_PRESET_GROUNDS` (six palette presets, one per role taken as the ground: `ink-paper` "Paper on ink", `paper-ink` "Ink on paper", `brand-blue` "Primary", `accent`, `caption`, `hint`), `palettePresetOfRole`, `groundRoleOfPreset`, `paletteValuesOf`, `palettePresets(roles, palette)`, `presetsFor(id, roles, palette)`; the labels in sentence case ("Chrome", "Noir", "Warm paper"; no Paper suffix, no id); the gem smoke `brand-blue` follows the kit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `shaders.panel.kit-colours`, `shaders.insert.words` (the tile words)                                                                                                                     | `controls.test.ts` "the kit palette": a deck without a kit reads today's pixels (`u_colorBack #2f5ce0`, `u_colors #ffffff,#86a8ff`); a kit's Primary, Background and Accent reach the preset; every entry carries the six palette presets; every label is sentence case; `catalog.test.ts` unchanged and passing                                                                                                                                                                                              |
| 5.5 the frame key and the frame shape                            | `packages/materials/src/sha256.ts` (new; the identity package's block function repeated, browser safe); `recipe-key.ts`: `recipeKey` over the package's sha256 (the same digests, pinned by `catalog.test.ts` against deck.json), `anchorOf`, `FRAME_LONG_SIDE`, `materialAspectOf`, `roundedAspect`, `frameSizeFor`, `FrameKeyBlock`, `FrameKeyInput`, `frameKeyInput`, `frameKeyOfInput`, `frameKeyOf(block, palette)`, `frameAssetId`, `isFrameAssetId`, `frameIsStale`. `capture.ts`: `frameSizeOf` (any pair with the long side 3200), `noiseTextureSrc` (Paper's noise data URI read from the package text in Node, so the smoke ring, god rays, grain gradient and metaballs capture hosted), `captureDocument(size)`, `servePaper(page, dist, size)`, `MaterialCaptureRequest.frameKey?`, `.palette?`, `renderMaterialFrames` (the render half, PNG bytes, no write), `captureMaterial` over it, the source's `size` and `frameKey`, the alt "The liquid metal shader in the diamond preset", `mountInPage` exported                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `shaders.frame.auto-capture`, `shaders.frame.box-aspect`, `shaders.frame.reuse-and-prune`                                                                                                | `capture.test.ts` (the 7.3 test): "a client frame and a hosted frame of one recipe carry one frameKey and both are fresh; the aspect and the kit move it, the pixel size does not" (Node) and, in the browser, "a frame at a box aspect takes the long side 3200, records its frameKey, and the diamond's bytes are measured" (3200 by 800 for a 600 by 150 box; the bytes are in section 2); the two M5 browser tests pass unchanged                                                                         |
| 5.5 the storage rule                                             | `packages/store/src/frames.ts` (new): `FRAME_GRACE_MS` (five minutes, the snapshot prune's), `FRAME_PRUNE_RETRIES`, `isFrameAssetId`, `assetFiles`, `referencedAssetIds` (every block's asset paths, the slide pictures, the kit's two slots), `isMaterialFrameAsset`, `supersededFrameOf`, `orphanFrameAssets`, `pruneFrameAssets(deps)` (one write of `asset.remove` per orphan at the head, then the files). `packages/materials/src/actions.ts`: `pngSize`, `frameCredit` ("Shader: Liquid metal, Paper Shaders, rendered in Turboslide"), `frameAssetOf`, `commitFrame` (the put, the record unless the head holds it, the block's `/asset`, the superseded frame's `asset.remove`, one write; the superseded file removed after), `shaderFrame` (the key check against the head as a `ConflictError`, `bytes` base64 or an `upload` key), `shaderCapture` (the hosted job for one block at its box aspect), `SHADER_ACTION_ID_STRINGS`, `SHADER_ACTION_IDS`, `registerShaderActions` (strings filtered through `isActionId`, the pattern of agent-actions.ts), `AssetActionDeps.placeInsert?`                                                                                                                                                                                                                                                                                                                                                                                                                       | `shaders.frame.reuse-and-prune`, `shaders.frame.one-capturer` (the server half; B7's `shader-frames.ts` handler wins the hosted registration and imports this lane's rule)               | `frames.test.ts` (new, 4 tests; the 7.3 test): the id shape; every reference; the prune removes the old orphan and the dangling record, keeps the referenced, the young and the photograph, writes once and nothing the second time; the superseded frame rule. `shader-actions.test.ts` (new, 2 tests, naming the six ids for the coverage test): on a scratch copy of the fixture, the insert, the set, the frame under its key, `existed` on the same bytes, the conflict on a moved key, the supersession |
| 5.5 the client capture and the one capturer                      | `packages/viewer/src/shader-frame.ts` (new): `FRAME_DEBOUNCE_MS`, `clientCanCapture`, `webglRendererOf`, `captureShaderFrame` (the pixel path of 5.5 on a hidden host at half the frame, `preserveDrawingBuffer`, `devicePixelsSupported` cleared, `setMinPixelRatio(2)`, `setFrame(anchor)`, `toBlob`), `ShaderFrameWrite`, `ShaderFrameWriteOutcome`, `ShaderCapturerDeps`, `materialBlocksOf`, `shaderBlocksTouched` (a block write, an insert, a slide replace on a shader; a kit colour write and a restore name every shader), `bytesToBase64`, `FRAME_UPLOAD_ABOVE_BYTES`, `createShaderFrameCapturer` (800 ms after the last change per block, one capture at a time, the write through the controller's queue, the 409 rule, the hosted fallback). `packages/viewer/package.json` exports `./shader-frame`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `shaders.frame.auto-capture`, `shaders.frame.one-capturer`, `shaders.frame.reuse-and-prune` (the client half; the controller's call is request R8)                                       | `__tests__/shader-frame.test.ts` (new, 5 tests, Node with fake deps): what a commit schedules; one capture 800 ms after the last change with the key, the renderer and the bytes; a fresh frame captures nothing; the conflict drops or retries once; the hosted fallback                                                                                                                                                                                                                                     |
| 5.6 the performance rules and the one live mount                 | `packages/viewer/src/MaterialMount.tsx`: `MaterialMountProps.selected?`, `.palette?`, `.scale?`, `.fps?`; `MountRecipe` (the recipe attribute with `speed`), `blockIdOfRoot`, `rootHasFrame`, `MountPlan`, `mountPlanFor` (selected plays, a frame shows, a frameless block mounts a still; every root plays without a selection, today's behaviour), `prefersReducedMotion` (followed live), `EDITOR_SHADER_FPS`, `throttleMount` (30 frames per second over Paper's own loop), the registry `liveShaderHandle`, `liveShaderBlockIds`, `previewShaderUniforms`, `previewShaderFrame`, `previewShaderSpeed`; the mount takes the palette and 1x below zoom 100. `packages/render/src/blocks/material.ts`: the box draws no label (the plate alone before the first frame), the recipe attribute rides the Speed control                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `shaders.perf.one-context` (the call site is request R6), `shaders.perf.hidden-pauses`, `shaders.perf.editor-frame`, `shaders.insert.words` (the sheet, the card, the viewer, the show)  | `packages/render` `blocks.test.ts` snapshot of `mat-live` updated (the label gone; two snapshots); `packages/viewer` 431 tests pass                                                                                                                                                                                                                                                                                                                                                                           |
| 5.3 the Shader section                                           | `packages/chrome/src/inspector/shader.tsx` (new): `ShaderSection`, `SHADER_WORDS`, `ControlSlider` (previews through the live mount while held, commits on release, the arrow keys step and Shift steps ten), the groups Shader (the still, the name, Change, the still's state line), Preset (the tiles), Colors (the kit's six swatches in the kit's order with the role's sentence, Custom), Form, Light and texture, Orientation, Motion, Dither (Two-tone, Plate), Advanced (collapsed; the frame time and the raw uniforms with Reset). `shader.css` (new). `PicturesPanel.tsx`: the Material section dropped, the header comment. `packages/chrome/package.json` exports `./inspector/shader`. `inspector/material.tsx` stays on the tree until request R9 (Inspector.tsx imports it)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `shaders.panel.section-groups`, `shaders.panel.slider-live-undo`, `shaders.panel.preset-tiles`, `shaders.panel.control-sentences`, `shaders.panel.kit-colours`, `shaders.panel.one-home` | `__tests__/shader-section.test.tsx` (new, 3 tests, jsdom): the groups in order with the sentences on the sliders, the tiles in sentence case with the current one pressed, the six swatches, no id on the surface; one `slide.update` per release with `/controls` and `/uniforms`, a preset clearing both, a swatch writing the role's preset; a parked control hidden with the switch off and drawn with it on (the B5 surface of `parked-controls.test.ts`'s todo)                                         |
| 5.4 the gallery's stills                                         | `packages/materials/src/previews.ts` (new): `SHADER_PREVIEW_SIZE`, `SHADER_PRESET_TILE_SIZE`, `SHADER_PREVIEW_ANCHOR`, `cardPresetOf` (the card in black and white: the featured preset when it reads so, else ink and paper), `shaderPreviewFile`, `shaderPreviewUrl`, `ShaderPreviewIndex`. `scripts/build-shader-previews.mjs` (new): the build over the capture page and sharp, `--check`, `--only`. `packages/materials/previews/` (new, committed): 125 webp files (17 cards at 320 by 200, 108 preset tiles at 96 by 60) and `index.json`, 136,512 bytes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `shaders.insert.gallery-thumbnails` (B1's row over these files)                                                                                                                          | `shader-actions.test.ts` reads the file names; `node scripts/build-shader-previews.mjs --check` exits 0                                                                                                                                                                                                                                                                                                                                                                                                       |
| 5.8 the actions                                                  | `packages/materials/src/actions.ts`: `shaderList` (`{ shaders, controls, categories }`, the entries with `category`, `still`, `featuredPreset`, `preview`, `presetPreviews` over the deck's palette), `shaderInsert` (the conversion through the dispatcher, the placement helper or the sheet's centre, one `block.insert`), `shaderSet` (one write of `shaderSetMutations`), `shaderFrame`, `shaderCapture`, `shaderRender` (`{ png, width, height, renderer, backend, ms }`, base64 PNG); `registerAssetActions` calls `registerShaderActions`. The schema entries and the ids are request R1 (`b5/actions-shader.patch.ts`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `shaders.agent.list-insert-set-render`                                                                                                                                                   | `shader-actions.test.ts`; the browser tests of `capture.test.ts` cover the render path                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 5.9 the licences                                                 | `THIRD_PARTY_NOTICES.md`: the Paper paragraph gains the "Shader:" credit line and the stills; the Glyphfield paragraph gains the controls port, the house renderers as not ported in ship two, and the three not shipped libraries with the reason (the integrator merges; B2 and B6 are not in this ship)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |                                                                                                                                                                                          |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 5.2 P1 the engine interface, the studio field, the house engines | Not built: the P1 condition of section 1 (a lane's P0 rows green on its own preview run before the integrator's first gate) could not be met on the shared checkout, and the P0 above took the ship. `engine.ts`, `proto.ts`, `present/ShaderLayer.tsx`, the Frame row, the background slot and View > Play shaders wait for the round after with their rows recorded not driven (`shaders.background.add-to-theme`, `shaders.library.glyph-engines-render`, `shaders.frame.scrubber-capture`, `shaders.view.play-setting`, `shaders.show.plays-when-on`, `shaders.show.frame-when-off`). The mount already reads `motion.play` nowhere: the show draws the frame (5.6's "until this lands")                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | the six P1 rows, not driven                                                                                                                                                              |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

Four files outside this lane's list carry one edit each, named here so the integrator sees them: `packages/chrome/package.json`, `packages/viewer/package.json`, `packages/store/package.json` and `packages/materials/package.json` gain one export line per new module (`./inspector/shader`, `./shader-frame`, `./frames`; `./controls`, `./shader-writes`, `./previews`, `./sha256`), because a package exports TypeScript source through explicit subpath exports and the chrome imports the viewer's registry and the materials' writes; `packages/render/src/__tests__/__snapshots__/blocks.test.ts.snap` carries the two updated `mat-live` snapshots of the label-free box (the behaviour of 5.5).

### 2. What was measured

- The bytes per capture (5.5, for the ship note; `renderMaterialFrames` on this machine, Chrome for Testing 147.0.7727.15, ANGLE Metal, Apple M5 Max): the liquid metal diamond at 3200 by 1800 as PNG, 350,337 bytes (781 ms); the same at 3200 by 800, 101,763 bytes (542 ms); the gem smoke Primary at 3200 by 1800, 798,338 bytes (730 ms); the smoke ring "Paper on ink" at 3200 by 1800, 2,244,188 bytes (1,033 ms). A noisy shader's frame is over 2 MB, so the capturer sends bytes over 3 MB through the presigned upload when the controller hands it one (`uploadAbove`, the picture path's threshold); under it, base64 in the `shader.frame` body.
- The stills: 125 files in 136,512 bytes, built in 29 s (`node scripts/build-shader-previews.mjs`); the card is black and white by construction (`cardPresetOf`).
- The tests, per package with `node_modules/.bin/vitest run` from the package: `packages/materials` 6 files, 42 tests (35 in Node, 7 in the browser including the new frame test; `TURBOSLIDE_SKIP_BROWSER=1` skips 3); `packages/store` 21 files, 259 passed and 1 skipped (as before) plus `frames.test.ts`; `packages/schema` 25 files, 583 passed; `packages/render` 18 files, 380 passed after the two snapshots were updated; `packages/viewer` 43 files, 431 passed; `packages/chrome` 79 files: 745 passed and 2 todo, and 2 failed in another lane's in progress files (section 5).
- `node_modules/.bin/tsc -b packages/schema packages/materials packages/store packages/render packages/viewer packages/chrome`: clean. `tsc -b apps/studio`: two errors, both in B7's in progress test files (`server/agent-actions.test.ts` 77 `mutates` on `never`; `server/shader-frames.test.ts` 453 `neutral` on `AssetTwins`), none in this lane's files.
- `node_modules/.bin/prettier --check` over every file of section 1: clean after `--write`.

### 3. The hand drive on 4415 (the rows before B4's drivers)

`b5/drive-4415.mjs`, playwright-core over Chrome for Testing 1217 with ANGLE Metal, one page at 1440 by 900, a `/new` deck, the block through the window API (`block.insert` of a material block with `preset: 'diamond'`, `controls: { speed: 1.5 }`, `motion: { play: 'show' }` and a 600 by 150 box, the setup the matrix rows name while Insert > Shader waits for B1's row), read on the live stage root (`[data-block="shader"][data-live]`; the hidden measure sheet carries a second root with no canvas), the deck trashed and removed, `/deck/<id>` 404. The readings, as the run recorded them (`b5/drive-4415.json`):

- The block inserts with the three new fields and the document keeps them (`preset diamond`, `motion.play show`, `controls.speed`): ok.
- The sheet's box draws no label and no text (`shaders.insert.words`, the sheet clause): ok. The filmstrip card draws no label (`shaders.insert.words`, the card clause; audit-shaders 21): ok.
- The recipe attribute rides the Speed control (`"speed":1.5`): ok.
- The live mount is one canvas over the box, mounted within 35 ms of the root (`shaders.perf.one-context`, the one block half; the two block half waits for request R6): ok.
- The selected mount plays (two screenshots of the box 900 ms apart differ) and holds its frame at Speed 0 (two identical): ok.
- `shader.list` on the window transport: not driven, `Unknown action "shader.list"`, the schema entry is request R1.
- The console: no error of the product; the two lines the reader caught were the TanStack devtools' server log forwarding, filtered.

Not driven on this server, with the reason: `shaders.panel.*` (the Shader section is mounted by request R3; the section's behaviour is the jsdom test of section 1), `shaders.frame.*` (the capturer is wired by request R8 and the `shader.frame` entry by R1; the pixel path and the write are the unit tests), `shaders.insert.selected-free-rectangle` (request R5 and B1's gallery), `shaders.perf.one-context` with two blocks (request R6), `shaders.perf.hidden-pauses` (Paper's observers pause the mount; a driver reads the rAF counter, B4's spec), `shaders.agent.list-insert-set-render` (R1).

### 4. Requests

R1 (the integrator, `packages/schema/src/actions.ts`, the header the integrator owns): the six `shader.*` ids in `ACTION_IDS` and their entries, with `material.list`, `material.capture` and `slide.setBackgroundMaterial` kept as they are. The exact text is `docs/gslides-parity/features/build/b5/actions-shader.patch.ts` (three hunks: the imports, the ids, the entries). Then `pnpm generate:contracts`. The handlers are registered on the tree (materials `registerShaderActions`; B7's `shader.frame` wins the hosted registration), the window transport carries the ids (B7's `SERVER_SIDE_WINDOW_ACTIONS_F2`), and `shader-actions.test.ts` names the ids for the coverage test. `shaders.agent.list-insert-set-render` reads red until this lands.

R2 to R12 (the integrator, B7 for R5's studio half and R11): the hunks of `docs/gslides-parity/features/build/b5/integrator-hunks.md`, one per file, in the shared file table's order: R2 `inspector/format-sections.ts` (the `shader` section and the route of line 281), R3 `FormatOptions.tsx` (the mount, `onChangeShader`), R4 `editor-shell.ts` (the `insert.shader` branch under B1's R2), R5 `apps/studio/src/editor/place-insert.ts` (`PlacedKind` gains `material`, so the controller places the gallery's insert in the free rectangle) and `server/actions.ts` (`placeInsert` in the materials deps), R6 `Editor.tsx` (the `MaterialMount` call passes `selected`, `palette` and `scale`), R7 `Selection.tsx` (the chip word), R8 `controller.tsx` (the capturer and its three call sites), R9 `Inspector.tsx` and the deletion of `inspector/material.tsx`, R10 `block-css.ts` (the dead `.material-label` rule), R11 `vite.deploy.config.ts` (`materials/previews/*`), R12 `strings.ts` (optional).

To B1 (`dialogs/ShaderGallery.tsx`, `Background.tsx`; your R4 to this lane): the still of a card is `shaderPreviewUrl(entry.id)` and a preset's tile `shaderPreviewUrl(entry.id, preset.name)` from `@turboslide/materials/previews` (the files are on the tree); `MaterialEntry.category` is set on every entry (`'metal'` for liquid metal, `'fluid'` for gem smoke, `'light'` for god rays and neuro noise, `'gradient'` for the seven gradients and noises, `'graphic'` for spiral, dithering, waves and dot grid), `MaterialEntry.still` marks the four stills and `featuredPresetOf(entry)` the preset a card inserts; `GALLERY_MATERIAL_IDS` is the featured order then the catalog's. The block a card hands `insertBlockPlan` is `shaderBlockOf(id, entry, { preset })` from `@turboslide/materials/shader-writes` (the schema carries `motion` now, so no probe is needed). The hover mount takes `{ palette: shaderPaletteOfDeck(deck) }` for the kit's colours and `throttleMount(handle, 30)` from `@turboslide/viewer/MaterialMount` for the cap. The Add to theme and Apply to all rows wait with the P1 background slot.

To B4 (`core-walk/areas/shaders.mjs`, `core/shaders.spec.ts`): the readings match your conventions as written: the section's root `.ts-shader[data-control="formatOptions.shader"]`, the range `formatOptions.shader.<control>.slider` and the number field `formatOptions.shader.<control>`, the sentence as the tooltip's doc on the range (the shown tooltip carries name and sentence; the spec's apostrophe in "where the pattern's middle sits" is the straight one), the group titles `.ts-shader-group-title` in the order of 5.3 (the Advanced toggle's word carries the class too), the tiles `formatOptions.shader.preset.<name>` with `aria-checked` and `.ts-shader-tile-word`, the swatches `formatOptions.shader.color.<role>` in the kit's order (text, background, caption, hint, primary, accent) with `aria-checked`, `formatOptions.shader.color.custom` the hex field; `shader.list` answers `{ shaders, controls, categories }` and `shader.render` a JSON body with the PNG under `png`; the section's still line is `formatOptions.shader.still` with `data-stale`. The `selected`, `palette`, `scale` and `fps` props of the mount are read by the call site of request R6, as you noted; until it lands every root plays (two canvases with two blocks).

To B7 (`server/shader-frames.ts`, `server/actions.ts`): your imports name what this lane exports (`entryWithPalette`, `requireMaterial`, `shaderPaletteOf`, `frameAssetId`, `frameIsStale`, `frameKeyInput`, `frameKeyOf`, `recipeKey`, `assetFiles`, `pruneFrameAssets`, `supersededFrameOf`, `FrameFileEntry`); `shaderPaletteOfDeck(deck)` in `@turboslide/materials/presets` is the one call for the deck's palette in its appearance, so the key the client computes and the key your handler checks agree. The materials `shaderFrame` stays as the CLI's and the tests' implementation of the same write; your registration after the asset ids wins hosted. `slide.setBackgroundShader` in your id lists answers 501 until the P1 slot lands.

### 5. Another lane's in progress files

`packages/chrome` `editor-shell-render.test.tsx` "Insert > Material lists the catalog; a row writes a material block with that recipe" (`findByRole('dialog', { name: 'Material' })` finds no dialog) and `materials-sections.test.tsx` "the Material row: Choose lists the catalog, Place is one slide.setBackgroundMaterial with the dither" (`no control dialog.background.material.place`) fail on B1's in progress `dialogs/Background.tsx` and `dialogs/InsertMaterial.tsx` (the Shader row and the gallery replacing the Material row and dialog; B1's R2 rewrites the first test). Nothing in this lane's files reaches either. `tsc -b apps/studio` prints two errors in B7's `server/agent-actions.test.ts` and `server/shader-frames.test.ts` (section 2).

### 6. Decisions this lane took, for the integrator and the ship note

- The Colors group's six swatches pick the kit role the shader takes as its ground and write `/preset` (the role's palette preset), so a `brand.set /colors/*` recolours the shader through the preset and re keys the frame; a Custom hex writes the ground uniform and `/palette: 'custom'`. Storing hexes in `uniforms` would have frozen the kit's colours into the block.
- `applyControls` leaves `u_scale` alone (Glyphfield scales it from amplitude, frequency and detail); Frequency, Amplitude and Detail move the named uniform groups alone, so a control does what its sentence says.
- Brightness scales the entry's colour uniforms where no `u_brightness` exists, so the control moves every shader and the still equals the editor at rest (a CSS filter would not reach the capture).
- The frame key hashes the seven palette values (the six roles and the figure colour) and the aspect at two decimals, without the pixel size and the backend (5.5); a frame captured before the round has no key and is stale by that absence.
- `shader.list` names the stills by file (`preview`, `presetPreviews`), not by URL: the browser builds the URL through `shaderPreviewUrl` (a Vite asset), and a URL an agent can fetch waits for a route or R11's function assets.
- The material label's CSS rule stays in `block-css.ts` (B3's file, request R10); it matches nothing.

## Features round, ship two, fix round

The fixer of B5, attempt 2, on 2026-09-25 (times UTC, from 16:55Z). Attempt 1 ended without a report and left its edits uncommitted in this lane's files (`MaterialMount.tsx`, `inspector/shader.tsx`, `shader.css`, `shader-section.test.tsx`, the new `material-mount-plan.test.ts`); they were read hunk by hunk against the four findings, kept where they answered a finding, and completed (section 1). The tree was `3bd34f8` with the other fixers' uncommitted edits beside this lane's (B4's in `e2e/core/lib.ts`, `shaders.spec.ts`, `export.spec.ts`, `core-walk/areas/shaders.mjs`, `core-walk/index.mjs`, `toolkit.mjs` and `core-matrix.json`; B7's in `vite.deploy.config.ts`; the verifier's pass 1 section of `VERIFICATION.md` and its 64 pictures; the integrator's fixer's section of `integrator.md`); none of them was touched. No git write command ran; no `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build`; binaries ran as `node_modules/.bin/<tool>` and `node <script>`. The dev server was port 4415 from `apps/studio` (`TURBOSLIDE_STORE=tmp` under a private `TMPDIR` in the scratchpad's `fix-b5/`, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, two 60 character fake secrets, `vite.no-watch.config.ts`), started at 17:03Z after attempt 1's server on the same port (started 14:27Z on the no watch config, so blind to the edits after it) was stopped, and stopped before the return. The one Playwright run of this fix was the core walk's shaders area against 4415 under `.turboslide/e2e.lock`, taken with the `until mkdir` loop after the lock another lane held from 14:24Z was released (section 4); its scratch deck lived on the private tmp store. Nothing was created on a deployment; no token, cookie or secret was printed. `packages/store/src/pulse.ts`, `packages/realtime/src/blob.ts` and `room.ts` were not opened.

### 1. The four findings and what changed

1. **F1, severity 3, `shaders.panel.slider-live-undo` and `shaders.panel.preset-tiles` (the live canvas under the frame).** The mechanism, read in Paper's `shader-mount.js` (`packages/materials/node_modules/@paper-design/shaders/dist/`, lines 461 to 475): the library injects `@layer paper-shaders { :where([data-paper-shader]) { isolation: isolate; position: relative; & canvas { position: absolute; inset: 0; z-index: -1; ... } } }` and sets `data-paper-shader` on the host, so the block's `.material` box is a stacking context and Paper's canvas paints under the in-flow `img.material-frame` from the moment a frame lands. The fix is the second of the two the finding names: `raiseCanvas(handle)` in `MaterialMount.tsx` sets the canvas's inline `z-index: 0` when the mount's handle lands (an inline declaration outranks a layered `:where` rule), so the canvas paints above the frame while the mount is held and the frame stays painted under it, which keeps the frame contract on the first paint and on a lost context; Paper removes its canvas on dispose, so the frame shows again the moment the block is unselected. Hiding the image was not taken because it needs a matching show on dispose and on a failed mount, and it gains nothing over the canvas above. `packages/render/src/block-css.ts` (B3's) needs no change: the rule `.ts-sheet .material > canvas` stays as it is.

   Two more mechanisms sat under the same row and were found by this fix's probes on 4415 once the canvas was visible (section 4). The second: the mount's layout effect listed `onError` and `palette` by reference among its dependencies, and `EditorRoot.tsx` hands `Editor` a new error arrow on every render (`onError={(error) => controller.say(errorMessage(error))}`, line 1831), so every render of the root disposed the live mount and created another (a WebGL context per render); a render landing during a held slider sent the held look to a handle already disposed, and the new mount drew the rest look. Probe 3 read it directly: the canvas armed at pointer down was gone from the host by the time the drag was held in two holds of three with the document's revision unchanged. `MaterialMount.tsx` now keeps `onError` in a ref and keys the palette by its colours, so only the body, the selection, the appearance, the zoom band and the motion setting remount. The third: Amplitude reached the mount (probe 2: `u_distortion` 0.07 to 0.107 at 6 and 0.133 at 8, the section's preview path intact) and moved liquid metal's box by under a tenth of a percent of its pixels at rest, because `u_distortion` was the only amplitude key the featured entry has; `controls.ts` `AMPLITUDE_KEYS` gains `contour`, liquid metal's distortion along the shape's edge, so the control the row drags moves what a seller sees (the map is computed per entry from its own uniform names, so no other entry changes; the catalog test's range check covers every preset).

2. **F3, severity 2, `shaders.panel.control-sentences` (no tooltip on an unused slider).** `ControlSlider` in `inspector/shader.tsx`: a control the entry does not map (Strength, Detail and Grain on liquid metal) is drawn with `aria-disabled="true"`, `tabIndex -1`, dim through `is-off`, its pointer down, change and arrow keys refused in the handlers, and `disabled` is reserved for the write's busy state, because a disabled input takes no pointer events and so never shows its plate; the tooltip's doc is the sentence plus "; not used by this shader" (`SHADER_WORDS.notUsed` lowered), so the row's `includes(sentence)` read and a seller's hover both get the sentence. `shader.css`: the focus outline off for the off range. The unit test reads the plate's text on liquid metal's Strength (the sentence and "not used by this shader") and Amplitude (the sentence alone) and that a change on the off range writes nothing.
3. **`shaders.panel.one-home`'s second clause (the Background dialog's options link finds no Shader section for the covering picture; integrator finding 2).** `inspector/shader.tsx` gains `shaderGroundOf(block, deck)` (the picture whose asset's source is a material frame, with the preset the capture recorded under `ext.preset`), `isShaderGround` (a type predicate on `PictureBlock`, so the panel's filter narrows the block) and `GroundSection`: `ShaderSection` now takes `MaterialBlock | PictureBlock` and draws, for a placed ground, the head (the still, the name, the still line), one sentence naming the ground and how it changes, the Preset tiles, the Colors swatches and Advanced (the frame time and the raw uniforms with Reset over the asset's values that differ from the preset's), and no slider group and no Dither group (the ground record carries no controls; the picture's own Dither section is in the panel). Every write is one `slide.setBackgroundMaterial`, the write Place makes, with the picture's dither carried over, so the hosted job renders the ground again and moves the picture to the new frame. The section's root carries `data-ground="1"`. The mount is in `FormatOptions.tsx`, the integrator's file: request R13 (section 5) makes the panel's filter and its `case 'shader'` accept a shader ground; until it lands the section is not drawn for a picture and the row's second clause stays red on every tier. The first clause on the deployment (no ground is ever placed there) is F2, B7's bundle fix, and outside this lane.
4. **`shaders.perf.one-context` (two frameless blocks mount two canvases before their frames land; integrator finding 6).** `MaterialMount.tsx`: `mountPlanFor` (one root at a time) is replaced by `planMounts(roots, selected)`, decided over every root of the stage at once: the selected shader block plays; with no shader selected, the first root without a frame mounts a still at speed 0 so its plate is not empty and every other frameless root draws its plate until its frame lands; a stage handed no selection plays every root as before the round. Two frameless blocks are one canvas, never two, in every selection state (the unit test walks five frameless roots under four selections). The frame of a block that mounts nothing still lands: the capturer (`shader-frame.ts`) mounts its own hidden host for the pixels and never reads the stage's mount, so the second block's frame arrives 800 ms plus the capture after its insert whether or not it drew a canvas. B4's walk step needs no wait beyond what it reads (the first block's frame, decoded long before the swap); `shaders.spec.ts`'s canvas reads select the block they count (`glyph-engines-render`, P1, skipped on this build) or count the stage (`hidden-pauses`, `editor-frame`), which the plan keeps at one.

### 2. Files and hunks

| File                                                        | Hunks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Finding |
| ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| `packages/viewer/src/MaterialMount.tsx`                     | the header's three paragraphs; `MountRootFacts`; `planMounts` replaces `mountPlanFor`; `raiseCanvas`; the layout effect decides the plans over every root and raises each landing handle's canvas; `onErrorRef`, `paletteRef` and `paletteKey` take `onError` and `palette` out of the effect's dependencies                                                                                                                                                                                                        | 1, 4    |
| `packages/materials/src/controls.ts`                        | `AMPLITUDE_KEYS` gains `contour` with the reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 1       |
| `packages/viewer/src/__tests__/material-mount-plan.test.ts` | new: `planMounts` (the selected block alone; one still with no selection; every root without a selection; never two canvases over five frameless roots under four selections) and `raiseCanvas` (the z-index and a handle without a canvas)                                                                                                                                                                                                                                                                         | 1, 4    |
| `packages/chrome/src/inspector/shader.tsx`                  | the header's two paragraphs; `ShaderSectionProps.block` widened; `SHADER_WORDS.ground`; `MaterialAssetSource`, `ShaderGround`, `shaderGroundOf`, `isShaderGround`; `ControlSlider`'s off state (`aria-disabled`, the refused handlers, the doc with `notUsed`); the section split into `Head`, `PresetTiles`, `ColorSwatches`, `AdvancedGroup`, `NotInLibrary`, `presetBase`, `BlockSection` (the block's section, unchanged in what it draws and writes) and `GroundSection`; `ShaderSection` routes by block type | 2, 3    |
| `packages/chrome/src/inspector/shader.css`                  | the `is-off` comment; the off range's focus outline; `.ts-shader-ground-note`                                                                                                                                                                                                                                                                                                                                                                                                                                       | 2, 3    |
| `packages/chrome/src/__tests__/shader-section.test.tsx`     | two cases: the tooltip of a control the shader does not use; a placed ground drawn as a target and re rendered through `slide.setBackgroundMaterial`                                                                                                                                                                                                                                                                                                                                                                | 2, 3    |

No other file of this lane changed; `THIRD_PARTY_NOTICES.md`, the materials package, the schema, the render package and the store's `frames.ts` are as the ship left them.

### 3. Tests and checks

- `node_modules/.bin/tsc -b`: clean at 17:00Z and again at 17:06Z after the predicate edit.
- `packages/viewer`: 44 files, 436 passed (1.4 s), `material-mount-plan.test.ts` 5 among them. `packages/chrome`: 80 files, 750 passed, 2 todo (6.7 s), `shader-section.test.tsx` 5 among them. `packages/materials`: 6 files, 38 passed. `packages/store`: 21 files, 259 passed, 1 skipped.
- After the mount fix and the contour key: `node_modules/.bin/tsc -b` clean at 17:34Z; `packages/materials` 6 files, 38 passed (17:33Z, the range check over every preset covers the contour key); `packages/viewer` 44 files, 436 passed (17:34Z); `prettier --check` clean on `MaterialMount.tsx`, `controls.ts`, `shader.tsx` and this note. No jsdom in the viewer package (its config says the React components are exercised in the browser), so the remount's fix has no unit test; the drives of section 4 are its evidence.

### 4. The rows re driven

Two readings on 4415 (the memory tier), both under the lock and each in one browser: the core walk's shaders area, the driver of record for the five rows, and this lane's own probe at human speed (`fix-b5/probe-4415.mjs` in the scratchpad, after the verifier's `probe-stage.mjs`), because on this tier the walk's slider step never reaches the range and takes the three panel rows and the one context row down with it (below), so the walk alone cannot say what the fixes changed.

**The walk.** `node scripts/probes/editor-walk-probe.mjs --core --base http://localhost:4415 --only shaders --json <scratch>/fix-b5/walk-4415.json --matrix <scratch>/fix-b5/walk-4415-matrix.json`, started 17:08:49Z, 328 s, the scratch deck `untitled-20260925-v8dy` trashed and removed by the walk's own finally (`/edit` and `/deck` 404 in the log). The fourteen shaders rows by id from the JSON's `core.results`: passed `shaders.insert.gallery-thumbnails`, `shaders.insert.selected-free-rectangle`, `shaders.insert.words`, `shaders.background.place-answers` (the ground placed in 3,049 ms through the hosted capture on this tier), `shaders.view.play-setting`, `shaders.insert.gallery-hover-live`; not driven `shaders.background.add-to-theme` and `shaders.frame.scrubber-capture` (P1, not built); and the five of this fix:

| Row                               | The walk on 4415 | What the walk observed                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `shaders.panel.slider-live-undo`  | failed           | "mount at rest 0.00 percent moving; during the drag the value read 3.2 with 0.0 percent of pixels changed and revision 13 (13 before); release: revision 13, value 3.2 -> 3.2 (block controls null); Cmd+Z: value null, 95.52 percent of pixels differ from rest": the pointer never reached the range (the value and the revision did not move), and the Cmd+Z then undid the insert itself |
| `shaders.panel.preset-tiles`      | not driven       | "no Shader section in Format options": the block was gone after the slider step's Cmd+Z                                                                                                                                                                                                                                                                                                      |
| `shaders.panel.control-sentences` | not driven       | the same                                                                                                                                                                                                                                                                                                                                                                                     |
| `shaders.panel.one-home`          | failed           | "Pictures and materials sections Pictures and materials, Pictures0 (with the switch on); the Background dialog's link dialog.background.shader.options: Format options true, Shader section absent, Pictures and materials false, dialog false": the first clause holds and the link opens the panel; the section is absent until R13 (section 5) mounts it for the covering picture         |
| `shaders.perf.one-context`        | failed           | "with nothing selected 0 canvas on the stage (0 on the page); first selected: 0 on the stage, 0 on the page; second selected: 1 on the stage after 457 ms, the second's root 1 canvas, the first's root absent": the first block was gone (the slider step's Cmd+Z), so the step selected nothing for its first read; the second block alone mounted one canvas 457 ms after its selection   |

The slider step's read is the shape of integrator finding 16 on 4419 (B4's driver): the step reads the thumb's point off the range without scrolling the control into view (`shaders.mjs` `slider-live-undo`, unlike its `control-sentences` step, which scrolls each control first), and on this tier's panel the Amplitude range sits below the 900 px fold, so the drag's pointer moves over nothing, the value stays, no revision is written, and the step's Cmd+Z undoes the insert. That is a driver read, named to B4 in section 5, not a product change of this fix; the deployment's runs moved the value (VERIFICATION.md F1).

**The DOM reads in the app's browser pane** (not a Playwright run; one page on 4415 while the probe waited for the lock; the pane's stage is small, so only DOM facts were read there, never pixels; its deck `untitled-20260925-ie72`, trashed and removed through the window API at the end). Finding 1's mechanism: with the frame landed and the block unselected, the element at the block's centre is `img.material-frame`, the root holds 0 canvases and the box's `isolation` is `auto`; with the block selected, the element at the centre is the canvas, its inline `z-index` is `0` (computed `0`, `position: absolute`, 960 by 520 device pixels), the decoded 3200 px frame image stays under it, the box carries `data-paper-shader` with `isolation: isolate`, and the root holds 1 canvas; the verifier read `img.material-frame` in every state. Finding 2: with liquid metal's section open, 11 of 11 ranges show their sentence on hover; Strength, Detail and Grain read "how strong the effect is; not used by this shader", "how fine the pattern is; not used by this shader" and "how much film grain is mixed in; not used by this shader" with `aria-disabled="true"`, `tabIndex -1`, the `is-off` class and `disabled false`; the other eight read the sentence alone with no `aria-disabled` and `tabIndex 0`.

**The probe** (`fix-b5/probe-4415.mjs`, the lock taken 17:21:10Z and released 17:21:59Z, its deck `untitled-20260925-0ilo` trashed and removed, `/deck` 404; the runner's headless shell at 1440 by 900, the machine under the ship step's check and two other lanes' runs at the time, so every time below is a loaded reading). Liquid metal inserted through `shader.insert`, the frame in 231 ms.

- Finding 1, the stacking: unselected the centre hits `img.material-frame`, 0 canvases, the box's `isolation` `auto`; selected the centre hits the canvas, inline and computed `z-index` `0`, 480 by 260, the decoded frame under it, `isolation` `isolate`, 1 canvas. Two clips 900 ms apart while playing differ (1.51 percent of pixels by more than 40 of luminance; the verifier's read on the deployment was identical bytes), and under `prefers-reduced-motion: reduce` two clips 600 ms apart are identical bytes (the mount at rest).
- Finding 1, the row's gesture (`shaders.panel.slider-live-undo`): the Amplitude range scrolled into view and dragged at rest from 3.2 to 6, the revision 3 before, 3 during the drag, 4 after the release, the block's `controls` `{ amplitude: 6 }` after it; the re capture landed (`frame-f3d5de814947756d`); Cmd+Z put the value back to 3.2 (revision 5) and the canvas read identical bytes against the rest shot. During the drag the canvas's pixels did not move at the row's threshold (0.0 percent by more than 40 of luminance, the mean luminance unchanged, the bytes not identical): the second probe below reads whether the held value reaches the mount and how much each control moves liquid metal's canvas at rest, since Amplitude maps onto `u_distortion` alone for this entry (`controls.ts` `AMPLITUDE_KEYS`; 0.07 to about 0.11 at 6).
- Finding 1, the preset click (`shaders.panel.preset-tiles`): Diamond to Chrome wrote one revision (5 to 6, `preset` `chrome`) and the first clip the probe could take after the click, 1,513 ms on under the load, had moved 36.4 of mean luminance with 21.7 percent of pixels by more than 40; the row's 500 ms bound was not resolvable on this machine at the time (one screenshot and compare took over a second) and stands for the preview run.
- Finding 2 (`shaders.panel.control-sentences`): 11 of 11 ranges show their sentence on hover; Strength, Detail and Grain carry "not used by this shader" beside it, the other eight do not.
- Finding 4 (`shaders.perf.one-context`): a second block (gem smoke, `shader-2`, through `shader.insert`; the walk's `block.insert` shape is refused by the window API on this tier, "invalid input at /slot") with no frame yet: nothing selected, 1 canvas on the stage and on the page (the second's still; the first draws its frame); the first selected, 1 on the stage; the second selected, 1 on the stage 453 ms after the click, the second's root 1 canvas, the first's root 0 with its frame image decoded; the second's frame landed 506 ms later and the second kept its one canvas as the selected block.
- Finding 3: `slide.setBackgroundMaterial` placed liquid metal on the slide in 1,380 ms (the hosted capture on this tier); the read of the panel for the covering picture did not land, because the click at the sheet's centre did not select the covering picture under the objects and the panel kept the last block's sections (`shader`, `size`, `position`, `text`, `altText`, a shader block's). The walk's own reading of the link stands for the row: Format options opens and the Shader section is absent for the picture until R13.

**The second and third probes, before the mount fix** (`fix-b5/probe2-4415.mjs`, the lock 17:26:01Z to 17:26:17Z, deck `untitled-20260925-n1ek` removed; `probe3-4415.mjs`, 17:29:16Z to 17:29:49Z, deck `untitled-20260925-tweu` removed). Probe 2 set each range through its input event with the mount at rest and read Paper's `providedUniforms` and the drawing buffer (`readPixels` right after a forced render, 156 sample points) beside a screenshot clip: Amplitude 6 moved `u_distortion` 0.07 to 0.1067 and 0.03 percent of the pixels by more than 40 of luminance, Amplitude 8 0.133 and 0.05 percent; Brightness 1.8 moved nothing (the diamond preset's tint is white and its ground black, so a factor above 1 clamps) and Brightness 0.5 moved the tint to 0.502 and 5.1 percent; Frequency 9 moved `u_repetition` 3 to 4.15 and 5.1 percent; Density 1.5 `u_softness` 0.05 to 0.076 and 0.0 percent; Rotation 180 4.5 percent; no revision moved. So the section's preview reaches the mount, and Amplitude on the featured entry was invisible by design of its key list. Probe 3 armed the mount before each pointer down (the canvas marked, `setUniforms` counted on the mount object) and read it while held: in the first hold at rest the marked canvas was gone by the hold (six calls had reached the disposed mount, a new mount stood with the held value re applied by the later moves), in the second hold at rest it stayed (eight calls on the same mount, `u_distortion` 0.1067), while playing it was gone again (two calls on the old mount), and one ArrowRight step stayed (one call, 0.0713); the revision never moved during a hold. A remount with no document change is a render of the parent, which is the mechanism section 1 item 1 names.

**After the mount fix** (`probes-after-fix.sh`, the lock 17:34:21Z to 17:35:03Z, the server restarted on the new code at 17:34Z; decks `untitled-20260925-i1dz` and `-hcto` removed, `/deck` 404). Probe 3: every hold keeps its marked canvas, at rest twice and while playing, with eight preview calls on the same mount each time and `u_distortion` 0.133 at the right end; the arrow step 0.0713 with `u_contour` 0.6112. Probe 2 with the contour key: Amplitude 6 moves `u_distortion` to 0.1067 and `u_contour` 0.6 to 0.915, 3.2 percent of the pixels by more than 40 (the buffer and the clip agree, 3.2 and 3.18), Amplitude 8 `u_contour` to 1.0 and 3.8 percent, the real mouse drag to 8 the same 3.8 percent while held; the other controls as before. The walk's `slider-live-undo` clause "the canvas changes during the drag" needs 0.5 percent, which the Amplitude drag now gives with the mount at rest on the live canvas; the row's own read on this tier still waits on the driver's scroll (section 5).

### 5. Requests

R13 (the integrator, `packages/chrome/src/FormatOptions.tsx`, the Shader section's mount; finding 3, `shaders.panel.one-home`'s second clause). Three hunks, the smallest that draw the section for a placed ground:

The import (line 50):

```ts
import { ShaderSection, isShaderGround } from './inspector/shader';
```

The filter, `case 'shader':` in `sectionsFiltered` (line 588):

```ts
        case 'shader':
          /* the features round, ship two (docs/FEATURES.md 5.3): one shader block's one home, and
             the covering picture of a placed shader ground (the fix round; inspector/shader.tsx) */
          return !many && (selected?.type === 'material' || isShaderGround(selected, deck));
```

The render, `case 'shader':` (line 822; `isShaderGround` narrows `selected` to the picture, so `block={selected}` types as `MaterialBlock | PictureBlock`; the generated rows stay the block's alone, a ground's picture rows are the picture sections'):

```tsx
            case 'shader':
              return selected === undefined ||
                !(selected.type === 'material' || isShaderGround(selected, deck)) ? null : (
                <Section key={section.id} {...common}>
                  <ShaderSection
                    block={selected}
                    deck={deck}
                    write={write}
                    settings={{ advancedTools }}
                    onChange={onChangeShader}
                  />
                  {selected.type === 'material' ? renderGeneratedAdvanced('shader') : null}
                </Section>
              );
```

Nothing else moves: `leadingFormatSection` keeps a material as the only block whose Shader section leads (it reads no deck), and the Background dialog's link opens the panel with `section: 'shader'`, which scrolls to the section wherever it sits. The section over a ground draws no Change button (`GroundSection` hands `Head` no `onChange`; Change background is the ground's picker), so `onChangeShader` is read for a block alone. `shader-section.test.tsx`'s ground case pins what the section draws and writes; the panel's own test file is the integrator's.

To B1 (`dialogs/Background.tsx`): nothing. `openShaderOptions` already selects the covering picture through `onSelectBlock` and opens Format options at `'shader'`; with R13 the section is there.

To B4 (`core-walk/areas/shaders.mjs`, `core/shaders.spec.ts`): nothing this lane needs. `shaders.perf.one-context`'s step asserts the first block's frame, decoded before the swap, and one canvas per state, which `planMounts` gives; the second block's frame is not asserted and lands through the capturer's own host. One observation from section 4 for the slider step on the memory tier (integrator finding 16): `slider-live-undo` reads the thumb's point off the Amplitude range without scrolling the control into view, and on 4415's panel at 900 px the range sits below the fold, so the drag moves over nothing and the step's Cmd+Z undoes the insert, which takes `preset-tiles`, `control-sentences`, `kit-colours` and `one-context` down with it; a `scrollIntoViewIfNeeded` on the control before `sliderOf`, as the `control-sentences` step does, would put the pointer on the range. This lane's probe drags that way and reads the product (section 4).

### 6. Deviations

- Finding 3 is fixed in this lane's file and not observable on any tier until R13 lands in `FormatOptions.tsx`: the row's second clause reads red on 4415 in section 4 for that reason, and its first clause on the deployment is F2's (B7). No park is proposed by this lane: the fix is small and named.
- The walk of section 4 waited for `.turboslide/e2e.lock`, held by another lane's `playwright test` from 14:24Z, and its handling broke the one browser rule for under three minutes: the first waiter (a harness bound shell with a ten minute cap) was replaced by a detached one at 17:09Z, but the first waiter's `node` child had taken the lock and started the walk at 17:08:49Z and outlived its shell, and the detached waiter then took the lock the other lane released at 17:11:32Z and started a second walk, stopped with its browser at 17:12:5xZ once seen; from 17:08:49Z to 17:11:32Z the other lane's spec run and this walk were up together, and from 17:11:32Z to 17:12:5xZ the two walks were. The JSON and the readings of section 4 are the first walk's (its `startedAt` and its deck `untitled-20260925-v8dy`, trashed and removed by its finally); the second walk's rows errored "Target page, context or browser has been closed" in the shared log, it wrote no JSON, and its scratch deck `untitled-20260925-ni5s` stayed on this lane's private tmp store, which ends with the server. The lock was released by a watcher on the first walk's exit at 17:14:18Z; the probe took it again with the `until mkdir` loop.
- F7 (the preset tiles' colours under the gallery's sentence, severity 1, this lane's `scripts/build-shader-previews.mjs`) was not in this fix's four findings and was not touched.
- The app's browser pane's deck `untitled-20260925-ie72` (section 4) could not be trashed by the probes: the pane and the probes are different anonymous principals on the memory tier, and `deck.trash` answered "You can view this presentation. Request edit access to change it" for the probes' one; the pane itself was closed before its cleanup ran. The deck stays on this lane's private tmp store under the scratchpad, which ends with the server (stopped at 17:35Z); nothing of it reached a deployment.
- The lock was released under this lane's probe once by another lane at about 17:21:15Z (the folder read free while probe 1 held it from 17:21:10Z to 17:21:59Z); the other lanes' scripts release the folder without owning it, so the lock is advisory on this machine today. This lane's own scripts released only what they took, apart from the watcher of the bullet above.
