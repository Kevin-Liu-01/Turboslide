# The focus round, b5 Documents

Notes of builder b5, the documents lane, in the shape of `integrator.md`. The binding specification is `docs/FOCUS.md`; the orchestrator's three rulings on its section 9 stand above it. Nothing here ran a git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build`. Day 1, 2026-09-15.

## 0. Owns

The lane prompt's list, in substance:

- `README.md` and `docs/readme/**`: the section "What works today" from FOCUS.md section 7, the parked features under an "Advanced tools" heading, the screenshots refreshed from a dev server or the preview when a surface changed, every screenshot showing the default view.
- `docs/FOCUS.md` links.
- `apps/studio/src/components/home/copy.ts` and the facts pipeline for any /home claim FOCUS.md retires (the shape count): `apps/studio/src/components/home/facts.ts` and the copy lint `copy.test.ts` where the retired key leaves the type.
- The four skills' prose, through `pnpm generate:contracts`, by request to the integrator (the lane writes the sentences; the integrator applies them and runs the generator).
- `docs/updates.md`, the entry of the focus round.
- `docs/gslides-parity/focus/build/b5.md`, this note.

Correction to `integrator.md` section 5: the integrator read `b5` as the server lane (`apps/studio/src/server/**`, `controller.tsx`, `packages/store`, `packages/realtime`, `packages/identity`, `access.ts`, `reduce.ts`, `docs/hosting.md`, ranks 1, 3, 5, 6, 7, 13, 19, 20, 21, 36). That is not this lane. b5 is the documents lane above and touches no product code outside `apps/studio/src/components/home/copy.ts`, `facts.ts` and `copy.test.ts`. The server files and ranks of that row need an owner (FOCUS.md section 5 names "the integrator" for the server area); request R1 asks the orchestrator to confirm. The working tree already carries edits to `apps/studio/src/server/authorize.ts`, `routes/api/access.$.ts`, `routes/s.$token.ts` and `editor/controller.tsx` from another lane, so someone is on it.

## 1. What the audits disproved on the README and /home

Read from `docs/FOCUS.md` sections 4, 5 and 7, `docs/gslides-parity/focus/audit-orchestrator-shapes.md` and `judge-sales.md` rejection 12:

| Claim on production                                                                                                                  | Where                                                                                         | What the audit saw                                                                                                                                                                                    | Change                                                                                                                                                                     |
| ------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| "135 presets in Google's four categories ... each drawn from the ECMA-376 preset geometry"                                           | `README.md` 146; `README.md` 404 ("the 135 shape presets with the ECMA geometry interpreter") | Every preset draws as its bounding rectangle (`shapePath` returns the box; FOCUS.md section 4 item 1; `audit-shapes` rows 4 to 8, 12 to 23)                                                           | The count leaves the README; the architecture line names the preset table and points at FOCUS.md section 4 for which presets draw their own path                           |
| "shape presets drawn from the PowerPoint geometry" (the numbers strip) and "N shapes in Google's four categories" (the objects card) | `copy.ts` `NUMBERS` `shapes`, `CARDS` `objects`                                               | The same (`audit-orchestrator-shapes.md` "Consequences", first item)                                                                                                                                  | The figure leaves the strip and the card; `shapePresets` leaves `HomeFacts` and `COUNT_KEYS` so the lint "reaches every fact key" holds without it                         |
| "An item Turboslide lacks is in its place, disabled, with one sentence on what to do instead"                                        | `copy.ts` `EDITOR.claims[0]`; `README.md` 53 ("22 are shown disabled")                        | Not disproved by an audit; changed by FOCUS.md 3.1: a Later stub is hidden with the switch off                                                                                                        | Rewritten: a feature not yet tested end to end is behind Tools > Advanced tools                                                                                            |
| "Every object drags, resizes, rotates, groups and reorders"                                                                          | `copy.ts` `EDITOR.claims[1]`; `README.md` 8                                                   | Groups passed their audit rows and are parked (FOCUS.md 3.2 Arrange)                                                                                                                                  | "groups" leaves the default view sentence                                                                                                                                  |
| "A read only link with Slide, Grid and Book views ... Skipped slides and speaker notes stay out of the view and present links"       | `copy.ts` `CARDS` `viewer`; `README.md` 227                                                   | Every link is an edit link in fact (`audit-present` rows 26, 29; FOCUS.md rank 1); the exclusion rows are not driven (`share.view-link-excludes-skipped-and-notes`, `.present-link-excludes-skipped`) | The card names the three links and the viewer's modes without the read only claim; the exclusion sentence is held until its rows pass (FOCUS.md 7)                         |
| "Pictures upload, crop, mask to any shape and adjust"                                                                                | `copy.ts` `CARDS` `objects`, `COMPARE` `pictures`                                             | Upload, drop and paste are refused as stale while a write is pending, a picture cannot go first on a new deck (`audit-images` rows 1, 6 to 8; ranks 5 and 6); masks are parked                        | The card and the row describe the picture path in the words of FOCUS.md 2.4 and name masks as advanced                                                                     |
| "Lists with nine bullet and six numbering presets ... autofit ... links to a URL or a slide"                                         | `copy.ts` `CARDS` `text`                                                                      | The toolbar list buttons leave a plain block (rank 9), autofit grow does not grow (rank 11), Cmd+K lands the URL as text (rank 2)                                                                     | The card names what the Format menu and the toolbar carry without the three claims; the list presets and autofit modes are not named                                       |
| "Apply layout keeps the content and never refuses"                                                                                   | `copy.ts` `CARDS` `layouts`; `README.md` 111                                                  | Apply layout carries untouched placeholders between layouts (rank 8, `audit-slides` rows 59 to 64)                                                                                                    | "moves the slide's content into the new layout's placeholders"; the README's sentence "moves only what you typed" is FOCUS.md 7's and is rendered only once the row passes |
| "Several people edit ... with live carets and pointers"                                                                              | `copy.ts` `COMPARE` `together`; `README.md` 243                                               | Edits from a second browser arrive late or not at all (rank 20); live pointers are parked                                                                                                             | The row names the roster, comments and version history by author; live pointers leave                                                                                      |
| "The same pipeline handles any photograph or a frozen frame of a shader material"                                                    | `copy.ts` `DITHERS.claims[0]`                                                                 | The Dither control sends `picture.dither` without its input (`audit-images` rows 50, 51, 54); the control is parked                                                                                   | The claim names the command line and the API as the path                                                                                                                   |
| "Every card is one thing the editor does today"                                                                                      | `copy.ts` `CARDS.lead`                                                                        | Eight of the twelve cards name parked features                                                                                                                                                        | The lead says what the default view is and where the rest sits                                                                                                             |
| "None; the Transition item is present and disabled with a sentence"                                                                  | `copy.ts` `COMPARE` `transitions`                                                             | Hidden with the switch off (FOCUS.md 3.1); `copy.test.ts` asserts the row starts with "None;"                                                                                                         | "None; the Transition item appears, disabled, only under Advanced tools"                                                                                                   |
| Help > Documentation answers 404                                                                                                     | `packages/chrome/src/dialogs/Help.tsx` 77 (`b3`'s, rank 37)                                   | `audit-export` row 39                                                                                                                                                                                 | Not this lane's file; the README's Documentation table is what the link should reach (request R6)                                                                          |

## 2. The README

`README.md` is rewritten from the top. The Feature tour (lines 46 to 312 of the production README) is gone; in its place:

1. The introduction keeps its first sentence (it is `SITE.description`, reused by the head, the manifest and the card; `packages/theme/brand/site.ts`), drops "groups" from the canvas sentence, and gains one paragraph on the rule of the focus round with a link to `docs/FOCUS.md`. The link list gains FOCUS.md and `docs/updates.md`.
2. "What works today" is rendered, never typed, between the markers `<!-- what-works:begin -->` and `<!-- what-works:end -->` by `docs/readme/what-works.mjs` from `docs/gslides-parity/focus/core-matrix.json` (read through `scripts/probes/core-matrix.mjs`, so a row added to the JSON reaches the README with no second edit) and from `docs/readme/what-works-data.mjs`, which carries FOCUS.md section 7's paragraphs verbatim, one per feature, with the pictures each feature shows. A feature's paragraph and pictures appear only while every row of the feature passes; a feature with a failed or not driven row is listed under "Held until every row passes" with the count and the ids of the rows that hold it; the outputs sentence of section 7 prints only when its five rows pass; the source line names the matrix, the renderer and either the audits' date or the run's origin, commit and date. Without `--results` the render reads the matrix's `today` (works passed, broken and flaky failed, not driven kept) and prints each held row's audit word; with `--results <run.json>` it reads a run of FOCUS.md 6.2 as `{ commit, origin, date, results: { "<id>": "passed" | "failed" | "not driven" } }` (request R10 to `b4` for that shape). `--check` exits 1 when README.md is not what the renderer writes; `--print` writes nothing. Today's render lists every feature as held, which is what FOCUS.md 6.3 says of production on 2026-09-15; the counts (10 of 43, 11 of 78, 22 of 66, 14 of 42, 17 of 56, 15 of 20, 5 of 5, 3 of 18, 9 of 18, 4 of 7, 2 of 5, 3 of 20, 1 of 4, 7 of 8) equal FOCUS.md section 7's held list.
3. "Advanced tools": the switch, hidden not disabled, blocks still render, the Font box exception, the parked set by menu in prose (a summary of FOCUS.md 3.2 to 3.4, which stays the binding list), the shapes and lines rule of section 4, the transports unchanged with `describe().state.settings.advancedTools` and `data-advanced-tools`, and the rule for bringing a feature back (section 8) with what returns first. No count is typed.
4. "The identity", "Why it is fast", "Getting started", "The CLI in brief", "Architecture", "Documentation" and "License" stay, with these changes: `pnpm check` no longer names a step count ("the acceptance chain"); the command list gains `node docs/readme/what-works.mjs`; the CLI section says every command runs whether or not its menu row is in the default view and names the commands of parked features as the advanced ones; the `packages/schema` paragraph names the preset table and `shapePath` with FOCUS.md section 4 for which presets draw their own path (the 135 leaves); the `packages/chrome` paragraph names the switch and the flag; the `scripts/` line names the probe's `--core` mode; the Documentation table gains `docs/FOCUS.md`, `docs/updates.md` and `docs/gslides-parity/focus/`.

Copy rules kept: sentence case headings without trailing periods, full sentences, no em dash (grep over the three documents finds none), Title Case only on button labels, the product written Turboslide, no count typed by hand in the rendered section.

`docs/readme/what-works.test.mjs` (vitest) asserts: every matrix feature has one entry; every paragraph and the lead are FOCUS.md's text verbatim (the outputs sentence in lower case inside FOCUS.md's longer sentence); every picture exists and is attached to exactly one feature, every alt and caption ends with a full stop; the render from today's matrix prints no paragraph, lists every held row with its audit word and the held counts add up to the matrix's broken, flaky and not driven rows; a run with every row passed prints every paragraph, every picture, the outputs sentence and no held list; one failed row holds its feature and a missing id is not driven; README.md carries the markers once and the block the renderer writes. The root vitest project `scripts` includes `scripts/**/*.test.mjs` only, so the file ran through a config under `.turboslide/` (section 7); request R3 adds `docs/readme/**/*.test.mjs` to the project so `pnpm test` and check step 5 carry it.

`docs/FOCUS.md` needed no link edit: nothing in it points at the README's old anchors, and the README now links to it from the introduction, the rendered section, the Advanced tools section and the Documentation table.

## 3. The /home copy

`apps/studio/src/components/home/copy.ts`:

- `NUMBERS`: the `shapes` figure is replaced by `checks` (the check step count, `formatCount(f.checkSteps)`, source `scripts/check.mjs --list; packages/theme/brand/facts.json`), so the strip keeps six figures (`copy.test.ts` asserts six and a source matching `.ts|.md|.json|LICENSE`).
- `EDITOR.claims`: the disabled stub sentence becomes "The default view holds what a seller uses weekly; a feature not yet tested end to end sits behind Tools > Advanced tools"; "groups" leaves the object sentence.
- `SHOT_ALT`: 03 (the Image submenu), 06 (a picture rotated), 07 (a picture dragged), 08 (Format options on a picture), 13 (typed text, light appearance) describe the pictures the shoot script takes (section 4).
- `CARDS`: the lead names the default view and the switch; `shell`, `canvas`, `layouts` (title "Layouts"), `text`, `objects` (title "Pictures", pictures 03 and 08), `tables`, `charts` (icon only), `exports`, `viewer`, `files` and `quality` are rewritten to what the default view holds and what sits behind Advanced tools, in the words of FOCUS.md section 2; `present` is unchanged. Twelve cards over the fifteen pictures, as the shape test requires.
- `DITHERS.claims[0]`: the pipeline runs "from the command line and the API".
- `COMPARE`: `account`, `together`, `export` (keeps "No ODP or SVG"), `import` (keeps "No PowerPoint import yet"), `pictures` and `transitions` (keeps "None;") rewritten as section 1 says.
- `AGENTS.closing`: one sentence that every action stays on every transport whether or not its menu row is in the default view.

`facts.ts`: `shapePresets` leaves `HomeFacts`, `COUNT_KEYS` and `HOME_FACTS`; the docblock records why. `copy.test.ts`: the two probe fact objects lose the key (a type change alone; no assertion weakened). `facts-data.ts` (generated by `scripts/build-home-assets.ts`) still carries `shapePresets: 135`, unread; request R5 drops it from the generator's list at the next build of the home assets. `packages/theme/brand/facts.json` keeps its `shapePresets` count (a fact of the source tree that no surface states any more).

Three cards describe the default view after the fixes of FOCUS.md section 5 land (text: the toolbar marks on a word and a link on a word; pictures: upload, drop and paste; layouts: content moved by Apply layout). If `text`, `images` or `slides` is in the ship's parked list, those three sentences must say "behind Advanced tools" for that release; request R9 names them for the ship step. The `viewer` card names the three links without a role claim, so it is true whether or not `share` ships.

## 4. The screenshots

`docs/readme/shoot-readme.mjs` shoots the fifteen pictures from a running Turboslide through the product's own controls, at 1440 by 900 with a device scale of 2 (2880 by 1800 JPEGs, the originals' size, which `scripts/build-home-assets.ts` reads), every one in the default view: `/new` (01); File > Download hovered (02); Insert > Image hovered (03); the layout plate from the toolbar with the last tile hovered (04); the filmstrip card menu with Duplicate slide hovered (05); a Section header slide's own picture shrunk by its south east handle, then rotated by its ring mid drag (06), dragged by its frame edge toward the sheet's centre mid drag (07), and Format options on it (08); File > Download > Microsoft PowerPoint with More options open (09); `/decks` in the card view (10); `/present/gt-brand` with a show connected from a second page of the same browser (11); `/deck/gt-brand?present=1` on slide 6, The Blue Marble, with the toolbar shown (12); the Title and body slide with typed text on a deck whose appearance is light, set by `deck.set /defaults/appearance` through the window API as a setup write (13); the viewer in book and grid view (14, 15). The scratch deck is written through the editor (the name in the title row, the two slides through the New slide arrow, the text into the runs), then moved to the trash, deleted forever and checked for a 404, the probe's own teardown. `--out` names the folder (default `docs/readme`), `--only 06,07` a subset, `--base` the origin; on a preview the run sends `VERCEL_OIDC_TOKEN` as `x-vercel-trusted-oidc-idp-token`. Exit 1 when a picture could not be taken.

Smoked today against this lane's dev server on 4365 (`TURBOSLIDE_STORE=tmp`, `TURBOSLIDE_REALTIME=memory`) under `.turboslide/e2e.lock`, into the session scratchpad and never into `docs/readme`: 14 of 15 pictures on the first full pass (08 timed out on a hidden twin of `toolbar.formatOptions`; fixed by taking the visible control), then 07, 08 and 12 on reruns after three fixes (a picture moves by its frame edge, a drag inside it pans the crop; the two undos run only when their picture is taken; the new slide's id is read from the slide list, not from the active slide, which moves a frame later). The reshoot of 13 with the last fix was queued behind another lane's lock (`b6`, `security.spec.ts`) when this note was written; section 7 records what it did.

The committed pictures under `docs/readme` are untouched today, on purpose:

1. The dev server's tmp store paints "Edits are kept on this server instance only ..." in the bottom bar of every editor picture; the pictures for the README come from the preview, which has the Blob store and no such line.
2. The default view is still landing: at 22:28 the shared `model.ts` carried 71 rows with `advanced: true` (the File > Download submenu showed PowerPoint and PDF alone, Insert showed Image, Text box, Shape, Line, Link, Comment and New slide, the filmstrip menu twelve rows), and the bottom bar's two view buttons and the shortcuts dialog were not yet behind the switch (`b3`'s open items).
3. A replaced picture fails check step 29 (`build-home-assets.ts --check` compares the sha256 of every source picture) until the integrator reruns `node scripts/build-home-assets.ts`, which rewrites `apps/studio/public/home/*.jpg`, `shots.json`, `shots.ts` and `facts-data.ts` (request R5).

The plan: after merge 1, with the flags of 3.2 to 3.4 landed, run `node docs/readme/shoot-readme.mjs --base <preview origin>` with the OIDC token in the environment (no lock needed against a deployment), read the fifteen pictures, and hand the integrator R5. Until then the README's alt texts and captions describe the pictures the script takes, not the pictures on disk; the difference is recorded here so nobody reads the old pictures as the default view.

Two observations from the smoke, for the lanes: the size readout and the snap guide did not appear in the mid drag pictures of the picture (06 and 07) on the dev server, although the picture moved and rotated; the caption of 07 therefore describes the drag and not a guide. Whether the guides draw for a layout's own picture after its first canvas write is `b2`'s to say (`images.guides.centre-x` is a matrix row). And the layout's covering picture is a slide level `picture` field, not a block with a position box, so `slide.get` names no picture object until the first handle drag converts the slide; the shoot selects it by a click and reads its id from the handles.

## 5. docs/updates.md

Created; the round's tree had none (`help.updates` is omitted in `model.ts` with "No release notes page", and round five's copy is on its own branch, untouched). One entry, "2026-09-15, the focus round": the rule, the switch, parked never deleted, the shapes decision, the matrix, sharing under ruling (2), the fixes by reference to FOCUS.md section 5, the transports. No count typed; the counts are the README section's and the release note's. The README links to it from the introduction and the Documentation table.

## 6. Matrix rows this lane covers

None. The documents lane drives no matrix row; it renders the README section from the matrix and the run, and its script takes pictures, which are never a driven step.

## 7. Commands run and their results

| Command                                                                                                                                                                                                                                  | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `node docs/readme/what-works.mjs` then `node_modules/.bin/prettier --write README.md` then `node docs/readme/what-works.mjs --check`                                                                                                     | wrote 26 lines between the markers; "README.md is current" after prettier (the render is prettier stable)                                                                                                                                                                                                                                                                                                                                                              |
| `node_modules/.bin/vitest run --config .turboslide/vitest.readme.config.mjs` (a config under the ignored `.turboslide/`, root the repository, include `docs/readme/**/*.test.mjs`)                                                       | 1 file, 8 passed                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `cd apps/studio && ../../node_modules/.bin/vitest run src/components/home`                                                                                                                                                               | 3 files, 26 passed (`copy.test.ts`, `facts.test.ts`, `shots.test.ts`); one earlier failure, the strip figure's source line not matching `/\.(ts\|md\|json)\|LICENSE/`, fixed by naming `facts.json` beside `check.mjs`                                                                                                                                                                                                                                                 |
| `node_modules/.bin/tsc -b apps/studio`                                                                                                                                                                                                   | errors in other lanes' in progress files only: `packages/chrome/src/comments/comments-model.ts` (five errors, `b3`), `packages/viewer/src/Editor.tsx(1444)` `SHEET` (`b2`), `packages/chrome/src/dialogs/FindReplace.tsx` two unused imports (`b1`); zero errors under `apps/studio/src/components/home`                                                                                                                                                               |
| `node_modules/.bin/prettier --check README.md docs/updates.md docs/readme/*.mjs apps/studio/src/components/home/{copy,facts}.ts apps/studio/src/components/home/copy.test.ts docs/gslides-parity/focus/build/b5.md`                      | clean                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `git show 2b12e31:packages/chrome/src/tokens.css \| cmp - packages/chrome/src/tokens.css`                                                                                                                                                | identical                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `grep -n "—\|–" README.md docs/updates.md docs/readme/what-works-data.mjs`                                                                                                                                                               | no match                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| the dev server: `TURBOSLIDE_STORE=tmp TURBOSLIDE_REALTIME=memory TURBOSLIDE_LOCAL_OPEN=1 TURBOSLIDE_SESSION_SECRET=<40 characters> TURBOSLIDE_DOWNLOAD_SECRET=<42 characters> node_modules/.bin/vite dev --port 4365` from `apps/studio` | `/new`, `/decks` and `/present/gt-brand` answer 200. A first start with a 12 character secret answered 500 on every route: `TURBOSLIDE_SESSION_SECRET must be at least 32 characters` (`apps/studio/src/server/auth/secret.ts` 35); the lane prompt's "any string" is any string of 32 characters or more. Stopped before this lane returned                                                                                                                           |
| `until mkdir .turboslide/e2e.lock; ...; node docs/readme/shoot-readme.mjs --base http://localhost:4365 --out <scratchpad>/shots-4365; rmdir .turboslide/e2e.lock`, 22:35                                                                 | 01 to 05 ok; the run stopped at the picture: "the Section header slide carries no picture" (the layout's picture is a slide field, not a block); the scratch deck trashed, deleted and 404 through the product                                                                                                                                                                                                                                                         |
| the same with `--only 06,...,15`, 22:38                                                                                                                                                                                                  | 06, 07, 09, 10, 11, 12, 13, 14, 15 ok; 08 failed (`toolbar.formatOptions` has a hidden twin; boundingBox timed out); the scratch deck trashed, deleted and 404                                                                                                                                                                                                                                                                                                         |
| the same with `--only 07,08,12,13`, 22:41                                                                                                                                                                                                | 12 and 13 ok; 07 and 08 failed in `selectPicture` because the two undos ran without their pictures and undid the resize (fixed: the undos run only with their pictures); the scratch deck trashed, deleted and 404                                                                                                                                                                                                                                                     |
| the same with `--only 07,08`, 22:44                                                                                                                                                                                                      | 07 and 08 ok; the scratch deck trashed, deleted and 404                                                                                                                                                                                                                                                                                                                                                                                                                |
| the same with `--only 13`, after the new slide id fix                                                                                                                                                                                    | queued behind `b6`'s lock (`security.spec.ts`) when this note was written; see the addendum at the end of this note                                                                                                                                                                                                                                                                                                                                                    |
| Pictures read: 02, 03, 05, 06, 07, 08, 11, 12, 13 (twice)                                                                                                                                                                                | 02, 03 and 05 show the default view as the shared tree had it (Download: PowerPoint and PDF; Insert: seven rows; the card menu without Change theme and Transition); 06 shows the rotated picture mid drag; 07 the picture at the sheet's centre mid drag without a guide line; 08 Format options with Size & rotation, Position and Picture; 11 "Slideshow connected"; 12 slide 6 of 85 with the toolbar; 13 showed slide 1 twice (the new slide id bug), fixed after |

Not run by this lane: `node scripts/check.mjs` (any step), `pnpm build`, the Playwright specs, the probe, any deploy.

## Requests

Under `integrator.md` section 6's protocol. R1 is for the orchestrator; R6 and R8 are for `b3`; R10 is for `b4`; the rest are for the integrator.

- R1 (orchestrator). Confirm `b5` is the documents lane with the Owns list of section 0, and name the owner of the server row `integrator.md` section 5 gave to `b5` (`apps/studio/src/server/**`, `apps/studio/src/editor/controller.tsx`, `packages/store/**`, `packages/realtime/**`, `packages/identity/**`, `packages/schema/src/access.ts`, `reduce.ts`, `docs/hosting.md`; ranks 1, 3, 5, 6, 7, 13, 19, 20, 21, 36; the enforce mode and the access record of ruling (2)). Why: two notes must not both claim `b5`.
- R2 (integrator; `skills/*/SKILL.md` and `packages/agent/src/generate/skills.ts`, then `pnpm generate:contracts`). The four skills document the parked actions as advanced (FOCUS.md 3.1, "The CLI, MCP, HTTP and window transports are unchanged ... documented as advanced"). The exact sentences, each one paragraph, to be placed as named:
  1. `skills/turboslide-api/SKILL.md`, a new paragraph at the end of "The Google Slides parity actions": "Since the focus round (docs/FOCUS.md) the editor's default view holds the features a seller uses weekly; every other menu row sits behind Tools > Advanced tools. The actions of those rows (tables, charts, diagrams, word art, the shape galleries other than rectangle, rounded rectangle and ellipse, the line kinds other than line and arrow, groups, masks, dithers, materials, icons, guides, the ruler, publish to the web, import slides, the bundle) stay on every transport and are the advanced actions: they run as before, and a block they insert renders everywhere, but a person sees their rows only with the switch on."
  2. `skills/turboslide-studio/SKILL.md`, a new paragraph under "Connect" after item 1: "The editor's default view shows the core menu rows alone (docs/FOCUS.md section 2); the rest are behind the check row Tools > Advanced tools, remembered in the browser. `describe().state.settings.advancedTools` reads the switch and `.pt-viewer[data-advanced-tools]` is present while it is on. A parked row is absent, not disabled, so `activate(label)` on its label finds nothing with the switch off; flip the switch through the product (`data-control=\"menu.tools.advancedTools\"`) or run the action through `invoke`, which the switch never gates."
  3. `skills/turboslide-create/SKILL.md`, one sentence at the end of "Before writing": "A block type the editor's default view no longer offers from a menu (table, chart, dia, icon, material, the gallery shapes) is still valid in the document and renders on every surface; it is edited by a person only with Tools > Advanced tools on (docs/FOCUS.md section 3)."
  4. `skills/turboslide-verify/SKILL.md`, one item at the end of "Completion rules": "- A feature is in the editor's default view only when every row of its part of the core matrix (`docs/gslides-parity/focus/core-matrix.json`) passes on the preview and on production; a row nobody drove is not driven, never passed (docs/FOCUS.md section 6)."
  5. `packages/agent/src/generate/skills.ts`, `generateActionsReference`, the paragraph after `GENERATED` gains the sentence: "An action whose menu row sits behind Tools > Advanced tools in the editor (docs/FOCUS.md section 3) runs on every transport exactly as before; the switch gates the rows a person sees, never the action." Then `pnpm generate:contracts` regenerates `skills/turboslide-api/references/actions.md` (check step 3 compares it).
     Why: FOCUS.md 3.1 and 7; the README's Advanced tools section says "the skills document the parked ones as advanced" and is true only once these land.
- R3 (integrator; `vitest.config.ts`). The `scripts` project's `include` gains `'docs/readme/**/*.test.mjs'` so `docs/readme/what-works.test.mjs` runs under `pnpm test` and check step 5. Why: section 2; today the test runs only through a config under `.turboslide/`.
- R4 (integrator; `scripts/check.mjs`). One command joins step 19 (`pnpm format:check`) or step 29's brand checks: `node docs/readme/what-works.mjs --check`, so a README whose rendered section is behind the matrix fails the chain. Why: FOCUS.md 7, "rendered, never typed".
- R5 (integrator; `scripts/build-home-assets.ts`). Drop `'shapePresets'` from its `COUNT_KEYS` so `facts-data.ts` stops carrying a value no surface prints (section 3), and rerun `node scripts/build-home-assets.ts` after the README pictures are replaced from the preview (section 4), which rewrites `apps/studio/public/home/*.jpg`, `apps/studio/src/components/home/shots.json`, `shots.ts` and `facts-data.ts`; then `--check` passes step 29 again. Why: the retired shape count and the refreshed pictures.
- R6 (`b3`; `packages/chrome/src/dialogs/Help.tsx` 77, rank 37, `help.documentation-link`). The Documentation link's `href` is `/docs`, which answers 404 on production. The address that exists and the /home nav already uses (`copy.ts` `home.nav.docs`) is `https://github.com/Kevin-Liu-01/Turboslide/blob/main/docs/README.md`; the README's own Documentation table is the other candidate (`.../blob/main/README.md#documentation`). Either makes the row pass. Why: rank 37; the docs index is the page the link promises.
- R7 (integrator; `docs/README.md`, the index). Rows for `FOCUS.md` (the binding specification of the focus round, the synthesizer, 2026-09-15), `updates.md` (the release notes, b5, 2026-09-15), `readme/what-works.mjs`, `readme/what-works-data.mjs` and `readme/shoot-readme.mjs` (the README renderer and the picture shoot, b5, 2026-09-15) and `gslides-parity/focus/` (the audits, the judgments, the matrix and the build notes). Why: the index lists every document; the README's Documentation table now points at these.
- R8 (`b3`, for a later round, not this ship; `model.ts` `help.updates`). The row is omitted with the reason "No release notes page"; `docs/updates.md` now exists. Under section 8's rule the row returns only with a matrix row of its own, so this is a note for the later list, not a change for this round.
- R9 (integrator, the ship step). Three actions on the ship's commit: (a) `node docs/readme/what-works.mjs --results <the ship's run JSON>` then prettier, so the README section carries the run's origin, commit and date and the parked features are listed from the run, never typed; (b) `node docs/readme/shoot-readme.mjs --base <preview>` under the ship's commit, the fifteen pictures read, then R5's rebuild; (c) if `text`, `images` or `slides` is in the ship's parked list, the three sentences of section 3 in `copy.ts` (`CARDS` `text`, `objects`, `layouts`) are edited to "behind Advanced tools" for that release, and if `share` is parked the `viewer` card stands as written (it makes no role claim). Why: FOCUS.md 6.2 and 7; the /home page cannot read the run at runtime.
- R10 (`b4`; the run JSON of FOCUS.md 6.2). The renderer reads `{ "commit": "<sha>", "origin": "<url>", "date": "<yyyy-mm-dd>", "results": { "<id>": "passed" | "failed" | "not driven" } }`, or the `results` map alone (an id the run does not name is not driven, the rule of `parkedFeaturesOf`). If the probe's `--core` mode and the core specs write another shape, name it in `b4.md` and this lane adapts `readResults` in `docs/readme/what-works.mjs`; the by feature parked list the ship note renders can come from the same file through `parkedFeaturesOf`.

## Deviations and decisions recorded

1. The README section is rendered by a script under `docs/readme/` rather than by `docs/gslides-parity/focus/render-focus.mjs`, which renders FOCUS.md itself and is not this lane's; the paragraphs are held once in `what-works-data.mjs` and pinned to FOCUS.md's text by the test, so the two cannot drift.
2. The held list in the README prints the ids of the rows that hold a feature, as FOCUS.md 7 says ("listed as held with the rows that hold it"), but not the held paragraph, so the README never describes a feature that does not pass (the sales judgment's rejection 12).
3. `facts.ts` and `copy.test.ts` are edited by this lane under the "facts pipeline" grant of the lane prompt; `integrator.md` lists `apps/studio/src/components/**` under `b3`. The edits are the removal of one key from a type, an array and two test fixtures.
4. The numbers strip keeps six figures by replacing the shape count with the check step count, which already flows through the facts; a matrix row count on /home would need a `coreRows` fact in `facts.json` (`scripts/build-brand.ts --facts`) and is not asked for this round.
5. The screenshots are refreshed from the preview after merge 1, not from today's dev server, for the three reasons of section 4; the script is smoked and the pictures on disk are the round four originals until then.

## Addendum, 23:06

The reshoot of picture 13 with the new slide id fix did not run: `b6` held `.turboslide/e2e.lock` from 22:48 (`security.spec.ts`, then `share.spec.ts`) through 23:06, this lane's queued command was stopped before it could take the lock, and the lock stayed `b6`'s. The fix (`newSlideWithLayout` reads the new id from `slide.list` before and after the tile click instead of the active slide) is therefore unverified; the first thing the merge 1 shoot on the preview will show is whether 13 lands on the Title and body slide. The dev server on 4365 was stopped; no lock is held by this lane; the pictures under `docs/readme` are unchanged.

## Fix round

Written 2026-09-16 by the b5 fixer against `docs/gslides-parity/focus/VERIFICATION.md` (pass 1). The one finding this lane was given is F2 (severity 2): `skills/turboslide-api/SKILL.md` was 52 lines against the 50 line cap of `packages/agent/src/__tests__/skills.test.ts` (`MAX_LINES`), so check step 5 (`pnpm test`) was red on the tree.

### What the mechanism was

The four `SKILL.md` files are hand written prose; `packages/agent/src/generate/skills.ts` writes only the `references/*.md` tables, so the generator and its committed outputs were not the cause (`node src/generate/main.ts --check` from `packages/agent` reports every committed contract current on this tree). R2 item 1 of this note asked for the advanced sentence "as a new paragraph at the end of The Google Slides parity actions"; a paragraph is a blank line plus a line of text, and the file was exactly 50 lines at `2b12e31`, so the request as written could not fit. The other three skills stand at 34, 41 and 41 lines.

### The fix

`skills/turboslide-api/SKILL.md` only. The advanced sentence now closes the paragraph that points at `references/actions.md` (the paragraph that already carries the transport rule "An action whose milestone has not landed answers `NotImplementedError` (501)"), and the separate paragraph is gone. The list of advanced actions is unchanged (tables, charts, diagrams, word art, the shape galleries other than rectangle, rounded rectangle and ellipse, the line kinds other than line and arrow, groups, masks, dithers, materials, icons, guides, the ruler, publish to the web, import slides, the bundle) and still matches FOCUS.md 3.2 to 3.4 and section 4; the wording was tightened to one sentence with no em dash. The file is 50 lines with its trailing newline. `packages/agent/src/generate/skills.ts` is untouched: its sentence lives in the preamble of `references/actions.md`, which has no line cap. The README (line 92) and `docs/updates.md` (line 17) say the skills document the parked actions as advanced; both stay true.

This edit was made directly rather than by request because the finding's owner is "integrator, with b5 (the sentence)" and the fixer prompt places a fix inside the owner's files; if the integrator prefers to re-apply it, the text above is the whole change. No matrix row was touched (a prose edit with no product change), so no row was rerun. No dev server was started and no Playwright ran.

### Commands run and their results

| Command (from the directory named)                                                                                                                                          | Result                                                  |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| `wc -l skills/*/SKILL.md` (repo root)                                                                                                                                       | 50, 34, 41, 41                                          |
| `../../node_modules/.bin/vitest run` (`packages/agent`)                                                                                                                     | 12 files, 243 tests passed (the skills test among them) |
| `node src/generate/main.ts --check` (`packages/agent`)                                                                                                                      | every committed contract is current                     |
| `node_modules/.bin/prettier --check skills/turboslide-api/SKILL.md README.md docs/updates.md docs/gslides-parity/focus/build/b5.md apps/studio/src/components/home/copy.ts` | all matched files use Prettier code style               |
| `node docs/readme/what-works.mjs --check` (repo root)                                                                                                                       | README.md is current                                    |
| `node_modules/.bin/vitest run docs/readme` (repo root)                                                                                                                      | 1 file, 8 tests passed                                  |
| `../../node_modules/.bin/vitest run src/components/home` (`apps/studio`)                                                                                                    | 3 files, 26 tests passed                                |
| `../../node_modules/.bin/vitest run src/menus/__tests__/default-view-words.test.ts` (`packages/chrome`)                                                                     | 1 file, 8 tests passed                                  |

### For the integrator

- Nothing to apply for F2 beyond the edit above; check step 5's second red test (F1, `authorize.test.ts`) is b6's or the integrator's and is not touched here.
- A note for the ship, under ruling (1): if the `shapes.*` rows do not pass on the enforce preview and on production and Insert > Shape leaves the default view, the phrase "the shape galleries other than rectangle, rounded rectangle and ellipse" in `skills/turboslide-api/SKILL.md` and the README's Insert bullet become "the shapes" (every preset parked, the lines feature standing on its own). This lane will make that edit when the ship's parked list is known; it is not a change for this fix round.

## Cycle 2 (the integrator, 2026-09-16)

The documents lane did not run in the second cycle; the integrator made the two edits this note's fix round section planned for the ship, in this lane's files, and records them here (`integrator.md` "Cycle 2 merge" carries the same rows):

- `README.md`: the section "What works today" re-rendered from the 397 row matrix with `node docs/readme/what-works.mjs` then prettier (`--check` reads "README.md is current"; b1 R28, b4 C2-R2). In the Advanced tools section the Insert bullet's phrase "the shape galleries other than Rectangle, Rounded rectangle and Ellipse" became "the shapes (Rectangle, Rounded rectangle and Ellipse, and the galleries: the block arrows, the callouts and the equation signs)" and "the line kinds other than Line and Arrow" became "the lines (Line and Arrow, the rule, ...)", and the shapes and lines paragraph gained one sentence saying both sit behind the switch in this release with the pass 2 counts (4 of 20 shapes rows, 1 of 5 lines rows failed on the preview), since Insert > Shape and Insert > Line are parked whole under ruling (1) (b1's cycle 2 parking edit; b3 R16).
- `skills/turboslide-api/SKILL.md` line 16: "the shape galleries other than rectangle, rounded rectangle and ellipse, the line kinds other than line and arrow" became "the shapes, the lines"; the file stays at 50 lines (the cap of `packages/agent/src/__tests__/skills.test.ts`).
- `docs/updates.md`: the shapes bullet gained the sentence "At the first ship of the round both sit behind Tools > Advanced tools: the verifier's second pass failed 4 of 20 shapes rows and 1 of 5 lines rows on the preview."

`node docs/readme/what-works.mjs --check` exits 0 and `docs/readme/what-works.test.mjs` passes in the root vitest run after the render. The README pictures under `docs/readme` are unchanged (the shoot is the ship step's, R9).
