# B3, the objects editing model

## Features round, ship one

Notes of builder B3 of the features round, ship one (docs/FEATURES.md section 2 whole, the lane table of section 6, the rows of 7.1). The binding specification is `docs/FEATURES.md`; `docs/PRODUCT.md`, `docs/SYNC.md`, `docs/RETURN.md`, `docs/FOCUS.md` and `docs/gslides-parity/focus/AMENDMENTS.md` A1 stand under it, A1 rules 1 and 3 amended for tables alone by FEATURES.md 2.1. Started 2026-09-22 on the shared checkout at `88b68e7` while B1, B2, B4, B6 and B7 landed beside it. The dev server port is 4413 (`TURBOSLIDE_STORE=tmp`, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, two secrets of more than 32 characters, `apps/studio/vite.no-watch.config.ts`), stopped before the report. No git write command, no `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran. Typecheck with `node_modules/.bin/tsc -b`, tests with `node_modules/.bin/vitest run` per package.

Every item below lands as one commit in the integrator's merge (docs/FEATURES.md section 6: "each item of sections 2 to 5 lands as one commit, so a red row without `parks` reverts one commit in the fix round"); section 2 names the files and the hunks of each item so a red row's item reverts alone.

### 0. Requests, written first

- R1, the integrator, `packages/chrome/src/menus/strings.ts`: one word added to `CANVAS`, `editData: 'Edit data'` (the button under the selected chart, docs/FEATURES.md 2.2 rank 7). The file is B1's by request to the integrator (section 6); the one line is in this lane's diff so the Overlay typechecks, and the integrator keeps or moves it.
- R2, the integrator, the render snapshots `packages/render/src/__tests__/__snapshots__/blocks.test.ts.snap` and `canvas.test.ts.snap`: the chart snapshots (eight) were regenerated with `vitest run -u` on the two test files after every bar, point and slice gained `data-series` and `data-category` (rank 7; render/blocks/chart.ts is this lane's). No other snapshot changed; `git diff` on the two files shows the attributes alone.
- R3, B4 (a record): B4's drivers of the features round rows landed in `scripts/probes/core-walk/areas/{tables,charts,diagrams,shapes,wordart,arrange}.mjs` while this lane built, so this lane drove its rows with the walk (section 3) and by hand where the walk read a mechanism it could not name. Two readings the drivers and this lane's implementation may disagree on are recorded in section 3 by id.
- R4, B6 (answered): B6's R6 to this lane, the chip word `Logo` for a `shot` or `picture` whose asset carries `role: 'logo'`, landed in `packages/viewer/src/Selection.tsx` `blockDisplayName(slide, blockId, assets?)`, with `Editor.tsx` passing `shown.deck.assets` at its two call sites. Without the assets table the word stays Image, so every other caller is unchanged.
- R5, the integrator (a record, no action): `apps/studio/src/editor/EditorRoot.tsx` gained `isControlRefusal` and the sentence `CONTROL_REFUSED` beside the reject card (rank 8, the refusal card's gate); the sentence is one string in the file, not in `strings.ts`, so the file compiles without a request.
- R6, the integrator, `packages/chrome/src/ToolbarHead.tsx` (no lane's in section 6): `ToolbarButtonProps` gains an optional `value?: string` drawn as `data-value` on the button, so the Align control reads its effective alignment (`data-value="center"` on a closed shape with no explicit alignment; docs/FEATURES.md 2.2 rank 3, the row `shapes.label.centred-default`: "the tail's Align reads centre"). One prop, one attribute; `ToolbarTail.tsx` passes it for the `align` op alone.
- R7, B4, the drivers of `scripts/probes/core-walk/areas/tables.mjs`, `charts.mjs` and `diagrams.mjs`, from the two runs on this lane's server (section 3):
  - `tables.cell.click-places-caret`: the drag of the second table lands on the snap grid (860 to 944 for a move of 80: the sheet's snaps, `snapGuides` on by default), so "moves that table exactly" reads 84 by 40. Place the table at a multiple of 8 (`x: 864`), hold Cmd for the drag (`mods: ['meta']` skips the snaps), or read the move within the snap's tolerance. The click half of the row passes.
  - `tables.range.bold-italic`, `tables.range.size-color`, `tables.panel.table-first`, `diagrams.step.one-object`: the revision check `rev1 === rev0 + 1` reads `describe().state.revision` right after `t.settled()`, and on the memory tier the room's revision follows about two seconds later (this lane's probe: `rev 3` at 0.5 s to 1.5 s after Cmd+B, `rev 4` from 2 s, `pending 0` throughout, the save words "All changes saved" after); `t.waitRevision(rev0 + 1)` before the read, as `t.setBlock` does, reads the bump.
  - `tables.panel.table-first`: the sections are read from `[data-control^="formatOptions.section."][data-control$=".toggle"]`, an attribute no section head carries; the head is `data-section-toggle="formatOptions.section.<id>.toggle"` with `data-control="formatOptions.<id>"` (FormatOptions.tsx `Section`), so "sections in order" read empty and `first` false while the Table section stood first at 152 px.
  - `tables.range.size-color`: a table's text size is its `size` ladder (blocks/table.ts `TABLE_SIZES`, 20 down to 15; a table has no per cell size and the run model has no size mark), so from the default 20 the size step up has nowhere to go and now says "A table's text is 20 px at most"; step down instead, or place the table at `size: 18` and step up.
  - `charts.panel.no-duplicate-controls`: "Title 2" counts the Chart data section's `<label>` and the `.ts-chart-label` span inside it as two, and "textareas 1" is the Alt text section's Description field, which a chart carries in the default view (docs/PRODUCT.md section 5); count one of the two selectors and the textareas under `[data-section="chart"]`.
  - `diagrams.insert.group`, `diagrams.edit-label`, `diagrams.light-appearance`, `diagrams.present`, `diagrams.reload`: a step is one shape with its label since rank 9, so "five labels" are the five shapes' texts (`block.text` on the `roundRect` shapes, the `.shape-text` layer on the stage), never text blocks; the five drivers read text blocks and now read 0 labels.
- R8, B7 or the integrator, `apps/cli/src/store-actions.ts` `diagramInsert` (3226 to 3268): the blocks' ids are freed against the slide (`freeId`, `step-2` becomes `step-2-2` when a diagram already sits on the slide) and the connectors' `connect.start.block` and `connect.end.block` keep the template's ids, so a second diagram on a slide binds its connectors to the first diagram's steps (or to nothing) and a moved step leaves its arrows behind. The id mapping should pass through `renameConnectorRefs` (already imported at line 48 for the paste path) before the inserts. This lane read it because `diagrams.step.one-object` inserts a second process diagram on the area's slide and read "0 of 2 connectors moved (0 bound to the step)"; a first diagram on a fresh slide follows its connectors.

### 1. What this lane changed, by item (docs/FEATURES.md 2.2 rank order)

Every item names the rank, the mechanism as read in the source, the change, its tests and its rows. Section 3 records what the runs read on 4413.

#### Rank 1, the chart grid owns its keys (`charts.grid.type-to-edit`, `charts.grid.escape-stays`)

- The mechanism (audit-objects 1): the stage's window capture keydown consumed a bare letter on the grid's `td` because the Format options panel root was not in `CHROME_CONTROL`.
- `packages/viewer/src/Editor.tsx` `CHROME_CONTROL`: `[role="grid"]`, `[role="gridcell"]`, `[role="columnheader"]`, `[role="rowheader"]` and `.ts-panel` join the selector, so every key pressed inside a panel is the panel's.
- `packages/chrome/src/inspector/chart.tsx` `onCellKey`: Escape on the active cell with no open field blurs the cell (leaves the grid) and stops; the section stays mounted and the chart stays selected because the stage never sees the key.
- Tests: `packages/chrome/src/__tests__/chart-grid.test.tsx` "Escape on the active cell with no open field leaves the grid and keeps the section drawn".

#### Rank 2, one click places the caret, a drag from a cell of the selected table selects a range, a drag from inside an unselected table moves it (`tables.cell.click-places-caret`, `tables.cell.click-then-type`, `tables.range.drag-from-selected`)

- `packages/viewer/src/Selection.tsx` `objectPressPlan`: a new input `cell` (the cell of a table the press landed in); the `press` plan gains `caret` (the cell a pointer up without a move opens at the click point) and `range` (a move from the cell selects a range instead of moving the table). Rules: an unselected table selects, arms the drag and names the cell; the one selected table names the cell with `range: true` and `drag: false`; a table in a multiple selection, in a group or under a modifier keeps A1 as written; Commenting and Viewing mode keep their rule.
- `packages/viewer/src/Editor.tsx`: `Press` gains `tap`, run by `armPress` on a pointer up with no move; `armCellRangeDrag` takes `{ blockId, anchor, session? }` and a `tap`, so the range drag works from an open cell (as before) and from a cell of the selected table with no session; `openCellAt` opens the run at the click point and `startEdit` records the cell as `lastCell`; `onPointerDown` reads the pressed cell (`resolveRun`, `cellPointer`) and hands it to the plan; a tap on a mark of the selected chart is the same `tap` (rank 7).
- The ring band as a move target while a cell is open: `Editor.tsx` computes `tableFrame` (a table with a cell open or a cell or range active) and hands the table's free-move chip and eight resize handles to the view; `packages/chrome/src/Overlay.tsx` draws the chip as a button and the four frame edges while `editing && tableFrame` (`frameLive`); `onHandleDown` ends the open session first when the handle is the edited table's. The column seams stay hidden while a cell is active (docs/RETURN.md 2.4).
- Tests: `packages/viewer/src/__tests__/press-rules.test.ts` "objectPressPlan on a table" (three cases); `packages/chrome/src/__tests__/overlay-component.test.tsx` "keeps the chip and the frame edges as the move surface while a table cell is open" and "draws a static chip and no frame edges while another run is being edited".

#### Rank 3, a shape's label is centred (`shapes.label.centred-default`)

- `packages/render/src/block-css.ts` `.ts-sheet .shape-text`: `display: flex; flex-direction: column; justify-content: center; text-align: center` as the default; the inline `text-align` of `typography.align` and the flex of `valign` (primitives.ts) override it, and the exporter reads the computed style (`packages/export/src/scene/measure.ts` 797 `alignOf(cs.textAlign)`).
- `packages/chrome/src/ToolbarTail.tsx` `align` options: a closed shape with no explicit alignment reads Center and Middle.
- Tests: none new in render (the CSS string is not pinned); the row measures the drawn centre.

#### Rank 4, a key on a selected table appends in the remembered cell (`tables.selected.typing-appends`)

- `packages/viewer/src/Editor.tsx` `entryRunOf`: the anchor cell of a range, else the table's `lastCell` (set by the selecting click's tap and by every cell session), else the first run; `entryCaretFor`: `'end'` on a table for a typed key and Enter, `entryCaret` elsewhere. `table-session.ts` is unchanged: `lastCell` already lived in the Editor and follows the selection (cleared when the selection leaves the table).

#### Rank 5, the arrows cross cells (`tables.cell.arrows-cross-cells`, `tables.range.shift-arrows`)

- `packages/viewer/src/InlineText.tsx`: `CaretPlacement` gains `'start'`; the prop `cellArrows`; `arrowLeavesCell` (exported, pure over the DOM selection: Right at the text's end, Left at its start, Up on the first visual line and Down on the last, read from the caret's rectangle against the text's line boxes; a plain arrow from a collapsed caret, Shift from the selection's edge); the end reasons `arrow-left|right|up|down` and `range-left|right|up|down`.
- `packages/viewer/src/table-range.ts` `adjacentCell(block, cell, direction)`: Left and Right in reading order over the drawn cells, Up and Down in the column; a merged cell counts as one; null at the grid's edge.
- `packages/viewer/src/Editor.tsx` `endEdit`: the four arrow reasons open the adjacent cell through `pendingEdit` with the caret at the matching edge (`start` after Right and Down, `end` after Left and Up); the `range-*` reasons select the table and set the range from the cell to the adjacent one; at the grid's edge the cell stays selected as a run. The stage keydown: Shift with an arrow while a range stands on the selected table steps the range's focus. `cellArrows` is passed to `InlineText` for a table cell.
- Tests: `packages/viewer/src/__tests__/table-range.test.ts` "adjacentCell" (four cases, merged cells among them).

#### Rank 6, a double click on a diagram label opens it, Tab moves (`diagrams.label.double-click-opens`, `diagrams.label.tab-next`)

- `packages/viewer/src/Editor.tsx` `onDoubleClick` `member` case: a member that carries a run (a text box, a shape with its label) opens its text at once with the member entered behind it; `endEdit` `tab`/`shift-tab` on a block in a group moves to the next or previous label of the group in document order with its text selected (`groupLabelRuns`), the member entered; the stage's Enter on a selected group opens the first label.

#### Rank 7, a double click on a chart opens its numbers (`charts.double-click.opens-data`, `charts.mark.click-selects-cell`)

- `packages/render/src/blocks/chart.ts`: every bar (`rect`), point (`circle`) and slice (`path`, the one slice `circle`) carries `data-series` and `data-category`, after the geometry attributes so the existing tests' regexes hold.
- `packages/viewer/src/Editor.tsx`: `CHART_CELL_EVENT` (`turboslide:chart-cell`) and `ChartCellDetail`; `requestChartCell`; a double click on a chart (not on its title run) asks for the 1,1 cell with `open`; Enter on a selected chart the same; a tap on a mark of the selected chart asks for the mark's cell and shows `<category>: <value>` in the readout chip for 1.5 s (`markReadout`, the view's `valueReadout`); the view's `editData` names the selected chart.
- `packages/chrome/src/Overlay.tsx`: listens to the event and opens Format options on the Chart data section when `open` is set for its slide; draws the Edit data button (`bar.chart.editData`, `.ts-bar-btn`, `packages/chrome/src/Overlay.css`) under the selected chart's ring unless `parked-controls.ts` `isParked('bar.chart.editData', shell.settings)` says the ship parked it; the readout chip shows `valueReadout` when no gesture readout stands.
- `packages/chrome/src/inspector/chart.tsx`: a module level listener keeps the last requested cell per chart (`pendingCells`) and notifies mounted sections; a section mounting reads it as its initial active cell (clamped); a mounted section makes it active and focuses it.
- Tests: `overlay-component.test.tsx` "draws the Edit data button under a selected chart inside the shell", "draws no Edit data button without a chart or outside the shell", "shows a tapped chart mark's value", "opens Format options on Chart data when the stage asks for a chart cell"; `chart-grid.test.tsx` "makes the cell the stage names active, mounted or mounting".

#### Rank 8, text formatting on a range writes every cell (`tables.range.bold-italic`, `tables.range.size-color`)

- `packages/chrome/src/editor-shell.ts`: `tableTextTargets(table, selection)` (the range's drawn cells, the caret's cell, or every drawn cell, each with its text and whole plain range; empty cells left out); `tableMarksPlan(facts, table, edit, label)` (one `slide.update` of `text.mark` per cell: a mark cleared when every cell carries it whole, set otherwise; `b` through the bold run; a colour set or cleared); `format.text.bold` on a table goes through it; `textStylePlan` on a table with a range or with no cell goes through it (a caret in one cell keeps the cell's `text.style`; the highlight keeps its path); `format.alignIndent.*` on a table writes the range's columns, the caret's column, or every column. The refusal when no selected cell has text: "Type into a cell first".
- `packages/viewer/src/Editor.tsx`: `toggleWeight` (Cmd+B) and `toggleMarkOnSelection` (Cmd+I, Cmd+U, Cmd+Shift+X, Cmd+., Cmd+,) on a table write one `text.replace` per cell over the range or the whole table (`tableMarkTargets`, `tableMarkMutations`), one commit.
- `apps/studio/src/editor/EditorRoot.tsx`: the refusal card's gate. A reject notice with `reason: 'invalid'` and no typed text (`isControlRefusal`) is said once in the snackbar as "The change could not be applied to this object" and dismissed; the card and the save state's conflict word read the remaining notices alone.
- Tests: `editor-shell.test.ts` "Bold, Italic, a colour and Align on a table write every selected cell in one slide.update"; the size step keeps its `/size` write on the table (unchanged).

#### Rank 9, a diagram step is one shape with its label (`diagrams.step.one-object`, `diagrams.export.step-label`)

- `packages/schema/src/diagrams.ts` `node(id, shape, label, box, look, z, group)`: one `ShapeBlock` with `text`, `typography: { align: 'center', weight: 500 }`, `valign: 'middle'`, `padding: 16` and the style's text colour; no text block. The grid, hierarchy, process, relationship and cycle templates use it; the timeline keeps a text block under each dot. `nodes()` keeps its counts.
- `packages/chrome/src/DiagramPanel.tsx` unchanged: the preview (`pickers/DiagramPicker.tsx`, B1's) draws shapes and lines and never drew the labels.
- Tests: `packages/schema/src/diagrams.test.ts` (the labelled node assertions read the shape's text; no text block outside the timeline; the process fixture case reads the steps' text).

#### Rank 10, Legend > None from the toolbar writes none (`charts.legend.none-from-toolbar`)

- `packages/chrome/src/ToolbarTail.tsx`: the `legend` and `numberFormat` picks go through `chartTailPlan` (chart-tools.ts) and `chartWriteInput`, so None writes `legend: 'none'` and Right removes the field; the legend list's check mark reads `legend ?? 'right'`.

#### Rank 11, Format options lists each chart property once (`charts.panel.no-duplicate-controls`)

- `packages/chrome/src/FormatOptions.tsx`: `renderGeneratedAdvanced` draws the generated rows of the chart and the table sections only while Tools > Advanced tools is on; the Table section draws its slot first and the generated rows after, as the Chart data section does.
- Tests: `format-options-round-two.test.tsx` "the chart and the table sections".

#### Rank 12, the grid's remove controls are visible (`charts.grid.remove-visible`)

- `packages/chrome/src/inspector/chart.css`: the series swatch and the plate swatch at 16 px; the remove control at opacity 1 on `.is-active-line` (the header of the active cell's row or column) beside the hover and active rules; the header menu's plate.
- `packages/chrome/src/inspector/chart.tsx`: `is-active-line` on the row and column headers of the active cell; a right click on a series or category header opens a one row menu (`formatOptions.chart.menu`, `formatOptions.chart.menu.remove`, "Remove <name>") that runs the same `removeSeries` or `removeCategory` edit; Escape or a click elsewhere closes it; the row is disabled with the cap's sentence at one series or one category.
- Tests: `chart-grid.test.tsx` "marks the active cell's row and column headers", "a right click on a series header lists Remove".

#### Rank 13, the Table section leads (`tables.panel.table-first`)

- `packages/chrome/src/inspector/format-sections.ts`: `leadingFormatSection(block)` and `sectionsInOrderFor(sections, block)`; `FormatOptions.tsx` sorts through them (a chart and a table lead).
- `packages/chrome/src/inspector/table.tsx`: the Row heights list (one field per row) becomes one Height field (`formatOptions.table.height`) for the range's rows, the caret's row, or every row of a table selected by one click, writing `/rows` once; the placeholder reads `mixed` when the rows differ; Distribute rows and Distribute columns stay (`formatOptions.table.distributeRows`, `.distributeColumns`).
- Tests: `table-section.test.tsx` (the two Height field cases replace the row heights case); `format-sections.test.ts` "the leading section".

### 2. Files and hunks per item, for a revert alone

| Item (rank) | Files and hunks |
| --- | --- |
| 1 | `packages/viewer/src/Editor.tsx` (`CHROME_CONTROL`); `packages/chrome/src/inspector/chart.tsx` (`onCellKey` Escape case); `chart-grid.test.tsx` (the Escape case) |
| 2 | `packages/viewer/src/Selection.tsx` (`PressPlan`, `objectPressPlan`); `packages/viewer/src/Editor.tsx` (`Press.tap`, `armPress`, `armCellRangeDrag`, `openCellAt`, the `pressedCell` read and the `press` case of `onPointerDown`, `tableFrame`, the `handles` branch, `onHandleDown`'s session end, the view's `tableFrame`); `packages/chrome/src/Overlay.tsx` (`frameLive`); `press-rules.test.ts`, `overlay-component.test.tsx` (the two frame cases) |
| 3 | `packages/render/src/block-css.ts` (`.shape-text`); `packages/chrome/src/ToolbarTail.tsx` (`closedShape` in the `align` options) |
| 4 | `packages/viewer/src/Editor.tsx` (`entryRunOf`'s `lastCell` branch, `entryCaretFor`, its two callers) |
| 5 | `packages/viewer/src/InlineText.tsx` (`CaretPlacement`, `placeCaret`, `CellArrow`, `arrowLeavesCell`, the reasons, the `cellArrows` prop and its `onKey` branch); `packages/viewer/src/table-range.ts` (`CellDirection`, `adjacentCell`); `packages/viewer/src/Editor.tsx` (`endEdit`'s arrow cases, the Shift arrow branch of the stage keydown, `cellArrows` on `InlineText`); `table-range.test.ts` |
| 6 | `packages/viewer/src/Editor.tsx` (`groupLabelRuns`, the `member` case of `onDoubleClick`, the group branch of `endEdit`'s `tab`, the group branch of the `enter` key) |
| 7 | `packages/render/src/blocks/chart.ts` (`data-series`, `data-category`); the two render snapshots; `packages/viewer/src/Editor.tsx` (`CHART_CELL_EVENT`, `ChartCellDetail`, `MARK_READOUT_MS`, `markReadout`, `requestChartCell`, `chartMarkAt`, `showMarkReadout`, the chart branches of `onDoubleClick` and the `enter` key, the mark tap in the `press` case, the view's `editData` and `valueReadout`); `packages/chrome/src/Overlay.tsx` (the imports, the listener, `editData`, the button, `barStyle`, the readout fallback); `Overlay.css` (`.ts-bar-btn`); `strings.ts` (`CANVAS.editData`); `packages/chrome/src/inspector/chart.tsx` (the module listener, `requestedCell`, the section's listener effect); `overlay-component.test.tsx`, `chart-grid.test.tsx` (the event cases) |
| 8 | `packages/chrome/src/editor-shell.ts` (the table-tools import, `TYPE_INTO_CELL`, `tableTextTargets`, `textIsBold`, `tableMarksPlan`, the table branches of `textStylePlan`, `format.text.bold` and `format.alignIndent.*`, `parseText` import); `packages/viewer/src/Editor.tsx` (`tableMarkTargets`, `tableMarkMutations`, the table branches of `toggleWeight` and `toggleMarkOnSelection`, the marks imports); `apps/studio/src/editor/EditorRoot.tsx` (`isControlRefusal`, `CONTROL_REFUSED`, the gate effect, `conflicts`); `editor-shell.test.ts` |
| 9 | `packages/schema/src/diagrams.ts` (`node`, `nodes` doc); `packages/schema/src/diagrams.test.ts` |
| 10 | `packages/chrome/src/ToolbarTail.tsx` (the chart-tools import, the `legend` options' default, the `legend`/`numberFormat` pick) |
| 11 | `packages/chrome/src/FormatOptions.tsx` (`renderGeneratedAdvanced`, the two section bodies); `format-options-round-two.test.tsx` |
| 12 | `packages/chrome/src/inspector/chart.css` (the swatch size, the active line rule, the menu); `packages/chrome/src/inspector/chart.tsx` (`isActiveLine`, `menu` state, `onHeaderContextMenu`, the menu markup); `chart-grid.test.tsx` |
| 13 | `packages/chrome/src/inspector/format-sections.ts` (`leadingFormatSection`, `sectionsInOrderFor`); `packages/chrome/src/FormatOptions.tsx` (the sort); `packages/chrome/src/inspector/table.tsx` (the Height field); `table-section.test.tsx`; `format-sections.test.ts` |
| B6 R6 | `packages/viewer/src/Selection.tsx` (`blockDisplayName`'s `assets` parameter); `Editor.tsx` (the two call sites) |
| P1 item 7 | `packages/viewer/src/Editor.tsx` (the `pendingEdit` line in the grid change branch of the reconcile effect; the `spansChanged` branch of the range effect; `runCellMemo` and its layout effect) |
| P1 item 8 | `packages/viewer/src/Editor.tsx` (`wordArtScale`, `scaleReadout` and its effect, the `free-resize` branch of `gestureAt`, `valueReadout: scaleReadout ?? markReadout`); `packages/chrome/src/Overlay.tsx` (the joined size readout) |
| P1 item 9 | `packages/viewer/src/Editor.tsx` (`hoverPrompt`, `clearHoverPrompt`, `promptHoveredCell`, the calls in `onPointerMove`, `onPointerLeave` and `startEdit`, the `PROMPTS` import); `packages/render/src/blocks/table.ts` (the empty cell's paragraph); `packages/render/src/block-css.ts` (`.para:empty::before`); `packages/render/src/__tests__/table.test.ts` (the empty cell case) |
| Fixes read in the runs | `packages/chrome/src/ToolbarTail.tsx` (the table branch of `pickColor` for Text color; rank 8); `packages/chrome/src/editor-shell.ts` (the table ladder of `format.text.size.*`; rank 8); `packages/chrome/src/ToolbarHead.tsx` (`value`, R6; rank 3) |

### 3. The rows on 4413

Every reading below is B4's driver as it stands on the checkout (`scripts/probes/core-walk/areas/*.mjs`, the features round rows) run by `node scripts/probes/editor-walk-probe.mjs --core --base http://localhost:4413 --only <areas>` against this lane's server, under `.turboslide/e2e.lock`, at 1440 by 900, on a fresh deck trashed and deleted forever by the walk's own finally block (every run's cleanup line reads 404 on `/edit` and `/deck`). "passes" and "fails" are the driver's verdicts in the last run that drove the row; a reading in parentheses names the mechanism read from the run's observed text. The rows this lane could not drive by the walk were driven by hand with `docs/gslides-parity/features/build/b3/b3-probe.mjs` (the debug steps, section 4).

P0 (docs/FEATURES.md 2.2):

| Row | Last run | Reading |
| --- | --- | --- |
| `charts.grid.type-to-edit` | run 2, passes | the first key opened the 1,1 cell with "4", Enter committed 42, the chart stayed selected |
| `charts.grid.escape-stays` | run 2, passes | the first Escape closed the field and kept the value, the panel and the active cell; the second left the grid with the chart selected |
| `tables.cell.click-places-caret` | run 4, passes | the click half passed every run (chip Table, eight handles, the session in 2,2 within 300 ms); the second table's drag read 84 by 40 in runs 1 to 3 (the snap, R7) and "moved by 80,40 true" in run 4, with no cell open during or after and 1,1 unchanged |
| `tables.cell.click-then-type` | run 2, passes | "R2C3" to "R2C3x" in one gesture, 1,1 unchanged, one Cmd+Z |
| `tables.range.drag-from-selected` | run 2, passes | the range ring over 6 cells, the table unmoved; the band drag moved it 80 by 40 exactly |
| `shapes.label.centred-default` | run 2, passes | centre offsets 0,0 px before and after the se resize; the Align control reads `center` (run 1 failed on the read alone, before R6's `data-value`) |
| `tables.selected.typing-appends` | run 2, passes | "x" appended in 0,0 after the marquee, "y" in 2,3 after the click and Escape, each one Cmd+Z |
| `tables.cell.arrows-cross-cells` | run 2, passes | End+Right to 1,2 with the caret at 0, Down to 2,2, Home+Left to 2,1, Up to 1,1 (run 1 failed: the server still served the first InlineText, section 4) |
| `tables.range.shift-arrows` | run 4, passes | the range ring "1,1:2,2" over 4 cells, Delete cleared the four texts and kept the rest, Cmd+Z restored them (runs 2 and 3 read `revision 92 -> 92` before the room's revision followed, R7) |
| `diagrams.label.double-click-opens` | run 2, passes | the double click opened the step's own run with the caret inside; two labels in 5 clicks |
| `diagrams.label.tab-next` | run 2, passes | Tab to the next label with its text selected, Shift+Tab back, Enter on the group opened the first label |
| `charts.double-click.opens-data` | run 2, passes | Chart data with the 1,1 cell active after the double click within 500 ms; the Edit data button under the chart opened the section |
| `charts.mark.click-selects-cell` | run 2, passes | the second bar made cell 2.1 active and the readout showed the value |
| `tables.range.bold-italic` | run 4, passes | 4 of 4 cells bold and then italic, no refusal card, Cmd+Z cleared each (runs 2 and 3 read the save words "Saving…" and `revision 93 -> 93` because the room's revision follows about two seconds later, R7; this lane's probe read the bump at 2 s and "All changes saved") |
| `tables.range.size-color` | runs 2 to 4, fails on two driver readings | the size step from 20 has no rung above (the table ladder, R7) and now says so; the colour wrote `[H0]{c:green}` into every cell (this lane's probe, `probe-debug-2.json`) while the driver read the `.para` span's colour, not the mark's (R7); Align right wrote every column of the range in every run |
| `diagrams.step.one-object` | runs 2, fails on two mechanisms outside this lane | 3 boxes with their own text, 0 separate labels, the double click opened the step's own run, the drag moved the step with its label, one Delete removed it; the connectors did not follow because `diagramInsert` freed the ids and left `connect` on the old ones (R8), and the revision check (R7) |
| `diagrams.export.step-label` | not driven here | `core/export.spec.ts` is B4's and B7's; the exporter writes a shape's text (the process fixture's `sp` count in `gslides-fixture.test.ts` holds) |
| `charts.legend.none-from-toolbar` | run 2, passes | None wrote `legend: 'none'` and no legend was drawn, the list checked None, Right restored it |
| `charts.panel.no-duplicate-controls` | runs 1 to 2, fails on the driver's counting | one Chart type group, one Legend, the JSON view present with the switch on; "Title 2" is the label and its span counted twice and "textareas 1" the Alt text Description field (R7) |
| `charts.grid.remove-visible` | run 2, passes | the active row's remove control at opacity 1 with the pointer away, the swatch 16 by 16, the header's right click listed Remove |
| `tables.panel.table-first` | runs 1 to 4, fails on the driver's section selector and revision check | the Table section top at 152 px in view, one Height field, no per row fields, Distribute rows evened the heights (`[null, null, null]`: `table.distribute` with no total clears the sizes, the action's rule); the sections read empty through `formatOptions.section.*.toggle` and `revision 51 -> 51` (R7) |

P1 (docs/FEATURES.md 2.3), landed under the P1 condition's own reading (the P0 rows above read green on this server except where the driver's reading is the mechanism):

| Row | Last run | Reading |
| --- | --- | --- |
| `tables.command.keeps-caret` | run 4, passes | Insert row below from the right click menu: the session back in 1,1 (the run cell memo reopens the caret the right click had ended, section 1); Align right from the tail: the session in 1,1; the range stayed a range after Merge ("range ring after 1,1:1,2"); run 3 had read "session false" on the insert before the memo |
| `wordart.resize.scales-letters` | run 3, passes | 800 by 120 to 1027 by 205, `typography.size` 88 to 150, drawn 150 px, the readout "1027 × 205 · 150 px", one Cmd+Z back to 88 and 800 by 120 |
| `tables.cells.prompt-hovered-only` | run 3, passes | 0 prompts with the pointer away, 1 in the hovered cell 2,2 alone, the filmstrip card (a clone) draws 0 |
| `tables.seam.row-drag`, `tables.edge.add-row-column`, `tables.heads.select-row-column`, `tables.bar.row-column-buttons` | not driven | not built this ship (P1 items 1 to 3; the drivers read not built with their `parks` ids) |
| `brand.objects.kit-colours-first`, `tables.paste.tsv-makes-table`, `tables.paste.into-cell-spreads`, `arrange.group.tail-text-controls`, `wordart.tail.fill-outline` | not driven | not built this ship (P1 items 4, 5, 6 and 10) |

Rows outside this lane's set that read red on this server because of this lane's items: `diagrams.insert.group`, `diagrams.edit-label`, `diagrams.light-appearance`, `diagrams.present`, `diagrams.reload` (five labels are now the shapes' texts, R7); `tables.cells.tabular-figures` is B2's and read "a paragraph null" on this server.

### 4. Run ledger

Every run on this lane's server (`http://localhost:4413`, the tmp store, the memory realtime tier), the JSON and the log under `docs/gslides-parity/features/build/b3/`:

1. `walk-4413-run1` (06:49Z to 07:00Z): `--only tables,charts,diagrams,shapes,wordart`, 135 steps; the server had been started at 06:44 and served every item but the arrow fixes and the Align value, which were written after it; the `pkill -f 'vite dev --port 4413'` restarts that followed missed the process (its command line reads `vite.js dev --port 4413`), so runs before 07:06 read the 06:44 build. Read for: the click model, the appends, the chart rows, the diagram label rows, the sorted sections.
2. `probe-debug-1.json` (this lane's probe, 07:03Z): the bold range's acknowledgement watched every 500 ms (revision 3 to 4 at 2 s, pending 0, "All changes saved"); the tail's Text color on a range wrote nothing (fixed: the table branch of `pickColor`); the arrows and Shift+Right read the 06:44 build.
3. `walk-4413-run2` (07:07Z to 07:17Z): the same five areas on the server restarted by pid at 07:06:59 with every fix of section 1; 114 ok, 16 failed, 5 not driven, 0 console errors; the readings of section 3.
4. `probe-debug-2.json` (07:18Z): the tail's Text color on the header range wrote `[H0]{c:green}` into every cell of the range.
5. `walk-4413-run3` (07:21Z to 07:26Z): `--only tables,wordart` on the server restarted at 07:20:39 with the P1 hunks of items 7, 8 and 9; `wordart.resize.scales-letters` and `tables.cells.prompt-hovered-only` pass, the range half of `tables.command.keeps-caret` passes.
6. `walk-4413-run4` (07:31Z to 07:36Z): `--only tables` on the server restarted with the run cell memo (item 7's second half); 56 ok, 3 failed, 4 not driven, 0 console errors. The three: `tables.range.size-color` and `tables.panel.table-first` on the driver readings of R7, `tables.cells.tabular-figures` (B2's). B4's drivers changed on the checkout between the runs (`tables.mjs` grew by 1124 lines against `88b68e7`), so a row that read red in run 2 and green in run 4 on the same mechanism (`tables.cell.click-places-caret`'s drag, the revision checks of `tables.range.shift-arrows` and `tables.range.bold-italic`) read the driver's change, not this lane's; this lane's code for those rows is the same in runs 2 and 4.

The scratch decks of every run and probe were trashed and deleted forever by the runs' own finally blocks; each cleanup line reads 404 on `/edit/<id>` and `/deck/<id>`. The server was stopped before this report.

### 5. What this lane did not land

- 2.3 items 1 to 3 (the row seams, the "+" edges and the heads, the bar under the table): not built; their rows read not driven with their `parks` ids, as the drivers write them. They wait for the round after or a fix round with room.
- 2.3 item 4 (the kit's six roles first on the object plates), item 5 (the spreadsheet paste), item 6 (the group tail's text controls), item 10 (the word art tail): not built; their rows read not driven.
- The A1 alternative of question 8 was not built: the default (the caret on the first click) is what landed.

### 6. The P1 items that landed, and their reversal

Each is one hunk set of section 2's table and reverts alone: item 7 (`tables.command.keeps-caret`: `pendingEdit` on the grid change, the merged cell as the range, the run cell memo), item 8 (`wordart.resize.scales-letters`: `wordArtScale`, `scaleReadout`, the Overlay's joined readout), item 9 (`tables.cells.prompt-hovered-only`: `promptHoveredCell` and `clearHoverPrompt` in Editor.tsx, the empty paragraph in render/blocks/table.ts, the `.para:empty::before` rule in block-css.ts, the updated `render/src/__tests__/table.test.ts` case). Under docs/FEATURES.md section 1 a P1 item lands only when the lane's P0 rows are green on the lane's own preview run before the integrator's first gate run; this lane has no preview run of its own, so the integrator reads section 3 and decides; the three are separable so the decision costs one revert each.
