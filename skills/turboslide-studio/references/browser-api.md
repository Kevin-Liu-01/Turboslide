# Turboslide studio browser API

Generated by `pnpm generate:contracts` from `packages/schema/src/actions.ts`; do not edit by hand.

The adapter is `window.turboslide.studio`; it is ready after the `turboslide:studio-api-ready` event, whose detail is `describe()`. The ownership model is Glyphfield’s: the component that owns state registers an adapter with an `owner` element, and the getter resolves the last registration whose owner is connected and not under `[inert]`, `[hidden]`, `[aria-hidden="true"]` or `[data-active="false"]`. Controls are found by normalized accessible label (`aria-label`, `title`, `name`, `aria-labelledby` or the wrapping `<label>`) and by `data-control` id; inspector controls read `<block id>: <property label>` (`list: Size`) with `data-control="block.list.size"`. A missing label throws `RangeError`.

## Methods

| Method                  | What it does                                                                           |
| ----------------------- | -------------------------------------------------------------------------------------- |
| `version`               | The API version, 1                                                                     |
| `describe()`            | The active owner, its actions and whether source read and apply are supported          |
| `controls()`            | Visible interactive controls with accessible labels, data-control ids and values       |
| `activate(label)`       | Activates a control by accessible label or data-control id; RangeError when missing    |
| `set(label, value)`     | Sets a control by label or data-control id and dispatches input and change             |
| `readSource()`          | The exact current source document of the active owner                                  |
| `applySource(doc)`      | Applies a JSON string or object through the validator, then waits two animation frames |
| `invoke(action, input)` | Runs a standard action or delegates to the active owner                                |
| `download(artifact)`    | Saves a returned artifact with its deterministic file name                             |

## Owners

| Owner           | Active when                                        | Actions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --------------- | -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `editor`        | a deck is open at /edit/:deckId                    | `deck.info`, `slide.list`, `slide.get`, `slide.insert`, `slide.remove`, `slide.move`, `slide.update`, `slide.replace`, `block.set`, `block.insert`, `block.remove`, `block.move`, `section.set`, `slide.lease`, `asset.add`, `asset.dither`, `material.capture`, `render.slide`, `render.sheet`, `lint.run`, `fix.run`, `diff.run`, `version.save`, `version.list`, `version.restore`, `view.goto`, `view.mode`, `view.theme`, `view.present`, `export.run`, `build.run`, `validate.run` |
| `viewer`        | /deck/:deckId in slide, grid or book mode          | `view.goto`, `view.mode`, `view.theme`, `view.present`, `render.slide`, `render.sheet`                                                                                                                                                                                                                                                                                                                                                                                                   |
| `source-drawer` | the source drawer is open; delegates to the editor | `source.read`, `source.apply`                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `presenter`     | /present/:deckId                                   | `view.goto`, `view.present`                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

## Standard actions

The six Glyphfield standard actions are handled by the API itself: `source.read`, `source.apply`, `controls.list`, `control.activate`, `control.set`, `artifact.download`. Everything else is delegated to the active owner.

## Window actions

| Action              | Label              | Mutates | Milestone | Description                                                                                                    |
| ------------------- | ------------------ | ------- | --------- | -------------------------------------------------------------------------------------------------------------- |
| `deck.info`         | Deck info          | no      | M1        | Title, theme, sections with slide ids and titles, counts and the current revision.                             |
| `slide.list`        | List slides        | no      | M1        | Every slide in deck order with its number, section, title, kind and lint counts.                               |
| `slide.get`         | Get slide          | no      | M1        | The normalized slide, the assets it references and its last render record.                                     |
| `slide.insert`      | Insert slide       | yes     | M2        | Inserts a slide into a section after the named slide, or first.                                                |
| `slide.remove`      | Remove slide       | yes     | M2        | Removes a slide from the deck and its section.                                                                 |
| `slide.move`        | Move slide         | yes     | M2        | Moves a slide to a section after the named slide, or first.                                                    |
| `slide.update`      | Patch slide        | yes     | M2        | Applies a mutation list to one slide and returns the normalized slide with its findings.                       |
| `slide.replace`     | Replace slide      | yes     | M2        | Replaces a slide with the given document; what the source drawer’s Apply emits.                                |
| `block.set`         | Set block property | yes     | M2        | Writes one property of a block by JSON pointer; an absent value deletes it.                                    |
| `block.insert`      | Insert block       | yes     | M2        | Inserts a block into a slot after the named block, or first.                                                   |
| `block.remove`      | Remove block       | yes     | M2        | Removes a block from its slide.                                                                                |
| `block.move`        | Move block         | yes     | M2        | Moves a block to a slot after the named block, or first.                                                       |
| `section.set`       | Set sections       | yes     | M2        | Replaces the section list, the only place slide order lives.                                                   |
| `slide.lease`       | Lease slide        | yes     | M2        | Takes a lease on a slide for the author; another author’s write is rejected with 409 unless forced.            |
| `asset.add`         | Add asset          | yes     | M5        | Adds a file or URL as an asset with its role, alt, source and optional treatment; returns twins and metrics.   |
| `asset.dither`      | Dither asset       | yes     | M5        | Re-runs the two-tone pipeline on an asset with new parameters and reports plate clearance.                     |
| `material.capture`  | Capture material   | yes     | M5        | Renders a shader material at 3200 by 1800 and captures frames at the given anchors as assets with recipe keys. |
| `render.slide`      | Render             | no      | M1        | Renders slides in one or both themes at 1x or 2x and returns render records with image paths.                  |
| `render.sheet`      | Contact sheet      | no      | M1        | Renders a contact sheet per theme with a JSON cell map from cell boxes to slide ids.                           |
| `lint.run`          | Lint               | no      | M1        | Runs the grammar linter on slides in the static, rendered or both layers and returns findings.                 |
| `fix.run`           | Fix                | yes     | M2        | Applies the fix mutations of findings that carry one and reports what remains.                                 |
| `diff.run`          | Diff               | no      | M2        | The mutation list between two revisions in prose, optionally with before and after crops.                      |
| `version.save`      | Save version       | yes     | M2        | Saves a named version at the current revision.                                                                 |
| `version.list`      | List versions      | no      | M2        | The saved versions with author, note and revision.                                                             |
| `version.restore`   | Restore version    | yes     | M2        | Restores a version as a mutation, so it is undoable and visible in History.                                    |
| `view.goto`         | Go to slide        | no      | M3        | Shows a slide in the viewer, the editor or the presenter.                                                      |
| `view.mode`         | View mode          | no      | M3        | Switches between slide, grid and book mode.                                                                    |
| `view.theme`        | View theme         | no      | M3        | Sets the theme of chrome and sheet together through the gt-theme key.                                          |
| `view.present`      | Present            | no      | M3        | Enters or leaves present mode.                                                                                 |
| `export.run`        | Export             | no      | M2        | Exports the deck to PPTX, Google Slides or PDF in native or flatten mode and returns the verified report.      |
| `build.run`         | Build standalone   | no      | M1        | Writes the single-file HTML deck with fonts and assets inlined and asserts the size budget.                    |
| `validate.run`      | Validate           | no      | M1        | Validates a deck directory and returns the issues and the normalized document.                                 |
| `source.read`       | Read source        | no      | M3        | Glyphfield standard action: the exact current source document of the active owner.                             |
| `source.apply`      | Apply source       | yes     | M3        | Glyphfield standard action: applies a JSON string or object through the active owner’s validator.              |
| `controls.list`     | List controls      | no      | M3        | Glyphfield standard action: the visible interactive controls with labels, data-control ids and values.         |
| `control.activate`  | Activate control   | no      | M3        | Glyphfield standard action: activates a control by accessible label or data-control id.                        |
| `control.set`       | Set control        | no      | M3        | Glyphfield standard action: sets a form control by accessible label or data-control id.                        |
| `artifact.download` | Download artifact  | no      | M3        | Glyphfield standard action: saves a returned artifact with its deterministic file name.                        |

## Errors

| Class                 | When                                                                                    |
| --------------------- | --------------------------------------------------------------------------------------- |
| `TypeError`           | malformed input, with the Zod path                                                      |
| `RangeError`          | an unknown action, label or id                                                          |
| `ConflictError`       | a stale baseRevision or a held lease, with the holder and the current document attached |
| `NotImplementedError` | an action declared in the table whose implementation lands in a later milestone         |
| `Error`               | renderer and codec failures                                                             |

## Completion

A resolved `applySource` or `invoke` is a React commit boundary, not a render: wait for the stage to re-render (two animation frames, then `document.fonts.ready`, then image decode) before reading pixels. Re-read the source after every apply; the validator may have canonicalized text.
