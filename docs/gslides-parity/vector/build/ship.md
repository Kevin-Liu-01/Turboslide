# The ship step, the vector round

The ship step of the vector round (docs/VECTOR.md 6.2; the prompt of 2026-09-24), on the worktree `/Users/kevinliu/repos/Turboslide-vector`, branch `vector/round`, started 21:35Z on 2026-09-25 (the round's day is 2026-09-24; times are UTC). The rules of AGENTS.md and the prompt hold: the git writes of this step are the rebase of `vector/round` onto `origin/main`, the fast forward of `main`, the ship commit and its push, and the production table's commit and push; no `pnpm install`, `pnpm add` or `pnpm exec` was run by hand (`node scripts/check.mjs` runs its own steps from step 2), no build outside `scripts/check.mjs`; nothing on Vercel or GitHub was created, changed or deleted apart from the pushes (production's deployment is Vercel's own build of the push; this step deployed no preview, section 4 says why); `round-five/integration` was never touched; no lane's file was edited; no token, cookie or secret is printed here or in the evidence (every command that needed one ran through `scratchpad/vector/with-tokens.mjs`, which reads the files into the child's environment and prints nothing); every scratch deck this step's runs made on the shared Blob store was trashed and deleted forever before the return (section 7). The machine carried other lanes' runs throughout: ship two's Playwright specs from `/Users/kevinliu/repos/Turboslide` (40 minutes in when this step started) and another project's Playwright, the one minute load at 104 when this step started and 115 to 150 through its checks, on 18 cores.

Sections: the tree found (1), the preconditions as read (2), the parked list (3), the rebase and the checks after it (4), where this step stopped (5); part A: the ship commit (6), the wait for ship two (7), the rebase after the wait and the checks (8), the fast forward and the push (9), the deployment and the smoke (10); part B: production (11), the store after the runs (12), the verdict of the ship as read on production (13), the documents of the table and the commit (14). For Kevin, the production table's VP.6 in VERIFICATION.md.

## 1. The tree found

`vector/round` at `4061007` (the fix round's commit over `08dfc73`; fifteen commits over the merge base `3c1f18a`), clean but for the verifier's pass 2: `docs/gslides-parity/focus/VERIFICATION.md` modified (+234 lines, the section "Vector round, pass 2 (the verifier, 2026-09-24)", written but never committed, since a verifier makes no git write) and 110 untracked files under `docs/gslides-parity/focus/verification/` (pass 1's `vector-*.png` and pass 2's `vector-p2-*.png`, 106 pictures, 14 MB, and the four ledgers `vector-verify-preview-run1.json`, `vector-verify-preview-run2.json`, `vector-verify-p2-preview-run1.json`, `vector-verify-p2-preview-specs-rerun.json`). `origin/main` at `da00b5e` after `git fetch origin` at 21:35Z: two commits past the merge base, `88d8e21` (the ship one hotfix's production table: `hotfix.md`, VERIFICATION.md, five ledgers, `updates.md`) and `da00b5e` (the gate change: the template rows skip on production in `brand.spec.ts`, `settleDefaultTemplate` in `core-gate.mjs` puts Blank back after the drivers, `lib.ts`); neither the chrome fix nor the ship one hotfix had to be rebased onto again (the integrator's second rebase already carried `3c1f18a`), and ship two had not reached `origin/main` when this step fetched, nor when it pushed (section 5). No `.github/` folder, no `decks/untitled-*`, `decks/e2e-*`, `scripts/__pycache__`, `.turboslide` or `.vercel` path on the branch (`git diff --name-only` over the 216 files of the branch against the merge base); `docs/gslides-parity/verification-3/{layout-shift.json,layout-shift.md,parity-audit.json}` unchanged. The repository's local identity is `Kevin <kk23907751@gmail.com>`, the author of every commit of the branch. Production served `dpl_4hTgBSidwHSte78caHrmsdsRYfdq` (`turboslide-1dq2jhyp5`, created 08:12Z on 2026-09-25, `da00b5e`) when this step started; it is the rollback. The development OIDC credential in `.turboslide/vercel-dev.env` was valid to 2026-09-26T06:12Z (the verifier's refresh) and was not needed: production is not behind Vercel Authentication and this step deployed no preview.

## 2. The preconditions as read

Each one this step read itself, on the tree before the rebase where the reading is the verifier's run of record and on the rebased tree where the reading is this step's (section 4):

- `git grep -n '<<<<<<<'` outside prose: nothing in a text file (one binary match, a PNG under `design-4/shader/previews/`, a byte pattern in an image, not a marker); the same after the rebase and the autostash's resolution.
- `pnpm generate:contracts` current: check step 3 ok on the rebased tree (the tracked generated files, the generation, `git diff --exit-code` over them), as the verifier read it on `4061007`.
- The last preview run with every row green apart from the parked rows and the recorded flakes: the verifier's pass 2 run of record, `vector-verify-p2-preview-run1.json` (the enforce preview `turboslide-kltqit0j2` built from `4061007`, 18:15:26Z to 20:43:57Z on 2026-09-25, 8,911 s, `retries` zero, cost rows over their ceiling 0): 827 rows, 790 passed, 20 failed, 17 not driven; the vector round's 44 rows 42 green and two red (`svg.render.vector-at-zoom`, `svg.copy.markup`, section 3); the once rerun of the six spec files that carried a red row, `vector-verify-p2-preview-specs-rerun.json` (20:44:20Z, 2,274 s): 126 rows, 108 passed, 15 failed, 3 not driven. The run of record is younger than the last code change (`4061007` is the tree it measured, and the rebase changed no source, section 4), so no rerun by this step. The 18 rows of earlier rounds not green in the run of record, each with the class the verifier read from the run's JSON (VERIFICATION.md V.4, V.5): the parked list's (`view.live-pointers.second-browser`, `versions.show-changes-marks`, the three `inbox.*` and three `templates.*` rows), Kevin's standing `decks.file.open-list-search` (69 decks listed after 19.9 s), the blob tier's timing class red once and green on the once rerun (`collab.presence-chips`, `decks.trash.editor-undo-snackbar`, `brand.logo.replace-every-slide`, `fonts.budget.no-load-before-ready`, `logos.cache.open-licence-only`; `formatting.clear.inline-marks` and `formatting.persistence` from one reject card of the walk, green in the `--only formatting` rerun), the store's asset write time on the blob tier under a machine at load 121 (`images.insert.upload-while-pending`, red in both readings of this deployment, green on the memory tier twice and in pass 1's preview runs; the same row the features round's ship one recorded in the same class on its confirming preview), and the fixture only `logos.index.refresh-fixture` (the manifest computed from the answering instance's copy of the index; production never drives it). No earlier round's core row is red with a mechanism outside the classes 6.2 forgives, so the ship rule of VECTOR.md section 6 stops nothing.
- The burst with three companions: the verifier's three runs on the run of record's preview (18:22Z to 18:27Z; `b7-burst.mjs --writes 60 --assets 5 --companions 3`), 71 calls each, 0 failed, the slowest call 2.8, 3.3 and 2.5 s, each run's deck removed by the burst.
- `node scripts/check.mjs`: the verifier's chain on `4061007` (steps 2 to 16, 19, 22 to 25, 28 to 30: every step ok but 25, the container verify's cell budget, the standing red of ship one's pass 2 and this round's pass 1; 17, 18 and 26 by hand on 4429 with the standing reds of 26) and this step's own chain on the rebased tree (section 4).
- The branch's content: this round's source, tests, specs, probes, docs and evidence under `docs/gslides-parity` alone (section 1).

## 3. The parked list

Rendered, never typed: `docs/gslides-parity/vector/build/ship/render-ship-json.mjs` (the features round's renderer copied into this round's folder with its rule sentence changed to VECTOR.md 6.2's one run of record and the once rerun) over `vector-verify-p2-preview-run1.json` and `vector-verify-p2-preview-specs-rerun.json` with `--carry docs/gslides-parity/focus/ship-f1afe1e.json`, written to `docs/gslides-parity/focus/ship-ea332aa.json` (named after the branch's head after the rebase, the same source as `4061007`, the tree the run of record measured; ship one named its list after its fixes commit the same way). The renderer's mechanical reading: `wouldPark` of the run of record `formatting`, `inbox`, `brand`, `fonts`, `templates`, `assist`; of the rerun `inbox`, `fonts`, `templates`; `wouldParkRows` red in both `logos.index.cached-offline`, `logos.index.refresh-fixture`, `svg.copy.markup`, `svg.render.vector-at-zoom` and the templates rows (subsumed by their feature); the carried rows put back by `--carry` where they did not read green (every one of the ten). The rulings of the ship rule over that reading, each in the list's `notParked` with its reason:

- `parkedFeatures`: `inbox`, `templates` (carried; red again in both readings).
- `parkedRows`, ten, the carried list unchanged: `versions.show-changes-marks` (`file.versionHistory.showChanges`), `view.live-pointers.second-browser` (`view.livePointers.mine`, `view.livePointers.collaborators`), and the eight P1 rows whose controls are not on the build (`arrange.group.tail-text-controls`, `logos.kit.find-a-logo`, `logos.picker.variants`, `tables.bar.row-column-buttons`, `tables.edge.add-row-column`, `tables.heads.select-row-column`, `tables.seam.row-drag`, `wordart.tail.fill-outline`).
- `notParked`, the features: `fonts` (its entries are the two P1 rows not on the build; `fonts.budget.no-load-before-ready` red once and green on the rerun), `formatting` (the two rows of one reject card, green in the `--only formatting` rerun and on the memory tier; the blob tier's admission class, V.5 finding 5), `brand` (`brand.logo.replace-every-slide` red once and green on the rerun; the P1 row), `assist` (`assist.quota.429` not driven with no Upstash, PRODUCT.md 8.2's exemption).
- `notParked`, the rows: `logos.index.cached-offline` and `logos.index.refresh-fixture` (fixture only rows production never drives; parking `insert.logo` would hide Insert > Logo for rows the fixture preview alone reads), `svg.render.vector-at-zoom` (the blob tier's timing class VECTOR.md 6.2 forgives and records by id: the viewer loaded right after the row's own twenty writes answered at a revision behind the tab's; the sheet at 100 and 200 percent and the show drew the svg in the same reading, the memory tier read it green twice and the verifier's hand drove it green twice on the same preview) and `svg.copy.markup` (the harness's own mechanism on a protected preview: the read ahead's fetch of the vector file follows the route's 302 to the public store host with the OIDC header the Playwright context stamps on every request, the store refuses that preflight, and the copy writes the envelope alone; the markup and the envelope both on the clipboard on the memory tier twice; production sends no header, so the production gate is the reading that rules, and a red there with a mechanism outside the forgiven classes re parks `picture.svg.copy` by a follow up commit of the parked list alone).
- `leaves`: none (no carried row read green in both readings).
- Not listed, by the rule: `decks.file.open-list-search` (`file.open`), Kevin's standing row, never chased; the renderer reads it flaky (a walk row absent from the specs rerun) and the ship note records it here.

`node scripts/probes/core-matrix.mjs --emit-parked docs/gslides-parity/focus/ship-ea332aa.json` wrote the same fourteen ids into `packages/chrome/src/parked-controls.ts` between its markers (`bar.table`, `dialog.logo.kind.wordmark`, `dialog.logo.tone.mono`, `file.versionHistory.showChanges`, `handle.table.add.column`, `handle.table.add.row`, `handle.table.head.column`, `handle.table.head.row`, `handle.table.row`, `panel.brand.logo.find`, `toolbar.group.text`, `toolbar.wordart.outline`, `view.livePointers.collaborators`, `view.livePointers.mine`); `--check` reads it current; the one line that changed is the marker comment naming the list it came from. No `advanced` flag of `menus/model.ts` or `menus/toolbar-tails.ts` moves: nothing new parks, nothing leaves, and the vector round's own unflagging (the four grids, Change shape, Mask image, the Crop predicate) is B1's commit. The ledger tests: `packages/chrome/src/parked-controls.test.ts` pins the same fourteen ids (its case renamed for this list; 69 passed with `menu-model.test.ts`, 2 todo); `menu-model.test.ts`, `presence.spec.ts` and `model.ts` keep their references to `ship-f1afe1e.json`'s `leaves`, a record of the Go to slide word's return that stands. `docs/VECTOR.md` section 7 gains the paragraph on this list; the README's Advanced tools section names the list, removes the four grids, Change shape and Mask image from the lines of what is behind the switch (they are in the default view since this round; Image by URL since the product round, which the section still listed) and reads the galleries' return with the interpreter as done; `node docs/readme/what-works.mjs --check` reads the README current (its "What works today" is rendered from the matrix's `today` and B5's commit rendered it). `shipVerdict` over the run of record with this list reads 18 rows outside it, every one classified above (`ok false` is the gate's mechanical word; the ship note is the classification).

## 4. The rebase and the checks after it

`git rebase --autostash origin/main vector/round` at 21:45Z: the fifteen commits applied without a conflict over `da00b5e` (the fix round's hunks in `brand.spec.ts` and `lib.ts` sit sixteen lines under `da00b5e`'s, as the integrator's fix round placed them; the head is `ea332aa`), and the autostash of the verifier's VERIFICATION.md section met `88d8e21`'s hotfix table appended at the same end of the file: resolved by keeping both, the hotfix table first (lines 6548 to 6620) and the verifier's pass 2 after it (6621 on), the stash dropped, the file left as a working tree change for the ship commit. `git diff --stat 4061007 HEAD`: eleven files, every one of `88d8e21` and `da00b5e` (two e2e files, the gate script, three notes and five ledgers); no source of the product changed in the rebase, so the deployment of record (`turboslide-kltqit0j2`, built from `4061007`) is the rebased tree's source and no further preview was deployed. The one source line this step changes before the push is the marker comment of `parked-controls.ts` (section 3), a comment the build strips; recorded as a deviation from the letter of "a source file changed" rather than spent on a two hour gate.

The checks on the rebased tree with this step's edits, `node scripts/check.mjs --only <n>` one step per invocation (`scratchpad/vector/ship/logs/check-step-<n>.log`, `check-summary.txt`), 21:47Z on, the machine at load 115 to 150 under ship two's specs:

| Step                                                                                                                                                                    | Result                                                                                                                                                                                                                    |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2 (the routes), 3 (the contracts, unchanged), 4 (`tsc -b`), 5 (`pnpm test`, the whole unit suite), 6 (the build, the client bundle check, the greps)                    | ok, 21:47:00Z to 21:50:16Z                                                                                                                                                                                                |
| 7 to 16 (the import at 85 slides, the validate, the 170 renders with no page error, the compare to the shoot at 0.5 percent, the sheet, the lint, the brand deck build) | ok, 21:50:16Z to 21:53:03Z                                                                                                                                                                                                |
| 19 (`pnpm format:check` and the README check)                                                                                                                           | ok, 21:53:03Z to 21:55:39Z                                                                                                                                                                                                |
| 22 to 25, 28 to 30                                                                                                                                                      | still running when this step was told to return (21:56Z); the loop's summary is `scratchpad/vector/ship/logs/check-summary.txt` and the verifier read 22 to 24 and 28 to 30 ok and 25 the standing red on the same source |
| 17, 18, 26 (the server steps), 20, 21, 27, 31, 32                                                                                                                       | not run by this step (the verifier's readings on the same source stand: 17 and 18 ok, 26 the standing reds)                                                                                                               |

## 5. Where this step stopped

This step was told to return at 21:56Z on 2026-09-25, before the fast forward of `main`, the ship commit and the push: `vector/round` stands rebased at `ea332aa` with the working tree changes of sections 3 and 4 uncommitted (README.md, docs/VECTOR.md, VERIFICATION.md with the verifier's pass 2 section merged under the hotfix table, `parked-controls.ts` and its test, and the untracked `ship-ea332aa.json`, the four verifier ledgers, the 106 pictures, this note, `build/ship/render-ship-json.mjs` and `build/ship/shoot-production.mjs`). Nothing was pushed, no production gate ran, no scratch deck was made on the store by this step (the only store call was a read of `template.list` and `deck.list` through the bearer at 21:49Z: the default Blank, three "Core spec template" cards and the decks of ship two's specs in flight, none this step's). The ship commit's message with the acceptance summary is written at `scratchpad/vector/ship/ship-commit-message.txt`, the production gate's runner at `scratchpad/vector/ship/run-gate-production.sh` (the bearer alone through the wrapper, `--parked` the new list, `--matrix docs/gslides-parity/focus/verification/gate-production-vector.json`), and the pictures script at `build/ship/shoot-production.mjs`; the continuation runs, in order: the check loop's tail read, `git checkout main && git merge --ff-only vector/round`, the ship commit by the explicit path list above with that message and the repository's identity, `git push origin main` (a rebase onto `origin/main` first if ship two landed, the list renamed if the head's hash moves), the poll of `vercel inspect https://turboslide.vercel.app` to Ready, the production gate detached and polled at most every ten minutes, the new write probe, the hosted smoke, the pictures, then the README's two sections, the release note, the production table under "# Vector round, the production table (2026-09-24)" in VERIFICATION.md, and the second commit and push.

`node_modules/.bin/tsc -b` is step 4 (exit 0 in 25 s). The changed packages' suites after the rebase: the whole unit suite in step 5 (`pnpm test`), and by hand `packages/chrome` `parked-controls.test.ts` and `menus/__tests__/menu-model.test.ts` (69 passed, 2 todo) after the test's case was renamed; `scripts/probes/core-matrix.test.mjs` runs inside step 5 (`vitest run` at the root covers the `scripts` project). Prettier over every file this step touched (README.md, docs/VECTOR.md, VERIFICATION.md, the two chrome files, the renderer copy, the shoot script, the list): clean after `--write` on the list and the shoot script.

## 6. The continuation, part A: the ship commit

The ship step's continuation (part A: the ship commit, the wait for ship two, the rebase after it and the push) started 22:00Z on 2026-09-25 on the same worktree, `vector/round` at `ea332aa` with section 5's working tree changes and the check loop of section 4 still running (step 24 at 22:00Z; the machine at load 97 to 114 under ship two's preview gate on `turboslide-bxjtrzqfy` and another project's Playwright). `git status`: the five modified files and 113 untracked paths of section 5, nothing else; `docs/gslides-parity/verification-3/parity-audit.json` unmodified (the check loop's step 20 is not in the loop, so nothing rewrote it). The scratchpad stood, so the commit message is the one section 5 named, read from `scratchpad/vector/ship/ship-commit-message.txt` unchanged. Before staging, every text file of the list was scanned for token shapes (JWT prefixes, Blob tokens, bearers, the OIDC header, the two session secrets, 64 hex characters): zero matches in the thirteen text files; the 106 pictures are PNG. Staged by the explicit path list (the five modified files, `ship-ea332aa.json`, the four ledgers, `ship.md`, `build/ship/render-ship-json.mjs`, `build/ship/shoot-production.mjs` and `verification/vector-*.png`, whose 106 matches are exactly the untracked pictures, none tracked before), 119 paths; a grep of the staged list for `.github`, `.turboslide`, `.vercel`, `decks/untitled-`, `decks/e2e-` and `__pycache__` matched nothing.

The ship commit: `5bf158a` on `vector/round` at 22:01:48Z, author `Kevin <kk23907751@gmail.com>` (the repository's own identity, the author of every commit of the branch), subject "Turboslide vector round: every shape preset drawn from its geometry with the Shape menu as glyph grids, icons on the visual rows, and SVG import, paste and export kept as vector", the acceptance summary of section 5's message as the body and the `Co-Authored-By` trailer. The tree is clean after it. The parked list keeps its name `ship-ea332aa.json`: the list is named after the commit whose source the run of record measured (ship one and ship two named theirs after their fixes commits the same way), and `ea332aa` is that commit as long as the rebase of step 3 keeps its hash; if the rebase rewrites it, the list is renamed to the rewritten hash of that same commit (the head below the ship commit) and the rename folded into the ship commit by the one amend the prompt allows.

The check loop of section 4 finished at 22:07:20Z, after the commit and on the same source: step 22 ok (21:55:39Z to 21:58:02Z), 23 ok, 24 ok (the canvas fidelity at 0.5 percent, 22:01:22Z), 25 exit 1 (22:01:22Z to 22:06:35Z: the container verify's standing red, two slides of the light deck over the cell budget, `03 table` at 1.008 percent and `07 styles` at 0.546 percent with one block each out of budget, the same reading as ship one's pass 2 and this round's pass 1), 28 ok (the audit: 0 advisories at high or above, 0 open, 0 acceptances expired), 29 ok (the brand, home assets, definitions and native record checks), 30 ok (the Vercel output build, 22:06:44Z to 22:07:20Z). `docs/gslides-parity/verification-3/parity-audit.json` stayed unmodified (step 20, the audit that rewrites it, is a server step outside the loop); nothing to restore. So the whole chain minus the server steps reads on the rebased source as the verifier read it on `4061007`: every step ok but 25's standing red.

## 7. The wait for ship two

At 22:00Z `origin/main` stood at `da00b5e` (the state's reading) with no commit of ship two; the shared checkout `/Users/kevinliu/repos/Turboslide` (read only for this step) stood on `features/ship-two` at `573dfe0` ("the parked list named after the fixes commit", 20:03Z), and a core gate ran from this machine against ship two's preview `turboslide-bxjtrzqfy`, not against production. So ship two was still in its preview gate when this part started, with its ship commit, its push, its production gate (about 2 h 35 min) and its table commit ahead of it. The wait runs as `scratchpad/vector/ship/wait-ship-two.sh`, detached: every ten minutes (a `sleep 600` in a loop) `git fetch origin`, then three readings (a commit on `origin/main` whose subject begins "Turboslide features round, ship two: the production table"; `pgrep -f 'core-gate.mjs --base https://turboslide.vercel.app'` empty; the last commit on `origin/main` older than ten minutes by its committer date), one line per poll in `wait-ship-two.log`; it stops early when ship two's commits are on `origin/main` but no table follows within two hours of their first sighting, or when four hours pass. Its polls:
sixteen, 22:03:33Z to 00:33:56Z on 2026-09-26, every one reading ship2=0 table=0 gate=0 with `da00b5e` the last commit on `origin/main` (its committer date 01:12 PDT on 2026-09-25, so the age reading grew from 49,873 s to 58,895 s). The loop ended with that part's return at 00:35Z (a detached process ends with the step that started it), 2 h 30 min after its start. Nothing of ship two reached `origin/main` in that time: the shared checkout still stood on `features/ship-two` at `573dfe0` and its core gate still ran against its preview `turboslide-bxjtrzqfy` (pid 7415 at 00:39Z), with its push and its production gate ahead of it.

At 17:30 PDT (00:30Z on 2026-09-26) Kevin wanted the round on `main` now, so the wait ended there: ship two lands after this round and its ship step rebases over it. Part A restarted at 00:39Z on the same worktree, `vector/round` at `5bf158a` with this note the one dirty file: `pgrep -f "core-gate.mjs --base https://turboslide.vercel.app"` matched nothing (no production gate from this machine), so nothing to wait for; a fifth worktree of the repository, `/Users/kevinliu/repos/Turboslide-live` on `live/round` at `5bf158a`, had appeared since 22:00Z (not this step's; untouched).

## 8. The rebase after the wait and the checks

`git fetch origin` at 00:39:25Z: `origin/main` at `da00b5e` (the gate guard on `88d8e21` and `3c1f18a`), unchanged since 22:00Z; `git merge-base origin/main vector/round` reads `da00b5e`, and `vector/round` is sixteen commits ahead of it (`3db8e5b`, the specification, to `5bf158a`, the ship commit). `git rebase --autostash origin/main vector/round` at 00:41:11Z: "Current branch vector/round is up to date", the autostash of this note applied back, no conflict, no hunk touched. The head stays `5bf158a` and `ea332aa` keeps its hash, so `docs/gslides-parity/focus/ship-ea332aa.json` keeps its name and its `commit` field, README.md's Advanced tools sentence and the note's references stand, and the one amend the prompt allows is not used. No source file changed in the rebase, so no preview is deployed and no gate runs before the push: the run of record stays the verifier's pass 2 on `turboslide-kltqit0j2` built from `4061007` (827 rows, 790 passed, 20 failed, 17 not driven, retries zero, cost rows over ceiling 0; the round's 44 rows 42 green; `svg.render.vector-at-zoom` and `svg.copy.markup` red with their mechanisms in `notParked`, the production gate to rule).

Production before the push, `vercel inspect https://turboslide.vercel.app` at 00:43Z: `dpl_4hTgBSidwHSte78caHrmsdsRYfdq` (`turboslide-1dq2jhyp5-kl01s-projects.vercel.app`, target production, Ready, created 01:12:25 PDT on 2026-09-25 from the push of `da00b5e`), aliased to `turboslide.vercel.app`, `turboslide-kl01s-projects.vercel.app` and `turboslide-git-main-kl01s-projects.vercel.app`.

The checks after the rebase, the prompt's list, run in order by one detached script (`scratchpad/vector/ship/partA/checks.sh`, its logs beside it) from 00:41:42Z on a machine at load 230 to 300 (ship two's preview gate and other sessions' work), one at a time so the chain adds one process to that load: `node_modules/.bin/tsc -b`; the contracts by `node src/generate/main.ts --check` in `packages/agent` (the `--check` of `pnpm generate:contracts`, run as node so no pnpm runs); `node scripts/check.mjs --greps`; `node docs/readme/what-works.mjs --check`; `prettier --check` over the round's files (`git diff --name-only origin/main..vector/round` minus the pictures, 169 paths, the ignored evidence JSON and ledgers skipped by `.prettierignore`); then `vitest run` from each of `packages/schema`, `packages/chrome`, `packages/viewer`, `packages/render`, `packages/export` and `apps/studio` (its server unit tests; Playwright is not vitest's). The readings:

- `tsc -b` exit 0 (00:41:42Z to 00:44:55Z, 3 min 13 s at that load against 25 s in section 5).
- The contracts check exit 0 (8 s): `packages/agent/generated` is current.
- `check.mjs --greps` exit 0; `what-works.mjs --check` exit 0.
- `prettier --check` over the 169 text files of the diff: exit 2 on the first pass with zero `[warn]` lines, the 2 being "No parser could be inferred" for nine files prettier does not handle (`.prettierignore`, the four svg fixtures of `apps/studio/e2e/fixtures`, the two `.patch` files and `pictures-readings.txt` under `docs/gslides-parity/vector/build`, and the canvas snapshot `canvas.test.ts.snap`); the second pass over the 159 files minus those extensions exit 0, "All matched files use Prettier code style".
- The suites, `vitest run` in each package: schema 27 files, 609 passed; chrome 77 files, 737 passed, 2 todo; viewer 43 files, 432 passed; render 20 files, 389 passed; export 25 files, 179 passed, 1 skipped; apps/studio 72 files, 550 of 551 passed with one 5 s timeout (`agent-actions.test.ts`, "answers deck.info with the reader's counts", a file the round does not touch, last changed 2026-09-22), the run's transform at 196 s and its imports at 396 s under the load. That file alone: 6 of 6 in 1.19 s of tests. A second whole run of the studio suite at the default timeout: 549 of 551, the same test and `auth/actions.test.ts` "an anonymous principal reads its label" (untouched since 2026-09-14) timing out at 5 s with imports at 702 s; that file alone 7 of 7. A third whole run with `--testTimeout=30000`: 72 files, 551 of 551 passed in 31 s (00:54:50Z to 00:55:23Z, load 94 to 102). So the studio suite's logic is green on the ship commit's source; the two timeouts are the machine's load during the whole-suite imports, on tests of earlier rounds, and section 6's check loop read the same suite green in step 5 on this source at 22:00Z.

No amend, no new preview, no gate: the branch pushes as `5bf158a`.

## 9. The fast forward and the push

`main` is checked out in no other worktree (`git worktree list`: the shared checkout on `features/ship-two`, the hotfix and chrome worktrees detached at `88d8e21` and `da00b5e`, the live worktree on `live/round`), so the prompt's first path ran on this worktree: this note stashed for the switch (`main` at `2be923e` lacks it), `git checkout main` at 00:55:59Z ("behind origin/main by 8 commits"), `git merge --ff-only vector/round`: `main` fast forwarded from `2be923e` through `da00b5e` to `5bf158a`, the tree identical to `vector/round`'s, and `git push origin main` at 00:56:13Z: `da00b5e..5bf158a main -> main`, no force, accepted at the first try (6 s); `git fetch origin` reads `origin/main` at `5bf158a`. The stash popped back onto `main`, so the worktree now stands on `main` at `5bf158a` (`vector/round` at the same commit) with this note the one dirty file, the state part B starts from. Ship two did not land before the push: `origin/main` moved from `da00b5e` to `5bf158a` with no commit between, and the shared checkout still ran its preview gate; its ship step rebases over this round. The wait of section 7 ran 2 h 30 min (22:03Z to 00:33Z) and ended on Kevin's word, not on ship two's table.

## 10. The deployment and the smoke

The poll (`scratchpad/vector/ship/partA/poll-and-smoke.sh`: `vercel inspect https://turboslide.vercel.app` every 30 s, at most 15 minutes): 00:56:31Z, 00:57:05Z and 00:57:41Z still `dpl_4hTgBSidwHSte78caHrmsdsRYfdq` Ready; 00:58:14Z `dpl_93BST3WFAgcCsWPRpmWcPnfjAxg3` (`turboslide-iz88udnbq-kl01s-projects.vercel.app`), target production, Ready, the deployment the push started (`vercel ls` at 00:57Z read it Building, 30 s old, in the Production environment, beside a preview build `turboslide-q5ijqfnm9` 10 s old that is not this step's: nothing here deploys a preview). So `turboslide.vercel.app` serves `5bf158a` from 00:58Z, two minutes after the push. The GT team's production `turboslide-gt.vercel.app` builds from the same push under its own project; `vercel inspect` cannot read it under this project's context, so its deployment is not recorded here.

The hosted smoke, `node scripts/hosted-smoke.mjs --base https://turboslide.vercel.app --token-env TURBOSLIDE_TOKEN` through the wrapper (`--no-oidc`; the bearer of hosts.json's production row read into the variable, never printed), 00:58:18Z to 00:58:40Z: 40 of 40 passed, six rows skipped by name (the `/s/` exchange, the 410 after unpublish, the private document 403, the twin URL derivability, the restricted 404 and the template copy, each wanting a flag this step does not pass). Among the rows: `/` 307 to /new with noindex, /new, /deck/gt-brand (no notes key), /edit/gt-brand, /decks (109 cards, 7.8 s), /decks/trash, /print/gt-brand (85 pages), /present/gt-brand, /home, the asset twin, the headers of the three page routes, the json asset attachment, the cross site POST 415, the unsigned thumbnail 302 in shadow mode, the CSP report 204, the fourteen CDN rows (MISS then HIT), the two thumbnail rows, the effects backend and `deck.info` with 1 snapshot at revision 26. No deck was written on the store (the template copy row runs only with `--template-copy`), so nothing to trash.

Part A stops here. The worktree stands on `main` at `5bf158a` (`vector/round` the same commit), this note the one dirty file, `docs/gslides-parity/verification-3/parity-audit.json` unmodified. Part B runs the production gate (`scratchpad/vector/ship/run-gate-production.sh`, detached, polled at most every ten minutes, `--parked docs/gslides-parity/focus/ship-ea332aa.json`, the store swept after), the new write probe, the pictures, the README's two sections, the release note, the production table under "# Vector round, the production table (2026-09-24)" in VERIFICATION.md, and the second commit and push; it rules on `svg.render.vector-at-zoom` and `svg.copy.markup`. Ship two's preview gate still ran on `turboslide-bxjtrzqfy` at 00:59Z (pid 7415); its ship step rebases onto `5bf158a`, and its production gate and part B's must not run on one deployment at once (the Ship phase's rule).

After the push, at 00:58:49Z (17:58 PDT), Kevin's chrome commit `aff6e8a` ("a text object's squares sit on its ring, and the clock sits right after the name": `packages/chrome/src/TitleRow.tsx`, `packages/viewer/src/Gestures.tsx` and three tests, 26 insertions, 11 deletions) landed on `origin/main` on top of `5bf158a` (the round is its ancestor; nothing of the round was rewritten), pushed from the chrome worktree as the prompt said it would be. Production rebuilt from it: `dpl_AeJWPdqqvtwCDVmiQLeBDQxpR4um` (`turboslide-9fysrw6kq-kl01s-projects.vercel.app`) Ready and aliased at 01:00:46Z, so `turboslide.vercel.app` served `5bf158a` from 00:58:14Z to about 01:00:40Z and serves `aff6e8a` since. The smoke's 40 of 40 above was measured on `5bf158a`'s deployment; run again on the current one at 01:01Z it reads the same 40 of 40 (`smoke-2.log` beside the first). Part B's production gate runs on whatever deployment serves `main` when it starts, `aff6e8a`'s or a later push's, and the ship json stays named after `ea332aa`, the source the run of record measured.

## 11. The continuation, part B: production

Part B (the production gate, the probes, the pictures, the documents and the table commit) started 01:03Z on 2026-09-26 on the same worktree, detached at `origin/main` (`aff6e8a`: Kevin's chrome commit on top of the round's `5bf158a`; `vector/round` left at `5bf158a`), with this note the one dirty file. The rules of the header hold: the git writes of this part are the one commit of section 14 and its push; no `pnpm install`, `add` or `exec`, no build outside `scripts/check.mjs`, `round-five/integration` never touched, no lane's file edited, no token, cookie or secret printed (every command that needed the bearer ran through `scratchpad/vector/with-tokens.mjs --no-oidc`; production is not behind Vercel Authentication and the OIDC token was not needed or refreshed), nothing on Vercel or GitHub created, changed or deleted beyond the push; no dev server, no lock (a deployment base takes none), no other lane's process touched. The machine carried ship two's work throughout: its preview gate on `turboslide-ffc7yeovv` from 01:20Z (its shared checkout moved to `5f4c535`, "the parked list from the two preview runs", during this part's first run), the one minute load at 66 to 88 through this part's run of record.

### 11.1 The deployment

`vercel inspect https://turboslide.vercel.app` at 01:03Z: `dpl_AeJWPdqqvtwCDVmiQLeBDQxpR4um` (`turboslide-9fysrw6kq-kl01s-projects.vercel.app`), target production, Ready, created 00:58:54Z, its build log (`--logs`) opening on `Cloning github.com/Kevin-Liu-01/Turboslide (Branch: main, Commit: aff6e8a)` at 00:58:56Z, aliased to `turboslide.vercel.app`, `turboslide-kl01s-projects.vercel.app` and `turboslide-git-main-kl01s-projects.vercel.app`. So production serves Kevin's chrome commit with the round as its parent (part A's 10: the round's own build, `dpl_93BST3WFAgcCsWPRpmWcPnfjAxg3`, served from 00:58:14Z to about 01:00:40Z), and every reading below is of `aff6e8a`. The deployment before the push, `dpl_4hTgBSidwHSte78caHrmsdsRYfdq` (`da00b5e`), is the rollback. `origin/main` stayed at `aff6e8a` through this part (read at every poll); ship two did not push while the gate ran.

### 11.2 Run 1, stopped: another lane's sweep took the walk's deck

The gate (`scratchpad/vector/ship/run-gate-production.sh`: `node scripts/probes/core-gate.mjs --base https://turboslide.vercel.app --out <scratch>/gate-production-run1 --shots --parked docs/gslides-parity/focus/ship-ea332aa.json --matrix docs/gslides-parity/focus/verification/gate-production-vector.json`, the bearer alone through the wrapper) started 01:03:54Z. Its walk made `untitled-20260926-ixpi` at 01:03:58Z and read 144 rows green through the decks, slides and text areas; at step 145 (`text.align.toolbar-and-key`, about 01:17Z) the write came back in a reject card reading "A change was not applied. The room answered 404", the title row read "Couldn't save, retrying", the recover's reload of `/edit/untitled-20260926-ixpi` found no editor (`window.turboslide` undefined in every `page.evaluate` after it), and every row to the walk's end read that error (the walk ran on to its cleanup: 144 ok, 71 failed, 409 not driven at 01:22Z). `/edit/untitled-20260926-ixpi` answered 404 from four requests and `deck.list` with the trash at 01:21Z read 73 decks against 109 at this part's baseline of 01:06:48Z: 37 decks created between 21:18Z on 2026-09-25 and 01:04Z on 2026-09-26 had vanished, this run's walk deck among them (the ids are in `scratchpad/vector/ship/partB/baseline-decks.txt` and `decks-0121Z.txt`), while the shared checkout `/Users/kevinliu/repos/Turboslide` (read only for this part) had moved from `573dfe0` to `5f4c535` ("ship two: the parked list from the two preview runs") and started a walk against a new preview, `turboslide-ffc7yeovv`, at 01:20Z. So a sweep of the store by a created today rule from ship two's ship step, between its preview runs, is the mechanism read here (the rule of the hotfix's 16 item 13 and this part's item in 14: a sweep names the decks its own artefacts name, never a date), and nothing of the product. The run was stopped at 01:22:11Z from the wrapper's pid, the process tree of this run alone (nothing of ship two's), before its specs started; nothing of it was written under `docs/` (the `--matrix` file is written at the end), and its log and folder are kept in the scratch folder as `gate-run1-stopped-0121Z.log` and `gate-production-run1-stopped-0121Z/`. The walk's deck was already gone, so the run left nothing to sweep.

### 11.3 Run 2, the run of record

The same command with `--out <scratch>/gate-production-run2`, 01:22:16Z to 04:16:11Z, 10,435 s: 827 rows, 780 passed, 25 failed, 22 not driven (1 manual), 0 no step; `retries` zero; cost rows over their ceiling 2; the gate's verdict failed by the rows of 11.4. The walk (01:22:17Z, 3,759 s, the deck `untitled-20260926-djxj`, no stall, no retired deck): 595 matrix rows, 577 passed, 7 failed, 11 not driven, 650 steps (632 ok, 7 failed, 11 not driven), 8 console errors (resource loads answering 404 five times and 502 three times, none on a row). The specs (02:25:02Z, 5,338 s): 200 passed, 16 failed, 10 skipped (`assist.quota.429`, the five templates rows the guard of `da00b5e` skips on production, `fonts.picker.specimen-rows`, `logos.index.refresh-fixture`, `logos.index.cached-offline`, `sync.resend.idempotent`), 0 flaky. The cost probe (03:54Z, 1,330 s): 6 rows, 3 passed, 2 failed, 1 not driven, seven instances answering `sync.status` over the run. The default template read `blank` after the drivers and nothing was reset. The ledger is `docs/gslides-parity/focus/verification/gate-production-vector.json`; the walk table, the Playwright report with the failed rows' traces and the cost probe's JSON are in the scratch folder `gate-production-run2/`.

The round's 44 rows: 43 of 44 green in the run of record. Every walk row of the round passed on the first try (the thirteen `shapes.geometry.*` rows the walk drives, `shapes.insert.grid-shapes`, `-arrows`, `-callouts` and `-equation`, `shapes.insert.named-rows`, `shapes.icons.named-rows`, `menus.icons.insert-rows`, `menus.icons.format-rows`, `menus.icons.one-family`, `shapes.change-shape.plate`, `shapes.mask-image.plate`); of the spec rows, `shapes.geometry.text-rect`, `shapes.geometry.export.pptx-prst-avlst`, `svg.export.pdf-vector`, `svg.export.pptx-svgblip`, `svg.export.pptx-fallback`, `svg.export.web-page`, `logos.export.svgblip`, the five `svg.import.*` rows, `svg.render.picture-gestures`, `svg.copy.markup` and the four `svg.sanitize.*` rows passed; `svg.render.vector-at-zoom` read red with the preview's reading and `shapes.geometry.export.raster-modes` red once on its first download (11.4).

The two svg rows part A left to this gate: `svg.copy.markup` passed on production, the reading that rules on it: `text/plain` the markup (`<svg xmlns="http://www.w3.org/2000/svg" …`, 250 characters), `text/html` the envelope (`<!--turboslide:v1:{"kind":"blocks",…`), and Cmd+V landing a second picture at 416 by 216, 16 px right and down. The preview's red was the harness's header on the store host's redirect, as the ship json's `notParked` recorded, so `picture.svg.copy` stays in the default view and nothing is parked. `svg.render.vector-at-zoom` failed on its viewer assertion alone, with the preview's reading to the letter: the sheet at 100 and at 200 percent drew `/decks/untitled-20260926-j3uh/assets/mark.svg` (`currentSrc` the svg, 0 PNG requests of the asset), the show drew the same file, and the viewer at `/deck/<id>#s/<slide>` drew no `img` of the picture at revision 23, right after the row's own writes (V.5 finding 6: a page load answered by an instance at a revision behind the tab's, the blob tier's timing class VECTOR.md 6.2 forgives and records by id). It passed on the specs rerun once (the viewer drawing the svg), so it is recorded flaky in that class. Not a park: the four `intake.svg.*` ids stay in the default view; the viewer's revision wait in the row is B5's open request (V.10 item 5).

### 11.4 The rows not passed, by class

- Carried parks, red or not driven as the list expects: `inbox.bell-panel-toggle`, `inbox.settings-persist`, `inbox.notification-arrives`; `templates.gallery.page` (`templates.save.as-template`, `templates.save.same-name-replaces`, `templates.card.rename-and-delete`, `templates.default.use-for-new`, `templates.deck.read-only` not driven on production by the guard); `view.live-pointers.second-browser`; `versions.show-changes-marks`; the eight P1 rows whose controls are not on the build.
- The environment's: `assist.rewrite.card-accept-undo`, `assist.notes.draft`, `assist.free-ask.fallback-sentence`, `assist.mark.chip-and-history`, `assist.agent.propose-accept` (`assist.propose` answers 500 "The assistant is not set up on this Turboslide yet" without `ANTHROPIC_API_KEY`); `assist.quota.429` not driven without Upstash.
- Not driven by their rule: the four other P1 rows (`brand.objects.kit-colours-first`, `fonts.picker.specimen-rows`, `fonts.table.takes-family`, `templates.deck.read-only`); `logos.index.cached-offline` and `logos.index.refresh-fixture` (the index reads thesvg.org on production; `apps/studio/src/server/logos.test.ts` named); `sync.resend.idempotent` (no `origin` in the version list); `cost.editor-hidden.calls` (headless Chromium reports no hidden page); `text.clipboard.paste-without-formatting` (manual).
- Kevin's standing rows, never chased: `decks.file.open-list-search` (74 decks listed after 10,329 ms, the filter then read 0 and 74) and `sync.title.concurrent-both-kept` (round 1's second pair of words not in both titles within 5 s), both red in the run of record; `decks.file.open-list-search` red again in the walk's rerun (80 decks after 5,668 ms).
- The round's rows red once: `svg.render.vector-at-zoom` (11.3) and `shapes.geometry.export.raster-modes` (the Perfect PowerPoint of the row's four slide deck did not arrive within the download's 60 s bound, `download()` in `lib.ts` 739, so the PDF half and the two reports were not reached; the row passed on the preview). It passed on the specs rerun once (both files, both reports), so it is recorded flaky, the export function's time once on production; nothing parks.
- Earlier rounds' core rows red once in the run of record, each in a recorded class, rerun once (11.5): `decks.editor.move-to-trash` (the walk: the address did not move to `/decks` within 20 s after File > Move to trash; the cleanup's Delete forever then read `/edit/<id>` 404), `text.layout-runs.type` (the walk: the Big number's value not read back after a reload, a reload answered at a revision behind the tab's), `slides.layout.plate-four-columns` (`waitEditor`, `lib.ts` 121: the editor shell not settled within 90 s on the fresh deck, the return round's editor load class, VERIFICATION.md F9 and C3-F5), `decks.file.make-a-copy` ("decks/northwind-renewal-v4-without-notes exists already; pick another name": the fixed fixture id both lanes' export specs use, in the store since 20:55Z on 2026-09-25 from another lane's run, the shared store's collision the hotfix table recorded), `images.background.upload-picture` (no picture within 5 s, the asset put timing, ship one's SF.6 item 8), `comments.reaches-second-browser` (the second browser listed 0 threads 9,056 ms after the owner's submit with its stream connected, the second browser bound across instances, S.3), `sync.block.concurrent-same-offset-order` (round 3: A and B not in the same order within 5 s, 0 repair splices) and `sync.block.offline-replay-converges` (A's resend carried offset 18, not shifted past B's three words); the hotfix's production read the two sync rows red under the default template and green under Blank.
- The cost rows over once: `cost.editor-editing.calls` (store simple 175 a minute against 140, four instances answering `sync.status`; the function requests 68.92 against 75, advanced 66 against 85) and `cost.two-tabs-idle.calls` (one store `list` in the window against none; advanced 4 against 30), 11.6.

### 11.5 The reruns once, the two probes and the pictures

The walk's rerun, `node scripts/probes/editor-walk-probe.mjs --core --base https://turboslide.vercel.app --only decks,text --parked docs/gslides-parity/focus/ship-ea332aa.json --matrix docs/gslides-parity/focus/verification/production-vector-walk-rerun.json` (04:16:55Z, 628 s, the deck `untitled-20260926-3hcd`; the walk's `--matrix` writes its table under that name, as the earlier rounds' walk rerun ledgers): 111 steps, 108 ok, 2 failed, 1 not driven; 105 rows passed, 2 failed, 1 not driven. `text.layout-runs.type` passed (the Big number read back after the reload) and `decks.editor.move-to-trash` passed (the address moved to `/decks`, the card not listed): both recorded flaky in the blob tier's timing class. Red in the rerun: `decks.file.open-list-search` (Kevin's standing row) and `decks.save.acknowledged`, green in the run of record, red in the rerun alone (one window API write of the walk, a `block.remove` at 04:26:08Z in the text area, not acknowledged within 60 s; 0 fresh decks made), the return round's stall class on the blob tier (C3S-F4: an instance behind the store not answering a write), recorded, not chased.

The specs' rerun, `node scripts/probes/core-gate.mjs --base https://turboslide.vercel.app --only specs --spec chrome,decks,export,images,share,svg,sync --parked docs/gslides-parity/focus/ship-ea332aa.json --matrix docs/gslides-parity/focus/verification/production-vector-specs-rerun.json` (04:16:55Z, 3,887 s): 162 rows, 149 passed, 12 failed, 1 not driven, `retries` zero. Green after a red in the run of record, recorded flaky in their classes: `shapes.geometry.export.raster-modes`, `svg.render.vector-at-zoom`, `slides.layout.plate-four-columns`, `images.background.upload-picture`, `comments.reaches-second-browser`, `sync.block.concurrent-same-offset-order`, `sync.block.offline-replay-converges`, and Kevin's standing `sync.title.concurrent-both-kept`. Red again: `decks.file.make-a-copy`, the same fixture collision, red in both production readings and the blocker named in 13. Red in the rerun alone, green in the run of record, each in a bound class of the blob tier and recorded, not chased: `versions.restore` (the restored text not within 35 s, the panel notice drawn), `decks.trash.confirm-dialog-chrome` (the trash page's "Restored" snackbar not within 15 s), `collab.roster.go-to-slide` (`waitEditor`, the editor shell not settled within 90 s), `share.dialog.co-edit-from-copied-link` (the second browser reading `viewing` for 5 s after the link with Editor access), `sync.structural.concurrent` (one browser at 3 slides against 2 for 15 s after the concurrent delete; the two drags agreed at 4,214 ms), `sync.undo.after-remote` (round 1: A's word still in B 5 s after the undo), `svg.copy.markup` (the paste half: "Cmd+V lands a second picture" false within its bound, with the copy half green, the markup and the envelope both on the clipboard; the flake 4429 read once, V.5 finding 8) and `svg.export.web-page` (its teardown assertion alone: "owner 14: every scratch deck is trashed, deleted forever and answers 404" failed while the page itself read as the row expects, one svg data URI, the picture's, 0 PNG data URIs, in 1,952 ms; the store's refusal window on the deck's delete, V.5 finding 10); the parked `templates.gallery.page`, `view.live-pointers.second-browser` and `inbox.notification-arrives`. The walk's and the specs' reruns ran at once on their own decks, as the hotfix's did, with ship two's preview gate on the machine.

The cost rows' rerun, `node scripts/probes/core-gate.mjs --base https://turboslide.vercel.app --only cost --cost-rows cost.editor-editing.calls,cost.two-tabs-idle.calls --parked docs/gslides-parity/focus/ship-ea332aa.json --matrix docs/gslides-parity/focus/verification/production-vector-cost-rerun.json` (05:23:28Z, 438 s, after the pictures and with no other driver of this part on the deployment): 2 rows, 2 passed; `cost.editor-editing.calls` 64.20 function requests a minute (193 in 3.01 min) against 75, store simple 92 against 140, advanced 21 against 85, one instance answering; `cost.two-tabs-idle.calls` advanced 18 against 30, `list` 0, one instance. The run of record's 175 came with four instances answering the deck's `sync.status` (11.6).

`node scripts/probes/new-write-probe.mjs --base https://turboslide.vercel.app` (04:17:21Z, 52 s): all steps ok (the double click on the title placeholder, the first write creating `untitled-20260926-z833` and moving the address, twelve sequential edits each acknowledged at the next revision, no stale message, no 404 on the draft thumbnail route, the slide switch keeping the editor, the client id and the one stream, the reload at the tab's revision, the second page's immediate keystroke landing; the cross tab presence counts recorded, not asserted, on the blob tier), the deck trashed and deleted forever by the probe. `node scripts/hosted-smoke.mjs --base https://turboslide.vercel.app --token-env TURBOSLIDE_TOKEN` through the wrapper (04:18:14Z, 13 s): 40 of 40 passed (part A read the same 40 of 40 twice on this deployment's predecessor and on this one). Both ran while the reruns' drivers ran on their own decks, as the hotfix's did.

`node docs/gslides-parity/vector/build/ship/shoot-production.mjs --base https://turboslide.vercel.app` (05:22:02Z to 05:23:27Z, 85 s): eighteen pictures at 1440 by 900 under `docs/gslides-parity/focus/verification/production-vector-<surface>-1440-<appearance>.jpg` and the record `production-vector-views.json`. In each appearance: the Insert menu (every row of VECTOR.md 3.2 with an icon, the script's row reading in the record), Insert > Shape with its seven rows, the Shapes grid (99 tiles, 94 distinct glyphs), Arrows (26, 26), Callouts (4, 4) and Equation (6, 6) open from the menu bar, an svg pasted as markup with Figma's prolog (landed in 2,240 ms light and 1,419 ms dark, `src` the deck's `assets/pasted.svg`, `object-fit: contain`) and selected with the toolbar Crop disabled, Format > Image with the picture selected (Crop image `aria-disabled` with the tooltip "An SVG picture cannot be cropped. Resize it instead"; the Format rows' icons read), and the picture at 400 percent drawing `pasted.svg` at 3,284 css px against a natural width of 96 with 0 PNG asset requests. The two scratch decks (`untitled-20260926-8g69` light, `-m3y9` dark) were trashed and deleted forever by the script, `/edit` answering 404 each.

### 11.6 The cost rows

The six cost rows of SYNC.md 6.1 in the run of record, beside their ceilings (the table is VP.4 of the production table): `cost.editor-idle.calls` 9.66 function requests a minute against 12 (the hotfix's and the preview's 9.66), store simple 34 against 40 (3 the probe's own heads), advanced 9 against 11, two instances; `cost.editor-hidden.calls` not driven by its rule (the visible tab 9.32 a minute, 9 polls); `cost.editor-editing.calls` 68.92 against 75 (the preview's 68.92), simple 175 against 140, over, advanced 66 against 85, four instances answering; `cost.two-tabs-idle.calls` advanced 4 against 30, `list` 1 against none, over, one instance; `cost.show.calls` 0 function requests, the store's one head at the first sample the probe's own and the page's 0, the settle 155.89 s; `sync.pull.no-listing` passed (`versions` 0, `get` 17 against 12 commits, the two `list` calls of the window on `assets`, the row's own uploads). The rerun once of the two rows over (11.5): both under their ceilings with one instance (simple 92, `list` 0). The function counts match the hotfix's and the preview's to the second decimal, so the round adds no function request (VECTOR.md 4.5); the simple count of the editing window scales with the instance spread of the blob tier under fluid compute (the hotfix's SFH.4: 121 to 155 on two instances), not with the round's code, whose one store read of its own (the svg read ahead) is one fetch per asset per page on a selection of an svg picture, outside the probe's states. The two large deck measures: the 85 slide PDF in 15.4 s, the editable PowerPoint with embedded fonts in 176.8 s.

## 12. The store after the runs

This part's baseline at 01:06:48Z, before run 1's walk had made anything but its own deck: `deck.list` 109 live decks (the trash not asked), the templates `blank`, `gt-brand` and five "Core spec template" cards of other lanes' runs, the default `blank`. The store at 01:21Z, after another lane's sweep (11.2): 73 live decks. After the runs, at 05:31Z to 05:32Z, `deck.list` with the trash through production with the bearer read 115 decks (94 live, 21 trashed) after the runs. Of the 36 deck ids this part's own artefacts name (the walk tables, the spec reports, the cost probe, the write probe and the pictures' record), one was still on the store: `untitled-20260926-onbn` (the decks spec's thumbnail deck of the run of record), trashed and deleted forever (`trash 200`, `remove 200`, `/edit answers 404`); every other one was removed by its driver's own teardown (the walk's `untitled-20260926-djxj` and `-3hcd`, the write probe's `-z833`, the pictures' `-8g69` and `-m3y9`, the specs' decks and copies), and run 1's `-ixpi` was gone before the sweep (VP.2). Left alone under the rule (no artefact of this part names them): the decks other lanes' gates made on the shared store during this part's window (among them `untitled-20260926-6i7w` and `-37w2`, the "Brand spec deck", "Core spec template" and run tagged `northwind-renewal-v4-without-notes-mb29` decks of ship two's preview gate, and `-zh66`, `-vknt`, `-7dfe` and `-g98d`, whose titles match the specs' fixtures but which no report of this part names), the earlier rounds' decks and `gt-brand`, and `northwind-renewal-v4-without-notes` of 20:55Z on 2026-09-25 (VP.6 item 2). The templates facet: `blank`, `gt-brand` and nine "Core spec template" cards of other lanes' runs (20:59Z on 2026-09-25 to 04:59Z on 2026-09-26; this part's brand spec wrote none, its five templates rows skipped on production), the default `blank` on the instance asked at 05:31Z and read `blank` by the gate after its drivers.

## 13. The verdict of the ship, as read on production

The vector round is on production at `5bf158a` with Kevin's chrome commit `aff6e8a` on top, and every feature of the round is in the default view as VECTOR.md 6.2 and the parked list say: Insert > Shape is the three named rows over the four grids with their glyphs, every one of the 135 presets draws from its definition, Change shape and Mask image are in the default view, every Insert and Format row of 3.2 and 3.3 carries its icon, and an svg uploaded, pasted, dropped or fetched stays vector on the sheet, in the show, in the viewer, in the PDF, in the PowerPoint (beside its PNG fallback) and in the web page, with a logo taking the same path. The production run of record read 827 rows, 780 passed, 25 failed, 22 not driven, `retries` zero, and 43 of the round's 44 rows green on the first reading; the forty fourth, `svg.render.vector-at-zoom`, read red once with the preview's viewer timing and green on the rerun, and `shapes.geometry.export.raster-modes` read red once on a download's 60 s bound and green on the rerun; `svg.copy.markup` passed on production, so the harness's header was the preview's whole mechanism. Nothing of the round parks: the list `ship-ea332aa.json` stands as shipped, `parked-controls.ts` keeps its fourteen ids, no `advanced` flag moves, VECTOR.md 7 and the README's Advanced tools paragraph read the production result, and no revert is made. The rows of earlier rounds not green are in their recorded classes (11.4, 11.5): eight red once and green on the rerun, six red in the rerun alone, the parked list's, the environment's, the P1 and fixture rows', Kevin's two standing rows (`decks.file.open-list-search` red twice, `sync.title.concurrent-both-kept` red once and green on the rerun, never chased), and one red in both production readings, named as the blocker for the round after and never hidden: `decks.file.make-a-copy`, the copy refused with the product's own sentence because the spec's fixed fixture id `northwind-renewal-v4-without-notes` sits in the shared store from another lane's run since 20:55Z on 2026-09-25; the seam is the spec's fixed id against a leftover the store's refusal window kept (a per run tag on the fixture id, or the store per lane), not the product's Make a copy, and the hotfix's production read the row green once the leftover was gone. The two cost rows over their ceiling once read under them on the rerun with one instance answering (11.6). The write probe read all steps ok and the hosted smoke 40 of 40; run 1 of the gate was stopped after another lane's sweep took its deck, and the store is left with nothing of this part's (12).

## 14. The documents of the table and the commit

This section and 11 to 13; VERIFICATION.md "Vector round, the production table (2026-09-25)" (VP.1 to VP.6, the hotfix table's shape); the release note "2026-09-25, the vector round" in `docs/updates.md`; the README's "Advanced tools" paragraph with the production sentence on the two svg rows, and its "What works today" section regenerated by `node docs/readme/what-works.mjs` (rendered from the matrix's `today`, so the section did not change; the production readings are in the ledgers and the table); the ledgers `gate-production-vector.json` (the run of record), `production-vector-walk-rerun.json` (the walk's table), `production-vector-specs-rerun.json` and `production-vector-cost-rerun.json`, the eighteen pictures `production-vector-*.jpg` and `production-vector-views.json` under `docs/gslides-parity/focus/verification/`. `docs/VECTOR.md` is not touched by this part (its section 7 paragraph on the list stands: nothing parks); the ship json, `parked-controls.ts` and the `advanced` flags are unchanged. `docs/gslides-parity/verification-3/parity-audit.json` was not rewritten (no check chain ran in this part). Prettier ran over the four documents (`--write`) and `--check README.md` reads clean; `node docs/readme/what-works.mjs --check` reads the README current. The paths were scanned for token, JWT, bearer and cookie shapes before the add. The commit on `main`, "Turboslide vector round: the production table", with the trailer, by an explicit path list: `README.md`, `docs/updates.md`, this note, `docs/gslides-parity/focus/VERIFICATION.md`, the four ledgers, the eighteen pictures and the views JSON; never `.github/`, never `.turboslide/` or `.vercel/`. Then `git push origin HEAD:main`; the hash is in this part's report. Vercel's build of the push touches no source (notes, ledgers and pictures alone), so production keeps serving the code of `aff6e8a`.
