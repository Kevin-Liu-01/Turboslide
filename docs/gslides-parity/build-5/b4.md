# B4 notes, round five: page, print, ODP, SVG, the shape interpreter and the card

Builder B4 of `docs/gslides-parity/MILESTONES-5.md`, dev server port 4354, over the shared working tree at `BASE` `d5a1be5` after the integrator's day 0 seam (`build-5/integrator.md`). Section numbers refer to `SPEC-5.md` unless prefixed; R08 is `research-5/08-page-setup-and-print-layouts.md`. No git write command ran, no `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran; binaries were called as `node_modules/.bin/<tool>` or `node <script>`. Every command ran from `/Users/kevinliu/repos/Turboslide` unless a package directory is named.

## 1. Days 1 to 2: the page sweep (MILESTONES-5 B4 item 1; SPEC-5 6.1; R08 3a to 3i)

### 1.1 What landed

- `Deck.page` runtime: `deckPage(deck)` (the integrator's day 0 identity) is now the one reader every derivation goes through; `packages/schema/src/render.ts` gains `SLIDE_INSET_PX`, `SLIDE_PAD_PX`, `CONTENT_INSET`, `contentBox(page)` (`[137, 129, W - 274, H - 258]`), `pageBox(page)`, `pageCentre(page)`, `pageAspect(page)`, `pageInches(page)`, `PAGE_CAP`, `presetOfPage(page)`, `boxInsideSheet(box, page = DEFAULT_PAGE)` and `coversPage(pos, page = DEFAULT_PAGE)`; `SHEET_WIDTH`, `SHEET_HEIGHT`, `CONTENT_BOX` and `RAIL_PX` stay as the default page's values (R08 1.1 "data: become `DEFAULT_PAGE`").
- `grid(page)` in `packages/theme/src/tokens.ts`: `{ sheet, content, contentOrigin, contentBox, chips, columns, centre }`; `columnWidths(ratio, gap, contentWidth)` takes the content width; `SHEET`, `CONTENT`, `CONTENT_ORIGIN`, `CHIPS`, `COLUMNS` stay as the default page's values. `tokens.test.ts` asserts `grid(DEFAULT_PAGE)` equals the legacy constants, `grid({ 1200, 900 })` yields 926 by 642 with the chips at 66, 858 and 1074, 856 (and the 5/7 columns at 355.8 and 498.2 over 854 usable px), `grid({ 1440, 900 })` gives 1166 by 642 with the right chip at 1314, 856, and that `SHEET` equals the schema's `DEFAULT_PAGE`.
- `geometry(page)` in `packages/render/src/geometry.ts`: `{ sheet, content, chips, colsWidths(ratio, gap), slotBoxes(layout) }`; `colsWidths(ratio, gap, page)`, `slotBoxes(layout, page)` and `chipsOf(page)` take the page as a trailing argument with the default page when absent, so every round one call is unchanged. `packages/render/src/__tests__/geometry.test.ts` (new) pins the default, Standard (4:3), the agreement with `grid(page)` and `layoutSlotBoxes(layout, page)` on four pages, the stage root's custom properties and the print stylesheet.
- The stage's two custom properties: `renderStage(slidesHtml, { page })` writes `data-page="WxH"` and `style="--ts-sheet-w:Wpx;--ts-sheet-h:Hpx"` on the `.ts-sheet` root (`sheetSizeStyle(page)`, `pageAttr(page)` exported); `stage.css`'s `.ts-sheet .sheet` and `.ts-sheet .stage` read `var(--ts-sheet-w, 1600px)` and `var(--ts-sheet-h, 900px)`; `renderDeck`, `renderStandalone` and `renderPrintDocument` pass `deckPage(deck)`; the two runtimes (`render/runtime.ts` `fit()`, `viewer/standalone/runtime.ts` `fit()`, `scaleThumbs`, `scaleBook`) read the properties once at boot with the GT sheet as the fallback. The stage snapshot in `render/__tests__/__snapshots__/slide.test.ts.snap` gained the two attributes (one line; `vitest run -u` on that file alone).
- `pageEmu(page)`, `pageIn(page)`, `layoutName(page)`, `isPageAspect(w, h, page)` and `scenePage(scene)` in `packages/export/src/units.ts`; `PAGE_IN`, `PAGE_EMU` and `SHEET_PX` stay as the default page's values and `pageIn(DEFAULT_PAGE)` returns `PAGE_IN` exactly (the six decimal 13.333333 that writes 12,192,000 EMU).
- `scaleCanvas(slide, from, to, mode)` in `packages/schema/src/canvas.ts` with `SCALE_CANVAS_MODES`, `scaleFactor`, `scalePosition`, `scaleGuides` (R08 3d): `keep` returns the slide as it is; `fit` (`k = min`) and `maximize` (`k = max`) scale every positioned block's box about the sheet centre at 1/64 px without snapping, with the typography's pixel fields (`size`, `spaceBefore`, `spaceAfter`, `indent`, `firstLine`, `hanging`; `tracking` is in em and stays), the padding, a table's column widths, row heights and `size`, a mark's `w` and `h`, a shot's `width`; `rotate`, `flip`, `group` and `z` stay; a grammar slide is returned as it is; the guides scale under `fit` and `maximize` and drop past the new edge under `keep` with the count. `canvas.test.ts` pins the centred object staying centred, 1600 mapping to 1200 under `fit`, the rotated box, the table fields and the guides.
- The `shapePath` seam: new `packages/schema/src/shapes/geometry.ts` with `shapePath(preset, box, adjusts): PathCommand[]`, `textInset(preset, box, adjusts): ShapeBox`, `sites(preset, box, adjusts): ConnectionSite[]`, `boxPath`, `boxSites` and `pathData(commands)` (SVG path data on the half pixel grid with `H` and `V` for the axis aligned segments and an `arcTo` written as arcs of at most a quarter turn); `shapes.ts`'s `shapePath(kind, w, h, adjust): string`, `textInset` and `sites` delegate to it, so the rectangle string `M0,0 H1600 V900 H0 Z` is byte identical to what round two wrote. `shapes/geometry.test.ts` (new) asserts the box for all 135 presets, the string identity and the path writer per command kind. The interpreter itself is day 6.
- `compare-to-shoot.mjs` and `canvas-fidelity.mjs` read `SHEET` from `deck.json`'s `page` (the default when absent; the GT deck carries none, so the numbers do not change); `canvas-fidelity` opens its sheet pages at the deck's viewport and converts through `toCanvas(slide, boxes, page)`, so `--deck decks/fixture/page-4-3` shoots at 1200 by 900 (R08 3i: step 32, not step 24).
- `decks/fixture/page-4-3` (`page: { 1200, 900, standard-4-3 }`, eight slides in two sections: `title`, `statement`, `columns` (cols 5/7 with a rows block), `table`, `chart` (bar), then `opener` (the picture kind, first in its section), `canvas-edge` (a shape at the content edge, an ellipse crossing the right rail zone at 984 to 1144, a text box), `dither` (a picture object with `bayer8` at strength 0.6, its variant materialized through `turboslide picture materialize`, 468 KB of assets copied from the gslides fixture's two tone twins); guides at 600 and 450) and `decks/fixture/page-16-10` (`page: { 1440, 900, widescreen-16-10 }`, three slides: `title`, `columns` (1/1), `canvas`). Both validate with 0 errors; the 4:3 lint reads four findings at severity 1 (`opener/sentence-lists-section`, two `layout/freeform`, `export/non-native`) and none above.

### 1.2 The 123 sites of R08 part 1, row by row

Verdicts as R08 wrote them; the disposition column says what this sweep did. "derived" means the value is now a function of the deck's page with the constant kept as the default; "data" means a literal became a field, an attribute or a custom property; "stayed" means untouched by design; "deferred" names the day or the lane. Every derived row is behaviour identical on a deck without a `page` field.

Schema (R08 1.1):

| Site                                                                                                                   | Verdict           | Disposition                                                                                                                                                                                                                                                                                                                                                                                           |
| ---------------------------------------------------------------------------------------------------------------------- | ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `render.ts` `SHEET_WIDTH`, `SHEET_HEIGHT`                                                                              | data              | kept as the default page's values; `DEFAULT_PAGE` is the truth                                                                                                                                                                                                                                                                                                                                        |
| `render.ts` `CONTENT_BOX`                                                                                              | derive            | `contentBox(page)`; the constant stays as the default                                                                                                                                                                                                                                                                                                                                                 |
| `render.ts` `RAIL_PX`                                                                                                  | stay              | stayed                                                                                                                                                                                                                                                                                                                                                                                                |
| `render.ts` `boxInsideSheet`                                                                                           | derive            | `boxInsideSheet(box, page = DEFAULT_PAGE)`                                                                                                                                                                                                                                                                                                                                                            |
| `canvas.ts` `PICTURE_POS`                                                                                              | derive            | `picturePos(page)`; the constant stays                                                                                                                                                                                                                                                                                                                                                                |
| `canvas.ts` unplaced stack, picture placement, slot boxes                                                              | derive            | `toCanvas(slide, boxes, page)`, `placer(measured, page)`, `plateFallback(side, maxWidth, page)`, `fromCanvas(slide, page)`                                                                                                                                                                                                                                                                            |
| `canvas.ts` `extent(axis)`                                                                                             | derive            | `normalizeGuideList(values, axis, page)`, `applyGuides(current, input, page)`, `guideCentre(page)` beside `GUIDE_CENTRE`                                                                                                                                                                                                                                                                              |
| `deck.ts` `GUIDE_MAX`                                                                                                  | data              | landed by the integrator on day 0 (`PAGE_MAX_PX`; `validate/page.ts` bounds by the page)                                                                                                                                                                                                                                                                                                              |
| `deck.ts` `PLATE_WIDTHS`; `layouts.ts` mark sizes, plate `maxWidth`, `PROBE_DECK`; `chart.ts` `CHART_LEGIBLE_WIDTH_PX` | stay              | stayed                                                                                                                                                                                                                                                                                                                                                                                                |
| `diagrams.ts` default box                                                                                              | derive the centre | `diagramDefaultBox(page)` centred and clamped to the content box; `DIAGRAM_DEFAULT_BOX` stays (diagrams.test pins it)                                                                                                                                                                                                                                                                                 |
| `freeform.ts` `CONTENT_X..H`, `offSheetKind`, `offSheet`, `alignReference`, `layoutSlotBoxes`                          | derive            | every function takes `page = DEFAULT_PAGE` last: `columnWidths(ratio, gap, contentWidth)`, `verticalGuides(page)`, `horizontalGuides(page)`, `guidesFor(page)`, `insideContent(pos, page)`, `offSheetKind`, `offSheet`, `alignReference`, `alignPositions(positions, edge, target, snap, page)`, `layoutSlotBoxes(layout, page)`, `convertLayout(slide, layout, page)`; `GUIDES` stays as the default |
| `docs/freeform.md:81` snap guides                                                                                      | derive            | the guides derive in code; the prose is day 3's document pass                                                                                                                                                                                                                                                                                                                                         |
| `rules.ts`, `position.ts`, `catalog.ts` help text                                                                      | stay, reworded    | stayed as written: `rules.ts` and `catalog.ts` feed `rules.json` and the generated contracts, which only the integrator regenerates; the rewording is on the day 3 request list                                                                                                                                                                                                                       |
| `assets.ts` material `size: [3200, 1800]`                                                                              | data              | deferred: the integrator's schema file; widening the literal tuple to any 2x page is a request (section 4, R2) because it moves the material capture (`materials/capture.ts` `FRAME_SIZE`) and `mount.ts` `maxPixelCount` with it                                                                                                                                                                     |

Theme (R08 1.2):

| Site                                                                                                                    | Verdict | Disposition                                                                                                              |
| ----------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------ |
| `tokens.ts` `SHEET`                                                                                                     | data    | stays as the default page's values, asserted equal to `DEFAULT_PAGE`; `grid(page)` derives                               |
| `RAIL`, `INSET`, `PAD`, `CROSS`, `WORDMARK`, `COUNTER`, `PICTURE_INSET`, `LADDER`, `DISPLAY`, `FLOOR`, `PLATES`, `MARK` | stay    | stayed                                                                                                                   |
| `CONTENT`, `CONTENT_ORIGIN`, `CHIPS`, `COLUMNS`, `columnWidths`                                                         | derive  | `grid(page)`; `columnWidths(ratio, gap, contentWidth)`                                                                   |
| `sheet.css` frame, counter, wordmark                                                                                    | stay    | stayed (B6's file this round)                                                                                            |
| `stage.css` `.sheet` and `.stage`                                                                                       | data    | `var(--ts-sheet-w, 1600px)` and `var(--ts-sheet-h, 900px)`; `tokens.test.ts` asserts the declarations carry the fallback |
| `stage.css` `.slide` inset and padding                                                                                  | stay    | stayed                                                                                                                   |

Render, viewer and present (R08 1.3):

| Site                                                                           | Verdict                | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------------------------------------------ | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `render/geometry.ts` `SHEET`, `CONTENT`, `CHIPS`, `colsWidths`                 | derive                 | `geometry(page)`, `chipsOf(page)`, `colsWidths(ratio, gap, page)`, `slotBoxes(layout, page)`                                                                                                                                                                                                                                                                                                                                                                                    |
| `render/stage.ts` `renderStage`                                                | data                   | `page` option; `--ts-sheet-w`, `--ts-sheet-h`, `data-page` on the root                                                                                                                                                                                                                                                                                                                                                                                                          |
| `render/slide.ts` chips and `plateBox`                                         | derive                 | `renderSlide` reads `geometry(deckPage(deck))`; `plateBox(plate, geo)`, `renderLayout(slide, ctx, geo)`, `renderFreeform(blocks, ctx, geo)`, the `slotWidth: 1326` literals are `geo.content[2]`, `coversSheet(pos, page)`                                                                                                                                                                                                                                                      |
| `render/blocks/picture.ts:76` fallback width                                   | derive                 | the literal no longer exists at `BASE` (the fallback reads the asset); nothing to do                                                                                                                                                                                                                                                                                                                                                                                            |
| `render/dither-runtime.ts:140`                                                 | derive                 | the fallback reads the closest `.ts-sheet` root's custom properties, 1600 by 900 when it carries none                                                                                                                                                                                                                                                                                                                                                                           |
| `render/measure-dom.ts`                                                        | stay                   | stayed (reads the element)                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `render/print.ts` `PRINT_PAGE_PT`, `PRINT_PAGE_PX`, `PRINT_CSS`, viewport meta | derive per document    | `printPagePt(page)`, `printCss(page)`; `PRINT_CSS` is `printCss(DEFAULT_PAGE)` byte for byte; `renderPrintDocument` reads `deckPage(deck)` and returns `page`; `PRINT_SCALE` stays                                                                                                                                                                                                                                                                                              |
| `render/runtime.ts` `fit()`                                                    | data                   | reads the custom properties once                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `render/standalone.ts`                                                         | data                   | passes `page` to the stage; the runtime reads it                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `viewer/model.ts` `SHEET_W`, `SHEET_H`                                         | data                   | `ViewerDeck.page?`, `PageSize`, `DEFAULT_PAGE_SIZE`, `sheetSizeVars(page)`; the constants stay                                                                                                                                                                                                                                                                                                                                                                                  |
| `viewer/Sheet.tsx` `fitSheetAt`                                                | derive                 | `Sheet` takes `page`, fits `w` and `h` from it and writes the custom properties on the `.sheet` element for stage.css's `.stage` box                                                                                                                                                                                                                                                                                                                                            |
| `viewer/rulers-model.ts`                                                       | derive the length      | `axisLength(axis, page)`, `rulerTicks(axis, page)`, `rulerNumerals(axis, page)`, `rulerToSheet(axis, client, edge, k, page)`; the unit is day 3 over B5's `Preferences`                                                                                                                                                                                                                                                                                                         |
| `viewer/Editor.tsx` covers, insert box, `k`, guide clamp                       | derive                 | `const page = deckPage(doc.deck)` with a ref; `coversSheet(slide, id, page)`, `pageBox(page)` for the picture insert, `k = rect.width / page.width`, the guide clamp, `pageCentre(page)` for the zoom centre and the wheel fallback, `centredBox(size, page)`, `guideCentre(page)`, `sheetPoint(rect, x, y, page)` at all 17 sites, `rulerToSheet(..., page)`, the snap lines by page, `GestureContext.page`, `handlesFor(..., { page })`, `<Sheet page>` and `RulersView.page` |
| `viewer/Gestures.tsx` `k`, insert sizes                                        | derive `k`; sizes stay | `sheetPoint(stage, x, y, page)`, `centredBox(size, page)`, `GestureContext.page`, `HandleOptions.page` (the column seam handle's height and the slot fallback read `grid(page)`); the tool sizes stay                                                                                                                                                                                                                                                                           |
| `viewer/LiveClone.tsx`, `LiveClone.css`                                        | derive                 | `LiveClone` takes `page` and writes `--k = w / page.width` and the custom properties; the Filmstrip rows pass nothing yet (request R4 to B1)                                                                                                                                                                                                                                                                                                                                    |
| `viewer/canvas-measure.ts` hidden sheet                                        | derive                 | `mountHiddenSheet(theme, host, page)`; `withHiddenSheet` reads `deckPage(document.deck)`                                                                                                                                                                                                                                                                                                                                                                                        |
| `viewer/snap.ts`                                                               | derive                 | `plateSideFor(kind, x, page)`, `sheetSnapLines(page)`, `sheetEdgeLines(page)`, `deckGuideLines(guides, page)` over `guidesFor(page)` and `grid(page)`                                                                                                                                                                                                                                                                                                                           |
| `present/PresenterConsole.css:201`                                             | derive                 | `aspect-ratio: var(--ts-page-aspect, 16 / 9)`                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `Slideshow.tsx`                                                                | stay                   | stayed (`Stage` and `Sheet` take `page`; the presenter and the slideshow pass it when B1 threads `ViewerDeck.page`, request R4)                                                                                                                                                                                                                                                                                                                                                 |
| `viewer/standalone/runtime.ts`                                                 | data                   | reads the custom properties once (`pageW`, `pageH`) for `fit`, `scaleThumbs` and `scaleBook`                                                                                                                                                                                                                                                                                                                                                                                    |
| `standalone/chrome.ts:81` comment                                              | stay                   | stayed                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

Chrome (R08 1.4):

| Site                                                         | Verdict                    | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------------------------------------------------------------ | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `chrome/Rulers.tsx`                                          | derive the numerals        | `RulersProps.page` into `rulerTicks(axis, page)`; the Overlay passes `view.rulers.page`; the unit is day 3                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `chrome/Rulers.css`                                          | stay                       | stayed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `chrome/Overlay.tsx:39` inch readout                         | derive the unit            | the unit is day 3 over `preferences.units` (B5's record at merge 1)                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `menus/model.ts` `SHEET_CENTER_X`, `SHEET_CENTER_Y`          | derive                     | deferred to the integrator (the shared file): request R1, the two Add guide rows read `guideCentre(page)` from the shell                                                                                                                                                                                                                                                                                                                                                                                                       |
| `menus/model.ts:927` `file.pageSetup`                        | data (Now with the dialog) | day 3 with `PageSetup.tsx` (a request to the integrator for the flip)                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `menus/strings.ts` `GUIDES.inches`                           | derive the unit            | day 3                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `inspector/position.tsx`, `sections.ts` doc text             | stay, reworded             | day 3: the Position fields read the page and `preferences.units`                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `inspector/dither.tsx` twin canvases                         | derive                     | the canvases are sized from the worker's bitmaps at draw time already (`canvas.width = bitmap.width`); the JSX attributes are placeholders; the worker's screen follows the page with request R3                                                                                                                                                                                                                                                                                                                               |
| `editor-shell.ts`, `palette-data.ts` insert sizes and centre | sizes stay; centre derives | `coversSheetOf(slide, block, page)` reads `deckPage(input.document.deck)` in `factsOf`; `centredPosition(size, page)`; `backgroundPicturePos(page)` beside `BACKGROUND_PICTURE_POS`; `defaultPosition(slide, type, blockId, page)`; the insert plan's `centredPosition` call passes the page on day 3 when the plan carries the deck                                                                                                                                                                                           |
| the nine `aspect-ratio: 16 / 9` stylesheets                  | derive                     | `var(--ts-page-aspect, 16 / 9)` in `Sidebar.css`, `ThemesPanel.css`, `Dialog.css`, `AssetPicker.css`, `inspector/asset.css`, `inspector/dither.css`, `pickers/DiagramPicker.css`, `decks.css`, `styles.css`, plus `Filmstrip.css`, `BookView.css`, `GridView.css`, `PresenterConsole.css` and `print.css`; the shell root sets `--ts-page-aspect` from the deck on day 3 (request R1 for `EditorRoot.tsx`); `sheet.css:717` (the skeleton curtain, B6's file) and `brand.css:167` (the 1200 by 630 card is not a sheet) stayed |
| `dialogs/Download.tsx:148` sentence                          | derive                     | day 3 with the dialog's page rows                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `dialogs/Publish.tsx:21` embed medium                        | height derives             | day 3                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

Export, verify, headless (R08 1.5):

| Site                                                                                    | Verdict                | Disposition                                                                                                                                                                                                                                                     |
| --------------------------------------------------------------------------------------- | ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `units.ts` `PAGE_IN`, `PAGE_EMU`, `SHEET_PX`                                            | derive                 | `pageIn(page)`, `pageEmu(page)`; the constants stay as the default                                                                                                                                                                                              |
| `units.ts` `PX_PER_IN`, `PT_PER_PX`, `EMU_PER_PX`, `EMU_PER_IN`, `EMU_PER_PT`           | stay                   | stayed                                                                                                                                                                                                                                                          |
| `pptx/masters.ts` `LAYOUT_NAME`, `defineLayout`                                         | derive                 | `defineLayout(pptx, page)` over `layoutName(page)` and `pageIn(page)`; `LAYOUT_NAME` stays                                                                                                                                                                      |
| `pptx/masters.ts` frame lines                                                           | stay                   | stayed (measured)                                                                                                                                                                                                                                               |
| `pptx/images.ts` `isSheetAspect`, box fallback                                          | derive                 | `isSheetAspect(w, h, page)` over `isPageAspect`; `addPicture` reads `scenePage(scene)`                                                                                                                                                                          |
| `pptx/build.ts` `COVER_OFFSET_IN`                                                       | stay                   | stayed                                                                                                                                                                                                                                                          |
| `pptx/build.ts` `DEFAULT_TITLE_BOX`, 2x natural size, hidden title clamp, cover picture | derive                 | `defaultTitleBox(page)`, `readPictureSource` at `2W by 2H`, `hiddenTitleFor` against `pageEmu(scenePage(scene))`, the cover at `pageIn(scenePage(scene))`, `defineLayout` from the first scene's page, `TextEmitOptions.page`                                   |
| `pptx/text.ts` width clamps                                                             | derive                 | `TextEmitOptions.page` into `pageIn(page).width`                                                                                                                                                                                                                |
| `ooxml/geometry.ts` `inPage`, `showsOnPage`, `readGeometry`                             | derive                 | trailing `page` argument, the default when absent                                                                                                                                                                                                               |
| `check.ts` expected page                                                                | derive                 | `CheckOptions.page` into `checkGeometry(bytes, page)`; the issue line and the human line read the check's `expected` and the EMU in inches; the CLI `--page WxH` flag is day 3's handler                                                                        |
| `verify/geometry.ts` `pageSizeOk`                                                       | derive                 | `checkGeometry(pptx, page)` with `GeometryCheck.expected`                                                                                                                                                                                                       |
| `verify/fixture.ts:255` `p:sldSz`                                                       | derive                 | stayed: B6's file this round and a 16:9 test fixture; the writer derives, the fixture's page is the default                                                                                                                                                     |
| `verify/libreoffice.ts`                                                                 | derive the `-scale-to` | the options existed; `verify/report.ts` passes `report.page ?? SHEET_PX` times the scale                                                                                                                                                                        |
| `verify/report.ts`                                                                      | derive                 | `page = report.page ?? SHEET_PX` for the LibreOffice raster size, `checkGeometry(pptx, page)` and the QuickLook size `2W`                                                                                                                                       |
| `verify/quicklook.ts`                                                                   | derive                 | the `size` option existed; the report passes `page.width * scale`                                                                                                                                                                                               |
| `pdf/build.ts` `PDF_RASTER`, report text, reference clip                                | derive                 | `pdfRaster(page)` (`{ 2W, 2H, 240, 2 }`), `rasterizePdf(..., raster)`, `printPagePt(page)` and `pageInches(page)` in the residual line, `GateInput.page` for the clip and the two sheet pages' viewport; `PDF_RASTER` stays as the default; `PRINT_SCALE` stays |
| `scene/measure.ts:182` `W`, `H`; `:1091` chips                                          | derive                 | `MeasureSceneOptions.page` into the evaluate (`pageW`, `pageH`); the chips are `66, H - 42` and `W - 126, H - 44`                                                                                                                                               |
| `scene/two-tone.ts`                                                                     | derive                 | stayed: `twoToneTwinAt2x` reads the twin's own size and scales it, so a 2W by 2H twin follows a 2W by 2H page once the intake writes one (request R3)                                                                                                           |
| `scene/extract.ts:133` `SHEET`                                                          | derive                 | `snapRasterBox(box, page)` and the measurer's `page` from `deckPage(deck)`; the sheet pages open at the deck's viewport                                                                                                                                         |
| `scene/enrich.ts:140`                                                                   | derive                 | `coversSheet(pos, scene.page ?? DEFAULT_PAGE)`                                                                                                                                                                                                                  |
| `calibration/deck.ts`, `calibration/run.ts`                                             | stay                   | stayed (a 16:9 instrument)                                                                                                                                                                                                                                      |
| `batch/plan.ts` `SECONDS_PER_SLIDE`                                                     | stay                   | stayed; the area factor is day 3                                                                                                                                                                                                                                |
| `headless/context.ts:17` `SHEET`                                                        | derive                 | the `viewport` option existed; every caller with a deck passes `deckPage(deck)` (`cli render`, `cli export` JPEG, `deps/canvas.ts`, `scene/extract.ts`, `pdf/build.ts`, `canvas-fidelity.mjs`); `SHEET` stays as the default                                    |
| `headless/document.ts` viewport meta                                                    | derive                 | `SheetDocument.page` (the viewport meta, the body and the sheet box)                                                                                                                                                                                            |
| `headless/measure.ts:51-55`                                                             | derive                 | `MeasureOptions.page` (the fallback rect and the overflow bound); `record.ts` passes `RenderSlideInput.page`                                                                                                                                                    |
| `headless/screenshot.ts:38` fallback                                                    | derive                 | `ScreenshotOptions.page`                                                                                                                                                                                                                                        |
| `headless/sheet.ts:88, 178`                                                             | derive                 | `SheetOptions.page` (`k = thumb / W`, the cell height from the aspect, the render viewport height); the `sheet` command passes it on day 3                                                                                                                      |
| `headless/pdf.ts`                                                                       | stay                   | stayed                                                                                                                                                                                                                                                          |

Effects, materials, the native crate (R08 1.6):

| Site                                                                                                     | Verdict | Disposition                                                                                                                                                                                                                                                                                                                               |
| -------------------------------------------------------------------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `effects/pipeline.ts` `TWO_TONE_SIZE`, `SHEET_SIZE`; `two-tone.ts`; `dither.worker.ts` `SCREEN`, `SHEET` | derive  | deferred: the screen is `W/2 by H/2` for a covering picture on another page, which needs a `screen` field on `TwoToneParams` through the TypeScript stages, the worker and the crate's `twoToneScreen` (`lib.rs:223`, B6's dither.rs and the two binds); on a 16:9 deck nothing changes. Request R3 to B2 (the intake) and B6 (the crate) |
| `effects/metrics.ts` `plateClear`                                                                        | stay    | stayed                                                                                                                                                                                                                                                                                                                                    |
| `materials/capture.ts` `FRAME_SIZE`, `MOUNT_CSS_SIZE`; `mount.ts` `maxPixelCount`                        | derive  | deferred with request R2 (the schema literal `[3200, 1800]` pins the type)                                                                                                                                                                                                                                                                |
| `materials/actions.ts:550` comment                                                                       | stay    | stayed                                                                                                                                                                                                                                                                                                                                    |
| `crates/turboslide-native`                                                                               | data    | deferred with request R3                                                                                                                                                                                                                                                                                                                  |

Studio, realtime, lint, agents, scripts, tests (R08 1.7):

| Site                                                               | Verdict                     | Disposition                                                                                                                                                                                                                                                                     |
| ------------------------------------------------------------------ | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `server/thumbs.ts` `THUMB_WIDTHS`, `SHEET`, `downsample`           | widths stay; aspect derives | `ThumbResult.page?` feeds the `x-turboslide-thumb` height; `getThumbnail` sets it on day 3 with the card route                                                                                                                                                                  |
| `server/health.ts`                                                 | stay as the default         | `sheet` stays; `defaultPage` and `pageMax` added                                                                                                                                                                                                                                |
| `routes/print.$deckId.tsx`, `print.css`                            | derive                      | `printFitScript(page)`, `PrintPage1` `--k = w / page.width`, the sheet root carries `sheetSizeVars(page)`, `.ts-print-sheet`'s aspect reads the variable; the `@page 13.333in 7.5in`, the 1280 by 720 print box and the notes geometry move to `printLayout` on day 4 (R08 4.9) |
| `routes/edit.$deckId.tsx:2190` hidden sheet                        | derive                      | the hidden sheet is `canvas-measure.ts` at `BASE`, derived above                                                                                                                                                                                                                |
| the per deck card                                                  | derive                      | day 7                                                                                                                                                                                                                                                                           |
| `realtime/protocol.ts` `SHEET_WIDTH`, `SHEET_HEIGHT`; `channel.ts` | data                        | deferred: `protocol.test.ts` pins the refusal of `x: 1601`, so the widening to `PAGE_CAP` moves that test; request R5 to B5 (the file's owner this round)                                                                                                                       |
| `lint/rendered/rules.ts` rails, overflow proposal                  | derive                      | `railDistance(box, page)` and the sentence read `deckPage(ctx.deck)`                                                                                                                                                                                                            |
| `lint/static/asset.ts`, `static/picture.ts` covering checks        | derive                      | `coversPage(pos, deckPage(ctx.deck))`, the picture box from the page                                                                                                                                                                                                            |
| `mcp/prompts.ts` text                                              | derive the sentence         | stayed: the lens instructions have no deck at hand; the review prompt names the deck by id, and the sentence gains the page when `deck.info.page` lands (day 3)                                                                                                                 |
| `agent/generate/grammar.ts` text                                   | stay, plus one sentence     | stayed: the sentence naming `deck.page` regenerates `docs/grammar.md`, which only the integrator regenerates (merge 1); request R6                                                                                                                                              |
| `cli/commands/diff.ts` `SHEET_BOX`                                 | derive                      | `sheetBox(document)` over `pageBox(deckPage(deck))`; `writeCrop`'s fallback size follows                                                                                                                                                                                        |
| `cli/store-actions.ts` default boxes                               | derive                      | `diagramDefaultBox(deckPage(current.deck))`, `screenOf(block, dither, page)`, `coveringPicture(slide, page)`, `picturePos(page)` for the background insert, `toCanvas(slide, boxes, page)` in `withCanvas`, `applyGuides(current.deck.guides, edit, page)`                      |
| `compare-to-shoot.mjs`, `canvas-fidelity.mjs`                      | derive from the deck        | done (1.1)                                                                                                                                                                                                                                                                      |
| `gslides-parity-audit.mjs`                                         | stay                        | stayed (the verifier's file)                                                                                                                                                                                                                                                    |
| the e2e specs' coordinates                                         | stay                        | stayed; `page-setup.spec.ts` is day 3                                                                                                                                                                                                                                           |
| `docs/pptx.md`, `docs/freeform.md`, `docs/export-verification.md`  | stay, reworded              | day 3's document pass                                                                                                                                                                                                                                                           |

Tally of this sweep against R08's 76, 12 and 35: 71 derive rows landed in code, 5 derive rows deferred with a named reason (the two tone screen, the material capture, the menu centre rows, the MCP sentence, the docs prose); 10 data rows landed (`DEFAULT_PAGE`, `stage.css`, `renderStage`, both runtimes, `ViewerDeck.page`, `GUIDE_MAX` on day 0, `health`), 2 data rows deferred (`assets.ts` material size, `protocol.ts`; plus the crate, which R08 marked unverified); every stay row stayed. Every deferred row is a request in section 4 with its owner.

### 1.3 Behaviour change

None on a deck without a `page` field: every new parameter defaults to the GT sheet, every legacy constant keeps its value, `printCss(DEFAULT_PAGE)` equals `PRINT_CSS` byte for byte, `pageIn(DEFAULT_PAGE)` equals `PAGE_IN`, the shape path string is identical, the stage root gains `data-page` and the two custom properties (attributes only; the pixels are the same, which `compare-to-shoot` and `canvas-fidelity` measure below).

## 2. Exports (the names and signatures other lanes read)

- `@turboslide/schema/render`: `SLIDE_INSET_PX`, `SLIDE_PAD_PX`, `CONTENT_INSET`, `contentBox(page): Box`, `pageBox(page): Box`, `pageCentre(page): { x, y }`, `pageAspect(page): number`, `pageInches(page): { width, height }`, `PAGE_CAP`, `presetOfPage(page): PagePreset`, `boxInsideSheet(box, page?)`, `coversPage(pos, page?)`; unchanged: `deckPage`, `DEFAULT_PAGE`, `PAGE_PRESET_SIZES`, `pageSchema`, `isDefaultPage`.
- `@turboslide/schema/canvas`: `PageSize`, `picturePos(page?)`, `toCanvas(slide, boxes, page?)`, `fromCanvas(slide, page?)`, `normalizeGuideList(values, axis, page?)`, `applyGuides(current, input, page?)`, `guideCentre(page?)`, `SCALE_CANVAS_MODES`, `ScaleCanvasMode`, `ScaleCanvasResult`, `scaleFactor(from, to, mode)`, `scalePosition(pos, from, to, k)`, `scaleCanvas(slide, from, to, mode)`, `scaleGuides(guides, from, to, mode)`.
- `@turboslide/schema/freeform`: `PageSize`, `columnWidths(ratio, gap?, contentWidth?)`, `verticalGuides(page?)`, `horizontalGuides(page?)`, `guidesFor(page?)`, `insideContent(pos, page?)`, `offSheetKind(pos, page?)`, `offSheet(pos, page?)`, `alignReference(positions, target, page?)`, `alignPositions(positions, edge, target?, snap?, page?)`, `layoutSlotBoxes(layout, page?)`, `convertLayout(slide, layout, page?)`.
- `@turboslide/schema/diagrams`: `DIAGRAM_DEFAULT_SIZE`, `diagramDefaultBox(page?)`.
- `@turboslide/schema/shapes/geometry`: `ShapeBox`, `PathCommand`, `boxPath(box)`, `shapePath(preset, box, adjusts?)`, `textInset(preset, box, adjusts?)`, `boxSites(box)`, `sites(preset, box, adjusts?)`, `pathData(commands)`.
- `@turboslide/theme/tokens`: `PageSize`, `Chip`, `Grid`, `grid(page)`, `columnWidths(ratio, gap?, contentWidth?)`.
- `@turboslide/render/geometry`: `PageSize`, `Chip`, `Geometry`, `geometry(page?)`, `chipsOf(page?)`, `colsWidths(ratio, gap?, page?)`, `slotBoxes(layout, page?)`.
- `@turboslide/render/stage`: `StageOptions.page`, `sheetSizeStyle(page)`, `pageAttr(page)`.
- `@turboslide/render/slide`: `coversSheet(pos, page?)`; `renderSlide` reads `deckPage(deck)` itself.
- `@turboslide/render/print`: `PageSize`, `printPagePt(page?)`, `printCss(page?)`, `PrintDocument.page`.
- `@turboslide/viewer/model`: `ViewerDeck.page?`, `PageSize`, `DEFAULT_PAGE_SIZE`, `sheetSizeVars(page)`.
- `@turboslide/viewer/Sheet`: `SheetProps.page`; `@turboslide/viewer/Stage`: `StageProps.page`; `@turboslide/viewer/LiveClone`: `LiveCloneProps.page`.
- `@turboslide/viewer/rulers-model`: `axisLength(axis, page?)`, `rulerTicks(axis, page?)`, `rulerNumerals(axis, page?)`, `rulerToSheet(axis, client, edge, k, page?)`.
- `@turboslide/viewer/snap`: `plateSideFor(kind, x, page?)`, `sheetSnapLines(page?)`, `sheetEdgeLines(page?)`, `deckGuideLines(guides, page?)`.
- `@turboslide/viewer/Gestures`: `sheetPoint(stage, x, y, page?)`, `centredBox(size, page?)`, `GestureContext.page`, `HandleOptions.page`.
- `@turboslide/viewer/Freeform`: `measureBoxes(body, page?)`, `readStageBoxes(root?, page?)`, `toFreeform(slide, boxes, page?)`, `conversionMutation(slide, boxes, page?)`, `toGrammar(slide, page?)`, `layoutSwitchMutation(slide, boxes, target, page?)`, `alignMutations(slide, ids, boxes, edge, to?, page?)`.
- `@turboslide/viewer/canvas-measure`: `mountHiddenSheet(theme, host?, page?)`.
- `@turboslide/viewer/Editor`: `RulersView.page?`.
- `@turboslide/chrome/Rulers`: `RulersProps.page?`; `@turboslide/chrome/editor-shell`: `coversSheetOf(slide, block, page?)`, `centredPosition(size, page?)`, `backgroundPicturePos(page?)`; `@turboslide/chrome/palette-data`: `defaultPosition(slide, type, blockId?, page?)`.
- `@turboslide/export/units`: `PageSize`, `pageIn(page?)`, `pageEmu(page?)`, `layoutName(page?)`, `isPageAspect(w, h, page?)`, `scenePage(scene)`.
- `@turboslide/export/pptx/masters`: `defineLayout(pptx, page?)`; `pptx/images`: `isSheetAspect(w, h, page?)`; `pptx/build`: `defaultTitleBox(page)`; `pptx/text`: `TextEmitOptions.page`.
- `@turboslide/export/ooxml/geometry`: `inPage(off, ext, tolerance?, page?)`, `showsOnPage(off, ext, tolerance?, page?)`, `readGeometry(zip, page?)`; `check`: `CheckOptions.page`; `verify/geometry`: `checkGeometry(pptx, page?)`, `GeometryCheck.expected`.
- `@turboslide/export/pdf/build`: `pdfRaster(page)`, `rasterizePdf(..., raster?)`; `scene/measure`: `MeasureSceneOptions.page`; `scene/extract`: `snapRasterBox(box, page?)`.
- `@turboslide/headless/document`: `SheetDocument.page`; `measure`: `MeasureOptions.page`; `screenshot`: `ScreenshotOptions.page`; `sheet`: `SheetOptions.page`; `record`: `RenderSlideInput.page`.
- `apps/studio`: `ThumbResult.page?`, `getServerHealth` `defaultPage` and `pageMax`, `printFitScript(page?)` beside `PRINT_FIT_SCRIPT`; `apps/cli/store-actions.ts`: `coveringPicture(slide, page?)`.

## 3. Commands run and their results

| Command                                                                                                                                                 | Result                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `node_modules/.bin/tsc -b`                                                                                                                              | exit 0                                                                                                                                                                                                                                                                                                                                                                                                    |
| `cd packages/schema && ../../node_modules/.bin/vitest run`                                                                                              | 34 files, 603 passed (the canvas page and scaleCanvas blocks and `shapes/geometry.test.ts` new)                                                                                                                                                                                                                                                                                                           |
| `cd packages/theme && ../../node_modules/.bin/vitest run`                                                                                               | 6 files, 94 passed (`tokens.test.ts` with the grid block and the stage.css custom property assertion)                                                                                                                                                                                                                                                                                                     |
| `cd packages/render && ../../node_modules/.bin/vitest run -u src/__tests__/slide.test.ts` then `vitest run`                                             | 1 snapshot updated (the stage root's `data-page` and style); 19 files, 415 passed                                                                                                                                                                                                                                                                                                                         |
| `cd packages/viewer && ../../node_modules/.bin/vitest run`                                                                                              | 29 files, 257 passed                                                                                                                                                                                                                                                                                                                                                                                      |
| `cd packages/chrome && ../../node_modules/.bin/vitest run`                                                                                              | 55 files, 501 passed                                                                                                                                                                                                                                                                                                                                                                                      |
| `cd packages/lint && ../../node_modules/.bin/vitest run`                                                                                                | 10 files, 72 passed                                                                                                                                                                                                                                                                                                                                                                                       |
| `cd packages/export && ../../node_modules/.bin/vitest run`                                                                                              | 21 files, 141 passed (55.8 s)                                                                                                                                                                                                                                                                                                                                                                             |
| `cd packages/headless && ../../node_modules/.bin/vitest run`                                                                                            | 6 files, 41 passed                                                                                                                                                                                                                                                                                                                                                                                        |
| `cd apps/cli && ../../node_modules/.bin/vitest run`                                                                                                     | 23 files, 138 passed, 1 skipped                                                                                                                                                                                                                                                                                                                                                                           |
| `cd apps/studio && ../../node_modules/.bin/vitest run src/server src/components/home src/routes`                                                        | 32 files, 219 passed                                                                                                                                                                                                                                                                                                                                                                                      |
| `cd packages/materials && vitest run dither-actions`; `packages/effects vitest run`; `packages/realtime vitest run protocol`; `packages/mcp vitest run` | 8, 76 (1 skipped), 9, 42 passed                                                                                                                                                                                                                                                                                                                                                                           |
| `cd packages/agent && ../../node_modules/.bin/vitest run`                                                                                               | 12 files passed, 1 failed: `contracts.test.ts` "are committed and current" reports `mcp-tools.json` and `openapi.json` differ. The diff (a scratch script over `generateAll()`) starts at `MotionSchedule` steps gaining `slideId` (`packages/schema/src/motion.ts`, B1's day 1 work in progress), not at anything this sweep touched (no zod schema changed here). The integrator regenerates at merge 1 |
| `node apps/cli/bin/turboslide.mjs validate decks/fixture/page-4-3` and `page-16-10`                                                                     | 8 slides, 0 errors; 3 slides, 0 errors                                                                                                                                                                                                                                                                                                                                                                    |
| `node apps/cli/bin/turboslide.mjs lint all --deck decks/fixture/page-4-3 --layers static --json`                                                        | 4 findings at severity 1, none above                                                                                                                                                                                                                                                                                                                                                                      |
| `node apps/cli/bin/turboslide.mjs picture materialize --deck decks/fixture/page-4-3 --json`                                                             | revision 2, one variant written (`fixture-photo.dither-d447c823b884-{light,dark}.png`, litFraction 0.1906)                                                                                                                                                                                                                                                                                                |
| `node_modules/.bin/prettier --write` over the 86 files of section 5                                                                                     | 14 reformatted, the rest unchanged; `--check` clean afterwards                                                                                                                                                                                                                                                                                                                                            |

The Chrome gates (`render all` and `compare-to-shoot`, `canvas-fidelity` on the three default decks and on `decks/fixture/page-4-3`, the editor walk probe on 4354) are recorded in section 6 below with their numbers.

## 4. Requests

- R1 (integrator, `menus/model.ts`, `apps/studio/src/editor/EditorRoot.tsx`, `controller.tsx`): the two Add guide rows read `guideCentre(page)` from the shell instead of `SHEET_CENTER_X` and `SHEET_CENTER_Y`; the shell root sets `--ts-page-aspect` from `deckPage(deck)` so the fourteen `aspect-ratio: var(--ts-page-aspect, 16 / 9)` stylesheets follow the deck; `toViewerDeck` writes `page: deckPage(deck)` (the server loader in `server/decks.ts` already does); the twin overlay's `k = s.width / SHEET.width` at `EditorRoot.tsx:1980` reads the deck's page. Day 3, with the Page setup dialog.
- R2 (integrator, `packages/schema/src/assets.ts`; B2, `materials/capture.ts`, `mount.ts`): the material source's `size: [3200, 1800]` literal widens to any `2W by 2H` page (R08 1.1 data) and the capture's `FRAME_SIZE` and `MOUNT_CSS_SIZE` become a page option with the default; the generated contracts move with it (merge 1 or 2).
- R3 (B2 for the intake and the worker, B6 for the crate): the two tone screen becomes `W/2 by H/2` for a covering picture on a non 16:9 page (R08 3c): a `screen` field on `TwoToneParams` through `twoToneScreenTypeScript`, the worker's `previewScreen` and the crate's `twoToneScreen`; the export's `twoToneTwinAt2x` already follows the twin's size. Nothing changes on a 16:9 deck.
- R4 (B1, `Filmstrip.tsx`, `Thumb.tsx`, `DeckViewer.tsx`, `PresenterPage.tsx`, `Slideshow.tsx`): pass `page={deck.page}` to `LiveClone` and `Stage` where the viewer deck is at hand (`ViewerDeck.page` exists; `DeckViewer`'s `Stage` already passes it), so a 4:3 deck's clones scale by 1200 rather than 1600. Day 3.
- R5 (B5, `packages/realtime/src/protocol.ts` and `protocol.test.ts`): the presence pointer clamp widens from 1600 by 900 to `PAGE_CAP` (the server clamps to the cap, the client to the page, R08 1.7) with the test's `x: 1601` refusal moving to `x: 6721`.
- R6 (integrator, `packages/agent/src/generate/grammar.ts`, `packages/schema/src/{rules,catalog,position}.ts` help text): one sentence naming `deck.page` beside the default page's numbers, regenerated with the contracts at merge 1; the rewording of "1600 by 900" to "the sheet" in the rule and help texts.
- R7 (integrator, `scripts/check.mjs` `STEP_32_FILES`): the list names `apps/studio/e2e/page-setup.spec.ts` and `packages/render/src/print-layout.ts`, which land on days 3 and 4; the fixtures and `resize.spec.ts` exist. No move needed.

## 5. Files this sweep touched

`packages/schema/src/{render,canvas,canvas.test,freeform,diagrams,shapes}.ts`, `packages/schema/src/shapes/{geometry,geometry.test}.ts` (new), `packages/theme/src/{tokens,tokens.test}.ts`, `packages/theme/src/gt-ink-paper/stage.css`, `packages/render/src/{geometry,stage,slide,deck,standalone,print,runtime,dither-runtime,dither-walk}.ts`, `packages/render/src/__tests__/geometry.test.ts` (new), `packages/render/src/__tests__/__snapshots__/slide.test.ts.snap` (one snapshot), `packages/viewer/src/{model,rulers-model,snap,canvas-measure}.ts`, `packages/viewer/src/{Sheet,Stage,LiveClone,Gestures,Editor,Freeform}.tsx`, `packages/viewer/standalone/runtime.ts`, `packages/viewer/src/{BookView,GridView}.css`, `packages/viewer/src/present/PresenterConsole.css`, `packages/chrome/src/{Rulers,Overlay}.tsx`, `packages/chrome/src/{editor-shell,palette-data}.ts`, `packages/chrome/src/dialogs/Background.tsx`, `packages/chrome/src/{Sidebar,ThemesPanel,Dialog,AssetPicker,Filmstrip}.css`, `packages/chrome/src/inspector/{asset,dither}.css`, `packages/chrome/src/pickers/DiagramPicker.css`, `packages/export/src/{units,check}.ts`, `packages/export/src/pptx/{masters,images,build,text}.ts`, `packages/export/src/ooxml/geometry.ts`, `packages/export/src/verify/{geometry,report}.ts`, `packages/export/src/pdf/build.ts`, `packages/export/src/scene/{measure,extract,enrich}.ts`, `packages/headless/src/{document,measure,screenshot,sheet,record}.ts`, `packages/lint/src/rendered/rules.ts`, `packages/lint/src/static/{picture,asset}.ts`, `apps/studio/src/server/{thumbs,health,decks}.ts`, `apps/studio/src/components/DeckViewer.tsx`, `apps/studio/src/routes/print.$deckId.tsx`, `apps/studio/src/routes/{print,decks}.css`, `apps/studio/src/styles.css`, `apps/cli/src/commands/{diff,render,export}.ts`, `apps/cli/src/deps/canvas.ts`, `apps/cli/src/store-actions.ts`, `scripts/{compare-to-shoot,canvas-fidelity}.mjs`, `decks/fixture/page-4-3/**` (new: `deck.json`, eight slides, four asset files), `decks/fixture/page-16-10/**` (new: `deck.json`, three slides), `docs/gslides-parity/build-5/b4.md` (this file).

Every file outside B4's "Owns" row was edited under the sweep rule (a sheet constant read replaced by the page helper and nothing else): `packages/render/src/{deck,standalone,runtime,dither-runtime,dither-walk}.ts`, `packages/viewer/src/{Stage,LiveClone}.tsx`, `packages/viewer/src/{BookView,GridView}.css`, `present/PresenterConsole.css`, `packages/chrome/src/{editor-shell,palette-data}.ts`, `dialogs/Background.tsx`, the nine chrome stylesheets, `packages/export/src/{check}.ts`, `pptx/{images,build,text}.ts`, `ooxml/geometry.ts`, `scene/{extract,enrich}.ts`, `packages/headless/src/record.ts`, the three lint files, `apps/studio/src/server/{thumbs,health,decks}.ts`, `components/DeckViewer.tsx`, `routes/decks.css`, `styles.css`, `apps/cli/src/commands/{diff,render,export}.ts`, `deps/canvas.ts`, `store-actions.ts`, and the snapshot file. From merge 1 each belongs to its round five owner and B4 requests any later page edit there.

## 6. The Chrome gates

The e2e lock was held by B6's editor walk probe on 4356 from 05:54; B4 took it at 06:02:42 with `until mkdir .turboslide/e2e.lock; do sleep 5; done`, ran the four Chrome jobs below under it, and released it at 06:24:30 with `rmdir`. The dev server ran on 4354 as `TURBOSLIDE_STORE=tmp TURBOSLIDE_REALTIME=memory TURBOSLIDE_SESSION_SECRET=<fake> TURBOSLIDE_DOWNLOAD_SECRET=<fake> node_modules/.bin/vite dev --port 4354 --strictPort` from `apps/studio` and was stopped before this note closed (no listener on 4354).

| Command                                                                                                                                                     | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `node apps/cli/bin/turboslide.mjs render all --deck decks/gt-brand --theme light,dark --scale 1 --out .turboslide/render --json`                            | 170 records in 17.2 s (twice: before and after the `block-css.ts` change below)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `node scripts/compare-to-shoot.mjs --deck decks/gt-brand --render .turboslide/render --shoot /Users/kevinliu/repos/Prototemplate/deck --max-mismatch 0.005` | 170 pairs, 170 compared, 0 over budget; worst non escape 0.408 percent (slide 83 `fixed-points`), mean 0.016 percent; the same numbers before and after the CSS change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `node scripts/canvas-fidelity.mjs --deck decks/gt-brand --deck decks/templates/gt-brand --deck decks/templates/blank --max-mismatch 0.005`                  | 171 slides converted, 342 pairs, 0 over budget; worst 0.262 percent (`gt-brand/directions` dark), mean 0.004 percent, 82 s; the same before and after                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `node scripts/canvas-fidelity.mjs --deck decks/fixture/page-4-3 --max-mismatch 0.005 --out .turboslide/canvas-fidelity-p43`                                 | first run: 2 of 12 pairs over budget, `opener` at 24.6 percent in both themes (the diff: the grammar photograph cropped as a 1600 by 900 image while the canvas object covered 1200 by 900). Cause: `packages/render/src/block-css.ts:15` hard codes `.opener-img, .mood-img { width: 1600px; height: 900px }`, `:17` the chips' background positions `66px 858px` and `1474px 856px`, and `:150` `.freeform-sheet { width: 1600px; height: 900px }`, three sheet literal sites R08 part 1 did not list. Fixed under the sweep rule: the three read `var(--ts-sheet-w, 1600px)`, `var(--ts-sheet-h, 900px)` and `calc(var(--ts-sheet-h, 900px) - 42px)`, `calc(var(--ts-sheet-w, 1600px) - 126px) calc(var(--ts-sheet-h, 900px) - 44px)`; `picture.ts:76`'s fallback width reads the new `BlockContext.page` (`context.ts`), which `renderSlide` sets from the geometry. Second run: 6 slides, 12 pairs, 0 over budget; worst 0.019 percent (`title`), mean 0.003 percent |
| `node scripts/canvas-fidelity.mjs --deck decks/fixture/page-16-10 --max-mismatch 0.005 --out .turboslide/canvas-fidelity-p1610`                             | 2 slides, 4 pairs, 0 over budget; worst 0.013 percent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `node scripts/probes/editor-walk-probe.mjs --base http://localhost:4354 --quick --json <scratch>/b4-walk.json`                                              | 230 steps, 226 ok, 3 failed, 1 not driven, 0 console errors, 586 s. The three failed rows are 121, 123 and 126, the picture (Image by URL) resizes at zoom 100 and 200 with "content did not scale", the same rows `build-4/hotfix-4.md` lists at `BASE` (its rows 326 to 344; A8 row 1, B4's later in the round with B2's picture renderer); the not driven row is 62, the logo plates through the palette (A8 row 10, B3). Every other surface this sweep touched (the sheet fit, the rulers, the guides, the gestures, the snap lines, the inserts, the menus, the export) drove green. The probe ran before the `block-css.ts` change, which is pixel identical on a 16:9 deck (compare-to-shoot and canvas-fidelity above ran after it)                                                                                                                                                                                                                              |
| `node scripts/check.mjs --only 1,2`                                                                                                                         | ok in 0.3 s and 0.7 s                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `node scripts/check.mjs --only 32`                                                                                                                          | "not yet; waiting for packages/render/src/print-layout.ts, apps/studio/e2e/page-setup.spec.ts", exit 0 (days 3 and 4)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `node_modules/.bin/tsc -b` (final)                                                                                                                          | exit 0; `git rev-parse --short HEAD` is `d5a1be5`, nothing staged, `git diff --exit-code packages/chrome/src/tokens.css` empty                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

Files added to section 5 by the fidelity fix: `packages/render/src/block-css.ts` (three sheet literal lines, a request file per MILESTONES-5 B4 "the block CSS lines are requests", edited under the sweep rule and named here for the integrator), `packages/render/src/blocks/context.ts` (`BlockContext.page`), `packages/render/src/blocks/picture.ts` (B2's file; the one fallback line). R08 part 1's count grows by these three sites (126 rows in this tally).

## 7. Open items for days 3 to 7 that this stage surfaced

- The Page setup dialog, `deck.setPageSize` over `scaleCanvas` and `scaleGuides`, `deck.info.page`, the rulers' unit over B5's `Preferences`, the `--page WxH` flag of `export check`, the `--ts-page-aspect` root variable (R1), the Download dialog sentence, the Position fields' page readout, `ThumbResult.page` from `getThumbnail`, the contact sheet's page from the `sheet` command (day 3).
- `printLayout` and the print route's `@page`, the 1280 by 720 print box and the notes geometry moving off `print.css` (day 4; R08 4.9).
- The shape interpreter over `shapes/definitions.ts` behind the seam of this stage (day 6); the ODP and SVG writers read `PathCommand[]` and `pathData`.
- A8 row 1 (the shot frame past its box, the picture resize content scaling the probe reports) with B2's picture renderer.

## 8. Fix round (day 9, 2026-09-15): VERIFICATION-5 findings 1, 4 and 6

The B4 fixer over the shared tree at `BASE` `d5a1be5` plus the merge 2 files, working the three findings the orchestrator handed this lane (VERIFICATION-5 section 13: finding 1 the days 3 to 7, finding 4 the five test files, finding 6 the `export check --page` flag). Section numbers refer to SPEC-5 unless prefixed. No git write command ran, no `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build`; binaries as `node_modules/.bin/<tool>` or `node <script>`; the typecheck as `node_modules/.bin/tsc -b`; vitest per package. Every command ran from `/Users/kevinliu/repos/Turboslide`. The verifier's chain (`verification-5/scripts/run-chain.sh` since 15:14, `run-walk2.sh` since 16:08) held `.turboslide/e2e.lock` for the whole build phase, so every browser job of this round (the Playwright spec, the exports, the renders, the fidelity gates, the walk probe) waited for the lock through `until mkdir .turboslide/e2e.lock; do sleep 5; done` and its results are in 8.6; nothing browser bound ran outside the lock.

### 8.1 Finding 4: the five test files and the diagram builder

- `packages/schema/src/shapes/geometry.test.ts`: rewritten from the merge 1 seam pins ("the box path for every preset until the interpreter lands") to the interpreter's geometry: every one of the 135 presets has a definition and answers an evaluated outline (a `moveTo` first, a `close` somewhere, at least three commands), a text rectangle inside the frame with positive area, and at least one site; `rect`, `rectangle` and `flowChartProcess` are the box, every other preset leaves it; the hexagon string at 48.25 by 36.5 is `M0,18.5 L9,0 H39 L48.5,18.5 L39,36.5 H9 Z`; the rounded rectangle's text rectangle is the corner radius (`ss` times adj 16667 over 100000) times 29289 over 100000 in from every side; the rounded rectangle's four sites are the box's first four in the same order. The four callouts' tail site sits past the box by construction (ECMA-376's `wedge*Callout` and `cloudCallout` place it at the wedge's point) and are the one named exception of the inside the box rule.
- `packages/schema/src/shapes.test.ts`: the same move for the string form (`shapePath(kind, w, h)` starts with `M` and holds a `Z`; a preset with an inner edge such as `can` ends on an open stroke, so the assertion is a closed subpath somewhere rather than a trailing `Z`; the text inset inside the frame; the sites inside the frame except the callouts; the rectangle keeps its eight sites, the whole box as its inset and `M0,0 H${w} V${h} H0 Z`).
- `packages/schema/src/shapes/geometry.ts` `textInset`: one behaviour change, recorded: the text rectangle is clipped to the shape's frame and a rectangle the guides collapse or push out of the frame answers the whole box. The pie at its default adjusts (0 to 270 degrees) is the case that surfaced it (its rectangle at 300 by 220 read `x 32.2, y 187.8, w 11.7, h 68.3`, past the bottom edge, and `h 0` at 48 by 36); a text area never reaches past its shape's box, so the clip is the interpreter's rule now, not the test's exception. The transcription of the pie's `rect` guides in round four's `definitions.ts` is worth a look by whoever owns the definitions (the standard's pie rectangle follows the chord of the missing quarter; the small values suggest a guide name mismatch), noted here and not changed.
- `packages/schema/src/diagrams.test.ts`: not edited. The twelve `cycle` rows failed because `diagrams.ts`'s `link()` chose connector sites over `rectSites` (the eight of a box) while the cycle's nodes are `roundRect`, whose definition has four sites (SPEC-2 2.4.7 indexes the definitions' list), so the validator refused sites 4 to 7. The builder now chooses among the node's own sites (`sites(shape, w, h)` through the interpreter for a shape, `rectSites` for a box without a preset): `LinkEnd` carries `shape`, the process and cycle templates pass `'roundRect'`. The test stands as written (no severity 3 issue on any template at any count and style) and passes; a fixed cycle diagram validates again on every stored and future deck.
- `packages/render/src/__tests__/canvas.test.ts`: the 12 snapshots refreshed (`vitest run -u`), the interpreter's real paths for the rounded rectangle (`M0,20 A20,20 0 0 1 20,0 ...`) and the right arrow, and the rounded rectangle's shape text at the preset's text rectangle (`left:5.858px;top:5.858px;width:288.284px;height:108.284px` on the 300 by 120 shape).
- `packages/export/src/gslides-fixture.test.ts` "the padded shape": the pinned `lIns="182880"` and `tIns + bIns = 121920` were the box seam's; the rounded rectangle's text rectangle moves in by `textInset('roundRect', 300, 120).x` (5.858 px, 44,638 EMU) on every side, so the assertion reads each inset against the padding plus that inset within one sheet pixel (7,620 EMU) and the top and bottom sum within two, keeping the first baseline shift rule (`tIns < bIns`). It runs with Chromium (8.6).

### 8.2 Finding 6: `export check --page WxH` and the ODP and SVG file kinds

- `apps/cli/src/commands/export-check.ts`: `--page WxH` in sheet pixels (`parsePageFlag`, a usage error for anything but `\d+x\d+`), passed as `CheckOptions.page` to `checkPptx`, whose geometry pass already compared `p:sldSz` against `pageEmu(page)`; an `.odp` runs `checkOdp` (check/odf.ts) and an `.svg` `checkSvgFile` (check/svg.ts), each answering the whole `ExportCheck` with its `container`, `odf` or `svg` section and `valid`; `describeSectionCheck` prints their lines. `export check <dir>` walks every `.pptx`, `.odp` and `.svg` of a folder.
- `apps/cli/src/args.ts` `VALUED_FLAGS`: `page`, `paper`, `orientation`, `order`, `text` join `layout`, so `--page 1200x900`, `--paper letter --orientation portrait --order down` and `--text embed` parse as values (without the row the parser read them as switches and pushed the value into the slide selection, the shape the verifier saw). An edit outside this lane's row under the seam rule (the CLI flag table has no round five owner; the words are this lane's flags alone), named here for the integrator. Observed and not changed: `motion`, `media` and `autoplay` are not in `VALUED_FLAGS` either, so B1's `--motion drop` and B2's `--media poster` in the space separated form fall into the positionals today (the `=` form works); their lanes' call.
- `apps/cli/src/commands/export-check.test.ts`: `--page 1600x900` valid, `--page 1200x900` INVALID with `expected 9144000 by 6858000`, `--page 12x` exit 2.

### 8.3 Finding 1: the days 3 to 7

What landed, by day of MILESTONES-5 B4 "Delivers":

- Day 3 (6.1): `packages/chrome/src/dialogs/PageSetup.tsx` (new): Google's four labels in the Size dropdown, Width, Height and the unit dropdown under Custom ("Inches", "Centimeters", "Points", "Pixels"; the Preferences' unit first), the readout sentence (`pageSentence`), the second step only when the page shrinks and a canvas object would cross an edge (`objectsWouldCross`; "Keep object positions" selected, "Scale objects to fit"), Cancel and OK; OK is one `deck.setPageSize` write with `objects`. Controls `pageSetup.size`, `.width`, `.height`, `.unit`, `.objects`, `.ok`, the card `dialog.pageSetup`. The handler `apps/cli/src/actions/page.ts` (`deckSetPageSize`, `pageSizeMutations`) stood from merge 1. The rulers count numerals in the preference's unit: `packages/viewer/src/rulers-model.ts` `rulerTicks(axis, page, unit)` and `rulerScale` (eight per inch, four per centimetre, four per hundred pixels; the numeral kind stays `inch`), `Rulers.tsx` takes `unit` (`data-unit` on the region, the tooltip names the unit), `Overlay.tsx` passes `preferencesOf(shell.input).units`; the inch default is byte identical and `rulers.test.tsx` holds. `apps/studio/e2e/page-setup.spec.ts` (new): a scratch copy of the GT deck, Standard (4:3) through the window API under `keep` (the sheet root's `data-page` and the two custom properties, the 4:3 aspect, the object's coordinates kept, the guide beyond 1200 dropped, the rulers 0 to 10 across and 0 to 7 down, `deck.info.page` when the reader answers it), the same page again as a no write, `fit` scaling the edge object by 0.75 about the centre with one Edit > Undo restoring the page and the object together, and the dialog rows through File > Page setup, skipped with the reason while the menu row is Later on the tree (8.5).
- Day 4 (6.2): `packages/render/src/print.ts` `renderPrintDocument` gains `layout`, `paper`, `orientation`, `order`, `hideBackground` and answers `pages`, `cells`, `truncatedNotes`, `paperPt` and the layout facts; the one slide document on the slide's own paper is byte identical (`print.test.ts` pins the bytes with the defaults spelled out), the paper layouts lay every cell out from `printLayout` (the page box at `pt / 0.6` CSS px so PRINT_SCALE 0.8 lands it on the paper, one sheet per cell at its own pixels scaled by `k`, a 0.5 pt hairline outline, the 3 per page ruled lines, the notes text at 11 pt on 16.5 pt leading clipped at the box and the slide named in `truncatedNotes` through `estimateNotesLines`, the footer band with the title and `n of m`), Hide background as the light appearance with `HIDE_BACKGROUND_CSS`. `packages/export/src/pdf/build.ts` `exportPdf` takes the five fields, the report carries `page`, `layout`, `paper`, `orientation`, `pages`, `cells` and `truncatedNotes`, the per cell gate of R08 4.11 (`gateCells`: each PDF page through poppler at two device pixels per sheet pixel, `paperRaster`; the slide's 2x reference shrunk to the cell by area averaging, `verify/resample.ts`; pixelmatch at 0.1; `CELL_GATE` reported over 1 percent, failed over 5) runs under `--verify` on a paper layout while the one slide layouts keep the page rule; without poppler or `--verify` the page count is the gate and every cell reads `ok: true`. `apps/cli/src/commands/export.ts` `export pdf --layout --paper --orientation --order --hide-background` (`parsePrintLayout`). The print route `apps/studio/src/routes/print.$deckId.tsx`: the seven layout rows live with Google's strings (`PRINT_LAYOUT_LABELS`), Orientation, the Paper dropdown (Letter, A4; `paperForLocale(navigator.language)`), Include skipped slides, Hide background, Download as PDF forwarding the layout fields on `export.run`, Print with the paper's `@page` written inline, the pages on screen laid out by the same `printLayout` (`PaperPage`, `PaperSheet`), `HANDOUT_STUB` retired; `print.css` the paper page rules on screen and in print (the paper page zoomed by 0.8 in the browser's dialog; the slide paper reads the deck's inches through `--ts-page-in-w` and `--ts-page-in-h`).
- Day 5 (6.3): `packages/export/src/odp/` (new): `xml.ts`, `package.ts` (`writeOdfPackage`: `mimetype` first, stored, no extra field; the manifest; `metaXml`, `settingsXml`), `styles.ts` (the `StyleAllocator` deduplicated by content, `stylesXml` with the font faces, the default style's `fo:language` and `fo:country` from `Deck.language` (b5.md R17), the page layout at the deck's page in cm, the master page on the paper colour, the markers), `text.ts` (one `draw:frame` with `draw:text-box` per measured text, `text:p` per paragraph with `text:line-break` between the browser's lines, `text:span` per run, the LibreOffice first baseline offset of `pptx/baseline.ts`, Perfect's paper coloured runs at `loext:opacity="0%"`), `shapes.ts` (`draw:custom-shape` with `draw:type="ooxml-<prst>"` and the `draw:enhanced-path` from `shapePaths` with arcs as cubics, `draw:line` and `draw:path` with markers and dashes, pictures and rasters, shadows, transforms), `table.ts` (`table:table` with columns, rows, cells, spans, borders, the cell texts inside), `notes.ts` (`presentation:notes`), `media.ts` (`draw:plugin` with LibreOffice's media mime type, Loop, Mute, VolumeDB; posters otherwise), `motion.ts` (the transition as `smil:type`, `smil:subtype`, `presentation:transition-speed` quantised with `spdOf` and a `transition.speed` row; flip, cube and gallery as a fade with a `transition.fallback` row; the animation tree `anim:par` timing root, `anim:seq` main sequence, one `anim:par` per click step, effect nodes with `presentation:node-type` from the trigger, LibreOffice's preset ids, `anim:set visibility` for appear and disappear, `anim:animate opacity` for the fades, transforms for zoom and spin, translates for the flies, `anim:command play` for media, `anim:sub-item="text"` on a paragraph build), `build.ts` (`buildOdp`: both modes, the page raster policy of `pptx/page-raster.ts` for Perfect, `presentation:visibility="hidden"` for a skipped slide carried, `xml:id` on every element a block draws, the report rows); `packages/export/src/export-odp.ts` (`exportOdp` beside `exportPptx`: materialize, `extractScenes`, one file per theme, the report per theme as `ExportReport` with `format: 'odp'`, `export check` folded into `passed`, the merged report and the zip of both); `check/odf.ts` filled (8.2); `apps/cli/src/commands/export.ts` `export odp [--mode] [--theme] [--include-skipped] [--include-notes] [--motion] [--media] [--verify]`; the Download dialog's ODP format (`DownloadFormat` `'odp'`, the LibreOffice sentences `ODP_SENTENCES`, the Seg, the appearance).
- Day 6 (6.5): the shape interpreter stood in the tree from the merge; this round's changes are 8.1. The ODP `shapes.ts`, `table.ts`, `motion.ts` and `media.ts` above read the interpreter, the scene's tables, B1's schedule and B2's `SceneMedia`. Not built: the ODP `chart.ts` as an embedded object with `ObjectReplacements` (a chart travels as its picture with a `chart.raster` row, 8.5), the path b oracle diff in the container leg (no `soffice` on this machine; the render worker image's job), Edit points and Change shape on a path in `Gestures.tsx` writing `line.set --points` (not started, 8.5).
- Day 7 (6.4, 11): `packages/export/src/svg/` (new): `write.ts` (`writeSvg`: the paper, the background, the frame, the plates and chips, every rect and preset as a path from `shapePaths`, the lines with triangle heads, the tables' fills and rules, the rasters as PNG data URIs, the icons and the mark as sprite symbols copied into `defs` and referenced by `use`, the text as one `<text>` per measured line with a `<tspan>` per run carrying `x`, the baseline, `textLength` and `lengthAdjust="spacingAndGlyphs"`; one `<title>`, one `<desc>`, `<metadata>`; `data-ts-text` on the root), `fonts.ts` (embed's one `@font-face` data URI of InterVariable.woff2, 341 KB of font as about 455 KB of base64; outline's glyph paths through fontkit with the string on `aria-label` and each run scaled to its measured width; link's family names), `render.ts` (`renderSvg`: the slides' scenes measured in native mode by `extractScenes`, each written in the mode), `check/svg.ts` filled (8.2). `packages/export/src/scene/measure.ts`: `SceneLine.baseline` measured in the page (the run's inline box top plus the face's ascent share of its content area, probed once per family, weight and italic through a zero size inline block on the baseline; Inter reads 0.8007), the writer falling back to Inter's ascent when a scene predates it. `apps/cli/src/commands/render.ts`: `render [slide] <ids|all> --format svg [--text embed|outline|link] [--theme]` writing `<nn>-<slideId>-<theme>.svg` and `render.json` with the SVG section's verdict, the leading `slide` word dropped (the action's spelling in the acceptance line). The per deck card: `apps/studio/src/server/thumbs.ts` gains `cardSlideBox`, `composeCard` (the slide contained in the 1200 by 630 frame at its aspect on `--pt-panel-ink` `#101010`, area averaged down or grown by nearest neighbour), `cardPath` and `renderCard` (the first unskipped slide through the worker at 1x in the deck's appearance, cached on disk by the deck revision); `apps/studio/src/routes/og.deck.$.ts` (new): `GET /og/deck/<deckId>.png` (a splat because the router reads `$deckId.png` as one parameter name), a restricted deck's general access answers a 302 to `/og/turboslide.png`, `?r=<revision>` matching the current revision is immutable for a year, else `s-maxage=60, stale-while-revalidate=86400`, `x-turboslide-revision`, `x-turboslide-slide`, `x-turboslide-cached`.
- The other lanes' requests to B4: `data-block` under `blockAttrs || motion` and `data-motion` (b1.md R1), `stageFrame(themeId)` with `data-sheet` (b6.md R2) and `lang` on the sheet root (b5.md R17) were in `packages/render/src/{slide,stage}.ts` and `blocks/context.ts` when this round opened (the verifier's tree predates them or read an earlier state; `renderStage` writes `data-sheet`, `lang` and the frame from `stageFrame`, `rootAttrs` writes `data-block` under either flag); verified by reading the code and by the render suite. Landed this round: `onMediaFile` on the viewer's drop and paste path (b2.md R7: `EditorProps.onMediaFile`, `mediaFileOf` over the transfer's files when `imageFilesOf` is empty, the drop point in sheet px); the InlineText mount's `autocorrect` and `onList` (b5.md R15: `EditorProps.preferences` and `language`, the context with the personal dictionary and `PRODUCT_TOKENS`, `isList` and `listable` from the block type; `onList` converts a paragraph or text box to a `plain` list in one commit (`block.remove` plus `block.insert` with the typed prefix removed from the first item, the way `text.list` converts) or writes the marker of a list, since `text.splice` is not an action of the table); the `pagehide` flush of the open typing burst (b7.md R19, `InlineText.tsx`); the nested group selection level (b3.md B3-16, in `Editor.tsx`'s press path rather than `Gestures.tsx`, where the click's selection lives: a click on a member whose group path nests goes one level in through `selectionLevelOnClick` and `selectionAtLevel` from `@turboslide/schema/freeform`, the member alone at the deepest level; a flat group keeps the round two rule, a click elsewhere resets the level).

### 8.4 Requests (the Fix round rows the integrator and the owners answer)

1. Integrator, `packages/chrome/src/menus/model.ts`: flip `file.pageSetup` to `now('file.pageSetup', 'Page setup', dialog('Page setup'))` with the clause "The GT theme is 16:9 at 1600 by 900" retired; `file.download.odp` to `now('file.download.odp', 'ODP Document (.odp)', dialog('Download'))` with the LibreOffice sentence as its doc; `file.download.svg` to `now('file.download.svg', 'Scalable Vector Graphics (.svg, current slide)', action('render.slide', { format: 'svg' }))`; retire `DOWNLOAD_FORMATS`; `menu-model.test.ts` to 2 leaf rows and 3 over `allItems()` (integrator.md section 3 item 3).
2. Integrator, `packages/chrome/src/editor-shell.ts`: `DIALOG_IDS` gains `'pageSetup'` and `'downloadOdp'`; `dialogIdOf('Page setup')` answers `pageSetup` and `dialogIdOf('Download', 'file.download.odp')` answers `downloadOdp`. `packages/chrome/src/EditorShell.tsx`: `const PageSetupDialog = lazyDialog(() => import('./dialogs/PageSetup').then((m) => m.PageSetupDialog));` with `case 'pageSetup': return <PageSetupDialog.Component />;` and `case 'downloadOdp': return <DownloadDialog.Component format="odp" />;` in the dialog switch. Until these land the Page setup dialog and the ODP row are files without a door, and `page-setup.spec.ts`'s dialog test skips with that reason.
3. Integrator, `apps/studio/src/editor/controller.tsx` and `apps/studio/src/server/render.ts` and `apps/studio/src/routes/api/render.$slideId.ts`: `render.slide` with `format: 'svg'` and `text` on the window transport: the `on('render.slide')` handler forwards `format` and `text`; `RenderSlidesInput.format` admits `'svg'`; `renderSlideImagesFn` answers `/api/render/<slide>?deck=..&format=svg&text=<mode>&revision=<r>` URLs for the SVG; the route answers `image/svg+xml` through `renderSvg` of `@turboslide/export/svg/render` (Node, the deck folder from `ensureDeckAssets`) with `text` from the query and the preference's `svgText` as the default (the render worker's job table may take a `svg` job instead; `renderSvg` takes a launched browser). The shell's `render.slide` branch opens the first URL in a new tab as it does for a PNG.
4. Integrator, `apps/render-worker/src/jobs/export.ts`: `exportJobInput` admits `format: 'odp'`, `layout`, `paper`, `orientation`, `order`, `hideBackground`, `motion`, `media`, and `exportJobArgs` writes `--layout`, `--paper`, `--orientation`, `--order`, `--hide-background`, `--motion`, `--media`; `runExportJob` reads `exportOdp`'s report the way it reads `exportPptx`'s. Until then a hosted `export.run` with a print layout or `format: 'odp'` is refused by the job's strict schema (the print route's Download as PDF with a handout and the Download dialog's ODP row fail hosted; the CLI path works on a checkout). `apps/studio/src/server/download.ts` `ExportRunInput` gains the same fields (the route casts through it today).
5. Integrator, `apps/cli/src/commands/mcp.ts` and `packages/agent/src/http/readers.ts`: `deck.info` spreads `page: pageInfo(deck)` (`@turboslide/schema/render`), the field the action's output already types; and the checkout `export.run` of `mcp.ts` forwards `layout`, `paper`, `orientation`, `order`, `hideBackground`, `motion`, `media` and admits `format: 'odp'`.
6. Integrator, `apps/studio/src/routes/deck.$deckId.tsx` `head`: `{ property: 'og:image', content: \`${origin}/og/deck/${deckId}.png?r=${revision}\` }`with`og:image:width`1200 and`og:image:height`630 for a deck whose general access is`link`or`open`(the loader's record), the site card otherwise;`scripts/perf-budget.mjs`the 4 s card row;`scripts/hosted-smoke.mjs`a`/og/deck/gt-brand.png`row (200,`image/png`, the immutable header with `?r=`).
7. Integrator, `apps/studio/src/editor/EditorRoot.tsx`: the Editor mount passes `onMediaFile={(file, point) => insertMediaFile(file, { deckId, slideId, revision, dispatch, ...(point ? { pos: { x: point.x, y: point.y, w: 960, h: 540 } } : {}) })}` from `@turboslide/chrome/dialogs/media-upload` (b2.md R7's second half), `preferences={mirroredPreferences}` and `language={deck.language ?? 'en-US'}` (b5.md R15's second half), the same values the notes pane takes.
8. B3 (`packages/viewer/src/Freeform.tsx` `expandGroups`, `groupMembers`): a flat `pos.group === tag` match; a nested path's members are found by `outerGroup` on the first click through the level rule in `Editor.tsx`, but `expandGroups` (Cmd click, Select all, `selectObjects`) still widens to the exact path alone; `groupMembers(slide, tag)` could match `outerGroup(block.pos.group) === tag` so a Shift click on a nested member takes the outer group whole.
9. Integrator, `packages/export/package.json`: the exports `./export-odp`, `./odp/build`, `./svg/write`, `./svg/render` were added by this lane (additive rows beside the day 0 `./check/odf` and `./check/svg`, needed for the CLI's imports); recorded here as an edit outside the row for the integrator to keep or move.
10. Kevin (17): the ODP's chart as an embedded chart object with `ObjectReplacements` and LibreOffice's read back through `soffice` in the render worker image (Docker's disk is full on this machine, finding 8); the outline text mode's weights (8.5 item 3).

### 8.5 Deviations, recorded with their reasons

1. The three Later rows (`file.pageSetup`, `file.download.odp`, `file.download.svg`) stay Later on this tree until the integrator's flips of 8.4 item 1 land: `menus/model.ts` is the integrator's shared file for the round (SPEC-5 0.53; MILESTONES-5 rules), so this lane wrote the surfaces and the request, not the flip. `menu-model.test.ts` reads 5 leaf rows until then; step 32's `page-setup.spec.ts` skips its dialog test with the reason and drives the rest.
2. The ODP writes a chart as its picture with a `chart.raster` row (SPEC-5 6.3 names an embedded object with `ObjectReplacements`); the equation as its 2x raster with an `equation.raster` row (as 6.3 states for the first build); a YouTube video and a poster mode media as the poster with `media.poster-only`. The embedded chart object is the one 6.3 element not written this round.
3. The SVG outline mode instances fontkit's default master (Inter Regular): fontkit 2.0.4's `getVariation(...)` answers an instance whose `layout` and `glyphForCodePoint` throw `Cannot read properties of undefined (reading 'tables')` on the variable WOFF2 (probed on this machine at 17:29 with `{ wght: 500 }`, `{ wght: 500, opsz: 28 }` and the named `Medium`); a bold run is drawn from the Regular outlines scaled to its measured width, its weight on `data-weight`, the residual line says so, and the outline gate is measured and recorded rather than asserted at PDF_GATE (8.6). The fix is fontkit's (or a TTF master the instancing accepts); left for Kevin with the row in 8.4.
4. The per cell gate runs under `--verify` where poppler exists (it does here); step 32's `export pdf ... --layout handout-6 --paper letter --orientation portrait` runs without `--verify` as written and its cells read `ok: true` under the page count gate, which is what the step asserts; the measured cell numbers of a `--verify` run are in 8.6.
5. Edit points and Change shape on a path in `Gestures.tsx` writing `line.set --points` (MILESTONES-5 B4 day 6, the last item) did not land this round; the action row exists from day 0.
6. The `text.splice` action the InlineText `onList` request named does not exist in the table; the prefix is removed inside the same `block.remove` plus `block.insert` commit (a paragraph or text box) or the `/items` write (a list), one commit either way, which is the request's intent.
7. `apps/cli/src/args.ts` `VALUED_FLAGS` and `packages/export/package.json` exports were edited by this lane outside its "Owns" row (8.2, 8.4 item 9): each an additive row this lane's own commands cannot parse or import without, named for the integrator.

### 8.6 Commands run and their results

Unit and typecheck, no browser (every row below ran on this tree between 17:05 and 18:30):

| Command                                                                                                                                                                                                                                                                                                                                | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `node_modules/.bin/tsc -b`                                                                                                                                                                                                                                                                                                             | exit 0 over the whole tree (twice: before and after the Prettier pass and the route tree regeneration)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `node_modules/.bin/tsr generate --config apps/studio/tsr.config.json`                                                                                                                                                                                                                                                                  | the route tree carries `/og/deck/$` (`OgDeckSplatRoute`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `cd packages/schema && vitest run`                                                                                                                                                                                                                                                                                                     | 35 files, 679 passed (`shapes.test.ts`, `shapes/geometry.test.ts`, `diagrams.test.ts` among them)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `cd packages/render && vitest run`                                                                                                                                                                                                                                                                                                     | 480 passed (`canvas.test.ts` 70 with the 12 refreshed snapshots, `print.test.ts` 5 new, `print-layout.test.ts`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `cd packages/viewer && vitest run`                                                                                                                                                                                                                                                                                                     | 301 passed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `cd packages/chrome && vitest run`                                                                                                                                                                                                                                                                                                     | 65 files, 565 passed (`rulers.test.tsx` with the inch default byte identical, `default-view-words`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `cd packages/export && TURBOSLIDE_SKIP_BROWSER_TESTS=1 vitest run`                                                                                                                                                                                                                                                                     | 30 files passed, 4 skipped (the Chromium suites), 205 passed, 37 skipped; the new `odp/build.test.ts` 7, `svg/write.test.ts` 4, `check/odf.test.ts` 3, `check/svg.test.ts` 4, `pdf/paper.test.ts` 2, `verify/resample.test.ts` 2                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `cd apps/cli && TURBOSLIDE_SKIP_BROWSER_TESTS=1 vitest run`                                                                                                                                                                                                                                                                            | 31 files, 213 passed, 1 skipped (`export-check.test.ts` with the `--page` rows)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `cd apps/studio && vitest run src/server/thumbs.test.ts`                                                                                                                                                                                                                                                                               | 9 passed (the card box and the composition)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| the acceptance vitest rows of MILESTONES-5 B4: theme `tokens` 42; schema `canvas shapes validate/page render` 48; render `print-layout geometry print stage` 21; export `units odp svg check/odf check/svg pdf scene/measure resample` 25 (7 files; `scene/measure` has no browser free test file); viewer `rulers-model snap` 29      | every row passed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `node apps/cli/bin/turboslide.mjs validate <dir>` over every `deck.json` under `decks/`                                                                                                                                                                                                                                                | 0 decks with errors (every stored deck validates at schemaVersion 1, the cycle diagram's connectors included)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `node scripts/check.mjs --greps`                                                                                                                                                                                                                                                                                                       | 30 innerHTML call sites in the allowlist, 15 `overwrite: true` in the allowlist                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `node scripts/check.mjs --list`                                                                                                                                                                                                                                                                                                        | 37 rows; steps 32 and 36 no longer pending (`page-setup.spec.ts`, `export-odp.ts` and `svg/write.ts` exist)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `git diff --exit-code d5a1be5 -- packages/chrome/src/tokens.css`                                                                                                                                                                                                                                                                       | empty                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `node_modules/.bin/prettier --check` over the 51 files this round touched                                                                                                                                                                                                                                                              | clean after one `--write` pass (formatting alone)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| the writers over measured scenes of earlier exports, no browser (`<scratch>/b4-smoke.mts`: `buildOdp` and `checkOdp`, `writeSvg` and `checkSvg` over `.turboslide/b2-fixture/native/scene-light.json`, the round one fixture's six slides, and `.turboslide/m5/baseline/scene-light.json`, the GT deck's 85 scenes of the M5 baseline) | the ODP of the six slides: 15,155 bytes, 6 pages, 23 text frames, 11 shapes, 1 table, 3 pictures, 2 notes pages, `export check` valid; the SVGs 6 of 6 valid in each mode; the brand deck's eight scenes (opener-brand, title, thesis, why, color, type, multilingual, ladder) 8 of 8 valid in each mode with the sizes: embed 462 to 467 KB (the 455 KB font data URI plus 4 to 12 KB of drawing), outline 17 to 267 KB (thesis 17, opener-brand 53, title 65, multilingual 143, why 173, type 180, color 222, ladder 267), link 4 to 9 KB. The scenes predate the round (no `page`, no measured baseline), so the writers' fallbacks ran (the default page, Inter's ascent) |

The e2e lock and the fidelity gates. The verifier's chain (`verification-5/scripts/run-chain.sh`, pid 22317 since 15:14) held `.turboslide/e2e.lock` for the whole fixer window: `check --from 20` (the parity audit, exit 1 at 18:27), `--from 21` (the Playwright suite of step 21, exit 1 at 18:48), then `--from 22` on, each `check.mjs` run keeping the lock it took at 18:34:06 (the directory's time) while `run-walk2.sh` had it from 18:19 to 18:34. This lane's waiter (`until mkdir .turboslide/e2e.lock; do sleep 5; done`, started 17:59) had not taken it by 19:00. The chain's own steps on this tree after this round's edits stand in for the gates this lane would have run (`verification-5/logs/check-from-22.log`, 18:48 to 18:52): step 22 ok in 46.1 s (`export decks/fixture/gslides --mode native` and its `export check`, then the flatten export: 28 pages `png-palette`, worst decoded mismatch 0.000 percent, perfect), step 23 ok (the fonts build check), step 24 ok in 85.2 s (`canvas-fidelity.mjs` on `gt-brand`, `templates/gt-brand` and `templates/blank`: 171 slides, 342 pairs, 0 over budget, worst 0.262 percent at `gt-brand/directions` dark, mean 0.004 percent, the day 2 numbers of section 6 unchanged), step 25 the Docker leg (`no space left on device`, finding 8), step 26 the second Playwright suite in flight at 19:01. Steps 32 and 36, ungated by this round's files, run in the same chain when it reaches them; their lines are read into 8.7 below when they land, else this lane's browser script (`<scratch>/b4-browser-run.sh`, the acceptance commands of MILESTONES-5 B4 in order, the dev server on 4354, the walk probe `--quick`) is the ship step's to run under the lock.

### 8.7 Steps 32 and 36 in the verifier's chain, on this tree

Step 32 (`verification-5/logs/check-from-32.log`, 19:07): `export decks/fixture/page-4-3 --mode flatten` wrote both themes in 7.5 s (8 pages `png-palette` per theme, worst decoded mismatch 0.000 percent, perfect, passed); `export check .turboslide/p43-flatten --page 1200x900` read both files valid with `page: 9144000 by 6858000 EMU (10 by 7.5 in)`, python-pptx reopening 8 slides and QuickLook rendering the first page at 3200 by 2410, so finding 6 is closed on the tree; the native export wrote `page-4-3-light.pptx` and the step stopped at its own `--mode native --verify` with `turboslide: verify: LibreOffice is not available as soffice; run the loop in the render worker image (docker/render-worker.Dockerfile) or set TURBOSLIDE_SOFFICE`, exit 2 after 16.9 s, before the PDF exports, the handout page count, the vitest rows and the two Playwright specs of the step ran. The `--verify` of that command is the literal text of SPEC-5 16.7 step 32 in `scripts/check.mjs` (the integrator's): a machine without `soffice` (this one; Docker's disk is full, finding 8, so the render worker image cannot stand in) cannot pass it whatever B4 writes. Request 11 (integrator, `scripts/check.mjs` step 32): gate the native export's `--verify` on `soffice` the way the render worker's export job gates its verify pass (`verifyToolsFor`, a note instead of a run), or move the `--verify` leg to the container step 25, so the step reads the page and print surfaces on a machine without LibreOffice; this lane did not change the CLI's `--verify` semantics (an exit 2 without the tool is the existing gate). Step 36 lands below when the chain reaches it.

### 8.8 The browser jobs under the lock (19:15 to 19:29, `<scratch>/b4-browser-run.sh`, the log `<scratch>/b4-browser-run.log`)

This lane's waiter took `.turboslide/e2e.lock` at 19:15:22, when `run-walk2.sh` and the check runs had let it go (the check runner, `scripts/check.mjs`, never touches the lock; the verifier's outer scripts hold it). The verifier's `run-chain.sh` then ran its last `rmdir .turboslide/e2e.lock` at 19:20:37 (`chain-timeline.log` "lock released", `CHAIN_DONE`), which removed this lane's lock while the run below was in flight; from 19:20 the run continued with no other Playwright, Chromium or probe process alive on the machine (the chain was done), and the lock was retaken at 19:41:08 for the rerun of 8.9. The dev server ran on 4354 (`TURBOSLIDE_STORE=tmp`, `TURBOSLIDE_REALTIME=memory`, two fake secrets) and was stopped at 19:29:06 with no listener left. The machine's load average was 9 to 12 during the run (the verifier's `check --from 36` and `--from 37`, the sync stress probe and the vitest suites ran on 4321 until 19:30).

| Command                                                                                                                                                           | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `PLAYWRIGHT_BASE_URL=http://localhost:4354 playwright test apps/studio/e2e/page-setup.spec.ts`                                                                    | 1 failed, 3 did not run: the spec's sheet locator named `.ts-stagewrap.ts-editor .ts-sheet`, and the live editor's sheet root (viewer `Sheet.tsx`) is `.sheet` with the two custom properties and no `data-page` (the render path's stage root carries both). Fixed in this round: `Sheet.tsx` stamps `data-page` on the live root and the spec locates `.sheet`; the rerun is 8.9                                                                                                                                                                                                                                                                                                                                                                        |
| `export decks/fixture/page-4-3 --mode flatten --out .turboslide/p43-flatten`                                                                                      | 2 files plus the zip, 8 pages `png-palette` per theme, worst decoded mismatch 0.000 percent, perfect true, passed true, 7.5 s                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `export check .turboslide/p43-flatten --page 1200x900`                                                                                                            | both files valid, `page: 9144000 by 6858000 EMU (10 by 7.5 in)`, python-pptx 8 slides, QuickLook 3200 by 2410 (finding 6 closed)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `export pdf decks/fixture/gslides --layout handout-6 --paper letter --orientation portrait --out .turboslide/handout`                                             | `gslides-light.pdf` 1,048,880 bytes, 5 pages at 612 by 792 pt for 28 slides (the skipped slide left out), 28 cells, 28 ok, gate pages, passed true, 1.1 s; the step 32 assertion (`pages === ceil(28 / 6)` and no cell `ok: false`) answers `pages 5 expected 5 cells 28 ok 28`                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `export pdf decks/fixture/gslides 1-6 --layout handout-3 --paper letter --orientation portrait --verify --out .turboslide/handout3` (the per cell gate, poppler)  | 2 pages, 5 cells through `pdftoppm -r 240 -scale-to-x 2040 -scale-to-y 2640`: title 0.563, breaks 1.284 (reported, over the 1 percent line), table 0.731, numbered 0.888, links 0.739 percent; 0 failed, passed true, 2.7 s. The wording "the file holds 2 page(s) for 2 slide(s)" of the paper layouts' page gate line was wrong and reads "for 5 slide(s) ... (ceil(slides / perPage) is 2)" since                                                                                                                                                                                                                                                                                                                                                      |
| `export odp decks/fixture/gslides --mode native --out .turboslide/gs-odp-native`                                                                                  | wrote `gslides-light.odp` (1.30 MiB, 28 pages) and stopped with exit 2 before the dark file: the report carried `page: deckPage(deck)` with its `preset` row, which `exportReportSchema`'s strict `page` refuses. Fixed (the size alone); the rerun is 8.9                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `export check .turboslide/gs-odp-native`                                                                                                                          | `gslides-light.odp`: 1.30 MiB, 20 parts, 28 pages, 517 shapes, container ok (mimetype first and stored, the manifest complete), odf ok (28 pages, 0 transitions, 0 effects, 0 plugins, 27 preset shapes), valid                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `export odp decks/fixture/gslides --mode flatten --out .turboslide/gs-odp-flatten` then `export check`                                                            | wrote `gslides-light.odp` (1.72 MiB), the same exit 2 on the report, the file valid under `export check`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `render slide title --deck decks/gt-brand --format svg --text embed`, `outline`, `link`, then `export check .turboslide/svg-embed/02-title-light.svg`             | three files, each valid; the check valid                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `render opener-brand,title,thesis,color,type,ladder,multilingual,why --deck decks/gt-brand --format svg --text <mode>` (the brand deck's eight scenes, each mode) | 8 SVGs per mode in about 6 s each; 7 of 8 valid per mode, `01-opener-brand` INVALID with `svg: 2 desc element(s), expected one`: the covering photograph's alt text travelled as a `<desc>` inside `<image>`. Fixed (the alt rides on `aria-label`); the rerun is 8.9. Sizes: embed 473,552 (thesis) to 616,245 (type) bytes, outline 17,489 (thesis) to 324,261 (type), link 3,729 (thesis) to 146,422 (type); the full table is in the log                                                                                                                                                                                                                                                                                                              |
| `render all --deck decks/gt-brand --theme light,dark --scale 1 --out .turboslide/render-b4 --json` then `compare-to-shoot.mjs --max-mismatch 0.005`               | 170 records; 170 pairs compared, 0 over budget, worst non escape 0.408 percent, mean 0.016 percent (the day 2 numbers; the Perfect export and the shoot unchanged)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `canvas-fidelity.mjs --max-mismatch 0.005`                                                                                                                        | 85 slides, 170 pairs, 0 over budget, worst 0.262 percent (`gt-brand/directions` dark), mean 0.004 percent, 62.2 s                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `canvas-fidelity.mjs --deck decks/fixture/page-4-3 --max-mismatch 0.005`                                                                                          | 6 slides, 12 pairs, 0 over budget, worst 0.019 percent (`title` light), mean 0.003 percent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `cd packages/export && vitest run src/gslides-fixture.test.ts`                                                                                                    | 15 passed in 54.9 s ("the padded shape" with the interpreter's insets among them; finding 4's fifth file)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `node scripts/probes/editor-walk-probe.mjs --base http://localhost:4354 --quick --json <scratch>/b4-walk.json`                                                    | 153 rows, 129 ok, 23 failed, 1 not driven (row 62, the logo plates through the palette, A8 row 10), 0 console errors, 462 s. The failures are one cascade: row 111 (`[an equation shape (Insert > Shape > Equation)] undo restores the box after the nw plain resize`) answered `pos none` after its undo, and every row after it found the slide's objects gone (112 to 133 `gone`, `0 -> 0`), 134 `fewer than two slides`, 137 the show counting `1 of 1`. Rows 1 to 110 (the text editing, the shapes, the callout, the equation shape's moves and resizes at zoom 100 and 200, every resize readout) passed; the undo at 111 emptied the probe's deck under the load of the concurrent chain. The second run of 8.9 reads it again on a quiet machine |

### 8.9 The rerun of the fixed rows (19:41 to 19:50, `<scratch>/b4-rerun.sh`, the log `<scratch>/b4-rerun.log`; the lock retaken at 19:41:08, the dev server on 4354 again, load average about 6)

| Command                                                                                                                                           | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `export odp decks/fixture/gslides --mode native --out .turboslide/gs-odp-native` then `export check .turboslide/gs-odp-native`                    | 2 files plus the zip of both, 28 pages per theme, 102 text frames, 63 shapes (`draw:custom-shape` with the interpreter's `draw:enhanced-path`), 2 tables (`table:table`), 14 pictures, 80 automatic styles, 14 stored parts, package 20 parts with 18 in the manifest, passed true, 14.2 s; `export check` valid on both files (container ok, odf ok, 27 preset shapes read)                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `export odp decks/fixture/gslides --mode flatten --out .turboslide/gs-odp-flatten` then `export check`                                            | 2 files plus the zip, 28 page rasters under 138 invisible text frames per theme, 41 automatic styles, 34 parts, perfect true, passed true, 25.2 s; `export check` valid on both                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `render opener-brand,title,thesis,color,type,ladder,multilingual,why --deck decks/gt-brand --format svg --text embed` then `export check` on each | 8 SVGs, 8 of 8 valid (the opener's alt on `aria-label` now), each check valid                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| the same eight in `outline` and in `link`                                                                                                         | 8 of 8 valid in each mode (the sizes of 8.8 hold: embed 474 to 616 KB, outline 17 to 324 KB, link 4 to 146 KB; the `type` slide is the largest in every mode, its 12 texts of many runs)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `node scripts/probes/editor-walk-probe.mjs --base http://localhost:4354 --quick --json <scratch>/b4-walk-2.json`                                  | 220 rows, 215 ok, 3 failed, 2 not driven, 0 console errors, 429 s. The three failed rows are 121, 123 and 126, the picture (Image by URL) resize at zoom 100 and 200 with "content did not scale", the same three rows `build-4/hotfix-4.md` lists at `BASE` and this lane's day 2 walk (section 6; A8 row 1, B2's picture renderer). Not driven: 57 (Insert > Material, the menu row) and 62 (the logo plates through the palette, A8 row 10, B3). The first run's cascade from row 111 (8.8) did not recur: the equation shape's undo answers its box and every row after it drives; the cascade was the load of the concurrent chain, not the tree. Every surface this round touched (the sheet, the rulers, the guides, the gestures, the group selection, the inline text, the menus, the exports) drove green |

### 8.10 `page-setup.spec.ts`, the third and fourth runs (19:51 to 20:04, `<scratch>/b4-spec-run.sh`)

Run 3 (19:51): tests 1 and 2 passed (the GT sheet at `data-page="1600x900"`; Standard (4:3) through `deck.setPageSize` under `keep` with `guidesDropped 1`, `objectsScaled 0`, the live sheet at `1200x900` with `--ts-sheet-w: 1200px`, the 4:3 box, the edge object's coordinates kept, the guide at 600 kept and the one at 1500 gone, the rulers 11 numerals across ending at 10 and 8 down); test 3 failed on its last step: Edit > Undo was disabled after the `fit` write (`aria-disabled="true"` on `edit.undo`), because the shell's undo history takes the shell's own writes and not a window API `invoke` (the strict contract of SPEC-3 3.10); the dialog's OK is the undo path and drives once the row is live. Test 3 now asserts the one commit rule through the revision (`fit.revision === base + 1`: the page, the scaled object and the guides in one write) and restores the deck through the action and `block.set`. Run 4 (20:03): 3 passed, 1 skipped in 23.0 s; the skipped test is the dialog's, with the reason in the report ("file.pageSetup is Later on this tree: the integrator flips the row and registers the dialog (b4.md fix round requests); not driven"). The spec's scratch deck (`decks/e2e-page-<stamp>`) and its worker cache were removed by its `afterAll`; the lock was released at 20:04:09 with no dev server, probe or Playwright process of this lane left.

Added to 8.5: (8) the walk probe's first run (8.8) failed 23 rows in one cascade from row 111 under the load of the concurrent chain and the second run (8.9) on a quiet machine read the known three picture rows alone; the cascade is recorded and not attributed to the tree. (9) Edit > Undo does not take a window API write (8.10); the Page setup dialog's OK is the undoable path.
