# Turboslide round four, build status

Kept by the integrator (`docs/gslides-parity/MILESTONES-4.md`, "Integrator"). One heading per builder: what landed at merge 1 and at merge 2, what was cut, the open requests with where they went, and the deviations recorded. Section numbers refer to `docs/gslides-parity/SPEC-4.md` unless prefixed SPEC, SPEC-2 or SPEC-3. Started on day 0, 2026-09-14, over `main` at `d5d7f07` (`BASE`); nothing is committed until the ship step. Full notes: `docs/gslides-parity/build-4/integrator.md` (day 0: the line citation map, the reassignments, the commands) and the builders' `b1.md` to `b5.md` as they land. Kevin's directives of the round: transitions between pages and actions in the slides faster and more performant; a custom Turboslide icon, favicon, identity and interface using Bayer dithers and shaders, with the turbo fast story mapped to the code; a `/home` page; the three mark candidates recorded with the dither identity shipping; the canvas selection boxes in a blue that does not blend into the background.

## Day 0

The tree: `main` at `d5d7f07`, the round three ship and its hotfix, clean apart from the untracked `.github/`, the nine mockup PNGs under `design-4/*/previews/`, the round three ship flags, `verification-4/` (the verifier's, holding PP 9.5's two production JSON files and log) and, from today, `research-5/` (another workflow's). None of them touched.

The integrator's day 0 (MILESTONES-4 "The order", day 0; `build-4/integrator.md` sections 1 to 8):

| Item                              | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `BASE` named                      | `d5d7f07`; SPEC-4's citations were written at `28cb63b`, 28 of the cited files changed between the two                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| The line citation map (6.6)       | `integrator.md` section 2: every citation re-read, with the current line or the moved construct; the facts the tree contradicts (the four views, the `--ts-` presence tokens in `styles.css`, the skeletons' path, `recordDeckOpened`'s file, `createGtBrandDeck`, `invertBits`) in section 7                                                                                                                                                                                                                                                                                   |
| Reassignments                     | `integrator.md` section 3, pointed at from MILESTONES-4 "Day 0 amendments": the skeletons under `apps/studio/src/components/` (B1), `recordDeckOpened` in `decks.index.tsx` (B3 the signature, B4 the call site), the ruling (1) files for `--pt-select` (B1, including `packages/lint/src/chrome.ts`), the print route's `notFoundComponent` line (B3 through a request), the nested native ignore (B4), the `/home` root class for the chrome lint and `main` as its landmark (B2 through a request), the `styles.css` acceptance grep narrowed to the eleven identity tokens |
| `@vercel/functions`               | 3.9.7 in the catalog and `apps/studio/package.json`; SPEC-4 names no version, the newest release on 2026-09-14 was pinned; `pnpm install` once (one package added; frozen install "Already up to date")                                                                                                                                                                                                                                                                                                                                                                         |
| The theme exports                 | `./brand` and `./brand/site` in `packages/theme/package.json`, plus `@turboslide/effects` as a dependency and `brand` in the theme's tsconfig include with the effects reference, for `brand.ts`'s `bayer8` import (0.10)                                                                                                                                                                                                                                                                                                                                                       |
| `scripts/perf-budget.mjs`         | moved from `design-4/` (the design copy stays for the ship step to remove) with the root lookup, the stream pattern `/api/decks/[^/?]+/stream` (4.4) and the rows of 4.7: image bytes per route, the `cdn` check, the cold first byte report, the `/home` LCP element flag, the reserved `vitals` row; the `/home` landmark is `main`                                                                                                                                                                                                                                           |
| `scripts/check-vercel-output.mjs` | new, step 30's assertions (0.45): the header rules of 1.6 and the `/` redirect in `config.json`, the icon set of 0.13 and the prerendered `/home` under `static/`, the function directory sizes against 250 MB. Ran in report mode against the 2026-09-11 output: 7 of 32, every miss a round four item, four functions at 146.6 MB                                                                                                                                                                                                                                             |
| `scripts/check.mjs`               | steps 29 to 31 as gated stubs that skip by name today (`--list` prints 31); step 31's `needs: 'node-server'` builds and serves the node-server output on 4321 with the tmp store when opted in (`TURBOSLIDE_CHECK_PERF=1` until merge 2)                                                                                                                                                                                                                                                                                                                                        |
| `.gitignore`                      | 0.38: the wasm module's four files and the Linux x64 glibc addon tracked; the nested `packages/native/npm/linux-x64-gnu/.gitignore` is B4's to change                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Root `build:brand` script         | `node scripts/build-brand.ts` (1.5)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Gates                             | `node scripts/check.mjs --only 1,2` green (0.3 s, 1.0 s); `node_modules/.bin/tsc -b` exit 0; `node scripts/check.mjs --only 4,5,6` green (0.4 s; 82.0 s with 266 files, 2,850 tests passed and 3 skipped; 7.2 s with the bundle check and the greps)                                                                                                                                                                                                                                                                                                                            |
| Baselines taken today             | the CDN rows against production: every icon path, `/og/turboslide.png` and `/home` answer 404 with `x-vercel-cache: MISS` (R02 section 1's finding, the day 0 number for 4.1's last row); the node-server build boots in 2 s and serves the routes once `TURBOSLIDE_SESSION_SECRET` and `TURBOSLIDE_DOWNLOAD_SECRET` are set (it answers 500 on every route without them; the runner now sets fake values for its own server), and a one run report of the moved script against it met 65 of 111 local rows on the day 0 tree (`integrator.md` 6.2)                             |

The verifier's day 0 (6.3: the baseline run against the round three ship deploy, the hash check of the PP 9.5 files) is the verifier's to record under `verification-4/` and in VERIFICATION-4.md; not the integrator's.

## Merge 1

Day 1, 2026-09-14 (MILESTONES-4 "Integrator" Delivers item 2; `build-4/integrator.md` sections 9 to 14). The tree: `BASE` plus the integrator's day 0 files, B1's day 1 (the brand module, the tokens, the chrome mark, the icon set, the head, the selection colour), B3's day 1 (the editor split) and the requests of `b1.md` and `b3.md` that fall in the integrator's files. The first run of the merge was interrupted by a network outage after 35 tool calls and left no notes; the second run reconstructed its edits from the tree and finished the merge. Nothing is committed.

| Item                  | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1's day 1            | landed (`b1.md` section 1): `packages/theme/src/brand.ts` and `brand.test.ts` (28 tests), `packages/theme/brand/site.ts`, `packages/chrome/src/brand.css`, `TurboslideMark`, `AppBarBrand`, `EmptyFigure`, the `linkComponent` slot and `TitleHomeLink`, the Progress ramp edge, `scripts/build-brand.ts` with the icon set under `apps/studio/public/` and `brand-manifest.json`, the head tags of 1.6 and `NotFound` in `__root.tsx`, `--pt-select` and `--pt-guide` in `tokens.css` with the readers in `Overlay.css`, `Marquee.css` and `Guides.css` and the chrome lint's `select` and `guides` scopes (ruling 1) |
| B3's day 1            | landed (`b3.md` section 1): `apps/studio/src/editor/{controller,EditorRoot,shell-bridge}.tsx` and `routes/-edit-search.ts`, the route file at 89 lines, `new.tsx` without the namespace import; every unit moved verbatim (82 of 82) with the hotfix's write path intact; `router.tsx` untouched (day 1's second item lands next)                                                                                                                                                                                                                                                                                      |
| Requests applied      | b1.md R1 (the chrome exports and the theme dependency, one `pnpm install`), R2 (`Thumb.capture?` typed in B4's file, pre-authorized by MILESTONES-4 "The seams"), R3 (AGENTS.md), R4 (the Guides comment); b3.md R1 (`INNER_HTML_ALLOW`), R2 (AGENTS.md), R3 and R4 (two comments)                                                                                                                                                                                                                                                                                                                                     |
| Requests forwarded    | b1.md R5 to B3 and R7 to B2; b3.md R5 to B5; the verifier's requests to the builders under each heading below; the verifier's requests on the perf script are the integrator's for merge 2 ("Open requests")                                                                                                                                                                                                                                                                                                                                                                                                           |
| AGENTS.md             | the round four sections: `brand.css` in the Code rules, the dev server exception with the ports 4341 to 4346 and both secrets, the catalog entry and the two workspace edges, the round four ownership line, the acceptance lines for steps 29 to 31 and the probe, the seams (the brand module, the chrome components and the `linkComponent` seam, the selection colour rule, the editor split, the `import()` allowance by place, `--version`, the head), the merge 1 deviations; four sentences that named the edit route file now name the controller or record the split                                         |
| Gates                 | `tsc -b` exit 0 (0.13 s incremental; 29.5 s forced over 21 projects); vitest theme 72, chrome 496, viewer 240, lint 72, studio 173, all passed; `check --only 1,2` in 1.0 s; `check --only 4,5,6` in 94.6 s (step 5: 268 files, 2,892 passed, 3 skipped; step 6: the bundle check, 25 innerHTML call sites in the allowlist); `check --only 29` ok ("16 files match the manifest, the rebuild and the facts of SPEC-4 6.4"); the default view words, Tooltip and brand chrome tests 32 of 32                                                                                                                           |
| The `/new` boot probe | 17 of 17 on a dev server on 4321 with `playwright.config.ts`'s environment on the file store (`untitled-20260914-9bv3`, trashed and deleted forever; `decks/` as found; the server stopped); the head smoke: every path of 0.13 served, the tags of 1.6 present, `/no-such-page` 404 with the three tooltipped controls                                                                                                                                                                                                                                                                                                |
| Step 18               | `check --only 18` ok in 106.1 s: `lint --chrome` on `/deck/gt-brand`, `/edit/gt-brand`, `/new` and `/decks` over three widths and two themes, 78 audits, 0 with findings (the title row's mark and the selection colour readers on the served pages)                                                                                                                                                                                                                                                                                                                                                                   |
| Step 30               | the gate is open on this machine since `brand-manifest.json` exists; not run (its assertions wait for B3's `routeRules` and B2's `/home`); `TURBOSLIDE_CHECK_VERCEL=0` skips it on a linked machine until merge 2                                                                                                                                                                                                                                                                                                                                                                                                      |
| Deviations            | `integrator.md` section 13; AGENTS.md "Round four, merge 1"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

## Merge 2

Day 6, 2026-09-14 (MILESTONES-4 "Integrator" Delivers item 4; `build-4/integrator.md` sections 15 to 21). The tree: `main` at `e2904d5` (`BASE` `d5d7f07` plus another workflow's two round five commits, `1d2a132` the research and design documents and `e2904d5` their formatting, neither touching a round four file) with every builder's days 2 to 5 in the shared checkout and the integrator's merge 2 edits. Nothing is committed. The merge ran as two sessions: the first stopped inside step 31 after the merge edits, steps 1 to 27, the preview and the first smoke; the second (`integrator.md` section 18's opening) read the tree and the notes, re-ran the cheap steps, ran step 31, the second smoke and the perf run on the preview, and closed the merge.

| Item                        | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The merge order             | B1, B4, B3, B2, B5: every request in the integrator's files applied, every request in nobody's file applied as the smallest edit with a note, two lines in builders' files applied on the builders' own wording (`-access-page.tsx`'s import, b3.md R6; `print.$deckId.tsx`'s `PrintMissing`, b3.md R7 on ruling 3); the table is `integrator.md` section 15                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Steps 29 to 31              | 29 is four checks (the brand build, the /home assets, the shape table, the native record); 30 runs for real on this linked machine; 31 lost its `TURBOSLIDE_CHECK_PERF` gate and gained the per route preload ceilings; `--list` prints 31                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| The check's other lines     | step 18 lints `/home` and `/no-such-page` (the shell driver accepts the Not found page's 404); step 26 runs `home-page.spec.ts`; step 6's allowlists name `PresenterPage.tsx` (innerHTML) and `thumbs.ts` (the stamped thumbnail objects)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| The scripts                 | `hosted-smoke.mjs`: the `/home` row, fifteen `cdn` rows, the two thumbnail rows, the `effects backend` row and the `--template-copy` row; `tooltip-audit.mjs`: `/home` in the default list and the `main[data-hydrated]` wait; `perf-budget.mjs`: the four metric changes of the header; `check-vercel-output.mjs`: the pictures under `brand/` and `home/`; `check-client-bundle.mjs`: the largest chunk ceiling (reported until the vendor group lands) and the per route preload ceilings                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| The chain                   | `--only 1,2` ok; `--only 3` fails as expected on B4's uncommitted `openapi[.]json.ts` header change (`pnpm generate:contracts` reports 14 files current); `--only 4,5,6` ok after two fixes below (step 5: 278 files, 2,968 tests passed, 3 skipped, 111 s); steps 7 to 18 ok (step 18: 90 audits over six URLs, 0 with findings, 134 s); step 19 green in the second session (below); steps 20 to 31 in the "The check at merge 2" table below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Step 6's two fixes          | the largest chunk ceiling reports until the vendor group lands (SPEC-4 0.27; `integrator.md` section 17: the entry chunk is 914,233 bytes, the group is dead inside the Vite build); `OVERWRITE_ALLOW` names `apps/studio/src/server/thumbs.ts` (the stamped thumbnail objects)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Step 19                     | met by `prettier --write` over this round's committed `research-4/` and `design-4/` records, the round three `verification-3/` records and B1's `b1-selection/shoot-selection.mjs` (whitespace and table alignment; the round three precedent for `research-3/`); the pass also touched the other workflow's `research-5/`, `design-5/` and `SPEC-5.md` (restored from `HEAD`; the other workflow then committed them formatted as `e2904d5`) and the verifier's untracked `verification-4/` files (ten, reformatted, not restorable; whitespace only, recorded for the verifier); `packages/native/wasm/` joined `.prettierignore` and the glue was regenerated (`check-record.mjs`: 4 outputs match). In the second session the step named only the two outputs step 27 rewrites (`verification-3/layout-shift.json` and `.md`, written unformatted by the audit); after `prettier --write` on them the step is green on the whole tree, the first time this round                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| The preview                 | `https://turboslide-fw2ypjeei-kl01s-projects.vercel.app` (the second deployment; the first failed on the CLI upload leaving the wasm module out, `.vercelignore` corrected); `hosted-smoke.mjs` 36 of 39 on the first run, the three misses the smoke's expectations corrected for a Vercel deployment (`integrator.md` section 19), then 37 of 37 with `--template-copy` once (`smoke-20260914-3sut` created from the GT template, trashed and removed) and 37 of 37 again in the second session without the copy row (the stamped thumbnail now the 302 to the Blob object on a fresh instance; `x-vercel-cache` still `MISS` while the principal cookie rides on it). Production redeployed at 13:06 from `main` by Vercel's git deploy of the other workflow's push (round three code plus round five documents); no `--prod` deploy ran in this merge                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| The perf run on the preview | `perf-budget.mjs --profile deployment --runs 3 --write` against the preview (`integrator.md` section 19; the JSON in `build-4/integrator-perf/perf-budget-preview-2026-09-14.json`): 78 of 157 budgets met against the day 0 production run's 58 of 121; the scratch deck `untitled-20260914-9apa` removed through the actions API at once (404 after). Met: every CLS row, `/home` at 52 ms first byte, 208 ms LCP and 142 ms ready cold with the twin as the LCP element, the seven `cdn` rows `HIT`, the twins 0 re-fetched (verifier request 4), the idle editor at 3 calls a minute (day 0: 465), `edit->decks` 13 ms in page (day 0: a 1,114 ms document load), the filmstrip's first pass and p95 at 42 and 17 ms and 99 fps, the write path's commit at 89 ms and the home card in 40 ms. Missed: the js decoded and largest chunk rows on every route (the 914 KB entry; 30 to 55 percent less JavaScript than day 0 and still over), the cold loads of `/` and `/new` (LCP 1.7 to 1.8 s with a 1.2 to 1.6 s longest frame, where the node-server build reads 64 to 163 ms: for the verifier), `/decks` cold ready 2.1 s and warm 0.9 s against 1.0 and 0.6 s (down from 5.7 and 6.5 s at day 0), `/deck`'s cold LCP and ready 1.2 and 1.3 s (day 0: 0.4 and 0.6 s; the deferred slides as escaped JSON, b3.md finding 9), the `'data-only'` routes' first byte (`/edit` 556 and 270 ms, `/present` 527 and 286 ms; their LCP and ready fell), `decks->edit` 606 ms in page, `home->new` 1,079 ms as a document navigation, the filmstrip's longest frame 83 ms and the DOM counts, and the two saved rows on the blob checkpoint (1,484 and 1,018 ms) |

### The check at merge 2

From `integrator.md` section 18, steps 20 to 31 (steps 1 to 19 are in the table above; the second session re-ran 3, 4, 19, 28, 29 and 30 and ran 31). `pnpm check --list` prints 31; the selected steps that passed pass by their own commands, the failures are listed with their owners.

| Step | Result                         | What it says                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 20   | FAIL, the round three baseline | the parity audit 2,980 pass, 5 fail, 384 skipped, the same five rows as `HEAD` (VERIFICATION-3 section 11); no row moved at merge 2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 21   | FAIL, 26.5 min                 | the fifteen round two specs: 71 tests, 54 passed, 12 failed, 5 did not run; the twelve are listed in `integrator.md` section 18 with a reading (three revision rows at the write path's answer, the menu and dialog rows at B4's lazy loads, `validate.run` without a client handler) for the verifier's attribution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 22   | ok, 46.9 s                     | the fixture deck exported in both modes, the flatten report perfect                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 23   | ok, 48.5 s                     | `fonts build --check`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 24   | ok, 98.4 s                     | the canvas fidelity gate at 0.5 percent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 25   | FAIL, Docker's disk            | `no space left on device` copying the darwin addon into the image (round three's finding 25; nothing pruned by the round's rule)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 26   | FAIL, 12.8 min                 | the eight round three specs and `home-page.spec.ts`: 49 tests, 29 passed (the nine `/home` rows among them), 6 failed, 14 did not run; the six in `integrator.md` section 18 (`comments`, `presence`, `realtime` test 4, `security` upload 403, `share`'s request access form on the `'data-only'` route, `versions-by-author`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 27   | FAIL, recorded, 626 s          | the layout shift audit on `vite preview` over step 6's build: 75 failures and 7 skipped states against `HEAD`'s 71 and 8 (VERIFICATION-3 finding 26); the four new rows are `L5` load entries of 0.0003 to 0.0004 on `/decks` (B3's streamed list); `/home` has no cell in the round three script; the two outputs formatted afterwards for step 19                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 28   | ok, 2.8 s                      | `pnpm audit --prod --audit-level=high`: 0 advisories at high or above                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 29   | ok, 5.9 s                      | the brand build (31 files), the `/home` assets (48 files), the shape table and `ids.ts` current, the native record (4 wasm outputs match, the addon pending)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 30   | ok, 10.6 s                     | the Vercel build with `/home` prerendered and `check-vercel-output.mjs` 38 of 38 (the nine header rules, the `/` redirect, the static files, eight functions at 127.4 MB against 250 MB)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 31   | FAIL, recorded, 254.9 s        | the node-server build on 4321 with the tmp store and `perf-budget.mjs --profile local --write --runs 3`: 104 of 151 budgets met (the JSON in `build-4/integrator-perf/`); every route's first byte, LCP and ready under its ceiling but `/present` warm ttfb, `/home` at 7 ms, 76 ms and 34 ms with the twin as the LCP element and CLS 0, seven of eight transitions, the twins and the text burst met; the misses are the js decoded and largest chunk rows on every route (the 914 KB entry, `integrator.md` section 17), the DOM counts on `/deck`, `/edit` and the filmstrip (B4, R14), the three filmstrip frame rows (B4; a regression against the day 0 machine baseline), `decks->edit` in page 820 ms (B3), the idle poll at 5 calls a minute (B4), and four write rows (the new slide's acknowledgement on the memory cadence, the capture and the home card on a cold tmp store, the card paint at 17 ms). `check-client-bundle.mjs --base` by hand: `/decks` preloads 1,066,030 B against 600,000 (FAIL), `/deck` and `/edit` under theirs |

## The account boundary of the round

The orchestrator's ruling (2): no Marketplace install, no paid resource, no DNS or domain change, no sign up. The GitHub social preview image is Kevin's manual upload; the repository description and topics refresh runs through `gh repo edit` in the ship step with the strings of `docs/brand.md`.

## B1 Brand assets, tokens, chrome mark, favicon and card pipeline

Day 1 landed at merge 1 (`b1.md` section 1; the "Merge 1" table above): items 1 and 2 of Delivers plus ruling 1. Readings recorded in `b1.md` 1.2 and AGENTS.md: 6.4's "208 ink cells" is the path's area (52 of 64 cells lit at N = 8, both pinned); the hinted 12 px mark in the 16 px tile; `icon-dark-*` as the paper plate with the ink mark; the progress track keeps the port's 2 px; `SITE.origin()` without a request in the root `head()`; the About Turboslide anchor until `home.tsx` exists. Open for B1: day 2's twins and card (the four `/brand/*.png` paths answer 404 until then), the verifier's Layout grid plate that does not close on Escape (in no row; B1's if room allows on day 5, else the fixer round's). Reassigned to B1 on day 0: `apps/studio/src/components/EditorSkeleton.tsx` and `PresenterSkeleton.tsx` (the curtain picture only), and the ruling (1) files for `--pt-select` (`tokens.css` one addition, `Overlay.css`, `Editor.css`, `Marquee.css`, `Guides.css`, the selection chip, `packages/lint/src/chrome.ts` and its test). The acceptance line `git diff --exit-code packages/chrome/src/tokens.css` reads "differs from `BASE` by the `--pt-select` block only"; the `styles.css` grep reads "none of the eleven identity tokens". The theme package already exports `./brand` and `./brand/site` and depends on `@turboslide/effects`.

Merge 2 (`b1.md` section 2): days 2 and 3 landed as reported (the twins and the card through `--capture`, the wordmarks and lockups, the README lockup PNGs, the previews, the raster edge check, the CLI banner and `--version`, the MCP sentence, the print bar mark, `writeThemeColor`, `brand-files.test.ts`, `facts.json`, the curtain picture, the Not found `Link`, `build-brand.ts --check` over 31 files). Applied for B1: R8 (the shell driver accepts the Not found page's 404; step 18 audits `/no-such-page`, 6 audits, 0 findings), R12 (the eight twins and the card in the smoke's `cdn` rows and in step 30's static list), R13 (the step 29 note). R9 and R10 went to B3 (R9 is in the tree: `/brand/**` takes the day and week rule; R10's skeleton grid is a fixer round check on the built output), R11 to B5 (met: docs/brand.md carries the contrast table and the lockup `<picture>`), R14 to B2 (met: `facts-data.ts` reads `facts.json`). Open for B1: the fitted curtain shot on the built skeleton (R10), the Layout grid plate on Escape (verifier finding 9), the fixer round's rows. Deviation recorded: `b1-selection/shoot-selection.mjs` was formatted by the integrator's step 19 pass (whitespace).

## B2 The /home route and its hero

Not started (day 2, from merge 1). Fixed on day 0: the page renders a `main` element (the perf check's landmark), does not reuse `.ts-home-page` (the `/decks` class), and names its root class in `b2.md` so the integrator adds it to the chrome lint's roots at merge 2. The CDN rows for `/home` and the image budget rows are in `scripts/perf-budget.mjs` already.

From merge 1 (b1.md R7): the eleven `--ts-` tokens are on `:root` from `brand.css`, which `__root.tsx` loads for every route, so `home.css` declares none; the twins come by `SITE.twins.hero` and the brand assets by the `SITE` paths of `@turboslide/theme/brand/site`; B3's `linkComponent` wrapper (the router's `Link` to `LinkSlotProps`) is the model for the page's router links.

Merge 2 (`b2.md`): the `/home` route and its nine bands, `copy.ts`, `facts.ts` over the generated `facts-data.ts`, `build-home-assets.ts` (48 files under `apps/studio/public/home/`), `home-page.spec.ts` (9 rows) and the six shots landed as reported; every request to the integrator applied: R1 (`.ts-product` in the lint's roots and the shell driver's `readySelector`; step 18 audits `/home` with `--states ''`, 6 audits, 0 findings), R2 (step 26 runs `home-page.spec.ts`; `build-home-assets.ts --check` is in step 29's chain), R3 (`/home` in the tooltip audit's default list, the `main[data-hydrated]` wait; the acceptance line's `--only` reads `--url`), R4 (the `/home` smoke row: on the preview 200, three of three marks, no noindex meta, `x-vercel-cache: HIT` on the first request already), R8 (AGENTS.md). R5 (the continuous hero frame) went to B1 and stays open (the first pipeline cell shows P1's 7.0 s frame with its caption); R6 is confirmed in B3's tree (the `document.prerendering` gate); R7 went to B5 (met: the README's speed rows and the two cards). Findings for the fixer round: `routes/home.tsx` imports `HOME_META` from `copy.ts` at module level, so the page's 30 KB of copy rides in every route's entry chunk (`integrator.md` section 17); the `/home` js decoded ceiling (600 KB) misses while the entry chunk is 914 KB.

## B3 Route and transition performance

Day 1 landed at merge 1 (`b3.md` section 1): the mechanical split, 82 units in exactly one file each, the hotfix's write path in `controller.tsx` with the same call counts, the `/new` probe 17 of 17 on 4343 before and after. B3's findings 1 to 6 are in `b3.md` section 2; finding 1 (four specs read or seed the checkout's `decks/` and cannot pass on a tmp store server) and finding 4 (a direct `eslint` disagreeing with `lint-packages.mjs` on the moved code) are open decisions for B3 and B4 on day 2. From merge 1 B3 holds b1.md R5 (`linkComponent` filled in `EditorRoot`, `AppBarBrand` on `/decks`, `/decks/trash` and the four views, `EmptyFigure` for the empty states), the print route's `notFoundComponent` line for the integrator (ruling 3), the verifier's finding 6 (the `/edit/gt-brand` stall on production) and request 2 (the hosting banner over `toolbar.layout` on the tmp tier), and 1.6's `routeRules` with the assets cache header (verifier request 4). Fixed on day 0: `recordDeckOpened` and `RECENT_KEY` are B3's (`decks.index.tsx`); the loader's `readHomePrefs()` stays awaited; the You need access `notFoundComponent` goes on `deck`, `edit` and `present` directly and on `print` through a request (B1's file); the four views of SPEC-3 6.7 are server actions, so Delete forever's optimistic path lands in `decks.trash.tsx` alone. `scripts/check-vercel-output.mjs` names the `routeRules` of 1.6 and the `/` redirect it will assert after B3's `vite.deploy.config.ts` lands.

Merge 2 (`b3.md` sections 6 and 7): days 2 to 5 landed as reported (`defaultPreloadStaleTime`, view transitions, the three `'data-only'` routes over the skeletons with the trimmed payload, `shouldReload: false` on `/new`, the You need access page on the deck, edit and present routes, the hosting banner off the toolbar, the streamed `/decks` and `/decks/trash` with the Recent row's cookie mirror, `AppBarBrand` on both, the viewport preloads, the optimistic trash actions, the title row as a `Link`, the presenter's own chunk, the `/new` prerender gate, the deferred slides of `/deck` and `/embed`, the GET `getDeck` and `getDeckSlides` with the CDN header on the public shape, the `routeRules` with the `/` redirect, the seed twins out of the bundle, the prerendered `/home`, the vendor group in both configs, five specs' rows). Applied for B3: R6 (the chrome export and the import line), R7 (the print route's `AccessPage`, ruling 3), R9 (`INNER_HTML_ALLOW`), R10 (AGENTS.md), R14 (B4 before B3; `DROP_SEED_TWINS` ships as `true` with B4's blob fetch and the integrator's tmp fetch both in place; the preview's template copy from the GT template passed in 4.4 s). R11 (the twins row on the node-server build) is step 31's row in the check table. R13 waits for the addon. R8 went to B4 (the version log refresh on panel open; open), R12 to B5 (met in docs/hosting.md section 12). Findings from the merge: the vendor group is compiled into the config and never applied by the Vite build (`integrator.md` section 17: not B3's code, the bundler); the `/deck` document grew to 401 KB on the preview with the deferred slides riding as escaped JSON (b3.md finding 9; the verifier's `/deck` rows); `editor.spec.ts` row 7 (finding 7) is a pre existing tooltip mismatch in nobody's round four code. Measured at step 31 on the node-server build (`integrator.md` section 18): the LCP and ready of `/`, `/new`, `/edit` and `/present` fell from 646 to 808 ms at day 0 to 64 to 312 ms, `edit->decks` and `trash->decks` are 16 and 11 ms in page, `home->new` 281 ms as a document navigation, and `decks->edit` in page reads 820 ms against 300 with 47 cards on the tmp store (B3's dev server smoke read 228 ms): the fixer round's row for B3. The layout shift audit gained four `L5` load rows of 0.0003 to 0.0004 on `/decks` from the streamed list (step 27).

## B4 Editor action and render performance

Not started (day 1: the store and the native outputs). Fixed on day 0: `@vercel/functions` is installed; the root `.gitignore` keeps the wasm module and the Linux addon, and `packages/native/npm/linux-x64-gnu/.gitignore` (B4's) must drop `*.node`; the four wasm files of an earlier local build show as untracked and are replaced by CI's outputs with `BUILD-RECORD.json` before anything is committed; `dither.worker.test.ts` exists and is extended, not created; `server/decks.ts` has no `createGtBrandDeck` (the template copy is `@turboslide/store/templates` `createDeck`); the call site of `recordDeckOpened` with the new arguments is B4's in `controller.tsx` after the split. Ruling (3): `undo.spec.ts` is B4's file, so the re-pin to the checkpointer coalescing with the owner's reason (SPEC-3 finding 55) is B4's.

From merge 1: `apps/studio/src/editor/controller.tsx` is B4's (3,122 lines: `createEditorController` with the hotfix's write path, `toViewerDeck`, `warmThumbs`, `THUMB_W`); `Thumb.capture?: 'never' | 'when-available'` is typed (b1.md R2), the behaviour is day 2's; the day 0 amendment's "B4 passes the new arguments from `controller.tsx`" is void, because the two `recordDeckOpened` call sites moved with `EditorRoot` (B3's file) and the day 3 change stays there. The verifier's findings 3 (the filmstrip's 4,921 nodes with 85 cards on production) and 10 (the duplicated slide's thumbnail not arriving within 45 s) are B4's; the memory tier's two second checkpoint cadence against 4.6's local ceilings (verifier request 3) is decided with the integrator.

Merge 2 (`b4.md`): days 1 to 5 landed as reported (the four round Blob write, the manifest list, the `.thumbs/` helpers, `fetchAsset` with `fetchMissingTwins`, the clone first filmstrip with `useNearWindow`, the controller's memo and warms, the thumbnail cache on Blob with retention and `waitUntil`, the contracts routes with the CDN header, the diet rows 3 to 7, the session poll interim, the wasm module rebuilt byte for byte with `BUILD-RECORD.json` and `check-record.mjs`, the worker's wasm mount with the parity row, four specs). Applied for B4: R1 (`cacheControlMaxAge` through `blob-vercel.ts`), R2 (the tmp tier's twin fetch), R3 (the generator moved; `--check` in step 29), R6 (`check-record.mjs` in step 29; `native.yml` stays under `packages/native/ci/` until the ship step touches `.github/`), R11 (the smoke's thumbnail rows and the `instance` facts on `/api/agent`: the preview runs `typescript` on glibc 2.34, Node 24.19.0), R12 (`PORTED_FROM.json`), R13 (AGENTS.md), R14 (the perf check's DOM nodes after GC). Not taken: R4 (the validators importing `ids.ts`; `integrator.md` section 17 says why it alone would not reach the chunk ceiling), R5 (the `ViewerShell` render prop), R8 (`@turboslide/native` as a studio dependency), R10 (waits for the addon). R7 is Kevin's (the Linux addon: no `cargo-zigbuild` or `zig` here; the function runs the TypeScript stages until it is committed). R9 went to B3 (optional, not built). Findings: the render route's answers carry the anonymous principal cookie, so the CDN never caches a thumbnail (`x-vercel-cache: MISS` on the second stamped request on the preview) and 0.31's `s-maxage=60` has no effect at the edge; a stamped request on the public store answered a 200 from the instance's disk rather than the 302 to the Blob object on the first smoke (the second smoke, on a fresh instance, read the 302 with `x-turboslide-source: blob` both times); step 6's greps named `thumbs.ts`'s two stamped `overwrite: true` puts, allowlisted with the reason. Measured at step 31 on the node-server build (`integrator.md` section 18): the twins row is 0 of 16 re-fetched with 0 revalidations (b3.md R11 met on the local build), the text burst reaches the local commit and the acknowledgement 97 ms after the last keyup with the clone carrying the text at once, the stream connections are 0 a minute; against that, the filmstrip's three frame rows read 100, 67 and 84 ms against 100, 20 and 50 (the day 0 machine baseline read 17, 10 and 17 ms on the round three build), the DOM counts after GC are 5,833 on the filmstrip, 2,649 on `/edit` and 2,501 on `/deck` against 1,500 (2,148, 1,847 and 1,601 live elements), the idle poll makes 5 server function calls a minute against 4, and a new slide's acknowledgement lands 2,067 ms after pointerdown on the memory channel while the capture after the save and the scratch deck's home card take 5.7 and 5.4 s on a cold tmp store: the fixer round's rows for B4.

## B5 README, repository metadata and documents

Not started (day 2, from B1's merge; `README.md` after the README workflow finishes, 0.52). Nothing reassigned. From merge 1: b3.md R5 (`docs/hosting.md:341`'s `runSyncExport` is `apps/studio/src/editor/controller.tsx`; `docs/freeform.md`'s sentence about the `on(...)` table points at the controller); the verifier's finding 2 (JavaScript at 3,049 to 3,187 KB decoded on every route, `index-D6IA9RCO.js` at 1,202,643 bytes) for `docs/performance.md`, which also names `TURBOSLIDE_SESSION_SECRET` and `TURBOSLIDE_DOWNLOAD_SECRET` beside the node-server start command; B1's `icon-dark-*` reading for the README's `<picture>`.

Merge 2 (`b5.md`): docs/brand.md and docs/performance.md (new), docs/hosting.md section 12, docs/native.md, `packages/native/README.md`, the four skills' sentences, docs/README.md's rows, README.md per SPEC-4 section 5 (the lockup `<picture>`, the eight speed rows, the identity section, `<img>` tags) and docs/freeform.md line 130 landed as reported; R3 (the three `gh repo edit` commands) waits for Kevin's confirmation of the strings at the ship step (ruling 2), R5 is in AGENTS.md, R4 went to B2 (the page's rows agree with the README's numbers), R6 to the verifier. For the fixer round (b5.md section 4): docs/native.md's "What runs where" can now cite the preview's record (`describeBackends().selected` `typescript`, glibc 2.34, Node 24.19.0 from `/api/agent`'s `instance` block); docs/hosting.md and docs/performance.md take the merge 2 numbers when the verifier's run exists.

## Integrator

Day 0, merge 1 and merge 2 done as the tables above record; `build-4/integrator.md` holds the commands and their results (sections 6, 12, 18 and 19). Merge 2 applied every request of days 2 to 5 that fell in the integrator's files or in nobody's file, the verifier's requests 1 to 4 on the perf script, the tooltip audit's page list, the smoke's rows, the bundle ceilings, step 18's URL list, step 26's spec, the chrome lint's roots, steps 29 to 31 as real steps, step 30 for real, the preview with two smoke runs and the perf run on the preview. `pnpm check` is not 31 of 31 on this tree: steps 3 (the uncommitted `openapi[.]json.ts` header), 20 (the round three parity baseline), 21 and 26 (the round two and round three specs' failures listed in the check table), 25 (Docker's disk), 27 (the layout shift rows of round three plus four new `/decks` rows) and 31 (the js decoded and largest chunk rows, the `/deck` DOM count) are recorded failures with their owners in the check table and "Open requests". Next: the verifier's day 6 on the node-server build and the preview; the fixer round in the same merge order; the ship step (the `gh repo edit` commands after Kevin confirms the strings, `native.yml` under `.github/workflows/`, the addon if Kevin builds it, the commit per builder).

## Verifier

Day 0 is the verifier's baseline run (6.3) and the hash check of the two PP 9.5 files (0.49); recorded by the verifier in `build-4/verifier.md` and `verification-4/BASELINE.md` (58 of 121 budgets met on production, 73 of 121 on a `vite preview` with the tmp store), not here. The verifier's six requests on `scripts/perf-budget.mjs` and step 31: 5 (the stream pattern) is done since day 0; 6 (the placeholder secrets for a local run) is done in step 31's runner and recorded in AGENTS.md; 1 to 4 are the integrator's for merge 2 ("Open requests").

## Open requests

After merge 2 (`build-4/integrator.md` section 15 has every request with its application):

| Request                                                                                                                                                                                                                                                                                                                                               | From                                             | To                             | State                                                                                                                                   |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ | ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| R5: the continuous hero frame (`hero-frame.jpg` on `SITE`) for the pipeline strip's first cell                                                                                                                                                                                                                                                        | b2.md                                            | B1                             | open; the cell shows P1's 7.0 s frame with its caption                                                                                  |
| R10: the editor skeleton's grid before the route chunk; the fitted curtain shot on the built output                                                                                                                                                                                                                                                   | b1.md                                            | B3, B1                         | open; a check on the node-server build                                                                                                  |
| R8: the version log refreshed on panel open (`listVersions` once)                                                                                                                                                                                                                                                                                     | b3.md                                            | B4                             | open                                                                                                                                    |
| R7: the Linux x64 glibc addon (`cargo zigbuild` or the `native.yml` workflow)                                                                                                                                                                                                                                                                         | b4.md                                            | Kevin                          | open; the function runs the TypeScript stages (`instance.effectsBackend` `typescript` on the preview)                                   |
| R13 and R10: `@turboslide/native-linux-x64-gnu` in `apps/studio/package.json` and the deploy config's lines                                                                                                                                                                                                                                           | b3.md, b4.md                                     | the integrator                 | waits for the addon                                                                                                                     |
| R6: `native.yml` under `.github/workflows/`, `TURBOSLIDE_NATIVE_REQUIRED=1` on step 5                                                                                                                                                                                                                                                                 | b4.md                                            | the ship step                  | `.github/` is untracked and outside the round's files                                                                                   |
| R3: the three `gh repo edit` commands of docs/brand.md section 15                                                                                                                                                                                                                                                                                     | b5.md                                            | the ship step                  | after Kevin confirms the strings (ruling 2)                                                                                             |
| The vendor chunk group and the 914 KB entry chunk; the schema table out of the root graph                                                                                                                                                                                                                                                             | the integrator (section 17)                      | the fixer round                | the `/home` and `/decks` js decoded rows and the largest chunk ceiling miss by construction until then                                  |
| The render route's anonymous principal cookie on thumbnail answers (the CDN never caches them)                                                                                                                                                                                                                                                        | the integrator (section 19)                      | B4 and the identity runtime    | finding, open                                                                                                                           |
| The filmstrip frame rows on the node-server build (first pass 100 ms, steady p95 67 ms, longest 84 ms against 100, 20 and 50; the day 0 machine baseline read 17, 10 and 17 ms) and the DOM counts after GC (filmstrip 5,833, `/edit` 2,649, `/deck` 2,501 against 1,500)                                                                             | step 31 (`integrator.md` 18)                     | B4, the fixer round            | open; b4.md R14's note on the 200 percent margin stands, and the frame rows are the round's one regression against the machine baseline |
| `decks->edit` in page 820 ms against 300 on the node-server build with 47 cards (228 ms on B3's dev server)                                                                                                                                                                                                                                           | step 31                                          | B3, the fixer round            | open                                                                                                                                    |
| The idle poll at 5 server function calls a minute against 4 (the 0.37 interim's 2 s pause)                                                                                                                                                                                                                                                            | step 31                                          | B4, the fixer round            | open                                                                                                                                    |
| A new slide's acknowledgement at 2,067 ms on the memory channel (the text burst's at 97 ms); the capture after the save 5.7 s and the home card 5.4 s on a cold tmp store; the card paint 17 ms against 16                                                                                                                                            | step 31                                          | B4 and B3, the fixer round     | open; the local profile's "saved" is the acknowledgement since merge 2 (`perf-budget.mjs` header)                                       |
| On the preview: `/` and `/new` cold LCP 1.7 to 1.8 s with a 1.2 to 1.6 s longest animation frame (the node-server build reads 64 to 163 ms and a 52 ms frame); `/deck` cold LCP and ready 1.2 and 1.3 s against day 0's 0.4 and 0.6 s (the deferred slides as escaped JSON); the `'data-only'` routes' first byte 270 to 556 ms; `home->new` 1,079 ms | the perf run on the preview (`integrator.md` 19) | the verifier (day 6), B3       | open; one afternoon's sample of three per route                                                                                         |
| On the preview: `/decks` cold ready 2.1 s and warm 0.9 s against 1.0 and 0.6 s (day 0: 5.7 and 6.5 s); `/decks/trash` cold 1.1 s                                                                                                                                                                                                                      | the perf run on the preview                      | B4 (the list), B3 (the loader) | open; the `blob` tier list and the streamed shell cut the page by four to seven times                                                   |
| The Layout grid plate on Escape (finding 9)                                                                                                                                                                                                                                                                                                           | verifier.md                                      | B1 or fixer                    | open                                                                                                                                    |
| `editor.spec.ts` row 7 (finding 7), `realtime.spec.ts` test 4                                                                                                                                                                                                                                                                                         | b3.md, merge 1                                   | the fixer round                | pre existing, open                                                                                                                      |
| Step 19 on the other workflow's `research-5/`, `design-5/` and `SPEC-5.md`                                                                                                                                                                                                                                                                            | the check                                        | the other workflow             | closed: committed formatted as `e2904d5`; step 19 is green on the whole tree                                                            |
| Four `L5` layout shift rows on `/decks` at 0.0003 to 0.0004 (the streamed list under `Suspense`)                                                                                                                                                                                                                                                      | step 27 (`integrator.md` 18)                     | B3, the fixer round            | open; round three's 71 rows stand as recorded (VERIFICATION-3 finding 26)                                                               |
| `/home` has no cell in `layout-shift-audit.mjs` (round three's route list)                                                                                                                                                                                                                                                                            | step 27                                          | the verifier, B5's script      | open; the `/home` CLS is asserted by the perf budget's `cls` row instead (0.0000 on the node server)                                    |
| The `/new` boot probe's last row passes on the optimistic card removal and can leave the deck on disk                                                                                                                                                                                                                                                 | `integrator.md` 20 item 13                       | the orchestrator's probe       | open; the probe is round three's script and was not changed                                                                             |
| `decks/e2e-realtime-mu1opv3t/` left by step 26's `realtime.spec.ts` on the file store                                                                                                                                                                                                                                                                 | the check                                        | closed                         | removed by hand at the close (`integrator.md` section 21); `git status -- decks/` empty                                                 |

## Deviations recorded so far

`build-4/integrator.md` section 7: the `@vercel/functions` pin, the theme's effects dependency, the tracked wasm folder, the environment gates of steps 30 and 31, the perf script's `/home` landmark and result keys, the output check's failing rule, the facts the tree contradicts, the orchestrator's rulings, the untracked `research-5/` and `verification-4/`, and the cached build times of the day 0 gates.

Merge 1 (`build-4/integrator.md` section 13; AGENTS.md "Round four, merge 1"): the reconstruction of the interrupted run from the tree; `Thumb.capture?` typed in B4's file on the seams' pre-authorization; step 30's `TURBOSLIDE_CHECK_VERCEL=0` opt out while the gate is open and the assertions wait for merge 2; the probe on the file store under the e2e lock; the forced `tsc -b` as the typecheck claim; the Playwright rows of b3.md finding 1 not run at merge; the `import()` rule recorded with four allowed places and none landed; b3.md finding 4 left to the owners; step 18 run beyond the named gates; step 19 still seeing the other workflow's untracked documents. B1's readings (`b1.md` 1.2) and B3's (`b3.md` section 3: the `recordDeckOpened` call sites in `EditorRoot.tsx`, the CSS import kept in the route file) are recorded in AGENTS.md's paragraph.

Merge 2 (`build-4/integrator.md` sections 17, 19 and 20; AGENTS.md "Round four, merge 2"): the step numbering kept to SPEC-4 6.1; the largest chunk ceiling reported until the vendor group lands (SPEC-4 0.27), the vendor group dead inside the Vite 8.2.2 build with the probes recorded; `thumbs.ts` in the overwrite allowlist; the formatting pass over this round's records and the round three verification records, the other workflow's files restored from `HEAD`, the verifier's untracked `verification-4/` files reformatted and not restorable; `.vercelignore` and `.prettierignore` edited with notes; the wasm glue regenerated; the preview's `TURBOSLIDE_PUBLIC_STORE_HOST` read from the public CSP header; the `instance` block on `/api/agent`; the smoke's rows corrected for a Vercel deployment (the platform's noindex on previews, the config level redirect, the rewritten `cache-control`, the public store's 302); the perf check's four metric changes; the addon, `native.yml`, the `gh repo edit` commands and the fixer round items left as listed above.

## The fix round and the ship step

Day 7 and day 8, 2026-09-14 (MILESTONES-4 "Fixer round" and "Ship step"; `build-4/ship.md` is the ship step's note, VERIFICATION-4.md the verifier's two passes and, in 16.1, the production table after the ship). The round's main commit `43707c3` shipped on day 6 ahead of the verifier's pass with the tree after merge 2; hotfix 2 (`cb646e9`, `02e92c6`), B4's poll cadence (`720ccb4`) and hotfix 3 (`1e78132`, `6679c54`, `04d95c2`) shipped on day 7 beside the fixer round; the fixer round itself and the verifier's record shipped on day 8 as `2ee1db4` ("Turboslide round four: the fix round, the verifier's record and the entry chunk under its ceiling"), the second commit `43707c3`'s message promised, and the production table as the commit after it.

| Item                          | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The fixer round               | the integrator's (findings 1, 2, 3, 8, 9, 10, 11, 13; `integrator.md` section 22): the render walk out of the root graph and the `/home` head out of the entry, so the entry chunk is 577,949 bytes against 600,000 (was 914,233); the controller's answer at the acknowledged revision; the production numbers on `/home`, the README and `docs/performance.md`; the default view words on the built output; the perf script's `home->new` and twins rules; B3's (findings 4, 6, 7; `b3.md` section 8): the banners in the status row, the trashed banner on a warm instance, the `/deck` document at 179 KB with the slides fetched after the mount, the `'data-only'` first byte measured and handed to Kevin; B4's (findings 5, 12; `b4.md` section 7): `content-visibility: auto` off the windowed cards, the poll cadence |
| The verifier's pass 2         | VERIFICATION-4.md section 1: the tree ready for the ship step's commit; `pnpm check` 24 of 31 with every failing step a recorded state; the deployment profile 113 of 157 on the preview; findings 21 to 24 new; the decisions for Kevin in section 12                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| The ship step's chain         | `ship.md` section 3: 22 of 31 steps ok on the tree that ships; steps 3 (by construction), 5 (three 5 s timeouts under load; the files pass in isolation), 19 (another agent's untracked file only), 20 (2,979 / 5 / 385, round three's five rows), 21 (58 / 7 / 6), 25 (Docker's disk), 26 (25 / 6 / 18), 27 (66 cells against 75, 0 gained) and 31 (128 of 151 budgets met) each for the recorded reason; another agent's in flight edits reached the checkout from 22:28 PDT and are recorded in `ship.md` section 5                                                                                                                                                                                                                                                                                                          |
| The commit                    | `2ee1db4`, 192 files by explicit path list: the fixer round's source and tests, the verifier's record (`VERIFICATION-4.md` and 128 files under `verification-4/`), the check's rewritten records under `verification-3/`, the ship step's chain record, the fixer notes, the two hotfix notes formatted for step 19, `.gitignore` (`__pycache__/`), the README and `docs/performance.md` sentences that named the pre fix entry chunk in the present tense; `controller.tsx` staged as the fixer round's content alone (`ship.md` section 4). Committed tree Prettier clean (`check-ship/prettier-committed-tree-2026-09-14.txt`); `generate:contracts` current                                                                                                                                                                 |
| Production                    | `dpl_9qwLq7hWVbdg99uo34NAzrZ9naVg` (`turboslide-42f8yn887`), the git deploy of `2ee1db4`, aliased to `https://turboslide.vercel.app` at 23:05 PDT: the smoke 37 of 37, the `/new` write probe all 27 rows, the perf budget 119 of 151 (pass 2's production 93), the head and the static layer right, `/home` at 1440 and 390 in both appearances, the selection blue on a dark slide of a scratch deck (VERIFICATION-4.md 16.1)                                                                                                                                                                                                                                                                                                                                                                                                 |
| The repository metadata       | the description set to `docs/brand.md`'s sentence; the four topics refused by GitHub's cap of 20 (the repository holds 20; ruling 2 allows no removal): Kevin's choice among `docs/brand.md`'s fourteen removals; the homepage and the social preview stay Kevin's manual steps                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Open for Kevin and round five | VERIFICATION-4.md section 12 (the manual checks, the addon, the Marketplace installs, the decks on production, the five ceiling decisions of item 8), findings 21 to 24, `ship.md` section 6 (`text-styles.spec.ts:203` once in step 21) and section 8 (the deviations: the commit subject, the lock, `--facts`, the preload ceilings tail); the other workflow's hotfix 4 (`build-4/hotfix-4.md`, in flight at the close) and its files, none of them in the round's commits                                                                                                                                                                                                                                                                                                                                                   |
