# Google Slides parity round five, verification

The verifier's record of the Google Slides parity round five (`docs/gslides-parity/SPEC-5.md` and its binding amendments `SPEC-5-amendments.md`; section numbers below refer to SPEC-5 unless prefixed A, SPEC, SPEC-2, SPEC-3 or SPEC-4; the build plan is `MILESTONES-5.md`; the day 0 baseline is `verification-5/BASELINE.md`), pass 1 after merge 2, written 2026-09-15 (PDT) on the shared checkout at `/Users/kevinliu/repos/Turboslide`: `main` at `d5a1be5` (`BASE`) plus the uncommitted working tree of the seven builders and the integrator (331 tracked files changed against `BASE`, 606 porcelain rows). Nothing in the tree was edited by the verifier except the files this role owns (`scripts/gslides-parity-audit.mjs`, this document and `docs/gslides-parity/verification-5/`); no git write command ran; the other lanes' untracked `.github/` and the mockup PNGs under `design-4/*/previews/` were not touched. No report was trusted: every claim below was rerun here and the sentence says where the number comes from. A step a script could not drive is written as not driven, never as passed.

Every number here comes from this machine (Apple M5 Max, macOS 26.4, Node 24.13.0, Chrome for Testing 147.0.7727.15 through `packages/headless` and `playwright-core` 1.62.1; Docker 29 with a full VM disk; the fonts venv under `.turboslide/venv` and the Prototemplate checkout present; `soffice`, Inkscape and Figma absent on the host; the load average between 4 and 12 while the integrator's chains ran beside this pass) or from the integrator's second preview deployment of the merged tree, `https://turboslide-ho9rltx7u-kl01s-projects.vercel.app` (the final tree at 14:31, `vercel deploy --yes --archive=tgz`; the production tiers: `TURBOSLIDE_REALTIME=blob`, `TURBOSLIDE_AUTHORIZE=shadow`, `TURBOSLIDE_MAIL=off`, no `DATABASE_URL`, anonymous principals, the one public Blob store shared with production). The preview sits behind Vercel Authentication: every probe carried `VERCEL_OIDC_TOKEN` from the integrator's `vercel env pull` (kept in the session scratchpad) as `x-vercel-trusted-oidc-idp-token`, and the bearer rows carried the agent bearer of `~/.config/turboslide/hosts.json`, both read in process and never printed. This lane's own dev server ran on 4357 (the tmp store, the memory realtime tier, fake secrets) inside the lock chain alone; the check chain ran its own servers on 4321 and 4344 and stopped them. Every Playwright and headless Chromium run of this pass ran under `.turboslide/e2e.lock`, taken with `until mkdir` behind the integrator's two chains (the editor walk, the smoke and the perf run against the first preview, then `check.mjs --from 20`) and released by this lane's chain. Kevin's directives of the round, verbatim: "using it is very hard, changes keep messing up, its some youre at revision 2 vs revision 12 despite being yourself, then you can see yourself editing it even though it's you in the slide, fix"; "expanding and shrinking doesnt really work, the scaling isnt going super well. and fix the revision out of dateness on our own browser instance lol, this system needs to be reworked to be a lot better and accurate. add more font options too"; "keep going on all of these and dont stop until literally all google slides features are supported with full agent queryability and editability esp on locals"; "we must, must must be able to drag and move around ANYTHING, including backgrounds and shaders but all text in the same exact way the google sldies is like a canvas"; "make sure its multiplayer too with all collab features"; "add more features like being able to add images in background and apply dithers over those"; "remember this is actually going to be used by majority sales in our org".

## 1. Verdict

Pass 1 closes with the chain still running (the orchestrator closed the session at 16:35 PDT while this lane's lock chain was at the editor walk on the preview, row 490 of about 800; the remaining steps of `verification-5/scripts/run-chain.sh`, then `run-dev-phase.sh` and `run-walk2.sh`, write their logs and tables under `verification-5/logs/` and `verification-5/` as they finish and release the lock themselves). What this pass establishes: the merged tree typechecks (`tsc -b --force` exit 0) and its unit suites pass alone except B4's five files; the round five surfaces of B1, B2, B3, B5, B6 and B7 are in the tree and open on the preview; the fresh write probe is 29 of 29 on the blob tier and the sync stress probe 16 of 20; the import fidelity table is complete; the perf run is 99 of 201 with the client weight over its ceilings on every route. What blocks the round: B4's days 3 to 7 are absent (finding 1); every hosted render through the CLI child fails on the deployment with the Temml file missing from the bundle (finding 2), so Download as PowerPoint, PDF and fresh thumbnails are broken hosted; a chat message is never listed on the blob tier (finding 3); the `instanceof BlobExistsError` catches do not hold in the deployed bundle and a poisoned instance answers 500 to every route (finding 11); and this lane's own preview walk removed the seed deck from the shared store for nine minutes and left production's warm instances poisoned (finding 0, the incident and its restore). The ship conditions of 16 are not met; the fixer round has findings 1 to 5 and 11 to 14 to close, and the ship step must redeploy production or wait for its instances to recycle before any smoke against it is read.

## 2. `pnpm check` (16.7, the 37 steps)

The full chain (`node scripts/check.mjs` continued after every failing step, section 3 of `run-chain.sh`) had not started when this pass closed: it runs after the preview items of the same chain and writes `verification-5/logs/check-from-<n>.log` per segment; `node docs/gslides-parity/verification-5/scripts/tables.mjs check` builds this table from those logs. Read before the chain: steps 1, 2 and 4 ok (1.6, 4.9 s; `tsc -b` exit 0), 3 FAIL as written (the uncommitted contracts, finding 9), 5 red on the merged tree (B4's five files, finding 4; the chrome, studio, agent, mcp, spelling and cli suites pass alone: 564, 280, 247, 12, 19 and 4 tests), 19 ok (71.8 s, the tree formatted), 25 FAIL (the Docker VM disk, finding 8), 28 ok (0 advisories at high), 29 ok (4 outputs match `BUILD-RECORD.json`, the Linux addon pending), 32 and 36 gated (`not yet`, 1 and 2 files pending), `--list` 37 rows. The integrator's own run of steps 6 to 20 at 14:06 to 14:16 read 6 to 19 ok and 20 FAIL before the audit's `visibleMenus` edit (`scratchpad/check-chain.log`).

## 3. The parity audit (16.1, step 20)

The audit script is the verifier's (`scripts/gslides-parity-audit.mjs`). This pass added the round five phase `roundFive` (the function `checkRoundFive`, 2026-09-15): one row per planned row of 14.1 (`GS5_PLANNED_EFFECTS`, the row is `now` or a live toolbar control, each miss naming its lane), the Later count at 2 leaves and every Omit row with a reason, the labels the research marks unverified listed without failing, the Google shortcut rows of round five out of `OMITTED_SHORTCUTS`, and the behaviour rows: the Motion panel's six entry points, controls, eight transition kinds and fifteen animation labels in Google's order and Apply to all slides writing every slide; Insert > Audio's two tabs and Insert > Video's three with the search tab disabled under its clause; the Templates and Building blocks panes' categories; the gallery page's three headings; Import slides with Keep original theme unchecked and the Upload tabs accepting `.pptx`; Page setup, the print dropdown's seven rows, the ODP and SVG rows; Preferences' two tabs and the Substitutions table; File > Language's submenu; the spell check card's four buttons; the Dictionary panel; the Accessibility menu under the toggle; Insert > Equation with its chord, the toolbar toggle and the six dropdowns; Edit theme's mode, toolbar controls and layout rows; the Themes panel's three groups and Import theme; Join chat's panel and its first line; the Font dropdown against `font.list`. The integrator's merge 2 edit (`BAR_MENUS = visibleMenus(DEFAULT_MENU_CONTEXT)`, 10.4 item 9) stands.

Static readings of the model on this tree (`node -e` over `packages/chrome/src/menus/model.ts` and `keys.ts`, 15:00): 5 Later leaf rows (`file.email.collaborators`, `file.download.odp`, `file.download.svg`, `file.pageSetup`, `tools.activityDashboard.viewers`) against 14.2's 2; 23 Omit rows, 0 without a reason; 4 rows carry `unverified: true` (the Accessibility menu's `accessibility.nextFormattingChange` "Next formatting change", `accessibility.previousFormattingChange` "Previous formatting change", `accessibility.nextSlide` "Next slide", `accessibility.previousSlide` "Previous slide"); `OMITTED_SHORTCUTS` still holds 27 rows and `GS5_KEY_ROWS` names 18 Google rows; the equation chord is `Cmd+Option+Shift+E` and the Motion panel's `Cmd+Option+Shift+B`.

The round five phase's first run against this lane's dev server on 4357 (15:23) found no server (the chain started the server with a relative log path after a `cd`, so it never came up); the rerun is `run-dev-phase.sh` (queued behind the lock; its table lands at `verification-5/parity-audit-round-five-dev-4357.json`, its log `logs/parity-round-five-dev.log`). The full audit against the second preview (`--out verification-5/parity-audit-preview.json`) and check step 20 on the node-server build run later in the chain. Read statically on the tree before the runs: the flip rows of B4 (`file.pageSetup`, `file.download.odp`, `file.download.svg`) fail, every other planned row of 14.1 is `now` or a live toolbar control; the Later count row fails (5 leaves against 2); the Omit rows all carry a reason; the four unverified labels are listed; five planned Google shortcut rows are still in `OMITTED_SHORTCUTS` (finding 7).

## 4. The motion walk in two browsers and the seven scenarios (16.2, 16.8, A7)

The seven scenarios and the two browser motion walk were driven on the second preview by this lane's script `verification-5/scripts/preview-walk.mjs` (a Sales pitch copy through `deck.create --from sales-pitch`, the product's own menus, dialogs and panels at human speed, one screenshot per step under `verification-5/preview-walk-shots/`, the tables `preview-walk.json` and `preview-walk-2.json`). The first run (15:46 to 16:01, 58 rows: 16 ok, 39 failed, 3 not driven) is the one that removed the seed deck (finding 0); most of its misses were the script's own assumptions, listed row by row below and corrected for the second run (`run-walk2.sh`, queued behind the lock; its rows are marked "run 2" below once it has run). A row that fails in both runs for a product reason names its finding.

| Scenario | Step                                                                                                                                 | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Evidence and screenshots                                                                                                                                                    |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S1       | File > Open, the Upload tab, a `.pptx` chosen, the editor moves to the new deck, the Import report says what changed in one sentence | run 1 not driven (the walk chose the file and never pressed Open; the dialog's Upload tab reads "A Turboslide bundle (.zip) from Download or the command line, or a PowerPoint file (.pptx)" and its input accepts `.zip,.pptx,application/zip,application/vnd.openxmlformats-officedocument.presentationml.presentation`, so the refusal sentence is gone and `.pptx` is accepted); run 2 presses Open (`dialog.open.ok`) and reads the report                                                                                                                                                                                                                                                                                                                                                                       | `01-S1-open-dialog-upload-tab.png`, `02-S1-import-report.png`; the hosted bundle route imports `01-text.pptx` (the smoke's `pptx import` row, 201, 2 report rows, 3 slides) |
| S2a      | Insert > Video has three tabs; the demo clip is a poster root on the Product demo slide                                              | tabs Search YouTube, By URL, Upload (the first disabled with its clause); the poster root `.ts-media[data-media="demo"][data-kind="video"]` draws Turboslide's frame with the play glyph and "The product demo clip"; its `data-src` and `data-duration` are empty in the editor although `media.list` answers the asset (`assets/demo.cc0aef83.webm`, 2,840 B, 5,000 ms, VP9): finding 12                                                                                                                                                                                                                                                                                                                                                                                                                            | `03-S2-demo-slide-poster.png`, `04-S2-insert-video-dialog.png`                                                                                                              |
| S2r      | a `Range` request on the copy's clip answers 206; the media grant                                                                    | ok: `GET Range: bytes=0-99` 206 `content-range: bytes 0-99/2840`, `accept-ranges: bytes`, `video/webm`, `public, max-age=31536000, immutable`; `HEAD` 200 length 2840; the grant `GET /api/x/upload/media` with the bearer 403 `{"error":"forbidden"}` (the route is the browser's; not driven with a session)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | the row's evidence in `preview-walk.json`                                                                                                                                   |
| S2b      | the show plays the clip on the next click; the presenter console shows what is playing                                               | FAIL: the Product demo slide has `data-steps="0"` in the show (`motion.compile product-demo` answers `steps: []`, the template's slide carries no `playMedia` animation and the compiler derives none from `playback.start: 'click'`), so ArrowRight moves to the next slide and nothing plays; the presenter console showed no media row: finding 13. Run 1 opened the console in a second browser context, which the `BroadcastChannel` channel cannot reach, so its console rows are the script's own; run 2 opens the console as a second page of one context                                                                                                                                                                                                                                                     | `05-S2-show-before-click.png`, `06-S2-show-after-click.png`, `07-S2-presenter-console.png`                                                                                  |
| S3a      | the pricing rows carry By paragraph; Slide > Transition, Fade, Apply to all slides writes every slide                                | the Motion panel opens from the toolbar's Transition button with "Slide Transition, Type None, Apply to all slides" and "Object Animations: Appear (On click) rows"; the document's `pricing.animations[0]` is `appear, click, byParagraph: true`; run 1's `selectOption('fade')` left the select at None and no slide changed (the panel's select did not take Playwright's change, or the write did not land): run 2 reads the select's value after the change and falls back to the `motion.transition` action, recording which half fails                                                                                                                                                                                                                                                                         | `08-S3-motion-panel-pricing.png`, `09-S3-apply-to-all.png`                                                                                                                  |
| S3b      | the show advances one pricing row per click                                                                                          | the show reads `data-steps="3"` on Pricing and the screenshots show Starter alone after the first click, Starter and Team after the second, all three after the third; run 1's hidden count read the wrong nodes (0 at every step), run 2 counts the rows' visibility                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `10-S3-show-step0.png` to `13-S3-show-step3.png`                                                                                                                            |
| S3c      | Left arrow reverses one step                                                                                                         | ok: step 3 to step 2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `14-S3-show-left.png`                                                                                                                                                       |
| S3d      | the PPTX carries the builds                                                                                                          | ok on the checkout: the Sales pitch's Editable text file holds one `p:bldP build="p"` and the embedded `a:videoFile` (6.2)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |                                                                                                                                                                             |
| M1       | the presenter and the audience share the steps through the channel; the pen stroke reaches the console                               | run 1 not driven: the console was a second context (the channel is `BroadcastChannel` plus `localStorage`, same context only; `motion.spec.ts` opens a second page of one context and passes in check step 33); run 2 uses one context                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `15-M1-audience-step0.png` to `20-M1-presenter-stroke.png`                                                                                                                  |
| S4       | Edit theme: the mode, Text and background 1 and Accent 1 set, Rename, every slide follows, the Themes panel lists the edited theme   | the mode enters with its toolbar (Background, Colors, Fonts, Insert placeholder, Rename, the close), the THEME tile "Plate" checked, the LAYOUTS tiles, the canvas and the right pane (Frame: Rails, Rules, Registration crosses, Inset; Corner slot: Mark; Counter: Show slide numbers, Side, Format; Chips); `theme.set /colors/light/ink #1a3d8f` answers revision 1 with the record; run 1 wrote "accent" (not a slot: the slots are `ink`, `paper`, `ink-2`, `plate`, `ok` (Accent 1), `warn`, `no`, `info`, `titanium`, `raised`, `link`) and renamed before the tab had the write ("The presentation moved to revision 1 while this change was on its way", the new wording of A3 item 8 on a base the script read too early); run 2 writes `ink` and `ok` in the deck's appearance and settles between writes | `21-S4-theme-mode.png` to `24-S4-themes-panel.png`                                                                                                                          |
| S4b      | a picture in the corner slot; the deck saved as a template of the organisation                                                       | not driven: no template save action exists in SPEC-5 13; the corner picture needs an asset in the copy                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |                                                                                                                                                                             |
| S5a      | File > Print settings and preview offers 3 slides per page with note lines on Letter                                                 | FAIL: the layout dropdown lists "1 slide without notes", "1 slide with notes" and the five Handout rows disabled; no Paper dropdown (finding 1, B4)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `25-S5-print-layout-dropdown.png`                                                                                                                                           |
| S5b      | Download as PowerPoint offers Editable text and Perfect                                                                              | ok: `dialog.download.mode.flatten` and `.native`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `26-S5-download-dialog.png`                                                                                                                                                 |
| S5c      | the Editable text file keeps the animations and the video; the Perfect file shows every object at rest                               | on the checkout (6.2; step 34's export of the template); the hosted export itself answers 500 on the preview (finding 2)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |                                                                                                                                                                             |
| S6       | Join chat from the title row; a message reaches the second browser within a second; the panel says nothing is saved                  | run 1 not driven (the walk opened the roster before the other person had arrived, and the Join chat row is drawn once one other participant is present); the hosted port never lists a sent message (finding 3, the smoke's row and the API probe), so the row cannot pass on the blob tier; run 2 waits for the other person before opening the roster                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `27-S6-error.png`                                                                                                                                                           |
| S7a      | `deck.setPageSize standard-4-3` keeps every object and the stage turns 4:3                                                           | run 1 not driven (the input key is `objects`, not `mode`); run 2 passes `objects: 'keep'`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `28-S7-editor-4-3.png`                                                                                                                                                      |
| S7b      | File > Page setup opens the dialog                                                                                                   | FAIL: the row is disabled with the tooltip "Page setup" and its Later clause (finding 1, B4)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `29-S7-file-menu-page-setup.png`                                                                                                                                            |
| S7c      | an imported 4:3 `.pptx` is 4:3 in the editor and in every export                                                                     | not driven: none of the five fixtures is 4:3; the 4:3 fixture deck's Editable text export is read in 6.2 (`p:sldSz` 10 by 7.5 in)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |                                                                                                                                                                             |

## 5. The import fidelity table (16.3, step 34)

`node apps/cli/bin/turboslide.mjs import <fixture> --dry-run --json` over the six fixtures of `packages/import/src/__fixtures__/pptx/` on the merged tree (15:20), and `cd packages/import && vitest run`: 18 files, 201 tests passed, the round trip gate among them (fixture 05 exported and imported again adds no report row, every position within 1 px is the suite's assertion; the python oracle ran through the venv). The table is `verification-5/import-fidelity.json`; the producer inbox `.turboslide/inbox` holds 14 notification records (`anon_*.json`) from round four and no `.pptx`, so the producer rows are empty. No fixture reports a visual fidelity number (the reader reports counts and rows). Every fixture's source page is 1600 by 900; none is 4:3, so S7's imported 4:3 file is not driven (section 4).

| Fixture                       | Producer                    | Slides | Imported | Substituted | Dropped | Rows by status and code                                                                                                                                                                                                                                                                  | Fonts (runs)                                                                                                                                                                                                                                       |
| ----------------------------- | --------------------------- | ------ | -------- | ----------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `01-text.pptx`                | python-pptx                 | 3      | 8        | 0           | 0       | kept `slide.hidden` 1, kept `theme.layouts` 1                                                                                                                                                                                                                                            | Calibri 27, every run in Inter                                                                                                                                                                                                                     |
| `02-shapes.pptx`              | python-pptx                 | 3      | 27       | 3           | 1       | kept `shape.strokeWidth` 4, kept `group.nested` 1, kept `theme.layouts` 1, substituted `shape.fill` 1, `shape.effect` 1, `shape.custom` 1, dropped `shape.hidden` 1                                                                                                                      | Calibri 6                                                                                                                                                                                                                                          |
| `03-pictures-tables.pptx`     | python-pptx                 | 2      | 10       | 2           | 0       | kept `background.picture` 1, `table.border` 1, `table.style` 2, `theme.layouts` 1; substituted `picture.format` 1, `table.columns` 1                                                                                                                                                     | Calibri 56                                                                                                                                                                                                                                         |
| `04-charts-motion-media.pptx` | python-pptx                 | 4      | 17       | 5           | 0       | kept `transition.advance` 1, `equation.mathml` 1, `theme.layouts` 1; substituted `chart.kind` 4, `chart.truncated` 1                                                                                                                                                                     | Calibri 7                                                                                                                                                                                                                                          |
| `05-roundtrip.pptx`           | Microsoft Office PowerPoint | 28     | 136      | 10          | 28      | kept `master.chrome` 399, `table.margins` 29, `table.border` 2, `background.picture` 2, `chart.slices` 1, `theme.layouts` 1; substituted `text.runSize` 1, `shape.custom` 3, `list.glyph` 4, `list.scheme` 2; dropped `shape.hidden` 28 (one hidden shape per slide, the exporter's own) | GT Inter Text 22 (56), GT Inter Display (30), GT Inter Text 20 (21), GT Inter Text 24 Medium (18), GT Inter Text 26 (12), GT Inter Text 20 Medium (9), GT Inter Text 22 Medium (4), GT Inter Text 14 (2), GT Inter Text 15 (1), every run in Inter |
| `05b-roundtrip-perfect.pptx`  | Microsoft Office PowerPoint | 28     | 113      | 7           | 28      | kept `master.chrome` 28, `perfect.raster` 28, `theme.layouts` 1; substituted `text.runSize` 1, `list.glyph` 4, `list.scheme` 2; dropped `shape.hidden` 28                                                                                                                                | the same nine GT Inter faces                                                                                                                                                                                                                       |

Every report validates (`validation.ok: true`, 3 to 40 issues by fixture); the theme mode is `adopt` with the scheme read (Calibri as major and minor for the python-pptx files). `docs/import-pptx.md` names the substituted and dropped classes (B3's file; read, not rerun). The hosted import row of the smoke passed on the preview (section 10: `01-text` through the bundle route, 201 with 2 report rows and 3 slides, trashed and removed), and S1 in section 4 drives the same path through File > Open.

## 6. The exports (16.4) and the files opened

### 6.1 The container legs

Docker 29 is installed and running, but the Docker VM's disk is full: `docker system df` reads 150 images at 100.8 GB (56.23 GB reclaimable), 14 containers at 5.07 GB and 18 volumes at 1.19 GB, and `docker build -f docker/render-worker.Dockerfile` stopped at `COPY . .` with `write /docs/gslides-parity/design-4/type/previews/home-light.png: no space left on device` (check step 25, `verification-5/logs/docker-legs.log`, 14:41:12 to 14:41:30, exit 1). Round three recorded the same disk (VERIFICATION-3 section 12 item 9); nothing was pruned (the orchestrator's ruling 3: Docker never prunes anything). The only image present, `turboslide-render-worker:latest` (`cacc939f8245`, built 2026-09-13 00:08, round three's code), does not know the round five commands (`turboslide export decks/fixture/motion --mode native --verify` answers `no slide "decks/fixture/motion" in deck.json sections` in it), so no container leg of 16.4 ran: not the motion fixture with `--verify`, not `soffice --convert-to odp` and the `content.xml` read back, not the ODP writer's files through `soffice --convert-to pdf`, not the SVG fixture rasterised, not the handout PDF's page count, not the 4:3 fixture, not the equation fixture's `a14:m` count, not the stills leg. The commands, for Kevin once the VM disk is freed (or a prune accepted), from the repository root:

```
node scripts/check.mjs --only 25
docker run --rm -v "$PWD/.turboslide/container:/work" turboslide-render-worker turboslide export decks/fixture/motion --mode native --verify --out /work/v5/motion-native
docker run --rm -v "$PWD/.turboslide/container:/work" turboslide-render-worker sh -c 'cd /work/v5/motion-native && for f in *.pptx; do soffice --headless --convert-to odp --outdir /work/v5/motion-odp "$f"; soffice --headless --convert-to pdf --outdir /work/v5/motion-pdf "$f"; done'
docker run --rm -v "$PWD/.turboslide/container:/work" turboslide-render-worker turboslide export decks/fixture/equation --mode native --verify --out /work/v5/eq-native
docker run --rm -v "$PWD/.turboslide/container:/work" turboslide-render-worker turboslide export decks/fixture/page-4-3 --mode native --verify --out /work/v5/p43-native
docker run --rm -v "$PWD/.turboslide/container:/work" turboslide-render-worker turboslide export pdf decks/fixture/page-4-3 --verify --out /work/v5/p43-pdf
```

The image's LibreOffice is 25.2.3.2 (`soffice --version` inside it), so the ODP oracle exists once the image rebuilds. `soffice` is not installed on the host (`which soffice` empty), so no host side ODP or PDF conversion ran either. The ODP writer itself (`packages/export/src/export-odp.ts`) and the SVG writer (`packages/export/src/svg/write.ts`) are not in the tree (B4), so the ODP and SVG legs have no file to convert even with the container (section 13, finding 1).

### 6.2 The checkout exports

Every command from the repository root on the merged tree; the times are wall clock on this machine.

| Command                                                                                                                                                                                                | Result                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `turboslide export pptx all --mode native --theme light --deck decks/templates/sales-pitch --out <scratch>` then the zip read                                                                          | exit 0; the file carries 1 `p:bldP build="p"` (the pricing rows), 1 `a:videoFile` with `ppt/media/media-cc0aef83.webm` (the demo clip embedded), 0 `p:transition` (the template ships without a transition; S3 applies Fade on the preview copy); the same numbers B3 read at merge 2                                                                                                                                    |
| `turboslide export pptx all --mode native --theme light --deck decks/fixture/page-4-3 --out <scratch>` then the zip read and `export check`                                                            | exit 0; `ppt/presentation.xml` carries `<p:sldSz cx="9144000" cy="6858000"/>` (10 by 7.5 in, the 4:3 page); `export check` answers `INVALID` with `page size 9144000 by 6858000 EMU, expected 12192000 by 6858000`: the check has no `--page WxH` flag (`apps/cli/src/commands/export-check.ts` reads none), so step 32's `export check --page 1200x900` cannot pass as written (finding 5, B4)                          |
| `turboslide export pdf decks/fixture/gslides --layout handout-6 --paper letter --orientation portrait --out <scratch>`                                                                                 | `turboslide: no slide "letter" in deck.json sections`: the CLI does not take `--paper` (nor `--layout handout-6`) on this tree, B4's print options did not land (finding 1); step 32's handout command cannot run                                                                                                                                                                                                        |
| `turboslide render title --deck decks/gt-brand --format svg --text embed --out <scratch> --json`                                                                                                       | `turboslide: no slide "embed" in deck.json sections`: no `--format svg` on the render command (B4's SVG writer absent), so no SVG file exists to open in Chromium, Inkscape or Figma (Inkscape and Figma are also not installed on this machine: `which inkscape` empty, no `/Applications/Figma.app`); the SVG rows of 16.8 are not driven                                                                              |
| the five family deck (A7): a copy of `decks/fixture/page-16-10` with five headings in `roboto`, `merriweather`, `playfair-display`, `jetbrains-mono`, `oswald` through `Typography.family`; `validate` | 3 slides, 0 errors, 0 warnings                                                                                                                                                                                                                                                                                                                                                                                           |
| `turboslide export pptx all --mode native --theme light --deck <five families> --out <scratch>`                                                                                                        | `passed: true`, 34 parts, 34 relationships; but `ppt/slides/slide3.xml` names `typeface="GT Inter Display"` 16 times and `"GT Inter Text 14"` 3 times and no Roboto, Merriweather, Playfair Display, JetBrains Mono or Oswald: the Editable text file does not name the families (A5 item 5, finding 4). The residual reads `fonts not embedded: GT Inter Display, GT Inter Text 26, GT Inter Text 14, GT Inter Text 22` |
| `turboslide export check <five families>-light.pptx` (with and without `--page 1440x900`)                                                                                                              | python-pptx reopens 3 slides and 17 shapes; `INVALID` with `page size 10972800 by 6858000 EMU, expected 12192000 by 6858000` in both runs (12 by 7.5 in is the right 16:10 size; the check cannot be told the page, finding 5)                                                                                                                                                                                           |
| the five family deck rendered, built as a standalone file and exported in Perfect mode (Chromium, inside the lock chain)                                                                               | not reached before this pass closed (section 4 of `run-chain.sh` writes `<scratch>/v5/fonts-deck/render.json`, `five-families.html` and `flatten.json`; the chain's timeline names their exit codes)                                                                                                                                                                                                                     |
| the motion fixture (`decks/fixture/motion`) and the equation fixture (`decks/fixture/equation`) in both modes with `export check`                                                                      | run by check steps 33 and 36 (section 2); B1's merge 2 line for the motion fixture (7 transitions, 40 effect nodes, 25 `bldP` with 1 `build="p"`, 1 `p:audio` and 1 `p:video`) and B6's for the equation fixture (19 `a14:m` in 19 `mc:AlternateContent` wrappers over 20 blocks) were not retyped here                                                                                                                  |

### 6.3 PowerPoint

The manual checklist of `docs/export-verification.md` extended as 16.4 says (the Transitions gallery, the Animation Pane, the media settings, the equation as native math, the edited theme, the 4:3 page at 10 by 7.5 in, no repair dialog) is Kevin's on a machine with PowerPoint (MILESTONES-5 "Ship step" item 3); no PowerPoint is installed here. The files for it are the Sales pitch export of 6.2 (1 build, 1 embedded video), the motion fixture's `motion-light.pptx` (check step 33's output under `.turboslide/motion-native/`), the equation fixture's file (`.turboslide/eq-native/`), the 4:3 fixture's file (`p:sldSz` 10 by 7.5 in) and the five family file (which names no catalog face, finding 4).

## 7. The layout shift gate (16.5, step 27)

`node scripts/layout-shift-audit.mjs --base <preview 2> --out verification-5/layout-shift-preview.json --report` runs in the chain after the editor walk and check step 27 runs it against the node-server build later; neither had run when this pass closed. The script gained the three new routes this round (`/decks/templates`, `/help/training`, `/help/updates`) and none of the driven rows 16.5 names (the Motion panel, the two media dialogs, the two panes, the gallery, the Import slides steps, Page setup, the print preview, Preferences, the spell check card, the Dictionary panel, the equation toolbar, the theme mode, the Themes panel's third group, the Chat panel): those stay not driven by the audit (the integrator's file); this lane's walk read the opening of the Motion panel, the Dictionary panel and the theme mode at zero layout shift entries in every cell (section 12). The day 0 baseline on production stands at 71 entries in 56 of 109 cells (`BASELINE.md` section 5), so 16.5's "stays at zero" is met on `/new` alone until the round decides the reading of the recorded state.

## 8. The perf budgets (16.6, step 31)

`node scripts/perf-budget.mjs --base https://turboslide-ho9rltx7u-kl01s-projects.vercel.app --profile deployment --runs 3 --write --json verification-5/perf-budget-preview.json` inside the lock chain, 15:39:40 to 15:45:59 PDT (`logs/perf-budget-preview.log`; the machine's load average 20 to 50 with this lane's own chain beside the run and the integrator's check chain waiting; the network the baseline's): 99 of 201 asserted rows met. The day 0 baseline on production read 114 of 151 (`BASELINE.md` section 4) and the integrator's run on the first preview at 15:15 read 114 of 201 (`.turboslide/perf-budget-merge2.json`). The full table, every asserted row with its ceiling and the baseline's value beside it, is `verification-5/perf-budget-preview-against-day0.md` (built by `scripts/tables.mjs perf`, nothing typed by hand). The `--write` scratch deck `untitled-20260915-pd17` was trashed and removed through the actions API right after the run (`logs/remove-perf-deck.log`: `deck.info` 200 at revision 3, `deck.trash` 200, `deck.remove` 200).

| Row (cold, medians of three)                                                   | Preview 2        | Ceiling           | Verdict | Baseline (production, day 0)    |
| ------------------------------------------------------------------------------ | ---------------- | ----------------- | ------- | ------------------------------- |
| `/` ttfb                                                                       | 136 ms           | 200               | ok      | 119 ok                          |
| `/` lcp                                                                        | 616 ms           | 700               | ok      | 440 ok                          |
| `/` ready                                                                      | 1,263 ms         | 700               | MISS    | 487 ok                          |
| `/` js decoded                                                                 | 2,557 KB         | 1,953 KB          | MISS    | 2,183 KB MISS                   |
| `/` largest chunk (`index-yTIYsqrw.js`)                                        | 709 KB           | 586 KB            | MISS    | 565 KB ok (`index-CYGrLim1.js`) |
| `/home` ttfb                                                                   | 47 ms            | 150               | ok      | 48 ok                           |
| `/home` lcp                                                                    | 392 ms           | 800               | ok      | 164 ok                          |
| `/home` ready                                                                  | 268 ms           | 800               | ok      | 110 ok                          |
| `/home` js decoded                                                             | 778 KB           | 586 KB            | MISS    | 663 KB MISS                     |
| `/home` images before ready                                                    | 40 KB            | 488 KB            | ok      | 40 ok                           |
| `/new` ttfb                                                                    | 155 ms           | 200               | ok      | 135 ok                          |
| `/new` fcp                                                                     | 528 ms           | 400               | MISS    | 236 ok                          |
| `/new` lcp                                                                     | 636 ms           | 700               | ok      | 268 ok                          |
| `/new` ready                                                                   | 1,053 ms         | 700               | MISS    | 342 ok                          |
| `/new` js decoded                                                              | 2,557 KB         | 1,953 KB          | MISS    | 2,183 KB MISS                   |
| `/decks` ttfb                                                                  | 262 ms           | 400               | ok      | 456 MISS                        |
| `/decks` lcp                                                                   | 1,072 ms         | 1,000             | MISS    | 1,252 MISS                      |
| `/decks` ready                                                                 | 959 ms           | 1,000             | ok      | 610 ok                          |
| `/decks` js decoded                                                            | 2,475 KB         | 586 KB            | MISS    | 2,102 KB MISS                   |
| `/decks/trash` ttfb                                                            | 495 ms           | 400               | MISS    | 348 ok                          |
| `/deck/gt-brand` ttfb                                                          | 419 ms           | 500               | ok      | 279 ok                          |
| `/deck/gt-brand` lcp                                                           | 908 ms           | 600               | MISS    | 412 ok                          |
| `/deck/gt-brand` ready                                                         | 1,319 ms         | 800               | MISS    | 507 ok                          |
| `/deck/gt-brand` js decoded                                                    | 2,083 KB         | 977 KB            | MISS    | 1,801 KB MISS                   |
| `/deck/gt-brand` dom nodes (1,621 live)                                        | 2,522            | 1,500             | MISS    | 2,503 MISS                      |
| `/edit/gt-brand` ttfb                                                          | 394 ms           | 200               | MISS    | 582 MISS                        |
| `/edit/gt-brand` fcp                                                           | 652 ms           | 400               | MISS    | 676 MISS                        |
| `/edit/gt-brand` lcp                                                           | 1,592 ms         | 1,200             | MISS    | 988 ok                          |
| `/edit/gt-brand` ready                                                         | 1,452 ms         | 1,200             | MISS    | 935 ok                          |
| `/edit/gt-brand` js decoded                                                    | 2,560 KB         | 1,953 KB          | MISS    | 2,186 KB MISS                   |
| `/edit/gt-brand` dom nodes (1,624 live)                                        | 2,231            | 1,500             | MISS    | 2,134 MISS                      |
| `/present/gt-brand` ttfb                                                       | 262 ms           | 200               | MISS    | 296 MISS                        |
| `/present/gt-brand` lcp                                                        | 648 ms           | 800               | ok      | 412 ok                          |
| `/present/gt-brand` ready                                                      | 955 ms           | 800               | MISS    | 583 ok                          |
| `/present/gt-brand` js decoded                                                 | 1,544 KB         | 1,172 KB          | MISS    | 1,334 KB MISS                   |
| `/decks/templates` ttfb, lcp, ready                                            | 44, 216, 151 ms  | 400, 1,000, 1,000 | ok      | no row (404 on day 0)           |
| `/decks/templates` js decoded                                                  | 1,189 KB         | 586 KB            | MISS    | no row                          |
| `/help/training` ttfb, lcp, ready                                              | 64, 504, 223 ms  | 150, 700, 700     | ok      | no row                          |
| `/help/training` js decoded                                                    | 709 KB           | 586 KB            | MISS    | no row                          |
| `/help/updates` ttfb, lcp, ready                                               | 63, 316, 224 ms  | 150, 700, 700     | ok      | no row                          |
| `/help/updates` js decoded                                                     | 709 KB           | 586 KB            | MISS    | no row                          |
| `/new`, `/decks` cold first byte, worst sample (the fresh instance gate, 16.7) | 156, 365 ms      | 1,200             | ok      | no row                          |
| every route cls                                                                | 0.0000 to 0.0021 | 0.05              | ok      | the same                        |

The other checks: transitions `decks->edit` 613 ms MISS (500; baseline 94), `back` 186 MISS (100; 30), `home->new` 833 MISS (300; 302, no prerender activation), `edit->decks` 34 ok, `trash->decks` 13 ok, `slideChange` 45 ok (50), `slideshow` 51 MISS (50), `layoutGrid` 39 ok; the filmstrip's first pass longest frame 100 ms MISS (at the 100 line), steady p95 17 ok (20), steady longest 91 MISS (50), fps 101 ok, DOM nodes with 85 cards 2,984 MISS (1,500; baseline 2,694); idle 2 function calls and 0 stream connections a minute ok; the twins 0 of 16 re-fetched ok; the seven `cdn` rows `HIT` ok; the field vitals endpoint collected 6 samples (LCP p75 472 ms on `/edit`, 980 on `/decks`, 316 and 328 on the help pages; CLS 0) and no INP sample yet, so the INP row reads n/a under its 200 ms ceiling; the write path: the text burst's last keyup to the saved revision 4,218 ms MISS (1,000; the blob tier's checkpoint cadence), the card clone after the commit 1,129 ms MISS (50), a new slide painted in 32 ms MISS (16) and saved in 2,285 ms MISS (500), the capture of the edited slide after the save `404` (finding 2, the render route's Temml file) and the home card of the scratch deck n/a for the same reason.

Reading. Three classes of miss. (a) The client weight, load independent and new this round: the entry chunk grew from 565 KB (579,024 B) at `BASE` to 709 KB (725,388 B in the build, over the 600,000 B ceiling of 16.6 and SPEC-4 3.12; `check-client-bundle` reports it, "no vendor chunk in the output", and does not fail) and `/edit` decodes 2,560 KB against the 2 MB ceiling (2,186 KB at `BASE`), `/decks/templates` 1,189 KB and the help pages 709 KB against the `/home` ceilings, so 16.6's "the round five features are lazy chunks" holds only in part (finding 10). (b) The DOM rows, unchanged from the baseline's classes (VERIFICATION-4 5.5). (c) The paint and ready rows (`/` and `/new` ready over 1 s, `/deck` and `/edit` LCP and ready, the transitions, the filmstrip frames) are worse than the baseline and than the integrator's run twenty minutes earlier on the same machine (its `/` cold ready 846 ms, `/edit` cold LCP 1,152); this lane's chain held Chromium beside the run and the load average sat between 20 and 50, so these rows are recorded with both numbers and stay misses until the fixer round's pass 2 measures them on a quiet machine (MILESTONES-5 "Fixer round": a row that fails on the deployment profile stays a failure until the fixer round closes it or Kevin accepts the ceiling). The first byte rows of the `data-only` routes (`/edit` 394 / 312, `/present` 262 / 309) are round four's recorded class.

## 9. Check steps 32 to 37 (16.7)

`--list` prints 32 as gated (`page-setup.spec.ts` pending) and 36 as gated (`export-odp.ts`, `svg/write.ts` pending), so both answer `not yet` with exit 0 and prove nothing (finding 1). The vitest groups of 32 to 37 pass alone on this tree (section 17: `schema canvas` 32, `render print-layout geometry motion theme-css equation` 95, `theme` 99, `export ooxml/timing ooxml/transition ooxml/media ooxml/ids ooxml/math` 63, `store media templates seed` 87, `viewer present` 54, `effects dither` 22, `schema autocorrect preferences` 69, `fonts` 19, `render fonts` 10, `import` 201, `spelling` 19), `check-dictionaries.mjs` ok. The Playwright rows of 33, 34, 35 and 37 and the export rows of 33, 34 and 36 run in the chain's check segment; the 32 commands cannot run as written even ungated (`export check --page`, `export pdf --layout --paper`, finding 6 and finding 1).

## 10. The hosted smoke rows and the boot probe (16.8)

`node scripts/hosted-smoke.mjs --base https://turboslide-ho9rltx7u-kl01s-projects.vercel.app --token-env TURBOSLIDE_TOKEN` with the OIDC token and the agent bearer in the environment (14:44, `verification-5/logs/smoke-preview2-fetch.log`; the fetch rows, no `--sync-probe`, no `--template-copy`): 42 of 43 rows pass; 7 rows skipped by name (the `/s/` exchange, the 410 after unpublish, the private document 403, the twin URL derivability, the restricted 404, the sync stress probe, the template copy; each needs a token, a store or a flag this run did not pass). The rows: `/` 307 to `/new` with `x-robots-tag: noindex`; `/new` 200 (75,063 chars, 3 of 3 shell marks, noindex; 7,071 ms on the cold function); `/deck/gt-brand` 200, 0 `notes` keys; `/edit/gt-brand` 200; `/decks` 200 with 28 cards; `/decks/trash` 200; `/print/gt-brand` 85 pages; `/present/gt-brand` 200; `/decks/templates` 200 with the gallery heading (55 ms, prerendered); `/help/training` 200 (64,362 chars); `/api/vitals` 200 with the endpoint's description; `/home` 200 with the three marks (the platform stamps `x-robots-tag: noindex` on every preview answer, reported); the cover twin 200 `image/png` 335,538 B; `/api/agent` 401 (the bearer rule); the headers of SPEC-3 8.8 on `/edit`, `/deck` and `/embed`; the JSON attachment; the cross site `text/plain` POST 415; the unsigned thumbnail 302 to the stored object (shadow mode); the CSP endpoint 204; the fifteen `cdn` rows `HIT` on the second request at the manifest's bytes; the thumbnail rows; `/help/updates` twice `HIT` then `HIT` (`public, max-age=0, must-revalidate`, the prerendered page); `effects backend: typescript; glibc 2.34; node v24.19.0 linux`; `pptx import` 201 (`smoke-pptx-nnfy` imported with 2 report rows and 3 slides, trashed 200, removed 200); `deck.info` 1 snapshot at revision 32. The one failure: `chat message` (`chat.send` 200 in 323 ms, then `chat.list` 200 with 0 messages, the sent one missing). Reproduced by hand at 14:46 with five `chat.list` calls over 8 s after a `chat.send` that answered `{"id":"chat_mu37cga7_…","at":…}` 200: every list answered `{"messages":[]}`, across three function instances (`x-vercel-id` `sr6g2`, `zqrh2`, `2gxsd`, `647d7`, `jcgx2`), and `chat.list {since: "2026-09-15T00:00:00.000Z"}` the same. The deck's revision did not move (`deck.info` reads 32 before and after). Cause, read in the tree: the hosted chat port (`apps/studio/src/server/actions.ts` 706 to 815) appends the chat entry with `appendWithRetry(room.channel, …)` and lists with `room.channel.since(deckId, from, head - from)`; on the blob tier `since` maps every stored record through `entryOfRecord` (`packages/realtime/src/blob.ts` 135 to 150), which writes `kind: 'edit'` for every record and carries no `chat` member, so `chatMessagesOf` (`packages/realtime/src/admission.ts` 341) filters every entry out. On the production tiers a chat message is accepted and never listed (finding 2, severity 3, S6).

The fetch rows this lane added on the second preview (15:12, `curl` with the OIDC header): `/og/deck/gt-brand.png` 404 (42,171 B, the not found page; the per deck card of 16.8 and SPEC-5 11 is B4's card render path, absent: finding 1); `/fonts/faces/current/roboto.css` 200 `text/css` 420 B (B7's fonts route); `/decks/templates`, `/help/training`, `/help/updates` 200 `HIT` (59,068, 64,368 and 43,579 B); `/openapi.json` 200 2,708,972 B; `/llms.txt` 200 6,417 B; a `Range: bytes=0-99` request on `/decks/gt-brand/assets/cover-fumadocs.png` 206 with `content-range: bytes 0-99/335538`, `accept-ranges: bytes`, `content-length: 100` (a picture asset; the media asset Range row runs on the Sales pitch copy's clip in section 4, S2r); `GET /api/x/upload/media` with the bearer and the fetch header 403 `{"error":"forbidden"}` (the grant route refuses a bearer caller; the browser's presigned path of B2 is not driven on a project with one public store, whose grant answers the refusal sentence, b2.md 2.6).

The full smoke inside the lock chain, `node scripts/hosted-smoke.mjs --base <preview 2> --token-env TURBOSLIDE_TOKEN --sync-probe --template-copy` (15:32 to 15:35, `logs/smoke-preview2-full.log`): 43 of 45 rows pass. The two new rows beside the fetch rows: `template copy` pass (`smoke-20260915-8fhq` created from the GT template at revision 0 in 5,993 ms, trashed in 419 ms, removed in 3,172 ms, `/deck/smoke-20260915-8fhq` 404 afterwards) and `sync stress probe` FAIL (the quick probe exits 1: 15 of 20 steps, section 11.2 has the five). The `chat message` row fails as in the fetch run (finding 3). The `/new` boot probe on the preview is section 11.1 (29 of 29).

## 11. The sync engine and the font catalog (A3, A5, A7, A8; Kevin's three reports)

### 11.1 The fresh write probe (hotfix 2, A3 item 5) on the preview

`node scripts/probes/new-write-probe.mjs --base <preview 2>` (15:35 to 15:36, `logs/new-write-probe-preview.log`): all 29 steps ok on the blob tier. The twelve sequential edits were each acknowledged at exactly the next revision (revision 12, `serverRevision` 12, `sync.pending` 0), no stale message in the state, the snackbar or a toast, the slide switch on the saved `/new` page kept the editor, the client id and the one stream, the reload opened the deck at the revision the tab had (13 to 13) and drew nobody else (zero chips, marks, outlines, carets, pointers; the roster one self row), the edit after the reload landed at 14, a second tab of the same person was nobody in either tab on every surface (A3 item 5; the roster one row), the first tab still listed nobody after the second tab closed, the first write after opening the deck was kept (finding 33 of round three: "opened at 14, now 15, rejects 0"), the immediate keystroke reached the document, and the deck was trashed and deleted forever. Kevin's first report ("revision 2 vs revision 12", "you can see yourself editing it") does not reproduce on the deployment.

### 11.2 The sync stress probe (A3 item 7) on the preview, two contexts and a third

`node scripts/probes/sync-stress-probe.mjs --base <preview 2> --json verification-5/sync-stress-preview.json` (15:36 to 15:39, 192 s, the full run): 16 of 20 steps ok, 4 failed, 0 not driven. Ok: the first write created the deck and attached the room (tier `blob`); 50 rapid edits each acknowledged at exactly the next revision (50 exact, 0 overshot, revision 51; acknowledgement 1,041 ms mean, 1,120 p50, 1,430 p95, 1,554 max); no stale message anywhere; no character lost; the reported revision equal to the server's with nothing pending; the save words "All changes saved"; a burst of 40 characters landed whole in one write (2,791 ms); two tabs alternating 10 rounds converged to one document and one revision (0 misses; propagation 2,035 ms mean, 2,765 p50, 3,545 p95); no stale message while alternating; neither tab listed itself; one person in two tabs saw nobody on every surface with one roster row each; A forgot the stranger once they left (2 ms); the reload in the middle of a burst lost nothing and B converged; the deck was trashed and deleted forever. Failed: step 14 (the stranger C was nobody in A within the wait while C saw A's two tabs: A `others []`, C `[9e726955:anon_7147165, 469803db:anon_7147165]`), step 17 (after A's reload A drew C, who had left, as one other with a chip: `others ["b17ea85a"]`, `chips 1`), step 18 (the five second offline window: 33 characters typed offline with 24 pending, A converged in 4,496 ms and whole, but B did not converge) and step 19 (A at revision 74, B at 73, the texts differ). The integrator's quick run against the first preview at 15:14 read 20 of 20 (`.turboslide/sync-stress-smoke-2026-09-15.json`, 12 edits, ack p50 1,188 ms) and the smoke's quick run against the second preview at 15:35 read 15 of 20 with steps 15 to 19 failing (the stranger not forgotten within 30 s, B not converged after the reload and after the offline window, the texts differing at one revision). Reading: the single editor's rows of A3 items 1, 4 and 8 hold on the blob tier; the cross instance rows (the shared roster of A8 row 4 and the last write reaching the other tab after an offline replay) hold in some runs and not in others within the probe's 15 s windows (finding 14, B7).

### 11.3 `resize.spec.ts` against the preview (A4, A7)

`PLAYWRIGHT_BASE_URL=<preview 2> node_modules/.bin/playwright test --config .turboslide/hotfix4-preview.playwright.config.ts apps/studio/e2e/resize.spec.ts` (16:01 to 16:05, `logs/resize-spec-preview.log`): the setup row failed and the seven rows after it did not run, because the spec copies `gt-brand`, which this lane had removed from the store four minutes earlier (finding 0); the rerun is the last step of `run-walk2.sh` (`logs/resize-spec-preview-2.log`), followed by the removal of the `e2e-resize-*` copy the spec leaves on a hosted store (A8 row 5, B4: the spec's `afterAll` removes a local folder alone).

### 11.4 The editor walk against the preview (A8, the human style gate)

`node scripts/probes/editor-walk-probe.mjs --base <preview 2> --json verification-5/walk-preview.json --shots verification-5/walk-preview-shots` was at row 490 of about 800 with 23 failures when this pass closed (16:05 to about 16:50; `logs/walk-preview.log`); every failure so far is a picture resize row (`content did not scale`, A8 row 1, B4), the class the integrator's walk against the first preview read too (`walk-merge2.json`: 802 rows, 776 ok, 25 failed, 1 not driven; its two other failures are the export rows 798 and 799, finding 2). `tables.mjs walk verification-5/walk-preview.json` summarises the table once it lands.

The quick walk against this lane's dev server on 4357 (`logs/walk-dev-4357-quick.log`, `walk-dev-4357-quick.json`, 15:24 to 15:32, 477 s): 220 steps, 215 ok, 3 failed, 2 not driven, 0 console errors. The three failures are the picture resize rows 121, 123 and 126 (`content did not scale`: the picture's content stays 211.5 by 141.5 in a box of 300 by 195, A8 row 1, B4), the same three every lane recorded at `BASE`; the two not driven rows are 57 (Insert > Material, no menu row) and 62 (the logo plates through the palette, A8 row 10, B3).

### 11.5 Kevin's three reports, re-walked on the preview

Report 1 (the revision and the self presence): section 11.1's probe answers it on the preview (12 edits at the next revision, no stale words, no ghost of the tab), and the walk's K1 row reruns it with screenshots in `run-walk2.sh` (run 1's K1 used the wrong title selector). Report 2 (resizing): the resize model holds for shapes, text boxes and groups on the checkout and the preview (the editor walk's resize rows pass for every kind but the picture) and fails for pictures (A8 row 1, B4: the picture's content does not scale with its box in every run of every lane, 23 rows of the preview walk); `resize.spec.ts` on the preview reruns in `run-walk2.sh`. Report 3 (fonts): the toolbar's Font dropdown and `font.list` exist (B7); the walk's K3 row (the dropdown's rows against `font.list`, Playfair Display applied) runs in `run-walk2.sh`; the Editable text export does not name the face yet (finding 5).

## 12. The chrome lint and the tooltip audit

`node apps/cli/bin/turboslide.mjs lint --chrome --url http://localhost:4357/<route> --widths 1440,1280,390 --themes light,dark --states ''` for `/decks/templates`, `/help/training` and `/help/updates` against this lane's dev server (15:23 to 15:24, `logs/lint-chrome-new-routes-dev.log`): "6 audit(s) over 3 width(s) and 2 theme(s): 0 with findings, 0 state(s) unapplied" for each of the three. The six existing routes at three widths and two appearances are check step 18 (section 2). The tooltip audit on the four new pages (`scripts/tooltip-audit.mjs --url .../decks/templates --url .../help/training --url .../help/updates --url .../home`) ran once without its server (the first chain's dev server started after the audit, a relative log path after a `cd`) and again in the dev phase rerun: pending the dev phase rerun (`logs/tooltip-new-routes-dev.log`).

The editor with the round five panels open, at 1440, 1280 and 390 in both appearances (the verifier's rows; neither `tooltip-audit.mjs` nor `layout-shift-audit.mjs` opens these surfaces yet): this lane's walk opened each surface on the Sales pitch copy and counted the interactive elements without a `data-tip` ancestor and the layout shift entries the opening produced (`PerformanceObserver` on `layout-shift` without recent input, the nine rules of SPEC-3 6.1 not applied). The Motion panel: 6 interactive controls, 0 without a tooltip, 0 shift entries at 1440 and 1280 in both appearances; at 390 the toolbar's Transition button is not drawn (the phone toolbar) and the panel was not opened. The Dictionary panel: 3 controls, 0 without a tooltip, 0 shift entries in every cell. The theme mode: 40 interactive controls, 7 without a tooltip in every cell (`themeMode.frame.rails`, `themeMode.frame.rules`, `themeMode.frame.crosses`, `themeMode.counter.show`, `themeMode.counter.side`, `themeMode.counter.format`, `themeMode.chips.show`: the right pane's checkboxes and selects; finding 15, B6), 0 shift entries (one cell at 390 dark read one entry of 0.0001 from `.ts-chip.is-live`). The equation toolbar did not appear within 10 s of Insert > Equation in any cell of run 1 (the toolbar mounts with the equation block's selection; the audit's round five row and run 2 read it again: pending). The spell check card and the Chat panel were not reached in run 1 (a nested menu row and the roster button when alone, the script's own); the second run's cells are in `preview-walk-2.json`.

## 13. Findings

Severity 3 breaks production or a scenario, 2 a wrong behaviour or a failed gate, 1 cosmetic. Each names the file and the owner (B1 to B7 or the integrator).

### Severity 3, the verifier's own

0. (3, the verifier; `docs/gslides-parity/verification-5/scripts/preview-walk.mjs`) The first run of this lane's preview walk trashed and deleted forever the seed deck `gt-brand` on the Blob store the preview shares with production, at 16:00:26 PDT. The walk's S1 step drove File > Open's Upload tab with `03-pictures-tables.pptx` on `/edit/gt-brand` and waited for the address to leave the source deck; the upload never moved the address (the Import report dialog did not open either, section 4), the step's id fell back to the deck named by the unchanged address, and the finally block's cleanup, meant for the decks the walk creates, ran `deck.info`, `deck.trash` and `deck.remove` on `gt-brand` (the row `cleanup gt-brand: deck.info 200 (revision 32); deck.trash 200; deck.remove 200` in `preview-walk.json`). From then until 16:09 `/deck/gt-brand`, `/edit/gt-brand`, `/present/gt-brand` and `/print/gt-brand` answered 404 on production, `deck.info` and every deck-less action (`/api/actions/<id>` without `?deck=`, whose default is `gt-brand`) answered `No deck gt-brand in the Blob store`, and this lane's `resize.spec.ts` run against the preview failed at its setup for the same reason (16:05, rerun in section 11). Fresh function instances did not re-seed the deck (`seedOnce` runs under `ready()` on `open` and `list`; six new instance ids answered 404 between 16:03 and 16:05), and `deck.create --from gt-brand --id gt-brand` stopped on `BlobExistsError: decks/gt-brand/assets/site-home-light.jpg exists` because `deck.remove` had left the 201 asset files under `decks/gt-brand/assets/` on the store (the manifest, the 85 slides, the version log and the three import records were gone). The restore, at 16:09: the committed `decks/gt-brand` of `d5a1be5` (the bundle's seed folder, 298 tracked files, `git archive HEAD`) pushed with the store's own seed function `pushDeckDir(client, 'gt-brand', dir, { overwrite: false })` through `layoutBlobClient` and the `BLOB_READ_WRITE_TOKEN` of the pulled development environment (`verification-5/logs/restore-gt-brand-2026-09-15.log`; the script `<scratch>/v5/restore-gt-brand.mjs` is recorded in section 17): 298 files pushed in 14.0 s, the existing assets skipped, `deck.json` present again; `deck.info` answers revision 25 with the eight sections, `slide.list` 85 slides, the four routes 200 on production (180,469, 294,268, 293,060 and 591,510 B) and `/deck/gt-brand` 200 on the preview. What is lost: the production copy's version log past the repository's (revisions 26 to 32, seven writes by earlier rounds' probes and audits on production, and the stored `.thumbs` stamps, which the render route rebuilds on demand once finding 2 is fixed); the deck's content is the repository's. The script and the cleanup helper now carry a `PROTECTED` set (`gt-brand`, `gt-brand-mu05h5vq`) and remove only ids they created with a `v5-`, `untitled-`, `smoke-` or `e2e-` prefix, and the S1 step keeps an imported id only when the address actually moved. Kevin should know that the seed deck on production was absent for nine minutes this afternoon and stands at the repository's revision now. The second consequence, read at 16:10 to 16:15: production's function instances that booted while the manifest was absent ran `seedOnce` (their `head(decks/gt-brand/deck.json)` was null), pushed the seed folder with `overwrite: false`, met an existing asset (the 202 twins had been re-uploaded at 16:04:41 to 16:04:46 by production's own `create` path during this lane's `deck.create --id gt-brand` attempt) and threw `BlobExistsError` past the catch that is written for it (finding 11), so their cached `readyPromise` is a rejection and every store call on them answers 500: at 16:13 `/new`, `/deck/gt-brand` and `/edit/gt-brand` on production answered 500, `/decks` 200 with no card, `deck.list` and `deck.info` 500 with `decks/gt-brand/assets/detail-code-panel-light.jpg exists in the Blob store already` on twelve of twelve requests over eleven instance ids. The store itself is consistent (`head` through the Blob API answers `deck.json` 96,268 B at 23:07:45Z, the slides, the index with `gt-brand` among its 8 rows), so an instance that boots now skips the seed and is healthy; the poisoned instances live while requests keep them warm. This lane stopped every request to production at 16:15 so they idle out, and re-read production before this document closed (the paragraph after the findings and the final report say what it answered). A production redeploy replaces the instances at once and is the ship step's or Kevin's.

### Severity 3

1. (3, B4; `apps/studio/e2e/page-setup.spec.ts`, `packages/export/src/export-odp.ts`, `packages/export/src/svg/write.ts`, `apps/studio/src/server/thumbs.ts`, `apps/studio/src/routes/deck.$deckId.tsx`, `apps/studio/src/routes/print.$deckId.tsx`, `apps/cli/src/commands/export.ts`, `packages/viewer/src/{Editor,InlineText,Gestures}.tsx`, `packages/render/src/{slide,stage}.ts`) B4's days 3 to 7 did not reach the merge: no Page setup dialog (`file.pageSetup` is Later with "The GT theme is 16:9 at 1600 by 900"), no print layouts (the dropdown's handout rows are drawn and disabled, `--layout`, `--paper` and `--orientation` are not CLI flags), no ODP writer (`file.download.odp` Later), no SVG writer (`file.download.svg` Later, `render --format svg` unknown), no per deck card (`/og/deck/gt-brand.png` 404 on the preview), no `og:image` line, no `onMediaFile` on the viewer's drop path (b2.md R7), no `InlineText` autocorrect props (b5.md R15) and no `pagehide` flush (b7.md R19), no `data-block` under `blockAttrs` and no `stageFrame` on the sheet root (b1.md R1, b6.md R2), no nested group selection rule (b3.md B3-16), no `lang` on the sheet root (b5.md R17). Check steps 32 and 36 answer `not yet`; the Later count reads 5 leaf rows (14.2 reads 2) and `menu-model.test.ts` pins the deviation; S5a (3 per page on Letter), S7b (File > Page setup) and the SVG rows of 16.8 fail or are not driven (sections 4, 6). Every 14.1 flip of B4's lane is a fail in the audit's round five phase (section 3).
2. (3, integrator and B6; `apps/studio/vite.deploy.config.ts` `PACKAGES_PATTERN`, `packages/render/src/theme-node.ts` `equationCssNode`) Every hosted render that runs the CLI child fails on the merged tree's deployment: `export.run` in both PPTX modes and for PDF answers 500 `export exited 2 without an export report: turboslide: ENOENT: no such file or directory, open '/tmp/turboslide/packages/render/assets/Temml.woff2'` and `GET /api/render/<slide>?deck=<id>&w=320` answers 404 with the same sentence for a fresh deck (this lane's probes on the second preview at 15:20, on a blank scratch deck created, exported and removed through the actions API; the integrator's editor walk on the first preview reads the same on its rows 798 and 799). `loadThemeBundle` inlines the 9 KB Temml supplement for every deck (`equationCssNode` reads `packages/render/assets/Temml.woff2` unconditionally; only the math face waits for `equations: true`), and the deployment's runtime file glob (`{theme/src/gt-ink-paper/*.css,theme/src/ts-plate/*.css,theme/assets/sprite.svg,fonts/src/inter.css,fonts/assets/InterVariable.woff2,fonts/assets/*/*,fonts/export/*,export/src/calibration/calibration.json}`) ships no `render/assets/*`, so the function has no file to read; `packages/render/assets/` is also untracked (the ship step commits it). Download as PowerPoint and PDF (S5), the thumbnails of every new deck's card and the walk probe's export rows are broken hosted until the glob names the folder; the smoke's thumbnail rows still pass because `gt-brand`'s thumbnails are stored objects from before. The round four production deploy passed the same walk rows (`build-4/hotfix-4/walk-production.json` rows 798 and 799).
3. (3, B7 and the integrator, with B5; `packages/realtime/src/blob.ts` `entryOfRecord`, `apps/studio/src/server/actions.ts` the chat port, `packages/realtime/src/admission.ts` `chatMessagesOf`) A chat message is accepted and never listed on the blob tier: `chat.send` answers 200 with an id, `chat.list` answers `{"messages":[]}` on every instance for at least 8 s and with `since` (section 10; the smoke's `chat message` row fails; S6 in section 4). `entryOfRecord` maps every stored record to `kind: 'edit'` with no `chat` member, so the chat entry the port appended through `appendWithRetry` comes back as an edit and `chatMessagesOf` drops it; the deck's revision does not move either (32 before and after), so the entry may not be stored as a record at all. S6 fails on the production tiers; the memory tier of a checkout (`chat.spec.ts`, step 35) is the only place the panel works.

### Severity 2

4. (2, B4; `packages/schema/src/shapes.test.ts`, `packages/schema/src/shapes/geometry.test.ts`, `packages/schema/src/diagrams.test.ts`, `packages/render/src/__tests__/canvas.test.ts`, `packages/export/src/gslides-fixture.test.ts`) Five test files fail on the merged tree, rerun alone here: `shapes.test.ts` "answers the box path for every preset until the interpreter lands" and "writes the same path string shapes.ts wrote before the seam", `shapes/geometry.test.ts` "answer the box as the path, the whole box as the text inset and eight sites", `diagrams.test.ts` twelve `cycle with 3 to 6 at outline, plate, ink` rows (`connect` codes against `[]`), `canvas.test.ts` 12 snapshots (`12 failed | 58 passed`), `gslides-fixture.test.ts` "the padded shape" (`tIns + bIns` 203,606 EMU against the pinned 121,920). B4's shape interpreter landed without its tests; check step 5 is red on the merged tree.
5. (2, B1 with B4 and B5; `packages/export/src/scene/extract.ts` line 250 `loadThemeBundle()`, `packages/export/src/pdf/build.ts` line 200, `packages/export/src/pptx/text.ts` `familyFor`) The Editable text export names no catalog face (A5 item 5): a deck whose headings carry `typography.family` `roboto`, `merriweather`, `playfair-display`, `jetbrains-mono` and `oswald` exports with `a:latin typeface="GT Inter Display"` (16) and `"GT Inter Text 14"` (3) alone (section 6.2). The scene's `family` is the computed `font-family` of the export capture, and the capture's theme bundle loads no `@font-face` for the deck's families (B7's request 16 to B1 and 17 to B4, `loadThemeBundle({ fonts: usedFontIds(deck, slides) })`, did not land), so `--ts-font-<id>` resolves to `inherit` and `catalogFace` sees the sheet's Inter. The standalone build and the render do carry the faces (`apps/cli/src/deps/render.ts` passes `deckFontsCss` and `fontSrc`; section 6.2's Chromium row).
6. (2, B4 and the integrator; `apps/cli/src/commands/export-check.ts`, `scripts/check.mjs` step 32) `export check` has no `--page WxH` flag: the 4:3 fixture's file (`p:sldSz cx="9144000" cy="6858000"`, the right size) and the 16:10 five family file read `INVALID` with `page size ... expected 12192000 by 6858000`, and step 32's `export check .turboslide/p43-flatten --page 1200x900` cannot pass as written once the step ungates.
7. (2, B1 and B2; `packages/chrome/src/menus/keys.ts` `OMITTED_SHORTCUTS`) Five of the eighteen Google shortcut rows of `GS5_KEY_ROWS` are still in `OMITTED_SHORTCUTS`: `animation-preview` (B1's `key.motion.preview`) and `play-video`, `video-play-pause`, `video-rewind`, `video-forward` (B2's `key.media.play`, `key.present.mediaToggle`, `key.present.mediaRewind`, `key.present.mediaForward`); the list holds 27 rows. The other thirteen planned rows are bound (`slide.transition` Cmd+Option+Shift+B, `tools.dictionary` Cmd+Shift+Y, the Accessibility rows). 16.1 reads "the eleven shortcut rows that left `OMITTED_SHORTCUTS`" (the audit's round five row).
8. (2, integrator, Kevin's machine; `scripts/check.mjs` step 25) Docker's VM disk is full and the render worker image cannot be rebuilt (`no space left on device` at `COPY . .`), so step 25 and every container leg of 16.4 did not run (section 6.1). Nothing was pruned.
9. (2, integrator; `scripts/check.mjs` step 3) Step 3 fails as written on the uncommitted tree: `pnpm generate:contracts` answers "every committed contract is current" and the diff is the uncommitted files (`packages/agent/generated/{cli,describe,llms-full,manifest,mcp-tools,openapi}`, `docs/grammar.md`, the three skill references); it passes once the ship step commits them, as in every round.
10. (2, integrator and every lane that adds client code; `scripts/perf-budget.mjs`, `scripts/check-client-bundle.mjs`) The largest client chunk `index-BLGSx17J.js` is 725,388 B in the build (`check-client-bundle` reports it over the 600,000 B ceiling and does not fail, "no vendor chunk in the output") and 708 KB decoded on the deployment (the integrator's perf run on the first preview, every route); 16.6 reads "the largest chunk under 600 KB". This lane's own run is section 8.
11. (3, the integrator with B2 and B7; `packages/store/src/blob-store.ts` `pushDeckDir`, `fetchMissingTwins`, `putSnapshot`, `seedOnce`; `packages/store/src/blob-vercel.ts`) `instanceof BlobExistsError` does not hold in the deployed bundle, so the catches written for a blob that exists rethrow: production's own `create --from gt-brand` path threw `BlobExistsError: decks/gt-brand/assets/site-home-light.jpg exists in the Blob store already` out of `fetchMissingTwins(seedId, true)` (16:04, this lane's restore attempt), whose body reads `catch (error) { if (!(error instanceof BlobExistsError)) throw error; }`, and `seedOnce` on the instances of finding 0 threw the same class out of `pushDeckDir`'s `if (error instanceof BlobExistsError) return;`. `packages/store/src/access-store.ts` 77 to 80 already carries the workaround (`error.name === 'BlobExistsError'`) with the comment that the class is duplicated across chunks; the store's other catches do not. The failure mode is the worst one the store has: a rejected `readyPromise` is cached for the instance's life and every route answers 500 (production at 16:13). The fix is the `error.name` test beside every `instanceof` on the two blob error classes, or one class defined in a module the bundler cannot duplicate, and a `ready()` that does not cache a rejection.
12. (2, the integrator with B2; `apps/studio/src/editor/EditorRoot.tsx` the render context, `packages/render/src/blocks/media.ts` `renderMedia`) The editor's media poster root carries `data-src=""` and `data-duration=""` for a stored clip: `renderMedia` reads the asset through `ctx.media?.(assetId)` and the editor's render context passes `assetUrl` and no `media` resolver, so `asset` is undefined, `src` is `''` and no warning is pushed (the guard `ctx.media !== undefined && asset === undefined` never fires). `media.list` on the same deck answers the record. The show's media controller mounts the element over `data-src`, so a stored video cannot play in the browser's show until the resolver is passed (S2b; `media.spec.ts` row 5 is check step 33's on the checkout).
13. (2, B3 with B1 and B2; `decks/templates/sales-pitch/slides/product-demo.json`, `packages/render/src/motion.ts` `compileMotion`) The Sales pitch's demo clip is Play (on click) by its playback and has no `playMedia` animation row, and the compiler derives a step from animation rows alone (`animation.effect === 'playMedia'`, lines 70 to 88), so `motion.compile product-demo` answers `steps: []`, the show reads `data-steps="0"` and the next click leaves the slide instead of playing the clip (S2, SPEC-5 3.2 "the show plays it on the next click"). B2's `media.insert` appends the row (b1.md request 5); a template slide written by hand needs it too, or the compiler derives it from `playback.start`.
14. (2, B7; `packages/store/src/blob-store.ts` the presence shards, `packages/realtime/src/blob.ts`, `apps/studio/src/server/room.ts`) On the blob tier the cross instance rows of the sync stress probe hold in some runs and not in others: the stranger's arrival and leave did not reach A within the probe's windows (steps 14, 15, 17 of the two runs against the second preview; forgotten in 2 ms in one run and not within 30 s in the other), and after the five second offline window B did not converge (A at revision 74, B at 73, the texts differing) in both runs against the second preview while the integrator's run against the first read 20 of 20. A3 item 3's "two users converge without losing an edit" and A8 row 4's shared roster are met on one instance and not across instances within 15 s (section 11.2).
15. (1, B6; `apps/studio/src/editor/theme-mode.tsx`, `packages/chrome/src/ThemeToolbar.tsx`) Seven controls of the theme mode's right pane carry no tooltip: `themeMode.frame.rails`, `themeMode.frame.rules`, `themeMode.frame.crosses`, `themeMode.counter.show`, `themeMode.counter.side`, `themeMode.counter.format`, `themeMode.chips.show` (section 12; Kevin's tooltip rule of 2026-09-11).
16. (1, the integrator; `apps/studio/src/server/headers.ts` 294) The report only Content Security Policy still carries `upgrade-insecure-requests` on the deployment, and Chromium logs "The Content Security Policy directive 'upgrade-insecure-requests' is ignored when delivered in a report-only policy" on every page load (15 console errors of the preview walk, 4 of the integrator's editor walk; A8 row 6, the integrator's).

### Severity 1

17. (1, B1, B2 and every lane; `docs/gslides-parity/build-5/*.md`) The five suites that time out under `pnpm test`'s load and pass alone (section 17) make step 5's verdict depend on the machine's load; a per file timeout or a serial pool for the Chromium bound suites would make the step read the tree alone.

## 14. Deviations recorded, with their reasons

1. B4's days 3 to 7 (finding 1): the surfaces are absent, not wrong; the reason is the lane's delivery, recorded by the integrator (10.5 item 1) and read here on the tree.
2. Docker (finding 8): the VM disk, not the code; the commands are recorded in 6.1 for Kevin.
3. Step 3 (finding 9): the uncommitted contracts, every round's state before the ship step.
4. The shadow authorization and the blob roster on the deployment: the parity audit against the preview carries the fifteen shadow rows and the per instance roster rows of `BASELINE.md` section 6 until Kevin switches `TURBOSLIDE_AUTHORIZE` to enforce mode (17 row 32); B7's shared roster (A8 row 4) is the round's answer for the roster rows, read in section 11.
5. nspell and the Portuguese and Dutch dictionaries (B5, b5.md deviation 6): `pt`, `pt-PT` and `nl` do not finish parsing within 13 minutes of CPU on this machine, so a deck in those languages waits on the Worker with "Checking the presentation"; the seven dictionaries stay under their gzip budgets (`check-dictionaries.mjs` ok, 3,321.5 of 3,710.9 KB). Kevin's row 21.
6. The Nitro WebSocket upgrade route is not written (the integrator, 10.5 item 3): a node-server deployment falls back to SSE; the flag, the client transport and the dev sidecar exist.
7. `HERO_LIVE` stays false (10.5 item 4): the still hero ships; section 8's `/home` rows are the second measurement the flag waits for.
8. The `equation/parse` lint rule (B6, R7) and `admitChat` (B7, b5.md R21) did not land; the chat port is composed in the hosted dispatcher over the room channel (which is where finding 3 sits).
9. The media Range 206 and media grant rows of 16.8 are not in `hosted-smoke.mjs` (the smoke deck carries no media asset); this lane read them on the Sales pitch copy's clip in section 4 (S2r) and on a picture asset in section 10.
10. The two workflows (`.github/workflows/lighthouse.yml`, `native.yml`) are untracked until a token with the workflow scope exists (Kevin).
11. `soffice`, Inkscape, Figma and PowerPoint are not installed on this machine: the ODP oracle and the SVG viewers are the container's (6.1) or Kevin's; the PowerPoint checklist is Kevin's (6.3).
12. The perf run's paint and ready rows were measured under this lane's own Chromium load (section 8); they stay misses with both numbers until pass 2.
13. The chain's dev server phase ran without its server (the relative log path after `cd apps/studio`); the lint rows ran against a server started by hand and the audit and tooltip rows rerun in `run-dev-phase.sh`.

## 15. The unverified Google facts (16.9), with the round's reading

Each fact 16.9 lists, with the reading the round took as built on this tree, where it is read from, and what this pass checked. A fact whose surface did not land (B4) carries the design's reading and "not built".

| Fact                                                            | The round's reading                                                                                                                                                                                                                                                                                                                        | Checked here                                                                                                                                                                                                                                |
| --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| the transition list of seven and its behaviours                 | `TRANSITION_KINDS`: None, Dissolve, Fade, Slide from right, Slide from left, Flip, Cube, Gallery (`packages/schema/src/motion.ts`); Slide from right is a push (`p:push dir="l"`), the Dissolve a 64 frame Bayer 8 mask sequence, the Gallery a translate with a small Y rotation and a 0.9 scale at the midpoint (`render/motion-css.ts`) | the select lists the eight labels in that order (the audit's round five row); the Editable text of the motion fixture writes `p:dissolve`, `p:fade`, `p:push` 2, `p14:flip`, `p14:prism`, `p14:gallery` (B1's `export check` line, step 33) |
| the default animation type and the two default durations        | Appear on click at 500 ms; a new transition at 500 ms (0.11; `motion.add` "appear when absent", "click when absent"; the Sales pitch's records carry `durationMs: 500`)                                                                                                                                                                    | `motion.add`'s input describes the defaults; the panel's Add animation row                                                                                                                                                                  |
| the slider's scale and labels                                   | 100 to 5,000 ms in steps of 100 with Slow 2,000, Medium 1,000, Fast 500 stops and a seconds readout marked `turboslide: true` (`MotionPanel.tsx` 86, 146, 172)                                                                                                                                                                             | read in the file; the slider is `motion.transition.duration` on a slide with a transition                                                                                                                                                   |
| Left arrow reversing a step                                     | a step, then the previous slide at its last step (0.14; `presentModel.ts` `previousPosition`)                                                                                                                                                                                                                                              | S3c on the preview (section 4)                                                                                                                                                                                                              |
| Auto-play advancing a step per tick                             | a step per tick over 1, 2, 3, 5, 10, 15, 30 and 60 s (`AUTO_PLAY_INTERVALS_MS`), Play and Loop (`present/strings.ts`)                                                                                                                                                                                                                      | the list read in the file; motion.spec.ts's Auto-play row is check step 33's                                                                                                                                                                |
| the published player playing steps                              | the standalone file plays the steps and the transitions (`__tsMotion` in the export, `--autoplay`, `--loop`)                                                                                                                                                                                                                               | check step 33 (`motion.spec.ts` "the standalone file plays the steps on the autoplay timer")                                                                                                                                                |
| Presenter view not previewing the next step                     | Turboslide previews one step ahead ("Step k of n", both `turboslide: true`; 0.14)                                                                                                                                                                                                                                                          | M1 on the preview (section 4)                                                                                                                                                                                                               |
| the PDF's resting objects                                       | every object at rest, entrance objects shown (0.3; the `motion.still` residual rows name the objects hidden after the last step)                                                                                                                                                                                                           | the residual rows of B1's export; the PDF leg needs the container (6.1)                                                                                                                                                                     |
| the audio icon's default size                                   | Turboslide's frame with the title as the poster; the icon's box is the block's `pos` (B2, `render/blocks/media.ts`); no fixed pixel size is asserted                                                                                                                                                                                       | read in the file                                                                                                                                                                                                                            |
| the labels Start at, End at and Mute audio                      | `media-strings.ts`: "Start at", "End at", "Mute audio" (Start at and End at marked unverified in the file's header)                                                                                                                                                                                                                        | read in the file; the Insert video dialog opened on the preview (S2a)                                                                                                                                                                       |
| the OK against Apply label of Page setup                        | R08's OK; not built (no Page setup dialog on this tree, finding 1)                                                                                                                                                                                                                                                                         | not built                                                                                                                                                                                                                                   |
| whether objects keep their coordinates after Page setup         | `deck.setPageSize` mode `keep` is the default ("Google's default leaves every coordinate", `actions.ts` 5840); the dialog is absent                                                                                                                                                                                                        | S7a on the preview: `deck.setPageSize standard-4-3` through the API, the title slide's blocks compared before and after (section 4)                                                                                                         |
| the handout strings and the close control's label               | R08's; not built (the print dropdown's handout rows are present and disabled, `print.$deckId.tsx` 84)                                                                                                                                                                                                                                      | the audit's print dropdown row (section 3) and S5a                                                                                                                                                                                          |
| the Preferences checkbox set and Google's default substitutions | the record of `packages/schema/src/preferences.ts` with twelve default substitution rows and the checkbox set of b5.md 1.2 (R10 13 marks both unverified)                                                                                                                                                                                  | the Preferences dialog's two tabs and the Substitutions table (the audit's row; `text-tools.spec.ts` in step 35)                                                                                                                            |
| the gallery's three category headings                           | Personal, Work, Education (`TEMPLATE_CATEGORIES`; `/decks/templates` h2s)                                                                                                                                                                                                                                                                  | the audit's gallery row; the smoke's `/decks/templates` row                                                                                                                                                                                 |
| the building block category labels                              | Agendas, Lists, Key statistics, Quotes, Headlines, Text callouts, Calls to action, People, Cards (`BUILDING_BLOCK_CATEGORY_LABELS`)                                                                                                                                                                                                        | the audit's Building blocks pane row (nine headings)                                                                                                                                                                                        |
| the conversion notice's label                                   | `ROUND_FIVE.importNotice` in the Import report dialog (B3)                                                                                                                                                                                                                                                                                 | S1 on the preview (the report dialog's sentence)                                                                                                                                                                                            |
| the "Keep original theme" default                               | unchecked (`ImportSlides.tsx` 28: "Google's checkbox, unchecked")                                                                                                                                                                                                                                                                          | the audit's Import slides row reads `keepTheme` false                                                                                                                                                                                       |
| the Define row                                                  | Tools > Dictionary opens the panel ("Look up a word", "Look up"); hosted the Wiktionary definition through `/api/define`, a checkout the link alone (`TURBOSLIDE_DICTIONARY`)                                                                                                                                                              | the audit's Dictionary panel row; `/api/define` is B5's checkout row                                                                                                                                                                        |
| spoken punctuation in notes                                     | none: "punctuation is what the recogniser returns" (`DictateBox.tsx` 23), as Google's Slides notes have no commands                                                                                                                                                                                                                        | read in the file; dictation needs a microphone and is not driven                                                                                                                                                                            |
| the magnifier and announcements labels                          | "Turn on collaborator announcements" (round three's row) is the only announcement toggle; no magnifier row is built (Google's is a browser feature)                                                                                                                                                                                        | read in the model                                                                                                                                                                                                                           |
| the four navigation row labels of the Accessibility menu        | "Next formatting change", "Previous formatting change", "Next slide", "Previous slide", each `unverified: true`                                                                                                                                                                                                                            | the audit lists them (section 3)                                                                                                                                                                                                            |
| chat's unsaved history                                          | nothing stored; the panel's first line "Messages are not saved; they leave when everyone has left" (`panel.chat.notSaved`); the room clears when the last participant leaves                                                                                                                                                               | the audit's Join chat row; S6 (which fails on the blob tier for another reason, finding 2)                                                                                                                                                  |
| the Verbalize mechanism                                         | a `role="status"` live region (`#ts-verbalize`) a screen reader speaks, plus `speechSynthesis` under Speak selection aloud (off by default)                                                                                                                                                                                                | read in `accessibility/verbalize.ts`; the Accessibility menu's rows in the audit                                                                                                                                                            |
| Google's SVG and ODP file contents                              | R09's design; not built (no writer on this tree)                                                                                                                                                                                                                                                                                           | not built                                                                                                                                                                                                                                   |
| the Recolor preset list                                         | No recolor, Grayscale, Sepia, Negative and the duotones over the theme's colours ("Colour, light", "Colour, dark"), the list `unverified: true` (`blocks.ts` 528, `inspector/picture.tsx` 28)                                                                                                                                              | read in the files                                                                                                                                                                                                                           |
| the nested group selection rule                                 | the design's first click on the outermost group, the second on the inner, a double click entering a level (0.48); B3's `pos.group` path landed, the gesture rule in `Gestures.tsx` (B4, B3-16) did not                                                                                                                                     | not built on the canvas; `block.group` and `block.ungroup` with paths are B3's tests                                                                                                                                                        |
| Google's easing curves and Spin direction                       | `ease-in-out` for transitions, `ease-out` for entrances, `ease-in` for exits, `linear` for Spin and the switches; Spin `rotate(360deg)` clockwise (`motion-css.ts` 40, "Turboslide's, unverified against Google")                                                                                                                          | read in the file                                                                                                                                                                                                                            |
| the five theme cap's reach over Import theme                    | five imported themes, the sixth refused with its sentence (`theme-import.ts` 13)                                                                                                                                                                                                                                                           | `import.spec.ts` row 3 in check step 34                                                                                                                                                                                                     |

## 16. What remains for Kevin (17, plus A7's fonts row), with the round's default as built

| #   | Item                                                                                            | The round's default as built on this tree                                                                                                                                                                                                                                                                                      |
| --- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Blank on the Plate theme                                                                        | Blank stays on GT with `blank-plate` beside it under `decks/templates/`; the eight templates ship on Plate; the switch is one `theme` line in `decks/templates/blank/deck.json`                                                                                                                                                |
| 2   | The `mark.kind: 'turboslide'` toggle and the second theme's label                               | in the schema (`themeMode.mark.kind` in the theme mode), the panel label "Plate"                                                                                                                                                                                                                                               |
| 3   | The YouTube Data API key                                                                        | the Search YouTube tab is drawn and disabled with its clause (the audit's Insert video row; `docs/hosting.md` 13)                                                                                                                                                                                                              |
| 4   | Storing a YouTube thumbnail as a poster                                                         | not offered; Turboslide's frame with the title is the poster                                                                                                                                                                                                                                                                   |
| 5   | The public URL position for media on restricted decks                                           | the `assetKey` prefix for media (`apps/cli/src/records/access.ts` rotates it); pictures on the precedent                                                                                                                                                                                                                       |
| 6   | Record                                                                                          | omitted                                                                                                                                                                                                                                                                                                                        |
| 7   | The bundle cap                                                                                  | 500 MB with the streaming unpack                                                                                                                                                                                                                                                                                               |
| 8   | The `tmp` tier and media                                                                        | refuses ("Audio and video need the Blob store on this instance", `media.spec.ts` row 2)                                                                                                                                                                                                                                        |
| 9   | `ogg` and `mov`                                                                                 | not this round                                                                                                                                                                                                                                                                                                                 |
| 10  | Present on another screen                                                                       | Now; the server answers `{ supported: false }`, Chromium with one screen shows the disabled state                                                                                                                                                                                                                              |
| 11  | Spotlight, Star, Offline, Q&A                                                                   | Spotlight (`insert.speakerSpotlight`) and Star (`title.star` over `prefs.set /starred`, the home page's Starred view) in; Offline and Q&A out                                                                                                                                                                                  |
| 12  | The default animation type and durations                                                        | Appear on click at 500 ms; a new transition at 500 ms                                                                                                                                                                                                                                                                          |
| 13  | Slide from right as push; Left arrow; Auto-play; a digit jump; the reversed transition          | push; a step; a step; step 0; reversed (`data-reverse`)                                                                                                                                                                                                                                                                        |
| 14  | The still rule and the residual                                                                 | adopted (`motion.still` rows in the Editable text report)                                                                                                                                                                                                                                                                      |
| 15  | The presenter's next preview one step ahead and "Step k of n"                                   | built (`PresenterConsole.tsx`; M1)                                                                                                                                                                                                                                                                                             |
| 16  | The Page setup pixel unit and the 16:10 preset                                                  | the sheet pixel at 120 per inch; 1440 by 900 (`decks/fixture/page-16-10`); the dialog itself is absent                                                                                                                                                                                                                         |
| 17  | The Paper dropdown, the handout footer, Hide background, OK, notes clipping, `p:sldSz`, the cap | `p:sldSz` without `type` and the 120 to 6,720 px cap are built (`PAGE_CAP`, the 4:3 export); the print options are not (finding 1)                                                                                                                                                                                             |
| 18  | ODP from the Turboslide writer, `loext:opacity`                                                 | not built (finding 1)                                                                                                                                                                                                                                                                                                          |
| 19  | The SVG default text mode, fontkit, no JRE, vectors                                             | fontkit 2.0.4 is in the catalog and attached to the exporter; the SVG writer is not built (finding 1)                                                                                                                                                                                                                          |
| 20  | The math font, Cambria Math, inline math                                                        | Latin Modern Math on the sheet; Cambria Math in the OMML runs; no inline math                                                                                                                                                                                                                                                  |
| 21  | German and Italian dictionaries                                                                 | seven languages (en, en-GB, fr, es, pt, pt-PT, nl under `apps/studio/public/dictionaries/`, 3.3 MB gzip); nspell does not finish parsing pt, pt-PT or nl within 13 minutes of CPU on this machine (b5.md deviation 6), so a Portuguese or Dutch deck waits on the Worker; the hunspell-asm fallback of R10 4.3 is Kevin's call |
| 22  | The definitions provider                                                                        | the Wiktionary panel hosted through `/api/define` (a `User-Agent` is required by Wiktionary), the link on a checkout; `TURBOSLIDE_DICTIONARY` switches (`docs/hosting.md` 13)                                                                                                                                                  |
| 23  | Preferences per principal; Speak aloud; the correction list; `text/spelling`                    | per principal with the localStorage mirror; Speak selection aloud off by default; the list ships (`autocorrect-lists.ts`); the rule at severity 1 over an injected checker (`fixtures.test.ts` waives its planted slide)                                                                                                       |
| 24  | Fonts beyond Inter                                                                              | superseded by A5: 26 families in `packages/fonts` (Inter plus 25 fetched at google/fonts `1ac2012`, OFL or Apache, 14.6 MB); the Font dropdown, `font.list`, `turboslide fonts list`; the Editable text export does not name the family yet (finding 4)                                                                        |
| 25  | Chat over the operation stream, nothing stored, commenters and above, 4,000 code points         | adopted in the design; on the blob tier a sent message is never listed (finding 2)                                                                                                                                                                                                                                             |
| 26  | Explore                                                                                         | omitted                                                                                                                                                                                                                                                                                                                        |
| 27  | Captions                                                                                        | a stub (`captionsStub`)                                                                                                                                                                                                                                                                                                        |
| 28  | The special characters drawing box                                                              | built with "Best guesses" (`stroke-recognizer.ts`, 13 tests)                                                                                                                                                                                                                                                                   |
| 29  | The five theme cap                                                                              | kept (the sixth import refused)                                                                                                                                                                                                                                                                                                |
| 30  | The live monochrome hero                                                                        | the gate is in place with `HERO_LIVE = false` (integrator 10.5 item 4); the still hero ships; the preview's `/home` rows of section 8 are the second measurement                                                                                                                                                               |
| 31  | The Turboslide additions marked `turboslide: true`                                              | each a row flag                                                                                                                                                                                                                                                                                                                |
| 32  | The infrastructure the hosted tiers need                                                        | the degraded tiers as today; `docs/hosting.md` names the switches (sections 9 to 13); nothing was installed, created, registered or requested by this pass; the Docker VM disk (6.1) and the workflow scope for `.github/workflows/lighthouse.yml` and `native.yml` (`gh auth refresh -s workflow`) are Kevin's too            |
| A7  | The font catalog's membership                                                                   | the 26 ids of `FONT_IDS`; a face is one catalog row and one asset folder                                                                                                                                                                                                                                                       |

## 17. Commands run

Every command from the repository root unless noted (the vitest runs `cd <package> && ../../node_modules/.bin/vitest run ...`); no `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran outside `scripts/check.mjs`'s own steps; Docker ran for step 25 alone and nothing was pruned; no git write command ran (`git status`, `git diff`, `git log`, `git check-ignore`, `git show` only); no secret, cookie value or token was printed (the tokens rode in the environment of the chain scripts). The lock chain scripts are `verification-5/scripts/run-chain.sh` and `run-dev-phase.sh`; their logs `verification-5/logs/`.

| Command                                                                                                                                                                                                                                                                                                                 | Result                                                                                                                                                                                                                       |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `node_modules/.bin/tsc -b`; `tsc -b --force`                                                                                                                                                                                                                                                                            | exit 0 (0.15 s incremental; 28.6 s wall forced) over the whole tree, 14:40                                                                                                                                                   |
| `git diff --exit-code d5a1be5 -- packages/chrome/src/tokens.css`                                                                                                                                                                                                                                                        | empty                                                                                                                                                                                                                        |
| `git status --porcelain \| wc -l`; `git diff --stat d5a1be5`                                                                                                                                                                                                                                                            | 606 rows; 331 files changed, 150,618 insertions, 123,849 deletions                                                                                                                                                           |
| `node scripts/check.mjs --list > verification-5/check-list-merge2-2026-09-15.txt`                                                                                                                                                                                                                                       | 37 rows; 32 (1 file pending) and 36 (2 files pending) gated                                                                                                                                                                  |
| `node scripts/check.mjs --only 1,2,3,4`; `--only 19,28,29`                                                                                                                                                                                                                                                              | 1 ok 1.6 s, 2 ok 4.9 s, 3 FAIL (the uncommitted contracts), 4 ok; 19 ok 71.8 s, 28 ok (0 advisories at high), 29 ok 7.1 s (4 outputs match `BUILD-RECORD.json`, the Linux addon pending)                                     |
| `node packages/agent/src/generate/main.ts --check`                                                                                                                                                                                                                                                                      | "every committed contract is current" (the ten generated files are modified, uncommitted)                                                                                                                                    |
| `node scripts/check.mjs --only 25` and the container legs (`verification-5/scripts/../logs/docker-legs.log`)                                                                                                                                                                                                            | FAIL: the image build stops at `COPY . .` with `no space left on device`; the legs did not run (6.1)                                                                                                                         |
| `node apps/cli/bin/turboslide.mjs import <fixture> --dry-run --json` over the six fixtures                                                                                                                                                                                                                              | section 5; `verification-5/import-fidelity.json`                                                                                                                                                                             |
| `cd packages/import && vitest run`; `packages/fonts`; `packages/render fonts`; `packages/spelling`; `packages/mcp tools`; `apps/cli commands/mcp.test`                                                                                                                                                                  | 201; 19; 10; 19; 12; 4 tests passed, each alone                                                                                                                                                                              |
| `cd packages/schema && vitest run shapes.test shapes/geometry diagrams`; `packages/render __tests__/canvas.test`; `packages/export gslides-fixture`                                                                                                                                                                     | 15 failed (B4's three files); 12 snapshots failed of 70; 1 failed of 15 ("the padded shape"): finding 4                                                                                                                      |
| the vitest groups of steps 32 to 37 alone (`schema canvas`, `render print-layout geometry motion theme-css equation`, `theme`, `export ooxml/timing ooxml/transition ooxml/media ooxml/ids ooxml/math`, `store media templates seed`, `viewer present`, `effects dither`, `schema autocorrect preferences`, `realtime`) | 32; 95; 99; 63; 87; 54; 22; 69; 100 tests passed                                                                                                                                                                             |
| `cd packages/chrome && vitest run --maxWorkers=2`; `apps/studio`; `packages/agent coverage.test contracts.test`                                                                                                                                                                                                         | 564; 280; 247 passed alone (under the load average of 100 while this lane's suites overlapped, 18, 2 and 2 tests had timed out; none is a defect)                                                                            |
| `node scripts/check-dictionaries.mjs`                                                                                                                                                                                                                                                                                   | ok, 7 dictionaries under their budgets, 3,321.5 of 3,710.9 KB gzip                                                                                                                                                           |
| `node apps/cli/bin/turboslide.mjs validate <deck>` over `decks/gt-brand`, `decks/fixture`, the five fixture decks and the eleven templates; every `deck.json`'s `schemaVersion` read                                                                                                                                    | every deck 0 errors; every `schemaVersion` 1                                                                                                                                                                                 |
| `node apps/cli/bin/turboslide.mjs export pptx all --mode native --theme light --deck decks/templates/sales-pitch` and the zip read                                                                                                                                                                                      | 1 `bldP build="p"`, 1 `a:videoFile`, 0 `p:transition` (6.2)                                                                                                                                                                  |
| `... export pptx all --mode native --deck decks/fixture/page-4-3`; `export check`                                                                                                                                                                                                                                       | `p:sldSz` 9144000 by 6858000; `export check` INVALID on the page (finding 6)                                                                                                                                                 |
| `... export pdf decks/fixture/gslides --layout handout-6 --paper letter --orientation portrait`; `render title --format svg --text embed`                                                                                                                                                                               | `no slide "letter"`; `no slide "embed"` (B4's flags absent, finding 1)                                                                                                                                                       |
| the five family deck: `validate`; `export pptx all --mode native`; `export check` with and without `--page 1440x900`; the zip read                                                                                                                                                                                      | 0 errors; `passed: true`; INVALID on the page; `typeface` GT Inter alone (finding 5)                                                                                                                                         |
| `node scripts/hosted-smoke.mjs --base <preview 2> --token-env TURBOSLIDE_TOKEN` (fetch rows)                                                                                                                                                                                                                            | 42 of 43 (the chat row, finding 3)                                                                                                                                                                                           |
| `curl` on `/og/deck/gt-brand.png`, the new routes, `/fonts/faces/current/roboto.css`, `/api/vitals`, `/llms.txt`, `/openapi.json`, a Range request on a picture asset, `GET /api/x/upload/media` (preview 2, the OIDC header)                                                                                           | section 10                                                                                                                                                                                                                   |
| `POST /api/actions/chat.send` then five `chat.list` over 8 s and one with `since` (preview 2, the bearer)                                                                                                                                                                                                               | 200 then `{"messages":[]}` six times (finding 3)                                                                                                                                                                             |
| `deck.create` (blank), `export.run` pptx native and flatten and pdf, `GET /api/render/title?deck=<id>&w=320&r=0`, `deck.trash`, `deck.remove` (preview 2, the bearer; two scratch decks `v5-export-wl2u2`, `v5-export-…`, `v5-thumb-…`, each removed)                                                                   | 500 `ENOENT ... Temml.woff2` on every export and 404 with the same sentence on the render (finding 2); `export.run` refuses `theme: "light"` as a string (the input is an array) and `format: "html"` (pptx, pdf, odp alone) |
| `node docs/gslides-parity/verification-5/scripts/run-chain.sh` (the lock chain, sections 2, 3, 4, 7, 8, 11, 12)                                                                                                                                                                                                         | sections 2, 3, 4, 7, 8, 11 and 12; in flight at the close of this pass (the timeline `logs/chain-timeline.log`)                                                                                                              |

## 18. Files under `docs/gslides-parity/verification-5/`

| File                                                                                                                                     | What                                                                                                                                      |
| ---------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| `BASELINE.md`, `baseline-2026-09-15.json`, the day 0 run files                                                                           | the day 0 baseline on production (section 1 of this document reads it)                                                                    |
| `check-list-merge2-2026-09-15.txt`                                                                                                       | `check.mjs --list` at merge 2, 37 rows                                                                                                    |
| `import-fidelity.json`                                                                                                                   | section 5                                                                                                                                 |
| `perf-budget-preview.json`, `perf-budget-preview-against-day0.md`                                                                        | section 8                                                                                                                                 |
| `sync-stress-preview.json`                                                                                                               | section 11.2                                                                                                                              |
| `walk-dev-4357-quick.json`, `walk-preview.json`, `walk-preview-shots/`, `walk-merge2.json` (the integrator's, against the first preview) | sections 11.4 and 13                                                                                                                      |
| `preview-walk.json`, `preview-walk-shots/` (38 screenshots), `preview-walk-2.json`, `preview-walk-shots-2/`                              | section 4                                                                                                                                 |
| `parity-audit-round-five-dev-4357.json`, `parity-audit-preview.json`, `layout-shift-preview.json`                                        | sections 3 and 7 once the chains land them                                                                                                |
| `logs/`                                                                                                                                  | every command's output: the chain timelines, the smoke, the probes, the perf run, the lint, the docker legs, the restore of the seed deck |
| `scripts/run-chain.sh`, `run-dev-phase.sh`, `run-walk2.sh`, `preview-walk.mjs`, `remove-scratch-decks.mjs`, `tables.mjs`                 | the lock chains, the scenario walk, the cleanup helper (with the protected set), the table builders                                       |
| `shots/`                                                                                                                                 | the seven screenshots of the hotfix afternoon this folder held before the round                                                           |
