# B4, the matrix and the drivers

The drivers lane of the sync and costs round (docs/SYNC.md section 6; the "Owns" list of 5.1). Written 2026-09-21 on the shared checkout at `b22ffa0` while the other lanes landed their files beside this one. Every claim below names what a run read; a row nobody drove is not driven, never passed.

## Sync round

### 1. What landed

- `docs/gslides-parity/focus/core-matrix.json`: the 16 rows of SYNC.md 6.1 in the file's row shape, with the interaction texts as the table gives them, `today` as the ordering and cost audits saw production at cbd4495 on 2026-09-20, the severity of a broken row by the product round's rule (5 and 4 give 3, 3 gives 2, 2 and 1 give 1), `setup` on the rows that make a window API write before the driven steps (the viewer's general access, the body blocks, the slide B deletes), `measure: true` on the five `cost.*` rows in SYNC.md 6.1's sense (the counts recorded beside the ceiling; the row holds the ship only over its ceiling on the preview), `note` naming the lane whose half a row waits on (the two title rows B1 and B2; the block order row and the viewer row B1; undo B2; the resend and the pull rows B3's cross instance halves), the `$comment` extended. The matrix holds 714 rows: 518 probe, 190 spec, 6 cost probe. `docs/FOCUS.md` re-rendered (`node docs/gslides-parity/focus/render-focus.mjs`, then prettier).
- `scripts/probes/core-matrix.mjs` and `core-matrix.d.mts`: `CORE_FEATURES` gains `sync` and `cost`, both in `UNPARKABLE_FEATURES`; `CORE_SPEC_DRIVERS` gains `core/sync.spec.ts`; `COST_PROBE_DRIVER` (`cost-probe`) joins `CORE_DRIVERS`; `costRows()`, `isCostRow()`; `rowsForDriver` reads the new driver. `parkedFeaturesOf` and `shipVerdict` keep the PRODUCT.md 8.2 rule for every measurement row (recorded, never counted), so a red cost row reads from `measured` and from the cost probe's own exit code; the gate adds the SYNC.md 6.2 reading (below).
- `scripts/probes/core-gate.mjs`: runs the cost probe as the third driver (`node scripts/probes/sync-cost-probe.mjs --all --base <origin> --out <out>/cost-probe.json`), merges its rows by id, renders the counts beside the ceilings under the measurement rows, `--only cost`, `--cost-rows <ids>`, `--cost-minutes <n>`; `--only specs` judges the spec rows alone and `--only probe` the walk probe's alone (before this round `--only specs` judged every row that was not the walk probe's, which would have read the cost rows as no step); `costOverCeiling` in the run's JSON and a cost row judged failed fails the run's exit code (SYNC.md 6.2: over its ceiling on the preview it holds the ship); a `--report` re-render reads `cost-probe.json` when the run has one.
- `scripts/probes/sync-cost-probe.mjs` (new, 6.3): one page state per process for 3 minutes at human speed in the shape of `audit-costs/measure.mjs`; every request the page made recorded (route, method, status, time; no header, cookie or body); `sync.status.storeCalls` sampled five times over the window at 1/3, 1/2, 2/3, 5/6 and 1 of it through `POST /api/actions/sync.status?deck=<id>` from the probe's own request context, the maximum per operation per `instance` kept and the instances recorded; the counts beside the ceilings in the JSON; exit 1 on a row over its ceiling. `--row <id>` drives one state, `--all` one child process per row and a merged JSON. On the preview the OIDC header on every request; on production nothing but the bearer for `sync.status`, read into memory from `TURBOSLIDE_TOKEN` or the origin's row of `~/.config/turboslide/hosts.json` (the JSON names the source, never the value). Locally `storeCalls` is absent or zero and the row asserts its function requests alone; a row whose ceilings are store calls alone (`cost.two-tabs-idle.calls`) records its counts and asserts its state (both tabs connected and in each other's roster); `sync.pull.no-listing` asserts the ten commits landed in A and every ops POST answered 200. A deployment run without the bearer records a row whose claim needs the counters as not driven with the reason. `cost.editor-hidden.calls` is not driven with the reason when `document.visibilityState` never reads hidden after a second page is brought to the front (the visible tab's counts are still recorded). The pure judgement (`judgeRow`, `foldSamples`, `classifyStoreCalls`, `routeOf`, `serverFnKind`, `summarize`) is exported and pinned by `scripts/probes/sync-cost-probe.test.mjs` (14 tests).
- `apps/studio/e2e/core/sync.spec.ts` (new): the ten spec rows. A and B are two browsers of one person (`lib.ts` `sameCookiesContext`, new: B carries A's storage state), C a fresh context; every test makes its deck from `/new`, tears it down in `afterEach` and in a `finally`, repeats a two person exchange three times inside the test; the sessions open by double click; the wire records every ops POST's shape and answer. The title rows annotate which mutation the title travelled as (`slide.set` or `text.splice`), the resend row skips as not driven on the `origin` claim when the record carries none (B3), the viewer row annotates C's role and fails when C is not a viewer under Anyone with the link, Viewer. `node_modules/.bin/tsc -p apps/studio/tsconfig.json --noEmit` reports nothing under `e2e/core`; Playwright's `--list` reads 10 tests.
- `scripts/probes/sync-ordering-probe.mjs` (new): the audit's run 2 to run 4 probes as one script with `--tier memory|blob` (the bounds of 6.1), `--phases`, `--edits`, `--prefix`, `--shots`; the phases `serial`, `title-open`, `title-closed`, `title-offline`, `block-open`, `block-closed`, `block-offline`, `reload`; C a stranger on the viewer floor after the general access is set to Anyone with the link, Viewer; the anomalies of the audit's vocabulary counted, the numbers to `<out>/<prefix>-ordering-run.json`, the reload documents beside it; exit 1 on an anomaly.
- `docs/gslides-parity/focus/render-focus.mjs`, `docs/readme/what-works-data.mjs`: the two features with their headings and paragraphs; the by feature table of FOCUS.md 6.3 reads "blocks the ship" for every unparkable feature with a red row (it read "parked" for chrome and the core features before). `docs/gslides-parity/focus/manual-checklist.md`: a paragraph on the sync rows and the cost rows, and why the hidden row is not a manual row.
- The tests beside this lane's modules: `scripts/probes/core-matrix.test.mjs` (the new describe for the round; the counts), `scripts/probes/core-gate.test.mjs` (the spec rows are the spec drivers' rows; the `--only` sentence). Accept or move them, as the product round did.

### 2. What a row reads on a tree where a lane's half is not merged

SYNC.md 6.2's known mechanism class. The drivers judge what the product does; they never wait for a lane. A title row on a tree where the title still travels as `slide.set` fails with the annotation "title travelled as slide.set (the whole value, audit-ordering item 1)", so the ledger reads the mechanism and not a flake. The resend row drives the word landing once in both browsers and skips on the record's `origin` when the build has none (B3, 3.2), which the gate reads as not driven with that sentence. The cost rows on a localhost server read the function requests alone (the tmp store; `sync.status` carries no `storeCalls` on this tree, B3), and say so in their measures.

### 3. The runs

Every run is against this lane's dev server on 4414 (`TURBOSLIDE_STORE=tmp` under a private `TMPDIR` in the scratchpad, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, `TURBOSLIDE_ASSIST=fixture`, `vite.no-watch.config.ts`), the memory tier, stopped before this lane returned. The cost probe is not a Playwright run and took no lock; the spec run needed `.turboslide/e2e.lock`, which another lane's Playwright run (`core/decks.spec.ts`, `slides.spec.ts`, `images.spec.ts`, started 13:22 PDT, still running at this lane's return) held for the whole of this lane's window, so the spec never ran on the checkout. Every scratch deck of the runs was trashed and deleted forever by the probe's teardown (`gone: true` on every row; the tmp store, no deployment). The evidence is under `build/b4/cost-smoke/`.

| Run                         | Command                                                                                                                                         | Started (PDT)               | Outcome                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| The owner's unit tests      | `node_modules/.bin/vitest run --project scripts`                                                                                                | 13:14 and 13:38             | 10 files, 91 tests: 90 passed; the one failure is `what-works.test.mjs` "README.md carries the markers once and the block the renderer writes" (README.md is the continuation agent's file, request 1). `sync-cost-probe.test.mjs` 14 passed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Typecheck                   | `node_modules/.bin/tsc -p apps/studio/tsconfig.json --noEmit`                                                                                   | 13:18                       | nothing under `e2e/core`; B1's in progress `room.ts` and `blob-admission.test.ts` errors (finding 4). `playwright test --list` reads the 10 tests of `core/sync.spec.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Cost probe smoke, one row   | `sync-cost-probe.mjs --row cost.editor-idle.calls --minutes 1`                                                                                  | 13:16                       | passed: 12 function requests in the minute (9 presence, 3 polls), 11.99 a minute against 12; `sync.status` 200 without `storeCalls` (absent), the store half zero on this base; teardown gone.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Cost probe smoke, every row | `sync-cost-probe.mjs --all --minutes 1`                                                                                                         | 13:20 to 13:29              | 5 passed, 1 failed, exit 1. `sync.pull.no-listing` passed (10 commits, 12 ops POSTs all 200, every word in A); `cost.editor-idle.calls` passed (12 a minute); `cost.editor-hidden.calls` failed on the visible tab's counts (12 a minute, 3 polls; `visibilityState` read visible after the second page was brought to the front) under the judgement before the fix, and reads not driven with that reason since (finding 2); `cost.editor-editing.calls` passed (12 edits, 69 requests: 54 presence, 12 ops, 3 polls, 68.99 a minute against 75); `cost.two-tabs-idle.calls` passed (23 requests over the two tabs, both connected and each in the other's roster); `cost.show.calls` passed (the load made 5 function requests, the 1 minute window after it 0, no poll). Instances 0 (no `storeCalls` on this tree). Every deck trashed and deleted forever. A 1 minute window is a smoke; the run of record is 3 minutes. |
| The sync spec               | `PLAYWRIGHT_BASE_URL=http://localhost:4414 playwright test apps/studio/e2e/core/sync.spec.ts --reporter=list,json` under `.turboslide/e2e.lock` | queued 13:31, never started | Not run: the checkout's lock was held from 13:03 by another lane's Playwright run of three core specs for this lane's whole window (`until mkdir` waited), and the waiting script was stopped before the return so it never took the lock against a stopped server. The ten `sync.*` spec rows are therefore not driven on this lane's server; nothing about them is claimed. The first run of record is the integrator's gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| The gate on 4414            | `core-gate.mjs --base http://localhost:4414 --only cost --cost-minutes 3 --lock <scratch>` and `--only specs --spec sync`                       | not started                 | Behind the same lock (a localhost gate run takes the lock for the specs). The cost probe's rows above are the gate's third driver as wired; `--report` over a folder holding `cost-probe.json` renders them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| The ordering probe          | `sync-ordering-probe.mjs --base <origin> --out <dir> --tier memory`                                                                             | not started                 | Written and syntax checked; not run this window (two browsers of other lanes were on the machine).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

### 4. Requests

- Integrator: `docs/FOCUS.md` was regenerated from the matrix (`node docs/gslides-parity/focus/render-focus.mjs && node_modules/.bin/prettier --write docs/FOCUS.md`). `README.md` was not: it carries the product round's uncommitted Advanced tools paragraph that the continuation agent commits, so this lane did not touch it. Until `node docs/readme/what-works.mjs && node_modules/.bin/prettier --write README.md` runs after that commit, two commands fail on README.md alone: `node docs/readme/what-works.mjs --check` ("README.md is not what docs/readme/what-works.mjs renders", check step 19) and `node_modules/.bin/vitest run --project scripts` (one test, `docs/readme/what-works.test.mjs` "README.md carries the markers once and the block the renderer writes"; the other 90 tests of the project pass). The section the renderer writes gains the two features as held ("Sync: 8 of 11 rows hold it", "Costs: 4 of 5 rows hold it" on today's data).
- Integrator: `.prettierignore` gains `docs/gslides-parity/sync/build/*/**/*.json` (the rule the return round's lanes carry), so the run JSON under `build/b4/` never fails check step 19; until then the JSON files there were formatted with prettier by this lane.
- Integrator, `packages/schema/src/actions.ts` (the shared header): `syncStatusSchema` gains the optional `storeCalls: { head, get, put, list, del, windowMs, instance }` B3's handler answers (`agent-actions.ts` gates it on `outputAccepts('sync.status', ['storeCalls'])`), and `pnpm generate:contracts` at the merge; without it the probe reads `sync.status` without `storeCalls` and records the store half not driven on a deployment ("sync.status carries no storeCalls on this build").
- B3, `sync.status` over HTTP: the handler's own reads of the deck (the room's open sync, `hasStoredDeck`) should count nothing in `storeCalls`, or the probe's five samples add their heads to the window they measure. The probe judges `cost.show.calls` ("no store call") on its first sample for that reason and records the four later ones; the idle and editing rows read the maximum of five samples against ceilings of 40 and 140 simple, where two samples per 60 s window add a few heads. If the counters cannot exclude them, tell B4 how many calls one `sync.status` read makes and the probe subtracts them by name.
- B1, B2 (through the rows, no file change): the title rows read the wire and annotate "title travelled as slide.set" or "text.splice"; the block order row counts a repair splice as any `text.splice` with `remove` above 0 from a session in the round's window; the undo row presses `Meta+z` with no session open after B's insert landed; the structural row expects B's Slide > Delete slide to land before A's drag's `block.set /pos` (A starts 120 ms after B's click) and reads the `.ts-conflict` card on A. If a driver's reading of your half differs from what your code does, name it in your note and B4 re points the reader.
- B5 R2 (`apps/studio/e2e/window-api.spec.ts` 365 to 400 and `scripts/perf-budget.mjs` 206 and 286, the idle budget of four server function responses in 60 s superseded by the 20 s poll): neither file is in this lane's "Owns" list this round; the integrator's, with B5's numbers (up to 6 in a 60 s window).
- B5 R4: the cost probe classifies the poll by the substring `timeoutMs` in the POST body, which reads the framed form with escaped quotes on a dev server and on a deployment; the smoke read 3 polls a minute on 4414 (the 20 s pause) and 0 on the show. No change needed.
- B6 R1 and R3 (the drain script, the Playwright rehearsal of the flip): not this lane's files under SYNC.md 5.1; left to the integrator with B6's shape.

### 5. Findings for the owners

- B5, B2 (a reading, not a defect): on this checkout's memory tier an idle editor tab posted 9 presence heartbeats in its first minute (`POST /api/decks/<id>/presence`, 5 s for 30 s then 10 s) and 3 unheld polls (every 20 s), 12 function requests in the minute against the row's ceiling of 12; B5's note reads no presence POST on the memory tier in its own windows, which this lane's smoke does not reproduce (the requests are in `build/b4/` per row). The 3 minute run of record settles at about 7 presence and 3 polls a minute if the quiet cadence holds, under the ceiling; the row is judged on the run of record, not on this minute.
- B5 (agrees with B5's finding 2): `document.visibilityState` read `visible` after a second page of the same context was brought to the front, headless, so `cost.editor-hidden.calls` is not driven on a Playwright base with that reason and the verifier drives it by hand on the preview. The first `--all` smoke read the row as failed on the visible tab's counts (12 a minute over 8; 3 polls) before the probe's judgement put the not driven reason first; the fix is in `judgeRow` and pinned by the unit test.
- Integrator and B3: `sync.status` over HTTP answers 200 without `storeCalls` on this tree (the handler gates the field on the output schema accepting it, and `packages/schema/src/actions.ts` `syncStatusSchema` does not yet), so every store half reads absent; the probe records it as such and, on a deployment, as not driven with the sentence naming B3.
- B1 (in progress at the time of this lane's typecheck, not a finding against the lane): `node_modules/.bin/tsc -p apps/studio/tsconfig.json --noEmit` reported `apps/studio/src/server/room.ts` (no exported member `synthesizeReplayed` of `@turboslide/realtime/blob`; `covers` not on `Entry`) and `blob-admission.test.ts` (`covers`), the shape of a lane's first commit landing before its second (`Entry.covers` in `protocol.ts`); nothing under `e2e/core`.

## Sync round fix round

The fixer of B4, on the tree the verifier's pass 1 read (`main` at `c4ad8b1`, the six lanes' and the integrator's work uncommitted), read from 03:05Z on 2026-09-22 (20:05 PDT on 2026-09-21) to 03:30Z, for the four findings the round assigned to this lane in `docs/gslides-parity/focus/VERIFICATION.md` "Sync and costs round, pass 1": F7 (`cost.two-tabs-idle.calls`, `list` 1), F8 (`cost.show.calls`, 11 store calls at the first sample), F9 (`joinC` and the ordering probe's C on the plain `/edit/<id>` under enforce) and F11 (`sync.block.concurrent-same-offset-order`'s 3 s bound on the blob tier), and for F13 (the ordering probe's serial latency), which sits in this lane's probe and cost one edit. Nothing here ran a git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build`; nothing on Vercel or GitHub was created, changed or deleted (the development OIDC token in `.turboslide/vercel-dev.env` was valid to 11:54Z and was not refreshed; no deployment was made: the drivers changed, not the deployed code, so the integrator's enforce preview `https://turboslide-bkwnt59my-kl01s-projects.vercel.app` is the blob tier of record); the round five branch was not touched; no token, cookie or secret is printed or written (the run JSON under `build/b4/fix-round/` was scanned for header names and JWT shaped strings, none). `.turboslide/e2e.lock` was held from 03:15:05Z to 03:16:02Z for the one Playwright run against the checkout's server; the runs against the preview took no lock (a deployment). This lane's dev server ran on 4414 (`TURBOSLIDE_STORE=tmp` under a private `TMPDIR` in the scratchpad, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, `TURBOSLIDE_ASSIST=fixture`, two 48 character secrets, `vite.no-watch.config.ts`) from 03:12Z and was stopped before the return. Every scratch deck of every run was trashed and deleted forever through the product and answered 404 (`gone: true` on every teardown line; the spec's `cleanUp` asserts it), on the tmp store and on the preview's store, which is production's.

### 1. The findings, read at the runs' JSON and the code

- **F7 and F8, the store calls inside the first window.** The pass 1 per row JSON (`cost.two-tabs-idle.calls.json`, `cost.show.calls.json` of the verifier's preview gate, read from the shared scratchpad) puts the two tabs row's `list` between 12 s and 43 s of the deck's life and the show row's between 44 s and 74 s, with the show row's `head` 9 and `put` 1 in the same window. Neither is the probe's `deck.info`: the probe invokes it through the window transport, which `controller.tsx` answers from the page's snapshot (`on('deck.info')`, no server call), so b3.md R7 (c) does not apply. The calls are the setup's afterlife, read from the code: (a) the deck's card renders once the deck has rested `CARD_THUMB_SETTLE_MS` (30 s, `card-thumb.ts`; every admitted write notes it), and a render is one put and one `list` (`thumbs.ts` `storeThumb` and `renderThumb` put the picture and `pruneThumbs` lists the slide's stamps); (b) the editor tab the show row leaves behind keeps its stream until the runtime reports the reader gone (`room.ts` `STREAM_READER_GONE_MS`, two heartbeats and 5 s, 35 s), and that close flushes the card render (`flushCardThumb`) and runs the snapshot prune (`blob.ts` `pruneAtClose`, one `list`) while the room's pulse heads ran until then; (c) the deck's open on an instance lists `assets/` once (`blob-store.ts` `pullAssets` through `ensureAssets`), which this round's preview run read at 7 s of the two tabs deck's life on the instance that opened it (`head` 13, `get` 12, `list` 1). The probe's own `sync.status` reads add one `deck.json` head each on the answering instance (R7 b), noted before the counters are read, so a quiet deck reads `head` 1 at the least and the show row's zero ceiling could never pass as written.
- **F9.** `share.setGeneralAccess` in link mode answers the link's `/s/<token>` URL once (`packages/schema/src/actions.ts`, the output's optional `url`; `apps/cli/src/records/access.ts` `shareUrl`); the route `s.$token.ts` takes the token from a navigation alone, writes the grant on the visitor's principal and redirects to `/deck/<id>`; the editor page then opens on the viewer floor. C in `joinC` and in the probe opened the plain `/edit/<id>`, which enforce never admits for a stranger.
- **F11.** The serial row splits its bound by tier (1 s memory, 5 s blob); the block row read 3 s on both. The preview run of this round agreed the two words in 2,880, 3,887 and 1,871 ms after the bursts (the same order, no repair splice), so a 3 s bound would have read two of three rounds red on a mechanism that is the tier's propagation.
- **F13.** The probe registered a word for B's sampler after `waitRevision`, so the sampler measured the checkpoint's cadence (2.2 s on every word on the memory tier) and not the word's arrival; the spec had this fixed at the merge (4.3 item 2) and the probe did not.

### 2. What changed

- `scripts/probes/sync-cost-probe.mjs`: the settle before the window: after the row's state is ready the probe reads the counters every `SETTLE_EVERY_MS` (10 s) until the row's quiet reading (`quietFor`: `list` 0 for every row; for the show row nothing beyond the probe's own heads) or `SETTLE_MAX_MS` (150 s), then pauses 2 s so the first window sample lands a full counters' window (`STORE_WINDOW_MS`, 60 s) after the settle's last read; the settle's samples, its length and whether it quieted are in `phases.settle` and in the merged `--all` JSON; a settle that never quiets is recorded and the window starts anyway (the measure line names the last read), so a product cost is never hidden. Every read of the probe is timed and each sample carries `own`, the probe's reads inside its window (`ownReadsIn`); the show row is judged on the first sample's calls beyond them (`head` less `own`, plus every `get`, `put`, `list` and `del`; a put or a list is never the probe's), the simple ceilings stay on the raw count (R7 b says they carry the samples' heads) and record `ownMax` beside it; the show row also records the function requests between the load and the window. The module comment names the mechanisms of section 1 and corrects the sentence about the probe's `deck.info`. `SETTLE_EVERY_MS`, `SETTLE_MAX_MS`, `STORE_WINDOW_MS`, `ownReadsIn` and `quietFor` are exported and pinned by `scripts/probes/sync-cost-probe.test.mjs` (16 tests, from 14: the own read count with its window edge, the quiet readings per row, the show row's judgement with own reads, a busy settle, and the changed measure text).
- `apps/studio/e2e/core/sync.spec.ts`: `joinC` reads the `url` of the `share.setGeneralAccess` answer, opens its path as a navigation in C's fresh context (`shareLinkPath`, the pathname and search of the URL whatever its origin, resolved against the run's base), waits for the `/deck/` or `/edit/` landing, then opens `/edit/<id>` as before; the ledger gets the annotation "C joined through" with the landing path (`pathForLedger`: `/s/<token>` literal when the exchange did not redirect, the deck id as `<id>`; the token is never written), and the plain address is the fallback when the answer carries no URL. `blockBound(tier)` (3,000 ms memory, 5,000 ms blob) beside `serialBound`; the block order row reads the tier, annotates it, polls to the bound, names the bound in its failure text and records the time to agreement of each round in its measure.
- `scripts/probes/sync-ordering-probe.mjs`: `BOUNDS.blob.blockConcurrent` 5,000; the access phase keeps the answer's `/s/` path in memory, C exchanges it as a navigation before `/edit/<id>` (`joinC` inside the join), `phases.access.link` says which path C took and `phases.access.landed` where the exchange landed (`pathForLedger`, the token never written); the serial phase registers each word for the samplers before `waitRevision` (`ackMs` and `revision` filled after). The header names the tier split and the join.
- `docs/gslides-parity/focus/core-matrix.json`: `sync.block.concurrent-same-offset-order`'s interaction reads "within 3 s on the memory tier and 5 s on the blob tier" and its note says why; `cost.two-tabs-idle.calls` and `cost.show.calls` gain a note on the settle and the own reads (the show's names the round after's published snapshot for the load's own reads, SYNC.md 5.2). The 714 rows and the counts of `core-matrix.test.mjs` are unchanged.
- `docs/gslides-parity/sync/build/b4/fix-round/`: the runs of section 3.

### 3. The runs

| Run                                | Command                                                                                                                                                                                                                                                  | Started (UTC)        | Outcome                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| The owner's unit tests             | `node_modules/.bin/vitest run --project scripts scripts/probes/sync-cost-probe.test.mjs scripts/probes/core-matrix.test.mjs scripts/probes/core-gate.test.mjs`                                                                                           | 03:14, 03:15         | 43 passed after the measure text was pinned again (16, 22 and 5); one assertion of the old measure text failed first and was updated to the new text.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Typecheck                          | `node_modules/.bin/tsc -p apps/studio/tsconfig.json --noEmit`                                                                                                                                                                                            | 03:14                | 3 errors, all in `apps/studio/src/server/room.ts` (`insertTieSide` not exported by `@turboslide/schema/transform`, `insertTie` not on the POST type, an arity), B1's F3 fix in progress on the shared tree; nothing under `e2e/core` (`fix-round/tsc-studio.log`). `node --check` and `prettier --check` pass on every edited file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| The three spec rows on 4414        | `PLAYWRIGHT_BASE_URL=http://localhost:4414 playwright test apps/studio/e2e/core/sync.spec.ts -g "same-offset-order\|viewer.live-updates\|reload.same-document"` under the lock                                                                           | 03:15:05             | 3 passed in 56.8 s. Block order: agreed 363, 360, 369 ms after the bursts (bound 3,000 ms, memory tier), 7 POSTs, no repair. Viewer: C joined through the `/s/` link (landed on `/deck/<id>`), role `viewer`, the words in 173, 172, 175 ms; the row is driven on the memory tier now (before this round C read `owner` on the plain address under shadow and the row skipped). Reload: C role `viewer`, reloads 917, 910, 892 ms, revisions 5, 5, 5 against the live 5, byte equal. `fix-round/sync-local.json`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| The cost probe smoke on 4414       | `sync-cost-probe.mjs --base http://localhost:4414 --all --minutes 1`                                                                                                                                                                                     | 03:15:11 to 03:26    | 5 passed, 1 not driven (`cost.editor-hidden.calls`, `visibilityState` read visible), exit 0; every settle quiet at its first read (the tmp store's counters read zero; 2.0 s each); every deck `gone: true`. `fix-round/cost-local/`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| The three spec rows on the preview | `PLAYWRIGHT_BASE_URL=<preview> playwright test ... -g "same-offset-order\|viewer.live-updates\|reload.same-document"` with `VERCEL_OIDC_TOKEN`                                                                                                           | 03:16:45             | 3 passed in 2.0 min. Block order: tier `blob`, bound 5,000 ms, agreed 2,880, 3,887, 1,871 ms after the bursts, 7 POSTs, no repair, final `pa3pb3pb2pa2pa1pb1end` (two rounds would have read red on the 3 s bound). Viewer: C joined through the `/s/` link (landed on `/deck/<id>`), role `viewer`, the words in 1,050, 549, 535 ms, no reload. Reload: reloads 1,728, 844, 1,370 ms, revisions 5, 5, 5 against the live 5, byte equal, C role `viewer`. `fix-round/sync-preview.json`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| The two cost rows on the preview   | `sync-cost-probe.mjs --base <preview> --all --rows cost.two-tabs-idle.calls,cost.show.calls --minutes 3` with `VERCEL_OIDC_TOKEN` and `TURBOSLIDE_TOKEN` (the production row of hosts.json read into the environment; the preview has no row of its own) | 03:16:48 to 03:25:24 | 2 passed, exit 0. Two tabs: the first settle read at 7 s of the deck's life on the instance that opened it read `head` 13, `get` 12, `list` 1 (the open's `pullAssets`), the second read 10 s later on the instance carrying the streams read `list` 0 and the window started (12.5 s); the five window samples on that instance read `list` 0, `put` 15 to 21, `head` 35 to 40 (2 of them the probe's), `del` 1 to 5, advanced 21 against 30, both tabs connected and mutual. Show: the settle read the editor tab's afterlife for 95 s (`head` up to 42, `put` 17 to 20, `get` 1, `list` 2 in one window, the stream's late close with its flush and prune), then quiet (`head` 6 with 6 own reads); the window's first sample read `head` 1 with 1 own read, the page's 0 against 0, function requests 0 in the window and 0 in the 95 s before it; samples 3 to 5 landed on a third instance that read `head` 5 and `get` 3 of its own (section 4 item 3). Both decks `gone: true`. `fix-round/cost-preview/`. |
| The ordering probe on the preview  | `sync-ordering-probe.mjs --base <preview> --out <dir> --tier blob --phases serial,block-open,reload --edits 6 --prefix b4fix`                                                                                                                            | 03:22:41, 73 s       | Every asked phase drove, anomalies 0, exit 0 (pass 1 ended at the access phase). Access: the `/s/` URL exchanged by C, landed on `/deck/<id>`; C joined with role `viewer` on `/edit/<id>` (no editor stage). Serial: 6 words, latency in B mean 687 ms and max 957 ms (the word's arrival now, F13), in C mean 806 ms, ack mean 663 ms, in order, none lost, none late. Block open: two rounds equal in 1,849 and 1,853 ms, no repair. Reload: 1,479, 567, 1,975 ms within 5 s, A, B and C the same document at revision 12. Teardown trashed, deleted forever, 404. The JSON holds `/s/` once, as the literal `/s/<token>`. `fix-round/ordering-preview/`.                                                                                                                                                                                                                                                                                                                                                       |

### 4. Requests and findings for the owners

1. Integrator, `docs/FOCUS.md`: re-render after the matrix edit (`node docs/gslides-parity/focus/render-focus.mjs && node_modules/.bin/prettier --write docs/FOCUS.md`); the block row's interaction text there still reads "within 3 s". `README.md` needs nothing (the renderer's block counts rows by `today`, unchanged).
2. Integrator, `docs/SYNC.md` (not this lane's file): 6.1's `sync.block.concurrent-same-offset-order` sentence gains the tier split the matrix carries; 6.3's harness paragraph gains the settle and the own read rule (the show row's "no store call" is read beyond the probe's own `sync.status` heads, R7 b); the pass 1 F8 sentence that names the probe's `deck.info` as a `list` is wrong on the transport (section 1) and the ship note should carry the mechanisms of section 1 instead.
3. B3 (a finding, no change asked this round): (a) a cold instance answering `sync.status` pulls the deck's mirror for the handler's `revision()` read (the show row's third instance read `head` 5 and `get` 3 of its own), where R7 (a) says an instance that never opened the deck reads zeros; the show row's first sample rule keeps it out of the judgement and the folded maximum carries it. (b) The minute after an editor tab navigates away costs the deck about 40 heads, 20 puts, 1 get and 2 lists on the instance that held its stream (the settle's readings of the show row), until the reader gone rule closes the stream at 35 s and the flush and prune run; the idle stream close of the round after (SYNC.md 8) is where that minute shrinks. (c) The deck's open lists `assets/` once (`pullAssets`), so every row's first minute holds one `list` on the opening instance; the settle covers it in the probe, and the pull row's `list` 2 of pass 1 (F6) had that open at 6 s inside its window with a second `list` between 24 s and 30 s that this lane cannot name from the JSON; with the settle the first is out of the window and the second is the owners' rerun to read.
4. B1 (through the rows): the block order row on the blob tier agreed at 1.9 to 3.9 s in this round's runs with the words in the same order; the 5 s bound reads the product and the tier's propagation (1.3 s mean, 2.9 s max on the serial row of pass 1) sits under it. The word split of F3 did not show in this round's five rounds (three in the spec, two in the probe).

### 5. Deviations

- The matrix row's bound differs from SYNC.md 6.1's sentence until request 2 lands; the verifier's fix names the split, and the matrix is the drivers' source of truth for the bound.
- `docs/FOCUS.md` reads the old interaction text until request 1 lands; this lane did not re-render it (not in its "Owns" list this round).
- F12 (the offline replay row's teardown on the memory tier, severity 1) is not addressed: not in this fix round's list, and the offline rows were not rerun by this lane.
- The settle's quiet reading can come from another instance than the one that did the work (the two tabs row: the busy read on `a36101f6`, the quiet one on `18ab0550`), a limit of per instance counters that 6.3 foresaw; every settle sample records its instance, so the JSON shows it. The settle lengthens a row by at most 150 s (12.5 s and 95 s in this round's preview run), so the gate's cost probe grows by a few minutes.
- The pass 1 per row JSON files that named the mechanisms were the verifier's scratchpad files (the same scratchpad root), not committed evidence; the committed `sync-p1-cost-probe-preview.json` carries the rows' measures alone.
- The preview run of the two cost rows and the ordering probe ran beside each other for about a minute (03:22:41 to 03:23:53); the cost rows are idle states and the ordering probe's bounds are 5 s on this tier, and no reading sat near a bound.
