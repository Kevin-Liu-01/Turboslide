# Turboslide focus round, the ship step

The ship step's note, written 2026-09-16 (UTC times below). The ship step verifies the preconditions of the round's ship on the merged tree, ships when they hold, and reports without shipping when a severity 2 or 3 finding stands on a core row that no seam file fixes. This pass reports without shipping. Nothing was committed, pushed or deployed; production still serves `main` at `b50e38f` (the docs only amendment commit over `2b12e31`, whose code it equals) in shadow mode. The binding specification is `docs/FOCUS.md` with `AMENDMENTS.md` A1 above it; the orchestrator's three rulings on section 9 stand; the verifier's pass 2 report is `docs/gslides-parity/focus/VERIFICATION.md`.

The ship step ran no git write command, no `pnpm install`, `pnpm add` or `pnpm exec` outside `scripts/check.mjs`, no build outside `scripts/check.mjs`, no Docker and no `vercel deploy`. Its one dev server was the check runner's own 4321 inside `--only 18`, started and stopped by the check under `.turboslide/e2e.lock` (taken 20:47:13Z, released 20:48:09Z). The run against the preview held no lock. Port 4367 was not used. No token, cookie or secret was printed.

## 1. The tree the ship step measured

- `main` at `b50e38f` equals `origin/main` (`git status -sb`: `## main...origin/main`, neither ahead nor behind). The working tree is merge 1 plus the fix round of 2026-09-16, uncommitted: `git diff --stat 2b12e31` reads 140 files changed, 27,382 insertions, 12,433 deletions, plus the untracked focus files (`apps/studio/e2e/core/`, `scripts/probes/core-*.mjs`, `scripts/probes/core-walk/`, `docs/gslides-parity/focus/build/`, `verification/`, `VERIFICATION.md`, `manual-checklist.md`, `docs/readme/what-works*.mjs`, `docs/updates.md`, the new unit tests).
- No source file changed after the verifier's pass 2 runs began (17:41Z): `find apps packages scripts docs/FOCUS.md docs/gslides-parity/focus/core-matrix.json … -newermt '2026-09-16 10:41'` lists build outputs only (`apps/studio/.output`, `.vercel/output`, `.turbo`, `routeTree.gen.ts`, `packages/native/wasm`). So pass 2 measured this code, and its product findings hold on this tree without a rerun; the fix round's cross lane requests listed in VERIFICATION.md section 9 are still not on the tree (checked by hand for b3 R14, b1 R10, b3 R18, b3 R20 below).
- The untracked `.github/` and the nine mockup PNGs under `docs/gslides-parity/design-4/*/previews/` are the pre round files day 0 recorded; an explicit path list at the commit would leave them out. No `decks/untitled-*`, `decks/e2e-*`, `scripts/__pycache__`, `.turboslide` or `.vercel` path is in `git status`.

## 2. The preconditions, checked by the ship step

| Precondition                  | Command                                                                                                             | Result                                                                                                                                                                                                                                                                                                                                                                             |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Types                         | `node_modules/.bin/tsc -b`                                                                                          | exit 0 (2.1 s, up to date)                                                                                                                                                                                                                                                                                                                                                         |
| `tokens.css`                  | `git diff --exit-code 2b12e31 -- packages/chrome/src/tokens.css`                                                    | exit 0, byte identical                                                                                                                                                                                                                                                                                                                                                             |
| Conflict markers              | `git grep -n '<<<<<<<' -- . ':!docs' ':!*.md'` and `git grep -n '^<<<<<<< ' -- '*.md' docs`                         | both empty                                                                                                                                                                                                                                                                                                                                                                         |
| Contracts current             | `pnpm generate:contracts` then a hash of the generated files before and after                                       | "generate:contracts: 14 files current"; no file changed                                                                                                                                                                                                                                                                                                                            |
| The default view words        | `node_modules/.bin/vitest run src/menus/__tests__/default-view-words.test.ts` in `packages/chrome`                  | 8 passed                                                                                                                                                                                                                                                                                                                                                                           |
| The README section            | `node docs/readme/what-works.mjs --check`                                                                           | "README.md is current"                                                                                                                                                                                                                                                                                                                                                             |
| The matrix module             | `node scripts/probes/core-matrix.mjs`                                                                               | 390 rows; 267 works, 41 broken, 21 flaky, 61 not driven (the counts of FOCUS.md 6.3)                                                                                                                                                                                                                                                                                               |
| Check steps 1, 2, 4           | `node scripts/check.mjs --only 1,2,4,5,6,19`                                                                        | ok                                                                                                                                                                                                                                                                                                                                                                                 |
| Check step 5                  | the same run                                                                                                        | FAIL under load (the load average read 19.75 during the run): 300 files, 3186 passed, 1 failed, 2 skipped in 81.4 s; the failure `apps/cli/src/commands/canvas.test.ts` "the fresh Title slide converts at the prompts' boxes"; the file alone right after: 5 passed (VERIFICATION.md F-check5, severity 1, an environment step)                                                   |
| Check step 6                  | `--only 6,19`                                                                                                       | ok in 1.8 s (the build, the client bundle 580,862 B under 600,000, the greps)                                                                                                                                                                                                                                                                                                      |
| Check step 19                 | the same run, then `node_modules/.bin/prettier --check .` partitioned by `git ls-files` and `git diff --quiet HEAD` | FAIL: 94 unformatted files; 74 are tracked and unchanged since `b50e38f` (committed unformatted before the round), 20 are this round's and all under `docs/` (the pass 2 evidence JSON under `verification/`, `build/b2.md`, `build/BUILD-STATUS.md`, the ten files under `build/b4-repros/`, `verification-3/layout-shift.json` and `.md`); no source file (F-format, severity 1) |
| Check step 18                 | `--only 18` under the lock (20:47Z to 20:48Z)                                                                       | FAIL exit 2 after 37.6 s: the `/deck` lint clean (24 audits, 0 findings, 0 states unapplied); the `/edit` lint stops at "state control not found: [data-control=\"view.gridView\"]" (`scripts/check.mjs` `EDITOR_STATES = 'editorGrid,editorMenu,editorPanel'` names the parked grid view; b1's R10 not on the tree; F19-standing, severity 2, a red tree gate)                    |
| Check step 3                  | not rerun                                                                                                           | fails by design on the uncommitted tree (the regenerated references differ from the committed ones) and passes once committed; the contracts themselves are current (above)                                                                                                                                                                                                        |
| Check steps 7 to 17, 20 to 32 | not rerun by the ship step                                                                                          | pass 2 recorded 7 to 17 green, 20 red on seven audit checks (F-step20), 21 red on three tests at the stop (F-step21), 22, 24, 26, 27, 31 and 32 not driven; the ship step did not spend the machine on a chain whose verdict the findings below settle                                                                                                                             |
| The enforce preview           | `curl -I` with `x-vercel-trusted-oidc-idp-token` on `/home`                                                         | 200 on `https://turboslide-gikztnsgb-kl01s-projects.vercel.app` (the pass 2 preview, deployed from this tree at 17:41Z); production `/home` 200                                                                                                                                                                                                                                    |
| `VERCEL_OIDC_TOKEN`           | the payload's `exp` read from `.turboslide/vercel-dev.env` without printing the token                               | expires 2026-09-16T21:15:09Z; the ship step's preview run was bounded to fit inside it                                                                                                                                                                                                                                                                                             |
| Production environment        | `vercel env ls production` (names only)                                                                             | `TURBOSLIDE_AUTHORIZE`, `TURBOSLIDE_REALTIME`, `TURBOSLIDE_MAIL`, `TURBOSLIDE_PUBLIC_STORE_HOST`, `TURBOSLIDE_SESSION_SECRET`, `TURBOSLIDE_DOWNLOAD_SECRET`, `TURBOSLIDE_TOKEN`, `BLOB_READ_WRITE_TOKEN` are set; `TURBOSLIDE_MISSING_RECORD` is not (F-missing-record's default `open` holds on production)                                                                       |
| Production deployment         | `vercel ls turboslide --prod`                                                                                       | the current production deployment is 2 h old (the push of `b50e38f`, docs only); the one before it 16 h old                                                                                                                                                                                                                                                                        |
| The repository description    | `gh repo view --json description,repositoryTopics`                                                                  | 137 characters, 20 topics; not changed (no ship)                                                                                                                                                                                                                                                                                                                                   |

## 3. The core gate, run ledger

The rule of 6.2: a rerun happens only after a code change or with a reason written in the ledger. The reason here: the ship step verifies the gate itself rather than taking pass 2's result on trust, and the token's window (21:15Z) and the preview's known stall inside `asset.add` (F-stall) bound the run to the two spec areas that hold the ship blocking rows (`surface`, and `share` with the comments and versions rows inside it). A partial run exits 1 by design and judges its own areas alone; the other areas keep pass 2's last run on this origin as their result.

| Run                                            | Origin                                                             | Started              | Duration                          | Rows driven                                                                                                                                                     | Result                                                                                                                  | Retries                                             | JSON                                                                                                                                                                     |
| ---------------------------------------------- | ------------------------------------------------------------------ | -------------------- | --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Pass 2 merged preview result (verifier)        | `https://turboslide-gikztnsgb-kl01s-projects.vercel.app` (enforce) | 2026-09-16T17:48:39Z | 50 min plus the two partial walks | 390                                                                                                                                                             | 329 passed, 60 failed, 1 not driven (manual); the two finally rows failed by the verifier's stop and read as not driven | 0                                                   | `verification/pass2-preview-merged.json`                                                                                                                                 |
| Ship step, `--only specs --spec surface,share` | the same preview                                                   | 2026-09-16T20:45:19Z | 12.1 min (725 s)                  | 25 rows of two spec files: 19 of `share.spec.ts` (12 `share.*`, 5 `collab.*`, `comments.reaches-second-browser`, `versions.restore`) and 6 of `surface.spec.ts` | 9 passed, 16 failed, 0 not driven; verdict failed (a partial run, exit 1 by design)                                     | 0 (Playwright's report: `flaky` 0, `unexpected` 16) | `verification/ship-gate-preview-specs.json` (the `--matrix` copy), `ship-gate-preview/` in the scratchpad (`core-gate.json`, `specs.json`, `core-matrix.md`, `gate.log`) |

The command: `VERCEL_OIDC_TOKEN=<from .turboslide/vercel-dev.env> node scripts/probes/core-gate.mjs --base https://turboslide-gikztnsgb-kl01s-projects.vercel.app --only specs --spec surface,share --out <scratch>/ship-gate-preview --matrix <scratch>/ship-gate-preview/ship-gate-preview-specs.json`, no `--parked` (no ship JSON exists, b3 R16). The rows:

- Passed (9): `share.copy-view-link`, `share.escape`, `share.file-menu-copy-link`, `share.stranger-cannot-edit`, `surface.advanced.off-by-default`, `surface.advanced.on-shows-parked`, `surface.parked-blocks-render`, `surface.parked-shortcut-unbound`, `surface.parked-block-core-rows`.
- Failed (16): `share.dialog.open`, `share.copy-present-link`, `share.copy-edit-link`, `share.view-link-lands-viewer`, `share.edit-link-lands-editor`, `share.view-link-cannot-edit`, `collab.edit-from-second-browser`, `collab.edit-from-owner`, `collab.slide-added-appears`, `collab.presence-chips`, `collab.rename-reaches-second-browser`, `share.view-link-excludes-skipped-and-notes`, `share.present-link-excludes-skipped`, `comments.reaches-second-browser`, `versions.restore`, `surface.advanced.remembered`.
- The mechanisms, from Playwright's failure text: the Present row's Copy link (and the Edit row's in `versions.restore`) leaves the spec's marker on the clipboard for 15 s while the snackbar reads "Link copied" and the dialog's error is empty (`{"clipboard":"cleared before the copy","error":"","busy":0,"snackbar":"Link copied"}`; six rows fall on this setup step, F-share-copy); the copied Edit link answers 404 to a fresh browser (`share.copy-edit-link`, "expected < 400, received 404", F-share-404); `surface.advanced.remembered` times out at 90 s in `waitEditor` (`core/lib.ts:76`, `page.waitForFunction`) after the navigation with the switch on (F-remembered). Each is the mechanism pass 2 read on this code.
- Against pass 2's merged result on the same origin: 15 of the 16 failed rows failed there too; `share.stranger-cannot-edit` failed in pass 2 (on the copy readback and a timing miss, VERIFICATION.md F-missing-record) and passed here with no code change between the runs, which 6.2 reads as flaky, not as passed. The 9 passing rows passed in pass 2 as well, less that one.
- The scratch decks: created from `/new` by the two specs and torn down in their `afterAll` through the product (`teardownAll` in `core/lib.ts`: File > Move to trash, Delete forever on `/decks/trash`, the actions API as the fallback, a 404 on `/edit/<id>` asserted within 20 s). The run's log names no deck id and the ship step did not list the preview's decks afterwards, so the teardown is the specs' record, not an independent read.

## 4. Verdict

Not shipped. The tree does not meet FOCUS.md 6.2 on the enforce preview, and the ship step's rule (a severity 2 or 3 finding on a core row stops the ship unless a seam file fixes it) stops it on several rows at once:

1. `surface.advanced.remembered` fails on the preview (pass 2, and again in the ship step's run, section 3). A failed `surface.*` row blocks the ship whatever the parked list says (FOCUS.md section 1 rule 4 and 6.2). Owner b4 with b1 (F-remembered).
2. Severity 3 product faults on core rows, each read by hand on the preview by the verifier on this code and none a seam file fix: the Share dialog's second Copy link in a session is refused as stale (F-share-copy, `Share.tsx`), a minted Edit link answers 404 to its visitor for up to a minute on the blob tier (F-share-404, `s.$token.ts` over `list()`), a comment's reply is not listed (F-comments-reply), the show draws no link element for a linked word (F-present-link), the Change background hex field writes no colour (F-hex-field), a restore from Version history changes nothing (F-versions), Cmd+V with the filmstrip focus pastes four objects (F-paste-multiplies), the Paste row is disabled in a second tab (F-slides-paste), a text box with autofit grow does not grow (F-autofit), a recordless deck opens as a legacy editor deck under enforce mode (F-missing-record), and `asset.add` through the window API sometimes never returns on the preview (F-asset-add-intermittent, F-stall).
3. Severity 2 findings on core rows: Format > Text > Size does nothing on a list block (F-list-size), View > Zoom > Zoom in from Fit lands on 125 (F-zoom-fit), the Transparency slider takes no mouse drag (F-transparency), the restored deck's Editing mode in the spec's chain (F-restore-mode), fourteen text rows and seven arrange rows failing in the walk's state (F-text-state, F-arrange-walk; driver faults, b4), and a Shift click at a second text box's centre moving the session (F5-standing).
4. The click model amendment A1 (`AMENDMENTS.md`, binding above FOCUS.md 2.3) is not implemented: one click at a text box's centre still opens the session, the matrix still asserts the one click rule in `text.title.single-click`, and none of A1's six new rows exists. The text rows that pass on this tree pass by the superseded rule; a ship would ship a click model Kevin has already replaced.
5. The parking edit of ruling (1) is not on the tree: `packages/chrome/src/menus/model.ts` still draws Insert > Shape with its three named rows and Insert > Line with Line and Arrow in the default view (b3's R14), while 4 of 20 shapes rows and 1 of 5 lines rows fail on the preview. The edit touches `model.ts`, `toolbar-tails.ts` and four chrome tests, b1's files, and is not a seam file; the ship step did not make it. Under FOCUS.md section 1 rule 4 a ship from this tree would also have to park `decks`, `slides`, `text`, `images`, `arrange`, `share`, `comments`, `versions` and `export` (each has a failed core row on the preview), which is not a product a sales team can use; the intent of the rule is a fix round, not that ship.
6. Red tree gates: check step 18 (F19-standing, the one line b1 R10 in `scripts/check.mjs`), step 20 (seven audit checks, F-step20), step 21 (three tests at the stop, F-step21). The ship step left R10 unapplied: dropping the `editorGrid` state removes a lint state rather than moving it behind the switch, and the choice between the two forms b1 offered is the integrator's.

Severity 1 items the ship step also left as they are, since no ship followed: the 20 unformatted files of this round (all under `docs/`, 13 of them other lanes' notes and evidence), the two placeholder cells of `BUILD-STATUS.md` (`RUN_SPECS_PLACEHOLDER`, `RUN_PREVIEW_PLACEHOLDER`), and check step 5's load timeout.

## 5. What the ship step did not run, and why

No commit, no push, no production gate, no `new-write-probe.mjs` or `hosted-smoke.mjs` on production, no production screenshots, no `gh repo edit`, no production table: each is a step after the ship, and there is no ship. Production is unchanged and the round four production table in `docs/gslides-parity/build-4/hotfix-4/` stays the last production observation. The scratch decks of the ship step's preview run were created and torn down by the specs' own teardown (section 3 records what the run's JSON says about them).

## 6. What lands before the next ship attempt

In the order a next fix round would take them; each names the request or finding as VERIFICATION.md sections 5 and 9 record it.

1. `surface.advanced.remembered` (b4 with b1): the editor's boot after a full navigation with the switch on, or the row's `waitEditor`.
2. The share tail (b6 with b7): `Share.tsx` `baseRevision()` after the first mint (F-share-copy); `s.$token.ts` resolving a token through `list()` on the blob tier (F-share-404); `TURBOSLIDE_MISSING_RECORD` and the record's read on the blob tier (F-missing-record); b3 R23 (the copied deck's record and the stream's recheck, the `resize.spec.ts` hang).
3. Comments and versions on the blob tier (b7 with b6): the reply's write or read back (F-comments-reply); the restore's write (F-versions).
4. The A1 click model (b2, b3, b4): the one click selection, the drag from inside, the double click session, and the six new rows.
5. The text and arrange product rows (b1, b2, b3): F-list-size, F-autofit, F-present-link, F-hex-field, F-zoom-fit, F-paste-multiplies, F-slides-paste (b4 FR2), F-transparency, F5-standing.
6. The drivers (b4): the walk's box B and selected word read (F-text-state), the arrange objects and the overlay read (F-arrange-walk), a bounded `invoke` (F-stall), `images.spec.ts` `pastePng` as a `ClipboardEvent` (b3 R20), `export.spec.ts:238`'s regex (b3 R18), `versions.name-current`'s locator, b7 R8.
7. The server (b7 with b4): `asset.add` on the blob tier (F-asset-add-intermittent).
8. The ship's own edits, once the rows pass: b3 R14 if any `shapes.*` or `lines.*` row still fails, `docs/gslides-parity/focus/ship-<commit>.json` (b3 R16), b1 R10, the formatting of this round's files, BUILD-STATUS.md's two cells, the FOCUS.md and README re-render.

## 7. For Kevin

See the ship step's report to the orchestrator; the items are the authorization switch (`vercel env add TURBOSLIDE_AUTHORIZE production` with the value `enforce`, then a redeploy; b6.md section 4 for what changes), the parked set (FOCUS.md section 3, with shapes and lines joining it under ruling (1) at the next ship attempt), the two manual checklist rows (`export.print.print-button`, `text.clipboard.paste-without-formatting`), the decision on sellers signing in before enforce mode (b6.md section 4 item 4), and the `TURBOSLIDE_MISSING_RECORD` decision (VERIFICATION.md section 8 item 1).

# Cycle 2 (the ship step, 2026-09-17)

The ship step's second pass, on the cycle 2 tree after the fix round: `main` at `b50e38f` plus the two cycles' uncommitted work and the fix round of 2026-09-17 (`git diff --stat b50e38f`: 162 tracked files, 20,051 insertions, 6,161 deletions; 214 paths in `git status`, the untracked focus files among them). Written 2026-09-17 (UTC times below). This pass reports without shipping. Nothing was committed, pushed or deployed; production still serves `main` at `b50e38f` in shadow mode (the current production deployment is 15 h old, the push of `b50e38f`). The binding specification is `docs/FOCUS.md` with `AMENDMENTS.md` A1 above it; the verifier's report on this tree is `docs/gslides-parity/focus/VERIFICATION.md` "Cycle 2, pass 2" (C2.1 to C2.9), whose verdict is "the tree does not ship".

The ship step ran no git write command (`git ls-remote`, `git status`, `git diff` and `git grep` only), no `pnpm install`, `pnpm add` or `pnpm exec` outside `scripts/check.mjs` and the `pnpm generate:contracts` script the ship's precondition names, no build outside `scripts/check.mjs` (its step 6), no Docker and no `vercel deploy`. It started no dev server (port 4378 stayed free; the check's browser steps were not rerun, C2.5 below says why). The one run against the preview held no lock; `.turboslide/e2e.lock` was absent throughout. The development token in `.turboslide/vercel-dev.env` had expired at 09:01:44Z when this pass began (09:08Z); `vercel env pull .turboslide/vercel-dev.env --environment development --yes` refreshed it (the same two keys, `BLOB_READ_WRITE_TOKEN` and `VERCEL_OIDC_TOKEN`; the new token expires 2026-09-17T21:11:44Z). No token, cookie or secret was printed.

## C2.1 The tree the ship step measured

- `main` at `b50e38f` equals `origin/main` (`git status -sb`: `## main...origin/main`; `git ls-remote origin refs/heads/main` reads `b50e38f`).
- No file under `apps`, `packages`, `scripts`, `docs`, `skills`, `README.md`, `AGENTS.md` or `vitest.config.ts` changed after 06:47Z on 2026-09-17, when the verifier's pass 2 deployed its preview and began its runs (`find … -newermt '2026-09-17 06:47:00 UTC'` less the build outputs lists nothing). Pass 2 therefore measured this code and its findings hold on this tree; the fix round requests it lists as not on the tree (b7's C2-R16 to C2-R23, b6's FR-1 to FR-3, b1's R32 to R38, b2's C2-R4 to C2-R6) are still not on it, and no lane note carries a section after "Cycle 2 fix round".
- Untracked paths outside this round's set: `.github/` and the nine mockup PNGs under `docs/gslides-parity/design-4/*/previews/` (pre round, day 0 recorded them) and three scratch decks `decks/e2e-realtime-mu4w1z8i`, `-mu4zuzds`, `-mu57y537` (left by check chains' spec runs on the file store; C2-F29 named eight, five are gone). An explicit path list at a commit would leave all of them out. No `.turboslide`, `.vercel`, `scripts/__pycache__` or `decks/untitled-*` path is in `git status`.
- `docs/gslides-parity/focus/verification/parked-cycle2.json` reads `{ "commit": null, "parkedFeatures": ["shapes", "lines"] }`; no `ship-<commit>.json` exists (none is written without a ship).

## C2.2 The preconditions, checked by the ship step

| Precondition                  | Command                                                                                                | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ----------------------------- | ------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Types                         | `node_modules/.bin/tsc -b`                                                                             | exit 0 (09:09:41Z, 2 s)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `tokens.css`                  | `git diff --exit-code 2b12e31 -- packages/chrome/src/tokens.css`                                       | exit 0, byte identical                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Conflict markers              | `git grep -n '<<<<<<<' -- . ':!docs' ':!*.md'` and `git grep -n '^<<<<<<< ' -- '*.md' docs`            | both empty                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Contracts current             | `pnpm generate:contracts` with a hash of `packages/agent/generated` and `skills` before and after      | "generate:contracts: 14 files current"; the hash unchanged (`07cb590d86fef5c4`)                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| The matrix module             | `node scripts/probes/core-matrix.mjs`                                                                  | 397 rows; 267 works, 45 broken, 21 flaky, 64 not driven (the counts of FOCUS.md 6.3)                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| The README section            | `node docs/readme/what-works.mjs --check`                                                              | "README.md is current"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| The default view words        | `node_modules/.bin/vitest run src/menus/__tests__/default-view-words.test.ts` in `packages/chrome`     | 8 passed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Check steps 1, 2, 4           | `node scripts/check.mjs --only 1,2,4,5,6,19` (09:09:43Z to 09:11:43Z)                                  | ok (step 1 "Already up to date" in 0.3 s, step 2 in 0.7 s, step 4 in 0.4 s)                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Check step 5                  | the same run (`pnpm test`)                                                                             | ok in 80.2 s: 306 files passed, 3277 tests passed, 2 skipped (the load average read 4.0; no child process timeout this time)                                                                                                                                                                                                                                                                                                                                                                                          |
| Check step 6                  | the same run                                                                                           | ok in 1.8 s (the build from the turbo cache, the client bundle check with the server marker in 0 of 95 client files, the greps)                                                                                                                                                                                                                                                                                                                                                                                       |
| Check step 19                 | the same run, then the 74 files compared against `git status` and `git diff --quiet b50e38f -- <file>` | FAIL exit 1 after 35.8 s: `pnpm format:check` lists 74 files; every one is tracked and unchanged since `b50e38f` (committed unformatted before the round), none is in `git status`, none is under this round's untracked folders; `what-works.mjs --check` passes alone (C2-F15, severity 1, the ship step's; left as it is since no ship followed and a format pass over 74 pre round files is not a seam edit)                                                                                                      |
| Check step 3                  | not rerun                                                                                              | fails by design on the uncommitted tree and passes once committed; the contracts are current (above)                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Check steps 7 to 18, 20 to 32 | not rerun by the ship step                                                                             | pass 2 ran every one of them on this code between 06:51Z and 08:50Z (C2.2 of VERIFICATION.md): 7 to 18, 22 to 24 and 28 to 30 ok, 27 skipped, 20 FAIL (5 audit checks, C2-F13), 21 FAIL (12 of 72 editor spec tests, C2-F14), 26 FAIL (3 of 51, C2-F17), 31 FAIL (the perf budget, C2-F18), 32 FAIL (382 passed, 14 failed, 1 not driven on 4321; `surface.cleanup` blocking there, C2-F19); no code changed since, so the readings stand and the ship step did not spend two hours of the machine to read them again |
| The enforce preview           | `curl` on `/home` with `x-vercel-trusted-oidc-idp-token`, then without                                 | 200 with the header, 302 without, on `https://turboslide-1ew78vsd0-kl01s-projects.vercel.app` (the pass 2 preview, deployed from this tree at 06:47Z); production `/home` 200                                                                                                                                                                                                                                                                                                                                         |
| `VERCEL_OIDC_TOKEN`           | the payload's `exp` read from `.turboslide/vercel-dev.env` without printing the token                  | expired 2026-09-17T09:01:44Z at the start of this pass; refreshed by `vercel env pull` to 2026-09-17T21:11:44Z                                                                                                                                                                                                                                                                                                                                                                                                        |
| Production environment        | `vercel env ls production` (names only)                                                                | `TURBOSLIDE_AUTHORIZE`, `TURBOSLIDE_REALTIME`, `TURBOSLIDE_MAIL`, `TURBOSLIDE_PUBLIC_STORE_HOST`, `TURBOSLIDE_SESSION_SECRET`, `TURBOSLIDE_DOWNLOAD_SECRET`, `TURBOSLIDE_TOKEN`, `BLOB_READ_WRITE_TOKEN` set; `TURBOSLIDE_MISSING_RECORD` not set (the `open` default holds)                                                                                                                                                                                                                                          |
| Production deployment         | `vercel ls turboslide --prod`                                                                          | the current production deployment is 15 h old (the push of `b50e38f`); the one before it 1 d                                                                                                                                                                                                                                                                                                                                                                                                                          |
| The repository description    | `gh repo view --json description,repositoryTopics`                                                     | 137 characters, 20 topics; not changed (no ship)                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

## C2.3 The core gate, run ledger

The rule of 6.2: a rerun happens only after a code change or with a reason written in the ledger. The reason here: the ship step verifies the gate itself rather than taking pass 2's result on trust, and it bounds the run to the two spec areas that hold the ship blocking rows (`surface`, and `share` with the collab, comments and versions rows inside it), which are also the rows pass 2 filed as flaky or failing on the blob tier's second browser (C2-F25, C2-F28). A partial run exits 1 by design and judges its own areas alone; every other row keeps pass 2's last run on this origin as its result. No code changed between pass 2's runs and this one, so a row that differs between the two is flaky under 6.2, not fixed.

| Run                                            | Origin                                                                                                                | Started                                                                                         | Duration                        | Rows driven                                                                                                                                                     | Result                                                                                                            | Retries                  | JSON                                                                                                                                                                                   |
| ---------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Pass 2 merged preview result (the verifier)    | `https://turboslide-1ew78vsd0-kl01s-projects.vercel.app` (enforce; deployed by the verifier at 06:47Z from this tree) | 2026-09-17T06:50:34Z (the run of record) plus three partial walks and the specs rerun to 07:56Z | 34.7 min plus the partial walks | 397                                                                                                                                                             | 382 passed, 14 failed, 1 not driven (manual); 8 rows flipped between two runs of the deployment (C2-F25)          | 0                        | `verification/cycle2-pass2-preview-merged.json`                                                                                                                                        |
| Ship step, `--only specs --spec surface,share` | the same preview                                                                                                      | 2026-09-17T09:12:14Z                                                                            | 6.6 min (395 s)                 | 25 rows of two spec files: 19 of `share.spec.ts` (12 `share.*`, 5 `collab.*`, `comments.reaches-second-browser`, `versions.restore`) and 6 of `surface.spec.ts` | 20 passed, 5 failed, 0 not driven; verdict failed (a partial run, exit 1 by design); every `surface.*` row passed | 0 (asserted by the gate) | `verification/cycle2-ship-gate-preview-specs.json` (the `--matrix` copy); `ship2/gate-preview-specs/` in the scratchpad (`core-gate.json`, `specs.json`, `core-matrix.md`, `gate.log`) |

The command: `VERCEL_OIDC_TOKEN=<from .turboslide/vercel-dev.env> node scripts/probes/core-gate.mjs --base https://turboslide-1ew78vsd0-kl01s-projects.vercel.app --only specs --spec surface,share --out <scratch>/ship2/gate-preview-specs --matrix docs/gslides-parity/focus/verification/cycle2-ship-gate-preview-specs.json`, no `--parked` (no ship JSON exists). The run overlapped nothing: the quick check chain (steps 1 to 6 and 19) had ended at 09:11:43Z, no dev server was up and no other Playwright process was alive. The rows:

- Passed (20): `share.dialog.open`, `share.copy-view-link`, `share.copy-present-link`, `share.copy-edit-link`, `share.escape`, `share.file-menu-copy-link`, `share.view-link-lands-viewer`, `share.edit-link-lands-editor`, `share.view-link-cannot-edit`, `share.stranger-cannot-edit`, `collab.edit-from-second-browser`, `collab.rename-reaches-second-browser`, `share.view-link-excludes-skipped-and-notes`, `share.present-link-excludes-skipped`, `surface.advanced.off-by-default`, `surface.advanced.on-shows-parked`, `surface.advanced.remembered`, `surface.parked-blocks-render`, `surface.parked-shortcut-unbound`, `surface.parked-block-core-rows`.
- Failed (5): `collab.edit-from-owner`, `collab.slide-added-appears`, `collab.presence-chips`, `comments.reaches-second-browser`, `versions.restore`. Every one is a second browser row on the blob tier (a change made in one browser read by another within its bound), the mechanism of C2-F28 and C2-F25.
- Against pass 2's merged result on the same origin with no code change between the runs: `collab.slide-added-appears`, `collab.presence-chips` and `comments.reaches-second-browser` failed there too (C2-F28 stands); `collab.edit-from-owner` and `versions.restore` passed there and failed here, which 6.2 reads as flaky, not as failed once and passed once. Every one of the 20 passing rows passed in pass 2 as well; the share cluster's copy, link and refusal rows have now passed three runs on this deployment (C2-F2 stays closed). `surface.advanced.remembered` passed for the second time on this deployment (C2-F3 stays closed).
- The scratch decks: created from `/new` by the two specs and torn down in their `afterAll` through the product (`teardownAll` in `core/lib.ts`: File > Move to trash, Delete forever on `/decks/trash`, the actions API as the fallback, a 404 on `/edit/<id>` asserted within 20 s). The run's JSON and log name no deck id, so the teardown is the specs' record, not an independent read; the ship step did not list the preview's decks afterwards.

## C2.4 Verdict

Not shipped. The tree does not meet FOCUS.md 6.2 on the enforce preview, and the ship step's rule (a severity 2 or 3 finding on a core row stops the ship unless a seam file fixes it) stops it on these rows; none of the files a fix needs is a seam file or the ship step's (the owners are b7, b6, b4 and b1 by the ownership table and the orchestrator's correction), so the ship step made no fix.

1. Severity 3 on core rows, the product on the blob tier (owner b7, with b6 and b4): the saving stall (C2-F24: ten minutes into pass 2's walk on the fresh preview the window API's writes stopped being acknowledged, the title row read "Saving..." for eight minutes and 162 rows were left not driven until partial walks drove them; the same mechanism the cycle 2 integrator's run of record met on 2026-09-17T00:31Z; the acknowledgement path in `room.ts`, the ops route, the room client's outbox and the blob store's deadline is unread and untouched by the fix round); `images.replace.drop-on-picture` (C2-F27: `asset.add` through the window API does not answer on the blob tier, two of two runs); `collab.presence-chips` and `comments.reaches-second-browser` (C2-F28: the second browser rows of the blob tier, presence per instance and the comment's pull).
2. Severity 2 on core rows, flaky under 6.2 with no code change between the runs (C2-F25; owner b7 for the blob tier, b1 for the File menu): `present.skipped-left-out`, `collab.slide-added-appears`, `comments.toolbar-and-menu-routes`, `export.print.file-menu-after-close`, and the four rows the stall took in the run of record (`versions.open-from-last-edit`, `versions.undo-restore`, `versions.restore`, `export.print.include-skipped`). The ship step's own bounded rerun on the same deployment (C2.3) added two flips: `collab.edit-from-owner` and `versions.restore` passed in pass 2 and failed here; ten rows of one deployment now differ between runs with no code change.
3. Severity 3 on core rows whose product half is measured working and whose driver or plan is not on the tree: `text.link.present-click` and `images.present.picture-and-ground` (C2-F5; b1's R32 for the two drivers' selectors, b4's files), `text.format-menu.size-increase` (C2-F1: the Format menu's Align, Spacing and Bold plans write `/typography` on a plain list block and the server refuses the write; b7's C2-R16 to b1, `editor-shell.ts`).
4. Severity 2 on core rows by the drivers' own setup: `decks.access.paint` (C2-F6; C2-R19, a trashed deck opened expecting Editing mode) and `images.export.pdf-with-picture` (C2-F8; C2-R20, the sixth download of an identity whose quota is five a day). Both are b4's spec files.
5. The two parked features fail with the switch on: `shapes.border.colour-weight-dash` and `lines.context.line` (C2-F11, b1 with b3). These are recorded deviations behind the switch and would not stop a ship on their own; `parked-cycle2.json` lists `shapes` and `lines`.
6. A blocking `surface` row on the check's server: `surface.cleanup` reads `/deck/<id>` 200 after Delete forever on the check's 4321 server (C2-F19, b7; C2-R22's `withDeckLock` around the stamp writes). It passes on the preview and on 4379, so it is a tree gate finding (check step 32 red), not a preview failure; the round asks the check green.
7. Red tree gates at severity 2: check step 20 (5 audit checks, C2-F13), step 21 (12 of 72 editor spec tests, C2-F14), step 26 (3 two browser spec tests, C2-F17: two parked assertions in the default view, C2-R21, and the realtime undo row), step 31 (the perf budget's `js decoded` on every route and `dom nodes` on the viewer, the editor and the filmstrip, C2-F18), step 32 (above), and `resize.spec.ts` hanging at its 50 percent test on a tmp store server (C2-F23; b6's FR-1, one browser context for the serial chain, is not on the tree). The round asks `resize.spec.ts` green.
8. Under FOCUS.md section 1 rule 4 a ship from pass 2's merged preview result would park `decks`, `text`, `images`, `present`, `share`, `comments` and `export` beside `shapes` and `lines` (each has a failed core row on the preview), leaving `slides`, `arrange`, `versions` and `help` in the default view; that is not the product the round set out to ship, and the rule's intent is a fix round.

What holds on this tree, so the next attempt starts from it: the click model of A1 passes every one of its rows on the preview and locally, in the walks and in the verifier's hand drive (C2.7 of VERIFICATION.md); the two views hold (no parked row, control or word in the default view at 1440 or 390; the switch remembered across a reload and off in a fresh browser); the share cluster's copy, link and refusal rows pass (C2-F2 closed); every `surface.*` row passes on the preview; `hosted-smoke.mjs` 40 of 40 and `new-write-probe.mjs` all steps ok on the preview; compare-to-shoot 170 pairs under 0.5 percent; `canvas-fidelity.mjs` ok; `tokens.css` byte identical to `2b12e31`; types clean; 3277 unit tests passing.

Severity 1 items left as they are, since no ship followed: the 74 pre round unformatted files (C2-F15), the layout shift audit's six `/edit` cells (C2-F12), the three scratch `decks/e2e-realtime-*` folders (C2-F29; the integrator's to remove), the React key spread warning (R33).

## C2.5 What the ship step did not run, and why

No commit, no push, no production gate, no `new-write-probe.mjs` or `hosted-smoke.mjs` on production, no production screenshots, no `gh repo edit`, no production table: each is a step after the ship, and there is no ship. Production is unchanged; the last production observation stays the round four table in `docs/gslides-parity/build-4/hotfix-4/`. Check steps 7 to 18 and 20 to 32 were not rerun (C2.2: pass 2 ran them on this code less than three hours earlier and no file changed); step 25 (Docker) never. The full core gate against the preview (the walk's 296 rows plus the seven specs, 35 minutes when it does not stall) was not rerun: its failing rows are settled by the findings above, and its one open question, whether the stall of C2-F24 recurs a third time, is the server lane's to read with the deployment's runtime log in reach, not the ship step's to observe again without one. The 4378 dev server was not started.

## C2.6 What lands before the next ship attempt

In the order a next fix round would take them; each names the request or finding as VERIFICATION.md C2.5 and the lanes' fix round sections record it.

1. The stall on the blob tier (b7; C2-F24): the acknowledgement path (`apps/studio/src/server/room.ts` `admitOps`, `routes/api/decks.$deckId.ops.ts`, `packages/realtime/client/room-client.ts`, `packages/store/src/blob-store.ts`) read with the function log of the window in reach, and the render worker sharing the instance (`thumbs.ts`, the export worker) ruled in or out; `asset.add` on the blob tier after six pictures (C2-F27); presence per instance and the second browser's comment pull (C2-F28, with b6); the memory tier's `admitOps` dropping the second `slide.insert` (b2's C2-R4); `withDeckLock` around the stamp writes (C2-R22, C2-F19).
2. The plan guard for a plain list block (b1; C2-R16, C2-F1) and the validator's sentence (C2-R17); the File menu after Close preview (C2-F25's last row, `MenuBar.tsx`); the Border color plate and Line end from the right click menu with the switch on (C2-F11, R35 with b3); the two parked assertions of check step 26 moved behind the switch (C2-R21); the parity audit's five checks (C2-F13, R37, R38 with b6).
3. The drivers (b4): the show's stage selectors (R32, C2-F5), the restore before `decks.access.paint` (C2-R19), the download's refusal sentence and the quota budget (C2-R20), `clickControl`'s `scrollIntoViewIfNeeded` (C2-R18), the skipped mark on the memory tier, `objects.spec.ts`'s Shapes row (R34), `resize.spec.ts`'s one browser context (b6's FR-1), the twelve editor spec tests of step 21 (C2-F14).
4. The integrator: the perf budget's ceilings or the bundle (C2-F18, measured against `main`'s build first), the three scratch decks under `decks/` (C2-F29), the React key spread at `FormatOptions.tsx` (R33).
5. The ship's own edits, once the rows pass: `docs/gslides-parity/focus/ship-<commit>.json` from `parked-cycle2.json` with the commit filled in, the FOCUS.md and README re-render if the matrix changed, and the 74 pre round files' formatting in a commit of its own if Kevin wants step 19 green.

## C2.7 For Kevin

See the ship step's report to the orchestrator; the items are the authorization switch (`vercel env add TURBOSLIDE_AUTHORIZE production` with the value `enforce`, then a redeploy; b6.md section 4 for what changes: decks with no access record stay open, decks made from `/new` since the record landed open only for their owner's browser, grantees and link holders, the plain `/deck/<id>` and `/edit/<id>` addresses sellers already sent stop opening for others, a seller whose anonymous cookie is gone is a stranger to their own decks until accounts land), the parked set (FOCUS.md section 3 with shapes and lines under ruling (1)), the two manual checklist rows (`export.print.print-button`, `text.clipboard.paste-without-formatting`), the decision on sellers signing in before enforce mode (b6.md section 4 item 4), and the `TURBOSLIDE_MISSING_RECORD` decision (VERIFICATION.md section 8 item 1).
