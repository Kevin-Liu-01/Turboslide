# B3 The store tier

The note of builder B3 of the sync and costs round (2026-09-21, times in PDT unless marked). The binding specification is `docs/SYNC.md` (3.2 the record's origin and the write path's check, 3.5 the head read and never listed, 3.6 the snapshots, the holes and the prune, 3.10 the card thumbnail, 4.5 the budget, 5.1 the "Owns" list, 6.3 the counters, 6.4 the named tests); `docs/HOSTING-MOVE.md` sections 7 and 9, `docs/PRODUCT.md` section 7, `docs/RETURN.md`, `docs/FOCUS.md` and `docs/gslides-parity/focus/AMENDMENTS.md` A1 stand under it; the audits under `docs/gslides-parity/sync/audit-*.md` are the evidence. Nothing here ran a git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build`; the blob tier ran against `blob-fake.ts` alone, never a store token; nothing on Vercel or GitHub was created, changed or read. The tree was at `b22ffa0` plus the other lanes' in progress work when this lane started; every file this lane edited is in section 2 and in SYNC.md 5.1's B3 row.

## Sync round

### 0. Requests, written first so the owners can act while this lane builds

Every request names the file, its owner under SYNC.md section 5 and the exact change. This lane's own files typecheck and pass their tests without them; the rows or the behaviour each unblocks are named.

**R1 (the integrator, `packages/schema/src/actions.ts`; SYNC.md 5.1's shared file table "B3 lands `sync.status.storeCalls` and `deck.info.counts.records`, `.holes` on day 0"; unblocks the cost probe's reading of `storeCalls` and the `holes` count of 6.4).** Two entries, both optional so every transport that answers today keeps answering:

- `syncStatusSchema` (line 864) gains, after `connected`:
  ```ts
  /** The calls this instance made to the Blob store under the deck's prefix in the last `windowMs`, by operation, and the id of the answering instance (docs/SYNC.md 6.3); absent off the blob tier and in the page's own answer */
  storeCalls: z
    .strictObject({
      head: z.number().int().nonnegative(),
      get: z.number().int().nonnegative(),
      put: z.number().int().nonnegative(),
      list: z.number().int().nonnegative(),
      del: z.number().int().nonnegative(),
      windowMs: z.number().int().positive(),
      instance: z.string().regex(/^[0-9a-f]{8}$/),
    })
    .optional(),
  ```
- `deck.info`'s `output.counts` (line 1082) gains, after `snapshots`:
  ```ts
  /** the version records the log holds (docs/SYNC.md 3.6) */
  records: z.number().int().nonnegative().optional(),
  /** the record numbers missing between the log's first and last record (docs/SYNC.md 3.6, the hole's second rule) */
  holes: z.number().int().nonnegative().optional(),
  ```
- `pnpm generate:contracts` once at the merge, as the table says. The handlers of R2 read the table's shape at run time (`outputAccepts` in `agent-actions.ts`) and add each field only once the table names it, so R1 and R2 land in either order and the dispatcher's output check never refuses an answer.

**R2 (the integrator, `apps/studio/src/server/actions.ts` `deckDispatcher`, after `registerReadActions(dispatcher, {...})` at line 1034; unblocks `/api/actions/sync.status` for the cost probe and `deck.info.counts.records` and `.holes` on the HTTP and MCP transports).** One call and two imports:

```ts
import { registerStoreStatusActions } from './agent-actions';
import { realtimeTier } from './room';
// after registerReadActions(...):
registerStoreStatusActions(dispatcher, { deckId, store: deckStore, tier: realtimeTier() });
```

The call goes after `registerRecordActionsFor(...)` (line 715) as well: the CLI's record actions register a `sync.status` of their own (`apps/cli/src/record-actions.ts` 762, `syncStatus`: `seq` 0, `tier` `memory` whatever the deployment runs, `transport` `file`), which is what `/api/actions/sync.status` answers on this tree (measured on 4413: `{"seq":0,"revision":25,"pending":0,"retained":0,"tier":"memory","transport":"file","connected":false}`), and a later `register` of the same id replaces it. `registerStoreStatusActions` (this lane, `agent-actions.ts`) registers `sync.status` with the store's revision as `seq` and `revision`, the deployment's tier and `storeCalls` (the window transport answers it in the page, controller.tsx 3449, and keeps doing so) and re-registers `deck.info` with the reader's own `deckInfo` plus `counts.records` and `counts.holes`. Its imports of the store and the reader run inside the handlers, so the client stub of `agent-actions.ts` (the edit route imports it) pulls no store graph; a static import of `agent-actions.ts` from `actions.ts` makes no cycle (`agent-actions.ts` reaches `actions.ts` through a dynamic import inside its handler alone).

**R3 (B1, `packages/realtime/src/blob.ts` `stopWatching`, line 371; SYNC.md 3.6 "the prune runs every 20th commit and once when the deck's last stream on the instance closes", invariant 13).** When the last stream of a deck on the instance closes (`state.listeners <= 0`, line 679), run the store's prune once, on the deck's queue so it never runs beside an append: `void queued(deckId, async () => { const store = (await storeOf(deckId)) as DeckStore & { pruneSnapshots?: () => Promise<number> }; await store.pruneSnapshots?.(); }).catch((error) => onError(error, `blob: pruning ${deckId} at the last stream's close`))`. `BlobStore.pruneSnapshots()` exists (it heads the manifest and lists `snapshots/` once); the file and memory tiers have no such method and the optional call is a no-op there. The write path prunes on every `SNAPSHOT_PRUNE_EVERY` records (this lane); without R3 a deck edited fewer than twenty times between two visits keeps its unreferenced snapshots until its twentieth record, which costs storage cents and no correctness.

**R4 (B1, `apps/studio/src/routes/api/decks.$deckId.stream.ts`, the closer at line 265 or the slot's release at 140; SYNC.md 3.10 "the card thumbnail renders ... once when the stream closes"; the row `decks.card.thumbnail-slide-1` reads its 10 s bound from this call once the settle is 30 s).** In the stream's one close path, after the slot's release: `const { flushCardThumb } = await import('../../server/card-thumb'); flushCardThumb(deckId);` (the dynamic import, as `write.ts` 598 imports `scheduleCardThumb`). `flushCardThumb` (this lane) moves a pending render to now, holds it at the 8 s floor when one ran inside it, and does nothing when no write waits, so calling it on every stream close is free for a reader's stream. Without R4 the card renders 30 s after the last write; a seller who edits and closes the tab inside those 30 s gets the card on the next write of the deck or never, which is the one part of `decks.card.thumbnail-slide-1` this lane cannot close alone.

**R5 (B2 with B1, the tab hide; SYNC.md 3.10 "once when the tab hides"; optional, the smaller half of the saving).** The server has no signal of a hidden tab today (no `hidden` or `visibilityState` in `protocol.ts`, `room-client.ts` or the presence route). The one change that fits the shared file table: `PresencePost` gains an optional `hidden?: boolean` (B1, `protocol.ts` `presencePostSchema`), the room client posts one heartbeat with `hidden: true` when `document.visibilityState` turns hidden (B2, `room-client.ts` `heartbeat`, beside the quiet cadence of 3.10), and the presence route calls `flushCardThumb(deckId)` when a post carries it (B1, `decks.$deckId.presence.ts`, the same dynamic import as R4). Declined, the stream's close (R4) and the 30 s rest cover the seller who closes the tab or stops typing; a tab hidden mid edit and kept open renders at the rest.

**R6 (B1, `packages/realtime/src/blob.ts` `announce`; SYNC.md 3.6 "the reader: ... `announce` publishes an external checkpoint at the first record above a hole (the tabs reload once at the head) and ops from the next record on", invariant 1).** The pull now continues past a missing record number (section 1.4), so `store.records()` on this tier can hold `n` and `n + 2` with `n + 1` missing, where before the mirror ended at `n`. `announce` publishes an `op` per record above `state.lastSeq` in log order; a tab at the revision of `n` then receives the op of `n + 2` with a base above its position and waits on its gap watch (8 s) or resyncs. The rule to land: while iterating the records, a record whose `n` is not the previous record's `n + 1` (the previous being in the same list; the first record of the list is never a hole's far side) is published as `checkpointOf(record, true)` (external, the tabs reload at the head once) and the records after it as ops, as `isReplayableRecord`'s restore case does today. `logHoles(records)` in `@turboslide/store/versions` counts the holes the same way for `deck.info`. Without R6 a hole is correct and slower (the gap watch), never a lost edit.

**R7 (B4, `scripts/probes/sync-cost-probe.mjs`, `docs/gslides-parity/focus/core-matrix.json`; the rows of section 3).** Three facts for the probe: (a) `sync.status.storeCalls` counts every call the bounded client made under `decks/<id>/` in the last 60 s, on the answering instance alone, and the answer's `instance` is an eight character id the process mints once, so two answering instances are told apart by it (SYNC.md 6.3); a sample from an instance that never opened the deck reads zeros with its own id, which is a fact about routing, not about the deck. (b) A `sync.status` sample costs the deck one `head` of `deck.json` when the mirror's sync window (750 ms) has passed, so five samples add up to five heads to a 3 minute window; the ceilings of 6.1 carry that. (c) `deck.info` still lists `snapshots/` once for `counts.snapshots` (unchanged this round), so a row that asserts `storeCalls.list` is 0 samples `sync.status` before it calls `deck.info`, or reads `deck.info` from the record the walk keeps. Until R1 and R2 land the HTTP `sync.status` is the CLI's placeholder (`seq` 0, `tier` `memory`, no `storeCalls`) and `deck.info` carries no `records` or `holes`; on the tmp store (a local base) `storeCalls` reads zeros as 6.3 says.

**R8 (a record for the integrator and B1; no action).** `Write.origin` landed in B1's `packages/schema/src/mutations.ts` at 190 as `{ clientId: string; opIds: string[] }`, the shape this lane's `StoreWrite` (`store.ts`) and `writeOriginSchema` (`versions.ts`) carry; the store reads `write.origin` structurally, so the merge order B3 then B1 of the shared file table holds and `StoreWrite` can become `Write` once both are merged. B1's `blob.ts` already passes the batch's origin to `store.write` and reads `WriteOutcome.replayed` (section 1.2); B2's `room-client.ts` already reads `EditorDeck.origins` (section 1.6). Nothing to change; the shapes agree.

### 1. What was built, by item

Every item names the SYNC.md section, the mechanism as measured, the change, its test and the rows it serves. The measurements are unit tests against `blob-fake.ts`, which records every operation; no deployment exists for this round's tree yet and this lane's dev server runs the tmp store, where the store tier is not exercised (6.3: `storeCalls` reads zero on a local base). A row is "driven" only in section 3's terms.

#### 1.1 The record names its origin, tolerated first (3.2; 6.4 "a record with origin parses under the tolerant schema")

- `packages/store/src/store.ts`: `WriteOrigin` (`{ clientId, opIds }`), `StoreWrite` (`Write & { origin?: WriteOrigin }`), `VersionRecord.origin?` and `DeckStore.write` typed over `StoreWrite`; the ok branch of `WriteOutcome` gains `replayed?: VersionRecord` (1.2).
- `packages/store/src/versions.ts`: `writeOriginSchema` (strict, `clientId` a non empty string, `opIds` an array of non empty strings) and `versionRecordSchema.origin` optional, so a reader on deployment N parses a record N plus 1 wrote and a record without the field parses as before; `RECORD_ORIGIN_WRITES = false` is the deployment constant of 3.2's landing (N tolerates and writes nothing; N plus 1 flips it to `true`; the ship note records that a rollback of N plus 1 lands on N, whose parser reads what N plus 1 wrote). `BlobStoreOptions.writeOrigin` overrides it (the tests pass `true`).
- Test: `versions.test.ts` "tolerate the origin a later deployment writes and parse a record without one as before": the record with an origin round trips, without one has no field, a foreign key inside `origin`, a missing `opIds`, an empty `clientId` and a numeric id are refused; `RECORD_ORIGIN_WRITES` is false. `versions.test.ts` "parse strictly" still refuses an unknown top level field.

#### 1.2 The write path's origin check and the record's origin (3.2, invariant 3; 6.4 the two instance resend)

- `blob-store.ts` `write()`: between the pull and `file.write`, against the records the head and the pull just proved, `recordNamingOps(readVersions(dir), write.baseRevision, write.origin.opIds)` (versions.ts, pure: the newest record above the base whose `origin.opIds` shares an id) answers `{ ok: true, document: <the mirror's>, revision: record.revision, entry: record, changed: [], issues: [], warnings: [], replayed: record }` with no snapshot, no claim, no manifest put and no pulse. A write without an origin, or one whose ids no record above its base names (every first attempt), takes the write path as before. The record written by a commit carries `origin` when `writeOrigin` (the option, else `RECORD_ORIGIN_WRITES`) is on.
- Measured (`hosted.test.ts` "two instances, one fake: the resend of a committed POST is answered with its record and commits nothing, and a third mirror applies the fold once"): instance one commits `{ tab-a, [op-1, op-2] }` as record 1 at 413 with the origin stored; instance two, its mirror at 412 with no record of the first attempt, receives the same write at base 412 and answers `ok`, `replayed.n` 1, `revision` 413, `entry.n` 1, `changed` empty, with zero puts by the resend and one record in the store; a resend of one op of the batch is the same answer; another POST from the same base with its own ids is the ordinary conflict at the head; a third mirror reads one record, the fold once (size 22) and the origin; a fourth store with the deployment default writes a record with no origin beside it.
- The channel's half is B1's and is on their tree (`blob.ts` `replayedRecordOf`, `synthesizeReplayed`; R8).

#### 1.3 The pull without the listing (3.5, invariant 7; audit-ordering item 3; 6.4 "a remote commit costs the reader no list")

- `blob-store.ts` `pull()`: `client.list(prefix)` is gone. The mirror's records keep their manifest rows (a record never changes, so the bytes this instance proved once are the store's; a row the manifest lost is the local bytes' md5, no call). The snapshot the manifest etag names is read first (one `get`, free on a CDN hit) and proves the document as before; its revision is the target of the walk. The records are walked by number from the mirror's last (`walkRecords`): each a `head` and a `get` (the proven read past the edge kept), one number at a time at the log's head and up to `RECORD_WALK_BATCH` (8) in parallel from the second hit in a row, so a warm mirror learning of one commit pays one hit and one miss and a cold mirror of a long log walks in rounds of eight; the bound stays `RECORD_WALK_MAX` (10,000). The comments sidecar is read through its index head (`syncSidecar`: one `head` of `comments/index.json`; a version the mirror holds ends it, a moved one runs `pullSidecar`, which reads the proven index and the thread files whose etag rows changed; a missing index removes the mirror's sidecar). `pull` takes the head its caller read (`syncNow`, `write`, `saveVersion`, `lease`), so a moved pulse costs one manifest head, not two. `list` stays in `pruneSnapshots`, `snapshots()`, `pruneThumbs`, `pullAssets`, `remove` and the listing page.
- Measured (`hosted.test.ts` "a remote commit costs the reader no list"): b synced at 412, a commits 413; b's read is exactly four heads and two gets (`deck.json`, `versions/1.json`, `versions/2.json` the miss, `comments/index.json`; the snapshot and record 1), no list; a second read inside the same head is one head; a cold mirror walks the log with no list. The reader's row of 4.5 ("one `deck.json` head and, when that etag moved, the records by number and the snapshot the etag names; one comments index head") holds with the comments head counted.
- Measured (`hosted.test.ts` "one edit is five puts and no list on the writer"): the writer's edit is five puts (the snapshot, the record, one slide body, the manifest, the pulse), two simple reads (the manifest head, the leases head) and no list.
- The existing tests that staged the listing's lag (`holdList`) still pass: the pull never reads the listing, so the lag cannot reach it.

#### 1.4 The walk past a missing number and the record kept on a timed out commit (3.6, invariant 1; 6.4 both hole cases)

- The reader's rule: the walk records a missing number instead of stopping while the records it found sit below the proven manifest's revision, up to `HOLE_WALK_LOOKAHEAD` (3) misses in a row, and stops at the first miss once they have reached it (a claim above the manifest counts as reached, and `dropRecordsAbove` removes it as before); the lookahead runs only when the walk knows a record (a deck with no record at all, the seed at 412 among them, pays one miss). A hole is counted by `logHoles(records)` (versions.ts: the numbers missing between the first and last record) for `deck.info.counts.holes` (R1, R2); Version history's restore range ends at a hole as `assertContiguous` says.
- Measured (`hosted.test.ts` "the reader's walk continues past a missing record number"): three commits, record 2 deleted from the store; a cold mirror reads the document at 415 proven from the snapshot, holds records 1 and 3, counts one hole, headed exactly `versions/1..4.json` and listed nothing; a mirror synced before the loss counts no hole of its own; with record 3 gone too a cold mirror heads 1 to 5 and stops (the lookahead), holds record 1 and reads 415; the seed with no record pays one head of `versions/1.json`.
- The writer's rule: `write()`'s commit catch keeps the record when the manifest put met `BLOB_DOCUMENT_WRITE_TIMEOUT_MS` and the head does not show our bytes (`claimed = false` before the rethrow, so `releaseClaim` deletes nothing); the head at our bytes finishes the write as before (the C3-F1 test still passes). A claim whose put never lands is taken over after `CLAIM_GRACE_MS` as before; a put that lands late makes it the record of its revision.
- Measured (`hosted.test.ts` "a commit put that lands after its deadline leaves the record in place"): the put is held and applied only after the writer's head; the writer throws `BlobTimeoutError`, the record stays (no `del`), a reader before the landing drops the claim above the manifest and reads 412 with no record, and after the landing reads 413 with record 1 at 413 and the change applied.
- `isBlobTimeoutError` joins the by name error helpers (a bundle with two copies of the module).
- The tabs' half of the reader's rule (the external checkpoint at the hole's far side) is B1's `announce` (R6).

#### 1.5 The leases and the export job record through the proof (3.5, invariant 8; audit-costs items 4 and 13; 6.4 both)

- `blob-store.ts` `pullLeases` reads `leases.json` through `provenGet` (the head's version, the body only when its md5 is that version, else the immutable copy under the version, else the url past the CDN, else the last body under its own version) and `pushLeases` writes it through `putWithCopy` (the copy under `decks/<id>/.turboslide/copies/<md5>.json` first, then the file). The write path's round one awaits both its reads to their end (`settle`), so a write that fails on one leaves no call of the other in flight (the "hung head" test asserts it).
- Measured (`hosted.test.ts` "pullLeases reads through provenGet"): the lease's push stores the copy under the file's version; with the CDN holding the old body a release on one instance is read on the other as no lease, through the head, the stale body and the copy, and an agent's write on the slide goes through.
- `apps/studio/src/server/export-jobs.ts` `writeExportJob` writes through `putWithCopy` and `readExportJob` reads through `provenGet`; `pruneExportJobs` removes the copies with the records by their upload time (they age together).
- Measured (`export-jobs.test.ts` "is written with its immutable copy first and read through the proof"): the copy is put before the record, a done record written under a held CDN reads back done through the copy, a record never queued is null after one head. The prune test counts the copies beside the records (4 for 2 jobs).

#### 1.6 The prune every 20th record (3.6, invariant 13; 4.4 rank 6; 6.4 "the prune lists on the twentieth commit alone")

- `snapshots.ts` `SNAPSHOT_PRUNE_EVERY = 20`, `pruneDue(n)`; `blob-store.ts` `write()` runs `pruneSnapshots` behind the answer only when `pruneDue(entry.n)`. The twentieth is the record number, which every instance agrees on (two instances never prune the same turn twice; a named version takes a number and its save is a turn when it lands on one). The prune at the last stream's close is R3. `BlobStore.pruneSnapshots()` stays public for it and for the tests.
- Measured (`hosted.test.ts` "the prune lists on the twentieth commit alone"): twenty commits make one `list` of `snapshots/`, on the twentieth, and one hundred puts. Four earlier tests that relied on the prune after every commit now call `pruneSnapshots()` or count the seed's snapshot beside the writes (section 2).

#### 1.7 The store call counters (6.3)

- `blob-store.ts`: `noteStoreCall`, `storeCallsFor`, `resetStoreCalls`, `storeInstanceId`, `deckIdOfPathname`, `STORE_CALLS_WINDOW_MS` (60,000), `STORE_CALL_OPS`, the `StoreCalls` type (`{ head, get, put, list, del, windowMs, instance }`). `boundedBlobClient` notes every call it bounds under the deck the pathname names (`folders` counts as a `list`; a path outside `decks/` under the empty key); the log and the instance id live on `globalThis` under `Symbol.for` keys, so a bundle with two copies of the module counts once and answers one id. The window slides on every note and every read; memory is bounded by the calls of one minute.
- Measured (`blob-store.test.ts`, new, four tests): the counts by deck and operation, the window's slide, the empty key, the deck off a pathname, the bounded client feeding the counters once for a client wrapped twice, the instance id's shape and stability.
- `apps/studio/src/server/agent-actions.ts` `registerStoreStatusActions` (R2 wires it, after the CLI's record actions whose `sync.status` placeholder it replaces): `sync.status` answers the store's position (`seq` and `revision` the store's revision, `pending` 0, `retained` 0, the deployment's `tier`, `transport` `file`, `connected` false, the CLI's shape for a caller without a stream) with `storeCalls` once the table names it; `deck.info` answers the reader's `deckInfo` with `counts.snapshots` (the store's, as today) plus `counts.records` and `counts.holes` once the table names them. `outputAccepts(id, path)` reads the table's shape.
- Measured (`agent-actions.test.ts`, three new tests over `createDispatcher` and a fake store whose log misses record 2): the two handlers pass the dispatcher's output check on this tree (the table without R1) and the tests assert the two fields when the table carries them, so they pass after R1 as well.

#### 1.8 The card thumbnail once at rest (3.10; 4.4 rank 2; audit-costs item 6)

- `apps/studio/src/server/card-thumb.ts`: `CARD_THUMB_SETTLE_MS` 30,000 (from 2,000), `CARD_THUMB_FLOOR_MS` 8,000 kept; the scheduler gains `flush(deckId)`, which moves a pending render to now, at the floor when one ran inside it, and does nothing when no write waits; `flushCardThumb(deckId)` is the runtime binding for the stream's close (R4) and the tab's hide (R5).
- Measured (`card-thumb.test.ts`): one render per burst at the settle; a flush 3 s after a write renders at once, a second flush inside the floor renders at the floor, a flush with nothing pending or of another deck renders nothing; the earlier floor test runs on a settle under the floor and adds the module's settle case.
- What this closes alone: a typing hour renders at most twice plus once at the end instead of every 8 s. What waits on R4 and R5: the render at the stream's close and the tab's hide.

#### 1.9 The resync read's `since` answer (3.2, invariants 3 and 10; the shared file table "B3 owns `write.ts`, B2 reads the answer in `resync`")

- `apps/studio/src/server/write.ts` `readEditorDeck`: the hand parsed input gains `since` (a non negative integer, else absent, safe across deployments); the answer gains `origins?: RecordOrigin[]` when `since` was given: for the records above `since`, the newest `RESYNC_ORIGINS_MAX` (`REPLAY_MAX_ENTRIES`, 2,000) considered, each record with an origin as `{ seq: record.revision, n, clientId, opIds }`, mutations stripped, oldest first; a record without an origin names nothing. Read off `deckRoom.store.records()` after the reads the loader makes anyway, so no store call of its own.
- Measured (`write.test.ts` "originsSince"): the records above `since`, the skip of a record without an origin, the empty answers, the bound (the newest `max`), no mutations and no aliasing of the record's list. B2's `room-client.ts` reads `origins` as `{ seq, opIds }` (their note R1: answered).

### 2. Files this lane changed

All in SYNC.md 5.1's B3 row:

- `packages/store/src/store.ts`: `WriteOrigin`, `StoreWrite`, `VersionRecord.origin`, `WriteOutcome.replayed`, `DeckStore.write` over `StoreWrite`.
- `packages/store/src/versions.ts`: `RECORD_ORIGIN_WRITES`, `writeOriginSchema`, `versionRecordSchema.origin`, `logHoles`, `recordNamingOps`.
- `packages/store/src/snapshots.ts`: `SNAPSHOT_PRUNE_EVERY`, `pruneDue`.
- `packages/store/src/blob-store.ts`: the header; `isBlobTimeoutError`; the counters section; `boundedBlobClient` counting; `HOLE_WALK_LOOKAHEAD`, `RECORD_WALK_BATCH`, `RECORD_WALK_MAX`; `BlobStoreOptions.writeOrigin`; `pull(knownHead?)` without the listing (the records' rows, the snapshot first, `walkRecords`, `syncSidecar`); `pullLeases` through `provenGet`, `pushLeases` through `putWithCopy`; `write()` (round one settled, the origin check, the record's origin, the timed out commit's claim kept, the prune on `pruneDue`); the callers of `pull` pass their head.
- `packages/store/src/hosted.test.ts`: six existing tests adapted (the seed's snapshot beside the writes now that the prune has a schedule; the leases head in round one; the list assertion of the write), seven new tests under "the sync and costs round: the store tier", `seedSnapshot()`.
- `packages/store/src/blob-store.test.ts` (new): the counters.
- `packages/store/src/versions.test.ts`: the tolerant origin, `logHoles`, `recordNamingOps`.
- `apps/studio/src/server/export-jobs.ts`, `export-jobs.test.ts`: the proof rule on the job record.
- `apps/studio/src/server/card-thumb.ts`, `card-thumb.test.ts`: the settle, `flush`, `flushCardThumb`.
- `apps/studio/src/server/write.ts`, `write.test.ts`: `since`, `origins`, `originsSince`, `RecordOrigin`, `RESYNC_ORIGINS_MAX`.
- `apps/studio/src/server/agent-actions.ts`, `agent-actions.test.ts`: `registerStoreStatusActions`, `outputAccepts`, `StoreStatusDeps`.
- `docs/gslides-parity/sync/build/b3.md` (this note).

Untouched of this lane's list, on purpose (the P1 items of SYNC.md 5.2 are the round after's): `presence-store.ts` (the copy names), `access-store.ts`, `blob-vercel.ts`, `blob-fake.ts`, `comments-store.ts` (read by the pull's `syncSidecar`, unchanged), `access.ts` (the 60 s trust), `published.ts` (the show snapshot), `thumbs.ts`.

### 3. Matrix rows this lane covers, for B4

The ids of SYNC.md 6.1 in the B3 row, with what proves each on this tree and what a driver reads:

- `sync.pull.no-listing`: the falsifier is `hosted.test.ts` "a remote commit costs the reader no list" (the reader's calls on a remote commit are four heads and two gets, no list) and "one edit is five puts and no list on the writer" (the writer's calls); passes on this tree (section 5). On a deployment the smoke reads `sync.status.storeCalls.list` on the answering instance (R1, R2, R7 b and c); not driven this lane (no deployment of this tree exists; the tmp store's counters read zero).
- `sync.resend.idempotent` (the two instance test): `hosted.test.ts` "two instances, one fake: the resend of a committed POST ..." passes on this tree; the Playwright row and the same instance case are B1's and B4's.
- `cost.two-tabs-idle.calls`: this lane's part is invariant 7 (no listing in the pull, sections 1.3 and 1.4); the presence pushes are unchanged this round (P1). Not driven (measure: true; the cost probe on a deployment).
- `cost.editor-editing.calls`: this lane's parts are the prune (one list per twenty commits instead of one per edit, section 1.6) and the card thumbnail (at most two renders a typing hour plus the flush, section 1.8; the flush waits on R4 and R5). Not driven (measure: true).
- The 6.4 unit tests named for B3 all exist and pass: `hosted.test.ts` (no list on the reader; the prune on the twentieth; `pullLeases` through `provenGet`; the two instance resend; the timed out commit's record; the hole with `logHoles` reading one; the tolerant parse in `versions.test.ts`), `export-jobs.test.ts` (the copy and the proof). The `announce` half of the hole case is B1's (R6).

### 4. Decisions recorded

- The immutable copies of `leases.json` live under `decks/<id>/.turboslide/copies/<md5>.json`, `putWithCopy`'s folder, and not under `leases/<md5>.json` as SYNC.md 3.5 spelled it: the state folder is never mirrored (`isMirroredDocument`), `remove` deletes it with the prefix, and the access record's copies live there already; a new `leases/` folder under the deck would be one more thing for the listing page and the removal to know. The export job record's copies follow the same rule (`exports/.jobs/.turboslide/copies/`).
- The prune's twentieth is the record number, so every instance agrees and the twentieth commit of a fresh deck is record 20; a named version's save takes a number and is a turn when it lands on one.
- The walk's lookahead is three misses and runs only when the walk knows a record (a cold mirror after its first hit, a warm mirror always); a deck with no record pays one miss per pull as before. A log whose first records are missing (a hole at 1) is not walked past; it reads as before the round.
- The walk grows its batch from the second hit in a row (1, 1, 2, 4, 8, 8 ...), so a warm mirror learning of one commit pays one hit and one miss; a cold pull of the largest production deck (376 records) is about fifty rounds of parallel reads instead of 752 sequential ones.
- `pull` takes the head its caller read, so a moved pulse costs one manifest head (4.5's row) instead of two; a head that moves between the caller's read and the pull is caught by the write path's own check (`synced !== head.version`) or the next tick, as before.
- The write path's round one awaits both reads to settle before it judges either, so a write that fails on the manifest head leaves no leases read in flight (the "hung head" test's "no call left behind").
- `WriteOutcome.replayed` is the ok branch with the first admission's `entry` and `revision`, the current document and no changed slides, so a caller that ignores the field reads a committed write at the record's revision and a caller that reads it (B1's `append`) synthesizes the client's entries.
- The server's `sync.status` answers the store's view: `seq` and `revision` are the store's revision (the seq of a record is its revision on the blob tier), `transport` `file` and `connected` false as the CLI answers for a caller without a stream; the page's own `sync.status` (controller.tsx) is unchanged.
- `deck.info` keeps its one `list` of `snapshots/` for `counts.snapshots` (R7 c); the two new counts cost no store call.
- `RECORD_ORIGIN_WRITES` is a constant, not an environment variable, so the two deployments of 3.2 differ by one commit the ship note names and a rollback lands on a known reader.
- The existing hosted tests that relied on the prune running after every commit were changed to call `pruneSnapshots()` or to count the seed's snapshot; their assertions about the write's own calls are the same or stricter (no list in the write).
- The `hidden` presence field of R5 is proposed and not built by this lane; the tab hide render is the smallest third of audit-costs item 6 and the stream close (R4) covers the seller who leaves.

### 5. Commands run and their results

Times are PDT of 2026-09-21. Every command ran from the repository root unless said; binaries as `node_modules/.bin/<tool>`; no `pnpm` script ran.

- `node_modules/.bin/vitest run --root packages/store src/hosted.test.ts` before any change: 68 tests passed (13:00).
- `node_modules/.bin/tsc -b packages/store/tsconfig.json`: exit 0 after the store changes (13:08).
- `node_modules/.bin/vitest run --root packages/store`: 20 files, 247 tests passed (13:19, again at 13:23 after `prettier --write`), the 68 of `hosted.test.ts` become 75 with the seven new tests, `blob-store.test.ts` 4, `versions.test.ts` 8.
- `cd apps/studio && ../../node_modules/.bin/vitest run src/server/card-thumb.test.ts src/server/export-jobs.test.ts src/server/write.test.ts src/server/agent-actions.test.ts`: 41 tests passed (13:20).
- `node_modules/.bin/tsc -b apps/studio/tsconfig.json`: exit 0 (13:21, 15.7 s); `node_modules/.bin/tsc -b` at the root: exit 0 (13:22). An earlier run at 13:10 met `packages/schema/src/transform.test.ts` mid edit (B1's lane, two type errors at 877 and 880), none in this lane's files; the root run at 13:22 was clean.
- `cd apps/studio && ../../node_modules/.bin/vitest run`: 69 files, 484 tests passed (13:22).
- `node_modules/.bin/vitest run --root packages/realtime`: 10 files, 131 tests passed; `--root apps/cli`: 23 files, 142 passed, 1 skipped (13:22; both consume `DeckStore.write` and `WriteOutcome`).
- `node_modules/.bin/prettier --write` on the four files it flagged of this lane's, then `--check` on every file of this lane: clean (13:23).
- On 4413 without a browser: `GET /home` 200, `GET /edit/gt-brand` 200, `POST /api/actions/deck.info?deck=gt-brand` the deck at revision 25, `POST /api/actions/sync.status?deck=gt-brand` the CLI placeholder of R2 (13:26).
- The dev server: `TURBOSLIDE_STORE=tmp TURBOSLIDE_REALTIME=memory TURBOSLIDE_LOCAL_OPEN=1 TURBOSLIDE_SESSION_SECRET=<fake, 49 bytes> TURBOSLIDE_DOWNLOAD_SECRET=<fake, 50 bytes> node_modules/.bin/vite dev --port 4413 --strictPort -c vite.no-watch.config.ts` from `apps/studio`, started 13:23, `/home` answered 200; stopped at the return (section 6 says when).
- The core walk on 4413 (`node scripts/probes/editor-walk-probe.mjs --core --base http://localhost:4413 --only versions,collab --json <scratch>/walk-versions.json`): queued behind `.turboslide/e2e.lock`, which another lane has held since 13:03; section 6 records whether it ran.

### 6. The walk on 4413 and the return

- The core walk did not run. `.turboslide/e2e.lock` was held by another lane's Playwright run from 13:03 (a fresh worker started at 13:27:51 under it) through this lane's return at 13:30, and this lane's queued `until mkdir` wait ended without the lock; nothing of this lane holds or waits on the lock now. So no row of the walk is driven by this lane on 4413; what stands for the tree's health is the browserless smoke of section 5 (`/home`, `/edit/gt-brand`, `deck.info`, `sync.status` answering on 4413) and the unit suites of the store, the studio, the realtime and the CLI packages, every one green on the tree with the other lanes' in progress work merged in the checkout.
- The rows of section 3 are unit tested, not driven: `sync.pull.no-listing` and `sync.resend.idempotent` have their falsifiers green in `hosted.test.ts`; `cost.two-tabs-idle.calls` and `cost.editor-editing.calls` wait for the cost probe on a deployment (B4, R7) and for R1 and R2 so `storeCalls` reads on the HTTP transport.
- The dev server on 4413 was stopped at 13:30. No scratch deck was made on any deployment; the tmp store's decks of 4413 live under the system temp folder the server named at start and are gone with it.
- What this lane leaves for the owners: R1 and R2 (the integrator, one table edit and one call), R3, R4 and R6 (B1: the prune at the last stream's close, the flush at the stream's close, the external checkpoint at a hole), R5 (B2 with B1, the hidden heartbeat, optional), R7 (B4, the probe's three facts). The P1 items of SYNC.md 5.2 in this lane's files are not built, as the round says.

## Sync round fix round

The fixer of B3, on the tree the verifier's pass 1 read (`main` at `c4ad8b1`, the six lanes' and the integrator's work uncommitted), from 20:05 to 20:20 PDT on 2026-09-21, for the two findings the round assigned to this lane in `docs/gslides-parity/focus/VERIFICATION.md` "Sync and costs round, pass 1": F4 (`sync.resend.idempotent`, severity 2) and F5 (`cost.editor-idle.calls`, severity 3). The other fixers were editing the same checkout meanwhile (B1's `packages/realtime/src/blob.ts` and `apps/studio/src/server/room.ts` changed at 20:14 PDT, inside this lane's runs). Nothing here ran a git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build`; nothing on Vercel or GitHub was read, created, changed or deleted; no dev server, no browser, no lock and no scratch deck (section 4 says why); the blob tier ran against `blob-fake.ts` alone. Every file edited is in SYNC.md 5.1's B3 row: `packages/store/src/presence-store.ts`, `presence-store.test.ts`, `blob-store.ts`, `hosted.test.ts`, `versions.test.ts`, and this note.

### 0. Requests

**R1 (the integrator, the N plus 1 commit; SYNC.md 3.2 and 5.1 "B3 then B1", the ship note).** `packages/store/src/versions.ts` line 30, `export const RECORD_ORIGIN_WRITES = false;` to `true`, is the whole of the N plus 1 commit inside this lane's files. Every test of the store package, the studio's server modules and the realtime package passes under both values (section 2 ran them flipped and restored the line), so the flip is that one line: `versions.test.ts` no longer pins `false` and `hosted.test.ts` pins the default's effect against the constant either way. Three sentences for the ship note: a rollback of N plus 1 lands on N, whose parser reads what N plus 1 wrote (3.2); on N a resend whose first attempt committed is answered with its record by the instance that committed it (section 1.1) and doubles the word only when the resend lands on another instance; on N plus 1 it is answered on every instance.

**R2 (B4, `docs/gslides-parity/focus/core-matrix.json`, the row `cost.editor-idle.calls`; SYNC.md 6.1, 6.2, question 4).** The advanced ceiling. A push is three puts as built and as SYNC.md 4.5 says (the copy, the record, the pulse), and after section 1.2 an idle tab pushes once per 20 s, so the model's expected count is 9 advanced operations a minute, not the 7 the row's ceiling of 11 was cut from. At 1.5 times the model (6.2's rule) the number is 13, under the 18 pass 1 measured. A 60 s window whose three cycles run a little under 60 s holds four pushes (12), so 11 can read red on a correct cadence about once in a hundred samples and 12 is the least number that cannot; 13 is the rule's. The `del` calls are free and stay outside the count. If Kevin answers question 4 with the model's count itself, that count is 9 and the window edge makes it flaky by design. The row's `interaction` text ("11 advanced") moves with the number, and SYNC.md 6.1's table with it (the integrator's edit).

**R3 (B4 and the integrator, the ship note; the row `sync.resend.idempotent`).** On deployment N the row's first two checks (the word once in both browsers) pass whenever the resend lands on the instance that committed the first attempt, and fail when it lands on another (the integrator's finding 11 and VERIFICATION S.6 F4 narrow to that case); the third check (`origin.opIds` on the record) reads not driven on N whatever the routing, since the stored record carries no origin until N plus 1. The known mechanism class of 6.2 names it on N; on N plus 1 all three checks are live.

### 1. What was fixed

#### 1.1 F4, `sync.resend.idempotent`: the instance that committed the first attempt answers the resend on deployment N (SYNC.md 3.2, invariant 3)

- The mechanism, read at the code: B1 retired room.ts's per instance memory of the last 512 admitted op ids (`rememberBlobAdmitted`, `blobAdmittedBefore`; b1.md R8 d) for the record's origin, and deployment N writes no origin (`RECORD_ORIGIN_WRITES` false), so on N neither check had anything to match: not the admission's cheap pass against the mirror (`channel.since` reads `store.records()`, `entryOfRecord` emits `covers` from `record.origin`) and not the write path's pass after its head and pull (`recordNamingOps` over `readVersions(dir)`). Production today catches the same instance case through the memory; N as verified caught none, which is a step back from production for the seller whose resend lands on the warm instance (the common routing).
- The change (`blob-store.ts`): the store holds in memory, per store object, the origin of every record it committed while the record's bytes stored none (`heldOrigins`, a `Map<n, WriteOrigin>` bounded to `HELD_ORIGINS_MAX` 2,000, the oldest number first). `records()` and the write path's check read the log with the held origins merged onto the records that carry none (`withHeldOrigins`), so on N the same instance's `covers`, its `admitOnBlob` check, its write path check and `readEditorDeck`'s `origins` answer (write.ts, unchanged) all see the first attempt's op ids, as they will everywhere on N plus 1. The bytes in the store and in the mirror are untouched (a reader on the older deployment parses them as before, which is what N exists for). The origin is held once the commit is proven (`committed` known, the timed out path's head at our bytes included) and never at the claim: a claim another writer takes over after `CLAIM_GRACE_MS` would name the wrong record. The cross instance case on N is unchanged and stays N plus 1's, as 3.2 designs it (R1).
- Measured (`hosted.test.ts` "deployment N, the same instance: ..."): with `writeOrigin: false`, instance one commits `{ tab-a, [op-1, op-2] }` as record 1, the stored bytes and the mirror's file carry no `origin`, `one.records()` reads the origin held, the resend at base 412 on instance one is answered `replayed.n` 1 at revision 413 with zero puts and one record in the store, a resend of one op of the batch the same; instance two reads the record with no origin and, at base 413 (the head the channel would place it at), commits the resend as record 2; instance one holds nothing for the number instance two took and nothing for a write it lost at the head; a cold mirror of the same store reads no origin on either record. The two instance test of 6.4 (`writeOrigin: true`) stands as it was, with its tail made independent of the constant's value (the default's effect asserted against `RECORD_ORIGIN_WRITES`, the committing instance reading its own origin either way).
- What this does not do: a resend that lands on another instance on N is placed and committed again, so a lost answer under that routing still doubles the word until N plus 1 is production. That is the deviation recorded for F4.

#### 1.2 F5, `cost.editor-idle.calls`: the deferred heartbeat's timer after the heartbeat, not ahead of it (SYNC.md 3.10, 4.5)

- The mechanism, read at the code and traced: `material` pushes a heartbeat's row once the record's row has under half its life left (15 s of 30), and `nextRefreshAt` armed a timer for a deferred heartbeat at exactly that half life. With the client at 10 s between heartbeats when quiet (B2's `PRESENCE_HEARTBEAT_QUIET_MS`), the timer always fired ahead of the heartbeat that would have carried the refresh: push at 0 (the row to 30), heartbeat at 10 deferred, the timer at 15 pushes the row to 40, heartbeat at 20 deferred, the timer at 25 pushes to 50, and so on, one push every 10 s, six a minute, eighteen puts, which is what the preview and the idle read by hand counted (17 and 12 on the two instances that took the heartbeats). At the active cadence of 5 s the same timer gave the 10 s cycle too, so SYNC.md 4.2's "a push every 15 s" was already an overcount of the code's cadence and its 4.5 model of a push every 20 s was never reached.
- The change (`presence-store.ts`): `PRESENCE_REFRESH_TIMER_MS` 5,000 (`SharedPresenceOptions.refreshTimerMs`), the remaining life at which the timer pushes a deferred row; it sits after the latest heartbeat the quiet cadence lands with the row under half its life (15 s minus 10 s) and ahead of the expiry by the others' 2 s tick and the push. The refresh therefore rides the heartbeat that finds the row under half its life, every second heartbeat, and the timer pushes only when no heartbeat of the tab reached the instance in time (the tab's heartbeats moved elsewhere, stopped, or arrived late), for one push at most. `nextRefreshAt` also arms nothing for a local row that ran out or a row the record holds under a higher clock (the tab moved instances): both are dropped by the next reconcile, and the old rule armed a timer at their record row's due time, which once past fired and re-armed itself at once until the reconcile (a stale heartbeat with a lower clock made that loop; never seen in a run, found reading the code).
- Measured (`presence-store.test.ts`, two new tests under fake timers and a clock that runs with them): an idle tab heartbeating every 10 s for a minute after its join pushes at 20, 40 and 60 s and nowhere else, twelve puts for the join and three refreshes (nine a minute), the record's row alive 30 s from its last heartbeat, no error; when the heartbeats stop after one more (deferred at 20 s of life left) the timer pushes that row once at 5 s of life left and nothing after; a heartbeat with a lower clock than the record's row arms no timer and a minute passes with no push and no timer of that instance firing. The existing tests of the file stand unchanged (22 passed): the half life refresh by the heartbeat, the five second floor, the lost race's one scheduled push, the leave under the floor.
- The numbers against the row: the store half of an idle tab is now 3 pushes and 9 advanced operations a minute on the instance that takes its heartbeats (12 in the rare 60 s window that holds four pushes), `del` one to three (free), `list` 0; the function half is untouched (9.66 a minute against 12). Against the ceiling of 11 the fix reads green except at that window edge (R2). A push stays three puts: the copy is the proof rule of 3.5 (the public host serves the overwritten record stale), the pulse is what moves every other instance's tick, and the copy rename with the roster from the pulse's copy is the round after's (SYNC.md 5.2), as the finding says.
- `cost.two-tabs-idle.calls` (this lane's row) follows: two rows at the same cadence are at most six pushes and 18 advanced a minute across the instances, fewer when one instance takes both tabs' heartbeats (one push carries every due row), against 30.

### 2. Commands run and their results

Times are PDT of 2026-09-21; every command from the repository root unless said; binaries as `node_modules/.bin/<tool>`; no `pnpm` script ran.

- `node_modules/.bin/vitest run --root packages/store src/presence-store.test.ts src/hosted.test.ts src/versions.test.ts src/blob-store.test.ts` before any edit: 4 files, 107 passed (20:08).
- `node_modules/.bin/vitest run --root packages/store src/presence-store.test.ts` after the presence edits: 22 passed, the two new tests among them (20:11).
- `node_modules/.bin/vitest run --root packages/store`: 20 files, 250 passed with `RECORD_ORIGIN_WRITES` false (20:14); the line flipped to `true` with `sed`, the same suite 250 passed (20:14), the line restored and read back as `false`.
- `node_modules/.bin/tsc -b packages/store/tsconfig.json` exit 0; `node_modules/.bin/tsc -b apps/studio/tsconfig.json` exit 0 (20:15).
- `node_modules/.bin/vitest run --root apps/cli`: 23 files, 142 passed, 1 skipped (20:15).
- `node_modules/.bin/vitest run --root packages/realtime`: 134 tests, 133 passed, 1 failed (20:15): `src/blob.test.ts` "prunes the store's snapshots once when the deck's last stream on the instance closes" timed out waiting for the `list`. Read at the code: B1's `pruneAtClose` (blob.ts, written 20:14:44) now runs only when a commit landed on the instance since the last prune (`state.commits`) and after a delay, while their test (written 13:31) subscribes twice without a commit and waits for the prune. This lane's edits touch neither `pruneSnapshots` nor the channel, and the test calls no `write` or `records`; the red is B1's mid edit tree and not this lane's. A second run at 20:18 read the same one red of 135.
- `cd apps/studio && ../../node_modules/.bin/vitest run src/server/write.test.ts src/server/agent-actions.test.ts src/server/blob-admission.test.ts src/server/export-jobs.test.ts src/server/card-thumb.test.ts`: 5 files, 69 passed with the constant false (20:17) and again with it flipped to true (20:18); `blob-admission.test.ts` is B1's admission over `store.records()` and read the merged log without a change.
- `node_modules/.bin/prettier --check` on the five edited source and test files: clean (20:17).

### 3. Matrix rows this lane touched, and how each was read

- `cost.editor-idle.calls` (measure: true; the cost probe on a deployment): not driven by this lane. A localhost base runs the tmp store, whose `storeCalls` read zero (SYNC.md 6.3), so the store half this fix changes cannot be read there; the blob tier locally is the fake alone; no deployment of the fixed tree exists and this lane creates none (the other fixers' edits were landing in the same checkout during this run, so a preview of it would measure a moving tree). The falsifier is the unit test of section 1.2; the reading of record is the integrator's rerun on the enforce preview after a named code change (6.2), where the expected store half is put 9 a minute per instance that takes the heartbeats, list 0.
- `sync.resend.idempotent`: not driven by this lane. On the memory tier the row skips by its rule (no origin on any record), and the same instance case this fix closes lives on the blob tier alone. The falsifier is the hosted test of section 1.1; on the preview the expected reading on N is R3's.
- `cost.two-tabs-idle.calls`, `cost.editor-editing.calls`: not driven (measure: true); the first follows section 1.2's cadence, the second is unchanged by this round's fixes.

### 4. Deviations

- F4's cross instance case on deployment N is not fixed and cannot be inside 3.2's landing: N writes no origin so that a rollback to the current production reader never meets a record it cannot parse. Recorded as the deviation, with R1's sentences for the ship note; the same instance case is fixed on N (section 1.1).
- The ceiling of `cost.editor-idle.calls` is B4's file and was not changed here; R2 carries the number and the reason. With the ceiling at 11 the fixed cadence reads green except in the rare window that holds four pushes.
- No dev server on 4413, no walk, no gate and no lock this run: neither row's blob tier half is drivable on a localhost server (section 3), the unit falsifiers are the measure, and the shared checkout was mid edit by the other fixers.
- B1's `blob.test.ts` prune at close test is red on the tree as it stands (section 2); not this lane's file and not touched.
