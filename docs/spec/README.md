# docs/spec/

The Turboslide specification, the milestone plan and the three experiment reports, as written on
2026-09-10. The files are the authority that `SPEC n.n` citations in code and in AGENTS.md refer to.

| File                                      | Original path (session scratchpad)             | Recovered from                                            |
| ----------------------------------------- | ---------------------------------------------- | --------------------------------------------------------- |
| `SPEC.md`                                 | `turboslide/design/SPEC.md`                    | one Write call, 137,166 characters, 19:59:51 UTC          |
| `MILESTONES.md`                           | `turboslide/design/MILESTONES.md`              | one Write call plus one Edit (the sheet check), 20:02 UTC |
| `experiments/tanstack-report.md`          | `turboslide/experiments/tanstack/REPORT.md`    | one Write call, 19:19:45 UTC                              |
| `experiments/pptx-report.md`              | `turboslide/experiments/pptx/REPORT.md`        | one Bash heredoc (`cat > REPORT.md`), 19:20:50 UTC        |
| `experiments/slides-and-render-report.md` | `turboslide/experiments/slides-and-render/...` | one Write call, 19:19:11 UTC                              |

## Provenance

The documents were written into the session scratchpad
`/private/tmp/claude-501/-Users-kevinliu-gt-gt-cloud/293a64b7-8ef6-4b00-b382-682288c84431/scratchpad/`
and that directory was cleared later the same day, before the M1 tree was committed. Nothing else
on the machine held a copy. The text above was recovered from the session transcripts under
`~/.claude/projects/-Users-kevinliu-gt-gt-cloud/` by replaying every Write, Edit and heredoc call
on those paths in timestamp order; no later edit to any of the five files appears in any transcript,
so the recovered text is the last version that existed. The text is as written: it still names the
scratchpad paths, and it was not edited for the repository's comment rules.

## Not recovered

- The three candidate designs (`design-document-first.md`, `design-editor-first.md`,
  `design-export-first.md`) that SPEC.md cites in its first paragraph. SPEC.md states every
  resolution in place, so the repository does not depend on them.
- The research reports 00 to 06 under `scratchpad/slides-editor/research/`.
- The M1 evidence directory (`render.json`, `compare.json`, the studio screenshots the integrator
  reported). It is regenerated by `pnpm check` into `.turboslide/`; archive that directory when a
  milestone is accepted.
- The 3200 by 1800 liquid metal render `openers-60-79/glyphfield/hi/lm-gem-c5-s2.png`, the source
  of the Prototemplate and Glyphfield opener. `packages/effects/fixtures/two-tone/manifest.json`
  records what was measured against it and OPENERS.md records how to render it again.

## Public repository

The documents cite private material (Prototemplate, Glyphfield, internal research). They live here
provisionally; moving `docs/spec/` to a private overlay is SPEC 11 and open question 14 in
`SPEC.md`, Kevin's decision.
