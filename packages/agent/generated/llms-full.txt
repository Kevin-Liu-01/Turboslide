# Turboslide

> A block document with a validator and a grammar linter; every operation is a named action in one table, reachable from the CLI, MCP, HTTP and the studio window API (SPEC 1, 7.1).

Turboslide holds the GT brand deck as typed JSON (deck.json plus slides/<id>.json), renders it with one renderer, lints it against the deck grammar, and exports it to PPTX with a verified report, pixel identical in its default mode (docs/pptx.md). There are no coordinates: a slide is a kind, a layout and typed blocks in named slots.

## Discovery

- /api/agent: the manifest (transports, rules, execution rules, action ids, skills, resources, what this instance implements)
- /openapi.json: the OpenAPI 3.1 contract of POST /api/actions/<id>
- /mcp: MCP over streamable HTTP; `turboslide mcp` is the same server over stdio
- /llms-full.txt: this guide with every action and every lint rule
- skills/turboslide-{create,api,studio,verify}/SKILL.md: the four skills with their generated reference tables
- docs/grammar.md: the deck grammar (slide kinds, layouts, blocks, rules)

## Rules

- Every mutating action requires baseRevision and rejects a stale one with 409 and the current document.
- Every write returns the normalized result; re-read from the response, not from memory.
- Unknown fields are rejected with unknown_field and a JSON pointer, except under ext on a slide, a block or an asset.
- An agent write to a slide another author leased is refused with 409 and the holder unless force is set.
- A claim about a deck names the revision; render both themes and lint before claiming a slide is done.

## Calling an action over HTTP

- POST /api/actions/<id>; GET /api/actions/<id> returns the action contract: input and output JSON Schema, transports, milestone and whether this instance implements it.
- Body: One JSON object, the action input exactly as the input schema states it; unknown fields are refused with 400 and code unknown_field plus a JSON pointer.
- Deck: ?deck=<slug> names the deck; without it the instance default (gt-brand) is used.
- Author: x-turboslide-author: agent:<runId> (or ?author=); a request without one writes as agent:http.
- Force: ?force=1 (or x-turboslide-force: 1) writes to a slide another author leased; without it the write is 409 with the holder and the current document.
- Auth: Authorization: Bearer <TURBOSLIDE_TOKEN> on every deployed instance; open only on localhost when the instance has no token; a request off localhost without a token is 401.
- Limits: 1048576 bytes per write body, 26214400 bytes per asset upload.
- Errors: { error: { name, status, message, code?, pointer?, currentRevision?, current?, holder?, milestone? } }; TypeError 400, RangeError 404, ConflictError 409, NotImplementedError 501, Error 500.

## MCP over HTTP

- MCP streamable HTTP: POST JSON-RPC to /mcp (initialize first), GET for the notification stream, DELETE to end the session; the mcp-session-id header carries the session.
- One MCP session is bound to one deck and one author at initialize; tools are deck_<action> for every implemented action on the mcp transport.
- ?deck=<slug> on the initialize request; the instance default otherwise. x-turboslide-author or ?author= on the initialize request; agent:mcp-http otherwise.
- deck_goto_slide (view.goto) is listed when a studio page is attached to the deck and runs in that page: the editor at /edit while it is visible, and a /deck, /present or /embed page opened with ?agent=1 while it is visible; a page hidden for 10 s detaches and the call answers 404 until it is shown again.

## Leases and revisions

- slide.lease takes ten minutes on a slide; an agent write to a slide another author holds is 409 with the holder unless force is set; a human write warns and goes through (SPEC 6.7).
- Every mutating action takes baseRevision; a stale one is 409 with currentRevision and the current document; re-read from the response and retry once.

## Renders and exports over HTTP

- GET /api/render/<slideId>?deck=<deckId>&theme=light|dark&scale=1|2 answers the PNG with the RenderRecord in the X-Turboslide-Record header; ?format=json (or Accept: application/json) answers the record. ?w=160|320|640 is the cached thumbnail.
- POST /api/export/<deckId> takes the export.run input as one JSON object (format defaults to pptx; out is ignored, the file lands in the job folder). ?sync=1 (or "sync": true in the body) runs the export inside the request and answers the file, or the ExportReport with the files and their URLs under ?format=json (or Accept: application/json); the X-Turboslide-Export-Report header carries the summary.
- A hosted studio (a serverless function) runs every POST synchronously, ?sync=1 or not, and says so in X-Turboslide-Sync: hosted: a queued job would get CPU only while another request kept the instance busy, and its record lives on that instance alone. Send ?sync=1 so the request means the same everywhere.
- In a checkout or against the Docker worker, a POST without ?sync=1 answers 202 with the job and a Location to poll: GET /api/export/<deckId>?job=<id> for the record (the ExportReport under report once done), GET without job for the deck's jobs.
- A file over 4.5 MB cannot leave a function: the route answers 302 to the stored copy when the deployment has a Blob store, else 413 with the JSON body; export one theme or a slide subset then.
- Auth: both routes require Authorization: Bearer <TURBOSLIDE_TOKEN> when the instance has the token set (the thumbnail variant of the render route stays open for the editor); without it they are open.

## Transports

- cli: turboslide <command> --json --author agent:<runId>
- mcp: turboslide mcp (stdio) or POST /mcp (streamable HTTP); tools deck_<action>
- http: POST /api/actions/<id> with the input as JSON
- window: window.turboslide.studio.invoke(<id>, input) after the turboslide:studio-api-ready event

## Skills

- turboslide-create (skills/turboslide-create/SKILL.md): Write or revise slides in the grammar
- turboslide-api (skills/turboslide-api/SKILL.md): Discover and write through the action table
- turboslide-studio (skills/turboslide-studio/SKILL.md): Drive the studio through the window API
- turboslide-verify (skills/turboslide-verify/SKILL.md): Render, lint, judge and verify exports

## Verification

- Render both themes, read the contact sheet by its cell map, run the grammar linter, judge with the six lenses (deck_review prompt), verify exports against the web render (skills/turboslide-verify).
- A claim about a deck names the revision it was verified at; a severity 3 finding blocks the ship step.

## Actions

Every mutating action takes baseRevision. The transports column says where the action is offered; an action whose milestone has not landed answers NotImplementedError (501).

- deck.info (Deck info; read; cli, mcp, http, window; M1): Title, theme, sections with slide ids and titles, counts (the canvas slides, charts and guides included) and the current revision. Input: none. CLI: turboslide info. MCP: deck_get_info.
- deck.create (New deck; mutates; cli, mcp, http, window; M4): Creates a deck under decks/ from a template of the index (template.list), the GT brand template or one title slide, and returns its id, revision and counts. Input: name, from, id?. CLI: turboslide deck create <name> --from <from> --id <id>. MCP: deck_create.
- deck.rename (Rename deck; mutates; cli, mcp, http, window; M4): Sets the deck title, the name the sidebar head, the toolbar and the deck list show; one deck.set of /title. Input: name, baseRevision. CLI: turboslide deck rename <name>. MCP: deck_rename.
- deck.set (Set a deck field; mutates; cli, mcp, http, window; GS1): Writes one field of the deck manifest by JSON pointer (/title, /theme, /defaults/appearance, /defaults/counter, /defaults/notes) as one deck.set mutation, the write the Themes panel and Slide numbers make; an absent value removes the field. Input: path, value?, baseRevision. CLI: turboslide deck set <path> <value>. MCP: deck_set.
- deck.pack (Pack deck bundle; read; cli; M6): Writes decks/<id> (deck.json, slides, assets, versions and the sidecars) as one zip with a manifest.json carrying the bundle version, the deck id, its revision and a digest per file (docs/deck-transfer.md). Input: id, out?, versions?. CLI: turboslide deck pack <id> --out <out>.
- deck.unpack (Unpack deck bundle; mutates; cli; M6): Creates a deck under decks/ from a bundle zip after validating it, under a free id when the bundle id is taken unless `replace` removes the deck that holds it (docs/deck-transfer.md). Input: file, as?, replace?. CLI: turboslide deck unpack <file> --as <as> --replace.
- deck.push (Push deck to a studio; mutates; cli; M6): Packs decks/<id> and uploads the bundle to a hosted studio through POST /api/decks/bundle with the bearer token, so the deck can be edited there (docs/deck-transfer.md). Input: id, to, token?, as?, replace?, fromUrl?. CLI: turboslide deck push <id> --to <to> --token <token> --as <as> --replace --from-url <fromUrl>.
- deck.pull (Pull deck from a studio; mutates; cli; M6): Downloads a deck bundle from a hosted studio through GET /api/decks/<id>/bundle with the bearer token and unpacks it into decks/, so the deck can be edited locally (docs/deck-transfer.md). Input: id, from, token?, as?, replace?. CLI: turboslide deck pull <id> --from <from> --token <token> --as <as> --replace.
- deck.list (List decks; read; cli, mcp, http, window; GS1): The manifest facts of every deck under decks/ (id, title, slide and section counts, revision, the stamps), newest first; decks in the trash are left out unless asked. Input: includeTrashed?. CLI: turboslide deck list --include-trashed. MCP: deck_list.
- deck.copy (Make a copy; mutates; cli, mcp, http, window; GS1): Copies a deck under a new id with a new title, every slide or the named ones, with or without the speaker notes, at revision 0 with an empty version log; the output names the new deck. Input: id, name, newId?, slideIds?, removeNotes?, copyComments?, baseRevision. CLI: turboslide deck copy <id> --name <name> --id <newId> --slides <slideIds> --remove-notes --copy-comments. MCP: deck_copy.
- deck.trash (Move to trash; mutates; cli, mcp, http, window; GS1): Moves a deck to the trash by writing trashedAt on its manifest: the home page, the fresh presentation route and deck.list leave it out until deck.restore clears the stamp or deck.remove deletes it; nothing else changes. Input: id, baseRevision. CLI: turboslide deck trash <id>. MCP: deck_trash.
- deck.restore (Restore from trash; mutates; cli, mcp, http, window; GS1): Clears trashedAt on a deck in the trash, so the home page and deck.list show it again. Input: id, baseRevision. CLI: turboslide deck restore <id>. MCP: deck_restore.
- deck.remove (Delete forever; mutates; cli, http, window; GS1): Deletes a deck: the folder under decks/ or the prefix in the Blob store, with its slides, assets and version log. Irreversible; the Trash page calls it, and `confirm` must be true. Input: id, confirm, baseRevision. CLI: turboslide deck remove <id> --confirm.
- deck.setBackground (Set theme background; mutates; cli, mcp, http, window; GS2): Writes the background colour every slide without its own takes, what Add to theme writes; null removes it. Input: background, baseRevision. CLI: turboslide deck background --color <color>. MCP: deck_set_background.
- deck.guides (Set guides; mutates; cli, mcp, http, window; GS2): Adds, removes, moves, sets or clears the deck’s guide lines, sheet pixels the same on every slide; the result is sorted and inside the sheet, and an empty result removes the field. Input: add?, remove?, move?, set?, clear?, baseRevision. CLI: turboslide deck guides --add-vertical <x> --add-horizontal <y> --remove-vertical <x> --remove-horizontal <y> --set <json> --clear. MCP: deck_set_guides.
- template.list (Templates; read; cli, mcp, http, window; P1): The template index: every template with its name, category, cover, sentence, slide count, theme and appearance, the organisation flag of a template saved on this deployment and its brand kit, plus the id of the template new presentations start from. Input: none. CLI: turboslide template list. MCP: deck_list_templates.
- template.slides (Template slides; read; cli, mcp, http, window; P1): The slides of one template in order with their titles and kinds, the rows the gallery and Import slides read. Input: id. CLI: turboslide template slides <id>. MCP: deck_template_slides.
- template.create (Save as template; mutates; cli, mcp, http, window; P1): Saves a deck as a template of this deployment: decks/templates/<slug of the name> with the deck manifest, its slides, its assets and its brand kit, listed under Your organisation in the gallery; a name an organisation template already carries replaces that template and keeps its slug (the dialog says so first). Input: deckId, name, sentence?, baseRevision. CLI: turboslide template create <deckId> <name> --sentence <sentence>. MCP: deck_create_template.
- template.update (Replace a template; mutates; cli, mcp, http, window; P1): Replaces the slides, assets and brand kit of an organisation template from a deck and keeps its id, name and sentence; the templates Turboslide ships are refused. Input: id, deckId, sentence?, baseRevision. CLI: turboslide template update <id> <deckId>. MCP: deck_update_template.
- template.rename (Rename a template; mutates; cli, mcp, http, window; P1): Renames an organisation template; its id and its folder stay, so every link and every default record still name it. The templates Turboslide ships are refused. Input: id, name. CLI: turboslide template rename <id> <name>. MCP: deck_rename_template.
- template.delete (Delete a template; mutates; cli, mcp, http, window; P1): Deletes an organisation template folder and its index row; presentations made from it are not changed. The template new presentations start from is refused until another is chosen, and so are the templates Turboslide ships. Irreversible; `confirm` must be true. Input: id, confirm. CLI: turboslide template delete <id> --confirm. MCP: deck_delete_template.
- template.setDefault (Use for new presentations; mutates; cli, mcp, http, window; P1): Makes a template the one /new and the Blank card start from on this deployment (decks/templates/default.json), whose name, logo and appearance every Reset and every default logo read; `blank` restores the blank deck Turboslide ships. An agent token needs the admin scope. Input: id. CLI: turboslide template default <id>. MCP: deck_set_default_template.
- slide.list (List slides; read; cli, mcp, http, window; M1): Every slide in deck order with its number, section, title, kind and lint counts. Input: sectionId?. CLI: turboslide slides. MCP: deck_list_slides.
- slide.get (Get slide; read; cli, mcp, http, window; M1): The normalized slide, the assets it references and its last render record. Input: slideId. CLI: turboslide slide get <slideId>. MCP: deck_get_slide.
- slide.insert (Insert slide; mutates; cli, mcp, http, window; M2): Inserts a slide into a section after the named slide, or first. Input: sectionId, after?, slide, baseRevision. CLI: turboslide slide insert --section <sectionId> --after <after> < slide.json. MCP: deck_insert_slide.
- slide.remove (Remove slide; mutates; cli, mcp, http, window; M2): Removes a slide from the deck and its section. Input: slideId, baseRevision. CLI: turboslide slide remove <slideId>. MCP: deck_remove_slide.
- slide.move (Move slide; mutates; cli, mcp, http, window; M2): Moves a slide to a section after the named slide, or first. Input: slideId, sectionId, after?, baseRevision. CLI: turboslide slide move <slideId> --to <sectionId> --after <after>. MCP: deck_move_slide.
- slide.update (Patch slide; mutates; cli, mcp, http, window; M2): Applies a mutation list to one slide and returns the normalized slide with its findings. Input: slideId, baseRevision, mutations. CLI: turboslide slide patch <slideId> --set <pointer>=<value>. MCP: deck_update_slide.
- slide.replace (Replace slide; mutates; cli, mcp, http, window; M2): Replaces a slide with the given document; what the source drawer’s Apply emits. Input: slideId, baseRevision, slide. CLI: turboslide slide put <slideId> < slide.json. MCP: deck_replace_slide.
- slide.setLayout (Set layout; mutates; cli, mcp, http, window; M6): Moves a content slide to another layout and refiles its blocks: to freeform the slide converts to the canvas, every block getting the position box it is drawn at (measured, gslides-parity SPEC-2 1.6); from freeform the boxes are dropped and the blocks fall into the target slots by geometry (docs/freeform.md). Input: slideId, layout, baseRevision. CLI: turboslide slide set-layout <slideId> --type <type> --ratio <ratio> --gap <gap> --layout <layout>. MCP: deck_set_layout.
- slide.new (New slide; mutates; cli, mcp, http, window; GS1): Inserts one slide made from a layout with empty placeholders after the named slide (or first in the section), as one slide.insert with template set; a Section header starts a new section that takes the slides after the anchor. Input: layout, after?, sectionId?, id?, baseRevision. CLI: turboslide slide new --layout <layout> --after <after> --section <sectionId> --id <id>. MCP: deck_new_slide.
- slide.duplicate (Duplicate slide; mutates; cli, mcp, http, window; GS1): Copies the named slides with fresh ids after the last of them, in one write; a copied section header starts a new section. Input: slideIds, baseRevision. CLI: turboslide slide duplicate <slideIds>. MCP: deck_duplicate_slide.
- slide.skip (Skip slide; mutates; cli, mcp, http, window; GS1): Sets or clears skip on the named slides in one write: a skipped slide is left out of the slideshow, the shared view, the downloads and the print unless asked. Input: slideIds, skip, baseRevision. CLI: turboslide slide skip <slideIds> --off. MCP: deck_skip_slide.
- slide.applyLayout (Apply layout; mutates; cli, mcp, http, window; GS1): Applies a layout to the named slides in one write, one slide.replace per slide: the title, the body, the lists, the tables and the pictures move into the matching placeholders and the rest is appended; only a Title slide or a Main point drops what does not fit, and the output names the dropped blocks. Input: slideIds, layout, baseRevision. CLI: turboslide slide apply-layout <slideIds> <layout>. MCP: deck_apply_layout.
- slide.import (Import slides; mutates; cli, mcp, http, window; GS1): Copies slides from another deck after the named slide in one write: the assets they use are copied into this deck, colliding slide ids are renamed, and the output lists the slides and the assets that came over. Input: sourceDeckId, slideIds, after?, sectionId?, baseRevision. CLI: turboslide slide import <sourceDeckId> <slideIds> --after <after> --section <sectionId>. MCP: deck_import_slides.
- slide.toCanvas (Arrange by hand; mutates; cli, mcp, http, window; GS2): Converts slides to the canvas, the freeform layout, every object taking the position box it is drawn at (measured on a rendered sheet); a slide already on the freeform layout is left as it is; one write, one browser page for the call. Input: slideIds, baseRevision. CLI: turboslide slide to-canvas <slideIds>. MCP: deck_slide_to_canvas.
- slide.setBackground (Change background; mutates; cli, mcp, http, window; GS2): Writes the background colour of slides in one write; null removes it and the theme default applies. A background picture is a picture object at the bottom of the stack. Input: slideIds, background, baseRevision. CLI: turboslide slide background <slideIds> --color <color>. MCP: deck_set_slide_background.
- text.replaceAll (Find and replace; mutates; cli, mcp, http, window; GS1): Replaces every occurrence of a string in the visible text of the deck (every Text of every block, the title slide fields, the table cells and the speaker notes) in one write, case insensitive unless asked; the output counts the replacements. Input: find, replace, matchCase?, slideIds?, baseRevision. CLI: turboslide text replace <find> <replace> --match-case --slides <slideIds>. MCP: deck_replace_text.
- block.set (Set block property; mutates; cli, mcp, http, window; M2): Writes one property of a block by JSON pointer; an absent value deletes it. Input: slideId, blockId, path, value?, baseRevision. CLI: turboslide block set <slideId>#<blockId> <path> <value>. MCP: deck_update_block.
- block.insert (Insert block; mutates; cli, mcp, http, window; M2): Inserts a block into a slot after the named block, or first; a block carrying pos on a slide that is not a canvas yet converts the slide first and lands as an object (gslides-parity SPEC-2 1.6). Input: slideId, slot, after?, block, baseRevision. CLI: turboslide block insert <slideId> --slot <slot> --after <after> < block.json. MCP: deck_insert_block.
- block.remove (Remove block; mutates; cli, mcp, http, window; M2): Removes a block from its slide. Input: slideId, blockId, baseRevision. CLI: turboslide block remove <slideId>#<blockId>. MCP: deck_remove_block.
- block.move (Move block; mutates; cli, mcp, http, window; M2): Moves a block to a slot after the named block, or first. Input: slideId, blockId, slot, after?, baseRevision. CLI: turboslide block move <slideId>#<blockId> --slot <slot> --after <after>. MCP: deck_move_block.
- block.align (Align blocks; mutates; cli, mcp, http, window; M6): Aligns objects on one edge, against the selection for several, the slide for one, or the content box; against the selection the shared edge is the extreme object's own edge unless snap is true, and the slide's and the content box's edges stay exact; a slide that is not a canvas yet converts first (gslides-parity SPEC-2 0.80, 1.6; docs/FOCUS.md rank 14). Input: slideId, blockIds, edge, to?, snap?, baseRevision. CLI: turboslide block align <slideId> --blocks <blockIds> --edge <edge> --to <to>. MCP: deck_align_blocks.
- block.distribute (Distribute blocks; mutates; cli, mcp, http, window; M6): Spreads freeform blocks along one axis with equal gaps between the first and the last, or with a fixed gap; with snap the moved edges round to the 8 px grid. Input: slideId, blockIds, axis, gap?, snap?, baseRevision. CLI: turboslide block distribute <slideId> --blocks <blockIds> --axis <axis> --gap <gap>. MCP: deck_distribute_blocks.
- block.order (Order block; mutates; cli, mcp, http, window; M6): Moves a freeform block through the stacking order, to the front or back or by one step, or to a given z rank, and renumbers the stack densely. Input: slideId, blockId, move?, z?, baseRevision. CLI: turboslide block order <slideId>#<blockId> --move <move> --z <z>. MCP: deck_order_block.
- block.duplicate (Duplicate block; mutates; cli, mcp, http, window; GS1): Copies the named blocks of a slide with fresh ids, each after its original; a positioned copy sits 16 px right and down and on top of the stack, a copied group takes a fresh tag and copied connectors keep their attachments to copied targets (gslides-parity SPEC-2 section 3). Input: slideId, blockIds, baseRevision. CLI: turboslide block duplicate <slideId> --blocks <blockIds>. MCP: deck_duplicate_block.
- block.group (Group; mutates; cli, mcp, http, window; GS2): Writes one group tag on two or more objects of a slide in one write; the tag is generated when none is given; a slide that is not a canvas yet converts first. Input: slideId, blockIds, group?, baseRevision. CLI: turboslide block group <slideId> --blocks <blockIds> --as <group>. MCP: deck_group_blocks.
- block.ungroup (Ungroup; mutates; cli, mcp, http, window; GS2): Removes the group tag from the named objects, or from every member of a named group, in one write. Input: slideId, blockIds?, group?, baseRevision. CLI: turboslide block ungroup <slideId> --group <group> --blocks <blockIds>. MCP: deck_ungroup_blocks.
- block.regroup (Regroup; mutates; cli, mcp, http, window; GS2): Writes a tag back on the named objects, the set the editor remembers from the last ungroup. Input: slideId, blockIds, group, baseRevision. CLI: turboslide block regroup <slideId> --blocks <blockIds> --as <group>. MCP: deck_regroup_blocks.
- block.rotate (Rotate; mutates; cli, mcp, http, window; GS2): Rotates objects to an angle or by an angle, degrees clockwise normalized to 0 up to 360, about each box or about the selection (a group rotation moves the members around the union centre); attached connectors follow; a slide that is not a canvas yet converts first. Input: slideId, blockIds, to?, by?, about?, baseRevision. CLI: turboslide block rotate <slideId>#<blockId> --to <to> --by <by> --about <about>. MCP: deck_rotate_block.
- block.flip (Flip; mutates; cli, mcp, http, window; GS2): Toggles one flip axis on objects; about the selection every member also mirrors about the union centre and its angle becomes 360 minus itself (a group flip); attached connectors follow. Input: slideId, blockIds, axis, about?, baseRevision. CLI: turboslide block flip <slideId>#<blockId> --axis <axis> --about <about>. MCP: deck_flip_block.
- block.crop (Crop image; mutates; cli, mcp, http, window; GS2): Writes the crop of a picture as fractions of it trimmed from each side; a slide that is not a canvas yet converts first when the picture is positioned. Input: slideId, blockId, trim, baseRevision. CLI: turboslide block crop <slideId>#<blockId> --left <left> --right <right> --top <top> --bottom <bottom>. MCP: deck_crop_image.
- block.mask (Mask image; mutates; cli, mcp, http, window; GS2): Clips a picture to a closed shape preset, or removes the mask with null. Input: slideId, blockId, mask, baseRevision. CLI: turboslide block mask <slideId>#<blockId> <mask>. MCP: deck_mask_image.
- block.resetImage (Reset image; mutates; cli, mcp, http, window; GS2): Removes the crop, the mask, the adjustments and the round one crop anchor and aspect of a picture in one write. Input: slideId, blockId, baseRevision. CLI: turboslide block reset-image <slideId>#<blockId>. MCP: deck_reset_image.
- block.adjust (Adjustments; mutates; cli, mcp, http, window; GS2): Writes a picture’s transparency (0 to 1), brightness and contrast (-1 to 1); null clears one. Input: slideId, blockId, transparency?, brightness?, contrast?, baseRevision. CLI: turboslide block adjust <slideId>#<blockId> --transparency <transparency> --brightness <brightness> --contrast <contrast>. MCP: deck_adjust_image.
- block.setAlt (Alt text; mutates; cli, mcp, http, window; GS2): Writes the description of an object: on the block for a shape, chart, line or text box, on the asset for a block that shows a picture, so two pictures of one asset share one description; the output names where it went. Input: slideId, blockId, alt, baseRevision. CLI: turboslide block alt <slideId>#<blockId> <alt>. MCP: deck_set_alt_text.
- block.shadow (Drop shadow; mutates; cli, mcp, http, window; GS2): Writes or removes the drop shadow of objects: colour, opacity, angle, distance and blur, the absent ones at Google’s defaults. Input: slideId, blockIds, shadow, baseRevision. CLI: turboslide block shadow <slideId>#<blockId> --on --color <color> --opacity <opacity> --angle <angle> --distance <distance> --blur <blur>. MCP: deck_set_shadow.
- block.autofit (Text fitting; mutates; cli, mcp, http, window; GS2): Writes the fitting mode of a text carrying block: Do not autofit, Shrink text on overflow or Resize shape to fit text (grow, positioned blocks only); with apply the fit is measured and written in the same write, and the output names the fields written. Input: slideId, blockId, autofit, apply?, baseRevision. CLI: turboslide block autofit <slideId>#<blockId> <autofit> --apply. MCP: deck_set_autofit.
- text.style (Text style; mutates; cli, mcp, http, window; GS2): Marks a range of a Text italic, underlined, struck, superscript or subscript, or gives it a text or highlight colour (true sets, false clears, null clears a colour), as one text.replace of the whole Text; the output returns the new markup. Input: slideId, blockId, path, range, marks, baseRevision. CLI: turboslide text style <slideId>#<blockId> <path> --range <range> --italic --underline --strike --superscript --subscript --color <color> --highlight <highlight>. MCP: deck_style_text.
- text.list (List options; mutates; cli, mcp, http, window; GS2): Writes the marker, the preset and the items’ levels of a list block (a paragraph or text box converts to a list first); levels run 1 to 9. Input: slideId, blockId, marker?, preset?, items?, level?, levelBy?, baseRevision. CLI: turboslide text list <slideId>#<blockId> --marker <marker> --preset <preset> --items <items> --level <level>. MCP: deck_set_list.
- text.spacing (Line and paragraph spacing; mutates; cli, mcp, http, window; GS2): Writes the line spacing (a factor) and the space before and after paragraphs (px) of text blocks; null clears one. Input: slideId, blockIds, line?, before?, after?, baseRevision. CLI: turboslide text spacing <slideId>#<blockId> --line <line> --before <before> --after <after>. MCP: deck_set_spacing.
- text.columns (Columns; mutates; cli, mcp, http, window; GS2): Writes the column count of text blocks, 1 to 3. Input: slideId, blockIds, columns, baseRevision. CLI: turboslide text columns <slideId>#<blockId> <columns>. MCP: deck_set_columns.
- text.indent (Indent; mutates; cli, mcp, http, window; GS2): Steps the left indent of text blocks by 64 px in or out, or writes it; on a list block with items the items’ levels step instead. Input: slideId, blockIds, by?, to?, items?, baseRevision. CLI: turboslide text indent <slideId>#<blockId> --in --out --to <to> --items <items>. MCP: deck_indent_text.
- text.case (Capitalization; mutates; cli, mcp, http, window; GS2): Rewrites the characters of a range in lowercase, UPPERCASE or Title Case, marks and links kept, as one text.replace. Input: slideId, blockId, path, range, mode, baseRevision. CLI: turboslide text case <slideId>#<blockId> <path> --range <range> <mode>. MCP: deck_set_case.
- text.insert (Insert text; mutates; cli, mcp, http, window; GS2): Inserts a string at a plain text offset of a Text, the special characters picker’s write, as one text.replace at [at, at]. Input: slideId, blockId, path, at, text, baseRevision. CLI: turboslide text insert <slideId>#<blockId> <path> --at <at> <text>. MCP: deck_insert_text.
- chart.setData (Edit chart data; mutates; cli, mcp, http, window; GS2): Writes a chart’s categories and series in one write; every series carries one value per category. Input: slideId, blockId, categories, series, baseRevision. CLI: turboslide chart set-data <slideId>#<blockId> < data.json. MCP: deck_set_chart_data.
- chart.setKind (Chart type; mutates; cli, mcp, http, window; GS2): Writes a chart’s kind; a pie keeps the first series and the output names the dropped ones. Input: slideId, blockId, kind, baseRevision. CLI: turboslide chart set-kind <slideId>#<blockId> <kind>. MCP: deck_set_chart_kind.
- table.merge (Merge cells; mutates; cli, mcp, http, window; GS2): Merges the cells from one corner to another into the anchor; the covered cells’ texts join the anchor’s with a line break. Input: slideId, blockId, from, to, baseRevision. CLI: turboslide table merge <slideId>#<blockId> --from <from> --to <to>. MCP: deck_merge_cells.
- table.unmerge (Unmerge cells; mutates; cli, mcp, http, window; GS2): Removes the merge that covers a cell. Input: slideId, blockId, at, baseRevision. CLI: turboslide table unmerge <slideId>#<blockId> --at <at>. MCP: deck_unmerge_cells.
- table.insertRows (Insert rows; mutates; cli, mcp, http, window; GS2): Inserts rows above or below a row, merges and cell styles shifted, in one write. Input: slideId, blockId, at, count?, where, baseRevision. CLI: turboslide table insert-rows <slideId>#<blockId> --at <at> --count <count> --above --below. MCP: deck_insert_rows.
- table.insertColumns (Insert columns; mutates; cli, mcp, http, window; GS2): Inserts columns left or right of a column, every row gaining empty cells, merges and cell styles shifted, in one write. Input: slideId, blockId, at, count?, where, baseRevision. CLI: turboslide table insert-columns <slideId>#<blockId> --at <at> --count <count> --left --right. MCP: deck_insert_columns.
- table.deleteRows (Delete rows; mutates; cli, mcp, http, window; GS2): Deletes the rows from one index to another; deleting every row removes the table. Input: slideId, blockId, from, to?, baseRevision. CLI: turboslide table delete-rows <slideId>#<blockId> --from <from> --to <to>. MCP: deck_delete_rows.
- table.deleteColumns (Delete columns; mutates; cli, mcp, http, window; GS2): Deletes the columns from one index to another; deleting every column removes the table. Input: slideId, blockId, from, to?, baseRevision. CLI: turboslide table delete-columns <slideId>#<blockId> --from <from> --to <to>. MCP: deck_delete_columns.
- table.distribute (Distribute rows or columns; mutates; cli, mcp, http, window; GS2): Gives every row an equal height or every column an equal width from a total; without a total the widths or heights clear so the content sizes them. Input: slideId, blockId, axis, total?, range?, baseRevision. CLI: turboslide table distribute <slideId>#<blockId> <axis> --total <total>. MCP: deck_distribute_table.
- table.cellStyle (Cell fill and border; mutates; cli, mcp, http, window; GS2): Writes the fill and the border of cells in one write; null clears one; a border weight of 0 is Google’s Transparent border. Input: slideId, blockId, cells, fill?, border?, baseRevision. CLI: turboslide table cell-style <slideId>#<blockId> --at <cells> --fill <fill> --border-color <color> --border-weight <weight> --border-dash <dash>. MCP: deck_style_cells.
- shape.set (Shape; mutates; cli, mcp, http, window; GS2): Writes the shape fields of shape blocks in one write: the preset, its adjust values, fill, border colour, weight, dash and corner radius; null clears one. Input: slideId, blockIds, kind?, adjust?, fill?, stroke?, width?, dash?, radius?, baseRevision. CLI: turboslide shape set <slideId>#<blockId> --kind <kind> --adjust <adjust> --fill <fill> --stroke <stroke> --width <width> --dash <dash> --radius <radius>. MCP: deck_set_shape.
- line.set (Line; mutates; cli, mcp, http, window; GS2): Writes the line fields of line blocks in one write: the kind, the start and end decorations, weight, dash, bend and points; a connect end attaches to a shape’s connection site and moves the end onto it, null detaches. Input: slideId, blockIds, kind?, start?, end?, weight?, dash?, bend?, points?, connect?, baseRevision. CLI: turboslide line set <slideId>#<blockId> --kind <kind> --start <start> --end <end> --weight <weight> --dash <dash> --bend <bend> --points <points> --connect-start <connectStart> --connect-end <connectEnd> --detach <detach>. MCP: deck_set_line.
- diagram.insert (Insert diagram; mutates; cli, mcp, http, window; GS2): Inserts a diagram template as one group of shape, text and line objects, centred on the sheet unless a box is given; a slide that is not a canvas yet converts first; the output names the ids. Input: slideId, kind, count, style?, pos?, after?, baseRevision. CLI: turboslide diagram insert <slideId> --kind <kind> --count <count> --style <style> --pos <pos>. MCP: deck_insert_diagram.
- section.set (Set sections; mutates; cli, mcp, http, window; M2): Replaces the section list, the only place slide order lives. Input: sections, baseRevision. CLI: turboslide sections set < sections.json. MCP: deck_set_sections.
- slide.lease (Lease slide; mutates; cli, mcp, http, window; M2): Takes a lease on a slide for the author; another author’s write is rejected with 409 unless forced. Input: slideId, minutes?, force?, release?. CLI: turboslide lease <slideId> --minutes <minutes> --force --release. MCP: deck_lease_slide.
- asset.add (Add asset; mutates; cli, mcp, http, window; M5): Adds a file, a data URL or a fetched URL as an asset with its role, alt, license fields and optional two-tone treatment; returns the asset with twins and metrics. Input: id?, file?, url?, role, alt, source?, title?, artist?, license?, shareAlike?, sourceUrl?, credit?, twoTone?, treatment?, plate?, upload?, replaceSource?, baseRevision. CLI: turboslide asset add <file> --role <role> --alt <alt> --two-tone --black <black> --plate <plate> --replace-source <replaceSource>. MCP: deck_asset_add.
- asset.dither (Dither asset; mutates; cli, mcp, http, window; M5): Re-runs the two-tone pipeline on an asset with new parameters and reports plate clearance; with fromRecorded it reads the committed twins back and verifies them cell for cell. Input: assetId?, treatment?, params?, plate?, source?, allTwoTone?, fromRecorded?, verifyCells?, pattern?, cell?, tone?, baseRevision. CLI: turboslide asset dither <assetId> --gamma <gamma> --plate <plate> --pattern <pattern> --cell <cell> --tone <tone> --all-two-tone --from-recorded --verify-cells. MCP: deck_asset_dither.
- asset.capture (Capture page; mutates; cli, mcp, http; M5): Captures a page at 1440 by 900 in one or both themes through a per-site recipe, optionally cropped to a region, with identical-region 2x detail crops as further assets. Input: url, id?, alt?, role?, viewport?, scale?, theme, region?, details?, recipe?, settleMs?, format?, allowHosts?, baseRevision. CLI: turboslide asset capture <url> --theme <theme> --scale <scale> --recipe <recipe> --region <region>. MCP: deck_asset_capture.
- material.capture (Capture material; mutates; cli, mcp, http, window; M5): Renders a shader material at 3200 by 1800 and captures frozen frames at the given anchors as assets with recipe keys, through the two-tone screen when asked; one anchor returns the asset, several the list. Input: materialId, preset?, uniforms?, size?, anchors, id?, role?, alt?, twoTone?, treatment?, plate?, backend?, baseRevision. CLI: turboslide material capture <materialId> --preset <preset> --uniforms <uniforms> --anchor <anchors> --two-tone --plate <plate>. MCP: deck_material_capture.
- material.list (List materials; read; cli, mcp, http, window; M5): The material catalog: every paper:* entry with its uniform schema and palette presets, and the proto:* engines with their availability. Input: materialId?. CLI: turboslide material list. MCP: deck_material_list.
- render.slide (Render; read; cli, mcp, http, window; M1): Renders slides in one or both themes at 1x or 2x and returns render records with image paths. Input: slideIds, themes?, scale?, format?, out?. CLI: turboslide render <slideIds> --theme <themes> --out <out> --json. MCP: deck_render.
- render.sheet (Contact sheet; read; cli, mcp, http, window; M1): Renders a contact sheet per theme with a JSON cell map from cell boxes to slide ids. Input: slideIds, themes?, cols?, thumb?, numbered?, overlay?, out?. CLI: turboslide sheet <slideIds> --cols <cols> --thumb <thumb> --overlay <overlay>. MCP: deck_sheet.
- lint.run (Lint; read; cli, mcp, http, window; M1): Runs the grammar linter on slides in the static, rendered or both layers and returns findings. Input: slideIds, layers?, rule?. CLI: turboslide lint <slideIds> --json. MCP: deck_lint.
- fix.run (Fix; mutates; cli, mcp, http, window; M2): Applies the fix mutations of findings that carry one and reports what remains. Input: slideIds, rule?, dryRun?, baseRevision. CLI: turboslide fix <slideIds> --rule <rule> --dry-run. MCP: deck_fix.
- diff.run (Diff; read; cli, mcp, http, window; M2): The mutation list between two revisions in prose, optionally with before and after crops. Input: from?, to?, staged?, render?, themes?, out?. CLI: turboslide diff <from> <to> --staged --render --theme <themes> --out <out>. MCP: deck_diff.
- version.save (Save version; mutates; cli, mcp, http, window; M2): Saves a named version at the current revision. Input: note. CLI: turboslide version save -m <note>. MCP: deck_version_save.
- version.list (List versions; read; cli, mcp, http, window; M2): The saved versions with author, note and revision. Input: none. CLI: turboslide version list. MCP: deck_version_list.
- version.restore (Restore version; mutates; cli, mcp, http, window; M2): Restores a version as a mutation, so it is undoable and visible in History. Input: n, baseRevision. CLI: turboslide version restore <n>. MCP: deck_version_restore.
- judge.bundle (Judge bundle; read; cli, mcp; M4): Packages renders, sheets with cell maps, render records and findings into a directory for a judge run. Input: slideIds, out. CLI: turboslide judge bundle --out <out>. MCP: deck_judge_bundle.
- view.goto (Go to slide; read; window, mcp; M3): Shows a slide in the viewer, the editor or the presenter. Input: slideId. MCP: deck_goto_slide.
- view.mode (View mode; read; window; M3): Switches between slide, grid and book mode. Input: mode. MCP: deck_set_view.
- view.theme (View theme; read; window; M3): Sets the theme of chrome and sheet together through the gt-theme key. Input: theme. MCP: deck_set_view.
- view.present (Present; read; window; M3): Enters or leaves present mode. Input: on. MCP: deck_set_view.
- view.zoom (Zoom; read; window, mcp; GS1): Sets the stage scale in the editor, a factor of the sheet size from 0.25 to 16 or fit, keeping a named sheet point under the stage centre; the output reports the effective zoom and the scroll; a view action with no document field. Input: zoom, center?. MCP: deck_set_zoom.
- export.run (Export; read; cli, mcp, http, window; M2): Exports the deck to PPTX in flatten (perfect, pixel identical) or native (editable text) mode and returns the verified report. Input: format, mode?, theme?, fonts?, embedFonts?, headings?, rasterScale?, pictureScale?, excludeShareAlike?, baseline?, verify?, slideIds?, includeSkipped?, includeNotes?, includeComments?, out?, batch?, merge?. CLI: turboslide export <format> --mode <mode> --theme <theme> --fonts <fonts> --embed-fonts --exclude-share-alike --baseline-target <baseline> --include-skipped --include-notes --include-comments --verify --out <out>. MCP: deck_export.
- export.check (Check export file; read; cli; M6): Reopens an exported PPTX with python-pptx, walks the package against its content types and relationships, and reports the pages, size, raster formats, fonts and any invalid part. Input: file, python?, quickLook?, out?. CLI: turboslide export check <file> --python <python> --out <out>.
- export.text (Download as plain text; read; cli, mcp, http, window; GS1): The deck as plain text: one block of paragraphs per slide in order, table cells joined by tabs, a blank line between slides, the notes after each slide when asked; skipped slides left out unless asked. Input: slideIds?, includeNotes?, includeSkipped?, includeComments?. CLI: turboslide export txt <slideIds> --include-notes --include-skipped --include-comments. MCP: deck_export_text.
- build.run (Build standalone; read; cli, mcp, http, window; M1): Writes the single-file HTML deck with fonts and assets inlined and asserts the size budget. Input: out, budgetMB?, quality?, includeSkipped?, includeNotes?. CLI: turboslide build --out <out> --budget <budgetMB>. MCP: deck_build.
- fonts.build (Build export fonts; read; cli; M2): Cuts the static export font set from InterVariable into packages/fonts/export with fonts.json (scripts/build-fonts.py); --check reports a stale committed set. Input: check?, python?. CLI: turboslide fonts build --check.
- import.run (Import deck; mutates; cli; M1): Imports the Prototemplate deck HTML into a Turboslide deck and writes the import report. Input: from, into. CLI: turboslide import <from> --into <into>.
- validate.run (Validate; read; cli, mcp, http, window; M1): Validates a deck directory and returns the issues and the normalized document. Input: path?. CLI: turboslide validate <path>. MCP: deck_validate.
- source.read (Read source; read; window; M3): Glyphfield standard action: the exact current source document of the active owner. Input: none.
- source.apply (Apply source; mutates; window; M3): Glyphfield standard action: applies a JSON string or object through the active owner’s validator. Input: value.
- controls.list (List controls; read; window; M3): Glyphfield standard action: the visible interactive controls with labels, data-control ids and values. Input: none.
- control.activate (Activate control; read; window; M3): Glyphfield standard action: activates a control by accessible label or data-control id. Input: string.
- control.set (Set control; read; window; M3): Glyphfield standard action: sets a form control by accessible label or data-control id. Input: label, value.
- artifact.download (Download artifact; read; window; M3): Glyphfield standard action: saves a returned artifact with its deterministic file name. Input: fileName.
- deck.tailor (Tailor for a customer; mutates; cli, mcp, http, window; P1): The tailoring pass as one write: replaces the customer name in every visible text and the notes, swaps the pictures whose alt text names the old customer for one asset, and skips the named slides; answers the counts per step and the slides that changed. Input: replacements?, logo?, skip?, baseRevision. CLI: turboslide tailor --replace <from>=<to> --skip <skip> --logo <assetId> --logo-alt <replaceAlt>. MCP: deck_tailor.
- assist.propose (Ask the assistant; read; cli, mcp, http, window; P1): Asks the assistant for a proposal over the named slides: a shorter rewrite or speaker notes, or a free ask that resolves to one of the two; answers signed cards a caller accepts with assist.accept. A read: nothing is written until a card is accepted. Input: intent, prompt, slideIds?, baseRevision. CLI: turboslide assist propose <prompt> --intent <intent> --slides <slideIds>. MCP: deck_assist_propose.
- assist.accept (Accept a proposal; mutates; cli, mcp, http, window; P1): Writes one card assist.propose returned, as one write by the assistant: the signature, the expiry and the deck are verified, a card behind the deck is re based when its blocks are unchanged and refused otherwise. Input: card, baseRevision. CLI: turboslide assist accept < card.json. MCP: deck_assist_accept.
- brand.get (Brand kit; read; cli, mcp, http, window; P1): The brand kit record of the deck: the colour roles, the faces, the logo and footer slots, the slide numbers, the frame toggles and the words that never translate; an empty object for a deck that reads the deployment’s default kit. Input: none. CLI: turboslide brand get. MCP: deck_brand_get.
- brand.set (Set a brand kit field; mutates; cli, mcp, http, window; P1): Writes one value of the brand kit by JSON pointer (/colors/light/primary, /fonts/display, /mark, /footer/logo, /footer/text, /counter/format, /frame/rails, /positions/mark, /lexicon, /appearance, /name) as one deck.set mutation under /brand, the write every Brand kit panel control makes; an absent value removes the field. The record is validated before the commit. Input: path, value?, baseRevision. CLI: turboslide brand set <path> <value>. MCP: deck_brand_set.
- brand.reset (Reset the brand kit; mutates; cli, mcp, http, window; P1): Removes the brand kit record, so the deck reads the deployment’s default kit, or one field of it when a pointer is given; a deck with nothing to remove writes no revision. Input: path?, baseRevision. CLI: turboslide brand reset [path]. MCP: deck_brand_reset.
- font.list (Fonts; read; cli, mcp, http, window; P1): The font catalog: every face with the name PowerPoint and Google Slides use, its category, weights, italic and licence (gslides-parity SPEC-5-amendments A5; docs/PRODUCT.md 4.2). Input: none. CLI: turboslide fonts list. MCP: deck_fonts_list.
- logo.search (Find a logo; read; cli, mcp, http, window; P1): Searches the cached index of thesvg.org’s brand marks by title, slug, alias and category, ranked prefix, then word start, then substring, brands before community; answers each mark’s variants, its licence as recorded with the seller’s sentence, its brand site and guidelines and whether its default reads on paper and on ink, with the index’s date and its last upstream failure. Input: query, limit?, kind?, collection?, since?. CLI: turboslide logo search <query> --limit <limit> --kind <kind> --collection <collection> --since <builtAt>. MCP: deck_logo_search.
- logo.insert (Insert a logo; mutates; cli, mcp, http, window; P1): Stores a thesvg.org mark on the deck as an asset with the role logo, PNG twins at 3x of the logo size, the sanitized SVG as its source file and a source record naming the mark, its licence and its guidelines, never the source’s address; with slideId it places the picture at the logo size in the free area of the body slot, with blockId it swaps that picture’s asset and keeps the box, with everySlide or kit it writes the brand kit’s logo slots in the same write. Input: slug, variant?, slideId?, box?, everySlide?, kit?, blockId?, baseRevision. CLI: turboslide logo insert <slug> --variant <variant> --slide <slideId> --every-slide --kit --block <blockId>. MCP: deck_logo_insert.
- logo.refresh (Refresh the logo index; read; cli, mcp, http; P1): Rebuilds the cached logo index from thesvg.org’s manifest inside its time budget, marks the variants that answer 404 unavailable, drops the marks the source removed with their cached files and answers the counts; with dryRun it answers the counts, the index’s date, its last failure and its progress without an upstream fetch. Input: dryRun?. CLI: turboslide logo refresh --dry-run. MCP: deck_logo_refresh.
- presence.list (Collaborators; read; cli, mcp, http, window; GS3): The roster of the deck’s room with the computed mark, slide, selection, pointer and follow target per participant; server side on the window transport. Input: none. CLI: turboslide presence list. MCP: deck_list_presence.
- presence.follow (Follow; mutates; cli, mcp, http, window; GS3): The attached page follows a client’s slide and scroll; refused for viewers, commenters, anonymous people and agents with Go to slide offered instead. Input: clientId. CLI: turboslide presence follow <clientId>. MCP: deck_follow_client.
- presence.unfollow (Stop following; mutates; cli, mcp, http, window; GS3): The attached page stops following. Input: none. CLI: turboslide presence unfollow. MCP: deck_unfollow.
- presence.pointer (Live pointer; mutates; cli, mcp, http, window; GS3): Turns the caller’s live pointer on or off, persisted on the principal record per deck. Input: on. CLI: turboslide presence pointer --on. MCP: deck_set_pointer.
- sync.status (Sync status; read; cli, mcp, http, window; GS3): The client’s position in the room: the last stream entry, the revision, the pending and retained counts, the tier, the transport and whether the stream is connected. Input: none. CLI: turboslide sync status. MCP: deck_sync_status.
- deck.watch (Watch; read; cli, mcp, http, window; GS3): Long polls for the checkpoints and comment entries since a revision, the polling form for a transport without a stream; answers at once when something is newer, else at the timeout. Input: since?, timeoutMs?. CLI: turboslide deck watch <id> --from <from> --since <since>. MCP: deck_watch.
- deck.follow (Follow a hosted deck; mutates; cli; GS3): Mirrors a hosted deck’s records into the checkout byte for byte as they land, with the comments when asked, and pushes local writes back when asked; CLI only. Input: from, push?, comments?, force?, theirs?. CLI: turboslide deck follow <id> --from <from> --push --comments --force --theirs.
- comment.add (Comment; mutates; cli, mcp, http, window; GS3): Starts a thread at an anchor (the deck, a slide, a block, a text range, a table cell or the notes) with a plain text body whose {@n} tokens name its mentions, optionally assigned; server side. Input: anchor, body, assignee?, baseRevision?. CLI: turboslide comment add <anchor> -m <text> --mention <who> --assign <who>. MCP: deck_add_comment.
- comment.reply (Reply; mutates; cli, mcp, http, window; GS3): Appends a reply to a thread; server side. Input: threadId, body, baseRevision?. CLI: turboslide comment reply <threadId> -m <text> --mention <who>. MCP: deck_reply_comment.
- comment.edit (Edit comment; mutates; cli, mcp, http, window; GS3): Rewrites a comment’s body, its author only; 409 with the current thread when the thread moved since expectedUpdatedAt. Input: threadId, commentId, body, expectedUpdatedAt, baseRevision?. CLI: turboslide comment edit <threadId> <commentId> -m <text>. MCP: deck_edit_comment.
- comment.delete (Delete comment; mutates; cli, mcp, http, window; GS3): Tombstones a comment, keeping its replies; the first comment’s tombstone keeps the thread; restore brings it back within 30 days. Input: threadId, commentId, restore?, baseRevision?. CLI: turboslide comment delete <threadId> <commentId> --restore. MCP: deck_delete_comment.
- comment.resolve (Resolve; mutates; cli, mcp, http, window; GS3): Marks a thread resolved; last writer wins. Input: threadId, baseRevision?. CLI: turboslide comment resolve <threadId>. MCP: deck_resolve_comment.
- comment.reopen (Re-open; mutates; cli, mcp, http, window; GS3): Reopens a resolved thread; last writer wins. Input: threadId, baseRevision?. CLI: turboslide comment reopen <threadId>. MCP: deck_reopen_comment.
- comment.assign (Assign; mutates; cli, mcp, http, window; GS3): Assigns a thread to a principal or an invitation (an agent principal is assignable), or clears the assignment with null; last writer wins. Input: threadId, assignee, baseRevision?. CLI: turboslide comment assign <threadId> <who> --clear. MCP: deck_assign_comment.
- comment.done (Mark as done; mutates; cli, mcp, http, window; GS3): The assignee marks the thread done; last writer wins. Input: threadId, baseRevision?. CLI: turboslide comment done <threadId>. MCP: deck_done_comment.
- comment.react (React; mutates; cli, mcp, http, window; GS3): Toggles the caller’s reaction on a comment from the 24 emoji palette; reactions are a set per emoji keyed by principal. Input: threadId, commentId, emoji, on, baseRevision?. CLI: turboslide comment react <threadId> <commentId> <emoji> --off. MCP: deck_react_comment.
- comment.list (Comments; read; cli, mcp, http, window; GS3): The threads of the deck or a slide with their anchors resolved against the current document, filtered by block, state, assignment to the caller, author, search text or revision; 403 without readComments. Input: slideId?, blockId?, state?, forMe?, author?, search?, includeDeleted?, since?, limit?. CLI: turboslide comments <slideId> --block <blockId> --state <state> --for-me --author <author> --search <search> --since <since>. MCP: deck_list_comments.
- comment.get (Comment thread; read; cli, mcp, http, window; GS3): One thread with its anchor resolved against the current document. Input: threadId. CLI: turboslide comment get <threadId>. MCP: deck_get_comment.
- comment.link (Link to comment; read; cli, mcp, http, window; GS3): The editor and the view URLs that open the deck on the thread’s slide with its card expanded; runs in the page on the window transport. Input: threadId. CLI: turboslide comment link <threadId>. MCP: deck_comment_link.
- notification.list (Notifications; read; cli, mcp, http, window; GS3): The caller’s inbox, newest first: mentions, replies, assignments, resolutions, reactions, access requests and grants, coalesced per thread inside a 15 minute window. Input: unread?, since?, limit?. CLI: turboslide notifications --unread --since <since>. MCP: deck_list_notifications.
- notification.markRead (Mark as read; mutates; cli, mcp, http, window; GS3): Marks the named notifications read, or every one; answers the unread count. Input: ids?, all?. CLI: turboslide notifications read <ids> --all. MCP: deck_mark_notifications_read.
- notification.settings (Notification settings; mutates; cli, mcp, http, window; GS3): Reads or writes the caller’s per deck level (All comments, Comments for you, None), the email switch and, for the owner, whether commenters read the Activity panel; a read when every field is absent. Input: level?, email?, activityForCommenters?. CLI: turboslide notifications settings --level <level> --email --activity-for-commenters. MCP: deck_notification_settings.
- activity.list (Activity; read; cli, mcp, http, window; GS3): The merged activity feed of the deck: version windows, comment events, share events, requests, role changes, renames, restores, named versions, exports, imports and trash; editors and the owner, commenters when allowed. Input: since?, kinds?, limit?. CLI: turboslide activity --since <since> --kind <kinds>. MCP: deck_list_activity.
- version.diff (Show changes; read; cli, mcp, http, window; GS3): The mutations between two revisions grouped by touched block and by author, the data behind Show changes; from omitted with staged is the last named version, to defaults to the current revision. Input: from?, to?, staged?. CLI: turboslide version diff <from> <to> --staged. MCP: deck_version_diff.
- share.get (Sharing; read; cli, mcp, http, window; GS3): The access record with its tokens hashed and the dead links of the last thirty days, plus the caller’s role, how it was reached and the capabilities it gives; a viewer receives the owner, the general access mode, their own grant and their capabilities only. Input: id. CLI: turboslide share get <id>. MCP: deck_get_share.
- share.setGeneralAccess (General access; mutates; cli, mcp, http, window; GS3): Sets Restricted or Anyone with the link with its role; the link mode mints a token and answers its /s/ URL once. Input: id, mode, role?, baseRevision. CLI: turboslide share access <id> --mode <mode> --role <role>. MCP: deck_set_general_access.
- share.createLink (Create link; mutates; cli, mcp, http, window; GS3): Mints a share link with a role, a label and an optional expiry and answers its /s/ URL once; several links with several roles may live at once. Input: id, role, label?, expiresAt?, baseRevision. CLI: turboslide share link <id> --role <role> --label <label> --expires <expiresAt>. MCP: deck_create_share_link.
- share.revokeLink (Revoke link; mutates; cli, mcp, http, window; GS3): Revokes one link; every session grant that names it dies on its next request. Input: id, linkId, baseRevision. CLI: turboslide share revoke-link <id> <linkId>. MCP: deck_revoke_share_link.
- share.rotateLink (Rotate link; mutates; cli, mcp, http, window; GS3): Mints a new token for a link and answers its URL once; the old token dies. Input: id, linkId, baseRevision. CLI: turboslide share rotate-link <id> <linkId>. MCP: deck_rotate_share_link.
- share.stop (Stop sharing; mutates; cli, mcp, http, window; GS3): Sets Restricted and revokes every link. Input: id, baseRevision. CLI: turboslide share stop <id>. MCP: deck_stop_sharing.
- share.invite (Share with people; mutates; cli, mcp, http, window; GS3): Writes one grant per address with a role and an optional message and sends the invitations unless told not to; never says whether an account exists, and answers sent, queued or no-mail per address with the deck link for hand delivery. Input: id, emails, role, message?, notify?, baseRevision. CLI: turboslide share invite <id> --email <emails> --role <role> --message <message> --no-notify. MCP: deck_invite.
- share.setRole (Change role; mutates; cli, mcp, http, window; GS3): Changes a grant’s role; the owner’s row is refused. Input: id, who, role, baseRevision. CLI: turboslide share role <id> --email <email> --role <role>. MCP: deck_set_role.
- share.remove (Remove access; mutates; cli, mcp, http, window; GS3): Removes a grant; the caller may remove their own at any role. Input: id, who, baseRevision. CLI: turboslide share remove <id> --email <email>. MCP: deck_remove_access.
- share.setExpiry (Access expiry; mutates; cli, mcp, http, window; GS3): Sets or clears a grant’s expiry, up to one year ahead. Input: id, who, expiresAt, baseRevision. CLI: turboslide share expire <id> --email <email> --at <expiresAt>. MCP: deck_set_access_expiry.
- share.settings (Sharing settings; mutates; cli, mcp, http, window; GS3): The gear’s five switches: editors can share, viewers can download, viewers can see comments, show names to people with the link, allow embedded HTML blocks; owner only, with a re-authentication within 10 minutes on the window transport. Input: id, editorsCanShare?, viewersCanDownload?, viewersCanSeeComments?, showNamesToLinkVisitors?, allowHtmlBlocks?, baseRevision. CLI: turboslide share settings <id> --editors-can-share <editorsCanShare> --viewers-can-download <viewersCanDownload> --viewers-can-see-comments <viewersCanSeeComments> --show-names-to-link-visitors <showNamesToLinkVisitors> --allow-html-blocks <allowHtmlBlocks>. MCP: deck_share_settings.
- share.requestAccess (Request access; mutates; mcp, http, window; GS3): Asks the owner for a role with an optional message; always answers the same sentence, so a caller learns nothing about whether the deck exists; rate limited per IP, and an anonymous caller must give an email. Input: id, role, message?, email?. MCP: deck_request_access.
- share.listRequests (Access requests; read; cli, mcp, http, window; GS3): The pending requests with the requester’s label, email, role, message and time. Input: id. CLI: turboslide share requests <id>. MCP: deck_list_access_requests.
- share.respond (Respond to request; mutates; cli, mcp, http, window; GS3): Approves a request as a role or declines it with null; the requester is told when asked. Input: id, requestId, grant, notify?, baseRevision. CLI: turboslide share respond <id> <requestId> --grant <grant> --notify. MCP: deck_respond_access_request.
- share.transferOwnership (Transfer ownership; mutates; cli, mcp, http, window; GS3): Offers the deck to a signed in principal or an email, who accepts or declines; owner only, anonymous targets refused. Input: id, to, baseRevision. CLI: turboslide share transfer <id> --email <email>. MCP: deck_transfer_ownership.
- share.acceptOwnership (Accept ownership; mutates; cli, mcp, http, window; GS3): The pending owner takes the deck; the previous owner becomes an editor. Input: id, baseRevision. CLI: turboslide share accept-ownership <id>. MCP: deck_accept_ownership.
- share.declineOwnership (Decline ownership; mutates; cli, mcp, http, window; GS3): The pending owner declines the offer. Input: id, baseRevision. CLI: turboslide share decline-ownership <id>. MCP: deck_decline_ownership.
- share.claim (Claim; mutates; cli, mcp, http, window; GS3): A signed in principal claims an unowned deck (an admin any unowned deck); the deck then reads Anyone with the address can view, so every view link and the embed keep working while editing by address stops. Input: id, baseRevision. CLI: turboslide share claim <id>. MCP: deck_claim.
- share.emailCollaborators (Email collaborators; mutates; cli, mcp, http, window; GS3): Sends a message to every collaborator or the named ones; declared this round and answers not implemented until round four. Input: id, to, message, baseRevision. CLI: turboslide share email <id> --message <message>. MCP: deck_email_collaborators.
- deck.publish (Publish to the web; mutates; cli, mcp, http, window; GS3): Mints the published player’s token and answers the player and embed URLs once; every edit is published, and the player never opens the editor. Input: id, baseRevision. CLI: turboslide deck publish <id>. MCP: deck_publish.
- deck.unpublish (Stop publishing; mutates; cli, mcp, http, window; GS3): Revokes the published token; the player and the embed answer 410. Input: id, baseRevision. CLI: turboslide deck unpublish <id>. MCP: deck_unpublish.
- account.decks (My presentations; read; cli, mcp, http, window; GS3): The caller’s deck index with the owner and the caller’s role per row: owned, shared with me, recent, trash, or all for an admin; on a checkout every deck as owned. Input: view. CLI: turboslide account decks --view <view>. MCP: deck_list_my_decks.
- account.tokens.create (Create API key; mutates; window, cli, http; GS3): Mints an API key owned by the caller with a name, scopes and an optional expiry, shown once; never over MCP, and refused on a checkout, which has no accounts. Input: name, scopes, expiresAt?. CLI: turboslide account tokens create --to <to> --name <name> --scope <scopes> --expires <expiresAt>.
- account.tokens.list (API keys; read; cli, mcp, http, window; GS3): The caller’s API keys without their secrets. Input: none. CLI: turboslide account tokens list --to <to>. MCP: deck_list_tokens.
- account.tokens.revoke (Revoke API key; mutates; cli, mcp, http, window; GS3): Revokes one of the caller’s keys; its MCP sessions close. Input: tokenId. CLI: turboslide account tokens revoke <tokenId> --to <to>. MCP: deck_revoke_token.
- account.me (Account; read; cli, mcp, http, window; GS3): The caller’s principal, trust state, label, mark, avatar choice and sessions; the CLI can write the mark as a PNG. Input: avatarPng?. CLI: turboslide account me --avatar-png <avatarPng>. MCP: deck_get_me.
- account.setName (Change name; mutates; cli, mcp, http, window; GS3): Sets the display name under the rules of SPEC-3 0.19 (NFC, 1 to 40 code points with a letter or digit, no bidi controls, the reserved and confusable lists, unique per deck by skeleton); the refusal sentences are fixed. Input: name. CLI: turboslide account name <name>. MCP: deck_set_my_name.
- account.setAvatar (Change avatar; mutates; cli, mcp, http, window; GS3): Chooses the mark: initials, a glyph field, a dither plate, or a picture for signed in principals only. Input: variant, initials?, salt?, picture?. CLI: turboslide account avatar --variant <variant> --another. MCP: deck_set_my_avatar.
- account.sessions (Sessions; read; cli, mcp, http, window; GS3): The caller’s sign in sessions, the current one marked. Input: none. CLI: turboslide account sessions --to <to>. MCP: deck_list_sessions.
- account.signOut (Sign out; mutates; cli, mcp, http, window; GS3): Ends one sign in session, or every other one with all. Input: sessionId?, all?. CLI: turboslide account sign-out --all --to <to>. MCP: deck_sign_out.
- account.forget (Forget this browser; mutates; window; GS3): Replaces the anonymous cookie with a fresh id and clears the browser’s mirrors; window only. Input: none.
- admin.bootstrap (Bootstrap admin; mutates; cli, http; GS3): With the static bearer, gives an email the admin role and mints the first API key, shown once. Input: email. CLI: turboslide admin bootstrap --to <to> --email <email>.
- admin.assignOwner (Assign owner; mutates; cli, mcp, http, window; GS3): An admin sets a deck’s owner. Input: id, to, baseRevision. CLI: turboslide admin assign-owner <id> --email <email>. MCP: deck_assign_owner.
- admin.flag (Kill switch; mutates; cli, mcp, http, window; GS3): Reads or flips one of the kill switches (realtime, presence, comments, invites, email, exports, uploads, renderThumbs, materialize, htmlBlocks, signup, readOnly), read within 5 s by every instance. Input: name, on?. CLI: turboslide admin flag <name> --on <on>. MCP: deck_admin_flag.
- admin.migrateStorage (Migrate storage; mutates; cli, http; GS3): Runs one step of the storage layout v2 migration (plan, copy, verify, cutover, delete or rollback) from its cursor, in batches. Input: step, batch?. CLI: turboslide admin migrate-storage <step> --to <to> --batch <batch>.
- admin.mail.list (Captured mail; read; cli, http; GS3): The mail captured under TURBOSLIDE_MAIL=capture, newest first; absent otherwise. Input: since?, limit?. CLI: turboslide admin mail --to <to> --since <since>.
- picture.dither (Dither; mutates; cli, mcp, http, window; GS3): Writes the deck’s two tone screen over a picture’s continuous source as a non destructive field, or removes it with null; a slide that is not a canvas converts first when the picture is positioned; answers the variant key, the metrics and any warnings. Input: slideId, blockId, dither, baseRevision. CLI: turboslide block dither <slideId>#<blockId> --pattern <pattern> --tone <tone> --cell <cell> --strength <strength> --black <black> --white <white> --gamma <gamma> --polarity <polarity> --off. MCP: deck_dither_picture.
- picture.materialize (Materialize dithers; mutates; cli, mcp, http, window; GS3): Writes the variant files and records of every dithered picture on the named slides, prunes the variants no picture references when asked, or names the missing variants without writing under dryRun; server side. Input: slideIds?, blockIds?, prune?, scale?, dryRun?, baseRevision. CLI: turboslide picture materialize <slideIds> --prune --scale <scale> --dry-run. MCP: deck_materialize_pictures.
- slide.setBackgroundPicture (Background picture; mutates; cli, mcp, http, window; GS3): One write for a covering picture object at the back of each named slide with an optional dither, replacing the covering picture already there unless told not to; the file, url and upload forms run asset.add server side first. Input: slideIds, assetId?, file?, url?, upload?, alt?, dither?, replace?, baseRevision. CLI: turboslide slide background-picture <slideIds> --asset <assetId> --file <file> --url <url> --alt <alt> --dither --no-replace. MCP: deck_set_slide_background_picture.
- slide.setBackgroundMaterial (Background material; mutates; cli, mcp, http, window; GS3): Captures a shader material at an anchor and places the frozen frame as a covering picture object at the back of each named slide with an optional dither, in one call; server side. Input: slideIds, materialId, preset?, uniforms?, anchor?, dither?, baseRevision. CLI: turboslide slide background-material <slideIds> <materialId> --preset <preset> --anchor <anchor> --dither. MCP: deck_set_slide_background_material.

## Lint rules

Severity 3 must fix (the gate), 2 should fix, 1 polish; `fix` marks the rules whose findings carry mechanical fix mutations that `turboslide fix` applies.

- sheet/overflow (rendered, severity 3): any data-block box outside 0..1600 by 0..900
- sheet/rail-touch (rendered, severity 2): a text box within 8 px of a rail or rule
- sheet/thumb-legible (rendered, severity 1): text whose rendered size (font size times the diagram scale) is under 2 px at the grid’s 0.14x
- type/floor-15 (rendered, severity 3): computed font size under 15 px on any text node
- type/svg-label-min (both, severity 3): diagram text under 18 px, or 18 px without sm
- type/weight-cap (both, severity 3, fix): display weight above 500 outside spec
- type/face (rendered, severity 3): a font status of fallback for Inter
- type/sizes-ladder (static, severity 2, fix): a heading or paragraph size outside the ladder
- type/ladder (static, severity 2, fix): a typography size on a heading, paragraph, text or box block outside the type ladder
- text/overflow (rendered, severity 2, fix): text that leaves its box on a positioned block under autofit none or absent, or when shrink reached the ladder floor; the fix writes the step down or the box height (gslides-parity SPEC-2 2.1.5)
- color/tokens-only (static, severity 3): a color literal in html CSS or raw svg outside the sanctioned exceptions (swatch, panel, logoPlates)
- color/semantic-icons-only (static, severity 3): a semantic hue on anything but an Icon; on a text run the finding is severity 1 (gslides-parity SPEC-2 0.6)
- color/off-palette (static, severity 2): a custom hex color on a primitive block instead of a palette token, named with the block
- icon/placement (static, severity 3): an icon outside a rows key, a plain row start, a board state or a dia; size not 20 or 24
- icon/known (static, severity 3): an IconName not in the sprite
- rows/two-lines (rendered, severity 2): a row value or a table cell taller than two line heights
- rows/key-snap (static, severity 1, fix): a key width outside the snap set
- dia/label-clearance (both, severity 2): a label box within 12 px of a stroke
- dia/stroke-grammar (static, severity 2): stroke not 1 or 1.5, round caps or joins, filled arrowheads over 8 px, fill outside ink, paper, plate
- dia/fit-slot (static, severity 1, fix): a viewBox width that does not equal its slot width
- dia/half-pixel (static, severity 1, fix): an odd-width stroke on an integer coordinate
- asset/twin-or-border (static, severity 3, fix): a shot or pair image with a neutral twin and no border
- asset/credit-on-plate (static, severity 3): a share-alike source whose credit is missing from the plate
- asset/license-missing (static, severity 2): a photograph with no license record
- asset/stretched (rendered, severity 2): an image drawn at an aspect other than its file’s, outside crop
- picture/plate-clear (static, severity 2): lit cells inside the plate rectangle or within 30 px of it
- picture/blank-twin (static, severity 3): a two-tone twin with lit fraction under 0.02 or over 0.98
- picture/mood-placement (static, severity 2): a mood slide directly before an opener, or two mood slides adjacent
- opener/sentence-lists-section (static, severity 1): the opener sentence does not mention each family in the section
- lines/law (rendered, severity 2): doubled hairlines within 4 px, junctions drawn twice, a border color outside the three roles
- copy/heading-period (static, severity 3, fix): a heading, plate title or key ending in a period
- copy/sentence-case (static, severity 2, fix): a heading with Title Case outside the proper-noun and token lists
- copy/token-first (static, severity 2): a heading starting with a product token
- copy/heading-is-name (static, severity 2): a heading containing a domain
- copy/no-em-dash (static, severity 3, fix): an em dash present
- copy/no-exclamation (static, severity 3, fix): an exclamation mark present
- copy/no-eyebrow (static, severity 2): a short cap or all-caps line as the first block above a heading
- copy/full-sentence-caption (static, severity 1, fix): a caption without a terminal period
- copy/contrast-pair (static, severity 1): "not" followed by a comma clause, for the copy judge
- copy/metaphor-candidate (static, severity 1): a word list hit (journey, unlock, supercharge)
- copy/empty-placeholder (static, severity 1): an empty Text where a layout placeholder waits for copy: a title, a subtitle, a paragraph, a text box, a list item, a table cell or a caption
- table/size (static, severity 3): a table over 20 by 20 cells, or a row without one cell per column
- chart/size (static, severity 2): more than 8 categories in a chart narrower than 960 px, or a pie with more than 8 slices (gslides-parity SPEC-2 0.60)
- contrast/both-themes (rendered, severity 2): sampled text against ground under 4.5:1 (3:1 at 26 px and up) in either theme
- layout/empty-half (rendered, severity 2): ink coverage under 2 percent in one column of a cols slide while the other exceeds 10
- layout/pair-gaps (rendered, severity 2): unequal gaps in a pair or details grid beyond 2 px
- layout/columns-aligned (rendered, severity 2): tops of side-by-side rows blocks differing by more than 1 px
- layout/freeform (static, severity 1): a slide arranged by hand on the canvas; Apply layout re-flows it (gslides-parity SPEC-2 1.4)
- freeform/overlap (static, severity 1): two objects that carry text placed over each other; a picture or a textless box under a text is the design (gslides-parity SPEC-2 1.4)
- freeform/off-sheet (static, severity 3): an object whose rotated bounding box lies wholly outside the 1600 by 900 sheet; one that crosses an edge is severity 2 (gslides-parity SPEC-2 0.96)
- scales/marker-equals-value (static, severity 3): a marker authored rather than derived; only possible in imported html
- escape/html-block (static, severity 2): an html block exists; export is raster for it
- html/sanitize (static, severity 2, fix): an html block without the htmlSanitized stamp: markup and CSS the parser and the tokenizer have not passed; the fix writes the cleaned strings and the stamp, by hand, never inside migrate
- export/non-native (static, severity 1): a block that exports as raster in the requested mode, listed per export
- numbers/contradiction (static, severity 1): the same noun with different numerals across slides, for the accuracy judge
- count/hard-coded (static, severity 3): a numeral equal to the slide count inside copy that should derive from the deck
