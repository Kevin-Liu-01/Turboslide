# Turboslide focus round, build status

Kept by the integrator. One section per merge: what landed, what was cut, the open requests with where they went, and the deviations recorded. Section numbers refer to `docs/FOCUS.md` unless prefixed SPEC, SPEC-2, SPEC-3 or SPEC-4. Started on day 0, 2026-09-15, over `main` at `2b12e31` (`BASE`); nothing is committed until the ship step. Full notes: `docs/gslides-parity/focus/build/integrator.md` (day 0: the citation map, the seam, the matrix module, the ownership table, the requests protocol, the ports, the gates) and the lanes' notes `b1.md` to `b6.md` as they land. Kevin's directive of the round, verbatim: "i think there are a bunch of features that donnt work like shapes. we can get rid of those for now and focus on teh features we need, that work, and that work well with every interaction tested." The rule it sets (section 1): a feature is in the default view only when every one of its interactions works on production and is in the test matrix.

## Day 0

The tree: `main` at `2b12e31`, clean apart from the untracked `.github/` and the nine mockup PNGs under `design-4/*/previews/`, none touched. Every code file equals the production commit `ec61c6b` (`git diff --stat ec61c6b 2b12e31 -- packages apps scripts` is empty), so FOCUS.md's line citations held on this tree before the day 0 edits; `integrator.md` section 2 maps the lines the edits moved. `packages/chrome/src/tokens.css` is byte identical to `BASE`. Round five stays on `round-five/integration`, untouched.

| Item                        | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `BASE` named                | `2b12e31`; production serves `main`; the audits and FOCUS.md read `ec61c6b`, whose code is byte identical                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| The switch (3.1)            | `MenuSetting` `'advancedTools'`; `DEFAULT_SETTINGS.advancedTools: false`; `STORED_SETTINGS` carries it under `ts-editor-settings`; the row `tools.advancedTools` "Advanced tools", `turboslide: true`, a check row after `tools.checkSlides` and before `tools.advanced`, with a clean tooltip sentence; the flag `advanced?: true` on `MenuItem`, `ToolbarControl`, the object form of `ContextEntry` and `PaletteEntry`; `isPresent` is the one predicate (a flagged row and a Later row are absent while the switch is off); `advancedToolsOn`, `presentControls`, `resolveContextEntries` and the `Presentable` type exported from `menus/model.ts` (`integrator.md` section 3)                                                                                           |
| The surfaces                | honour it today: the menu bar (`visibleMenus` drops an empty menu, `visibleItems` drops an emptied plain container), the rows and submenus, the right click menus (the entry level flag too), the title row, the toolbar head, every tail and the tail end (`presentControls`), Search the menus (`finderRows`), the palette (`presentPaletteEntries` over `PaletteContext.advancedTools`), the chord dispatcher (`useEditorKeys.ts`). Left to `b3`: the shortcuts dialog, the bottom bar's view buttons, the palette Insert group's row mapping, and every flag of 3.2 to 3.4 (none is set on day 0)                                                                                                                                                                         |
| The window API              | `EditorShellInput.onSettingsChange` from the shell; `EditorRoot` passes the settings to `controller.setShellSettings`; `describe().state.settings` reports them, `advancedTools` among them; `data-advanced-tools` on `.pt-viewer` while on                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| The matrix module (6)       | `scripts/probes/core-matrix.mjs` reads `docs/gslides-parity/focus/core-matrix.json` in place (no copy), validates every row on load and exports the 390 ids, the features, the drivers, the lookups and the two ship helpers of 6.2 (`parkedFeaturesOf`, `shipVerdict`); `core-matrix.d.mts` for TypeScript; `core-matrix.test.mjs` in the root vitest project `scripts`; `apps/studio/e2e/core/matrix.ts` (`rowsOfSpec`, `coreTitle`, `idOfTitle`) is the typed doorway of the core specs and is typechecked by `tsc -b`. Run directly the module prints the counts of 6.3, equal to the rendered document                                                                                                                                                                   |
| Tests                       | 14 chrome tests asserted a Later row in the default view or a new row uncovered; every one updated to assert it behind the switch, none deleted, and eleven new tests added across `menu-model`, `ContextMenu`, `sidebar-filmstrip`, `editor-shell-render`, `tool-finder`, `menu-component`, `editor-shell`, `palette-data` and `core-matrix.test.mjs`; `default-view-words.test.ts` no longer exempts `tools.advancedTools` (a tightening)                                                                                                                                                                                                                                                                                                                                   |
| Ownership                   | `integrator.md` section 5: the six lanes (`b1` text and slides, `b2` objects with the shapes and lines of section 4, `b3` surface, `b4` drivers, `b5` server, `b6` verifier) with the files each owns as read from FOCUS.md sections 4, 5 and 5.1, the day 0 files handed to `b3`, `b4` and `b5` from day 1, and six reassignments where FOCUS.md names one file for two owners (`Editor.tsx` to `b2`, `controller.tsx` to `b5`, `model.ts` to `b3`, the `focus` predicate to `b3` with the rule `b1`'s, the print and access routes to `b3` with `server/decks.ts` to `b5`, `Share.tsx` to `b3` with the record to `b5`). The lane prompts' Owns lists are authoritative; each lane writes its list at the head of its note on day 1 and the table is corrected against them |
| Requests protocol and ports | `integrator.md` sections 6 and 7: numbered requests under "Requests" in `build/<key>.md`, applied or reassigned by the integrator at a merge; the ports 4361 to 4366 for `b1` to `b6`, 4367 the integrator's, 4368 spare, 4321 the check runner's                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Gates                       | `node_modules/.bin/tsc -b` exit 0; chrome 54 files 510 passed; schema 24 files 503 passed; cli 21 files 131 passed 1 skipped; the `scripts` project 11 passed; `node scripts/check.mjs --only 1,2` ok in 1.7 s; `--only 4,5,6` failed once at step 5 on six `apps/cli` child process timeouts at 5000 ms with the machine's load at 16 from other workflows' servers, and passed whole on the rerun in 94.3 s (step 5: 287 files, 3048 passed, 2 skipped, 81.8 s; step 6: the build, the bundle check at 579,024 bytes under the 600,000 ceiling, the greps, 12.1 s); the default view words test passes in the chrome suite; prettier over the 23 files touched                                                                                                              |
| Not run on day 0            | check steps 17 to 31 (step 20's parity audit expects the Later rows drawn disabled and moves its Later checks behind the switch before merge 1, `integrator.md` I1; step 18's chrome lint and the tooltip audit, I2); no Playwright, no dev server, no deploy                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Deviations recorded         | `integrator.md` section 10: the Later stubs hide with the switch off through `isPresent` (3.1's decision, above the parity rounds' disabled stub rule); `visibleMenus` and `visibleItems` drop an emptied menu or plain container only with a context; the `ContextEntry` object form's `when` is optional; the window API reports the setting as a read on `state`, no action added; the matrix module reads the docs JSON in place; the default view changes on day 0 by the Later stubs and the new Tools row alone                                                                                                                                                                                                                                                        |

Open for merge 1 (`integrator.md` section 9): I1 the parity audit's Later pass behind the switch, I2 the tooltip audit's two passes, I3 AGENTS.md's focus round sections, I4 one `settings.advancedTools` assertion in a spec if `b4` or `b5` want it beside `core/surface.spec.ts`; for the lanes, the flags of 3.2 to 3.4 and the three remaining surfaces (`b3`), the probe's `--core` mode, the seven core specs, the manual checklist and the ship JSON (`b4`), enforce mode and the access record of ruling (2) (`b5`).

## Merge 1 (2026-09-16)

The seven lanes' work merged on the shared checkout over `main` at `2b12e31`, uncommitted, with every request of `b1.md` to `b7.md` answered (`integrator.md` "Merge 1", one row per request: applied, landed by another lane, or a finding with the owner named). The preview built from this tree with `TURBOSLIDE_AUTHORIZE=enforce` for that deployment alone is `https://turboslide-7o1m3892o-kl01s-projects.vercel.app` (a first preview, `https://turboslide-2weam880k-kl01s-projects.vercel.app`, carried the tree before the merge's own fixes below). Production still serves `main` at `2b12e31` in shadow mode; nothing moved there.

| Item                                                                                                | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The lanes                                                                                           | b1 the surface (the flags of 3.2 to 3.4, the named shape rows and the parked plate, the split list buttons, the toolbar focus rule, the parity audit's expectation table), b2 text and slides (links on the range, the session across the toolbar and the plates, autofit grow, the burst reconcile, the empty selection Delete, the layout carry, Find and replace), b3 objects (the three shape paths, the fresh shape's look, the picture against its box, asset.add against the head, align on the extreme object, the clipboard predicate, the sheet centre snap), b4 the drivers (the probe's `--core` walk of 289 rows in eleven areas, the seven core specs, `core-gate.mjs`, check step 32, the manual checklist), b5 documents (the README rendered from the matrix, the /home copy, `docs/updates.md`, the shoot script), b6 access and sharing (enforce mode with the restricted default and the role links, the rebuilt Share dialog, the comment anchor belt, the access paint), b7 the server (the record as a claim before the commit, one head per deck, the admission against the client's base, the outbox rebase, the draft's first picture, the comment anchor fallback)                                                                                                                                                                                                                                                                                                                                                           |
| Requests                                                                                            | every request applied or answered at this merge (`integrator.md` "Merge 1"); the two not applied and why: b2 R9 (Escape on every dialog, optional) and b6 R12 (no view transition into You need access; the paint is fixed by b6's ground and the row waits out the transition)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Found and fixed at the merge                                                                        | a caller with no identity was refused before an open deck's general access was read, so the enforce preview answered You need access to the first visit of `/deck/gt-brand` and the deck on the reload (`packages/identity/src/access.ts` `decide()` admits the read as the general access role; `access.test.ts`); the toolbar Select button opened an empty list instead of returning the tool (`ToolbarTail.tsx`); the chrome's zoom ladder stepped from the nearest rung, 71 to 125 (`editor-shell.ts` `zoomStepFrom`, now the next rung: 75); the toolbar Shape button lists the three named rows directly while the galleries are parked (`ToolbarTail.tsx`); a list block keeps the text tail and the Format > Text rows (`editor-shell.ts`); `hosted-smoke.mjs` reads enforce mode's 401 for a caller with no identity and the data only edit route's 404                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| The matrix                                                                                          | `core-matrix.json`: the three share copy rows name the `/s/<token>` link and its role; `slides.delete.canvas-focus-undo` removes from the focused card (rank 4 makes the canvas Delete a no op); two manual rows under ruling (3), `export.print.print-button` and `text.clipboard.paste-without-formatting`, with their steps in `manual-checklist.md`; `core-matrix.mjs` gains `manual` and `isManualRow`, and a manual row left not driven parks nothing and fails no ship while a failed one still does; FOCUS.md re-rendered; the README section re-rendered (`what-works.mjs --check` current) and its test pins the manual rule                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Contracts and generated files                                                                       | `pnpm generate:contracts` current (the align doc, the skills' advanced sentence); `facts.json` at 32 check steps and `facts-data.ts` without the shape count (`build-brand.ts --facts`, `build-home-assets.ts`); check step 3 fails on this uncommitted tree by design (the regenerated references differ from the committed ones) and passes once the tree is committed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Gates on the tree                                                                                   | `node_modules/.bin/tsc -b` exit 0; the root vitest 305 files, 3263 passed, 2 skipped (and after the server edits: realtime 89, store 151, studio 276, chrome 560 passed); prettier clean over every touched file; `tokens.css` byte identical to `2b12e31`. The check chain in parts (the runner stops at the first failure; steps 3 and 25 left out): 1, 2, 4, 6 to 18, 22 to 24, 28 to 30 ok; 5 FAIL once under load on `apps/cli/src/commands/canvas.test.ts` (a child process timeout, the ship step's F-check5; the file alone passed right after); 19 FAIL on 74 files committed unformatted before the round and 7 of the verifier's pass 2 evidence JSON (F-format; the README check is current); 20 FAIL on 8 audit checks (the audit's own click helper and table tail expectation under A1 and the parking, the parked guides, the second context's Tools menu, the outline titles; F-step20, b1); 21 FAIL on 13 of 66 editor spec tests (three pre-existing F-step21 rows, eight older specs whose entry or expectation predates A1, the PDF dialog's removed notes box, the landing spec's `poll` transport read); 26 FAIL on 3 of 42 (the inbox plate asserted in the default view, the realtime undo row's text, Show changes parked); 27 skip; 31 FAIL on the perf budget's `js decoded` and `dom nodes` ceilings on every route (no verdict on this step in pass 1 or 2; measured next against `main`); 32 FAIL as the 4378 gate did (357 passed, 39 failed, 1 not driven; `integrator.md` "The check chain" has every row and reason) |
| The core gate on the dev server (4367, tmp store, memory channel; `core-gate` took the lock itself) | the probe's core walk: 289 rows, 235 passed, 53 failed, 1 not driven (the manual paste row), 0 no step, 1278 s; the seven specs were not run by the merge 1 integrator against 4367 (the cell stood as a placeholder until the cycle 2 merge, VERIFICATION.md pass 1 F17); the verifier's pass 1 and pass 2 ledgers (`VERIFICATION.md` section 3) are the record of the specs on this tree. The run is a smoke of the merge, never a run of 6.2: the failed rows are classified below and the ship's evidence is the preview and production                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| The preview                                                                                         | `hosted-smoke.mjs --token-env TURBOSLIDE_TOKEN`: 34 of 40 on the first preview (the six rows the identity fix and the smoke's enforce reading above answer), 40 of 40 on the second; the core gate against the second preview was not run by the merge 1 integrator (the same placeholder); the verifier's pass 1 run against that preview and pass 2's against the fresh enforce preview (`VERIFICATION.md` section 3, `verification/pass2-preview-merged.json`: 390 rows, 329 passed, 60 failed, 1 not driven) are the record                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

### The failed rows of the dev server run, classified

Rendered from `core-walk.json` of the run (the scratchpad's `int/gate-4367/`); a row is either fixed at this merge (rerun on the preview), a driver fault handed to b4, or a finding for the verifier with the owner named. A row not reproduced on the preview or on production is a dev server or driver state observation, never a product verdict.

- Fixed at this merge, judged on the preview run: `arrange.zoom.menu-in` and its cascade `arrange.readout.200` (the ladder), `arrange.toolbar.select` (the Select button), `shapes.insert.named-rows` (the Shape button's rows, and the driver clicks the button when no split arrow exists), `text.list.chords`, `text.format-menu.rows-enabled`, `text.indent.toolbar`, `text.clear-formatting`, `text.format-menu.text-fitting` and the Format menu size, spacing and align rows (a list block is text; the driver turned box A into a list before them), `images.crop.menu-escape` (the driver read its revision before the previous redo settled; the toolbar Escape row proved the cancel writes nothing).
- Not reproduced: `slides.duplicate.context` and `slides.duplicate.two-selected-menu` (a repro on production and on the preview from a fresh deck copied the right click's card and both selected cards; on the dev server run the walk's earlier state held), `text.title.double-click` and the sixteen title, subtitle and caret rows behind it (the title slide reached the text area without its heading and lead runs; the repro's double click on a fresh deck opened the session on production and the preview), `text.persistence.reload` (their cascade), `decks.title.rename-blur` (the sheet's rectangle read null once after the rename field opened).
- Driver faults handed to b4 (b3.md section 7.1 named them): `images.present.picture-and-ground` (the selector now reads `img.shot`), `shapes.default-look` (the ground read from the transparent `.pt-slide`), `shapes.border.colour-weight-dash`, `lines.tail.colour-weight-dash-ends` (popover ids time out), `lines.context.line` (the field is `lineEnd`), `shapes.reload-and-viewer` (the viewer's fill read), `arrange.order.*` and `arrange.readout.200` (the objects the driver places and the overlay reads; b4.md section 7 item 2), `images.options.transparency` and `images.background.hex-field` (the slider drag and the hex field read; both failed the same way on production with b4's driver while the audit passed them by hand; owner b1 for the controls if the driver is right), `versions.name-current` (a locator timeout), `export.pptx.dialog` (the checkbox read), `export.print.include-skipped` (pages 9 to 9).
- Findings for the verifier: `text.autofit.textbox-grow` (pos.h 140 to 140 with 132 css px of content in a 99 px frame; b2's fix passed on b2's server; owner b2), `arrange.clipboard.paste-after-new-slide-button` (two objects landed on the new slide where one was copied; the stage's paste event handler and the payload are b3's; the row was broken on production), `versions.undo-restore` (the restore changed nothing on the memory tier; owner b7).

## Cycle 2 (2026-09-16 to 17)

The second cycle's lanes (b1 the surface, b2 text and slides, b3 objects, b4 the drivers, b6 access and sharing, b7 the server; b5 did not run) merged on the shared checkout over the first cycle's tree (`focus/cycle-1` at `4a87554`, never checked out; `main` at `b50e38f`), uncommitted, with every Cycle 2 request and every standing fix round request answered (`integrator.md` "Cycle 2 merge": applied, applied as the smallest edit in a lane's file with the note, landed by another lane, deferred with the reason, or a finding with the owner named). The binding specification is `docs/FOCUS.md` with `AMENDMENTS.md` A1 above it. Production still serves `main` at `b50e38f` in shadow mode; nothing moved there. The cycle's enforce preview built from the final tree is `https://turboslide-fqpj83rq5-kl01s-projects.vercel.app` (a first preview from the tree before the stalled store edits, `https://turboslide-na1km157s-kl01s-projects.vercel.app`, is recorded with its stopped run).

| Item                                                                                        | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The click model A1                                                                          | on the tree from b2 (`clickEntry`, `entryCaret`, `typingEntry`, the rewritten `onDoubleClick` and `onKey`), b3 (`objectPressPlan`, the `user-select: none` canvas rule) and b4 (the seven new rows, the rewritten one click rows, the walk's double click entry). Every A1 row passed on the integrator's 4378 rerun once the driver's prompt dismissal stopped moving the stage (`text.title.single-click`: move handle, eight resize handles, rotate, ring, chip, no session, no caret; `text.selected.typing-replaces`, `.enter-appends`, `text.title.double-click-enters`, `text.session.escape-twice`, `text.textbox.drag-inside-moves`, `text.context.inside-session`, `arrange.select.click`, `arrange.multi.drag-inside-moves-all`), and the title battery passed on the enforce preview                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Shapes and lines                                                                            | parked whole under ruling (1) (b1's cycle 2: Insert > Shape, Insert > Line, Format > Borders & lines and the two toolbar buttons behind the switch); FOCUS.md 3.2, 3.3 and 4, AGENTS.md, the README, `skills/turboslide-api/SKILL.md` and `docs/updates.md` say so; the parked list is `verification/parked-cycle2.json` (`shapes`, `lines`; the ship copies it to `ship-<commit>.json`); the walk drives their rows with the switch on                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| The merge's own fixes                                                                       | the Order rows for a box without a `pos.z` (`editor-shell.ts`, rank in the paint order), the zoom step from Fit (the stage's live scale through the handle's `scale()`), the hex field's Enter (writes and closes), the list size plan (`/size`), the download secret per process (b6 FR1), no cached null on the blob tier (b7 C2-R13), the room routes' link grants (b6 R1), the viewer and print payloads reading the room's live document (`getDeck` behind the checkpointer by up to 10 s), Escape with a dialog open (b1 R30), the older specs' double click entry (13 sites), `objects.spec.ts`'s Shapes tile, `editor.spec.ts` and `window-api.spec.ts` behind the switch (b1 R12), `check.mjs`'s `EDITOR_STATES` (b1 R10), and after the first preview run: a 5xx from the room is resent by the client (never a refused change), the ops route answers 503 with `retry-after` on a store deadline, and the store's deadline is judged after a grace turn. The drivers: the walk's prompt dismissal keeps the active slide, the Transparency slider is scrolled into view before the drag (F-transparency's mechanism was the panel's scroll, not the control), the hex field read takes the renderer's `.slide-bg` child, `cannotEdit` races the access page and the editor (b6 R2's addendum). The watcher blind Vite config is committed as `apps/studio/vite.no-watch.config.ts` |
| Contracts and the renders                                                                   | `pnpm generate:contracts` "14 files current"; `docs/FOCUS.md` re-rendered from the 397 row matrix (267 works, 45 broken, 21 flaky, 64 not driven); the README section re-rendered (`--check` current); `docs/hosting.md` "Reads" carries the store's cycle 2 rules                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Gates on the tree                                                                           | `node_modules/.bin/tsc -b` exit 0; the root vitest 305 files, 3263 passed, 2 skipped (and after the server edits: realtime 89, store 151, studio 276, chrome 560 passed); prettier clean over every touched file; `tokens.css` byte identical to `2b12e31`; check steps 1, 2, 4 ok; step 5 FAIL once under load on `apps/cli/src/commands/canvas.test.ts` "converts one slide of every kind" (a child process timeout, the ship step's F-check5; 3264 passed, 1 failed) and the file alone passed right after; the chain continued from step 6 (the row below)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| The core gate on the dev server (4378, tmp store, memory channel, the watcher blind config) | 397 rows: 359 passed, 37 failed, 1 not driven (manual), 0 no step, retries zero, 2010 s (`int2/gate-4378/`); the specs 98 of 101 (`decks.access.paint`, `images.export.pdf-with-picture`, `text.link.present-click`); 21 title rows failed in the driver's state (the state names the active slide before the stage draws it after the slides area's last row) and every one of them passed on the slides and text verification run once the text walk waits for the stage's `data-active` mark (159 rows: 155 passed, 3 failed: the duplicate timing row, the `blank-1` reject, the autofit paragraph); every `share.*`, `collab.*`, `comments.*`, `present.*` and `surface.*` row passed on the memory tier                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| The hosted smoke                                                                            | 40 of 40 on both previews (`--token-env TURBOSLIDE_TOKEN`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| The core gate on the enforce preview (the run of record)                                    | 397 rows: 215 passed, 20 failed, 162 not driven (1 manual), 0 no step; verdict failed; retries zero; 2224 s (`verification/gate-preview-cycle2.json`). `slide.new` through the window API did not answer within 60 s at every area's setup after the title battery, so 161 rows of text, images, arrange, shapes and lines are not driven; the share cluster fails on the mechanism pass 2 read ("the access record is at revision 0, not 1", the copied link's 404); `surface.advanced.remembered` fails (blocking). The features a ship on this run would park: every one but `slides` and `help`. Not a tree that ships (section 6.2); the findings below name the owners                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

### The findings of the cycle 2 runs, by owner

- b7 (the server): the `slide.new` stall on the enforce preview (a window API action that did not answer within 60 s at every setup after the first ten minutes of the walk, on both previews; the first preview's runtime logs carried `BlobTimeoutError` on the ops POSTs' `head decks/<id>/deck.json` beside a burst of fifteen presence POSTs completing inside two seconds, the shape of a stalled instance; the second preview's logs carried no error line while the tab posted no ops and opened no stream for minutes, so the room client stood offline or disconnected with `idle()` waiting; `flush()` returns while the stream is not connected). Severity 3 on the blob tier; the cycle 1 preview drove the same setups. `versions.undo-restore` and `versions.restore` on the preview (the restore changed nothing; the links half of `versions.restore` failed on the share cluster). The `blank-1` reject on the tmp store (the room's live document lacks a slide body its section lists while the disk is consistent at every 300 ms poll; deterministic at the first Format > Text > Size row of the walk; a reload heals it). `present.skipped-left-out` on the preview (the show counted the skipped slide). `export.print.include-skipped` on both tiers (the consequence of the two above). `decks.access.paint` (F-restore-mode, with b1).
- b6 (access and sharing): the share cluster on the enforce preview is the pass 2 mechanism unchanged by the proven read: the second Copy link in a session refused as "the access record is at revision 0, not 1" and "The sharing settings changed since they were read", `share.copy-view-link`'s readback minting a second token, the copied View and Edit links answering 404 to a fresh browser, and every row behind them (`share.view-link-lands-viewer`, `share.edit-link-lands-editor`, `share.view-link-cannot-edit`, `collab.edit-from-second-browser`, `collab.edit-from-owner`, `collab.presence-chips`, `versions.restore`, `comments.reaches-second-browser`); on the memory tier every one passed. Whether `provenGet`'s proof ever passes on the store (the head's version against the body's md5 for `access.json`) is the first question. `comments.toolbar-and-menu-routes` on the preview (4 cards where 1 was expected).
- b4 (the drivers) with b1: `surface.advanced.remembered` (F-remembered: `waitEditor` 90 s after the full navigation with the switch on; the row's 180 s poll did not reach the editor either), `images.background.upload-picture` on the preview (no picture object within 5 s of the upload), `export.print.download-pdf-follows-preview` on the preview (no download within 45 s), `images.export.pdf-with-picture` on the dev server (no download within 30 s), `text.autofit.textbox-grow` (the paragraph landed short of the frame on every run of this merge, so no grow was due).
- b3 (objects): `images.crop.menu-escape` (a write on the menu route's crop or its Escape), the parked shapes and lines rows with the switch on (the viewer's default fill, the Border color plate, the Line color swatch and the end markers, the Line end pick).
- b2 (text and slides): `slides.duplicate.two-selected-menu` on the memory tier once (one copy for two selected), the RangeError page error of b4 C2-R5.
- b1 with b2: `text.link.present-click` and `images.present.picture-and-ground` (FR7, the show draws no link and no picture element).
- The environment: check step 5's child process timeout under load (passes alone), the dev server's PDF export over 30 s, five 502 resource loads during the 4378 walk.

## Cycle 3 (2026-09-17)

The third cycle's lanes (b1 the surface, b4 the drivers, b6 access and sharing, b7 the server; b2, b3 and b5 did not run) merged on the shared checkout over the first two cycles' tree (the checkpoint `focus/wip` at `a85c307`, never checked out; `main` at `b50e38f`), uncommitted, with every Cycle 3 request answered (`integrator.md` "Cycle 3 merge": applied, applied as the smallest edit in a lane's file with the note, landed by another lane, deferred with the reason, or a finding with the owner named). The scope Kevin approved on 2026-09-17: the blob tier findings of `VERIFICATION.md` C2.5 (C2-F24, C2-F27, C2-F28), the flaky rows (C2-F25), the fresh draft's paint (C2-F26), the check server rows (C2-F19, C2-F20), the memory tier rows (C2-F21), `resize.spec.ts` (C2-F23) and the scratch decks (C2-F29); no feature returned to the default view and no surface was built. Production still serves `main` at `b50e38f` in shadow mode; nothing moved there. The cycle's enforce preview built from the final tree is `https://turboslide-edntdmv3u-kl01s-projects.vercel.app` (the fourth deployment of the merge, 19:50Z; the three before it, `turboslide-lx7chu1ss`, `-c6isfmygh` and `-ins42vhmy`, predate one of the merge's two product edits and carry the hosted smoke alone).

| Item                                                                                        | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The blob tier fixes (b7, b6)                                                                | on the tree: every Blob call cancelled underneath at its deadline (10 s reads, 20 s document puts, 90 s twin puts), the hosted head poll at 1 s and one at a time, a version record read through the head, `admitOnBlob`'s resend memory, the room client's lost POST echo match; the shared presence record and the comments index poll of b6 wired into the blob channel; `loadDeck` and `deckRevision` no longer serve the fixture for a missing id (C2-F19, C2-F20 closed: `decks.access.unknown-deck` and `surface.cleanup` pass on the memory tier gate and on the preview); `docs/hosting.md`, AGENTS.md and FOCUS.md rank 20 say so                                                                                                          |
| The drivers (b4)                                                                            | on the tree: the stall's cascade with a fresh deck per area and the row `decks.save.acknowledged`, the paint row `decks.new.ground-paint`, the eight flaky rows' driver faults, `resize.spec.ts` on one browser context (8 of 8 on 4384), the gate's refusal over scratch decks, the standing C2-R18 to C2-R20, R32, C3-R3 and R3 (b) and (c); the matrix at 399 rows                                                                                                                                                                                                                                                                                                                                                                                |
| The surface (b1)                                                                            | on the tree: Close preview runs no route view transition (`export.print.file-menu-after-close` passed on 4388 and on the preview), every menu renders through a portal into `document.body` (`lines.context.line` passed on both), the walk's green swatch click (R39, `shapes.border.colour-weight-dash` passed on both); C2-F11 closed                                                                                                                                                                                                                                                                                                                                                                                                             |
| The merge's own fixes                                                                       | `describe().state.comments.loaded` (b6 R3 a); no route transition on a document's first load (b1 R40, C2-F26); the plan guard for a `plain` list block with its test (b7's standing C2-R16, C2-F1: `text.layout-runs.type` and `text.clear-formatting` clean on the restarted 4388); the copy of Duplicate slide and the slide of New slide selected at the local apply, not two seconds later over the person's next click (C2-F21 `slides.duplicate.two-selected-menu`: "13 -> 15" on 4388 after "13 -> 14" on every memory tier gate since the round began; `select-after-write.ts` with its test); the eight scratch decks and the stray `--json/` folder removed; check step 32's decision recorded; `docs/FOCUS.md` and the README re-rendered |
| Contracts and the renders                                                                   | `pnpm generate:contracts` "14 files current"; `docs/FOCUS.md` from the 399 row matrix (267 works, 45 broken, 23 flaky, 64 not driven); the README section current                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Gates on the tree                                                                           | `tsc -b` exit 0; the root vitest 310 files, 3310 passed, 2 failed under a load average of 16 (both `apps/cli` 5 s timeouts, passing alone: 21 files, 131 tests); chrome 566 and studio 290 after the merge's edits; prettier clean; `tokens.css` byte identical; lint clean in the merge's files                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| The core gate on the dev server (4388, tmp store, memory channel, the watcher blind config) | 399 rows: 393 passed, 5 failed, 1 manual, retries zero, 1712 s, the specs 101 of 101; the five (`slides.duplicate.two-selected-menu`, `text.layout-runs.type`, `images.crop.menu-escape`, `shapes.reload-and-viewer`, `export.print.include-skipped`) are memory tier rows of C2-F21 and C2-F1; the first two pass on the restarted server after the merge's fixes (the slides and text walks), the other three stay with b3 and b7                                                                                                                                                                                                                                                                                                                  |
| The hosted smoke                                                                            | 40 of 40 on each of the four previews                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| The core gate on the enforce preview (the run of record)                                    | `https://turboslide-edntdmv3u-kl01s-projects.vercel.app`: 399 rows: 332 passed, 8 failed, 59 not driven (1 manual), retries zero, 2060 s; the walk's 54 not driven rows (the slides setup refused once as stale) all pass in the partial slides walk five minutes later (90 of 90); the eight failed rows are in the findings, two of them the saving stall (`deck.set`, `block.insert` at 60 s with "Saving…"); the spec rerun's flips are in the ledger. Not a tree that ships (section 6.2)                                                                                                                                                                                                                                                       |
| The spec rerun and the bursts on the preview                                                | the five failed spec files run again on the same deployment: 81 of 85, `decks.access.paint` failed both runs at the same step (a write on a deck just restored from the trash, 60 s on "Saving…"), seven rows flipped to passing and three to failing (flaky under 6.2); b7's burst with three companion pages hung a page load for 90 s in 3 of 3 runs on this preview and 0 of 3 on the cycle 2 preview today (the merge's control), and is clean without companions                                                                                                                                                                                                                                                                               |
| The check chain (`node scripts/check.mjs`, 1 to 32 less 3 and 25)                           | green at 1, 2, 4 to 18, 22 to 24, 28 to 30 (6 after `presence-store.ts` joined the overwrite allowlist); 27 skipped by its rule; red at 19 (the 74 pre round files), 20 (5 standing audit checks, 8 in cycle 2), 21 (12 standing editor spec tests, 13 in cycle 2), 26 (2 standing rows and the new `realtime.spec.ts:515`, a closed tab's queue applied twice), 31 (the perf budget, standing) and 32 (the core gate on the runner's file store: 394 passed, 4 failed memory tier rows, 1 manual; the specs 101 of 101, `decks.access.unknown-deck` and `surface.cleanup` among them: C2-F19 and C2-F20 closed)                                                                                                                                     |
| The scratch                                                                                 | the eight decks of C2-F29, the tmp store's `untitled-20260917-dzun`, the stray `--json/` shots folder and the chain's own `decks/e2e-realtime-mu62gm8c` removed; every deck of this merge's runs trashed, deleted forever and read as 404; `decks/` holds the three tracked folders; both dev servers stopped and the lock free at the return                                                                                                                                                                                                                                                                                                                                                                                                        |

### The findings of the cycle 3 runs, by owner

- b7 (the server), with b6: the blob tier hangs a request for 60 to 90 s under several pages at once on the cycle 3 tree where the cycle 2 tree did not today (`b7-burst.mjs --companions 3`: 3 of 3 runs on this preview met a 90 s page load, 0 of 3 on the cycle 2 preview; clean without companions), and the specs' two 60 s write stalls with "Saving…" (`deck.set` at the same step in both runs, right after a restore from the trash; `block.insert` once) and the owner page without its studio global are that shape (`integrator.md` "Findings from the preview runs of record"). The new cadences per open deck per instance (the 1 s manifest poll, the 2 s presence and comments polls) are the first suspects to bisect with a preview per change. Also: the slides setup refused once as stale after an undo and redo of a delete (the tab's filmstrip listing a slide the server had removed); `realtime.spec.ts:515` on the memory tier (a closed tab's queue applied twice in the other tab after Apply, new since cycle 2's chain, the room client's acknowledgement path); `export.print.include-skipped` on the memory tier (the print payload without the skip the store held); the asset stamp refused per instance in the bursts on both trees (`Asset "…" is not in deck.json (this instance …)`).
- b6 (access and sharing): `collab.presence-chips` failed once before its assertion (the owner page's studio global gone) and passed on the rerun; `comments.reaches-second-browser` passed its one preview run with the index poll; `comments.on-object`, `comments.reply` and `comments.resolve` flipped (the stall took the first run).
- b3 (objects): `images.crop.menu-escape` (a write on the menu route's crop then Escape), `shapes.reload-and-viewer` (the viewer's fill of a shape) and `shapes.fill.colour` (the hex value's undo) on the memory tier gates, standing; `images.insert.upload-while-pending` flipped on the preview; b1's R43 (the plate's shared control id) is an observation for the same lane.
- b4 (the drivers) with b7: the specs' teardown read `/edit/<id>` past its 20 s poll for four scratch decks on the blob tier (every one 404 minutes later); `present.skipped-left-out` flipped on `/decks` not hydrating within 30 s; `share.view-link-lands-viewer` flipped on the second browser's `.pt-viewer` not appearing within 60 s; `versions.restore` failed on the preview as in cycle 2's reruns.
- b1 (the surface): the check chain's standing items (step 20's `toolbar.table` expectation and the tooltip audit, step 31's bundle and DOM ceilings with the integrator), R41 deferred to the chrome's owner.
- b2 (text and slides) with b4, b3: the A1 rewrite backlog of check step 21 (nine editor spec tests), unchanged this cycle.
- Kevin: the agent bearer's rotation; the optional `VERCEL_BLOB_RETRIES=3`; the two manual checklist rows.
- The environment: the two `apps/cli` vitest timeouts under a load average of 16 (passing alone); the 74 pre round unformatted files of step 19; step 25 and 27 not run by the round's rules.

## Cycle 3 fix round, the integrator's finding (2026-09-18)

- C3-F3 (the editor unmounted into the router's page "Something went wrong!" on a thrown store error): fixed in `apps/studio/src/routes/edit.$deckId.tsx` (`shouldReload: false`, `errorComponent: EditRefused`), `router.tsx` (`defaultErrorComponent: PageRefused`), the new `routes/-refused-page.tsx` and `editor/refusal.ts`, `controller.tsx` `onResync` (a store error becomes a snackbar sentence, never a throw) and `deck.$deckId.tsx` (the sentence). Unit tests: `-edit-reload.test.ts` (the trigger and the fix on the shipped router), `-refused-page.test.ts`, `refusal.test.ts`; the studio project 304 tests green. The two 4388 probes (`int3fix/loader-rerun-probe.mjs`, `refused-page-probe.mjs`) are not driven: the lock was the b3 lane's for the whole round. Requests FR3-R1 and FR3-R2 to B7 in `integrator.md`.

## Cycle 3 stream fix round fix round, the integrator's fixer (2026-09-18)

- C2-F18 (check step 31): partly fixed. Zod left the entry chunk (583,982 to 464,817 B) and the dither worker (104,718 to 33,370 B) through three zod free values modules (`schema/blocks/dither-values.ts`, `schema/icon-names.ts`, the editor's `auto-title.ts` out of `server/write.ts`), `detectPlatform` and the five presenter icons left the menu model and the chrome's icon module behind on `/present`; `/home` 551 KiB, `/decks/trash` 508 and `/present` 1,156 pass. The perf script's write path follows A1 (a double click) and a thrown check is a failed row, so the run completes and writes its JSON with each sample's script list. Run 4: 132 of 151 budgets met; standing: `/`, `/new`, `/edit` (the editor's set, 2,147 to 2,150 KiB against 1,953), `/decks` (the 0.39 viewport preload against the 0.27 count, Kevin's decision since round four), `/deck` (the shared shell), the three `dom nodes` rows (the filmstrip's card shells), the load rows. `integrator.md` "Cycle 3 stream fix round fix round".
- F-check5 (check step 5): fixed. `apps/cli/vitest.config.ts` gives the CLI tests 30 s (the browser launching canvas writes), `blob-vercel.test.ts` gives its three timing tests 20 s; no assertion changed; cli 131 passed, store 190 passed.
- b6's R1 (the marks box) applied with a unit test; b7's SF-R2 written into `docs/hosting.md` "Reads" and `docs/sessions-polling.md` 3.1; b4's R3 (step 21 on the file store) left for the integrator's merge run.
