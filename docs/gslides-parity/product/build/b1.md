# The product round, B1 the chrome and the interface craft

Notes of builder B1 of the product round (docs/PRODUCT.md section 2 for the chrome items, section 3 whole; section 7's B1 row names the files and the rows). The binding specification is `docs/PRODUCT.md`; `docs/RETURN.md`, `docs/FOCUS.md` and `docs/gslides-parity/focus/AMENDMENTS.md` A1 stand under it; the interface audit (`docs/gslides-parity/product/audit-interface.md`) is the evidence. Nothing here ran a git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build`. The tree was clean at `cbd4495` when this lane started (the one untracked entry was `.github/`, not this lane's). Times are PDT on 2026-09-19 and the night into the 20th.

## Product round

### 0. Requests, written first so the owners can act while this lane builds

Every request names the file, the owner and the one change. R1 to R4 are the model rows section 7 says land as requests on day 0.

- R1, the integrator, `packages/chrome/src/menus/model.ts`, the Download submenu (lines 914 to 954): a row `file.download.options` labelled "Download options" after the PDF row, `dialog('Download options')`, `doc: 'The modes, Include skipped slides and Include speaker notes'`, `when: 'export'`; and in `packages/chrome/src/editor-shell.ts` a dialog id `downloadOptions` in `DIALOG_IDS` with `dialogIdOf('Download options') === 'downloadOptions'`, and in `EditorShell.tsx`'s `dialogNode` the case `<DownloadDialog.Component format="pptx" options />`. `dialogs/Download.tsx` (this lane) already takes `options` and draws the whole dialog for it (section 1.8). Until the row lands, a deck without a table or a chart has no way to reach the options dialog, since the two rows start their download at once (rank 8); the rows `export.download.options-dialog` and `export.download.mode-sentence` read red until then.
- R2, the integrator, `model.ts` `TITLE_ROW_ITEMS`: a row `title.sidePanel` labelled "Show side panel", `client('toggleSidePanel')` (or a `panel` effect the shell maps to reopen and close), `turboslide: true`, `doc: 'Reopens the last panel: Format options, Themes, Comments or Version history; closes the open one'`, so the completeness test and the parity audit find the control `title.sidePanel` (TitleRow.tsx draws it since this lane, with `data-control="title.sidePanel"` and no `data-menu-item`). Section 7.1 names the id.
- R3, the integrator, `packages/chrome/src/menus/strings.ts`: the words this lane wrote live in its own files until strings.ts takes them: the access sentences and `SHARE_NAME_ASKED_KEY` in `dialogs/Share.tsx` (`accessSentence`), `SHARE_NAME_PROMPT_TITLE` "Your name, shown to collaborators" in `dialogs/NamePrompt.tsx`, `COMMENTS_EMPTY_GESTURE` in `comments/CommentsPanel.tsx`, `RECENT_ROW_SENTENCE` in `apps/studio/src/routes/decks.index.tsx`, `TABLE_SENTENCE`, `PERFECT_SENTENCE`, `EDITABLE_SENTENCE` in `dialogs/Download.tsx`, `DELETE_FOREVER_LEAD` and the sentences of `dialogs/DeleteForever.tsx`, `ACCESS_LINKS` in `YouNeedAccess.tsx`, `CAPTION_REST` in `LayoutGrid.tsx`. Each is one exported constant; moving it is one import.
- R4, the integrator, `apps/studio/src/routes/home.tsx`: the facts band and For agents move below the fold (section 2 rank 22): render `HomeFacts` and `HomeAgents` after `HomeCards`. The hero's sentence and the Start from a template button are in `home/copy.ts` and `HomeHero.tsx` (this lane); the band order is home.tsx's, which section 7 does not give this lane.
- R5, B4, the drivers, four adaptations the product round's own rules make necessary (measured on this lane's server, section 3):
  - The first Share on a browser with no display name shows the name prompt first (`dialog.namePrompt` with `dialog.namePrompt.skip` and `dialog.namePrompt.continue`; the row `share.name-prompt.first-share`), once per browser (`localStorage` `ts-share-name-asked`). Every step that clicks `share.open` on a fresh context must press Skip (or set a name) before it waits for `dialog.share`: in the chrome area that is `decks.name.follows-heading` (the Share heading read), `chrome.field.boundary-3-1`, `chrome.focus.one-ring-rule` and `chrome.select.one-rule`, each of which timed out on `dialog.share` at 21:05.
  - The three link rows, the invite row, the requests band, the links list, the gear and the footer sit behind the More row (`dialog.share.more`; section 2 rank 3): `share.copy-view-link`, `share.copy-present-link`, `share.copy-edit-link`, `share.file-menu-copy-link` and every step that reads `dialog.share.owner`, `dialog.share.rows`, `dialog.share.emails` or `dialog.share.authorize` click More first. The first stage's controls are `dialog.share.mode`, `dialog.share.linkRole`, `dialog.share.accessSentence`, `dialog.share.address` (read only), `dialog.share.copy` (the one Copy link), `dialog.share.slideshow`, `dialog.share.people` when grants exist, and Done.
  - `chrome.cluster.gaps-heights`: the right cluster gained the side panel toggle's 32 px slot (`title.sidePanel.slot`) between the comments slot and the inbox slot, so the gaps read 8, 48, 8 where the driver expected 8 throughout; the row's expectation should list the new slot (its own gaps are 8 and 8).
  - `chrome.select.one-rule`: the Comments filter is absent with zero comments by 3.2 (the row `comments.panel.empty-gesture`); the step should add one comment first, or read the Share dialog's selects alone.
  - `chrome.disabled.token-both-appearances` and `chrome.hover.ground`: Undo is disabled on the deck at that point of the walk (nothing to undo after the walk's reload), so its colour equals Redo's and a disabled button takes no hover ground by 3.1; the steps should make one edit first, or hover an enabled button (New slide) and read ink-2 from the token.
- R6, B4, `scripts/probes/core-walk/toolkit.mjs` (line 1805, the seams' `.ts-bottombar` rect) and `apps/studio/e2e/editor.spec.ts` (lines 254 to 264), `scripts/layout-shift-audit.mjs` (line 137): the bottom bar left (section 2 rank 25); the seam list has ten seams now and the editor spec's bar assertions have nothing to find.
- R7, B5a, `packages/chrome/src/ThemesPanel.css` (the Brand kit panel): the current tile is one 2 px ink border and no outline (3.1, the tile row; audit-interface 19), as `LayoutGrid.css` and `Filmstrip.css` draw since this lane. One rule.
- R8, the integrator, `packages/chrome/src/Panel.css` `.ts-panel-x:hover`: `background: var(--pt-plate)` (3.1, the shell button row: the panel X takes the hover ground); `packages/chrome/src/NotesPane.css` the notes field's focus border: `var(--pt-field)` (3.1, the field row); `apps/studio/src/styles.css` `.pt-viewer.is-editor.ts-skeleton` `grid-template-rows: 44px 28px 40px minmax(0, 1fr)` (the skeleton's fifth row left with the bar; `EditorSkeleton.tsx` carries the value inline until the sheet does).
- R9, the integrator or B2, `packages/viewer/src/present/PresenterConsole.tsx`: the labels Timer, Time, Previous slide and Next slide in sentence case at 12.5 px `--pt-ink-2`, the arrows in `--pt-ink-2` at 32 px (3.6; audit-interface 26; the row `present.presenter.sentence-case`). `PresenterPage.tsx` (this lane) mounts the console and draws none of those words.
- R10, B7, `apps/studio/src/server/decks.ts` `DeckCard`: an optional `updatedBy` (the display name of the last editor, section 2 rank 4) so the card reads "Edited yesterday at 2:02 PM by Kevin" (3.6); `decks.index.tsx` reads the field when present and the time alone when absent.
- R11, the integrator (a record): `packages/viewer/src/theme.ts` `THEME_BOOT_SCRIPT` and its docblock ("never consults prefers-color-scheme") stand for the deck document; the studio's document boots with `APPEARANCE_BOOT_SCRIPT` in `apps/studio/src/routes/__root.tsx` (3.1.1) and no longer imports the viewer's constant. `DEFAULT_THEME` there is still `dark`, which `readStoredTheme()` answers to a caller with no stored key; every caller this lane found reads the stamped attribute, so nothing else changes.
- R12, B6 (a record): `packages/chrome/src/__tests__/palette-data.test.ts` fails on `main` with B6's author label change ("Assistant · r411" against the expected "agent:run-11 · r411"); not this lane's.
- R14, B5a or the integrator (a record, 21:23): `packages/schema/src/actions.ts` lines 50 and 52 import `brandKitSchema` from `./brand.ts` twice, and every vitest file of `packages/chrome` that reaches the schema fails at transform ("Identifier `brandKitSchema` has already been declared", vite:oxc) while the two lines stand; this lane's suites passed at 21:16, before the second import landed. Not this lane's file.
- R13, the integrator, `apps/studio/e2e/home-page.spec.ts` lines 253 and 254: the hero's `h1` is the seller's sentence since this lane (B4's driver for `decks.home.seller-lead` reads `main h1` and expects "Build the pitch, present it and send the link, in one place"; section 2 rank 22 says the hero leads with the seller's sentence), and the lockup with the mark and the word is a `p.ts-product-h1` with the same look; the spec's two assertions on `h1.ts-product-h1` should read `p.ts-product-h1`.

### 1. What changed, by item

Every item names the PRODUCT.md rank or section, what was measured, the change and its files. "Measured" means driven on this lane's dev server (port 4411, the tmp store, `TURBOSLIDE_REALTIME=memory`) with headless Chromium, or read from the unit tests named in section 5.

#### 1.1 The tokens and the state law (3.1; `chrome.contrast.titanium-light`, `chrome.disabled.token-both-appearances`, `chrome.field.boundary-3-1`, `chrome.hover.ground`, `chrome.floating.edge-frame`, `chrome.focus.one-ring-rule`, `chrome.filmstrip.one-ring`, `chrome.select.one-rule`, `chrome.check.draws-check`)

`packages/chrome/src/tokens.css`, the one token file:

| Token                    | Light                                                                                            | Dark                                           | Readers                                                                                                                                                                                                                                                                           |
| ------------------------ | ------------------------------------------------------------------------------------------------ | ---------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `--pt-titanium`          | `#6f747d` (4.70:1 on paper; was `#8a8f98`, 3.25:1)                                               | `#8a8f98` unchanged                            | the save words, the menu keys, the filmstrip numbers, the field labels, the version meta, the empty sentences                                                                                                                                                                     |
| `--pt-disabled` (new)    | `#8a8f98`                                                                                        | `#6b6e73`                                      | `.pt-ib.is-disabled`, `.pt-ib[aria-disabled='true']`, `.pt-ib:disabled` (tokens.css), `.ts-menu-item.is-disabled` (Menu.css), the size field with no text (EditorToolbar.css), `.ts-dialog-check.is-disabled` and disabled fields (Dialog.css), `.pt-select:disabled`             |
| `--pt-field` (new)       | `rgba(7, 7, 7, 0.44)` (3.1:1)                                                                    | `rgba(242, 242, 240, 0.44)` (3.95:1)           | every input, select and textarea of Dialog.css, EditorToolbar.css (the zoom box on hover, the size field, the hex field), share.css, decks.css (the search field), YouNeedAccess.css, VersionsPanel.css (the version name field), comments.css (the search field), `.pt-select`   |
| `--pt-plate`             | `rgba(7, 7, 7, 0.06)` (1.14:1; was 0.035)                                                        | `rgba(242, 242, 240, 0.08)` (1.16:1; was 0.05) | the hover ground of `.pt-ib`, the menu titles and rows, `.ts-dialog-x`, `.ts-title-home`, the stage ground                                                                                                                                                                        |
| `--pt-status-h`          | `0px` (was 32 px)                                                                                | the same                                       | the snackbar's bottom, the floating name prompt's bottom, the edit route's banner: the bottom bar left (1.5)                                                                                                                                                                      |
| `.pt-focus` (new class)  | `outline: 1px solid var(--pt-ink); outline-offset: -1px`                                         | the same                                       | the one focus rule, applied by tokens.css to `.pt-ib` and `.pt-select` and by the sheets to every dialog control, the layout tiles, the check and radio boxes, the `/decks` links (the 2 px offset ring left `Dialog.css`, `LayoutGrid.css`, `EditorToolbar.css` and `decks.css`) |
| `.pt-select` (new class) | 32 px, the field boundary, the chrome's own chevron drawn with two gradients, `appearance: none` | the same                                       | every select in a dialog by element (`.ts-dialog select`), the `/decks` sort select, the Comments filter, the access page's role select                                                                                                                                           |

The state law in the sheets: `.pt-ib:hover` gains `background: var(--pt-plate)` under the hairline (tokens.css); a disabled control takes no hover ground and no frame; `.pt-ib.is-solid:disabled` reads the disabled ground under paper text. Every floating plate takes the frame weight `--pt-edge` (Menu.css, Tooltip.css, `.ts-tb-swatches` in EditorToolbar.css, `.ts-layout-plate` in EditorShell.css). The current filmstrip card is one 2 px ink border and no outline, the selected card a 1 px ink border (Filmstrip.css); the current layout tile the same (LayoutGrid.css). A checked box in a dialog draws the menus' check (a 4 by 9 corner turned 45 degrees in paper) and not the 6 by 2 bar (Dialog.css). `ToolButton.css` lost its own disabled rule; tokens.css holds the state.

Measured on 4411 by B4's chrome walk at 21:02 to 21:05 (section 3): `chrome.contrast.titanium-light` saveWords 4.7, menuKey 4.7, filmstripNumber 4.7, panelLabel 20.14; `chrome.floating.edge-frame` File menu 5.68, tooltip 5.68, layout plate 5.68; `chrome.filmstrip.one-ring` frame border 2px solid ink, frame outline none, card outline none; `chrome.check.draws-check` ::after 4 by 9 px turned 45 degrees; `chrome.disabled.token-both-appearances` Redo reads `rgb(138, 143, 152)` light and `rgb(107, 110, 115)` dark (the two composites), the driver's comparison against Undo failed because Undo was disabled too (R5); `chrome.hover.ground` the File title hovered reads `rgba(242, 242, 240, 0.08)`, the plate, and Undo took no ground because it was disabled (R5).

#### 1.2 The rules outside the matrix (3.1.1)

- Tooltips do not fire on keyboard focus inside `.ts-menu`, `.ts-context-menu` or `[role="dialog"]` (`Tooltip.tsx` `quietOnFocus`, `QUIET_FOCUS_SURFACES`); hover alone shows them there. A menu row whose submenu or grid is open schedules no tooltip and the plate that was up leaves when the submenu opens (`Menu.tsx` `openSubmenu` calls `hideTooltip`; the row's `onMouseEnter` returns while `isOpen`; section 2 rank 28). Measured: `chrome.tooltip.none-on-focus-in-menus` "in the Share dialog none; over the object menu none"; `chrome.menu.no-tooltip-with-submenu` ok.
- A menu plate is as wide as its longest label between 220 px and 320 px (`Menu.css` `width: max-content; min-width: var(--pt-menu-w); max-width: 320px`). Measured: `chrome.menu.plate-fits-labels` "9 menus; cut labels none; Print settings and preview whole".
- Access keys: on macOS the label is plain text (`Menu.tsx` `AccessLabel` draws no `<u>` for `platform === 'mac'`); on Windows and Linux the mark is drawn and `Menu.css` underlines it only while `.ts-menu.is-alt` is on, which the root list stamps from Alt's keydown and keyup. Measured: `chrome.menu.no-mnemonics-mac` "access key marks 0, underlined 0"; the Windows drawing is unit tested (`menu-component.test.tsx`) and not driven.
- The first visit follows the operating system: `apps/studio/src/routes/__root.tsx` `APPEARANCE_BOOT_SCRIPT` reads `gt-theme`, then `gt-deck-theme`, then `prefers-color-scheme: dark` for dark and light otherwise, and stamps `data-theme` before first paint; a stored key wins afterwards. Not driven by this lane (the row `chrome.appearance.first-visit-follows-os` is `core/decks.spec.ts`'s, which B4 has written; section 3 names the run this lane did not make).
- The show's bar shows for three seconds on entry and after every pointer move over the show, then fades (`apps/studio/src/components/Slideshow.tsx` `BAR_ENTRY_MS`, `glanceBar`); the corner keeps it up while the pointer is there. Not driven (`core/present.spec.ts`, B4's).
- `?` opens the Keyboard shortcuts dialog as Cmd+/ does (`useEditorKeys.ts`: the key runs `help.keyboardShortcuts` before the retired letter path). The snackbar that named the Help menu no longer fires for `?`. Not driven (`help.shortcuts.question-key` is in B4's help area; this lane ran the chrome area alone).
- The corner radius: unchanged (`ToolButton.css` keeps the 8 px solid call to action, `TitleRow.css` the 8 px Slideshow and Share). The icon column: unchanged.

#### 1.3 Escape returns focus to the stage (section 2 rank 29; `chrome.menu.escape-focus-stage`)

`MenuBar.tsx` `onClose`: on `escape` from a menu the pointer opened the title is blurred and `.ts-stage, .pt-stagewrap` takes the focus; a menu the keyboard opened keeps the ARIA pattern and returns to its title. Measured: ok ("open the Help menu with a click, press Escape, read the focus").

#### 1.4 The presence tooltip and the save words (rank 30; `chrome.presence.tooltip`)

`presence/PresenceSlot.tsx` `presenceSlotTip`: the slot's group carries "Collaborators. Who is in this presentation now. Nobody else has it open" (the count when others are present). `TitleRow.tsx` `SaveState`: on a fresh draft that nothing has written (`save.draft && save.state === 'saved'`, not offline, not reconnecting) the cell keeps its width (the longest phrase's pseudo element) and shows no words (`is-untouched`, `aria-hidden`, out of the tab order); the words arrive with the first edit. Measured at 21:04: the tooltip read as above; the save words on a fresh `/new` still read "Not saved yet" because the first build hid them by visibility alone and the driver reads the text; the text is empty since 21:14 and the row is to be re-driven (section 3).

#### 1.5 The bottom bar leaves (rank 25; `chrome.bottom-bar.removed`)

`packages/chrome/src/BottomBar.tsx` and `BottomBar.css` are deleted; `EditorShell.tsx` no longer imports or mounts the bar; `EditorShell.css` has four grid rows and the overlay filmstrip, its scrim and the floating right panel reach `bottom: 0`; `tokens.css` `--pt-status-h` is 0. The side panel toggle sits in the title row's right cluster beside the comments glyph (`TitleRow.tsx` `title.sidePanel` in `title.sidePanel.slot`, the sidebar glyph while closed and the chevron while open, `aria-pressed`, the tooltips "Show side panel" and "Hide side panel"). The Filmstrip and Grid view buttons the bar held while Grid view was parked are gone; the View menu row stays behind the switch. `EditorSkeleton.tsx` draws no footer and carries the four row grid inline (R8). Measured: ok ("read the editor for a bottom bar, the side panel toggle and the stage height at 900").

#### 1.6 The toolbar folds at any width (3.4; `chrome.toolbar.fold-any-width`, `chrome.toolbar.bold-follows-selection`)

`ToolbarTail.tsx`: every control renders in a slot (`data-slot`); a layout effect measures the slots' widths (remembered per control id so a folded control is never measured at zero), the room the tail's end leaves (the pointer toggle and the Hide the menus chevron, `.ts-tb-end` and `.ts-tb-hide`), and `foldPlan` names the controls that fold: none while everything fits, else in the order Clear formatting, the indent pair, the list buttons, Highlight color, the fill and border group, then the rest from the right end, Format options never (`FOLD_ORDER`, `NEVER_FOLDS`, pure and unit tested). A folded slot is `display: none` and out of the accessibility tree (`EditorToolbar.css` `.is-folded`); the More button (`toolbar.more`, `data-count`) lists the folded controls alone with their labels, and its tooltip counts them. A ResizeObserver on the tail and the window's resize re-measure; the fixed fold at 1100 px is gone (`MORE_BREAKPOINT_PX` stays exported in `toolbar-tails.ts`, B5a's file, unread). The pressed state: `editor-shell.ts` `selectionMarks(facts)` answers the editor's reported marks (the InlineText's caret info carries `b` beside the schema's marks) or the marks of the selected range read from the block's text (`marksOfRange` plus the run flag `b`); `ToolbarTail.tsx` `markPressed` lights Bold on `b` or the block's weight 500, Italic on `i`, Underline on `u`. Measured: `chrome.toolbar.bold-follows-selection` "on the bold word aria-pressed true; on plain text false"; the fold row is `core/chrome.spec.ts`'s (B4) and was not run by this lane in the gate; the after pictures of section 4 (`toolbar-tail-table` at 1440 and 1280) show what the bar drew.

#### 1.7 The Share dialog in two stages (section 2 ranks 3 and 4; `share.dialog.one-link`, `share.dialog.slideshow-checkbox`, `share.dialog.more-row`, `share.dialog.you-label`, `share.name-prompt.first-share`)

`dialogs/Share.tsx`: the first stage is the General access select with the role select beside it while a link is on, one sentence under them (`accessSentence`: for Anyone with the link as Editor "Anyone with this link can open and edit this presentation. Pick Viewer before you send it to a customer", as Viewer "Anyone with this link can open it and cannot change it", as Commenter "Anyone with this link can open it and leave comments"; Restricted under shadow "Only you can open this address on this Turboslide until sign in arrives; pick Anyone with the link to share it", under enforce today's `restrictedDoc`; a legacy deck the open sentence), the address in a read only field (`dialog.share.address`: the tokenized general link this browser minted or the page carried under Anyone with the link, empty with the placeholder "Copy link creates the address" until then; the deck's `/edit/<id>` under Restricted and for a legacy deck) with the one Copy link (`dialog.share.copy`, which mints the link when the field is empty), the checkbox Open as a slideshow (`dialog.share.slideshow`, the field reads the present link), the people rows when a grant or a pending owner exists, Done. Behind the More row (`dialog.share.more`, "Settings" while open, the request count in its label while requests wait): the requests band, Add people by email, the owner row when the first stage drew no people, the three link rows with their Created dates, the general links with Rotate and Revoke, Switch to a link, Stop sharing, the gear's five switches under a labelled Permissions button, Publish to the web and the two footer sentences. The own row reads You (`personName`, rank 4) for the owner, the pending owner and a grant. The first Share on a browser with no display name shows the name prompt first (`NamePromptDialog` with `title` "Your name, shown to collaborators" and `onDone`, a Skip button, `SHARE_NAME_ASKED_KEY` once per browser; never for a signed in account). `share.css` carries the first stage's rows. Not driven by this lane (`core/share.spec.ts` is B4's; R5 names what the drivers must do first); the unit tests of section 5 drive the first stage, the swap, More and the prompt.

#### 1.8 Download: the one click rows, the table rule, the options dialog, the snackbar (ranks 7, 8, 11, 21; `export.download.pdf-direct`, `export.download.pptx-direct`, `export.download.options-dialog`, `export.download.mode-sentence`, `export.download.progress-per-slide` the dialog side)

`dialogs/Download.tsx`: File > Download > PDF Document runs `export.run` at once with no dialog (the component mounts and draws nothing while it runs); the snackbar reads the estimate ("Preparing your PDF, about 10 seconds for 1 slide"), then the progress from `input.export.progress` when its text names "slide k of n" (`progressSentence`), then "Saved <name>" with a Details action that opens the report; the component closes the shell's dialog when done. Microsoft PowerPoint (.pptx) does the same in the Perfect mode when the deck holds no table and no chart (`deckHasTableOrChart`); with either it opens the dialog with Editable text preselected and the table sentence above the modes ("This presentation has a table or a chart. Editable text keeps them editable in PowerPoint"); the Perfect mode's own sentence reads "Every slide as a picture with a text layer you can search. Tables and charts are pictures in this mode" (rank 11). `options` (the Download options row, R1) draws the whole dialog, which closes itself when the file is saved. The saved name follows rank 7 (`downloadFileName`, `downloadFileNames`: the title with `" \ / : * ? < > |` and the control characters removed, "(dark)" for the second file of a both appearances run, "(editable)" for the second PowerPoint mode, the id for an untitled deck), the estimate is the measured seconds per slide of the last export on this browser (`recordSecondsPerSlide`, `measuredSecondsPerSlide`, `ts-export-seconds-per-slide`), never under 10 s, its first value 2.5 s per slide for PowerPoint and 1.2 for a PDF until B7's production measurement replaces it. Not driven by this lane (`core/export.spec.ts` is B4's); unit tested (section 5).

#### 1.9 The panels (3.2; `comments.panel.empty-gesture`, `versions.panel.author-you`, `versions.field.square`)

- Comments (`comments/CommentsPanel.tsx`, `comments.css`): with zero threads the panel shows "No comments yet" and one gesture line, "Select something on a slide, then Insert > Comment (Cmd+Option+M)" (Ctrl+Alt+M on Windows; `platform` prop, passed by the shell), and no tabs, search or filter; they arrive with the first thread. The filter is the one select (`.pt-select`).
- Version history (`VersionsPanel.tsx`, `VersionsPanel.css`): a row names You for this browser's own principal (`me`, passed by `EditorShell.tsx` from the account or the presence self; `authorWord`) and the display name for another, the meta at 12 px in `--pt-ink-2`; the version name field is square in the field boundary; Name current version stays in the tools row at the panel's head.
- The Format options panel is B2's this round. The Themes panel is B5a's (R7).

#### 1.10 The dialogs and the access page (3.3; `decks.trash.confirm-dialog-chrome`, `decks.access.stranger-links`)

- `Dialog.tsx`: an action may carry `autoFocus` (the button takes the focus on open, `data-autofocus`), and a field with `data-select="all"` is selected on open; the X takes the plate ground on hover; every ring is the inset ring (`Dialog.css`). `dialogs/DeleteForever.tsx` is the confirm of `/decks/trash`: the 18 px title "Delete <title> forever?" or "Delete N presentations forever?", the lead "This cannot be undone.", one sentence naming what goes, Cancel and the solid Delete forever focused (`deleteForeverTitle`, `deleteForeverSentence`). `decks.trash.tsx` mounts it for one deck and for Empty trash; `decks.index.tsx`'s Make a copy dialog is the chrome Dialog too, and the page's own dialog primitive and its sheet left (`decks.css`).
- `YouNeedAccess.tsx`, `YouNeedAccess.css`, `apps/studio/src/routes/-access-page.tsx`: the page leads with Your presentations (solid) and New presentation (`ACCESS_LINKS`, router links through `linkComponent`, 40 px like the hero's) above the request form under its own head "Ask for access"; the form is hidden while the deployment runs `TURBOSLIDE_AUTHORIZE=shadow`, which the page reads after hydration from `GET /api/access/<id>`'s `authorize` field (the route names the mode whatever the deck's state); the server's HTML keeps the form so a browser without scripts can still ask; the fields are ruled in `--pt-field`, the role select is `.pt-select`.

#### 1.11 The layout plate (3.4, section 2 rank 24; `slides.layout.plate-four-columns`, `slides.layout.tile-sentences`)

`LayoutGrid.tsx`, `LayoutGrid.css`: four columns of 128 px tiles at 1280 px and up (the grid 560 wide, the plate 576), three under it; the arrows step by the column count; the GT layouts sit behind a disclosure row (`layout.<purpose>.gt`, "GT layouts (10)", `aria-expanded`, remembered per browser under `ts-layout-gt-open`, open on its own when the current layout is one of GT's); the caption row at the bottom (`layout.<purpose>.caption`, `aria-live`) reads the hovered or focused tile's `sentence` (B3's field, "your logo"), the current layout's at rest, "Point at a layout to read what it holds" with none; a tile that needs a picture adds "Add a picture first: this layout carries a full picture". The tiles keep `data-tip` for the tooltip audit and attach no tooltip handlers, so no plate floats over a neighbour. Not driven by this lane; unit tested (`layout-grid.test.tsx`).

#### 1.12 The empty and loading states (3.5; `decks.new.skeleton-one-frame`)

`apps/studio/src/components/EditorSkeleton.tsx`: `frames` (one for `/new`, three for `/edit` until a route knows its count), a quiet paper plate in a hairline where the sheet will stand (`.ts-skeleton-sheet`, `brand.css`) instead of the dither figure, no footer row. The routes' `pendingMs` are unchanged at 0: under `ssr: 'data-only'` the skeleton is the server's markup for the route and paints before the document whatever the client's timing, so the 300 ms rule of 3.5 has no place to act on the first load; a client side navigation to `/edit` from a card would be the place, and this lane left it at 0 because `pendingMinMs: 0` already lets the editor replace the skeleton the moment it is ready. `new.tsx` and `edit.$deckId.tsx` are unchanged for that reason (the frames prop takes its default of three on `/edit`; `/new` should pass `frames={1}`, which needs `pendingComponent: () => <EditorSkeleton frames={1} />` in `new.tsx`, a change this lane made in its head and not in the file: recorded as not done, section 6).

#### 1.13 The home and decks pages (3.6, section 2 ranks 4, 15, 16, 22, 26; `decks.card.thumbnail-slide-1` the client half, `decks.card.edited-relative-time`, `decks.card.more-glyph`, `decks.trash.button-heights`, `decks.notfound.sentence-case`, `decks.recent.this-browser-sentence`, `decks.recent.drops-trashed`, `decks.trash.editor-undo-snackbar`, `decks.home.seller-lead`)

- `decks.index.tsx`, `decks.css`: the card's plate is the deck's own paper with the title in its ink (`#ffffff` and `#070707` for a light deck, `#070707` and `#f2f2f0` for a dark one), never black on a light page; a failed capture is asked for again after 3 s and after 6 more (`Thumb`, `THUMB_RETRY_MS`; B7's `thumbs.ts` answers the copy on save); the card reads "Edited today at 2:02 PM", "Edited yesterday at 2:02 PM" or "Edited Sep 12, 2026" in the browser's locale (`editedLine`, `Intl.DateTimeFormat` with `timeStyle: 'short'`), "by <name>" when the listing carries `updatedBy` (R10); the list rows are 32 px; the more button is the icon set's `ellipsis-vertical`; the Opened on this device head carries "Presentations this browser opened. On another computer, open a presentation from its link" (`home.recent.sentence`); the row drops a deck the listing no longer holds (`onListed`) and the deck the editor moved to the trash; the sort select is the one select.
- The editor's Move to trash (`EditorShell.tsx`, the trash path at the `deck.trash` result): writes the trashed marker (`sessionStorage` `turboslide:trashed`: the id, the title, the facts the card draws) and leaves for `/decks`; `-recent.ts` `markDeckTrashed`, `takeTrashedMarker` read it once while young (15 s); the home page hides the card, drops the Recent entry and shows "Moved to trash" with Undo, which restores the deck (`restoreStoredDeck`), records the entry again and refetches.
- `decks.trash.tsx`, `decks.css`: Restore and Delete forever are 32 px, Delete forever in ink with the trash glyph, Empty trash the page's one solid button; the confirm is `DeleteForeverDialog`.
- `__root.tsx`: Not found's buttons read "New presentation", "Your presentations", "About Turboslide" at 40 px (`brand.css` `.ts-notfound-actions .pt-ib`).
- `home/copy.ts`, `HomeHero.tsx`: the hero's `h1` is the seller's sentence, "Build the pitch, present it and send the link, in one place" (no trailing period, a heading), at the lead's size; the lockup with the 48 px mark and the word at 66 px keeps its look as a paragraph above it (R13); the second button reads Start from a template and opens `/decks/templates` (B5b's gallery page); the fact line carries the parity sentence that was the hero's. The band order is R4.
- Not driven by this lane (`core/decks.spec.ts` is B4's); the after pictures of section 4 show the pages.

#### 1.14 The show (section 2 rank 23; `present.show.no-white-block`)

`Slideshow.css`: the snackbar inside the show takes the bar's tokens (`--pt-panel-ink` and `--pt-panel-text`), because the chrome's ink plate is paper in the dark appearance and painted a light block above the bar; an off snackbar is `visibility: hidden`. This lane did not reproduce the white block on its server before the change (the audit's picture is production's) and records the row as not driven.

### 2. The words a seller reads, new this round

Plain, sentence case, no exclamation marks, no trailing period on a heading: "Your name, shown to collaborators" and Skip; "Anyone with this link can open and edit this presentation. Pick Viewer before you send it to a customer"; "Only you can open this address on this Turboslide until sign in arrives; pick Anyone with the link to share it"; "Copy link creates the address"; "Open as a slideshow"; "More" and "Settings"; "Permissions"; "Created"; "Preparing your PDF, slide 3 of 6"; "Saved GT pitch for Acme.pdf" with Details; "The download did not finish: <reason>"; "This presentation has a table or a chart. Editable text keeps them editable in PowerPoint"; "Every slide as a picture with a text layer you can search. Tables and charts are pictures in this mode"; "Download options"; "Delete GT pitch for Acme forever?" with "This cannot be undone." and "6 slides and every version of GT pitch for Acme are deleted."; "No comments yet" with "Select something on a slide, then Insert > Comment (Cmd+Option+M)"; "You" on the own rows; "Presentations this browser opened. On another computer, open a presentation from its link"; "Edited yesterday at 2:02 PM"; "Show side panel", "Hide side panel"; "Collaborators. Who is in this presentation now. Nobody else has it open"; "GT layouts (10)"; "Point at a layout to read what it holds"; "Ask for access"; "New presentation", "Your presentations"; "Build the pitch, present it and send the link, in one place."; "Start from a template"; "One more tool that did not fit the bar" and "N more tools that did not fit the bar" on More.

### 3. The rows and what happened on this lane's server

Driven with B4's chrome area of the core walk on 4411 at 21:02 to 21:05 PDT (`node scripts/probes/editor-walk-probe.mjs --core --base http://localhost:4411 --only chrome`, the lock held; the table is in this lane's scratchpad, `b1-probe/chrome.json`): 49 steps, 40 ok, 9 failed, 178 s, one scratch deck `untitled-20260920-yd2o` trashed and deleted (404 on `/edit` and `/deck`).

| Row                                                                                                                                                                                                                                                                                                                                                            | Result                                         | What happened                                                                                                         |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `chrome.bottom-bar.removed`                                                                                                                                                                                                                                                                                                                                    | passes                                         | no bar, the toggle in the title row, the stage 724 px at 900                                                          |
| `chrome.menu.no-tooltip-with-submenu`                                                                                                                                                                                                                                                                                                                          | passes                                         | Insert > Table's grid open, no plate                                                                                  |
| `chrome.menu.escape-focus-stage`                                                                                                                                                                                                                                                                                                                               | passes                                         | focus on the stage, no ring on Help                                                                                   |
| `chrome.presence.tooltip`                                                                                                                                                                                                                                                                                                                                      | failed at 21:04, fixed at 21:14, not re-driven | the tooltip read; the save words read "Not saved yet" (hidden by visibility alone; the text is empty since)           |
| `chrome.contrast.titanium-light`                                                                                                                                                                                                                                                                                                                               | passes                                         | 4.7, 4.7, 4.7, 20.14                                                                                                  |
| `chrome.disabled.token-both-appearances`                                                                                                                                                                                                                                                                                                                       | failed: the driver                             | Redo reads the two composites; Undo was disabled too (R5)                                                             |
| `chrome.field.boundary-3-1`                                                                                                                                                                                                                                                                                                                                    | failed: the driver                             | timed out on `dialog.share` behind the name prompt (R5)                                                               |
| `chrome.hover.ground`                                                                                                                                                                                                                                                                                                                                          | failed: the driver                             | the File title took the plate; Undo was disabled (R5)                                                                 |
| `chrome.floating.edge-frame`                                                                                                                                                                                                                                                                                                                                   | passes                                         | 5.68 on the three plates                                                                                              |
| `chrome.focus.one-ring-rule`                                                                                                                                                                                                                                                                                                                                   | failed: the driver                             | timed out on the Share dialog behind the name prompt (R5)                                                             |
| `chrome.filmstrip.one-ring`                                                                                                                                                                                                                                                                                                                                    | passes                                         | one 2 px border, no outline                                                                                           |
| `chrome.tooltip.none-on-focus-in-menus`                                                                                                                                                                                                                                                                                                                        | passes                                         | none in the dialog, none over the object menu                                                                         |
| `chrome.menu.plate-fits-labels`                                                                                                                                                                                                                                                                                                                                | passes                                         | nine menus, no cut label                                                                                              |
| `chrome.menu.no-mnemonics-mac`                                                                                                                                                                                                                                                                                                                                 | passes                                         | 0 marks, 0 underlined                                                                                                 |
| `chrome.select.one-rule`                                                                                                                                                                                                                                                                                                                                       | failed: the driver                             | the Share select reads 32 px, appearance none; the Comments filter is absent by 3.2 (R5)                              |
| `chrome.toolbar.bold-follows-selection`                                                                                                                                                                                                                                                                                                                        | passes                                         | true on the bold word, false on plain text                                                                            |
| `chrome.cluster.gaps-heights` (an existing row)                                                                                                                                                                                                                                                                                                                | failed: the driver                             | the new slot's 32 px between the comments slot and the inbox slot (R5)                                                |
| `chrome.separators.once` (existing)                                                                                                                                                                                                                                                                                                                            | failed                                         | "Execution context was destroyed, most likely because of a navigation" during the step; not read; to be re-driven     |
| `decks.name.follows-heading` (existing)                                                                                                                                                                                                                                                                                                                        | failed: the driver                             | timed out on the Share heading behind the name prompt (R5)                                                            |
| `chrome.appearance.first-visit-follows-os`, `chrome.toolbar.fold-any-width`, `share.dialog.*`, `share.name-prompt.first-share`, `decks.*`, `export.download.*`, `help.shortcuts.*`, `present.show.*`, `comments.panel.empty-gesture`, `versions.panel.author-you`, `versions.field.square`, `slides.layout.tile-sentences`, `slides.layout.plate-four-columns` | not driven by this lane                        | their drivers are B4's specs and the help, share and slides areas of the walk, which this lane did not run; section 6 |
| `present.presenter.sentence-case`                                                                                                                                                                                                                                                                                                                              | not done                                       | R9                                                                                                                    |

### 4. The pictures

Under `docs/gslides-parity/product/build/b1/`: `before-<size>-<appearance>-<surface>.png` are the interface audit's production pictures of the surfaces below, copied beside `shoot.mjs` (this folder), the script that shoots the after pictures with the audit's helpers on one scratch deck from `/new`, trashed and deleted forever at the end, at 1440 by 900 and 1280 by 800 in both appearances against `--base`. The after pictures were not shot: the run waited for `.turboslide/e2e.lock` from 21:06 to 21:25 while two other lanes' core walks (ports 4413 and 4414) and a gate run (4417) held it, and this lane returned before the lock freed. The integrator or the verifier runs `node docs/gslides-parity/product/build/b1/shoot.mjs --base <origin>` under the lock against a server carrying this lane's files, and the pictures land beside the before ones under the names below. The four production pairs of the next paragraph needed no lock and were shot. The surfaces the script shoots: `home`, `notfound`, `access`, `editor`, `title-row`, `toolbar-hover`, `menubar-hover`, `tooltip-new-slide`, `menu-file`, `toolbar-tail-text`, `filmstrip`, `toolbar-tail-table`, `toolbar-more` (when the bar folded), `plate-layout`, `dialog-name-prompt`, `dialog-share`, `dialog-share-more`, `panel-comments-empty`, `panel-version-history`, `key-question`, `show-entry`, `show-after-3s`, `decks`, `decks-after-trash-snackbar`, `trash`, `trash-confirm`. A picture named here that is absent from the folder was not shot.

The four pairs section 3 of PRODUCT.md asked for first, under `docs/gslides-parity/product/audit-interface/`, shot against production (`main` at `cbd4495`) at 21:08 to 21:12 by `audit-interface/scripts/after-b1.mjs` with a fresh context per appearance, before any token change reached a deployment: `before-dark-toolbar-hover.png`, `after-dark-toolbar-hover.png`, `before-dark-filmstrip-numbers.png`, `after-dark-filmstrip-numbers.png`, `before-dark-format-options-labels.png`, `after-dark-format-options-labels.png`, `before-light-menu-frame.png`, `after-light-menu-frame.png`, `before-dark-menu-frame.png`, `after-dark-menu-frame.png` (the File menu with `--pt-edge` injected on `.ts-menu`). The scratch deck `untitled-20260920-h76f` was trashed and deleted forever and answers 404 on `/deck`.

### 5. Tests and commands

- `packages/chrome`: `node_modules/.bin/tsc -b packages/chrome` passes for this lane's files (three errors stand in other lanes' in progress files: `packages/render/src/slide.ts` 389, `packages/chrome/src/dialogs/ImportSlides.tsx` 42, `packages/chrome/src/inspector/palette.tsx` 126; `packages/fonts/src/catalog.ts` 26 and 27). `vitest run` in `packages/chrome`: 63 files pass; the two that fail are other lanes' (`inspector-sections.test.tsx`, B5a's accent token; `palette-data.test.ts`, R12).
- New and changed tests: `__tests__/toolbar-fold.test.ts` (new: `foldPlan`'s order and floor, `selectionMarks`), `download-dialog.test.tsx` (the one click paths, the table rule, the file names, the progress sentence, the options dialog closing itself), `share-dialog.test.tsx` (the first stage, the swap, More, You, the name prompt once; the existing tests open More first), `layout-grid.test.tsx` (the disclosure, the caption, the arrows by column), `menu-component.test.tsx` (no marks on macOS, the mark and the Alt underline on Windows), `editor-shell-render.test.tsx` (no bottom bar, the title row toggle opens and closes the panel, the GT group), `presence-slot.test.tsx` (the new slot), `export-menu.test.tsx` (hover, not focus, inside a dialog), `apps/studio/src/routes/-recent.test.ts` (the trashed marker); `packages/chrome/src/menus/__tests__/menu-model.test.ts`'s Font assertion of 8.3 is B5a's half.
- `apps/studio`: `vitest run src/routes/-recent.test.ts src/components/home/copy.test.ts` passes. `tsc -b apps/studio` passes for this lane's files; the errors that stand are `packages/schema/src/brand.test.ts` (B5a), `apps/studio/src/server/assist*.ts` (B6, B7), `apps/studio/e2e/core/chrome.spec.ts` 62 (B4), `apps/studio/src/server/templates.ts` 97 (B5b). The dev server on 4411 logged one route generator error from B5b's in progress `apps/studio/src/routes/decks.templates.tsx` ("Identifier 'Dialog' has already been declared"); every other route served.

### 6. What this lane did not do, and why

- The 300 ms rule of 3.5 (1.12): `new.tsx` passes `frames={1}` since 21:20; `pendingMs` stays 0 on both routes for the reason 1.12 gives.
- The after pictures of section 4 and a second chrome walk (for `chrome.presence.tooltip` after its fix and `chrome.separators.once`): not run, the e2e lock was other lanes' for the whole of this lane's last twenty minutes.
- The dev server on 4411 was stopped at 21:26 before this lane returned; the waiting picture run was stopped with it and took no lock.
- `present.presenter.sentence-case`: the words are the viewer's (R9).
- The rows this lane's files serve but did not drive (section 3): the gate's specs and the help, share, decks and present drivers are B4's and were written during this lane's build; this lane ran the chrome area alone and shot the pictures. A row this lane did not drive is not driven, never passed.
- Files outside section 7's list this lane touched, each for one rule its item needs: `packages/chrome/src/brand.css` (the skeleton's sheet plate, the Not found buttons' 40 px), `packages/chrome/src/comments/comments.css` (the empty state, the filter, the search field's boundary), `packages/chrome/package.json` (the export `./dialogs/DeleteForever`, so the trash page can mount the dialog), `packages/chrome/src/__tests__/export-menu.test.tsx` (the focus rule changed what the test asserted). The prompt's Owns list names `packages/chrome/src/**.css` and the tests of the owned components; the manifest line is the one change with no other home.
- Requests R1 to R12 above.

## Product round fix round

The B1 fixer, 2026-09-20 (times UTC), on VERIFICATION.md "Product round, pass 1" findings 10 and 14 as the fixer's prompt names them (its findings 1 and 2). Own server `TURBOSLIDE_STORE=tmp TURBOSLIDE_REALTIME=memory` on 4411, stopped before this lane returned; every Playwright run against it under `.turboslide/e2e.lock`; no git write command, no `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build`; the round five branch untouched; no token printed. The one file this round changed is `apps/studio/src/routes/decks.css`; everything else below is a measurement or a request.

A second pass of the same fixer ran 2026-09-21 05:46Z to 06:20Z on the tree as it stood (the first pass's `decks.css` change in place, none of R15 to R19 answered: `b4.md` and `integrator.md` predate the first pass, `room.ts` line 2003 and `actions.ts` `account.setName` unchanged, `decks.spec.ts` line 1538 still observes `document.documentElement`). Same server, same rules. The first pass's F3 ended before its rerun landed; this pass reran both rows and the share row and recorded them below. The machine carried a load average of 105 to 143 through the pass (eslint, three `tsc` runs from other worktrees and a Chrome for Testing of other sessions), which the timings below carry: the dev server's first save of a fresh deck took 82 s cold (draft check 53 s, create 28 s) and 26 s on the second deck, so the drivers' 30 s `newDeck` waits ran out twice before the write path was warm and the two rows ran on the third start.

### F1 What was found, by finding

- Finding 1, `decks.new.skeleton-one-frame` (the driver, not the product). The product draws the one frame skeleton: the server's HTML for `/new` carries `<div class="pt-viewer is-editor ts-skeleton" data-skeleton="editor" data-frames="1">` with one `.ts-skeleton-card`, no `canvas` and no `img` (read with curl from 4411 and, for the three frame skeleton of `main`, from production), and a `MutationObserver` on `document` installed by `addInitScript` saw it at 786 ms (the dev server's SSR answer) and saw it leave at 1276 ms when the editor stood: some 490 ms of skeleton. The spec's observer never saw it because it is attached to `document.documentElement`, which is `null` when a Playwright init script runs (`readyState` "loading", `document.body` null too): `new MutationObserver(read).observe(document.documentElement, …)` throws `TypeError: Failed to execute 'observe' on 'MutationObserver': parameter 1 is not of type 'Node'`, the init script ends there, `__skeleton.first` stays null, and the row's `facts.first === null` branch asserts the editor was ready inside 300 ms, which no first load is (657 to 832 ms). The 300 ms rule of PRODUCT.md 3.5 governs a client side navigation, and under `ssr: 'data-only'` the skeleton is the server's markup for the route (b1.md 1.12), so the first load always draws it and the row's `else` branch (one frame, no figure) is the branch that should run. Request R15 below; nothing in this lane's files changes the outcome, and the row stays red until the driver reads the document. The second pass measured the claim three times on 4411 with an init script that records the document's state at its run time and then installs the spec's observer inside a try: every run read `readyState` "loading", `document.documentElement` null, `document.body` null and the spec's call throwing `TypeError: Failed to execute 'observe' on 'MutationObserver': parameter 1 is not of type 'Node'`; the fallback observer on `document` then saw the skeleton with `frames` 1 and `canvases` 0 and saw it leave when the editor stood (first seen at 2636, 6698 and 9128 ms, gone at 30452, 9568 and 14391 ms: the dev server's cold compile under the machine's load, not the product's timing; the served HTML was read again with `grep -a`, the file carries bytes plain grep reads as binary). The integrator's note (`integrator.md` line 269) reads the row as a timing measurement; it is the observer's target, and no editor time changes it.
- Finding 1, `decks.card.edited-relative-time` (this lane, fixed). Two causes together, both measured: Chromium sizes a collapsed border cell's `height` as the content box, so `.ts-rows td { height: 32px }` with the 1 px `border-bottom` measured the `tr` at 33; and the row's more button is a `.pt-ib.pt-icon`, 32 px tall by `tokens.css`, so the cell's content forced 33 on its own even at a 31 px cell. A static page carrying the same rules over `file://` (no lock) read: as written 33; the button at 28 px alone 33; the cell at 31 px alone 33; the button at 28 px with the cell at 31 px 32; the button at 28 px with the cell at `height: 32px; box-sizing: border-box` 32. `decks.css` now declares the cell at 32 px `border-box` (the row's 32 include its rule) and `.ts-rows .pt-ib.ts-hm-card-more` at 28 by 28 (the selector outranks `.pt-ib.pt-icon`'s 32 px whatever order the sheets load in); the 16 px glyph stays centred. The row was rerun on 4411 after the change; its result is in F3.
- Finding 2, `share.name-prompt.first-share` (the server; requests R16 to R18). The prompt, its words and `account.setName` work: the record gets the name (`apps/studio/src/server/auth/actions.ts` `account.setName` writes `principals.put({ …record, name })`). The name then fails to reach the second browser's chip for three reasons, none in this lane's files:
  1. On every tier, `apps/studio/src/server/room.ts` `rosterEntryForReader` (line 1932) shows a named person to a reader admitted by link or by the open record as the role word unless `showNamesToLinkVisitors` is on: `if (entry.trust === 'label' || entry.trust === 'agent') return entry;` lets the generated labels through and turns both a typed name (`trust: 'guest'`) and a verified account (`trust: 'verified'`) into "The owner" with an initial mark. That is the local reading, "The owner · slide 1": the name had reached the record and the roster, and the filter hid it. The chrome's own rule (`packages/chrome/src/presence/presence-model.ts` `displayNameFor`) hides `verified` alone, and PRODUCT.md section 2 rank 4 says the typed name is what "the presence chips, the Share dialog and Version history show" to collaborators; the prompt's own words are "Your name, shown to collaborators". The typed name is the person's consent to be seen by the people the link admits; 0.12's protection is for account names and addresses.
  2. On the blob tier the name never reaches the roster at all: the principal store is a file store under the instance's state folder (`apps/studio/src/server/auth/principal.ts` `selectPrincipalStore` without Redis; `access.ts` line 172 records the same fact for link grants), so the record `account.setName` wrote on one instance is unknown to the instance that serves the owner's next presence heartbeat, whose `rosterEntryFor` resolves `identity.record` from its own file store and writes `label: "Iridium 594"` again. That is the preview reading. The link grants crossed instances by riding the principal's deck index on the Blob store (`users/<principalId>/decks.json`, `noteLinkGrant`, `linkGrantsFromIndex`); the display name has no such carrier.
  3. The time: the owner's tab re-posts presence on its 5 s heartbeat alone (`packages/realtime/client/room-client.ts`; nothing in `controller.tsx` touches presence after `account.setName` resolves), and `room.ts` caches the resolved identity 5 s per instance (`IDENTITY_CACHE_MS`), so even with 1 and 2 fixed the chip can read the old label for up to 10 s where the matrix says 5 s. The presence store's budget is not in the way: a label change is a material change (`presence-store.ts` `PRESENCE_VOLATILE_FIELDS` leaves the identity fields in the body).

### F2 Requests

- R15, B4, `apps/studio/e2e/core/decks.spec.ts` lines 1520 to 1546 (`decks.new.skeleton-one-frame`): observe `document` instead of `document.documentElement` (`new MutationObserver(read).observe(document, { childList: true, subtree: true, attributes: true })`), or attach when `document.documentElement` exists; the init script runs before the document has an element, and the row has read "no skeleton was drawn" on every run of this round for that reason alone. With that change the row reads the one frame skeleton the server sends (F1 above; `frames` 1, `canvases` 0). The `else` branch's two assertions are the row's real check on a first load; the `first === null` branch stays for a client side navigation, where the 300 ms rule of PRODUCT.md 3.5 applies.
- R16, the integrator, `apps/studio/src/server/room.ts` `rosterEntryForReader` (line 1932): a typed name passes through to a reader admitted by link (`if (entry.trust === 'label' || entry.trust === 'guest' || entry.trust === 'agent') return entry;`), so the filter hides verified accounts alone, as the chrome's `displayNameFor` already does and as PRODUCT.md section 2 rank 4 asks; the role word stays for `verified` while the owner's switch is off. `packages/chrome/src/__tests__/presence-model.test.ts` covers the chrome's side; the server's unit test of the filter (if one names the guest case) changes with it.
- R17, the integrator with B7, the display name across instances on the blob tier: the name needs a carrier every instance reads, the way `noteLinkGrant` gave the link grants one. One shape: `account.setName` also writes the name onto the principal's deck index (`packages/store/src/access-store.ts` `DeckIndex`, an optional `name?: string` in the type and `deckIndexSchema`, an `indexUpdates.name(name)` update), and `room.ts` `resolveIdentity` (or `cachedIdentity`) falls back to the index's name when the file record carries none, past the same 5 s cache the grants use. Another: the presence record's identity fields are re-resolved from a Blob backed principal record. Either way the presence POST body stays identity free (report 10 F31). Without a carrier the row cannot pass on the deployment, whatever the filter does.
- R18, the integrator (B3 owns `controller.tsx`'s named regions; this is outside them), the time after a rename: after `account.setName` resolves, the room client re-posts presence at once (a `room-client.ts` method that sets `presenceDirty` and `schedulePresence(0)`, called from the controller where `account.setName` returns), and `room.ts` drops the `identityCache` row for that identity on `account.setName` (same instance) so the next post resolves the new record. With R16 and R17 this brings the chip inside the matrix's 5 s; without it the row rides the heartbeat and the cache, up to 10 s.
- R19, the integrator, PRODUCT.md section 9 by the parks rule if R16 and R17 do not land this round: `share.name-prompt.first-share` cannot be made green from this lane's files; the prompt and its save work by hand and stay; the row's second half (the chip in the other browser) is the server's.

### F3 The rows rerun

- Before any change, both rows on 4411 (12:38Z, under the lock): `decks.card.edited-relative-time` failed, "Expected: 32, Received: 33"; `decks.new.skeleton-one-frame` failed, "no skeleton was drawn; the editor was ready inside 300 ms", 657.7 ms.
- After the `decks.css` change, the second pass on 4411 under the lock (`PLAYWRIGHT_BASE_URL=http://localhost:4411 node_modules/.bin/playwright test apps/studio/e2e/core/decks.spec.ts --grep "decks.new.skeleton-one-frame|decks.card.edited-relative-time"`): the first two starts (05:55Z, 06:01Z) died in the file's `beforeAll`, `newDeck`'s `page.waitForURL(/\/edit\//)` at 30 s while the page read "Saving…" (the server's first save took 82 s and 26 s cold under the load; the client's aborted POSTs are the server log's 500s), so neither row ran; the third start (06:14Z, the write path warm) ran both: `decks.card.edited-relative-time` passed in 2.8 s, annotation `when "Opened just now"; list row 32 px`; `decks.new.skeleton-one-frame` failed in 1.0 s with `{"first":null,"frames":null,"canvases":null,"gone":null}; ready at 850 ms` and the message "no skeleton was drawn; the editor was ready inside 300 ms", the `first === null` branch of F1, the observer never installed.
- `share.name-prompt.first-share` on 4411 under the lock (06:16Z, `core/share.spec.ts --grep share.name-prompt.first-share`): failed in 16.2 s; the prompt opened with its words, Copper Lane saved, the Share dialog followed, the guest opened the address and its chip read `The owner · slide 1 O` (the aria label and the initial) through the 10 s poll. The reading is F1 finding 2's cause 1 on the current tree: a generated label would have passed `rosterEntryForReader` unchanged, so the typed name had reached the roster with `trust: 'guest'` and the filter replaced it with the role word for the link visitor. No file of this lane changed for the row; R16 to R18 stand unanswered.

### F4 Tests

- `packages/chrome`: `node_modules/.bin/vitest run` 72 files, 655 tests pass (12:56Z). `apps/studio`: `vitest run` 69 files, 461 tests pass (12:56Z). No test reads `decks.css`.
- The second pass, 2026-09-21 06:15Z on the tree as it stood: `packages/chrome` 72 files, 655 tests pass in 16.7 s; `apps/studio` 69 files, 461 tests pass in 13.5 s. No TypeScript file of this lane changed in either pass, so `tsc -b` was not rerun for them. The server on 4411 was stopped and the lock was free when this pass returned; every deck the rows created lived in the server's tmp store, and no deck was created on a deployment.
