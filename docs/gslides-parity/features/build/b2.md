# B2, text and fonts

## Features round, ship one

Builder B2 of the features round, ship one (docs/FEATURES.md section 3, the lane table of section
6, the rows of 7.1 and the named tests of 7.3), 2026-09-22 on the shared checkout at `88b68e7`
(the tree was clean when this lane started; every change outside the Owns list below is another
lane's). The binding text is FEATURES.md 3.1 (the Inter verdict, five items), 3.2 (the eight
families), 3.3 (the size budget), 3.4 (the export path) and 3.5 (the P1 items this lane supplies);
the evidence is `docs/gslides-parity/features/audit-fonts.md` items 2, 4, 5, 6, 7, 12, 13 and 16
with `audit-fonts/` beside it. Dev server port 4412 (`apps/studio/vite.no-watch.config.ts`, the
tmp store, the memory channel, `TURBOSLIDE_LOCAL_OPEN=1`), stopped before the report. No git write
command, no `pnpm` command, no `vite build`. Scratch decks lived in the tmp store of the dev server
alone and each was trashed and deleted forever at the end of its run (the driver's finally block;
`/edit/<id>` answered 404); nothing was created on a deployment.

### Owns, as read from FEATURES.md section 6

`packages/fonts/**` (the assets, `src/inter.ts`, `inter.css`, `inter.test.ts`, `summary.ts`,
`catalog.ts`, `catalog.test.ts`, `names.ts`, `export/**`, `scripts/fetch-fonts.mjs`,
`scripts/catalog-light.mjs`, the generated `catalog-files.ts` and `catalog-light.ts`, the new
`woff2-names.ts`), `scripts/build-fonts.py`, `packages/schema/src/fonts.ts`, `typography.ts`,
`packages/theme/src/gt-ink-paper/sheet.css`, `tokens.ts`, `tokens.test.ts`,
`packages/chrome/src/tokens.css`, `packages/identity/src/marks-render.ts`,
`packages/render/src/theme-css.ts`, `theme-css.test.ts`, `fonts.ts`, `fonts.test.ts`,
`packages/chrome/src/inspector/typography.tsx`, `typography.css`,
`apps/studio/src/routes/__root.tsx`, the fonts paragraphs of `THIRD_PARTY_NOTICES.md`.

### 0. Requests, written first

Each is a change in a file another lane or the integrator owns. The hunks are written so they can
be applied as they stand.

- R1 (the integrator, `packages/chrome/src/menus/model.ts`; FEATURES.md 3.1 item 4, the shared
  file table of section 6 "B2 the `terms` of the Tabular figures row"): a row in the `format.text`
  submenu after `format.text.font` that opens Format options at its Text section, so Search the
  menus lists Tabular figures for a seller who never heard the word:

  ```ts
  now('format.text.tabularFigures', 'Tabular figures', panel('Format options'), {
    enabled: 'textBlockSelected',
    turboslide: true,
    doc: 'Every digit takes the same width, so numbers line up in a column',
    terms: ['tabular', 'numbers', 'digits', 'line up numbers', 'align numbers'],
  }),
  ```

  `finder.ts` `sellerTermsOf` already reads an item's own `terms` array, so no finder change is
  needed. The row's control on the panel is `formatOptions.typography.numerals`
  (`inspector/typography.tsx`, this lane); the menu row and the panel row are the two ways in. The
  half of the matrix row `formatting.numerals.tabular-row` that reads "Search the menus 'line up
  numbers' lists the row" is red until this lands (section 3 below); the other four readings of
  that row pass on 4412. The named test of 7.3 in `menu-model.test.ts` ("'line up numbers' with
  Tabular figures") is B1's and reads this row.

- R2 (B4, `scripts/probes/core-matrix.mjs` `CONTROL_SOURCE_PATHS`): add
  `'../../packages/chrome/src/inspector/typography.tsx'`, so the `parks` id
  `formatOptions.typography.numerals` of the row `formatting.numerals.tabular-row` validates
  (the id is a string literal there, `NUMERALS_CONTROL`, with `.sentence` for the sentence element).
- R3 (B4, `scripts/probes/core-walk/areas/fonts.mjs` and the matrix): the area's own `FONT_IDS`
  list holds the product round's 26 ids; the catalog is 34 (section 2.6), so the rows
  `fonts.agent.font-list` ("answers 26 rows") and `fonts.more-fonts.licence` ("lists the 26
  families") read 34 on this tree; the interaction texts and the list want the eight ids appended
  in `FONT_IDS` order (`geist`, `geist-mono`, `instrument-sans`, `manrope`, `bricolage-grotesque`,
  `schibsted-grotesk`, `newsreader`, `fraunces`). The categories the drivers read are Google's
  METADATA.pb categories at the pinned commit, which the fetch script records: Bricolage Grotesque
  is `sans` and Fraunces is `serif` there, not the Display group FEATURES.md 3.2 wrote them under
  (section 2.6 records the reading); `fonts.catalog.six-families` passed on 4412 with the six rows
  in those groups.
- R4 (B1, `packages/chrome/src/font-picker-model.ts`): the two changes FEATURES.md named for this
  lane's rows are already on the tree in B1's working copy as this note is written (the eight
  `DIRECTORIES` entries, and `licenceUrlOf` naming `v4.1` through `INTER_RELEASE_TAG`); the row
  `fonts.links.licence-v4-1` read green on 4412 with them (section 3). One line to add when B1
  touches the file next: the constant may read `INTER_LICENCE_URL` from `@turboslide/fonts/names`
  (the browser half; this lane put `INTER_RELEASE` and `INTER_LICENCE_URL` there so the picker,
  `summary.ts` and `inter.ts` read one string), but the chrome depends on the renderer and not on
  the fonts package, so the local constant stands unless the integrator prefers the import through
  `@turboslide/render/fonts`.
- R5 (the integrator, `packages/schema/src/blocks.ts`; B3, `packages/render/src/blocks/table.ts`;
  B1, `font-picker-model.ts` `takesFamily`; the P1 item "a table takes a family", audit-fonts 13,
  row `fonts.table.takes-family`): the table block schema (`tableBlockSchema`, blocks.ts 1917)
  carries no `typography` field, `renderTable` writes no `typographyDeclarations` on the `.table`
  root, and `takesFamily` answers false for `block.type === 'table'`, so nothing on this lane's
  files alone lets a table take a family. The three hunks: `typography: typographySchema` in the
  table fields (the schema, one line beside `shadow`); `...typographyDeclarations(block.typography)`
  in `renderTable`'s `inline` style (the cells inherit the family and the tabular figures rule of
  sheet.css still applies per cell); `takesFamily` returning true for a table. The Editable text
  PowerPoint then names the face in the cells through `pptx/table.ts` 124 `familyFor` with no
  export change (FEATURES.md 3.4). The row is not driven on this lane's server for that reason
  (section 3).
- R6 (the integrator, `pnpm generate:contracts` at the merge): `typographyObjectSchema` gained the
  optional `numerals` field (`'tabular'`), so the generated JSON schema of `block.set`'s typography
  value, the MCP tool list and openapi.json change by one property; no action id changed and
  `block.set /typography/numerals` is a pointer on the existing action (the shared file table's
  "B2 lands nothing" in actions.ts holds).

### 1. What changed, by FEATURES.md item

Every item is one set of hunks named with its files so a red row's item can be reverted alone in
the fix round.

#### 1.1 The 4.1 italic (3.1 item 1; audit-fonts 4; row `fonts.inter.italic-release`)

- `packages/fonts/assets/InterVariable-Italic.woff2`: replaced with `web/InterVariable-Italic.woff2`
  of `Inter-4.1.zip` (fetched from
  https://github.com/rsms/inter/releases/download/v4.1/Inter-4.1.zip, 33,707,794 bytes, into the
  scratchpad; the zip's `web/InterVariable.woff2` hashed `693b77d4…a8e3`, byte for byte the
  committed upright, as the audit found). The new file: 387,976 bytes, sha256
  `e564f652916db6c139570fefb9524a77c4d48f30c92928de9db19b6b5c7a262a`, name table "Inter Variable"
  "Italic" "Version 4.001;git-9221beed3", head created 2024-11-16, fsType 0, GSUB without `c2sc`
  and `smcp` (the 4.1 changelog's removal). The replaced file was 380,904 bytes, `0470791f…cc05`,
  "Version 4.000;git-a52131595".
- `packages/fonts/src/inter.ts`: `INTER_ITALIC.bytes` 387976, `sha256` as above, `release`
  `INTER_RELEASE`; `INTER` gained `release` and `path` too; `INTER_NAME_VERSION`
  ("Version 4.001;git-9221beed3") for the tests; the header names the v4.1 release and the
  replaced file. `version: '4.001'` stays on both (the font's internal version).
- `packages/fonts/src/names.ts`: `INTER_RELEASE` (`https://github.com/rsms/inter/releases/tag/v4.1`)
  and `INTER_LICENCE_URL` (`https://github.com/rsms/inter/blob/v4.1/LICENSE.txt`), in the browser
  half so `summary.ts` (no `node:` import) and `inter.ts` read one constant; `inter.ts` re-exports
  both.
- `scripts/build-fonts.py`: the `source.italic.release` string names `v4.1`; the docstring says
  which italic the twins are cut from. Rerun through `.turboslide/venv/bin/python
scripts/build-fonts.py`: the 17 italic instances under `packages/fonts/export/*-Italic.ttf`
  are rebuilt from the 4.1 file (each 345,340 to 346,360 bytes; the 4.0 cuts were 338,328 to
  339,392) and `fonts.json` records the new source (`version`, `sha256`, `bytes`, `release`) and
  the 17 new digests; the 17 upright instances are byte for byte unchanged (the build is
  deterministic and `git status` lists no upright file); `build-fonts.py --check` answers 0 stale
  files; `fonts.json` `version` stays `4.001+gt.2`.
- `packages/fonts/src/catalog.ts`: the present Inter row's `directory` reads `rsms/inter v4.1 web/`.
- `packages/fonts/src/catalog-files.ts` (generated): the Inter row's italic file carries the new
  bytes, sha256 and source string.
- `packages/fonts/src/inter.css`: the header names the v4.1 release.
- `packages/fonts/src/export.test.ts`: the release pin reads `v4.1`.
- `THIRD_PARTY_NOTICES.md`, the Inter section: both files with their sha256, bytes and name
  table version, the release tag v4.1, the licence URL, and the sentence recording the replaced
  4.0 italic and the corrected tag.
- `packages/fonts/src/woff2-names.ts` (new): a Node reader of a woff2's table directory, name
  table (Windows strings), and GSUB feature tags, ported from the audit's `woff2-names.mjs`, so
  `inter.test.ts` reads the version from the file's bytes and `catalog.test.ts` reads every
  family's `tnum` from its committed file.
- `packages/fonts/src/inter.test.ts`: the italic test reads bytes, sha256 and the name table
  version (`woff2Facts`) against the constants and asserts the 4.1 facts (no `c2sc`, no `smcp`,
  `tnum` present; the upright's `cv11` and `ss01`); a new test pins both release URLs, both
  licence URLs and `fonts.json` `source.italic` to the v4.1 tag and asserts no `v4.001` in the
  notices' Inter section or in `inter.css`.

#### 1.2 The two dead links (3.1 item 2; audit-fonts 5; row `fonts.links.licence-v4-1`)

- `packages/fonts/src/summary.ts` `licenceUrl`: Inter answers `INTER_LICENCE_URL` (v4.1).
- `packages/chrome/src/font-picker-model.ts` 261 `licenceUrlOf`: B1's line, on the tree in B1's
  working copy (R4).
- `packages/fonts/scripts/fetch-fonts.mjs`: every processed family's `licenceUrl` (the summary's
  function, so the same string More fonts carries) is fetched at the end of the run and its status
  printed; a status other than 200 is a failure of the run (exit 1) and never stops the files
  being written; `--skip-licence-check` turns it off for an offline run. The check chain never
  runs the script. The run of section 2.6 read 200 for all nine URLs (Inter's and the eight).

#### 1.3 The fallback face in the stacks (3.1 item 3; audit-fonts 6; row `fonts.fallback.in-stack`)

- `packages/theme/src/gt-ink-paper/sheet.css` `--display` and `--text`:
  `'Inter', 'Inter Fallback', 'Helvetica Neue', Arial, sans-serif`.
- `packages/theme/src/tokens.ts` `FONTS.display` and `FONTS.text`: the same string (the parity
  test compares the two).
- `packages/chrome/src/tokens.css` `--pt-display` and `--pt-text`: the same order.
- `packages/identity/src/marks-render.ts` 344: the initials' `font-family` reads
  `Inter, 'Inter Fallback', system-ui, sans-serif`.
- `packages/fonts/src/inter.css`: the fallback face's `src` is `local('Arial'), local('Liberation
Sans')`; `inter.ts` `INTER_FALLBACK.localSecond` and `fallbackFontFaceCss()` write the same
  (the metric numbers are untouched; `inter.test.ts` pins `fonts.json` `fallback` against the
  descriptors minus `localSecond`).
- `packages/theme/src/tokens.test.ts`: the sheet's computed stack is pinned family by family
  (`'Inter'`, `'Inter Fallback'`, then Helvetica Neue, Arial, sans-serif).
- Not changed: `packages/headless/src/sheet.ts` 135 writes its own `'Inter', 'Helvetica Neue',
Arial, sans-serif` on the capture document's body (the headless package is not this lane's and
  the capture never paints before its fonts are inlined); recorded for the integrator.

#### 1.4 Tabular figures (3.1 item 4; audit-fonts 2; rows `tables.cells.tabular-figures`, `formatting.numerals.tabular-row`)

- `packages/theme/src/gt-ink-paper/sheet.css`: one rule after the table sizes sets
  `font-variant-numeric: tabular-nums` on `.ts-sheet .table .td`, `.ts-sheet td`, `.ts-sheet th`
  (an imported block's cells), `.ts-sheet svg.chart .grid text` (the value axis tick labels),
  `.ts-sheet svg.chart .categories text`, `.ts-sheet svg.chart .value` and the status board's
  `.board .state` and `.board .who small` (the state and the address); running text (`p`, `.lead`,
  the headings) stays proportional. The chart and board layout rules stay in
  `packages/render/src/block-css.ts` (B3's file this round); the tnum rules sit in sheet.css so
  they are one place, and no property collides. The table comment that had kept the numerals
  proportional (the parity fix round's export finding) is rewritten to say why the sheet's
  alignment now wins and where the export difference is recorded (section 5, open item 1).
- `packages/schema/src/typography.ts`: `TYPE_NUMERALS = ['tabular']`, `Typography.numerals`,
  the annotated field (`label: 'Tabular figures'`, `control: 'toggle'`, `group: 'Text'`, `help`
  the sentence), `NUMERALS_SENTENCE` ("Every digit takes the same width, so numbers line up in a
  column") and `NUMERALS_UNAVAILABLE` ("This face has no tabular figures");
  `typographyDeclarations` emits `font-variant-numeric:tabular-nums` for `numerals: 'tabular'`
  (after the family). The write is `block.set /typography/numerals` on every transport (R6).
- `packages/chrome/src/inspector/typography.tsx`: the `numerals` field draws as `NumeralsRow`, a
  check row with the label, the sentence under it (`formatOptions.typography.numerals.sentence`),
  a native checkbox carrying `data-control="formatOptions.typography.numerals"` that writes
  `'tabular'` on and removes the field off, the on or off word, and the tooltip with the same
  sentence; the row reads the block's family (the theme's Inter when absent) and is disabled with
  the sentence and tooltip "This face has no tabular figures" when `hasTabularFigures(family)` is
  false. `NUMERALS_CONTROL`, `numeralsFamily` and `numeralsAvailable` are exported for a test.
  `typography.css`: the stacked label, the 11 px sentence, the disabled tint.
- The `tnum` flag pipeline: `fetch-fonts.mjs` records `features` (the upright file's GSUB tags)
  and `tnum` per family, for a fetched family from its facts and for a family a `--only` run
  keeps from its committed woff2 files (`withLocalFeatures`, so no row is left without the two
  fields); `catalog-light.mjs` carries `tnum` into `catalog-light.ts` (`LightFamily.tnum`);
  `summary.ts` `hasTabularFigures(id)`; `packages/render/src/fonts.ts` re-exports it for the
  chrome (the chrome depends on the renderer, not on the fonts package). The flags: 19 of the 34
  families carry `tnum` (Inter, Roboto, Open Sans, Lato, Montserrat, Source Serif 4, Merriweather,
  Lora, EB Garamond, Work Sans, Space Grotesk, Bebas Neue, Fira Code, Geist, Instrument Sans,
  Manrope, Bricolage Grotesque, Schibsted Grotesk, Newsreader); 15 do not (Poppins, Source Sans 3,
  Playfair Display, PT Serif, Libre Baskerville, Nunito, Raleway, DM Sans, Oswald, Roboto Mono,
  JetBrains Mono, IBM Plex Sans, IBM Plex Mono, Geist Mono, Fraunces). The audit's
  `catalog-features.txt` counted 12 of the 25 plus Inter; the script's read of the committed files
  agrees on every one of those and adds Bebas Neue's static file (the audit's line lists `tnum`
  for it too) and Fira Code.
- `catalog.test.ts`: every row carries the two fields, `tnum` equals the committed upright file's
  GSUB (`woff2Facts`), the light table and `hasTabularFigures` agree, and six named faces are
  pinned (Inter, Bebas Neue and Geist yes; Playfair Display, Fraunces and Geist Mono no).
- `tokens.test.ts`: the tnum selectors are pinned present and the running text selectors absent.
- The `terms` of the row for Search the menus: R1.

#### 1.5 The display features gated on Inter (3.1 item 5; audit-fonts 7; row `fonts.display-features.inter-only`)

- `sheet.css`: `--display-features: 'cv11', 'ss01'` on `.ts-sheet` beside the stacks;
  `.ts-sheet h1, h2, .big` read `font-feature-settings: var(--display-features)`.
- `packages/theme/src/tokens.ts`: `DISPLAY_FEATURES_TOKEN` (`display-features`) and
  `DISPLAY_FEATURES_OFF` (`normal`); `DISPLAY.features` unchanged.
- `packages/render/src/theme-css.ts`: `displayFeatures(display)` answers `DISPLAY.features` for
  `inter` and `normal` for every other family; `themeCss` writes
  `--display-features: <value>` beside `--display` when the kit sets `fonts.display`.
- `packages/schema/src/typography.ts` `typographyDeclarations`: `font-feature-settings:normal`
  when `family` is set and is not `inter` (the block's inline style beats the heading rule).
- Tests: `tokens.test.ts` (the root's value equals `DISPLAY.features`, the heading rules read the
  variable, no literal `cv11` outside the `.gt` word rules) and `theme-css.test.ts` (Fraunces and
  Playfair Display write `normal`, Inter writes the pair, the text role alone writes nothing, the
  rendered slide carries the declaration; `typographyDeclarations` for a family, for Inter, for
  `numerals` and for both).

#### 1.6 The eight families (3.2; audit-fonts 3 and 8; rows `fonts.catalog.geist`, `fonts.catalog.six-families`)

- `packages/schema/src/fonts.ts` `FONT_IDS`: `geist`, `geist-mono`, `instrument-sans`, `manrope`,
  `bricolage-grotesque`, `schibsted-grotesk`, `newsreader`, `fraunces` after `fira-code` (34 ids).
- `packages/fonts/src/catalog.ts` `FONT_SOURCES`: the eight rows, every one `take: variable`, the
  directories of FEATURES.md 3.2.
- `packages/fonts/src/names.ts` `FONT_NAMES`: the eight names as Google and PowerPoint write them.
- The fetch: `node packages/fonts/scripts/fetch-fonts.mjs --only
inter,geist,geist-mono,instrument-sans,manrope,bricolage-grotesque,schibsted-grotesk,newsreader,fraunces`
  at `GOOGLE_FONTS_COMMIT` `1ac2012c…` with fontTools 4.65.0 from `.turboslide/venv`; the eight
  asset folders under `packages/fonts/assets/<id>/` with their `LICENSE` (every one OFL 1.1,
  no Reserved Font Name), `catalog-files.ts` regenerated with 34 rows, then
  `node packages/fonts/scripts/catalog-light.mjs` and prettier on the two generated files. The
  woff2 bytes: Geist 69,840 + 73,056; Geist Mono 71,120 + 78,736; Instrument Sans 89,024 +
  94,780; Manrope 53,732; Bricolage Grotesque 205,036; Schibsted Grotesk 70,084 + 74,940;
  Newsreader 214,916 + 239,064; Fraunces 194,936 + 235,660; 1,764,924 bytes in all (the audit's
  estimate was 1,764,484; this venv's brotli packs a few files a few hundred bytes differently),
  1,824 KB on disk by `du`, about 12 percent on the catalog's 14.4 MB (3.3). The TrueType sources
  were 4,489,040 bytes for the 16 files including Inter's two present files.
- Categories, as Google's METADATA.pb records them at the pinned commit (the catalog's rule since
  the product round: "Google's category"): Geist, Instrument Sans, Manrope, Bricolage Grotesque
  and Schibsted Grotesk `sans`; Newsreader and Fraunces `serif`; Geist Mono `mono`. FEATURES.md
  3.2 wrote Bricolage Grotesque and Fraunces under Display; the fetch script has no hand override
  and the picker groups by the recorded category, so they sit under Sans serif and Serif (R3).
- `THIRD_PARTY_NOTICES.md`, the catalog section: thirty three families named, then one paragraph
  per added family with its source files, byte sizes, copyright line and licence.
- `catalog.test.ts`: 34 ids in order, the eight pinned as variable OFL 1.1 rows with a LICENSE
  and the recorded facts (names, categories, italics, weights), the byte budget between 1.70 and
  1.85 MB, `fontsInCategory('mono')` gains `geist-mono`.
- The export path (3.4) needs no change: the PDF embeds the faces the capture drew and the
  Editable text PowerPoint names a face through `FONT_NAMES`; section 3 records both measured.

#### 1.7 P1: the italic preloaded on /edit alone (3.1's last paragraph; audit-fonts 12; row `fonts.preload.italic-on-edit-only`)

- `apps/studio/src/routes/__root.tsx`: `ITALIC_PRELOAD_ROUTES` (`/edit/$deckId`); the head's
  links keep the upright preload on every route and add the italic's only when the leaf match is
  the editor (`crossOrigin: 'anonymous' as const`, so the conditional spread keeps the literal type
  the router's `LinkHTMLAttributes` wants). Every other route loads the italic on first use through
  `font-display: swap`. Measured on 4412: `/decks` one font preload (the upright), `/edit/<id>` two.
  The cold cache swap on `/deck` and `/present` is not measured here (the audit asked for it
  before the move; this lane's server is a dev server and measures nothing, AGENTS.md).

#### 1.8 P1: one fontStack for the block path and the kit path (3.5; audit-fonts 16)

- `packages/fonts/src/summary.ts`: `GENERIC_STACKS` (sans-serif `'Helvetica Neue', Arial,
sans-serif`; serif `Georgia, 'Times New Roman', serif`; monospace `ui-monospace, 'SF Mono',
Menlo, Consolas, monospace`), `fontFamilyStack(id)` answers the name then the category's stack,
  and `inter` answers the sheet's own `INTER_STACK` with the fallback face second.
- `packages/render/src/theme-css.ts`: `fontStack(id)` delegates to `fontFamilyStack`; the local
  `GENERIC_STACKS` and the `FONT_NAMES`, `CATEGORY_GENERIC` and `lightFamily` imports leave.
- `packages/render/src/fonts.ts` `fontVariablesRule` writes the same stack into
  `--ts-font-<id>`, so a heading set by the kit and a text box set by the block fall back to one
  face before the file loads. Tests: `catalog.test.ts`, `fonts.test.ts` and `theme-css.test.ts`
  pin the new strings and the equality of the two paths.

#### 1.9 P1: a table takes a family (3.5; audit-fonts 13; row `fonts.table.takes-family`)

Nothing landed on this lane's files (R5 names the three hunks in other lanes' files); the row is
not driven here.

#### 1.10 Not done

- The P1 specimens of 3.5 (`assets/<id>/specimen.svg` from the fetch script; B1's row
  `fonts.picker.specimen-rows`): not written this run; the fetch script gained the features read
  and the licence check alone. It is one commit for the round after or the fix round if B1's P1
  condition holds.

### 2. Files edited

`packages/fonts/assets/InterVariable-Italic.woff2` (replaced); `packages/fonts/assets/{geist,
geist-mono,instrument-sans,manrope,bricolage-grotesque,schibsted-grotesk,newsreader,fraunces}/`
(new, 13 woff2 files and 8 LICENSE files); `packages/fonts/export/*-Italic.ttf` (17 rebuilt),
`packages/fonts/export/fonts.json`; `packages/fonts/src/inter.ts`, `inter.css`, `inter.test.ts`,
`summary.ts`, `catalog.ts`, `catalog.test.ts`, `names.ts`, `export.test.ts`, `woff2-names.ts`
(new), `catalog-files.ts` and `catalog-light.ts` (generated); `packages/fonts/scripts/fetch-fonts.mjs`,
`catalog-light.mjs`; `scripts/build-fonts.py`; `packages/schema/src/fonts.ts`, `typography.ts`;
`packages/theme/src/gt-ink-paper/sheet.css`, `tokens.ts`, `tokens.test.ts`;
`packages/chrome/src/tokens.css`, `inspector/typography.tsx`, `inspector/typography.css`;
`packages/identity/src/marks-render.ts`; `packages/render/src/theme-css.ts`, `theme-css.test.ts`,
`fonts.ts`, `fonts.test.ts`; `apps/studio/src/routes/__root.tsx`; `THIRD_PARTY_NOTICES.md` (the
Inter section and the catalog section). Nothing outside the Owns list.

### 3. Matrix rows covered and how they were driven

Driven on 4412 by `b2-drive.mjs` (a Playwright script over `playwright-core`, the window API and
the DOM, the pace of the walk's helpers; copied beside this note as `b2/b2-drive.mjs` with the
last run's `b2-drive.json`) under `.turboslide/e2e.lock`, on a scratch deck from `/new` with a
title typed, a blank slide, a table, two paragraphs and a text box placed through `block.insert`,
trashed and deleted forever at the end (404 read). Three runs: the whole list twice (the first
run's two reads that failed were the driver's own, a sheet root selector and a cell measurement
over the `.para` block box; both fixed and rerun green), then the export rows alone twice.

| Row                                                                  | Result on 4412              | Observed                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------------------------------------------------------------------- | --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `fonts.inter.italic-release`                                         | passed                      | the italic the editor preloads is 387,976 B, sha256 `e564f652…`, name version "Version 4.001;git-9221beed3"; Cmd+I on the title renders an `I` run computing `italic` in Inter; the italic FontFace reads loaded                                                                                                                                                                                         |
| `fonts.links.licence-v4-1`                                           | passed                      | More fonts' Inter licence href `https://github.com/rsms/inter/blob/v4.1/LICENSE.txt`; 0 hrefs with `v4.001`; 34 rows listed (with B1's working copy of `font-picker-model.ts`, R4; measured after B1 merges, per the shared file table)                                                                                                                                                                  |
| `fonts.fallback.in-stack`                                            | passed                      | the sheet root (`.ts-stagewrap.ts-sheet`) computes `Inter, "Inter Fallback", "Helvetica Neue", Arial, sans-serif`; `document.fonts` lists Inter Fallback (loaded)                                                                                                                                                                                                                                        |
| `fonts.display-features.inter-only`                                  | passed                      | the title computes `"cv11", "ss01"`; in Playfair Display `normal`; back to Inter the pair; the kit's display face set to Fraunces, every heading `normal` in Fraunces; the kit cleared, the pair                                                                                                                                                                                                         |
| `tables.cells.tabular-figures`                                       | passed                      | a `.td` computes `tabular-nums`; "1111" and "0000" both 36.51 px; a paragraph computes `normal` with "1111" 24.01 px and "0000" 38.65 px                                                                                                                                                                                                                                                                 |
| `formatting.numerals.tabular-row`                                    | red on one of five readings | Format options > Text lists Tabular figures with the sentence under it and in the tooltip; on, `typography.numerals` reads `tabular`, the block computes `tabular-nums` and "1111" and "0000" both measure 28.75 px; in Bebas Neue the row is enabled; in Playfair Display disabled with "This face has no tabular figures"; Search the menus "line up numbers" lists nothing (R1, the integrator's row) |
| `fonts.catalog.geist`                                                | passed                      | Geist under Sans serif, Geist Mono under Monospace; the pick writes `family: geist`, the block computes Geist, a Geist face loads; the PDF names `Geist-Regular` (the export half below)                                                                                                                                                                                                                 |
| `fonts.catalog.six-families`                                         | passed                      | Instrument Sans, Manrope, Schibsted Grotesk and Bricolage Grotesque under Sans serif, Newsreader and Fraunces under Serif (Google's categories, R3); Fraunces on the heading computes Fraunces and loads                                                                                                                                                                                                 |
| `fonts.preload.italic-on-edit-only`                                  | passed (P1)                 | `/decks` one font preload (`InterVariable.woff2`); `/edit/<id>` two (the upright and the italic)                                                                                                                                                                                                                                                                                                         |
| `fonts.table.takes-family`                                           | not driven (P1)             | nothing on this lane's files lets a table take a family (R5)                                                                                                                                                                                                                                                                                                                                             |
| the export half (`fonts.export.pdf-face` with Geist and Fraunces)    | passed                      | the PDF (76,722 B, 2 pages) embeds `Geist-Regular`, `Fraunces-9ptBlack_opsz…` and four InterVariable subsets                                                                                                                                                                                                                                                                                             |
| the export half (`fonts.export.editable-names-face`, Embed fonts on) | passed                      | the Editable text PowerPoint (1,394,702 B, 2 slides, Embed fonts on) names `Fraunces`, `Geist`, `GT Inter Text 14`, `GT Inter Text 20`, `GT Inter Text 20 Medium` and `GT Inter Text 22` in `a:latin typeface` and embeds four `.fntdata` parts (the Inter export set's used faces; the deck carried no italic run at export time, so no italic instance was embedded)                                   |

The last run (the export rows alone, `b2/b2-drive.json`): the four rows of the export half and the
two catalog rows green; the first three runs of the PowerPoint step failed on the driver, not the
product (the Download dialog remembers the last file type, so the PowerPoint type must be clicked,
and Embed fonts sits behind the dialog's More disclosure, where a forced click on the clipped
native input landed on the scrim and closed the dialog; `export.spec.ts` 1240 opens More first).
Every scratch deck of the four runs (`untitled-20260922-rk3d`, `-2tqa`, `-k5gu`, `-s41b`) was
trashed and deleted forever and answered 404.

### 4. Commands run and their results

- `.turboslide/venv/bin/python scripts/build-fonts.py`: 34 faces, 18 files written (17 italics and
  `fonts.json`); `--check`: 0 stale files.
- `node packages/fonts/scripts/fetch-fonts.mjs --only inter,geist,…,fraunces`: 16 files, 4,489,040 B
  TrueType to 2,505,140 B woff2 (Inter's two present files counted at their own size), 34 rows
  written, 9 licence URLs at 200; then `node packages/fonts/scripts/catalog-light.mjs` and
  `node_modules/.bin/prettier --write` on the two generated files.
- `node_modules/.bin/tsc -b`: this lane's files clean. Two errors remain on the tree, neither
  this lane's: `packages/chrome/src/__tests__/logo-dialog.test.tsx` (B1 and B6's in progress
  dialog, imports the `Logo.tsx` exports as they are being written) and, earlier in the run,
  `apps/studio/src/server/logo-sanitize.ts` 383 (B6's) and `font-picker-model.ts` 230 before B1's
  eight `DIRECTORIES` entries landed (they have since).
- `node_modules/.bin/vitest run` per package: fonts 23 passed (3 files); schema 583 passed (25);
  theme 81 passed (5); render 376 passed (17); identity 79 passed (10); export 143 passed (19);
  chrome 682 passed and 4 failed in 2 files, both other lanes' in progress files
  (`__tests__/logo-dialog.test.tsx`, `parked-controls.test.ts`).
- `node_modules/.bin/prettier --check` on every edited file: clean after `--write` on five.
- The dev server on 4412 (`vite.no-watch.config.ts`, the tmp store, the memory channel) was
  started for the drives and stopped before this report.

### 5. Open items

1. The Editable text PowerPoint and tabular figures (1.4): DrawingML carries no OpenType feature,
   so a table cell the sheet draws with `tnum` exports with proportional digits, and the parity
   fix round measured a tabular "1" in a right aligned cell 4 to 6 px outside the 3 px cell budget
   when the sheet was tabular and the export was not (the comment sheet.css used to carry, focus
   `b2.md`). FEATURES.md 3.1 item 4 puts the alignment on the sheet; the export difference stands
   and the calibration's cell budget may read it on a digit heavy right aligned table. A later
   answer is an export instance with `tnum` frozen the way `build-fonts.py` freezes `cv11` and
   `ss01` for the display face (one more family in the exact set), recorded for the round after.
2. The categories of Bricolage Grotesque and Fraunces (1.6, R3): Google's, not the Display group
   FEATURES.md named; a hand override in `FONT_SOURCES` would be one field if Kevin wants them
   under Display.
3. `packages/headless/src/sheet.ts` 135 carries a fifth Inter stack without the fallback face
   (1.3); the integrator's or B7's file.
4. The P1 specimens (1.10) and the table's family (1.9, R5) wait on other lanes' files or the
   round after.
5. `apps/studio/e2e/core/brand.spec.ts` `fonts.budget.no-load-before-ready` counts catalog woff2
   requests before the ready mark and ignores Inter's files, so the preload change (1.7) does not
   move it; the editor's own request count before ready drops by one file on every route but
   `/edit`.
