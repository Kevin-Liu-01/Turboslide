# B4, the exports

## Vector round

Builder B4 of the vector round (docs/VECTOR.md sections 2.5, 4.6, 5.1, 5.4, 6.1 and 6.3), on the shared worktree `/Users/kevinliu/repos/Turboslide-vector` at `2be923e` on 2026-09-24 while B1, B2, B3 and B5 landed beside it (this lane started on the bare tree, built against the day 0 API, and swapped to B3's `assetVector` and B2's `textInset` and `sites` once they were on the tree; every file below is B4's alone). The dev server was port 4424 (`TURBOSLIDE_STORE=tmp` under a private `TMPDIR` in the scratchpad, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, `TURBOSLIDE_LOGO_UPSTREAM=fixture`, two 48 character secrets from `openssl rand -hex 24` minted in the command), stopped before the return. Playwright (the repository's `@playwright/test`, Chrome for Testing) ran against 4424 under `.turboslide/e2e.lock` (taken with the `until mkdir` loop, released by the runner's trap; no lock is held at the return), from a scratch spec under the ignored `.turboslide/b4-drive/` folder, removed before the return. No git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran. Every scratch deck was made on the tmp store of 4424, trashed and deleted forever through the product at the end of its drive, and left with the store when the server stopped; nothing was created on a deployment. No token, cookie or secret was printed. Times are UTC.

### 1. The items, their files and their hunks

Each item is one commit's worth; a red row's item reverts alone. The hunks are named by the symbol they add or change.

| Item | Files and hunks | Rows | Unit tests |
| --- | --- | --- | --- |
| 4.6 the svgBlip post process | `packages/export/src/ooxml/svg.ts` (new): `SVG_BLIP_EXT_URI`, `SVG_BLIP_NS`, `IMAGE_REL_TYPE`, `SVG_CONTENT_TYPE`, `looksLikeSvg` (the intake's sniff), `relsPartOf`, `nextRelationshipId`, `mediaNameFor`, `addSvgDefault`, `countSvgBlips`, `writeSvgBlip(zip, part, xml, name, svg)`: the ext of probe (b) inside the pic's `a:blip` (appended to an `a:extLst` the blip already holds, else a new one as its last child), the image relationship with the slide's next free id, `ppt/media/<safe name>.svg` with the bytes as stored, the `svg` default once; idempotent on a pic that carries one; the writer takes and returns the slide XML like the other rewrites, so it composes with them before `groupShapes` (the spec's `writeSvgBlip(zip, part, name, svg)` reads the part from the package, which would lose the in memory rewrites of the loop; the one deviation) | `svg.export.pptx-svgblip`, `svg.export.pptx-fallback`, `logos.export.svgblip` | `ooxml/svg.test.ts` (new, 9 tests): the helpers; over a package in probe (b)'s shape the ext, the rel, the media part and the content type once, `validatePackage` valid, `cleanContentTypes` keeping the default, the written package reopening; a second call idempotent; two pictures on one slide; a missing pic, a shape and a missing rels part changing nothing; a blip with an ext list gaining the ext inside it; a real pptxgenjs package built in Node (the PNG fallback stays a PNG, never the svg's bytes under a png name) |
| 4.6 the builder | `packages/export/src/pptx/build.ts`: the header; `BuildOptions.svgVector` (default true); `RoundTwoCounts.svgBlips`; `rewrites[].svgs`; `noteVector` (a placed raster carrying `svg` joins the slide's post process, or counts as left out under `svgVector: false`), called after `addRaster` in the flatten kit logo loop and the native raster loop; the post process step after the alt rewrites and before `groupShapes` (a missing or non svg file leaves the PNG alone with a residual line); `droppedEnds` from `toConnector`; the residual lines `svg: N picture(s) carry asvg:svgBlip …`, `svg: N vector picture(s) travel as PNG alone (svgVector false)`, `connectors: N attachment end(s) left unattached …` | the three above, `shapes.geometry.export.pptx-prst-avlst` (the file as written) | `pptx/build-svg.test.ts` (new, 4 tests, no browser): the Editable text file of a scene whose raster carries `svg` (the ext, the rel to a `.svg` media part holding the file's bytes, the 3x PNG fallback whose IHDR width is the box times three, the content type default, the package valid, `export check` classing the part `svg`); `svgVector: false` the PNG alone and no svg part; a raster picture, a missing file and a non svg file staying PNG alone with the residual lines and no warning; the Perfect file's kit logo object carrying it while a block raster is the sheet's alone |
| 4.6 the extractor | `packages/export/src/scene/types.ts`: `SceneRaster.svg`. `packages/export/src/scene/extract.ts`: the header; `THREE_X_KINDS` gains `svg`; `vectorFilesOf(slide, deck, theme, deckDir)` (schema imports alone: the picture and shot blocks through composites whose asset answers `assetVector`, the kit's mark and footer under `title-logo` and `footer-logo`); in `extractScenes` the map per slide, `raster.svg` set where the file exists, `scaleKind` `svg` before `logo` | `svg.export.pptx-fallback` (the 3x shot), `logos.export.svgblip` | `scene/vector.test.ts` (new, 3 tests): `svg` a 3x kind under auto; `vectorFilesOf` over an svg upload, a themed vector, a ship one logo's source, a tinted logo (none), a raster asset (none), a missing asset (none), a composite cell, the two kit names, the dark theme's file; an opener's plate and a kit drawing the GT mark |
| 4.6 export check | `packages/export/src/check.ts`: `mediaFormatOf` answers `svg` for a part whose bytes sniff as svg (the `formats` record of `export check` then counts the vector parts) | the report rows | the build test above reads `check.formats.svg` |
| 2.5 the label insets | `packages/export/src/pptx/text.ts`: the header; `shapeKindOf`, `layerInsetNetOfPreset` (the measured inset per side less the preset's text rectangle at the exported box, clamped at zero); `addShapeText` subtracts `textInset(kind, w, h, adjust)` before the padding and the baseline shift | `shapes.geometry.text-rect` (the file half; B2's layer is the sheet half) | `pptx/text.test.ts` (5 tests added): the pure subtraction and the clamps; `shapeKindOf`; a rounded rectangle's label at the ECMA rectangle writing no inset beyond the baseline shift; a layer 20 px inside it keeping the 20; a plain rectangle keeping the measured inset whole; the `THREE_X_KINDS` pin extended by `svg` |
| 2.5 the connector bound | `packages/export/src/ooxml/shapes.ts`: the header; `presetSiteCount` (rect's four, else the preset's list through schema `sites`, undefined off a preset); `ConnectorResult.dropped`; `toConnector` reads the target's `prst` and attaches an end only when its site is below the count | `lines.connector.*` (unchanged text), `shapes.geometry.sites` (the file half) | `ooxml/shapes.test.ts` (new, 6 tests): the counts; a rectangle corner end dropped and named while the other end attaches; a hexagon's last index kept and the next dropped; both ends dropped leaving the shape; a graphic frame target keeping the index; a missing target |

Not in this lane: `packages/export/src/pdf/build.ts` needed no change (the print document draws B3's `<img src=".svg">` and Chromium keeps it as vector, probe (a); the drive below measures it); `report.ts` needed no change (the counts ride in the residual, which `exportReportSchema` already carries; a typed `svgBlips` field would need the schema's header, not asked for).

### 2. The unit tests and the typecheck

`cd packages/export && TURBOSLIDE_SKIP_BROWSER_TESTS=1 ../../node_modules/.bin/vitest run`: 20 files passed, 4 skipped (the browser tests), 140 tests passed, 35 skipped, first at 01:15Z on the tree with B2's `shapes.ts` and B3's `assets.ts` landed and again at 02:01Z on the tree at the return (B1, B3 and B5 landed in between). `node_modules/.bin/tsc -p packages/export/tsconfig.json --noEmit`: clean at 01:15Z, 01:26Z and 02:01Z. `tsc -b` stops before this package at `packages/schema/src/shapes.test.ts`, B2's test, which still imported `DRAWN_PRESET_IDS`, `isDrawnPreset` and `roundRectRadius` from a `shapes.ts` that no longer exports them (TS2305 three times) when this lane last ran it at 01:15Z; B2's file, not touched here. The browser tests of this package (`export-pptx.test.ts`, `extract.test.ts`, the fixture and dither ones) were not run under vitest; the CLI drive of section 3 ran `extractScenes` and `buildPptx` end to end through the real browser instead.

At the return: the server on 4424 is stopped and the port free; `.turboslide/b4-drive/` is removed (its spec, config and records are copies under the scratchpad); the tmp store lists `gt-brand` and `templates` alone; `.turboslide/e2e.lock` exists, taken at 02:02Z by another lane's run after this lane's last release at 01:55Z (both drive scripts logged their release), so it was left in place.

### 3. What the drives measured

Two drives: the CLI over a fixture deck (the extractor, the builder, the PDF and the reports through the real browser, no server) and the studio on 4424 through the product's own Download dialog. Both ran under `.turboslide/e2e.lock` (one browser at a time); every file was read from its own bytes with the scratchpad's `read-exports.mjs` (jszip through the export package's `createRequire`, `pdfimages -list`) or the e2e lib's readers.

**A. The CLI over the fixture deck (01:27Z, `turboslide export pptx|pdf <deck> all`).** The deck (`scratchpad/vector/b4/deck`, validated against `deckSchema` and `slideSchema`): an svg upload of this round (`art`, `kind: 'svg'`, `vector: { neutral }`, a 1200 by 600 PNG twin at scale 3), a ship one logo asset (`figma`, `role: 'logo'`, `source.kind: 'logo'` without a tint, `sourceFile` the untinted svg), the kit's mark and footer on that logo, slide `s1` with a heading, the svg picture at 400 by 200 and the logo picture at 160 by 100, slide `s2` with a hexagon, a star (adjust 30000, 105146, 110557), a rectangular callout, a rounded rectangle labelled "Next step" and a right arrow labelled "Go". Every export exited 0 and every report passed.

- The Editable text file (40,727 bytes, 2.4 s): four `p:pic` objects carry `asvg:svgBlip` inside the `{96DAC541-7B7A-43D3-8B79-37D633B846F1}` ext, each with an image relationship to its own `ppt/media/<name>.svg` part (`ts-s1-art-1.svg` opening with the prolog, `ts-s1-mark-2.svg`, `ts-s1-footer-logo-3.svg`, `ts-s2-footer-logo-1.svg` opening with `<svg`) beside a PNG blip whose IHDR is the box times three: `ts:s1#art:1` 1200 by 600 for the 400 by 200 box, `ts:s1#mark:2` 480 by 300 for 160 by 100, the footer logos 87 by 54 for the snapped 29 by 18 box; `[Content_Types].xml` carries the `svg` default (pptxgenjs writes it itself; the writer added none). The report's residual reads `svg: 4 picture(s) carry asvg:svgBlip beside the PNG blip (docs/VECTOR.md 4.6); PowerPoint 2016 and later draw the vector, every other viewer the PNG fallback`. The shapes: `prst="hexagon"` with an empty list (pptxgenjs writes it as `<a:avLst></a:avLst>`), `prst="star5"` with `<a:gd name="adj" fmla="val 30000"/>` first then `hf` and `vf`, `prst="wedgeRectCallout"` with an empty list (the block carried no `adjust`; the sheet, PowerPoint and the file agree on the defaults; the studio drive below sets the pointer explicitly and reads `adj1` and `adj2`), `prst="roundRect"` and `prst="rightArrow"`. The label insets of the rounded rectangle and the right arrow: `lIns="0" tIns="0" rIns="0" bIns="34282"` (the baseline shift alone): the `.shape-text` layer sits at the ECMA text rectangle on the sheet (B2's), and `layerInsetNetOfPreset` nets it to zero, the file half of `shapes.geometry.text-rect`.
- The Perfect file (69,430 bytes, 3.4 s, `perfect: true`, both pages at 0.000 percent mismatch): the two sheet rasters at 3200 by 1800 and the kit's footer logo object on each slide (`ts:s1#footer-logo:3`, `ts:s2#footer-logo:1`) with the svgBlip beside its 87 by 54 PNG; the svg picture block and the logo block are in the sheet raster and have no object of their own (the Perfect rule of 4.6); no title logo object because the fixture's slides are content slides (the studio drive's deck has a title slide). Residual `svg: 2 picture(s) carry asvg:svgBlip …`.
- The PDF (63,075 bytes, 1.1 s, 2 pages at 960 by 540 pt, `passed: true`): the svg picture block and the logo picture block leave no image object (Chromium kept them as vector, probe (a), through B3's `<img src=".svg">`); each page holds one 396 by 252 image object with its soft mask, the kit's footer picture, which the stage's frame band draws from the PNG twin: `bandAssetResolver` (`packages/render/src/stage.ts` 92 to 101) reads `asset.twins` and never `assetVector`, while the title slot mark (`slide.ts` 414) and every picture block go through `imageResolver`, which does. So the PDF half of `logos.export.svgblip` ("the PDF page holds no image XObject for the logo") reads red on this tree for a kit whose footer draws the logo, and green for a logo block or a title slot mark alone; request R7 names the two line fix.

**B. The studio on 4424 through the Download dialog (the run of record 01:53Z to 01:55Z; two earlier runs, one refused at the config's `__dirname` under ESM and one whose record a worker restart overwrote, read the same seven outcomes).** The spec (`scratchpad/vector/b4/vector-exports.spec.ts`, its config and the records `record-run3.json` and `playwright-report-run3.json` beside it) made a deck from `/new`, added a slide, uploaded the svg through the window API (`asset.add` with a data URL, B3's intake: 203 ms; the record `kind: 'svg'`, `vector.neutral: assets/art-asset.svg`, a PNG twin, `size [400, 200]`, `scale 3`), placed a picture block of it at 400 by 200, and read every file from the download's bytes. Playwright starts a fresh worker after a failed test, so three scratch decks were made (`untitled-20260925-hb6x`, `-d53e`, `-t8eq`); each was trashed, deleted forever and answered 404 in its worker's teardown, and the tmp store lists `gt-brand` and `templates` alone at the return. Every file arrived within 30 s (the longest, the Perfect file of the logo deck, 5.1 s).

- `svg.export.pptx-svgblip`, green: the Editable text file (3.8 s) carries `ts:split-1#art:1` with `<a:ext uri="{96DAC541-7B7A-43D3-8B79-37D633B846F1}">` and `asvg:svgBlip` whose rel targets `ppt/media/ts-split-1-art-1.svg`, opening with `<svg xmlns=` (the intake's sanitizer rewrote the prolog away) and holding no `<script`; `[Content_Types].xml` carries the `svg` default.
- `svg.export.pptx-fallback`, green on its first half: the same pic's `a:blip` targets `ppt/media/image-2-1.png`, the PNG signature, IHDR 1200 by 600 for the 400 by 200 block (the 3x shot). The second half, `svgVector: false` through the window API, is not driven: `export.run` refused the key (`invalid input at /svgVector: Unrecognized key: "svgVector"`), the header field of R1; the builder's own switch is pinned by `build-svg.test.ts`.
- `svg.export.pdf-vector`, green: the PDF (2.6 s) has one page per unskipped slide (2) and zero image objects (`pdfImages` 0).
- `svg.export.web-page`, red: File > Download > Web page (in the default view on this server, no switch needed) arrived in 1.7 s as `untitled-20260925-hb6x.html` with no `data:image/svg+xml;base64,` in it and no PNG data URI of the asset either: the picture's `<img>` points at the relative `assets/art-asset.svg` the standalone build never inlined (R8, `apps/cli/src/assets.ts` `inlineAssets` walking the twins alone).
- `logos.export.svgblip`, green on its PowerPoint halves and red on its PDF half: `logo.insert { slug: 'figma' }` from the fixture upstream (the record `kind: 'svg'`, `vector.neutral` the sanitized source, a PNG twin at scale 3, B3's logos.ts) and the kit's mark and footer set to it through `brand.set`. The Editable text file (4.2 s) carries five vector pictures, `ts:title#footer-logo:1`, `ts:title#title-logo:2`, `ts:split-1#art:1`, `ts:split-1#logo:2` (the inserted logo block) and `ts:split-1#footer-logo:3`, each with its PNG blip and its `.svg` part; the Perfect file (5.1 s) carries the three kit objects `ts:title#footer-logo:1`, `ts:title#title-logo:2` and `ts:split-1#footer-logo:3` with the svgBlip over the sheet rasters; the PDF (2.1 s, 2 pages) holds 2 image objects, one per page: the kit's footer band picture, drawn from the PNG twin by `bandAssetResolver` (drive A, R7); the title slot mark and the logo block are vector.
- `shapes.geometry.export.pptx-prst-avlst`, green: a third slide with a hexagon, the star (adjust 30000, 105146, 110557), a rectangular callout with its pointer written (adjust −20833, 62500) and a rounded rectangle labelled "Next step" through `block.insert`; the Editable text file (4.8 s) carries `<a:prstGeom prst="hexagon"><a:avLst></a:avLst></a:prstGeom>` (pptxgenjs's form of an empty list), `prst="star5"` with `<a:gd name="adj" fmla="val 30000"/>` first then `hf` and `vf`, `prst="wedgeRectCallout"` with `<a:gd name="adj1" fmla="val -20833"/><a:gd name="adj2" fmla="val 62500"/>`, `prst="roundRect"`, and the label's `bodyPr` `lIns="0" tIns="0" rIns="0" bIns="34282"` (the baseline shift alone; the file half of `shapes.geometry.text-rect`). A callout inserted without `adjust` writes the empty list, which PowerPoint reads as the same pointer; B5's driver should set the pointer or read the empty list as the default (R5).
- `shapes.geometry.export.raster-modes`, green: the Perfect file (5.6 s, 3 slide parts) and the PDF (3.1 s, 3 pages) of the same deck; through the window API `export.run` in flatten mode answered `perfect: true` with every page at 0.000 percent mismatch (png-palette pages) and the PDF's report `passed: true` with its gate lines.

The window transport on 4424 carried `export.run`, `export.text`, `brand.get`, `brand.set`, `brand.reset`, `logo.search` and `logo.insert`.

### 4. The rows this lane covers

`svg.export.pdf-vector`, `svg.export.pptx-svgblip`, `svg.export.pptx-fallback`, `svg.export.web-page`, `logos.export.svgblip`, `shapes.geometry.export.pptx-prst-avlst`, `shapes.geometry.export.raster-modes` (B5 wrote the seven rows into the matrix with their drivers `core/export.spec.ts` and `core/logos.spec.ts`; the driver tests are B5's and were not on the tree at 18:30Z). The file half of `shapes.geometry.text-rect` and of `shapes.geometry.sites`.

### 5. Requests

R1 (the integrator, the header of `packages/schema/src/actions.ts`, `export.run` 2675 to 2700; B1's R6 names the same): the input gains `svgVector: z.boolean().optional().describe('Write the vector of every svg picture and logo as asvg:svgBlip beside its PNG blip (docs/VECTOR.md 4.6); on by default. false writes the PNG blip alone, which the parked control export.svg.vector selects')`. Without it the strict input refuses the key B1's dialog sends while the control is parked, and the row `svg.export.pptx-fallback`'s second half (`svgVector: false` through the window API) cannot be driven.

R2 (the integrator, `packages/export/src/batch/plan.ts` 63 to 78): `BatchExportInput` gains `svgVector?: boolean`, so `batchInputOf` (studio `export-batch.ts` 135 to 148) carries the field into the plan.

R3 (the integrator, `packages/export/src/batch/merge.ts`): (a) the build call (223 to 238) gains `...(plan.input.svgVector === false ? { svgVector: false } : {})`; (b) `relocateScene` (44 to 67) places the vector beside the raster, `const svg = place(raster.svg, 'vectors')` and `{ ...raster, file, ...(svg === undefined ? {} : { svg }) }`, and `localizeScene` (70 to 85) maps it back with `local(raster.svg)`. Today the absolute path of the batch instance's deck directory rides in the scene part; the builder finds it on the same instance and, on another, records `svg: <slide>: <pic> names a vector file that is missing; the picture travels as PNG alone` and writes the PNG alone. The hosted export of ship one runs its batches and its merge inside one job on one instance (`export-batch.ts`), so the file is found today; (b) makes it hold across instances.

R4 (the integrator, `packages/export/src/export-pptx.ts` 62 to 100 and 214 to 231): `ExportPptxOptions.svgVector?: boolean` passed as `...(options.svgVector === false ? { svgVector: false } : {})`; P1 a `--no-svg-vector` flag in `apps/cli/src/commands/export.ts`.

R5 (B5, the drivers): how the file reads. The picture's object is the `p:pic` named `ts:<slideId>#<blockId>:<rid>` (the raster id is `<blockId>:<rid>`); the Perfect file's logo objects are `ts:<slideId>#title-logo:<rid>` and `#footer-logo:<rid>`; the svgBlip's `r:embed` resolves through the slide's rels part to `../media/<name>.svg`; the PNG blip's target's IHDR width (`pngSize`) is the block's width times three (the snapped box); pptxgenjs writes the `svg` content type default itself, so the assertion is its presence, not a count of one; `export.run`'s report carries the line `svg: N picture(s) carry asvg:svgBlip beside the PNG blip …` in `residual`, and `svg: N vector picture(s) travel as PNG alone (svgVector false)` under the switch (R1). The e2e helpers this lane's drive used are the lib's (`zipEntries`, `zipEntriesRaw`, `pngSize`, `pdfImages`, `pdfPages`).

R7 (B3, or the integrator as the owner of `packages/render/src/stage.ts`; VECTOR.md 4.4 and 4.6, the logos paragraph): `bandAssetResolver` (92 to 101) answers the footer band's picture from `asset.twins`; with `const path = assetVector(asset, theme) ?? assetTwin(asset, theme)` (both in `@turboslide/schema/assets`, B3's) the kit's footer logo draws as vector on the sheet, in the print document and in the standalone page the way the title slot mark and every picture block already do through `imageResolver` (`slide.ts` 125 to 133), and the PDF half of `logos.export.svgblip` reads green for a kit whose footer draws the logo. The band's `<img>` keeps its `width` and `height` from `footerLogoBox`, so nothing else moves; `theme-css.test.ts` 330 and `title-mark.test.ts` read the title slot, not the band. The exports need nothing more: the Perfect file's footer object already carries the svgBlip (the extractor reads the kit's assets through `vectorFilesOf`, drive A).

R8 (the integrator, `apps/cli/src/assets.ts` 119 to 145; VECTOR.md 4.6 "The web page"): `inlineAssets` walks `asset.twins` alone, so the vector file B3's `imageResolver` writes into the `<img src>` of an svg picture (`assets/<id>.svg`) has no data URI in `build.assetUris`, the standalone's `assetSrc` (`packages/render/src/standalone.ts` 100 to 104) records it under `missing` and returns the relative path, and the downloaded page carries a broken `<img>` for the picture (drive B below: no `data:image/svg+xml;base64,` in the file). The hunk: after the twins, `const vector = vectorOf(asset); for (const path of vector === undefined ? [] : 'neutral' in vector ? [vector.neutral] : [vector.light, vector.dark])` through the same `inlineAssetFile`, which already answers an `.svg` as `image/svg+xml` pass through (86 to 88). The Web page row `svg.export.web-page` reads red until it lands; nothing in the export package draws the page.

R6 (the integrator, `packages/export/src/ooxml/zip.ts` 51 to 53, P1): `isStoredPart` stores every `ppt/media/*` part uncompressed, the PNG rule; an svg part deflates by three quarters, so an exception for `.svg` (deflate) would shrink a file carrying many vectors. No row needs it.
