# Turboslide actions

Generated by `pnpm generate:contracts` from `packages/schema/src/actions.ts`; do not edit by hand.

Every operation is one action in one table; the CLI subcommands, the MCP tools, the window API and the HTTP endpoints are generated from it. Every mutating action takes `baseRevision` and rejects a stale one with 409 and the current document; every write returns the normalized result so the re-read is free. An action whose milestone has not landed answers `NotImplementedError` (501).

| Action              | Label              | Input                                                                             | Mutates | Transports             | CLI                                                                             | MCP tool                | Milestone |
| ------------------- | ------------------ | --------------------------------------------------------------------------------- | ------- | ---------------------- | ------------------------------------------------------------------------------- | ----------------------- | --------- |
| `deck.info`         | Deck info          | none                                                                              | no      | cli, mcp, http, window | `turboslide info`                                                               | `deck_get_info`         | M1        |
| `slide.list`        | List slides        | `sectionId?`                                                                      | no      | cli, mcp, http, window | `turboslide slides`                                                             | `deck_list_slides`      | M1        |
| `slide.get`         | Get slide          | `slideId`                                                                         | no      | cli, mcp, http, window | `turboslide slide get <slideId>`                                                | `deck_get_slide`        | M1        |
| `slide.insert`      | Insert slide       | `sectionId`, `after?`, `slide`, `baseRevision`                                    | yes     | cli, mcp, http, window | `turboslide slide insert --section <sectionId> --after <after> < slide.json`    | `deck_insert_slide`     | M2        |
| `slide.remove`      | Remove slide       | `slideId`, `baseRevision`                                                         | yes     | cli, mcp, http, window | `turboslide slide remove <slideId>`                                             | `deck_remove_slide`     | M2        |
| `slide.move`        | Move slide         | `slideId`, `sectionId`, `after?`, `baseRevision`                                  | yes     | cli, mcp, http, window | `turboslide slide move <slideId> --to <sectionId> --after <after>`              | `deck_move_slide`       | M2        |
| `slide.update`      | Patch slide        | `slideId`, `baseRevision`, `mutations`                                            | yes     | cli, mcp, http, window | `turboslide slide patch <slideId> --set <pointer>=<value>`                      | `deck_update_slide`     | M2        |
| `slide.replace`     | Replace slide      | `slideId`, `baseRevision`, `slide`                                                | yes     | cli, mcp, http, window | `turboslide slide put <slideId> < slide.json`                                   | `deck_replace_slide`    | M2        |
| `block.set`         | Set block property | `slideId`, `blockId`, `path`, `value`, `baseRevision`                             | yes     | cli, mcp, http, window | `turboslide block set <slideId>#<blockId> <path> <value>`                       | `deck_update_block`     | M2        |
| `block.insert`      | Insert block       | `slideId`, `slot`, `after?`, `block`, `baseRevision`                              | yes     | cli, mcp, http, window | `turboslide block insert <slideId> --slot <slot> --after <after> < block.json`  | `deck_insert_block`     | M2        |
| `block.remove`      | Remove block       | `slideId`, `blockId`, `baseRevision`                                              | yes     | cli, mcp, http, window | `turboslide block remove <slideId>#<blockId>`                                   | `deck_remove_block`     | M2        |
| `block.move`        | Move block         | `slideId`, `blockId`, `slot`, `after?`, `baseRevision`                            | yes     | cli, mcp, http, window | `turboslide block move <slideId>#<blockId> --slot <slot> --after <after>`       | `deck_move_block`       | M2        |
| `section.set`       | Set sections       | `sections`, `baseRevision`                                                        | yes     | cli, mcp, http, window | `turboslide sections set < sections.json`                                       | `deck_set_sections`     | M2        |
| `slide.lease`       | Lease slide        | `slideId`, `minutes?`, `force?`                                                   | yes     | cli, mcp, http, window | `turboslide lease <slideId> --force`                                            | `deck_lease_slide`      | M2        |
| `asset.add`         | Add asset          | `file?`, `url?`, `role`, `alt`, `source`, `treatment?`, `credit?`, `baseRevision` | yes     | cli, mcp, http, window | `turboslide asset add <file> --role <role> --two-tone --black <black>`          | `deck_asset_add`        | M5        |
| `asset.dither`      | Dither asset       | `assetId`, `treatment`, `plate?`, `baseRevision`                                  | yes     | cli, mcp, http, window | `turboslide asset dither <assetId> --gamma <gamma> --plate <plate>`             | `deck_asset_dither`     | M5        |
| `asset.capture`     | Capture page       | `url`, `viewport?`, `scale?`, `theme`, `region?`, `recipe`, `baseRevision`        | yes     | cli, mcp, http         | `turboslide asset capture <url> --theme <theme> --region <region>`              | `deck_asset_capture`    | M5        |
| `material.capture`  | Capture material   | `materialId`, `uniforms`, `size?`, `anchors`, `baseRevision`                      | yes     | cli, mcp, http, window | `turboslide material capture <materialId> --anchor <anchors>`                   | `deck_material_capture` | M5        |
| `render.slide`      | Render             | `slideIds`, `themes?`, `scale?`, `format?`, `out?`                                | no      | cli, mcp, http, window | `turboslide render <slideIds> --theme <themes> --out <out> --json`              | `deck_render`           | M1        |
| `render.sheet`      | Contact sheet      | `slideIds`, `themes?`, `cols?`, `thumb?`, `numbered?`, `overlay?`, `out?`         | no      | cli, mcp, http, window | `turboslide sheet <slideIds> --cols <cols> --thumb <thumb> --overlay <overlay>` | `deck_sheet`            | M1        |
| `lint.run`          | Lint               | `slideIds`, `layers?`, `rule?`                                                    | no      | cli, mcp, http, window | `turboslide lint <slideIds> --json`                                             | `deck_lint`             | M1        |
| `fix.run`           | Fix                | `slideIds`, `rule?`, `dryRun?`, `baseRevision`                                    | yes     | cli, mcp, http, window | `turboslide fix <slideIds> --rule <rule> --dry-run`                             | `deck_fix`              | M2        |
| `diff.run`          | Diff               | `from`, `to?`, `render?`                                                          | no      | cli, mcp, http, window | `turboslide diff <from> <to> --render`                                          | `deck_diff`             | M2        |
| `version.save`      | Save version       | `note`                                                                            | yes     | cli, mcp, http, window | `turboslide version save -m <note>`                                             | `deck_version_save`     | M2        |
| `version.list`      | List versions      | none                                                                              | no      | cli, mcp, http, window | `turboslide version list`                                                       | `deck_version_list`     | M2        |
| `version.restore`   | Restore version    | `n`, `baseRevision`                                                               | yes     | cli, mcp, http, window | `turboslide version restore <n>`                                                | `deck_version_restore`  | M2        |
| `judge.bundle`      | Judge bundle       | `slideIds`, `out`                                                                 | no      | cli, mcp               | `turboslide judge bundle --out <out>`                                           | `deck_judge_bundle`     | M4        |
| `view.goto`         | Go to slide        | `slideId`                                                                         | no      | window, mcp            |                                                                                 | `deck_goto_slide`       | M3        |
| `view.mode`         | View mode          | `mode`                                                                            | no      | window                 |                                                                                 | `deck_set_view`         | M3        |
| `view.theme`        | View theme         | `theme`                                                                           | no      | window                 |                                                                                 | `deck_set_view`         | M3        |
| `view.present`      | Present            | `on`                                                                              | no      | window                 |                                                                                 | `deck_set_view`         | M3        |
| `export.run`        | Export             | `format`, `mode?`, `theme?`, `fonts?`, `headings?`, `verify?`, `out?`             | no      | cli, mcp, http, window | `turboslide export <format> --mode <mode> --theme <theme> --verify`             | `deck_export`           | M2        |
| `build.run`         | Build standalone   | `out`, `budgetMB?`, `quality?`                                                    | no      | cli, mcp, http, window | `turboslide build --out <out> --budget <budgetMB>`                              | `deck_build`            | M1        |
| `import.run`        | Import deck        | `from`, `into`                                                                    | yes     | cli                    | `turboslide import <from> --into <into>`                                        |                         | M1        |
| `validate.run`      | Validate           | `path?`                                                                           | no      | cli, mcp, http, window | `turboslide validate <path>`                                                    | `deck_validate`         | M1        |
| `source.read`       | Read source        | none                                                                              | no      | window                 |                                                                                 |                         | M3        |
| `source.apply`      | Apply source       | string or object                                                                  | yes     | window                 |                                                                                 |                         | M3        |
| `controls.list`     | List controls      | none                                                                              | no      | window                 |                                                                                 |                         | M3        |
| `control.activate`  | Activate control   | string                                                                            | no      | window                 |                                                                                 |                         | M3        |
| `control.set`       | Set control        | `label`, `value`                                                                  | no      | window                 |                                                                                 |                         | M3        |
| `artifact.download` | Download artifact  | `fileName`                                                                        | no      | window                 |                                                                                 |                         | M3        |

## Transports

- `cli`: `turboslide <command> --json`, machine output on stdout, human output on stderr; exit 0, 1 (findings at the gate or a verify failure), 2 (usage or validation error). `--author agent:<runId>` and `--base-revision <n>` on every write.
- `mcp`: tools named `deck_<action>` over stdio (`turboslide mcp`) or streamable HTTP at `/mcp`; render tools return image content beside the JSON.
- `http`: `POST /api/actions/<id>` with the input as JSON; `GET /api/agent` for the manifest, `/openapi.json` for the contract.
- `window`: `window.turboslide.studio.invoke(<id>, input)` in the studio; see `turboslide-studio`.

## Write protocol

1. Read: `deck.info`, then `slide.get` for the slide you will touch.
2. Make the smallest change: one `block.set`, `text.replace` or `slide.update` with a mutation list; `slide.replace` only when the whole slide changes.
3. Write with the `baseRevision` you read and `--author agent:<runId>`; on 409 re-read the returned document and retry once.
4. Take `slide.lease` before a series of writes to one slide; release by letting it expire.
5. Re-read from the response, not from memory; then render both themes and lint (`turboslide-verify`).

## Mutations

| op                | Fields                                        | Note                                          |
| ----------------- | --------------------------------------------- | --------------------------------------------- |
| `slide.insert`    | `sectionId`, `after?`, `slide`                | `after` absent inserts first                  |
| `slide.remove`    | `slideId`                                     |                                               |
| `slide.move`      | `slideId`, `sectionId`, `after?`              |                                               |
| `slide.set`       | `slideId`, `path`, `value?`                   | JSON pointer into the slide; no value deletes |
| `slide.replace`   | `slideId`, `slide`                            | What the source drawer emits                  |
| `block.insert`    | `slideId`, `slot`, `after?`, `block`          | `slot` is a layout slot or `plate`            |
| `block.remove`    | `slideId`, `blockId`                          |                                               |
| `block.move`      | `slideId`, `blockId`, `slot`, `after?`        |                                               |
| `block.set`       | `slideId`, `blockId`, `path`, `value?`        | JSON pointer into the block                   |
| `text.replace`    | `slideId`, `blockId`, `path`, `range`, `text` | Typing, coalesced; range in the markup string |
| `section.set`     | `sections`                                    | The only place order lives                    |
| `asset.set`       | `asset`                                       |                                               |
| `asset.remove`    | `assetId`                                     |                                               |
| `deck.set`        | `path`, `value?`                              | title, theme, defaults                        |
| `version.restore` | `n`                                           | A mutation, so it is undoable                 |

## Errors

| Class                 | HTTP | When                                                                                    |
| --------------------- | ---- | --------------------------------------------------------------------------------------- |
| `TypeError`           | 400  | malformed input, with the Zod path                                                      |
| `RangeError`          | 404  | an unknown action, label or id                                                          |
| `ConflictError`       | 409  | a stale baseRevision or a held lease, with the holder and the current document attached |
| `NotImplementedError` | 501  | an action declared in the table whose implementation lands in a later milestone         |
| `Error`               | 500  | renderer and codec failures                                                             |
