# Turboslide round four, the ship step

By the ship step, 2026-09-14, from 21:27 PDT, on the shared checkout at `/Users/kevinliu/repos/Turboslide` over `main` at `04d95c2` (the round four ship `43707c3` of 2026-09-14 with another workflow's round five commits, hotfix 2 `cb646e9` and its production table `02e92c6`, B4's poll cadence `720ccb4`, hotfix 3 `1e78132` and `6679c54` and its production table `04d95c2`). The task was written against `main` at `d5d7f07`; the round's main commit and its hotfixes had shipped before this step began, so this step ships what `43707c3`'s message promised as the second commit: the day 7 fixer round (MILESTONES-4 "Fixer round"; `build-4/integrator.md` section 22, `b3.md` section 8, `b4.md` section 7) and the verifier's record (`VERIFICATION-4.md`, two passes, never committed before this step). Section numbers refer to SPEC-4 unless prefixed; the findings are VERIFICATION-4 section 11's. No `pnpm install`, `pnpm add`, `pnpm exec` or `pnpm build` ran outside `scripts/check.mjs`'s own steps; Docker never; no secret was printed; the only git write commands are the two explicit path commits and their pushes.

## 1. The tree at the start, and the preconditions

`git status --short` at 21:27 PDT: 49 modified tracked files and, untracked, 128 files under `verification-4/` (the verifier's pass 1 and pass 2 evidence), `VERIFICATION-4.md`, `apps/studio/src/components/home/home-meta.ts`, `packages/render/src/dither-walk.ts` and `build-4/integrator-perf/perf-budget-preview-fix-2026-09-14.json`; every modified file is named in a fixer's file list (`integrator.md` 22.4, `b3.md` 8.5, `b4.md` 7.6) or is the check's own output (the three `verification-3/` audit records steps 20 and 27 rewrite, `verification-4/README.md`, the two `design-4/*/previews/*.json` records formatted by step 19). Untracked and never touched or committed: `.github/workflows/check.yml`, the nine mockup PNGs over 1 MB under `design-4/*/previews/home*.png`, `scripts/probes/editor-walk-probe.mjs` (hotfix 4's probe, in flight: its note `build-4/hotfix-4.md` does not exist yet and its walk against production held `.turboslide/e2e.lock` from 20:58 PDT through this step's chain), and `scripts/__pycache__/` (ignored since this step). `research-5/`, `design-5/`, `SPEC-5.md` and `MILESTONES-5.md` are committed by the other workflow and clean.

Preconditions read before anything moved: `cd packages/agent && node src/generate/main.ts --check` answers "every committed contract is current"; `git grep -n '<<<<<<<'` is empty over the whole tree, prose included; no listener on 4321 or 4341 to 4346 (`lsof`); `gh auth status` logged in as `Kevin-Liu-01` with the `repo` scope; `vercel whoami` `kevin-liu-01`; Node 24.13.0; the repository's local identity `Kevin <kk23907751@gmail.com>` (the identity of every commit since `43707c3`). Production before the ship: `turboslide-jfh7h0y4u-kl01s-projects.vercel.app`, the git deploy of `04d95c2` (created 20:17:28 PDT), aliased to `https://turboslide.vercel.app`, its `/home` loading `assets/index-CDkNPPA9.js` (the pre fix entry).

## 2. The edits before the commit

1. Finding 20 (the two hotfix notes and hotfix 2's walk record committed unformatted, so `HEAD` itself failed check step 19): `node_modules/.bin/prettier --write docs/gslides-parity/build-4/hotfix-2.md docs/gslides-parity/build-4/hotfix-2/walk.json docs/gslides-parity/build-4/hotfix-3.md`. Checked: the prose outside the fenced code blocks is identical with whitespace removed and table rules normalized; inside the fences Prettier gave the embedded scripts trailing commas and line wraps (the `walk.json` change is whitespace). `__pycache__/` joined `.gitignore` with a comment.
2. Finding 9 (one sentence in `README.md` and the same claim in `docs/performance.md`'s "Every route ships only its own code" row named the pre fix entry chunk in the present tense): both now read the 914,233 bytes as that run's number and the tree's 577,949 bytes with the source (check step 6, 2026-09-14). `docs/performance.md` was reformatted by Prettier afterwards (the table's column widths). Both files are B5's; the verifier assigned the README line to the ship step and the row is the same class.

## 3. The check chain on the tree that ships

`node scripts/check.mjs` in full through `verification-4/check-ship/run-check-chain.sh` (the verifier's pass 2 driver with its outputs under `check-ship/`; the logs are gitignored by `*.log`, so `check-ship/check-summary-2026-09-14.txt` carries every step line and the reason of every failing step), 21:32:10 to 22:53:48 PDT in nine segments under a load average of 13 to 30 (another workflow's editor walk against production with its Chromium, then its `resize.spec.ts` against a dev server on 4350, ran beside it the whole time). 22 of 31 steps ok; the nine that fail, each for the reason in its row:

| Step | Result                   | What it says                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---- | ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 3    | FAIL, 2.9 s              | by construction: `generate:contracts` reports "14 files current" and the diff is the fix round's label "Line length (ch)" in `docs/grammar.md`, `skills/turboslide-create/references/grammar.md`, `packages/agent/generated/{mcp-tools,openapi}.json`, which the working tree carries and `HEAD` does not; `cd packages/agent && node src/generate/main.ts --check` says "every committed contract is current"; the four files are in the ship commit, so the step passes on the committed tree                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 5    | FAIL, 110.7 s            | 283 files, 3,005 tests passed, 3 failed, 2 skipped: three 5 s timeouts in `apps/cli` under a load average of 22 (`canvas.test.ts` "converts one slide of every kind" timed out, then its next row met "baseRevision 25 is stale; the document is at revision 26", the timed out conversion landing late; `mcp.test.ts` "the dispatcher has deck.set" timed out). The two files pass in isolation at 21:35 PDT, 9 of 9 in 10.5 s, on the same tree (before any other agent's edit). A full rerun after the chain (`--only 5`, 22:54 PDT, `check-ship/check-only-5-rerun.log`) read 285 files, 3,022 passed, 3 failed: `packages/export` `gslides-fixture.test.ts` (the PDF page count), `packages/headless` `canvas-measure.test.ts` (the heading measures 1,326 px where the fixture carries 901) and `apps/cli` `canvas.test.ts` (the title's pos), each deterministic in isolation (1 failed of 15, 1 of 6, 1 of 5) and each a render or measure result: another agent's in flight edits to `packages/render/src/{slide.ts,block-css.ts,blocks/*.ts}`, `packages/schema/src/{catalog.ts,deck.ts}` and their tests reached the shared checkout between 22:28 and 22:49 PDT (section 5), none of them in this commit, and the two extra test files are theirs. The fix round tree's own suite is the verifier's pass 2 step 5 (283 files, 3,008 passed) and this chain's step 5 with its three load timeouts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 19   | FAIL, 107.0 s            | `prettier --check .` names one file, `scripts/probes/editor-walk-probe.mjs`, another agent's untracked in flight probe; every file this commit carries is Prettier clean, including the two hotfix notes and the walk record formatted in section 2 and the three audit records the chain rewrote (formatted after the chain, as merge 2 and pass 2 did). The committed tree's cleanliness is proved after the commit with `git ls-files -z \| xargs -0 prettier --check` (section 4)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 20   | FAIL, 1,109.5 s          | the parity audit on the runner's dev server (4321, the file store): 2,979 pass, 5 fail, 385 skipped over 3,369 rows against the committed record's 2,980 / 5 / 384 (`audit-compare.mjs`: one row changed state, `effects:textBlock2 format.text.italic` pass to skip with "could not select the text", the harness class pass 1 met on the same row; the three `defaultViewWords` rows differ only by the scratch deck's name). The five are round three's recorded rows (`title.account.signIn`, `insert.comment`, `format.dropShadow`, the guide context menu, `view.guides.delete`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 21   | FAIL, 1,285.0 s          | the fifteen round two specs: 71 tests, 58 passed, 7 failed, 6 did not run (pass 2 read 57 / 9 / 5): `canvas.spec.ts:323`, `charts.spec.ts:204`, `objects.spec.ts:159`, `tables.spec.ts:191` (the round two recorded states), `gslides-actions.spec.ts:321` and `:704` (the reseed class of integrator.md 22.3 item 5; `:99` and `:205` pass, `filmstrip.spec.ts:314` passes), and `text-styles.spec.ts:203` ("Cmd ] and Cmd [ indent a text block by 64 px"): after Cmd ] the version log read 8 records where the row expects 7, one more than the one write, on the check's shared server after fourteen specs; the row passed in the verifier's pass 2 chain on the same fix round code and could not be rerun in isolation on the ship tree because another agent's edits to `useEditorKeys.ts`, `InlineText.tsx` and `Editor.tsx` reached the checkout before the rerun (section 5). Recorded as a finding for the round two owner and B4 (section 6). Step 21 ran 22:01 to 22:22 PDT, before any other agent's edit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 25   | FAIL, 8.7 s              | Docker's disk: "write /docs/gslides-parity/verification-4/perf-budget-preview-ay9oweo3y-pass2-2026-09-14.json: no space left on device" while the image copies the repository (round three's finding 25, pass 1 and pass 2 the same)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 26   | FAIL, 766.9 s            | the eight round three specs and `home-page.spec.ts`: 49 tests, 25 passed, 6 failed, 18 did not run: `comments.spec.ts:87`, `presence.spec.ts:158`, `realtime.spec.ts:188`, `share.spec.ts:320`, `versions-by-author.spec.ts:96` (round three's recorded rows, the same as pass 2) and `security.spec.ts:177` ("the eight html payloads are inert") with "Execution context was destroyed, most likely because of a navigation": the step ran 22:27 to 22:40 PDT on the check's Vite dev server and another agent's first source edits landed at 22:28 to 22:39 PDT (section 5), each a page reload under the spec; pass 2's `security.spec.ts:352` passed. `home-page.spec.ts`'s rows passed. `decks/e2e-realtime-mu28ho2e/` left by `realtime.spec.ts` was removed by hand at the close                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 27   | FAIL, 579.6 s (recorded) | the layout shift audit against `vite preview` on 4344 over step 6's build (21:34 PDT, before any other agent's edit): 109 cells, 66 failures, 7 skipped states against `HEAD`'s 75 and 7; per cell against `HEAD` (`layout-shift-compare.mjs`): 0 cells gained an entry, 9 have fewer (the four `deck <width> <appearance> hash` cells, `decks 1440 light`, `decks 1440 dark`, `decks 1440 dark listView`, `decks 1280 light`, `deck 1440 light F`), 100 the same; 56 of 109 cells at zero load entries (`HEAD` 53). The gate holds; the 66 are round three's recorded state (VERIFICATION-3 finding 26). The two outputs were formatted for step 19 afterwards                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 31   | FAIL, 221.2 s            | the node-server build served on 4321 with the tmp store and `perf-budget.mjs --profile local --write --runs 3`: 128 of 151 budgets met over 199 rows (pass 2 122, the integrator's fix round run 118, pass 1 104), `.turboslide/perf-budget.json` copied to `verification-4/perf-budget-node-server-ship-2026-09-14.json` and laid beside day 0 and pass 2 (`perf-budget-node-server-ship-against-{day0,pass2}-2026-09-14.md`: 93 rows gained on pass 2, 23 lost, 64 unchanged). The 23 misses are the recorded classes of VERIFICATION-4 section 5.5 and nothing else: the sixteen `js decoded` rows (`/home` 678,544 B against 600,000, `/decks/trash` 633,187, `/decks` 2,152,278 by the viewport preloads, `/`, `/new` and `/edit` 2,235,108 to 2,237,854 against 2,000,000, `/deck` 1,844,190 against 1,000,000, `/present` 1,365,383 against 1,200,000), the five DOM rows (`/deck` 2,502 to 2,503, `/edit` 2,137 to 2,153, the filmstrip 2,688 against 1,500, the same counts as pass 2), the memory tier's "new slide to the saved revision" 2,021 ms against 250 (the checkpointer's cadence) and the scratch deck's home card 1,584 ms against 1,500 (pass 2 2,889). Met, and not in pass 2: every route's first byte, LCP and ready including `/` warm ready and `/decks` cold; the largest chunk 578,560 B (`index-zmcb2DMX.js`) under 600,000 on every route; the write path's capture 1,130 ms and the text burst's rows; `decks->edit` in page; the idle poll at 2 calls a minute. The build the step made at 22:49:59 PDT carries another agent's partial edits of 22:28 to 22:49 (section 5), so these numbers are the fix round tree plus that work; the verifier's pass 2 step 31 (122 of 151) is the clean reading of the fix round tree. The chain's `&&` stops before the per route preload ceilings, as in pass 2; the verifier's pass 2 numbers stand for them (`/decks` 729,747 B against 600,000, the one FAIL). The step's `--write` deck `untitled-20260915-<id>` on the tmp store was removed with its thumbnail and worker cache folders |

The steps that pass, with the numbers that matter: step 4 `tsc -b` clean; step 6 `pnpm build` with the largest client chunk `assets/index-CQkTTK17.js` 577,949 B under the 600,000 B ceiling (reported by 0.27's rule, no vendor chunk), the marker in 0 of 95 client files, node builtins in 0 of 71, the greps clean; step 12 the Perfect export compare of the GT deck (170 pairs, 0 over budget); step 17 `viewer.spec.ts` 8 of 8 with B3's extended row; step 18 the chrome lint on the six URLs at three widths in both appearances, 0 with findings, 0 states unapplied; steps 22 to 24 (the fixture in both modes, the fonts build check, the canvas fidelity gate: 342 pairs, 0 over budget, worst 0.262 percent); step 28 0 advisories; step 29 the brand build (31 files match the manifest, the rebuild and the facts of 6.4; the note that a newer production run than `facts.json`'s source exists under `verification-4/`, never a failure), the `/home` assets (48 files), the shape table and the native record (4 wasm outputs match `BUILD-RECORD.json`, the Linux addon pending); step 30 the Vercel output check 38 of 38 with eight functions at 127.4 MB against 250 MB.

## 4. The commit

One commit on `main`, by explicit path list, with the repository's local identity and the trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`; its hash is in section 7 and in `BUILD-STATUS-4.md`. What it carries, by class:

- The fixer round's source and tests: the integrator's (`packages/render/src/dither-key.ts` and the new `dither-walk.ts` with `packages/render/package.json`, the four importers of the walk in `packages/lint`, `packages/export` and `packages/materials`, the new `apps/studio/src/components/home/home-meta.ts` with `copy.ts`, `copy.test.ts`, `facts-data.ts` and `routes/home.tsx`, `packages/chrome/src/dialogs/special-characters-data.ts`, `packages/chrome/src/inspector/{props.ts,seg.tsx,select.tsx}`, `packages/schema/src/blocks.ts`, the generated `docs/grammar.md`, `skills/turboslide-create/references/grammar.md` and `packages/agent/generated/{mcp-tools,openapi}.json`, `packages/theme/brand/facts.json` with `apps/studio/public/brand-manifest.json`, `scripts/{perf-budget,gslides-parity-audit,check-client-bundle}.mjs`, `README.md`, `docs/performance.md`), B3's (`apps/studio/src/routes/{deck.$deckId.tsx,edit.$deckId.css,embed.$deckId.tsx}`, `components/DeckViewer.tsx`, `server/{decks.ts,write.ts,write.test.ts}`, `e2e/{editor,viewer}.spec.ts`), B4's (`packages/chrome/src/{Filmstrip,Sidebar}.css`, `packages/viewer/src/GridView.css`, `e2e/window-api.spec.ts`) and `apps/studio/src/editor/controller.tsx` as the fixer round's content alone (below).
- The verifier's record: `VERIFICATION-4.md` and every untracked file under `verification-4/` (pass 1 and pass 2: the check chains' drivers and flags, the perf budget JSONs with their tables and summaries, the parity audits, the chrome lint and tooltip audit outputs, the layout shift records, the brand checks and renders, the shots, the specs' log record, the smokes and probes; 128 files, 22.5 MB, the twelve full page `/home` shots 1.0 to 1.5 MB each; no `.log` file, since `*.log` is ignored and the verifier's record names the summaries instead), `verification-4/README.md`, and the check's three rewritten records under `verification-3/` (`parity-audit.json`, `layout-shift.json`, `layout-shift.md`, this chain's output, formatted).
- This step's record: `verification-4/check-ship/{run-check-chain.sh,chain-done.flag,check-summary-2026-09-14.txt}`, `perf-budget-node-server-ship-2026-09-14.json` with its two `against` tables, the fixer notes (`build-4/{integrator,b3,b4}.md`, `build-4/integrator-perf/perf-budget-preview-fix-2026-09-14.json`), the two hotfix notes and the walk record formatted, the two `design-4/*/previews/*.json` records formatted by step 19, `.gitignore`, and this note.

Never in the commit (the orchestrator's list and the other agents' work): `.github/workflows/check.yml`; the nine `design-4/*/previews/home*.png` mockups over 1 MB; `research-5/`, `design-5/`, `SPEC-5.md`, `MILESTONES-5.md` (committed by their workflow already); `scripts/probes/editor-walk-probe.mjs`, `build-4/hotfix-4.md` and every source, test and snapshot file of hotfix 4's fixer named in section 5; `scripts/__pycache__/`; nothing under `.turboslide/`, `.vercel/`, `dist/`, `decks/`, `node_modules/`, no log or env file, no scratch deck.

`apps/studio/src/editor/controller.tsx` is the one file both the fixer round and hotfix 4's in flight work touched (the integrator's `acknowledgedAbove` change of 22.1 and, from 22:41 PDT, hotfix 4's `import { typingKeyOf } from './typing-key'` with the function moved out). The commit carries the fixer round's content alone: `HEAD`'s file with the integrator's hunks (27 insertions, 4 deletions; `ACK_WAIT_MS`, the two `const base`, the two `async` settles, the two `revision: await acknowledgedAbove(base)` and the function), rebuilt from the working tree by removing the import and restoring `HEAD`'s `typingKeyOf` (`.turboslide/ship4/controller.fix.tsx`, Prettier clean with the repository's config; the rebuilt file differs from the working tree by those two edits alone), written to the object store with `git hash-object -w` and placed in the index with `git update-index --cacheinfo`, so the working tree file, which hotfix 4 is editing, was never touched or reverted. That content is what `tsc -b` (step 4), the build (step 6, 21:34 PDT), the dev server of steps 17 to 21 and the verifier's pass 2 chain typechecked, built and ran; the integrator's fixer typechecked and tested it in isolation (integrator.md 22.2).

## 5. Another agent's edits during the chain, recorded

Hotfix 4's fixer (its note `build-4/hotfix-4.md`, 21:58 PDT, untracked) began editing sources in the shared checkout at 22:28 PDT while this chain ran, and was still editing at the commit: `packages/viewer/src/gesture-life.ts` (22:28), `packages/render/src/blocks/{context,misc,panel}.ts` and `block-css.ts` (22:31), `packages/viewer/src/InlineText.tsx` (22:33), `packages/chrome/src/useEditorKeys.ts` (22:33), `packages/theme/src/gt-ink-paper/sheet.css` (22:34), the viewer, render and chrome tests and the render snapshot (22:32 to 22:39), `apps/studio/src/editor/typing-key.ts` and its test (22:41), `controller.tsx` (22:41), `packages/schema/src/catalog.ts` (22:45), `packages/viewer/src/Editor.tsx` (22:45), `packages/schema/src/deck.ts` (22:48), `packages/render/src/slide.ts` (22:48), the schema tests (22:49), the wasm outputs and the darwin addon rebuilt by its runs. What that means for this record: steps 1 to 21 and 27 measured the fix round tree (step 6's build at 21:34, step 21 ending 22:22, step 27 on that build); step 26 (22:27 to 22:40) ran on a Vite dev server that reloaded its pages as those files landed (`security.spec.ts:177`'s "Execution context was destroyed"); steps 28 to 31 and the step 5 rerun (22:40 to 22:56) ran on the fix round tree plus that partial work (step 31's node-server build at 22:49:59). None of those files is in the commit; production is built from the commit. The rule this step followed: never edit or revert another agent's file; the one shared file is staged as section 4 says.

## 6. Findings of the ship step

1. `text-styles.spec.ts:203` (step 21, one run, before any other agent's edit): after Cmd ] on the selected paragraph the version log grew by two records where the row expects one (8 where 7), on the check's shared server after fourteen specs; the row passed on the same code in the verifier's pass 2 chain. Not rerun in isolation on the ship tree (section 5 says why). Severity 2, for the round two owner of the spec and B4 (`controller.tsx`'s undo grouping and the version records): whether a second record is a checkpoint of an earlier spec's write landing as a version, or the indent write recorded twice. The production write path is the probe's and the audit's, both green on the deployment (section 7).
2. The three cli timeouts of step 5 under load (section 3) and the `format.text.italic` skip of step 20 are the harness classes the verifier recorded; no product change.
3. Step 19 on the whole working tree cannot pass while another agent's unformatted untracked file sits in it; the committed tree is clean (section 4's proof).

## 7. Production

The commit `2ee1db4a7bd60ec7c8df6ec496332b02bc28aea8` was pushed to `origin/main` at 23:04:52 PDT (`04d95c2..2ee1db4`). Vercel's git deploy `dpl_9qwLq7hWVbdg99uo34NAzrZ9naVg` (`turboslide-42f8yn887-kl01s-projects.vercel.app`) was created at 23:04:56 PDT and is Ready, aliased to `https://turboslide.vercel.app`; `/home` loads `assets/index-CQkTTK17.js`, the entry chunk the ship tree's build made at check step 6 (577,949 bytes), where the deployment before it (`04d95c2`, `turboslide-jfh7h0y4u`) loaded `index-CDkNPPA9.js` (916,787 bytes). The poll (`verification-4/production-ship/deploy-poll-2026-09-14.txt`) read the alias every 20 s; its state column never matched `vercel ls`'s bullet, so it ran to its cap while the entry had already changed, and `vercel inspect` confirmed the deployment at 23:15 PDT.

The production table is VERIFICATION-4.md section 16.1 (appended by this step) with the performance rows beside day 0 and pass 2; its files are under `verification-4/production-ship/`, `verification-4/production-home-*` and `verification-4/perf-budget-production-ship-*`. In one paragraph: the `/new` write probe answers all 27 rows on production (twelve edits each at exactly the next revision, no stale message, one roster row alone, a second tab as one other participant, the deck trashed and deleted forever); the hosted smoke is 37 of 37 with the bearer never printed; the perf budget meets 119 of 151 rows (pass 2's production 93, pass 1's 86, day 0's 58 of 121; the fix round's preview 113 of 157), every route's first byte, LCP and ready under their ceilings but the `'data-only'` routes' first byte (`/edit` 415 / 385, `/present` 340 / 346 ms), `/edit`'s FCP, `/decks` warm LCP 892, `/decks/trash` warm first byte 344, `/deck` warm LCP 408 and `/present` warm ready 572, the entry chunk 577,949 bytes under 600,000 on every route with the sixteen `js decoded` rows still over, the filmstrip 10.3 ms p95 and 118 to 120 fps with 2,958 nodes after GC, the idle editor at 2 calls a minute, the twins 0 of 16 re-fetched, `home->new` 326 ms with no prerender activation; the head and the static layer answer the right types from the CDN; `/home` at 1440 and 390 in both appearances has no horizontal overflow, 16 pictures and the hero twin as its LCP element; a rectangle selected on a dark slide of a scratch copy shows the ring and the chip in `rgb(61, 134, 240)` (`--pt-select` `#3d86f0`) and the copy was trashed and deleted forever (`/deck` 404). The repository description is `docs/brand.md`'s sentence; the four topics could not be added (GitHub's cap of 20 topics is full, and the ruling allows no removal); the homepage and the social preview stay Kevin's.

Two scratch decks were written on Kevin's production store by this step and both are gone: `untitled-20260915-vxc7` (the probe, trashed and deleted by the probe) and `ship4-selection-mu2ab01v` (the selection shot, trashed and deleted through the window API, `/deck` 404). The perf run wrote nothing (no `--write`). The `/decks` list and its trash otherwise hold what they held before the step (BASELINE.md finding 11's decks are Kevin's to keep or remove).

The production table commit ("Turboslide round four: the production table", the same trailer) carries this section, VERIFICATION-4.md 16.1, `BUILD-STATUS-4.md`'s closing section, `verification-4/README.md`'s rows and the files above; its hash is in `BUILD-STATUS-4.md` and the orchestrator's report.

## 8. Deviations of the ship step, recorded

1. The commit's subject is not the task's given title ("Turboslide round four: the Turboslide identity, the home page, route and editor performance, the selection blue"): that subject is `43707c3`'s, shipped before this step began, and a second commit under the same subject would name two different trees; the subject names what this commit is, the fix round and the verifier's record, and the body carries the acceptance summary the task asked for.
2. The browser runs against production (the write probe, the perf run, the `/home` shots, the selection shot) ran without taking `.turboslide/e2e.lock`, which another agent held from 22:53 PDT for `resize.spec.ts` and then its editor walk against its own dev server on 4350 (23:03 onwards). The lock serializes Playwright runs against the checkout's shared store and servers; these runs touched production alone and no local server or `decks/` folder, the verifier's pass 2 ran its deployment perf runs and audits outside the lock the same way, and waiting on a walk that took over an hour the last time would have held the ship for it. The machine's load average during the runs was 3 to 8 and is recorded beside the numbers.
3. `node scripts/build-brand.ts --facts` was not rerun (integrator.md 22.3 item 10): `facts.json`'s measured source stays pass 1's production run (`perf-budget-production-2026-09-14.json`), the run `copy.ts` and `copy.test.ts` are pinned to, while step 29 notes that newer production runs exist (pass 2's and this step's) and never fails on it. Adopting a newer run changes `facts.json`'s sha256, which `facts-data.ts` pins and `scripts/build-home-assets.ts` regenerates only by re-shooting the sixteen pictures; the integrator or B5 decides which run the facts name in round five.
4. `check.mjs` step 19 on the whole working tree and the full `pnpm test` rerun after the chain cannot be green while another agent's in flight files sit in the shared checkout (section 5); the committed tree's step 19 is proved by the tracked file check of section 4 (`check-ship/prettier-committed-tree-2026-09-14.txt`) and its test suite by the chain's step 5 before any other agent's edit plus the two files' isolated rerun.
5. The perf run's transitions and filmstrip rows measured production under another agent's Chromium walk on the same machine (load 3 to 8); the numbers stand as taken, and the filmstrip's 2,958 nodes after GC (the pass 2 preview read 2,817 on the same code) is within the run to run range the verifier recorded for that row.
6. The step 31 preload ceilings tail (`check-client-bundle.mjs --base`) did not run in the chain (the `&&` stops at the perf failure, as in pass 2) and was not run by hand: the verifier's pass 2 numbers stand (`/decks` 729,747 B against 600,000, the one FAIL, by the viewport preloads).
7. The tmp store's scratch deck of step 31 (`untitled-20260915-*` under the runner's tmp store with its thumbnail and worker cache folders) and `decks/e2e-realtime-mu28ho2e/` (step 26's `realtime.spec.ts`) were removed by hand at the close; the runner's node server on 4321 and the driver's `vite preview` on 4344 (whose pid the driver lost, so it was stopped by port) are down; no dev server, probe, browser or Playwright process of this step remains; the other agent's dev server on 4350 and its walk were left running.
