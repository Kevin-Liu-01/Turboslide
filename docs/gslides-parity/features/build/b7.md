# B7, the server and the exports

## Features round, ship one

Builder B7 of the features round, ship one (docs/FEATURES.md sections 3.4, 4.7, 4.8 and 4.11, the lane table of section 6, the rows of 7.1), on the shared checkout at `88b68e7` on 2026-09-22 while the other lanes landed beside it (the tree carried B2's fonts, B3's editor, B4's matrix, B1's dialog and B6's routes as this lane worked; every file below is B7's alone). The dev server was port 4417 (`TURBOSLIDE_STORE=tmp` under a private `TMPDIR` in the scratchpad, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, two 48 character secrets from `openssl rand -hex 24`; started once with `TURBOSLIDE_SVG_RASTER` unset and once with it set to `1`), stopped before the return. Playwright (playwright-core, Chrome for Testing 1217) ran against 4417 under `.turboslide/e2e.lock` (taken with the `until mkdir` loop, released by the runner's trap; no lock is held at the return). No git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran. Every scratch deck was made on the tmp store of 4417, trashed and removed through the window API at the end of its drive, and left with the store when the server stopped; nothing was created on a deployment. No token, cookie or secret was printed. The temporary diagnostic test this lane wrote under `packages/export/src/scene/` was removed before the return.

### 1. The items, their files and their hunks

Each item is one commit's worth; a red row's item reverts alone. The hunks are named by the symbol they add or change.

| Item                                                        | Files and hunks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Rows                                                                                                         | Unit tests                                                                                                                                                                                                                                                                                                           |
| ----------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 4.8 the logo role at 3x in the exports                      | `packages/export/src/scene/extract.ts`: `THREE_X_KINDS` gains `logo`; the header comment; `tagKitLogoElements` (new: the kit's footer and title slot pictures tagged as `mark` rasters with rids past `tagRasterElements`'s, the two images' decode awaited first); in `extractScenes`, `kitLogo` and the flatten `shotScales` (`[2, 3]` when the kit draws a picture logo), the kit tags after `tagRasterElements` on every page, `logoBlocks` per slide, the filter that drops an undrawn kit raster, the flatten `wantRasters` (the wordmark and the kit logos), `scaleKind` before `rasterScaleFor`; the re export of the three readers. `packages/export/src/scene/kit-logos.ts` (new, schema imports alone): `KIT_LOGO_BLOCK_IDS`, `kitHasPictureLogo`, `logoBlockIds`. `packages/export/src/pptx/build.ts`: the import and the flatten overlay loop after the sheet raster (the kit logo rasters placed at their boxes through `addRaster`, the wordmark on the master being the precedent) | `logos.export.pdf-pptx-crisp`                                                                                | `packages/export/src/scene/kit-logos.test.ts` (new, 5 tests): `logo` is a 3x kind under auto and the forced policies and the 1x types hold; `logoBlockIds` reads picture and shot blocks through composites and never a missing asset; an opener's plate; `kitHasPictureLogo` on the six kit shapes; the two kit ids |
| 4.7 the seller's svg sentence                               | `apps/studio/src/server/upload.ts`: `UPLOAD_REASONS.notSvg` ("SVG files are not accepted yet. Export the logo as a PNG and upload that"), `isSvgRefusal`, `uploadFailureReason` mapping `not_svg`; the header comment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `logos.intake.svg-sentence` (the server half; the chrome half is request R2)                                 | `upload.test.ts` "names the svg sentence of the features round as a whole sentence, and reads the intake's line"                                                                                                                                                                                                     |
| 4.7, 4.9 the two deployment variables                       | `apps/studio/src/server/flags.ts`: `LOGO_UPSTREAM_ENV`, `LogoUpstreamMode`, `logoUpstreamMode`, `SVG_RASTER_ENV`, `svgRasterOn` (read from the environment, never from the flag reader)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | the `logos.index.*` rows read the first through B6's `logo-index.ts`; `logos.intake.svg-sentence` the second | `flags.test.ts` "the features round variables" (3 tests, one pinning that `logo-index.ts` agrees on the name and the three values)                                                                                                                                                                                   |
| 4.11 the logo ids on the window transport (B6's R3)         | `apps/studio/src/server/agent-actions.ts`: `SERVER_SIDE_WINDOW_ACTIONS_F1_IDS` and `SERVER_SIDE_WINDOW_ACTIONS_F1` (filtered through `isActionId`, the pattern of the pending list, so the file typechecked before B6's schema entries landed and carries all three now), appended to `SERVER_SIDE_WINDOW_ACTIONS`; `logo.insert` in `DRAFT_CREATING_ACTIONS`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `logos.agent.search-insert`, `logos.insert.one-click-asset` (B6's rows; this is their transport)             | `agent-actions.test.ts` "carries the logo picker's three ids once the action table names them"                                                                                                                                                                                                                       |
| 4.11 the logo handlers on the deck dispatcher (B6's R4)     | `apps/studio/src/server/actions.ts`: `LOGO_ACTION_IDS`, `registerLogoActionsLazily` (B6's `registerLogoActions` over a dispatcher loaded on the first call, the way the materials package loads, because `logos.ts` imports sharp and the store client at its top), the call after `registerAssistActions`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | the same                                                                                                     | none of its own; `tsc -b apps/studio` and the export drive's `asset.add` path through the same dispatcher                                                                                                                                                                                                            |
| 4.7 P1 the URL intake's sentence and the User-Agent         | `packages/headless/src/capture/shared.ts`: `ALLOWLIST_SENTENCE`, `AllowHostOptions.allowPaths`, `assertAllowedHost` (the seller's sentence when paths are off, the `--allow` line when on), `fetchAllowed` passing `allowPaths`, `turboslideUserAgent`, the `user-agent` header on `pinnedFetch`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `logos.intake.url-sentence`                                                                                  | `shared.test.ts` "the features round (docs/FEATURES.md 4.7)" (3 tests) and the pin at "the process policy is the default the studio sets once" moved to the sentence                                                                                                                                                 |
| 4.7 P1 the hosted svg branch behind `TURBOSLIDE_SVG_RASTER` | `packages/headless/src/capture/shared.ts`: `IntakePolicy.svgRaster`, `SvgRasterPolicy`, `svgRasterPolicy`, `imageInfo` keeping an svg hosted under the policy (the refusal line unchanged). `packages/headless/src/capture/intake.ts`: `AssetIntakeOptions.svgRaster`, `SVG_RASTER_BOX`, `SVG_RASTER_SCALE`, `svgRasterSize`, `rasterizeSvg` (sharp at a density that draws at the target, `fit: 'fill'` on the computed box), the svg branch of `addAsset` (the sanitized source kept, one PNG twin at scale 3, the drop named in the warnings), the header comment. `apps/studio/src/server/actions.ts`: `setStudioIntakePolicy` carrying `svgRaster` from `svgRasterOn()` with B6's `sanitizeLogoSvg` (a pure import, `logo-sanitize.ts`)                                                                                                                                                                                                                                                       | `logos.intake.svg-sentence` (the raster half)                                                                | `intake-svg.test.ts` (new, 4 tests): the fit and the 3x size; hosted with the policy one PNG twin at 792 by 198 with the source kept and the `<script>` drop named; hosted without the policy the line it always had; a checkout keeps the svg bytes                                                                 |
| 3.4 the fonts' export path                                  | nothing to change: `pptx/fonts-map.ts` reads `FONT_NAMES` whole (`CATALOG_NAMES`) and the PDF embeds what the browser drew, so B2's eight `FONT_NAMES` entries (on the tree) are the whole export change; the eight folders sit at `packages/fonts/assets/<id>/*.woff2` beside `LICENSE`, which `PACKAGES_PATTERN`'s `fonts/assets/*/*` in `apps/studio/vite.deploy.config.ts` already names. The `packages` server asset group's size is measured on the first preview deploy by the integrator and never typed here                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `fonts.export.editable-names-face`, `fonts.export.pdf-face` (B2's rows, extended by Geist and Fraunces)      | none                                                                                                                                                                                                                                                                                                                 |

Not this ship: the `shader.frame` handler, the 30 s bound and the `RenderError` sentence in `actions.ts`, `download.ts`'s web page frame and the report's missing frame row are ship two's items of this lane (docs/FEATURES.md 5.5, 5.8) and were not touched.

### 2. What the drives measured on 4417

The drives are two playwright-core scripts in this lane's scratchpad (`drive-export.mjs`, `drive-intake.mjs`), each over the window API of a fresh `/new` draft; the JSON records sit beside their files there. Times in UTC.

**`logos.export.pdf-pptx-crisp` (14:17Z, the raster flag unset; 14:19Z again with it set, the same numbers).** A 396 by 252 PNG added as `role: 'logo'` (a logo insert's 3x twins, stood in for by the spec's PNG since B6's `logo.insert` fetches the network), `brand.set /mark { kind: 'picture' }`, `/footer/logo 'picture'` and `/footer/assetId`, then the toolbar's New slide and a `picture` block of that asset at 160 by 100 on the second slide (a block insert on the title slide converts it to a canvas, which the first run measured: its mark became a block shot at 2x, the drive's artefact and not the export's). The editor drew the footer picture and the title slot picture and no GT wordmark. The files, each downloaded through File > Download > Download options and read from the download's bytes:

- The PDF (22,194 bytes, 1.6 s): two image objects, 396 by 252 and 396 by 248, the title slot's twin and the picture block's; the first page carries the mark.
- The Perfect PowerPoint (45,183 bytes, 2.3 s): slide 1 the sheet at 3200 by 1800 and `ts:title#title-logo:2` at 396 by 252 in a 132 by 84 box (this lane's overlay, the 3x page opened for the flatten export because the kit draws a picture logo); slide 2 the sheet alone.
- The Editable text PowerPoint (21,770 bytes, 2.3 s): slide 1 `ts:title#title-logo:2` at 396 by 252 in a 132 by 84 box; slide 2 `ts:split-1#acme-mark:1` at 480 by 300 in a 160 by 100 box, the logo role shot at 3x (`THREE_X_KINDS`).

The footer's box is in none of the three files, and the row is not passed on this tree. The mechanism, measured with a temporary test that rendered the same deck with `renderDeck` and read the document: `frameBandHtml` (`packages/render/src/stage.ts` 168 to 174) writes the band's picture as `<div class="wordmark pos-bottom-left is-picture">` while the theme sheet hides `.wordmark:not(.ts-kit-wordmark)` whenever the kit draws the footer (`packages/render/src/theme-css.ts` 212 to 218, `kitDrawsFooter`); the viewer's `Frame.tsx` 52 adds `ts-kit-wordmark` and the editor shows the footer picture, and the export's document and the print document (the PDF) hide theirs, so a kit with a picture footer logo has no footer logo in any export today. This lane's tag found the image and measured it 0 by 0 (`width: auto` on a hidden element), and the raster left the scene. With the class added to the document string alone, the same test measured the footer at [72, 864, 28.28, 18] on the title slide and the content slide, shot it at 3x to 87 by 54 (the snapped 29 by 18 box) and the title slot to 396 by 252: the export path is ready and waits on request R1. `brand.export.pdf-logo` reads the title slot alone and is unaffected.

**`logos.intake.url-sentence` (14:01Z, 14:19Z).** `asset.add { url: 'https://thesvg.org/icons/figma/default.svg' }` from the page answered "Pictures can be fetched from these sites only: generaltranslation.com, prototemplate.com, glyphfield.com and Wikimedia Commons. Upload the file instead", no `--allow`; `asset.add` with `https://upload.wikimedia.org/wikipedia/commons/4/47/PNG_transparency_demonstration_1.png` answered an asset of 800 by 600 (the pinned fetch now sends `User-Agent: Turboslide/0.0.0 (+https://turboslide.vercel.app)`; audit-logos 16 measured 403 without it). Both halves of the row's interaction passed at the window API; the Image by URL dialog's own line is request R2.

**`logos.intake.svg-sentence` (14:01Z with the flag unset, 14:19Z with `TURBOSLIDE_SVG_RASTER=1`).** Flag unset: `asset.add` with an svg data URL answered the API's line "svg is not accepted here; send png, jpeg, webp or gif" (unchanged, as 4.7 asks); the snackbar the row reads is the viewer's mapping of that line (request R2), so the row's sentence half is not passed on this tree. Flag set: the same svg (a 200 by 50 viewBox carrying a `<script>`) landed as `{ size: [792, 198], scale: 3, twins: { neutral: 'assets/acme-svg.png' }, sourceFile: 'assets/acme-svg.source.svg' }`, 3x of its 264 by 66 fit, through the studio's dispatcher and B6's sanitizer; the row's raster half is on the tree behind the flag (docs/FEATURES.md section 8: the flag drops when the row has passed on production). The chooser's accept list is B1's and B6's (4.5).

No production deck was created or read; the production rows of this lane are the integrator's runs.

### 3. Commands and their results

- `node_modules/.bin/tsc -b packages/export`, `packages/headless`, `apps/cli`: clean. `tsc -b apps/studio`: clean for every file of this lane; the four errors it prints are B4's in progress `apps/studio/e2e/core/logos.spec.ts` (296 `dryRun` is not on `Counts`; 455 `headers` is possibly null, twice; 467 a `Record<string, string> | null` where `undefined` is wanted), not this lane's.
- Vitest, per package: `packages/export` `kit-logos.test.ts` 5 passed, `export-pptx.test.ts`, `report.test.ts`, `batch/merge.test.ts` 28 passed; `packages/headless` `src/capture` 3 files 28 passed (`shared.test.ts` with the three added and the moved pin, `intake-svg.test.ts` 4, `capture.test.ts`); `apps/studio` `flags.test.ts` 10, `upload.test.ts` 3, `agent-actions.test.ts` 6 passed. The browser test `extract.test.ts` was not run by this lane (it takes the fixture deck through the same `extractScenes`; the export drive covered the path on 4417).
- The two drives, section 2.

### 4. Requests

R1 (the integrator, `packages/render/src/stage.ts` `frameBandHtml`, 168 to 174; the file is in no lane's list): the band's div carries the class the viewer's Frame gives it, in both kit forms, so the theme sheet's `.wordmark:not(.ts-kit-wordmark) { display: none }` (`theme-css.ts` 217) no longer hides the footer logo in the export's document and the print document:

```ts
if (band.logo.kind === 'default')
  logo = `<div class="wordmark ts-kit-wordmark ${positionClass(band.logo.position)}" aria-hidden="true"><svg width="28" height="18" fill="currentColor"><use href="#gt-mark"/></svg></div>`;
else if (band.logo.kind === 'picture')
  logo = `<div class="wordmark ts-kit-wordmark ${positionClass(band.logo.position)} is-picture" aria-hidden="true" style="width:${band.logo.w}px"><img src="${escapeAttr(band.logo.src)}" width="${band.logo.w}" height="${band.logo.h}" alt=""></div>`;
```

`stage.test.ts` or `theme-css.test.ts` pins that a kit band's markup carries `ts-kit-wordmark`. Without it `logos.export.pdf-pptx-crisp` stays red on its footer half and every export of a kit with a picture footer logo lacks the footer logo (a defect older than this round: `brand.logo.use-on-every-slide` writes the footer slot and the PDF and the PowerPoints drop it). This lane's extractor and builder are ready for the class and change nothing else when it lands.

R2 (B3, `packages/viewer/src/Editor.tsx` `insertPictureFile` 3302 to 3390 and the By URL catch at 3446 to 3460, with `packages/viewer/src/picture-place.ts` `uploadFailureOf` and `uploadFailureSentence`, the integrator's file): the two sentences of 4.7 reach the seller as they stand. For `logos.intake.svg-sentence`: when `asset.add` refuses with the line `isSvgRefusal` reads (`apps/studio/src/server/upload.ts`: `/svg is not accepted here/`), the outcome's sentence is `UPLOAD_REASONS.notSvg` whole ("SVG files are not accepted yet. Export the logo as a PNG and upload that"), not "The picture could not be uploaded: the upload did not finish", which is what `uploadFailureOf` maps it to today (`did-not-finish`, the fallthrough); the same for the presigned route's `reason` when its `code` is `not_svg`. For `logos.intake.url-sentence`: when the server's message starts with "Pictures can be fetched from these sites only", the Image by URL dialog shows that sentence and not "the page could not read it and the server may not fetch from this host" (the `/allowlist|not a public address|private address/` branch no longer matches, because the sentence carries none of those words). One line each; the sentences are constants B3 may import from `@turboslide/headless/capture/shared` (`ALLOWLIST_SENTENCE`) and the studio's `upload.ts` (`UPLOAD_REASONS.notSvg`), or repeat by request in `packages/chrome/src/menus/strings.ts`.

R3 (B6, `apps/studio/src/server/logo-index.ts` 40 to 51): `LOGO_UPSTREAM_ENV`, `LogoUpstreamMode` and `logoUpstreamMode` are defined in `flags.ts` now (B6's R5); `logo-index.ts` may import them from `./flags` and drop its copy. Until then `flags.test.ts` pins that the two agree on the name and the three values, and nothing breaks either way.

R4 (B4, `docs/gslides-parity/focus/core-matrix.json` and the drivers): the three rows exist in the matrix; no driver names them yet on the tree (`grep url-sentence|svg-sentence|pdf-pptx-crisp` over `apps/studio/e2e/core` and `scripts/probes/core-walk` answers nothing). What the drives of section 2 read is what a driver reads: for `logos.export.pdf-pptx-crisp` the Perfect file's `ts:<slide>#title-logo:<n>` object at 396 by 252 and, once R1 lands, `#footer-logo:<n>` at 84 by 54 or more on every slide, the Editable file's the same plus a logo role block's PNG at 3x of its box; for the svg row `UPLOAD_REASONS.notSvg` in the snackbar once R2 lands, and with the preview's `TURBOSLIDE_SVG_RASTER=1` a `scale: 3` asset with a `.png` twin and a `.source.svg`.

R5 (the integrator, information): B2's note names `packages/headless/src/sheet.ts` 135, a fifth Inter stack without the fallback face, as "the integrator's or B7's file"; `sheet.ts` is not in this lane's list (B7 owns `capture/intake.ts` and `capture/shared.ts` in that package) and was not touched.

### 5. The P1 condition and the budget

The two P1 items (the URL sentence with the User-Agent; the svg branch behind the flag) landed beside the P0 items because their rows are `images` rows (unparkable, B4's note on each) and the integrator decides at the ship whether they count; each is one commit's worth and reverts alone (section 1). Neither adds a store call, a timer or a poll: the svg branch runs inside the `asset.add` write that exists, the User-Agent is a header on a fetch that exists, and the 3x page a flatten export opens for a kit with a picture logo is one more page in the export's own function call, never in the editor.
