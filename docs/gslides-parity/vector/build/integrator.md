# The integrator, the vector round

## Vector round

The integrator of the vector round (docs/VECTOR.md 5.1 to 5.3, 6.2; the prompt of 2026-09-24), on the worktree `/Users/kevinliu/repos/Turboslide-vector`, branch `vector/round`, port 4428 (4429 for the by hand reproductions). A first attempt of this phase ended without a report and without a commit, a note or a change on the tree (the tree at the start carried the five lanes' files uncommitted at `2be923e`, nothing else). Times are UTC (the lanes' notes are in PDT; 2026-09-24 20:00 PDT is 2026-09-25 03:00Z). Sections: the commits (1), the requests answered (2), the gates on the tree (3), the previews and their runs (4), the pictures (5), the findings by owner (6), the deviations (7).

### 1. The commits

The lanes' files by the path lists of their notes, one commit per lane in the order the prompt names (B5, B2, B3, B1, B4), after the specification, then the formatting and the seams; the branch rebased onto `origin/main` at `bb9c28a` (the chrome fix: a hairline between the menu bar and the toolbar, the rotation stem ending at the top centre square; four files, two of them B5's `core-matrix.json` and `chrome.mjs`, merged without a conflict and the fix's hunks read back on `HEAD`). Neither the ship one hotfix nor ship two had reached `origin/main` at the fetches of 03:22Z and 03:30Z, so the previews carry the chrome fix and not the hotfix; the hotfix lane's production gate was running from its own worktree during this phase. At 07:22Z, after the note's first commit, `origin/main` carried the hotfix (`f89b376` to `3c1f18a`: the logo routes tolerating the store's busy window, the logo index snapshot bundled with the deployment, the pulse across instances) and the branch was rebased onto it a second time: no conflict (`apps/studio/src/server/logos.ts` and `logos.test.ts` hold both B3's hunks and the hotfix's, `.prettierignore` both rounds' blocks), `tsc -b` exit 0, apps/studio 550 of 550, packages/chrome 737 of 737, packages/realtime 141 of 141, packages/store 256 of 257 with one skipped. The hashes below are the rebased ones; the runs of sections 3 and 4 read the tree before this rebase, and the ship step's production gate reads it with the hotfix.

| Commit              | What                                                                                                                                                                                         |
| ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `b6156c6`           | docs/VECTOR.md and the spec phase's note                                                                                                                                                     |
| `d38ca69`           | B5: the 43 matrix rows, the module, the walk's drivers, the specs, the fixtures, the README and FOCUS.md re-rendered                                                                         |
| `3d625d5`           | B2: the geometry interpreter, shapes.ts, primitives.ts, the resize test                                                                                                                      |
| `6c8ca95`           | B3: the asset, the intake and the sanitizer, the ways in, the sheet, copy                                                                                                                    |
| `b2169ec`           | B1: the icon table, the pickers, the toolbar tail, the strings, the Download dialog, the evidence under `build/b1/` and the two request patches                                              |
| `9d74764`           | B4: the svgBlip writer, the builder, the extractor, the label insets, the connector bound, the export check                                                                                  |
| `840160d`           | prettier over the ten lane files `prettier --check` listed                                                                                                                                   |
| `614bcf7`           | the integrator's day 0 requests and the seams (section 2), `pnpm generate:contracts` once                                                                                                    |
| `747f117`           | the walk's shape insert routes after the Shapes container left (section 3, the first gates' finding)                                                                                         |
| `7f8a2af`           | the diagram label row reaches a wrapped label's end (section 3)                                                                                                                              |
| `489d3df`           | the render worker's export job takes `svgVector` (section 3; preview 2 built from it)                                                                                                        |
| `101e971`           | the gestures row's driver reads the toolbar Crop with the picture selected (section 3)                                                                                                       |
| `4611b88`           | this note, the pictures and the ledgers under `build/integrator/`, b2.md's raw `<style>` in a code span (prettier was not idempotent on it: check step 19), the parity audit's JSON restored |
| the commit after it | the hashes above after the second rebase, and this paragraph                                                                                                                                 |

Every commit is authored as the repository's identity; no push was made (the ship step's); round five's branch was never touched.

### 2. The requests answered

Every request the five notes address to the integrator, with what was done. The chrome requests were landed by applying B1's two proven patches whole (`build/b1/model.ts.patch`, 50 hunks; `build/b1/r3.patch`, 4 hunks), then the hand edits below; every other request is one anchored edit of the seam script (`scratchpad/vector/integrator/seams.mjs`, 21 files, each anchor required once).

| Request                                           | Answer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| b1 R1 (`menus/model.ts`), b3 R2, b5 R7            | The patch applied: the seven Insert > Shape rows (the three named rows hoisted, Shapes, Arrows, Callouts and Equation as glyph grids in the default view), every icon of 3.2 and 3.3, Mask image and Change shape unflagged, `MenuContext.selection.vector`, `rasterPictureSelected`, `disabledReason` as a string or a function of the context, Crop image on the predicate with the svg sentence. `EditorShell.tsx` `runItem` reads the function form; `default-view-words.test.ts` skips the function form (its one sentence is `FORMAT.picture.svgCrop`, pinned by menu-model). |
| b1 R2 (`editor-shell.ts`)                         | `vectorPictureOf(deck, block)` through B3's `vectorOf`; the `vector` fact in `buildMenuContext`; `shapePickPlan` writes `adjust: null` so the three Change shape plates write one input.                                                                                                                                                                                                                                                                                                                                                                                            |
| b1 R3                                             | The patch applied: the dead `insert.shape.arrows.arrow` entries and the container walk of the render test leave.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| b1 R4, b5 R2 (the parity walk, `objects.spec.ts`) | `insertByTool` reaches the rectangle through `['insert.shape', 'insert.shape.shapes.rectangle']` at both sites; the objects spec no longer clicks the container. The core walk's own `insertShape` (B5's `areas/shapes.mjs`) still took both routes through the container and was fixed at `747f117` after the first gates (section 3).                                                                                                                                                                                                                                             |
| b1 R6, b4 R1, b5 R8 (`actions.ts` header)         | `export.run` takes `svgVector` (boolean, optional, default true; false writes the PNG blip alone, the parked control `export.svg.vector`); the CLI usage names `--no-svg-vector`; `pnpm generate:contracts` run once (6 of 14 files rewritten). The render worker's own job input refused the key until `489d3df` (section 3).                                                                                                                                                                                                                                                      |
| b4 R2, R3, R4                                     | `BatchExportInput.svgVector`; the merge passes it to the build and relocates `raster.svg` under `vectors/<theme>/` and back (a merge on another instance finds the vector file); `ExportPptxOptions.svgVector`; the CLI's `--no-svg-vector` switch, in `cli.ts`'s usage and in the help test's spelled map beside `--no-quick-look`; the worker's `exportJobInput` and its argv (`489d3df`).                                                                                                                                                                                        |
| b4 R6 (`zip.ts`)                                  | An svg media part deflates (the one media part that is text).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| b4 R7, b5 R6 (`stage.ts`)                         | `bandAssetResolver` answers `assetVector(asset, theme) ?? assetTwin(asset, theme)`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| b4 R8, b5 R5 (`apps/cli/src/assets.ts`)           | `inlineAssets` walks `vectorOf(asset)`'s files beside the twins.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| b3 R1, R4 (`EditorRoot.tsx`)                      | `pickPicture(svg, onFile)` adds `image/svg+xml,.svg` unless `intake.svg.upload` is parked; `<StageEditor parked={(id) => isParked(id, editorShell.settings)}>`; `packages/chrome` exports `./parked-controls`.                                                                                                                                                                                                                                                                                                                                                                      |
| b3 R3 (`figures.ts`)                              | `renderShot` writes `object-fit:contain` on a vector asset, as `picture.ts` does.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| b3 R7 (`headers.ts`)                              | The docblock says an `<img>` now loads an svg picture's file from the route and the headers stand.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| b2 R1 (`diagrams.ts`, its test)                   | `nearestSiteIndex`, `sitePoint` and `link` read the step's own sites; the process and cycle templates pass `roundRect`; the test bounds the index by `siteCount(target)`. The schema suite reads 609 of 609.                                                                                                                                                                                                                                                                                                                                                                        |
| b2 R2 (`validate.ts`)                             | A stored connector index at or past the count is severity 2 with "reads as an unattached end"; `attachConnector` keeps its refusal.                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| b2 R4 (`scene/measure.ts`)                        | The fill from the first geometry path that has one, the stroke from the first that has one, the shade overlay skipped.                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| b5 R4 (docs/VECTOR.md)                            | Six row texts of 6.1 and two bullets of 6.3 follow the tree; 6.4 records the twelve readings that differ from the specification as written.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| b2 R5 (B4's `text.test.ts`)                       | Not needed: the export suite reads 174 of 174 with the test as B4 left it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| b1 R1.2's last row (`insert.shader`)              | Ship two is not on `origin/main`; nothing to resolve yet (the rebase at the ship step names the icon).                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

The `.prettierignore` gains the round's run JSON, the icon contact sheets and the gate ledgers under `docs/gslides-parity/vector/build/*/`.

### 3. The gates on the tree

All from the worktree root, every command as written:

- `node_modules/.bin/tsc -b`: exit 0 after the seams, after the rebase and after each later commit. Two errors on the first pass, both the integrator's and fixed before the commit: an unused `c` in `measure.ts`, and `default-view-words.test.ts` passing a function valued `disabledReason` to a string parameter.
- vitest per changed package (`cd <package> && ../../node_modules/.bin/vitest run`, on `614bcf7`): schema 609 of 609 (27 files), headless 49 of 49, render 389 of 389, viewer 432 of 432, chrome 735 of 735 with 2 todo (77 files), export 174 of 174 with 1 skipped (the browser tests included, 80 s), apps/studio 536 of 536 (71 files), apps/cli 143 of 143 after the spelled map (one red before it: the help test wanted `--svg-vector` named, the CLI spells it `--no-svg-vector`), packages/agent 260 of 260, `vitest run --project scripts` 113 of 113; apps/render-worker 10 of 10 on `489d3df`. Every named unit test of VECTOR.md 6.3 is among them.
- `node scripts/check.mjs --greps`: exit 0 (innerHTML 27 call sites in the allowlist, overwrite: true 25).
- `node docs/readme/what-works.mjs --check`: README.md is current.
- `node_modules/.bin/prettier --check` over the 149 files of the round: 15 listed, formatted (ten lane files in `840160d`, five of the integrator's in `614bcf7`), then clean; every later commit's files checked before it.
- `node scripts/check.mjs` in full, on `614bcf7` and the rebased tree, in segments because the runner stops at the first failing step (`scratchpad/vector/integrator/check-*.log`): steps 1 to 4 ok (the install 5.4 s, the routes 8.2 s, the contracts 12.3 s, tsc 19.4 s); step 5 (`pnpm test`, 394 files in parallel) FAIL on three chrome tests at "Test timed out in 5000ms" while the 4428 gate and the deploy's archive ran on the same machine (presence-slot, inspector-sections, editor-shell-render; the three files alone 44 of 44, the chrome suite 735 of 735: the load's, not the tree's); steps 6 to 18 ok (the build and the client bundle check, the import at 85 slides, the validate, the render at 170 pages with no page error, the sheet, the lint, the brand deck build, the viewer spec 33.9 s, the chrome lint 183.5 s); step 19 FAIL on two files of the round, b2.md (prettier re-emitted a blank line under a raw `<style>` on every pass; the tag is in a code span now) and this note (written after the formatting pass; formatted at the commit); step 20, the parity audit, FAIL as it stands (standing; the four grid rows it now reaches read "no write; the stage armed the shape tool", section 6); step 21, the older studio suites, FAIL as it stands with ship one's exact count, 17 failed and 41 passed (the objects spec's failure is at line 306, the Shift click chip, past the rectangle insert re-routed at 283); step 22 ok (49.1 s), 23 skip (no fonts venv), 24 ok (75.4 s), 25 skip (no Docker daemon); step 26 FAIL as it stands (429.7 s, standing); step 27 skip (no preview on 4344); steps 28 to 30 ok (0.4 s, 2.5 s, 10.0 s); step 31 FAIL as it stands, 98 of 151 budgets met on 4321 (ship one read 113 on a quiet machine; the first attempt of the step met the hotfix lane's check on 4321 and was run again once the port was free); step 32, the runner's own gate on 4321: FAIL as it read (05:09Z to 07:18Z, 7733 s; `--only 32` after the chain, so it never overlapped the 4428 gate's walk): 827 rows, 768 passed, 36 failed, 23 not driven (1 manual), 0 no step; every cost row held (over their ceiling 0). The 36 reds and the not driven rows outside the parked controls, by mechanism: the lines area's seven rows (`lines.tail.colour-weight-dash-ends` met "Execution context was destroyed" at a navigation while the machine ran the preview's and 4428's specs, and the area's rows after it lost their page); ship one's parked list (`view.live-pointers.second-browser`, `versions.show-changes-marks`, `inbox.bell-panel-toggle`, `templates.gallery.page`, `templates.deck.read-only`); the runner's own environment, which is a checkout server on the file store without the assist and logo fixtures: the five `assist.*` rows (no cards), the eleven `logos.*` reds and five not driven ("the index holds no figma on this base", "the index reads network, not the ten mark fixture": the live thesvg.org index from the runner, not the fixture), `svg.import.url` (the mark route 404 there), and the eight `svg.*` reds (`svg.import.upload` "the asset reads kind 'svg'", the three sanitizer rows, the two export rows, `svg.copy.markup`, `svg.render.picture-gestures`), which read the checkout tier's intake: a checkout keeps an svg file as it came, with no sanitizer and no `kind: 'svg'` (docs/VECTOR.md 4.2, b3.md section 5), so the hosted rows of the round do not describe that server (section 6); `assist.quota.429` and `cost.editor-hidden.calls` as in every run; `text.clipboard.paste-without-formatting` (manual). Every shapes, diagrams and menus row of the round read green there with the fixed drivers. The ledgers: `build/integrator/check-step32-core-gate.json` and `check-step32-core-matrix.md`; the standing reds of the chain (19 on the round's two files, since fixed; 20, 21, 26, 31) and the skips (23, 25, 27) are as the earlier rounds recorded them.
- The core gate on 4428 (the memory tier; `node scripts/probes/core-gate.mjs --base http://localhost:4428 --out <scratch>/gate-4428 --lock <scratch>/gate-4428.lock --parked docs/gslides-parity/focus/ship-f1afe1e.json --matrix <scratch>/gate-4428/core-matrix-run.json --allow-scratch`, the server `TURBOSLIDE_STORE=tmp` under a private `TMPDIR`, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, `TURBOSLIDE_ASSIST=fixture`, `TURBOSLIDE_LOGO_UPSTREAM=fixture`, two 48 character secrets minted in the command, `vite.no-watch.config.ts`; `--allow-scratch` because the check chain's parity audit had left its "Audit scratch" deck under the checkout's `decks/`, removed before step 32 as the gate's message says). The run of record started 04:10Z on `614bcf7`: 827 rows, 757 passed, 17 failed, 53 not driven (1 manual), 0 no step; verdict failed; one cost row over its ceiling; 6993 s. The 17 red and the not driven rows outside the parked controls, each with its mechanism: `shapes.insert.rectangle-click`, `.rounded-click`, `.ellipse-click`, `.rectangle-drag`, `.ellipse-drag` and the 36 rows of the area not driven behind "setup failed: a rectangle to drive" (the walk's routes through the removed container; fixed at `747f117` and re-driven green on 4429, below); `diagrams.label.double-click-opens` (the driver's End on a wrapped label; fixed at `7f8a2af`, green on 4429); `svg.render.picture-gestures` (the driver's unbounded read of the toolbar Crop after the selection had left; fixed at `101e971`, green on 4429); `svg.export.pptx-fallback` (the render worker's refusal of `svgVector`; fixed at `489d3df`, green on 4429); `slides.import.none-preselected` and `decks.save.acknowledged` (one event: the store held the three imported slides at revision 67 while the tab read "landed 0" within 30 s and its next `slide.remove` never answered, the memory tier's follower under this machine's load; by hand on a quiet server the same import lands in 1.5 s, section 6); `sync.reload.same-document` (three reloads at 15 s against 5 s, the load); `cost.editor-editing.calls` at 84.98 function requests a minute against the ceiling of 75 on the memory tier under the load (the blob tier's reading, the one docs/SYNC.md 6.1 governs, holds on preview 1; the other four cost rows passed here too, `cost.editor-hidden.calls` not driven as always under headless Chromium); `sync.title.concurrent-both-kept` (Kevin's standing row); `inbox.bell-panel-toggle`, `templates.gallery.page`, `versions.show-changes-marks`, `view.live-pointers.second-browser` (ship one's parked list); the "not on this build" rows of ship one's eight P1 controls; `logos.index.cached-offline` (the fixture, exempt by ship one's rule); `assist.quota.429` (no limiter fact on a dev server); `text.clipboard.paste-without-formatting` (manual). The ledgers: `build/integrator/gate-4428-core-gate.json` and `gate-4428-core-matrix.md`.
- The rows the first gates read red for the drivers' reasons, re-driven on 4429 (a spare port, its own tmp store) with the fixed drivers: the shapes area (`editor-walk-probe.mjs --core --only shapes`, 04:50Z, 59 passed, 0 failed, 0 console errors; every one of the 42 shapes rows green, `shapes.insert.rectangle-click`, `.rounded-click`, `.ellipse-click`, `.rectangle-drag` and `.ellipse-drag` included, and the 36 rows that had read "setup failed: a rectangle to drive"), the diagrams area (04:58Z, 29 passed, 0 failed; the ten diagrams rows green, `diagrams.label.double-click-opens` included), and the export spec after the worker fix (`core-gate.mjs --only specs --spec export --lock <scratch> --allow-scratch`, on `489d3df`): 24 of 24 export rows green, `svg.export.pptx-fallback`'s second half included (the ledger `gate-4429-export-core-matrix.md`); and the svg spec after the gestures driver fix (`--spec svg`, on `101e971`): 12 of 12 svg rows green (`gate-4429-svg-core-matrix.md`). So on the memory tier every row of the round reads green once the four seams above are in, and the rows that stayed red on 4428 are the load class, the standing row and the parked list.

### 4. The previews and their runs

- Preview 1, the deployment of record for the whole matrix: `https://turboslide-rfp6bmnxb-kl01s-projects.vercel.app`, `vercel deploy --yes --archive=tgz -e TURBOSLIDE_AUTHORIZE=enforce -e TURBOSLIDE_ASSIST=fixture -e TURBOSLIDE_LOGO_UPSTREAM=fixture -e TURBOSLIDE_MAIL=off -e TURBOSLIDE_REALTIME=blob -e TURBOSLIDE_PUBLIC_STORE_HOST=ggmycvj7j6224ay5.public.blob.vercel-storage.com -e TURBOSLIDE_SESSION_SECRET=<hex> -e TURBOSLIDE_DOWNLOAD_SECRET=<hex>` from the worktree at `614bcf7`, 03:31:23Z to 03:34:30Z, the upload 351.4 MB. Preview 2, from `489d3df` (the worker fix), the same command, 05:40:27Z to 05:42:45Z: `https://turboslide-1ncbsb9tb-kl01s-projects.vercel.app`. Never `--prod`, no project setting, no env command. Every probe sent `VERCEL_OIDC_TOKEN` from `.turboslide/vercel-dev.env` and production's agent bearer as `TURBOSLIDE_TOKEN` (the bearer is validated on the shared store, so production's row of `~/.config/turboslide/hosts.json` holds on a preview; the smoke's `deck.info` row proved it) through `scratchpad/vector/integrator/with-tokens.mjs`, which reads both into the child's environment and prints nothing; the token expires 2026-09-25T07:24Z and was not refreshed.
- `node scripts/hosted-smoke.mjs <preview> --token-env TURBOSLIDE_TOKEN`: preview 1 (03:34:54Z to 03:35:16Z) 40 of 40 passed (the pages, the headers, the enforce rows: unsigned thumbnail 403, cross site POST 415; the CDN rows HIT on the second read; the effects backend typescript on node v24.20.0; `deck.info` with 1 snapshot at revision 26); preview 2 (05:43Z) 40 of 40.
- `node scripts/probes/new-write-probe.mjs --base <preview 1>` (03:35:16Z to 03:36:00Z): every step ok; the draft's first write moved the address to `/edit/untitled-20260925-i634`, twelve edits each acknowledged at the next revision, the reload at 13, no self chip, the second tab, File > Move to trash then Delete forever removed the deck.
- The gate on preview 1 (`node scripts/probes/core-gate.mjs --base <preview 1> --out <scratch>/gate-preview --parked docs/gslides-parity/focus/ship-f1afe1e.json --matrix docs/gslides-parity/focus/verification/vector-preview-run1.json`, detached, polled at most every ten minutes). A first start at 03:36Z ran without the bearer (the wrapper had looked the preview's own origin up in `hosts.json`, which has no row for it) and was stopped at 04:06Z, 330 rows in, once row 225 read "no bearer for this origin" (the rows reading a PowerPoint through the sync route, the cost probe's store half and the logos rows would have read not driven for the harness's reason); its two decks answered 404 before the restart. The run of record started 04:07:59Z with the bearer: 827 rows, 720 passed, 25 failed, 82 not driven (1 manual), 0 no step; verdict failed; every cost row of docs/SYNC.md 6.1 holds (`cost.editor-idle.calls`, `cost.editor-editing.calls`, `cost.two-tabs-idle.calls` and `cost.show.calls` passed, `cost.editor-hidden.calls` not driven under headless Chromium as in every run; over their ceiling 0); 9038 s, 04:08Z to 06:38Z. The red and not driven rows outside ship one's parked list, by mechanism: the drivers' (`shapes.insert.rectangle-click`, `.rounded-click`, `.ellipse-click`, `.rectangle-drag`, `.ellipse-drag` and the 36 rows not driven behind them; `diagrams.label.double-click-opens`; `svg.render.picture-gestures`), each re-driven green on the same preview or on preview 2 below; the render worker's `svgVector` refusal (`svg.export.pptx-fallback`), green on preview 2; the rows my own concurrent runs on this deployment cost between 04:51Z and 05:12Z (the shapes, diagrams and logos walks and the pictures): `share.dialog.open`'s 30 s navigation timeout and the 24 share, collab, comments and versions rows the file skipped behind it, `chrome.toolbar.fold-any-width`'s 90 s readiness wait (green on preview 2), `logos.cache.open-licence-only` (the mark route 500 in preview 2's 403 window), `brand.colors.collab-rerender`, `brand.surfaces.viewer-and-show` and `assist.viewer.disabled` (skipped behind the templates rows); the blob tier's timing class (`decks.list.search`, `decks.list.open-title`: the listing; `sync.block.concurrent-same-offset-order`, `sync.structural.concurrent`: two browsers); the fixture upstream (`logos.picker.search` at 691 ms against 300, `logos.insert.mono-tint` with no AWS tile behind the cloud switch, `logos.index.refresh-fixture` reading `undefined.slice`); Kevin's standing rows (`sync.title.concurrent-both-kept`, `decks.file.open-list-search`); ship one's parked list (`templates.gallery.page`, `templates.card.rename-and-delete`, `templates.default.use-for-new`, `templates.deck.read-only`, `inbox.bell-panel-toggle`, `inbox.settings-persist`, `inbox.notification-arrives`, `versions.show-changes-marks`, `view.live-pointers.second-browser`, the "not on this build" rows of the eight P1 controls); `logos.index.cached-offline` (the fixture, exempt); `assist.quota.429` (no limiter fact); `text.clipboard.paste-without-formatting` (manual). The rows of the round: 33 of the 43 new rows and the extended one green in the run of record; `svg.render.picture-gestures` and `svg.export.pptx-fallback` green on preview 2 after their fixes; the seven `shapes.*` new rows the driver cascade had not driven green in the shapes re-run. The ledger of record: `docs/gslides-parity/focus/verification/vector-preview-run1.json` (the gate's merged summary) with `build/integrator/preview-run1-core-gate.json`, `preview-run1-core-matrix.md`, `preview-run1-core-walk.json` and `.md`.
- The rows the run of record read red for the drivers' reasons, re-driven on preview 1 with the fixed drivers: the shapes area (`editor-walk-probe.mjs --core --only shapes`, 04:51Z to 04:58Z, 418 s: 58 passed, 1 failed, the one red `decks.file.open-list-search`, Kevin's standing row; every one of the 42 shapes rows green) and the diagrams area (04:58Z to 05:01Z: 26 passed, 1 failed, the same standing row; the ten diagrams rows green). A second pass of the logos area at 05:09Z is not a reading: its `/decks` page did not settle within 90 s while the preview served the specs phase and every later row failed on the lost page (its deck `untitled-20260925-k7bh` answers 404).
- On preview 2, after the worker fix, `core-gate.mjs --only specs --spec export,chrome` then `--spec svg` (05:43Z on): the export and chrome specs (05:43Z to 05:5xZ; `preview2-export-chrome-core-gate.json`, `-core-matrix.md`): 45 rows judged, 10 passed, 2 failed, 33 not driven (the other files' rows, not run); `chrome.toolbar.fold-any-width` green; the two reds `images.export.pdf-with-picture` and `shapes.export.pdf` on "Vercel Blob: Failed to fetch blob: 403 Forbidden", a six minute window of the shared store on this deployment alone (05:50Z to 05:56Z; preview 1's run and the hotfix lane's production gate on the same store met none; the smoke on preview 1 at 05:57Z read the store 200), and the export spec again at 06:03Z: 24 of 24 green (`preview2-export-core-gate.json`, `-core-matrix.md`), `svg.export.pptx-fallback`'s both halves included. The svg spec four times: the first (05:5xZ) in the 403 window (the pastes landed nothing); the second (06:00Z) and third (06:32Z, stopped for the driver fix) with the gestures row at its 300 s wall; the fourth (06:35Z, on `101e971`; `preview2-svg-core-gate.json`, `-core-matrix.md`): 12 rows, 10 passed, 2 failed: `svg.render.vector-at-zoom` (the viewer read right after the write answered an img without the svg: red three of three on this deployment, green on preview 1 and, by hand on both previews at 06:04Z, the viewer draws `/decks/<id>/assets/pasted.svg` at natural 96 and the file answers 200 image/svg+xml, so the spec's read races the store's read on another instance) and `svg.copy.markup` (the copy fired before the vector file's read ahead landed and wrote the envelope alone, B3's documented fallback; green on preview 1); `svg.render.picture-gestures` green. The share, logos, assist and brand specs together (06:39Z to 06:51Z, 691 s; `preview2-share-logos-assist-brand-core-gate.json`, `-core-matrix.md`): 54 rows, 39 passed, 3 failed, 12 not driven, over their ceiling 0; the three reds are ship one's parked list (`templates.save.as-template`, `view.live-pointers.second-browser`, `inbox.notification-arrives`) and the twelve not driven are the rows behind them and the fixture only rows; every share, collab, comments and versions row, `logos.picker.search`, `logos.insert.mono-tint`, `logos.index.refresh-fixture`, `logos.cache.open-licence-only`, `assist.viewer.disabled`, `brand.colors.collab-rerender` and `brand.surfaces.viewer-and-show` green, so preview 1's reds in those files were the load of my concurrent runs on it and the fixture's timing, not the tree.

### 5. The pictures

`scratchpad/vector/integrator/pictures.mjs` on preview 1 (03:42Z to 03:44Z, playwright-core over Chrome for Testing, 1440 by 900 at 2x, the default view, one scratch deck per appearance made from `/new` by typing the title, trashed and deleted forever through the product at the end, `/edit/<id>` 404 for both: `untitled-20260925-z5t2`, `untitled-20260925-p3co`; a first run at 03:38Z pasted the fixture under a second prolog and landed a text box, its two decks likewise removed: `untitled-20260925-a01e`, `untitled-20260925-mdo0`). Under `docs/gslides-parity/vector/build/integrator/`, each in `-light` and `-dark`:

- `insert-menu`: the Insert menu, every row with its glyph (Image, Logo, Text box, Shape, Table, Chart, Diagram, Word art, Line, Link, Comment, New slide, Slide numbers).
- `insert-shape`: Insert > Shape with the seven rows in order (Rectangle, Rounded rectangle, Ellipse, then Shapes, Arrows, Callouts, Equation after the divider), each with its glyph.
- `insert-shape-gallery`, `-arrows`, `-callouts`, `-equation`: the four plates from the menu bar in the default view; 99 tiles with 94 distinct glyphs, 26 of 26, 4 of 4, 6 of 6.
- `insert-line`, `insert-image`: the Line and Image submenus with their glyphs (the eight line kinds; Upload, Logo, By URL, From this presentation).
- `toolbar-shape`: the toolbar Shape dropdown with the same seven rows and glyphs.
- `svg-pasted-selected`: the fixture svg pasted as `text/plain` markup (Figma's form, with its prolog and comment), landed in 1469 ms (light) and 1315 ms (dark) as `assets/pasted.svg` at `object-fit: contain`, selected: the ring, the handles, the stem, the Picture chip; the toolbar Crop `aria-disabled`.
- `format-image-svg`: Format > Image with the picture selected, the rows with their glyphs, Crop image disabled and its tooltip reading "An SVG picture cannot be cropped. Resize it instead".
- `svg-pasted-zoomed` (and `-detail`, the same frame at 200 percent): the picture still draws `pasted.svg` (natural 96 px, drawn at 1642 css px), no PNG. In the dark picture a toast reads "Revision r3 arrived from outside this editor and is shown" after the tab's own paste on the blob tier (section 6).

`pictures-readings.txt` beside them carries every reading. The ledgers beside them: `preview-run1-core-gate.json` and `preview-run1-core-matrix.md` (the run of record), `preview-run1-core-walk.json` and `.md`, `preview-shapes-core-walk.json` and `.md`, `preview-diagrams-core-walk.json` and `.md`, `preview2-export-chrome-core-gate.json` and `-core-matrix.md`, `preview2-svg-core-gate.json` and `-core-matrix.md`, `gate-4428-core-gate.json` and `-core-matrix.md`, `gate-4429-shapes-core-walk.md`, `gate-4429-diagrams-core-walk.md`, `gate-4429-export-core-matrix.md`.

### 6. The findings by owner

Each names the owner and what was measured; none was chased beyond what the round needed.

1. The sync owner (the memory tier's follower): under this machine's load a committed write did not reach the tab. On 4428 the import of three brand slides committed at revision 67 (the slides and their four jpg assets on the tmp store) while the tab read "landed 0" for 30 s and its next `slide.remove` never answered (row 105 twice, `decks.save.acknowledged`; the first run's tab kept naming a slide the trim had removed and every later write of the deck was refused with "No slide file for split-3"); `sync.reload.same-document`'s three reloads took 15 s; the runner's gate on 4321 lost the lines area to "Execution context was destroyed". By hand on a quiet server the same import lands in 1.5 s at revision 2 on disk and in the tab. Ship one's memory tier gate read these rows green on a quiet machine; the blob tier read them green here.
2. B3 or the store's owner (`apps/cli/src/store-actions.ts` 1402): `assetFiles(asset)` names the twins and `sourceFile`, so an svg picture's `vector` file (`assets/<id>.<sha8>.svg`) does not travel with `slide.import` (and any other copy through it); the imported picture would draw its PNG twin on the sheet. Not a row of the round; P1.
3. Kevin's list, ship one: after a paste on the blob tier the tab shows "Revision r3 arrived from outside this editor and is shown" (the dark zoomed picture): `asset.add`'s answer carries no revision, so its reload lands one above what the tab acknowledged (the controller's own comment).
4. The diagram template's owner (`packages/schema/src/diagrams.ts`): a rounded rectangle's label now sits in its ECMA text rectangle (VECTOR.md 2.3), 7.12 px in on the five step process' 146 px steps at 22 px, and "Step 1 plus" wraps onto two lines ("Step 1" alone is one; measured on 4429). A smaller label size or wider steps in the template; the row's driver reaches the label's end by a select all now.
5. The parity audit's owner (check step 20, `scripts/gslides-parity-audit.mjs`): its pickers:objects rows for `insert.shape.gallery.pick`, `.arrows.pick`, `.callouts.pick` and `.equation.pick` read "no write; the stage armed the shape tool" because the audit expects a tile pick to write a block at once, while the product arms the draw tool and one click places (the core matrix's `shapes.insert.grid-shapes`); the rows were unreachable before the round (the grids sat behind the switch) and the expectation is stale.
6. The chrome tests' owner: check step 5 (`pnpm test`, 394 files in parallel) read presence-slot, inspector-sections and one editor-shell-render case at "Test timed out in 5000ms" under the load of the parallel runs; alone they pass in under a second each. The 5 s budget is tight for the whole workspace run on a busy machine.
7. The hosting owner: preview 2 met "Vercel Blob: Failed to fetch blob: 403 Forbidden" on downloads and asset writes for six minutes (05:50Z to 05:56Z) while preview 1 and the hotfix lane's production gate on the same store did not; the mark route answered 500 in the same window (`logos.cache.open-licence-only`). Transient; the same rows read green on the next pass.
8. The viewer and the read ahead on the blob tier: `svg.render.vector-at-zoom`'s viewer read right after the upload answered no svg img three of three times on preview 2 and once green on preview 1 (by hand the viewer draws the svg on both), and `svg.copy.markup`'s copy fired before the vector file's read ahead landed on preview 2 (the envelope alone, B3's fallback). Both are a read on another instance right after the write; the rows' drivers could wait for the img and for the read ahead.
9. B5's drivers, fixed by the integrator: the core walk's `insertShape` routes (`747f117`), the diagram label row's End (`7f8a2af`), the gestures row's toolbar read (`101e971`); and the parity walk's and the objects spec's container routes (`614bcf7`). `logos.index.refresh-fixture` read "Cannot read properties of undefined (reading 'slice')" on preview 1 (a driver reading a field the fixture's answer lacked, or the 403 window); the pass on preview 2 below re-reads it.
10. The rows' owner (B5) with B3: the svg rows of the matrix describe the hosted intake (a tmp or blob store), and on a checkout server on the file store (the check chain's step 32 on 4321) they read red by design, since a checkout keeps an svg file as it came, unsanitized and without `kind: 'svg'` (b3.md section 5). Either the rows skip on a checkout base with the reason, or the checkout's intake takes the sanitizer too.
11. The cost rows: `cost.editor-editing.calls` read 84.98 function requests a minute against 75 on the memory tier under the load; the blob tier (preview 1) held every ceiling. This round adds no store call and no poll (VECTOR.md 4.5), and the memory tier's reading is the machine's.

### 7. Deviations

- Two previews, not one (6.2 says one gate on the preview): the whole matrix ran once on preview 1, and the render worker's refusal of `svgVector` (found by that run and the memory tier alike) was fixed at `489d3df` and read on preview 2 through the export, chrome and svg specs alone. The ship step's parked list should be rendered from preview 1's run with preview 2's rows for `svg.export.pptx-fallback` and the svg and chrome rows it re-read; the ship's own production gate reads every row again.
- The rows the first gates read red for the drivers' reasons (the shapes area behind the removed container, the diagram label row) were re-driven on the same preview with the fixed drivers rather than on a fresh one: the product had not changed, only the walk.
- The check chain's step 5 read three chrome tests red at "Test timed out in 5000ms" under the load of the parallel runs; the three files alone read 44 of 44. Step 31 read 98 of 151 budgets for the same reason (ship one 113). Step 32 is recorded as it read (section 3).
- The first core gate on 4428 (03:26Z) was stopped at row 260 and is not a reading past row 104: beside the deploy's 351 MB archive, `turboslide render all` of the check chain and the preview's browser, the walk's tab lost its document's sync with the tmp store after the trim and import rows (the store held the imported slides, the tab read "landed 0" and kept naming `split-3`, which the trim had removed; every later write of that deck was refused with "No slide file for split-3"). The run of record (04:10Z) read the same row red once and recovered through B5's stall retirement; the mechanism is in section 6. Its log is kept in the scratchpad as `gate-4428-run1-poisoned.log`.
- `docs/VECTOR.md` 6.1 and 6.3 were amended at the merge (b5 R4) and 6.4 added, rather than leaving the specification and the matrix in disagreement.
- The picture script's first run pasted the fixture under a second XML prolog (the fixture carries its own), which the markup sniff rightly read as text; the second run pastes the file as it is.
- The parity audit's JSON (`docs/gslides-parity/verification-3/parity-audit.json`, tracked) was rewritten by check step 20 and restored before the note's commit, as ship one did.
- The shared store at the return (a listing with the trashed included at 07:19Z): every deck my runs made and could reach was trashed and deleted forever, the specs' own teardowns aside, eight through the actions route with the agent bearer at 07:20Z (`untitled-20260925-4gvu`, `-h80r`, `-rcit`, `-rmv0`, `import-bundle-source-mugihydy`, `northwind-renewal-v4-without-notes`, `core-spec-template-mughzrfo-mugi0bn11hfn`, `untitled-20260925-i54h`: the decks spec's fixtures and the brand spec's of preview 1's specs phase, and the svg spec's deck and an empty draft of preview 2's 403 window, each `/edit` 404 after). Three decks made between 05:46Z and 05:48Z (`untitled-20260925-nmeg` "Acme pricing review, Q3 2026", `-0l80` "A", `-g1wb` "Brand spec deck") fall in the hotfix lane's production gate's decks spec window on the same store and were left for the ship step to attribute once that run has ended; `untitled-20260925-rav6` is that gate's live cost probe deck. The check chain's own leftovers under the checkout's `decks/` (the parity audit's "Audit scratch", step 32's "Thumbnail deck") were removed after their runs ended, as the gate's message says.
