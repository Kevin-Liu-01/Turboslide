# The return round, b5 Documents

Notes of builder B5, the documents lane (tables, charts, diagrams, word art, the Slide numbers dialog), in the shape of the focus round's build notes. The binding specification is `docs/RETURN.md` (sections 2.4 to 2.8, 5 and 6); `docs/FOCUS.md` sections 3 and 6 and `AMENDMENTS.md` A1 stand under it. Nothing here ran a git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build`. Day 1 of the round, 2026-09-18. Dev server: port 4405.

## Return round

### 0. Owns

RETURN.md section 6's list for B5: `packages/viewer/src/Editor.tsx` (`endEdit`'s `tab` and `shift-tab` branch, the `pendingEdit` layout effect, the column seam handle), `packages/schema/src/blocks/table.ts` (`insertColumnsAt`), `packages/render/src/blocks/table.ts`, `packages/export/src/pptx/table.ts`, `packages/chrome/src/inspector/chart.tsx`, `chart.css`, `seg.css`, `packages/chrome/src/DiagramPanel.tsx`, the word art bar (`packages/chrome/src/dialogs/WordArtBar.tsx`), the Slide numbers dialog (`packages/chrome/src/dialogs/SlideNumbers.tsx`), the `format.table.*` plans in `editor-shell.ts` (B2 owns the file's kept cell pointer), plus their tests. New files under that ownership: `packages/viewer/src/table-seam.ts` and `packages/viewer/src/__tests__/table-seam.test.ts` (the seam handle's pure part, kept out of `Editor.tsx` so it is pinned by a unit test), and `packages/viewer/src/table-session.ts` with `table-session.test.ts` (the cell a parked session's pointer names after the grid changed; fix 10). Fix 10 also touches the session reconcile effect of `Editor.tsx` (the `[doc]` effect), a table scoped guard of eight lines named for B2, whose text session it sits in.

### 1. What the checkout measured before the fixes (dev server 4405, Playwright, 2026-09-18)

Driven with the product's controls and the window API for the setup writes, on scratch decks trashed at the end (`.turboslide/b5-return/tab-repro.mjs`, `tab-timeline.mjs`, `overlap-repro.mjs`).

1. Tab from a written cell (audit-objects rows 51, 93, 94): reproduced. Double click cell 1,1, type "First", Tab: `editing false`, active run null, `blockId table` at +50, +200, +600 and +1500 ms; the next keys opened cell 1,1 under A1 rule 4 and replaced it ("Next" in cell 1,1, cell 1,2 empty). Shift+Tab from a written cell ended the session the same way.
2. The mechanism, read with a MutationObserver and two temporary `console.debug` lines in `Editor.tsx` (removed): `finish('tab')` in `InlineText` ends the session; `endEdit`'s final write was null (every burst had landed), so the `!written` branch ran `startEdit` at once on the cell element under the pointer. That element belonged to the markup frozen at the session's start (`shownHtml` is `frozenHtml` while a session is open), which the re-render then replaced (`childList on .pt-slide +1 -1`, 13 ms after the Tab); the layout effect saw the new session's element outside the body (`editing rows/0/cells/1, contains false`) and ended it. The `pendingEdit` branch was never the problem; the immediate branch was, and RETURN.md's line 1600 reading ("opens the next cell at once only when nothing was written") was the path that broke once a burst had landed. Row 92 (Tab from an empty cell) works because nothing changed the markup.
3. Format > Table with the table selected by one click: every row disabled, as row 98 said. With a cell session open: B2's transient menubar press keeps the session parked through the menu (`2 menu open: editing true`), and Format > Align & indent > Center wrote column 1 for a caret in column 1 (fix 3's kept pointer, B2's `withKeptCell`, landed on the tree while this note was written; `tableCell: block?.type === 'table'` too, so fix 2's predicate is B2's write and B5 changed nothing there).
4. Insert column right through the window API on a table with no widths: the four columns drew at an equal share, `--table-cols: minmax(0, 1fr) x4`, no overlap. The audit's own sequence (`overlap-repro.mjs`: a cell typed, a second cell's session parked through Format > Align & indent > Center, a third session, the cell right click menu's Insert column right) reproduced row 97 exactly: after the insert the block read `columns [{}, {align: "center"}, {}, {}]` with four cells per row and the right template (`minmax(0, 1fr)` x4, 240 px tracks), yet in every row the cell of the centre aligned column drew at `510+677`, the whole row, over the other three (`0@510+169 1@510+677 2@679+169 3@848+169`). The mechanism is not the widths RETURN.md 2.4 fix 4 names: the render marks an aligned cell `class="td center"` (`packages/render/src/blocks/table.ts`, `column?.align === 'center' && 'center'`), and the sheet's layout helper `.ts-sheet .center { position: absolute; inset: 0; display: grid; place-items: center }` (`packages/theme/src/gt-ink-paper/sheet.css` line 221, the statement layouts' centring class) matches it and positions the cell over its row; `.ts-sheet .table .td.center` only wins `text-align`. Every centre aligned column drew that way in the editor, the show and the exports; the insert only revealed it because it ended the session whose frozen markup still showed the cell before the alignment landed (row 95 centred column 0, row 97 then saw column 0 across the row; row 85's `gridCol` of 960 was that cell's measured box). The `right` class has no such twin (`grep '^\.ts-sheet \.right'` finds nothing).

### 2. The fixes made

1. `Editor.tsx` `endEdit`, `tab` and `shift-tab` (fix 1): the next cell always goes through `pendingEdit`; the immediate `startEdit` on the current element is gone. The layout effect that opens a queued edit gains `editing` in its dependency list, so it runs on the render where the session ends (whether or not the markup changed) and opens the queued cell on the markup that render committed; `runElement` reads the fresh body. Verified on 4405 after the change: Tab from "First" moved the session to cell 1,2 (`focusin tbl/rows/0/cells/1`, `contenteditable=true` on it, `data-editing` set) and the typed "N" landed there. Row 92's path (empty cell, Tab at once) is the same path now.
2. `packages/schema/src/blocks/table.ts` (fix 4): `insertColumnsAt` gives a new column a width and rescales the others so the declared total stays whenever the grid carries widths (an unsized grid stays unsized); `deleteColumnRange` gives the removed columns' room to the columns left. New exports: `columnShares(columns, total)` (the grid template's rule as numbers: equal shares, px where set with the rest shared, proportional when every column is sized), `columnsAfterSeamDrag(columns, total, index, dx)` (the seam handle's write: the left column widens by dx, its neighbour narrows, neither under `TABLE_MIN_COLUMN_PX` 40, every column sized) and `TABLE_MIN_COLUMN_PX`. Tests in `table.test.ts` (18 pass).
3. `packages/render/src/blocks/table.ts` (fix 4): `tableColumnsTemplate` draws a fully sized grid as `minmax(0, <w>fr)` shares, so the row fills the table whatever the block's width and a table resized by its handles scales its columns; the mixed and unsized forms render byte for byte as before (the existing tests pin them). Test added.
4. `packages/export/src/pptx/table.ts` (fix 4): `normalisedColumnWidths` scales the measured `colW` to the table's box when their sum drifted from it, so no `gridCol` is wider than the frame (row 85's 240, 240, 240 and 960 for a 960 px table). Tests added (5 pass).
5. The column seam handle (fix 5): `packages/viewer/src/table-seam.ts` builds one `v` handle per inner seam of the selected table from the header row's measured cells (`handle.<block>.column.<c>`, label "<block>: Column seam <c + 1>", the layout's `col-seam` kind carrying a `blockId` and an `index`); `Editor.tsx` appends them beside the eight handles of a single selected table (not while a cell is edited, not in crop mode), `gestureAt` turns the drag into one `block.set /columns` through `columnsAfterSeamDrag` with the readout set to the moved column's box (its new width by the table's height, the `size` readout: see request 3), and `onHandleNudge` steps the seam by the arrow keys (1 px, Shift 10, the overlay's delta). One commit per drag, so Cmd+Z restores. Tests: `table-seam.test.ts` (8 pass).
6. `packages/chrome/src/inspector/chart.css` (charts, the one fix): `.ts-chart-kind.pt-seg.is-small .pt-ib { width: auto; padding: 0 8px }`. The mechanism: `chart.tsx` draws the Chart type `Seg` with `className="is-small ts-chart-kind"` outside the inspector's `.ts-ctl-seg` wrapper, so `Seg.css`'s small variant (the sidebar's two 28 px icon squares) sized every segment at 28 px and "Column" (37 px) clipped; the inspector's enum Segs get their text sizing from `seg.css` through that wrapper, which this rule now gives the chart's.
7. `packages/render/src/blocks/table.ts` (the overlap of row 97, the mechanism of item 4 above): a cell's alignment travels as an inline `text-align:center` or `text-align:right`, never as a `center` or `right` class, so no layout helper can reach a cell; the edge classes `first` and `last` stay. The scene builder reads the computed `textAlign` (`packages/export/src/scene/measure.ts` 760), so the PDF and the PowerPoint keep their alignment. `table.test.ts` pins the inline form and the absence of the two classes; the render snapshots of `gslides.test.ts` (the parity table) and `canvas.test.ts` (the export fixture's table-merge) were updated for that one attribute (4 lines each, `vitest run -u` on the two files; nothing else in them changed). The sheet's `.ts-sheet .table .td.center` and `.td.right` rules (sheet.css 795 to 800) are now dead and can go: request 10.
8. `packages/chrome/src/dialogs/SlideNumbers.tsx` (`slides.numbers.apply`): Google's dialog shape, On or Off radios (`dialog.slideNumbers.on`, `.off`), a Skip title slides check (`dialog.slideNumbers.skip-title`, disabled under Off), Apply (`deck.set /defaults/counter`: on, off or skip-title) and Apply to selected (`dialog.slideNumbers.selected`: one `slide.set /counter` per selected slide, on or off). Apply to selected needs the per slide field of request 5; until it lands the slide schema refuses the write and the dialog shows the sentence.
9. `Editor.tsx`, the session reconcile effect (`[doc]`), with `packages/viewer/src/table-session.ts` (`tableShapeOf`, `movedCellPointer`) and its test: a row or column added or removed under a parked cell session ends the session with no re-send; its last write follows the cell that moved (the cell one step along the change whose text is the one the session last saw), or is dropped when the cell is gone; the table stays selected and the sheet redraws its new grid. The mechanism this closes is section 7 item 2 (Insert row above duplicated the cell's text into the new row and Cmd+Z undid that write, not the row). The shape is recorded in `startEdit` and cleared in `endEdit`. Tests: `table-session.test.ts` (7 pass); the viewer suite 36 files 359 tests.
10. `Editor.tsx`, the stage's `<Frame>` (`slides.numbers.apply`): the editor stage's counter follows the deck's Slide numbers through the render's `slideCounter` (`@turboslide/render/deck`), as the render does for the PDF and the PowerPoint; the Frame drew `01 / 02` on every slide before whatever Insert > Slide numbers said (measured: after Skip title slides the title slide still read "01 / 02"; after the fix it reads nothing and the layout slide keeps "02 / 02"). The show's and the view route's counter is the viewer `Stage.tsx`'s Frame and the print route's, request 12.
11. `SlideNumbers.tsx`: the Apply to selected button is drawn only while the slide schema carries `counter` (`'counter' in contentSlideSchema.shape`), so no write the schema refuses leaves the dialog: on the checkout the refused `slide.set /counter` ("Unknown field counter; unknown data survives only under ext") sat in the save queue and every later save read "Couldn't save, retrying" (section 7 item 5, request 13). The button returns by itself when request 5 lands.

Typecheck: `node_modules/.bin/tsc -b packages/schema packages/render packages/export packages/viewer packages/chrome` clean. Vitest: schema 24 files 537 tests, render 15 files 354, export 19 files 139, viewer 35 files 352, all passing; chrome's chart, table, diagram and inspector suites 6 files 74 pass (the whole chrome suite is run before the report).

### 3. The matrix rows this lane covers (RETURN.md section 5, driven by B4's `core-walk/areas/tables.mjs`, `charts.mjs`, `diagrams.mjs`, `wordart.mjs`, `slides.mjs`, and `core/export.spec.ts`, `core/documents.spec.ts`)

Every `tables.*`, `charts.*`, `diagrams.*` and `wordart.*` row and `slides.numbers.apply`. The rows whose fix is this lane's: `tables.cell.tab-from-written`, `tables.cell.tab-from-empty`, `tables.cell.shift-tab`, `tables.cell.tab-last-appends-row` (fix 1); `tables.column.insert-keeps-widths`, `tables.export.pptx-editable` (fix 4); `tables.column.resize-seam` (fix 5); `charts.type.panel-legible` (the segments); `slides.numbers.apply` (the dialog). Rows that read B2's kept pointer: `tables.menu.format-table-with-session`, `tables.menu.format-table-selected`, `tables.menu.format-table-rows`, `tables.cell.align-menu`, `tables.cell.align-toolbar`. What the runs on 4405 measured is in section 6.

Rows this lane expects to read red on this tree, with the reason: `tables.cells.merge-unmerge` and `tables.tail.merge-unmerge-buttons` (the Editor has no cell range selection: `Selection` in `packages/viewer/src/Selection.tsx` is a block or one run, and no Shift click or drag across cells writes `cells`; both rows carry `parks`, so the merge controls stay parked and the table returns); `slides.numbers.apply` until request 5 lands (the driver reads Apply to selected and the title slide's counter).

### 4. Requests

Written with the exact code; the owner or the integrator makes the change.

1. To the integrator, `packages/viewer/src/Gestures.tsx` `Handle` (line 114): the doc comment "absent for the slide-level handles (seam, plate)" gains ", present on a table's column seam (table-seam.ts)". No code change: the table seam reuses the `col-seam` kind with a `blockId` and an `index`, and `isTableSeamHandle` tells the two apart.
2. To the integrator, `packages/chrome/src/Overlay.tsx` `handleDoc` (line 172): the `col-seam` case branches on the block, so the table's seam carries its own sentence instead of the layout's ratio snaps.
   ```ts
   case 'col-seam':
     return handle.blockId !== undefined
       ? 'Drag to resize the column; the next column takes the difference. Left and Right step 1 px, Shift 10 px.'
       : 'Drag to set the column ratio: snaps to 4/8, 5/7 and 1/1, then 10 px steps. Left and Right step it.';
   ```
3. To the integrator, a width readout for the seam drag. `packages/viewer/src/gesture-life.ts` line 11: `GestureReadout` gains `| { kind: 'width'; value: number }`. `packages/chrome/src/Overlay.tsx` lines 435 to 438 and `packages/viewer/src/Editor.tsx` line 4443 onward (the overlay view): the view gains `widthReadout: number | null` beside `sizeReadout`, rendered as `CANVAS.width(Math.round(value))` with `width: (px: number) => \`${px} px\``added to`CANVAS`in`menus/strings.ts`(line 605, beside`size`). Then `gestureAt`'s seam branch in `Editor.tsx`returns`{ kind: 'width', value: seam.left }`instead of the`size`readout it returns today (the moved column's width by the table's height, which shows the width as RETURN.md asks and reads as a column box). B5 makes the`Editor.tsx` half once the type lands.
4. To the integrator, `packages/chrome/src/FormatOptions.tsx` after `const sectionsShown = ...` (line 459 onward): the grid is the first thing the panel shows for a chart (RETURN.md 2.5, "the round makes the grid the first thing the panel shows for a chart").
   ```ts
   /* a chart's data is what a seller opens the panel for (docs/RETURN.md 2.5): its section leads */
   if (selected?.type === 'chart')
     sectionsShown.sort((a, b) => (a.id === 'chart' ? -1 : b.id === 'chart' ? 1 : 0));
   ```
   `Array.prototype.sort` is stable, so the other sections keep their order. `sectionsShown` is the array `filter` returned, so the sort is in place on a fresh array.
5. To the integrator, the per slide counter for Apply to selected (`slides.numbers.apply`; Google's dialog offers it). `packages/schema/src/deck.ts`: `SlideBase` (line 108 onward) gains
   ```ts
   /** Slide numbers > Apply to selected: this slide's number on or off whatever the deck's counter mode says (gslides-parity SPEC 7.2.4). */
   counter?: 'on' | 'off';
   ```
   and the slide base zod object (beside `skip`, line 436) gains
   ```ts
   counter: annotate(z.enum(['on', 'off']).optional(), {
     label: 'Slide number',
     control: 'select',
     snap: ['on', 'off'],
     group: 'Slide',
     help: 'Numbers this slide, or takes its number away, whatever the presentation’s Slide numbers say (Insert > Slide numbers > Apply to selected).',
   }),
   ```
   `packages/render/src/deck.ts` `slideCounter` (line 62): before the deck's mode,
   ```ts
   if (slide.counter === 'off') return '';
   if (slide.counter === 'on') return n < 1 || total < 1 ? '' : counterText(n, total);
   ```
   The dialog already writes `slide.set /counter` (fix 7). If the field is refused as a design decision, the Apply to selected button leaves the dialog and the row's note names the departure.
6. To B2 (`packages/viewer/src/InlineText.tsx`, the text session; the `InlineTextEndReason` union at line 1198): the cell session's arrow rules, Google's table behaviour. ArrowRight with the caret at the end of a cell's text, ArrowLeft at its start, ArrowDown on the last line and ArrowUp on the first end the session with the reasons `'cell-right' | 'cell-left' | 'cell-down' | 'cell-up'`, only when the run is a table cell (`cellPointer(options.current.pointer) !== null`, the exported helper of `Selection.tsx` line 437, which `InlineText.tsx` already imports for `nextCellPointer`); the keydown branch beside the Tab branch at line 1653:
   ```ts
   } else if (
     (e.key === 'ArrowRight' || e.key === 'ArrowLeft' || e.key === 'ArrowDown' || e.key === 'ArrowUp') &&
     !meta && !e.altKey && !e.shiftKey &&
     cellPointer(options.current.pointer) !== null
   ) {
     const at =
       e.key === 'ArrowRight' || e.key === 'ArrowDown'
         ? caretAtEnd(element, options.current.multiline)
         : caretAtStart(element, options.current.multiline);
     if (at) {
       e.preventDefault();
       e.stopPropagation();
       finish(
         e.key === 'ArrowRight' ? 'cell-right' : e.key === 'ArrowLeft' ? 'cell-left' : e.key === 'ArrowDown' ? 'cell-down' : 'cell-up',
       );
     }
   }
   ```
   (`caretAtEnd` is the mirror of `caretAtStart`; ArrowDown and ArrowUp use the line test the file already has for its list rules, else the end and start of the text.) B5 then adds the four reasons to `endEdit`'s cell switch in `Editor.tsx`: `cell-right` and `cell-left` take `nextCellPointer(block, pointer, ±1)` with the caret at the start (right) or end (left), `cell-down` and `cell-up` the same column one row down or up (`rows/${row ± 1}/cells/${col}`, the block selected past the last or first row), through `pendingEdit`. No matrix row asks for it this round; a row `tables.cell.arrows` would be B4's to add.
7. To B1 (`packages/chrome/src/menus/strings.ts` `DIALOGS.slideNumbers`, line 480): `applyToSelected: 'Apply to selected'`, so the dialog's literal `APPLY_TO_SELECTED` moves into the strings table like its siblings.
8. To B4, `scripts/probes/core-walk/areas/slides.mjs` `slides.numbers.apply` (line 1460 onward): a fresh deck's counter mode is `on` (`deckCounter` answers `on` when `defaults.counter` is absent; `packages/store/src/templates.ts` sets none), so the layout slide's counter reads the same words before and after Skip title slides ("2 / 2" both times; Google keeps the numbering and blanks the title slide's number), and the step's `onL !== before` can never hold. The criterion that matches the dialog and Google: after Apply with Skip title slides, the title slide's counter is empty (`onTitle === '' || onTitle === null`) and the layout slide's is unchanged (`onL === before`, both non empty); Apply to selected on the layout slide with Off writes that slide's counter empty and leaves the title's (once request 5 lands; until then the dialog shows the schema's refusal and the row reads red with that sentence); Cmd+Z after each restores the words read before. The dialog's controls are `dialog.slideNumbers.on`, `.off`, `.skip-title` (a check under On), `.apply` and `.selected`.
9. To the integrator or B3, the cell range (`tables.cells.merge-unmerge`, `tables.tail.merge-unmerge-buttons`, both with `parks`): the viewer's `Selection` has no range form, so no Shift click or drag across cells writes `EditorSelection.cells` and the merge rows stay disabled with one cell (audit-objects row 60). The shape when a lane takes it: `Selection` gains `{ kind: 'cells'; blockId: string; r0: number; c0: number; r1: number; c1: number }` (`packages/viewer/src/Selection.tsx` line 12), the Editor writes it from a Shift click on a second cell of the table whose cell is open or selected and from a pointer drag that starts in a cell and crosses into another (the move threshold of the existing gesture), the overlay draws one ring over the range's union box, `toShellSelection` (`apps/studio/src/editor/EditorRoot.tsx` line 302) maps it to `{ blockId, cell: { row: r0, column: c0 }, cells: { r0, c0, r1, c1 } }`, and `tablePlan`'s `rangeOf` already reads `cells`. Not this lane's files beyond `Editor.tsx`'s gesture, which B3 owns; B5 makes none of it this round.
10. To the integrator, `packages/theme/src/gt-ink-paper/sheet.css` lines 795 to 800: the rules `.ts-sheet .table .td.center { text-align: center; }` and `.ts-sheet .table .td.right { text-align: right; }` go; the render writes the alignment inline since fix 8, and a deck rendered by an older build carries no such class either (the classes were the renderer's, never stored). Optional, and a cleanup only: with the render change the collision is closed whether or not the two rules stay.
11. To the integrator, `packages/chrome/src/Dialog.tsx` `DialogCheck` (line 311 onward) and its CSS: the `.ts-dialog-check-box` span drawn over the native checkbox intercepts a click aimed at the input (`data-control` sits on the input), so a driver's or an agent's click on `dialog.slideNumbers.skip-title` toggles nothing while a person's click on the label does. `.ts-dialog-check .ts-dialog-check-box { pointer-events: none; }` in `Dialog.css`, or the input drawn over the box, lets the control's own box take the click.
12. To the integrator, `packages/viewer/src/Stage.tsx` line 72 and `apps/studio/src/routes/print.$deckId.tsx` line 337: the show, the view route and the print route draw `<Frame index={index} total={total} />` on every slide whatever Insert > Slide numbers says; each takes `counter={slideCounter(deck, slide, index + 1, total) !== ''}` from `@turboslide/render/deck` where the schema slide and deck are at hand (the print route has them; `Stage` takes a `counter: boolean` prop from `DeckViewer.tsx` 264 and `EditorRoot.tsx` 1847, computed the same way), the way `Editor.tsx`'s Frame does since fix 11. Until then `slides.numbers.apply`'s "in the show" read stays red.
13. To B7 or the integrator (`apps/studio/src/editor/controller.tsx` and the room client; `apps/studio/src/server/room.ts`): (a) a write the server refuses on validation (a schema error, not a base revision) must leave the queue with its error surfaced once (the dialog or the notice), not sit in it and hold every later save ("Couldn't save, retrying" for the rest of the session, section 7 item 5); (b) the undo of a `block.set` that created a field is sent as a valueless `block.set` (a deletion) and the server answers "/slots/main/0/border: Invalid input: expected object, received null" (section 7 item 6, B4's gate runs on 4404 and this lane's walk on 4405): the fold on the server turns the absent value into `null` somewhere between the ops endpoint and the slide's validation, in the sequence the walks ran: Fill colour, Border colour, Border weight and Border dash from the table tail on one table (the last three each a `block.set /border`), then four Cmd+Z half a second apart. One border write and one Cmd+Z saved on 4405 (section 7 item 7), so the fault is in the run of undos (an inverse carrying `value: null` for a field its write created, or two undos folding into one), which is what lost the typed text for the tables rows after `fill-border-tail` on 4404.

### 5. Not changed, and why

- `DiagramPanel.tsx` and `WordArtBar.tsx`: RETURN.md 2.6 and 2.7 name no fix; the rows `diagrams.edit-label`, `diagrams.reload`, `diagrams.light-appearance`, `wordart.edit` and `wordart.light-appearance` are measured (section 6) and a defect found there is fixed in its owner's file or requested.
- `chart.tsx`: the grid, Add series, Add category, the legend, the number format and the paste work as audited (rows 65, 114); the one fix was the segments' CSS. The panel ordering is request 4.
- The `format.table.*` plans in `editor-shell.ts` already fall back to cell 1,1 when the selection carries no cell, and B2's `withKeptCell` now feeds them the caret's cell; nothing to change in the plans.
- A row seam (row heights by a drag): not in RETURN.md's rows; the column seam is the fix named. A later round, or a request to B4 for a row first.

### 6. The runs on 4405 (recorded as they happen)

The lock (`.turboslide/e2e.lock`) was held by B4's walks on 4404, B2's on 4402 and B7's gate on 4407 for most of the evening; this lane's runs took it between them, each released with `rmdir` when done.

1. 18:59, `tab-timeline.mjs` after fix 1: Tab from the written cell 1,1 opened cell 1,2 (`focusin tbl/rows/0/cells/1` 23 ms after the Tab, `contenteditable` and `data-editing` set), the typed "N" landed in cell 1,2 (`keydown N target tbl/rows/0/cells/1`), `final editing true`. Before the fix the same script read `editing false, run null, blockId table` from +50 ms on and the "N" opened cell 1,1.
2. 19:00, `overlap-repro.mjs` on the tree before fix 8: the row 97 overlap reproduced and measured as section 1 item 4 says (the screenshot `.turboslide/b5-return/overlap-repro.png`). 19:56, the same script after fix 8: the same sequence (a typed cell, Format > Align & indent > Center on a parked session in column 1, a third session, the cell menu's Insert column right) ended with `columns [{}, {align: "center"}, {}, {}]`, four cells per row and every row drawn `0@510+169 1@679+169 2@848+169 3@1017+169`: the centred column in its own track, no cell over another.
3. 19:01, `seam-check.mjs` (fix 5): one click on a cell selected the table (`editing false`, chip Table) and the overlay carried `move column.0 column.1 resize.nw ... rotate`; the seam handle `handle.tbl.column.0` was visible with the label "tbl: Column seam 1"; a drag of 80 sheet px in 12 steps showed the readout "373 × 320" mid drag and "400 × 320" at the end and none after the release; the drawn widths went 320,320,320 to 400,240,320 with the block storing `[{width: 400}, {width: 240}, {width: 320}]` and the template `minmax(0, 400fr) minmax(0, 240fr) minmax(0, 320fr)`; Shift+ArrowLeft on the focused seam 1 wrote `[400, 230, 330]`; two Cmd+Z restored `[{}, {}, {}]` and 320,320,320 drawn; after a redo, Insert column right through the window API on the sized grid stored `[300, 240, 180, 240]` (sum 960, the new column an equal share, the others scaled by 3/4) and drew every row at 300,240,180,240, no overlap.
4. 19:03 to 19:38, B4's gate walk on 4404 (`/private/tmp/claude-501/-Users-kevinliu-gt-gt-cloud/293a64b7-8ef6-4b00-b382-682288c84431/scratchpad/b4/gate/full-1/core-walk.json`, deck `untitled-20260919-ymbe`, 446 rows, 404 passed, 41 failed, 1 not driven), their server restarted at 19:03 so it served this lane's fixes 1 to 7 (the render's alignment fix 8 landed at 19:02 and may or may not be in that transform). Read with `.turboslide/b5-return/summarise.mjs`; this lane's rows:
   - Passed: `tables.insert.grid`, `tables.cell.double-click-type`, `tables.cell.tab-from-written` ("after Tab editing true in 2,1; cells 2,0 North 2,1 South; cell 0,0 empty"), `tables.cell.tab-from-empty`, `tables.cell.shift-tab` ("after Shift+Tab 1,2; cell 1,2 West"), `tables.cell.tab-last-appends-row`, `tables.menu.format-table-with-session` (nine rows enabled, merge and unmerge disabled, rows 3 to 4, the typed cell kept), `tables.menu.format-table-selected` (columns 4 to 5), `tables.context.rows`, `tables.column.insert-keeps-widths` (169 x4 per row, no overlap), `tables.column.resize-seam` ("handle.table.column.0; widths 320,320,320 to 400,240,320; readout during 400 × 320; undo restored true"), `tables.cell.align-toolbar` (the tail's Align list opened with the seven options and Right wrote column 1 alone, the session kept), `tables.select.resize`, `tables.cell.fill-border-tail`; every `charts.*` row including `charts.type.panel-legible` ("segments Bar 19/36, Column 44/61, Line 24/41, Pie 18/34", none clipped), `charts.type.toolbar`, `charts.type.menu`, `charts.data.toolbar-edit-data` and `.menu-edit-data` (the grid in view at 240 px), `charts.legend.toolbar`, `charts.number-format.toolbar`, `charts.context`, `charts.light-appearance`, `charts.present`, `charts.reload`; every `diagrams.*` row including `diagrams.edit-label` ("text Step 5 plus; groups process, members 14") and `diagrams.reload`; every `wordart.*` row including `wordart.edit` ("Big words now", the outline kept).
   - Failed, with the mechanism each observation gives: `tables.cell.fill-border-tail` passed but its four undos left the save stuck: the reject card read "3 changes were not applied slides/blank-6.json /slots/main/0/border: Invalid input: expected object, received null" and the driver reloaded the editor, so the typed "Q1 revenue" was gone for every later row: `tables.context.insert-delete` ("text kept false" on each of the four rows while the counts changed), `tables.present`, `tables.light-appearance` and `tables.reload` ("text has Q1 revenue false", the cells and the show otherwise right), and `tables.distribute.rows-columns` (the setup writes of a width and a height did not draw, "widths 226,226,226 -> 226,226,226"). The tail's Border colour, weight and dash wrote the table's border (`border {weight: 2, color: red, dash: dash}`) while only Fill wrote the cell (`cells [{row: 1, column: 1, fill: amber}]`); the undo of the write that created `/border` reached the store as `border: null`. Section 7 below records what this lane measured of that undo.
   - `tables.menu.format-table-rows`: the counts changed on each of the seven rows and Delete table removed and Cmd+Z brought the table back, but "undo left 4x4" after Insert row above, "text kept false" after Insert column left and Delete row, and Distribute left "widths 282,197,197" unequal: the same stuck save (the run's order puts this row before the reload, after `format-table-selected`'s column insert; the widths 282,197,197 are a mixed grid the driver's earlier writes left). To be re-read on a run with the save fixed.
   - `tables.cell.align-menu`: "column aligns -,center,-; cell 1,1 text-align start" with `session true`: the align wrote the caret's column (B2's kept pointer) and the driver read the drawn cell while the session was still parked, so it read the markup frozen at the session's start (the freeze B4's own note names for `fill-border-tail`, which ends the session before it reads). A driver read after Escape reads the centred cell; the product side of it (a Format menu write on a parked cell session is not drawn until the session ends) is the frozen markup design of `Editor.tsx` (`shownHtml`), B2's and B3's, and is named here for the round's verifier.
   - `tables.cells.merge-unmerge`, `tables.tail.merge-unmerge-buttons`: no cell range (section 3, request 9); both carry `parks`.
   - `slides.numbers.apply`: the dialog carried "close, on, off, skip-title, selected, apply"; "counter on the layout slide 02 / 05 -> 02 / 05; on the title 04 / 05; after Cmd+Z 02 / 05". The layout slide's unchanged words are Google's behaviour (request 8); the title slide reading "04 / 05" after Skip title slides is either the write not landing (this row ran after the stuck save) or the deck's first slide not being `kind: 'title'` on that deck; section 7 records this lane's own read.
5. 19:57 to 20:03, this lane's walk on 4405 (`.turboslide/b5-return/walk-documents-1.json`, `--only decks,tables,charts,diagrams,wordart`, deck `untitled-20260919-rz7m`, 79 steps, 76 passed, 3 failed), the tree with fixes 1 to 9 served live (the default vite config, hot reload): every `charts.*`, `diagrams.*` and `wordart.*` row passed as on 4404; the tables rows passed except three. Passed here beyond 4404's list: `tables.context.insert-delete` ("text kept true" four times), `tables.cell.align-menu` ("column aligns -,center,-,-,-; cell 1,1 text-align center; after Cmd+Z -,-,-,-,-": the inline alignment of fix 8 draws), `tables.distribute.rows-columns` ("widths 296,95,95,95,95 -> 135 x5; even true; block restored true"), `tables.light-appearance` (light ground `rgb(255, 255, 255)`, cell text 20.14:1, the hairline 1 px, the show 20 cells with "Q1 revenue" and no editable field), `tables.present`, `tables.reload`; `tables.column.resize-seam` read "widths 192 x5 -> 272,112,192,192,192; readout during 272 × 320; undo restored true" on the five column table the earlier rows had built. Failed: `tables.menu.format-table-rows` ("undo left 4x4" after Insert row above and "undo left 5x4" after Insert column left, the counts and the text right otherwise: section 7 item 2), `tables.cells.merge-unmerge` and `tables.tail.merge-unmerge-buttons` (no cell range; both carry `parks`). `tables.cell.fill-border-tail` passed and again left "restored after 4 undos false" with the save stuck on the `/border: null` reject (section 7 item 1). `slides.numbers.apply` was not run (the slides area was not in `--only`); section 7 item 3 is this lane's read of the dialog.

### 7. Mechanisms measured on 4405 after the walk

1. The stuck save after the table tail's border writes (rows `fill-border-tail` on 4404 and 4405, then every row after it on 4404): the reject card reads "3 changes were not applied slides/<slide>.json /slots/main/0/border: Invalid input: expected object, received null". The tail's Border colour, weight and dash write the table's `/border` (`tablePlan` `tableBorder`, `ToolbarTail.tsx`, B2's), and the undo of the write that created the field reaches the store as `border: null`: the reducer's inverse of a `block.set` that created a field carries no `value` (`packages/schema/src/reduce.ts` 238 to 243, a deletion), so the `null` is put in on the way to the store, where a codec or a history entry turns an absent `value` into `null`, and the slide's strict schema refuses it. Not this lane's files (the controller's history and the room client are B7's and the integrator's); `numbers-undo-check.mjs` reads the write the undo sends (section 6 item 7 when it runs). Until it is fixed every Cmd+Z of a first table border write sticks the save, which is what lost the typed text for the rows after it on 4404.
2. Insert row above with a cell session parked in the table (`insert-row-session.mjs`, 20:20): the block went from 3 to 4 rows with "Q1 revenue" in rows 1 and 2 (the original moved down, the copy written into the new row at the session's positional pointer by the session's re-send 1.5 s later, `RECONCILE_RESEND_MS`), revision 2 to 3 while the session stayed open on `rows/1/cells/1`; the first Cmd+Z removed the copy (rows still 4), the second removed the row. That is `tables.menu.format-table-rows`' "undo left 4x4", and a seller's Insert row above duplicates the cell being edited. Fix 10 closes it; its re-run is item 8 below.
3. The Slide numbers dialog (`numbers-undo-check.mjs`, 20:20): the dialog carried the controls `close, on, off, skip-title, selected, apply`; the driver's click on the `skip-title` checkbox input was intercepted by the check's styled box (`DialogCheck`'s `.ts-dialog-check-box` span, `packages/chrome/src/Dialog.tsx` 338, drawn over the native input), so a click at the input's centre reached the span (Playwright: "intercepts pointer events"); a person clicks the row's label, which toggles the input. The rest of the read is item 8 below. Request 11: the box takes `pointer-events: none` (or the input is placed over it), so a click at the control's own box lands on the input the window API names.
4. 20:36, `insert-row-session.mjs` after fix 10: Insert row above with the session parked in cell 1,1 ended the session at once (`session false`), the block went to four rows with "Q1 revenue" once, in row 2 where its cell moved; the first Cmd+Z removed the row (three rows, "Q1 revenue" in row 1), the second undid the last typed burst ("Q1 rev", the history's burst granularity, B2's). No duplicate, no write into the new row.
5. 21:14, `numbers-undo-check.mjs` (Insert > Slide numbers with the switch on): the dialog carried `on, off, skip-title, selected, apply`; Skip title slides then Apply closed the dialog, the title row read "All changes saved", the layout slide kept "02 / 02" and the title slide's counter on the editor stage went from "01 / 02" to none (fix 11); the show opened on the title slide still drew "01 / 02" (the viewer `Stage.tsx` Frame, request 12). Apply to selected with Off on the layout slide was refused by the slide schema ("slides/blank-1.json /counter: Unknown field "counter"; unknown data survives only under ext on a slide, a block or an asset (SPEC 4.1)"), the dialog showed the sentence and stayed, and the title row then read "Couldn't save, retrying" for the rest of the run: the refused write stayed in the queue and every later write (a Cmd+Z, a table border write) waited behind it. Fix 12 keeps the write out of the dialog; the queue's behaviour is request 13. The radio inputs are covered by their dot the way the check is by its box (`.ts-dialog-radio-dot`, Dialog.tsx 377; request 11 covers both).
6. The same run, the table tail's Border weight 2 on a fresh table then Cmd+Z: the block gained `border: {weight: 2}` and the undo sent one op to `/api/decks/<id>/ops`: `{"op":"block.set","slideId":"blank-1","blockId":"tbl","path":"/border"}` with no `value`, the reducer's deletion form (`packages/schema/src/reduce.ts` 238 to 243). So the client asks for a deletion; the "expected object, received null" the walks read comes from the server's side of that op (B7's `room.ts` fold or the store's apply), which is what request 13 asks to trace. In this run the save was already stuck on the `/counter` refusal, so the border op's own fate was not read.
7. 21:19, the same script after fix 12: the dialog carried `on, off, skip-title, apply` and no Apply to selected; Skip title slides then Apply saved ("All changes saved"), the layout slide kept "02 / 02", the title slide's counter on the stage went to none, the show on the title slide still drew "01 / 02" (request 12); Off then the dialog's close wrote nothing and Cmd+Z saved. Then Border weight 2 from the table tail on a fresh table and one Cmd+Z: the undo sent the valueless `block.set /border` and the title row read "All changes saved" 2.5 s later, no reject card. So one border write and its undo save; the sequence the walks ran (the tail's Fill, Border colour, Border weight and Border dash on the same table, then four Cmd+Z at half a second apart) is what left "3 changes were not applied ... /border ... received null", and request 13 (b) names that sequence.

## Return round fix round

Day 2, 2026-09-19. The fixer for B5 read the "Return round, pass 1" section of `VERIFICATION.md` and finding 1 (severity 2): `tables.cells.merge-unmerge` and `tables.tail.merge-unmerge-buttons` red on every origin because "no cell range" (B5 request 9). The other two rows of that finding are other lanes (`versions.show-changes-marks` the stream fix round two fixer, `view.live-pointers.second-browser` B7). Dev server 4405; every scratch deck trashed and deleted forever by the run or by the probe's own teardown.

### 8. The cell range built (finding 1, B5 request 9)

The viewer had no cell range selection, so no Shift click or drag across cells wrote `EditorSelection.cells` and the merge controls stayed disabled with one cell (audit-objects row 60). The chrome was already built for it (`MenuContext.selection.cells`, the `cellRangeSelected` and `mergedCellSelected` predicates, `tablePlan`'s `rangeOf`, the `cellRange` context menu, the tail's Merge and Unmerge buttons); only the viewer never produced a range. This round produces one, in B5's files:

- New `packages/viewer/src/table-range.ts` with `packages/viewer/src/__tests__/table-range.test.ts` (13 tests): the pure part of the range. `rangeBounds` orders the anchor and focus cells and grows the bounds over any merged cell they touch (Google selects a merged cell whole, as `table-tools.ts` `rangeOf` grows the plan); `isMultiCell`, `drawnCellsIn` (covered cells left out), `cellInBounds`, `cellAtPoint` (the cell under a sheet point, read from the measured cell run boxes because the overlay covers the sheet), `rangeBox` (the union box the overlay draws), `rowsWithRangeCleared` (Delete clears the range's texts and keeps the cells), `tableRangeShapeOf` and `rangeStands` (a row, a column or a merged cell added or removed under the range ends it; a text change never does).
- `packages/viewer/src/Editor.tsx`: one `cellRange` state on the selected table, held through `cellRangeRef` and `cellRangeShape`. A Shift click on a second cell of the table whose cell is open or selected, and a drag from an open cell into another, write it (`shiftClickOnCell`, `armCellRangeDrag`); with no anchor yet, a Shift click on a cell of the table selected by one click selects that cell's run so the next Shift click has one. The overlay draws one ring over the range's union box (`.ts-select.is-cells`, the selection colour, `pointer-events: none` from `.ts-select`, so it never intercepts a click). `menuSelection()` returns the ordered bounds as `cells`, and the shell maps them into `EditorSelection.cells` (`apps/studio/src/editor/EditorRoot.tsx` `toShellSelection`), so `cellRangeSelected` and the plans read them with no change to the plans. Escape ends the range (one step in A1 rule 4's chain: session, object, nothing); a selection that moves off the table, another slide, or a grid change ends it; Delete or Backspace clears the range's texts in one write with one Cmd+Z. A printable key or Enter on a table with a range opens the range's anchor cell (A1 rule 4). A plain drag inside an open cell stays the browser's text selection.
- `packages/viewer/src/Editor.tsx`, the handles: while a table cell (a run whose pointer is a cell) or a cell range is active, the table's outer transform handles (the eight resize squares, the rotation ring) and the column seams are not drawn, as Google draws them only for the whole-table (block) selection. Without this a merged cell's centre, which sits on a resize square, took a right click meant for the cell menu and opened the object menu instead (measured on 4405, `.turboslide/b5-fix/diag2.mjs`). A single click still selects the table block and shows the handles and the seams, so `tables.select.resize` and `tables.column.resize-seam` are unchanged (both green on the 4405 walk below).
- After a merge the merged cell stands selected (its cell run), so Unmerge in the cell menu and on the tail acts on it at once; after an Insert row above with a two row range the range ends and the anchor cell is selected.

Typecheck: `node_modules/.bin/tsc -b packages/viewer packages/chrome apps/studio` clean. Vitest: viewer 37 files 372 tests (the 13 of `table-range.test.ts` new, over the round's 36 files 359 tests); chrome 66 files 621 tests, all passing.

### 9. What the checkout measured after the fix (4405, 2026-09-19)

- `.turboslide/b5-fix/range-check.mjs` (the product's own controls, advanced tools on so the parked merge controls are visible, a scratch deck trashed at the end): 14 of 14 ok. A Shift click on cell 1,1 with cell 1,0 open makes the range (the ring over the two cells, chip "Table", session ended); the cell menu on the range lists Merge cells enabled; Merge cells writes one span and the merged cell stands selected; the cell menu on the merged cell lists Unmerge cells enabled; Unmerge restores two cells; a drag from cell 1,0 into 1,1 makes the range (native text selection empty); Escape ends the range and keeps the table; Delete on the range 1,1 to 2,2 clears its four cells' text, the table stays, Cmd+Z restores; the table tail's Merge cells and Unmerge cells buttons merge and restore; a plain drag inside the open cell keeps the session and makes no range; a Shift click on the table selected by one click selects the cell then the range; Insert row above with a two row range adds two rows and ends the range. Log `.turboslide/b5-fix/range-check-3.log`.
- The B4 tables walk on 4405 (`node scripts/probes/editor-walk-probe.mjs --core --base http://localhost:4405 --only tables`, `.turboslide/b5-fix/walk-tables-1.json`): 21 of 23 `tables.*` rows pass, including every row the handles change could touch (`tables.column.resize-seam`, `tables.select.resize`, `tables.cell.align-menu`, `tables.cell.align-toolbar`, `tables.menu.format-table-rows`, `tables.cell.fill-border-tail`, `tables.distribute.rows-columns`, `tables.context.rows` reading 14 rows in the default view). The two red are `tables.cells.merge-unmerge` and `tables.tail.merge-unmerge-buttons`: the range is made (the driver's drag across the cells sets it), but the driver runs the default view where the merge controls are parked behind Tools > Advanced tools, so it reads "Merge cells absent". With the controls visible both rows drive green (the range-check above). Un-parking is the request below.

### 10. Requests

To B1 and the integrator, the four merge controls now return. The cell range that finding 1 (B5 request 9) named as missing is built and tested (section 8, 9); with the controls visible both merge rows drive green through the product's own gestures. Remove the return round re-park so the rows return this ship:

1. `packages/chrome/src/menus/model.ts` (~1760): drop `advanced: true` from `now('format.table.mergeCells', ...)` and `now('format.table.unmergeCells', ...)`; the comment above them ("re-parked from the return round's runs ... the Editor has no cell range selection") no longer holds and can go. `enabled: 'cellRangeSelected'` and `'mergedCellSelected'` stay; both predicates now receive real values.
2. `packages/chrome/src/menus/toolbar-tails.ts` (`MERGE_CONTROLS`, ~143): drop `advanced: true` from `toolbar.mergeCells` and `toolbar.unmergeCells` and the "re-parked from the return round's runs" comment.
3. `packages/chrome/src/menus/__tests__/menu-model.test.ts`: the ledger the re-park moved (the returned list, the parked list, the `tableCell` and `cellRange` menu counts) reverts with the two flags: the default view's cell menu is 16 rows again (the two merge rows return), and Merge/Unmerge are no longer in the parked-controls list. Update the expected counts to the pre-re-park numbers.

To B4 and the integrator:

4. `docs/gslides-parity/focus/core-matrix.json`: remove `parks` from `tables.cells.merge-unmerge` (`format.table.mergeCells`, `format.table.unmergeCells`) and `tables.tail.merge-unmerge-buttons` (`toolbar.mergeCells`, `toolbar.unmergeCells`); the rows return with the feature and no longer park it.
5. `scripts/probes/core-walk/areas/tables.mjs`: `tables.context.rows` expects 16 rows in the default view again (the two merge rows return), not 14; the merge-unmerge rows drive in the default view (the `advancedOn` gate on those two rows can go). `docs/FOCUS.md` re-rendered if its counts move.

Until requests 1 to 5 land, the two rows read red on the default-view walk (parked control), green with the control visible; the ship's parked list should drop `format.table.mergeCells`, `format.table.unmergeCells`, `toolbar.mergeCells` and `toolbar.unmergeCells`. The mechanism ships in B5's files this round whatever the integrator decides for the flags.

### 11. Not changed, and why

- `versions.show-changes-marks` and `view.live-pointers.second-browser` (finding 1's other two rows): other lanes (the stream fix round two fixer; B7), not touched.
- No git write command, no `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran; the 4405 dev server was stopped before this report.
