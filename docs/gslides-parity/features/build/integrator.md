# The integrator, ship one

## Features round, ship one

The integrator's note of the features round, ship one (2026-09-22, times in UTC), in the shape of the sync and costs round's build notes. The binding specification is `docs/FEATURES.md` (section 6 the lanes and the shared file table with its one merge order, section 7 the acceptance); `docs/PRODUCT.md`, `docs/SYNC.md`, `docs/RETURN.md`, `docs/FOCUS.md` and `docs/gslides-parity/focus/AMENDMENTS.md` A1 stand under it, A1 rules 1 and 3 amended for tables alone by FEATURES.md 2.1. The six lane notes beside this one (`b1.md`, `b2.md`, `b3.md`, `b4.md`, `b6.md`, `b7.md`) are read as claims; every number below was read first hand from a command this integrator ran, and a row nobody drove is not driven, never passed. The round was built on the shared checkout at `88b68e7`, the tree clean when the lanes started and carrying their uncommitted work when this merge began. This integrator is the one agent of the round allowed git write commands, on the branch `features/ship-one` alone, created from `88b68e7` with `git checkout -b`; nothing was pushed and `main` is untouched. No `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran outside `scripts/check.mjs`; `pnpm generate:contracts` ran once. The round five branch was never checked out, merged or cherry picked. No token, cookie or secret was printed; the two preview secrets came from `openssl rand -hex 32` inside the deploy command and the deploy's output was filtered of the lines that echo them.

Sections: 1 the merge and its commits, 2 the requests answered, 3 the gates on the tree, 4 the preview and its runs, 5 the rows by run, 6 the findings by owner, 7 the pictures, 8 deviations and what the ship step still owes, 9 the run ledger.

### 1. The merge and its commits

The lanes' work was committed by explicit path lists from each lane's note in the one merge order FEATURES.md section 6 gives (B4, B2, B3, B1, B6, B7; B5 is ship two's), one commit per lane with the lane's items and their hunks named in the body so the fix round can revert an item by hand. Where two lanes touched one file the file rides in the first lane's commit of the order and the other lane's hunk is named there: `packages/viewer/src/Editor.tsx` (B3's commit carries B6's `insertLogoAsset`), `packages/viewer/src/Selection.tsx` (B3's commit carries B6's chip word by request), `THIRD_PARTY_NOTICES.md` (B2's commit carries B6's thesvg.org section), `packages/chrome/src/menus/strings.ts` (B3's commit carries `CANVAS.editData`, the integrator's commit the dialogs' words). Then the integrator's three commits.

| Commit    | What                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `b26881c` | B4, the matrix and the drivers (71 rows, the drivers, `--emit-parked`, the logos spec, the coverage list generator, docs/FOCUS.md and the README re-rendered)                                                                                                                                                                                                                                                                               |
| `326cb07` | B2, text and fonts (the 4.1 italic, the links, the fallback, tabular figures, the display features, the eight families; THIRD_PARTY_NOTICES.md with B6's section)                                                                                                                                                                                                                                                                           |
| `ed09447` | B3, the objects editing model (the thirteen P0 items in rank order, the P1 items 7, 8 and 9; Editor.tsx with B6's `insertLogoAsset`; Selection.tsx with the chip word)                                                                                                                                                                                                                                                                      |
| `b2c16f6` | B1, the chrome and the pickers (parked-controls.ts, the Logo dialog, the Tailor slot, the Inter licence line, the P1 picker deltas)                                                                                                                                                                                                                                                                                                         |
| `4a6798f` | B6, logos and the server routes (the source kind, logo-model.ts, the sanitizer, the fixture, the index and the refresh, the service and the insert, the three actions, the route, the cron)                                                                                                                                                                                                                                                 |
| `15c28f0` | B7, the server and the exports (the logo role at 3x, the svg sentence, the two variables, the ids on the transports, the P1 URL sentence and the svg branch)                                                                                                                                                                                                                                                                                |
| `cf5a008` | prettier over the 22 lane files `--check` listed, nothing else                                                                                                                                                                                                                                                                                                                                                                              |
| `6f3b6d9` | the integrator's day 0 requests and the seams (section 2), the generated contracts, facts.json and facts-data.ts                                                                                                                                                                                                                                                                                                                            |
| `11dbb5a` | the ship's parked list `docs/gslides-parity/focus/ship-6f3b6d9.json` (empty) and `parked-controls.ts` written from it by `--emit-parked`                                                                                                                                                                                                                                                                                                    |
| `39ae62e` | the memory tier gate's seams (section 5): the appearance rule turned to thesvg's variant names, the dialog's one placement, the kit write in the slide's commit, the refusal card's gate and the local refusal's card, the print page's hydration mark, `slideStrings` over `blockStrings`, the six valued flags, `describe().state.assets`; the drivers and the specs corrected with the mechanism beside each; the matrix's two row texts |
| `44c2cc0` | the parked list renamed `ship-39ae62e.json` after the commit whose tree the runs of record and the preview read (still empty), `parked-controls.ts` re-emitted from it                                                                                                                                                                                                                                                                      |

The untracked `.github/workflows/` folder (three files dated 2026-09-10 and 2026-09-15, no lane's) was left as it was found.

### 2. The requests answered

Every request under the lane notes' "Features round, ship one" headings addressed to the integrator, or to a lane that did not act on it where the seam blocked a row, with what was done. The hunks are in commit `6f3b6d9`.

| Request                                                  | Answer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| b1 R1, b6 R1 (`menus/model.ts`)                          | `insert.logo` directly under the Image row in the default view with the icon `tag` (b1 asked for `building-office-2`, which is not in `icons.tsx`'s set), `insert.image.logo` after Upload from computer, `format.image.replaceImage.logo` after By URL in `replaceImageItems`; each with `dialog('Logo')`, `turboslide: true` and the terms logo, brand, company, mark. `MenuItem` gains `terms?: ReadonlyArray<string>` (b1 wrote a string; `finder.ts` `sellerTermsOf` reads an array alone). |
| b2 R1 (`menus/model.ts`)                                 | `format.text.tabularFigures` after `format.text.font`, `panel('Format options')`, enabled on a text block, the sentence as its doc, the five terms; `editor-shell.ts` `PANEL_SECTION_OF` maps it to the Text section.                                                                                                                                                                                                                                                                            |
| b1 R2 (`editor-shell.ts`, `EditorShell.tsx`)             | `'logo'` in `DIALOG_IDS`, `dialogIdOf('Logo')`, the lazy `LogoDialog` in `LAZY_DIALOGS`, `'logo'` beside `imageByUrl` and `fromThisPresentation` in `runItem` (the picture target), the `dialogNode` case.                                                                                                                                                                                                                                                                                       |
| b6 R3, b1 R2 (`editor/controller.tsx`)                   | `for (const id of SERVER_SIDE_WINDOW_ACTIONS_F1) serverSide(id);` after the P1 loop, so the window transport runs `logo.search`, `logo.insert` and `logo.refresh` on the server.                                                                                                                                                                                                                                                                                                                 |
| b1 R3 (`menus/strings.ts`, `panels/assist-strings.ts`)   | `LOGO_DIALOG` and `TAILOR_LOGO` moved into strings.ts (the dialog composes `DIALOG_WORDS` from `LOGO_DIALOG` and B6's `LOGO_WORDS`; Tailor.tsx and the two tests import from strings.ts); `TAILOR.logoFileDoc` reads "A PNG, JPEG, WebP or GIF under 20 MB".                                                                                                                                                                                                                                     |
| b3 R1, R2, R6                                            | `CANVAS.editData` kept; the eight chart snapshots accepted (the diff on the two files is the two attributes); `ToolbarHead.tsx`'s `value` prop accepted in B3's commit.                                                                                                                                                                                                                                                                                                                          |
| b6 R2 (`actions-gs3.test.ts`, the contracts)             | `ACTION_IDS` pinned at 186 with the three logo rows named; `pnpm generate:contracts` once (8 of 14 generated files rewritten); `packages/theme/brand/facts.json` rebuilt with `node scripts/build-brand.ts --facts` (186 actions, 163 MCP tools, 170 HTTP paths) and `apps/studio/src/components/home/facts-data.ts` with `node scripts/build-home-assets.ts`, both `--check` green.                                                                                                             |
| b6 R8 (`apps/studio/package.json` sharp)                 | Not landed: a dependency line needs `pnpm install` for the lockfile, which this pipeline does not run; `sharp` resolves through the root's hoisted copy and `vite.deploy.config.ts` externalizes it, which the preview's build proved (the logo routes answer; section 4). Kevin's `pnpm add sharp --filter @turboslide/studio` at a quiet moment.                                                                                                                                               |
| b6 R11 (`apps/cli/src/commands/logo.ts`, `cli.ts`)       | New: `turboslide logo search <query> [--limit --kind --collection]`, `logo insert <slug> [--variant --slide --every-slide --kit --block]`, `logo refresh [--dry-run]` over `runDeckAction` (`--host` read as `--to`, the spelling `core/logos.spec.ts` uses); the usage lines; on a checkout without `--to` the CLI says "logo.search runs on a hosted studio: pass --to <url>".                                                                                                                 |
| b7 R1 (`packages/render/src/stage.ts`)                   | `frameBandHtml`'s kit band carries `ts-kit-wordmark` in both kit forms; `theme-css.test.ts` pins the class. The export's document and the print document draw the footer logo again.                                                                                                                                                                                                                                                                                                             |
| b7 R2 (`picture-place.ts`, `Editor.tsx`)                 | `UploadFailure` gains `not-svg` with `NOT_SVG_SENTENCE` ("SVG files are not accepted yet. Export the logo as a PNG and upload that"; the studio's `UPLOAD_REASONS.notSvg` repeated because the viewer cannot import the studio), read from the intake's line or the `not_svg` code; the Image by URL dialog shows the server's allowlist sentence whole.                                                                                                                                         |
| b3 R8 (`apps/cli/src/store-actions.ts`)                  | `diagramInsert` applies the freed ids to every connector's `connect` through `renameConnectorRefs`, so a second diagram's arrows bind to its own steps; `apps/cli/src/commands/walk.test.ts` reads the binding and the one object step.                                                                                                                                                                                                                                                          |
| b4 (`.prettierignore`, the tests)                        | `docs/gslides-parity/features/build/*/**/*.json`; `core-matrix.test.mjs` and `logo-coverage.test.mjs` accepted in B4's commit; docs/FOCUS.md and the README needed no re-render (no matrix edit after B4's).                                                                                                                                                                                                                                                                                     |
| b2 R5 (the table's typography, P1)                       | Not landed: three lanes' files (the schema's table block, `renderTable`, `takesFamily`) for a P1 row whose condition no lane could meet; `fonts.table.takes-family` stays not driven.                                                                                                                                                                                                                                                                                                            |
| b7 R3 (`logo-index.ts` reading the flag from `flags.ts`) | Not done; `flags.test.ts` pins that the two agree, nothing breaks either way.                                                                                                                                                                                                                                                                                                                                                                                                                    |
| b2 (`packages/headless/src/sheet.ts` 135)                | Not done: the contact sheet's body stack lacks 'Inter Fallback'; no row reads it; one line for the round after.                                                                                                                                                                                                                                                                                                                                                                                  |
| `scripts/check.mjs` `OVERWRITE_ALLOW`                    | `apps/studio/src/server/logo-index.ts` added (check step 6's greps named the two `overwrite: true` sites: the index record and the open licence mark cache under the system prefix, never a deck's public asset).                                                                                                                                                                                                                                                                                |

The seams the merge opened, resolved in the same commit:

- `packages/chrome/src/palette-data.ts` `filterPalette`: two menu rows with one label and one score list the shallower path first, so Search the menus "logo" answers Insert > Logo before Insert > Image > Logo and Format > Image > Replace image > Logo (the three rows tie at the title match; `logos.insert.row` reads the first).
- `packages/chrome/src/menus/keys.ts` `assignAccessKeys`: the greedy letter assignment became a largest matching (an augmenting path over the items and the letters), because Insert > Logo took the one letter Special characters had left and the menu model test refused a sibling without a key.
- `packages/chrome/src/menus/__tests__/menu-model.test.ts`: the two named cases of FEATURES.md 7.3, the rows table (Logo and Tabular figures) and the counts (insert 23, format 32, 160 now rows, 199 rows).
- `apps/studio/src/editor/no-action-id-snackbar.test.ts`: extended to the editor's `notice(` calls and the Logo dialog's `setError(` calls (7.3, "B3, extended").
- `packages/export/src/pptx/text.test.ts`: the 3x kinds read icon, logo and mark (B7's item without its test).
- The four whole suite failures of the merged tree before these edits: `text.test.ts` (the 3x kinds), `theme/brand.test.ts` (facts.json at 183 actions), `cli/walk.test.ts` (four text labels of a diagram; a step is one object since B3's rank 9) and, after the request landed, none.

### 3. The gates on the tree

Run from the repository root unless said. The gates ran first on the merged tree at `11dbb5a` (the day 0 integration) and again on the committed fixes at `39ae62e` and `44c2cc0` where the row says so; a row without a second reading was not re-run after the fixes and says which tree it read.

| Gate                                                                  | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm generate:contracts`                                             | once; 8 of 14 files written (cli.json, mcp-tools.json, describe.json, openapi.json, manifest.json, llms-full.txt, actions.md, browser-api.md); a second run writes nothing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `node_modules/.bin/tsc -b`                                            | exit 0 at `11dbb5a` (after one error of the merge, `SNACKBARS` unused in Logo.tsx once its words moved) and exit 0 on the tree of `39ae62e` before its commit, the e2e files included (`apps/studio/tsconfig.json` lists `e2e`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `node_modules/.bin/vitest run` (every package)                        | At `11dbb5a`: 382 files, 4,143 tests: 4,134 passed, 3 skipped, 2 todo, 4 failed before the seams of section 2 (text.test.ts, brand.test.ts twice, walk.test.ts); the four suites green after them and the named tests of 7.3 for this ship green: fonts inter and catalog, theme tokens, render theme-css, studio logos, chrome logo-model, parked-controls, menu-model, no-action-id-snackbar, schema validators. After the fixes: the suites of the edited modules green (logo-model, logo-dialog, find-replace-matches, tailor-dialog, server/logos, refusal-gate, file-store, the scripts project's 110). The whole suite at `44c2cc0` (with the working tree's `refusal-gate.test.ts` and the logos spec edit, neither a source of the suite's modules): 383 files, 4,144 passed, 3 skipped, 2 todo, exit 0 (22:04Z to 22:09:51Z, beside the gate's cost probe).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `node scripts/check.mjs --greps`                                      | exit 0 after the allowlist entry and again at `44c2cc0`: innerHTML 27 call sites in the allowlist, overwrite: true 25 in the allowlist.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `node docs/readme/what-works.mjs --check`                             | README.md is current, at `11dbb5a` and at `44c2cc0`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `node scripts/build-brand.ts --check`, `build-home-assets.ts --check` | 31 files match the manifest and the facts; 48 files of 16 pictures match, facts-data.ts matches facts.json (at `11dbb5a` and at `44c2cc0`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `turboslide lint --chrome` (step 18's six pages on 4418)              | At `11dbb5a`: `/deck/gt-brand` 24 audits, `/edit/gt-brand` 18, `/new` 18, `/decks` 6, `/home` 6, `/no-such-page` 6, over 1440, 1280 and 390 in both appearances: 0 with findings, 0 states unapplied. Not re-run after the fixes, whose chrome change is the tile halves' files and the print page's hydration mark.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| prettier                                                              | `--check` over the 257 files of the ship (tracked changes since 88b68e7, the working changes and the untracked files, the binaries and LICENSE files aside): clean after `--write` on the 22 lane files of `cf5a008` and the integrator's own; `--check` clean over every file of `39ae62e` and `44c2cc0` before their commits (one `--write` on `file-store.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `node scripts/check.mjs` in full                                      | On the tree of `44c2cc0` with this folder, in chunks because the runner stops at the first failing step (01:34Z to 02:3xZ): steps 1 to 19 ok once the ledgers were under the ignore file (step 5's `pnpm test` 51 s, step 18's chrome lint 90.6 s, step 19's format check 35.3 s); step 20, the parity audit, FAIL as it stands: 3,127 pass, 40 fail, 397 skipped in 830 s (the sync ship recorded 37 rows; the three more are `effects:textBlock2` `format.textFitting`, `format.dropShadow` and `format.altText`, each a 4 s wait for `#ts-menu-canvas` on the loaded runner, and the rows it wrote to `verification-3/parity-audit.json` were restored before the commit, as the sync ship did); step 21, the older studio suites, FAIL as it stands: 17 failed, 41 passed in 8.4 min (the sync ship's 17 of 72); steps 22 to 24 ok (the native export round trip 37.2 s, the fonts build check 34.3 s, the canvas fidelity 62.1 s); step 25, the render worker's docker build, FAIL on this machine alone: "no space left on device" while docker copied the 1.1 GB build context (the runner's disk, not the tree); step 26 not run (the runner stops at 25; the sync ship recorded 26 as its four standing two browser files); step 27 skipped by the runner's rule (no production preview on 4344); steps 28 to 30 ok (the audit 0.5 s, the brand and definitions checks 3.9 s, the Vercel build with `check-vercel-output.mjs` 8.7 s); step 31, the perf budget, FAIL as it stands: 113 of 151 budgets met on 4321, the sync ship's exact reading; step 32, the gate, ran as the runs of 5.4 and 5.5 instead of 4321. Standing reds: 20, 21, 31 (the sync ship's), 25 the machine's, 26 not read. |
| the core gate on 4418 (the memory tier)                               | Run 1 at `11dbb5a`: 785 rows, 741 passed, 28 failed, 16 not driven (5.1). Run 2 at `44c2cc0`, the run of record: 785 rows, 760 passed, 8 failed, 17 not driven, retries zero, cost rows over their ceiling 0, 5,441 s (5.4); the 8 reds are four standing parked rows, the two P1 paste rows, one standing filmstrip row and `logos.insert.every-slide`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

### 4. The preview and its runs

Two enforce previews were deployed from the repository root at `44c2cc0` (the tree clean but for `.github/` and this folder), each with the recorded command of VERIFICATION.md plus the logo upstream FEATURES.md 4.9 and 7.2 name for a preview, the two secrets minted by `openssl rand -hex 32` into the command and never printed:

- The deployment of record, the fixture upstream: `https://turboslide-e4sw7h7ay-kl01s-projects.vercel.app`, `vercel deploy --yes --archive=tgz -e TURBOSLIDE_AUTHORIZE=enforce -e TURBOSLIDE_ASSIST=fixture -e TURBOSLIDE_LOGO_UPSTREAM=fixture -e TURBOSLIDE_MAIL=off -e TURBOSLIDE_REALTIME=blob -e TURBOSLIDE_PUBLIC_STORE_HOST=ggmycvj7j6224ay5.public.blob.vercel-storage.com -e TURBOSLIDE_SESSION_SECRET=<hex> -e TURBOSLIDE_DOWNLOAD_SECRET=<hex>`, 20:35:21Z to 20:39:00Z, the upload 337.2 MB. The gate's two runs, the hosted smoke and the write probe ran against it.
- The outage upstream (`TURBOSLIDE_LOGO_UPSTREAM=down`), the same command otherwise: `https://turboslide-ccb5xs0g7-kl01s-projects.vercel.app` (20:39:00Z to 20:43:05Z, 337.3 MB), for `logos.index.cached-offline` alone (the row reads the index a deployment holds while the upstream answers nothing; on the fixture upstream it reads not driven).

The first preview of this ship (`https://turboslide-nyldwoh75-kl01s-projects.vercel.app`, 15:37Z, the tree at `11dbb5a`) was superseded by the fixes commit before any gate run and is not a run of record; its smoke (40 of 40), write probe (ok) and logo route reads stand in the scratch logs alone. The logo index in the shared Blob store (`system/logo-index.json`) was built by that first preview at 15:45:28Z from the fixture's ten marks; the deployment of record reads the same index (refresh `dryRun` answers `updatedAt` 15:45:28Z, 10 icons, 9 brands, 1 cached mark, `upstream: fixture`, `credential: agent`).

Before the gate runs on the deployment of record:

| Probe                                                                              | Result                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `node scripts/hosted-smoke.mjs <preview> --token-env TURBOSLIDE_TOKEN` (20:39:45Z) | 40 of 40 passed: the pages, the headers, the enforce rows (unsigned thumbnail 403, cross site POST 415), the CDN rows with HIT on the second read (`/home` HIT on both), the effects backend (typescript; glibc 2.34; node v24.20.0), `deck.info` with 1 snapshot at revision 26.                                                                                                                            |
| `node scripts/probes/new-write-probe.mjs --base <preview>` (20:39Z to 20:41:09Z)   | every step ok: the draft's first write moved the address to `/edit/untitled-20260922-xeoh`, twelve edits each acknowledged at the next revision, the reload at 13, no self chip, the second tab, File > Move to trash then Delete forever removed the deck (the scratch deck is gone from the shared store).                                                                                                 |
| the logo routes, read with the bearer (20:41Z)                                     | `POST /api/logo/refresh { dryRun: true }` answers the shared index (above); `GET /api/logo/search?q=figma` lists Figma first with its three variants and "Free to use"; `?q=vercel` lists the five variants (default, mono, light, dark, wordmark); `GET /api/logo/mark/figma/default.svg` answers 200 image/svg+xml with nosniff, the sandbox policy and an hour of cache; `vercel/light.svg` fills `#000`. |

The two gate runs against the deployment of record, run one after the other with no code change between them (FEATURES.md 7.2's flakiness check), and the outage preview's logos file are read in 5.5.

### 5. The rows by run

#### 5.1 The memory tier, run 1 (the merged tree at `11dbb5a`, 4418, 15:47Z to 17:24Z)

`node scripts/probes/core-gate.mjs --base http://localhost:4418 --lock <scratch> --out <scratch>/gate-4418 --matrix <scratch>/gate-4418/ledger.json --parked docs/gslides-parity/focus/ship-6f3b6d9.json`: 785 rows, 741 passed, 28 failed, 16 not driven (1 manual), 0 no step, retries zero, cost rows over their ceiling 0, 5,831 s; verdict failed. The ledger copy is `build/integrator/gate-4418-run1-core-gate.json` with its `core-matrix.md`. Of the 71 ship one rows: 41 passed, 19 failed, 11 not driven. The cost rows on the memory tier: editor idle 10 function requests a minute (ceiling 12), editing 71.04 (ceiling 75), two tabs 19.66, the show 0, the store half zero on the file store.

The 28 reds by mechanism, read from the run's observed text:

| Rows                                                                                                                                                                 | Mechanism                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | What changed before run 2                                                                                                                                                                                                                                                                                                       |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `logos.picker.paper-and-ink`, `logos.picker.recents`, `logos.insert.one-click-asset`, `logos.insert.logo-size`, `logos.insert.mono-tint`, `logos.insert.every-slide` | The dialog passed `slideId`, `blockId` and `everySlide` to `logo.insert` and then called the editor's `insertLogoAsset`, so the handler placed a block and wrote the kit and the editor placed a second block: two pictures a click (recents read `before + 2`), the server's block under the crowded slot (`logo-7` at 72 by 107 off centre), no snackbar and no Undo for the kit write (509 to 511, Cmd+Z 511), the tile shot before its twin decoded (lit 0.01 percent). | `dialogs/Logo.tsx`: when the editor's handle carries `insertLogoAsset` the handler stores the asset alone (b6.md R10 as written) and the editor places once with the kit's slots and Undo in one commit; `logo-dialog.test.tsx` pins the input. The drivers wait for the placed picture's `img` to decode before they shoot it. |
| `logos.picker.empty-state`, `logos.picker.licence-words`                                                                                                             | Upload hands the chooser to the shell and closes the dialog (the design); the driver searched the kit's name in a closed dialog and timed out on `dialog.logo.search`; the cloud switch sits inside a closed `<details>` (More) and read not visible, so the AWS half never ran.                                                                                                                                                                                            | `areas/logos.mjs`: the dialog is reopened after Upload; `openMore()` opens the disclosure before the switch is read (the licence and the tint rows).                                                                                                                                                                            |
| `logos.insert.mono-tint` (the example mark)                                                                                                                          | The fixture's GitHub carries its own light and dark files, which the appearance rule prefers to a tint (b6.md section 5), so GitHub never inserts as a tinted mono; Stripe has a mono and no pair.                                                                                                                                                                                                                                                                          | The row and the driver read Stripe; the matrix row's note records the change.                                                                                                                                                                                                                                                   |
| `logos.picker.chrome-1280`                                                                                                                                           | The spec counted the tiles sharing the first tile's top, which is the Your brand group's one tile (perRow 1 with 10 tiles drawn in a 640 px card); the grid itself lays five a row.                                                                                                                                                                                                                                                                                         | `chrome.spec.ts` `logoGridFacts` reads the widest row inside one group.                                                                                                                                                                                                                                                         |
| `logos.export.pdf-pptx-crisp`                                                                                                                                        | The spec clicked `dialog.download.mode.perfect`; the dialog's control is `dialog.download.mode.flatten` (the Perfect file is the flatten mode), so the second download waited until the test's 300 s.                                                                                                                                                                                                                                                                       | `export.spec.ts`: the modes native and flatten.                                                                                                                                                                                                                                                                                 |
| `logos.agent.search-insert`                                                                                                                                          | `turboslide logo search figma --host <origin> --json` answered "pass --to <url>": the parser did not know `--host`, `--limit`, `--collection`, `--variant`, `--slide` and `--block` as valued flags, so `--host` read as a switch and its address as a positional.                                                                                                                                                                                                          | `apps/cli/src/args.ts` `VALUED_FLAGS` gains the six; the command answers Figma with Free to use against 4418.                                                                                                                                                                                                                   |
| `logos.intake.svg-sentence`                                                                                                                                          | The snackbar half passed ("SVG files are not accepted yet. Export the logo as a PNG and upload that" 226 ms after the chooser); the Upload from computer chooser's `accept` read `image/*`.                                                                                                                                                                                                                                                                                 | `apps/studio/src/editor/EditorRoot.tsx` `pickPicture`: the four raster types.                                                                                                                                                                                                                                                   |
| `logos.intake.url-sentence`                                                                                                                                          | The sentence half passed (the four sites, no CLI flag); the driver's Wikimedia address was the thumb path, which answers 400 to every caller with or without the server's User-Agent (curl, 2026-09-22); the file itself answers 200.                                                                                                                                                                                                                                       | `areas/logos.mjs`: the file's own address.                                                                                                                                                                                                                                                                                      |
| `tables.range.size-color`                                                                                                                                            | The typed table sits at the ladder's top (20), so the size step up has no rung and says so; the colour landed in every cell as a mark span (`[H0]{c:green}`) while the driver read the paragraph's own colour. Align right wrote every cell.                                                                                                                                                                                                                                | `areas/tables.mjs`: the typed table is placed at 18 so the step up has a rung; the colour is read where the glyphs draw (the element around the run's first text).                                                                                                                                                              |
| `charts.panel.no-duplicate-controls`                                                                                                                                 | One Chart type group and one Legend; "Title" counted twice (the label and the span inside it) and the Alt text section's Description textarea counted as the JSON view.                                                                                                                                                                                                                                                                                                     | `areas/charts.mjs`: the innermost of nested label matches counts once; the textareas are read inside `[data-section="chart"]`.                                                                                                                                                                                                  |
| `brand.objects.kit-colours-first`                                                                                                                                    | The table's fill plate and the text plate lead with the six kit roles (kit 6 of 6, the tooltip "Text #f2f2f0"); the chart's series swatches list the tokens (kit 0 of 6) and a new chart's series are default: FEATURES.md 2.3 item 4 (P1) is not landed (b3.md section 5).                                                                                                                                                                                                 | `areas/brand.mjs`: a chart plate with no kit role reads not built with the id, the rule the fill plate already followed; the P1 item's row is recorded not driven (FEATURES.md section 1).                                                                                                                                      |
| `fonts.catalog.six-families`                                                                                                                                         | Bricolage Grotesque sits under Sans serif and Fraunces under Serif, Google's categories at the pinned commit, which the fetch script records and the picker groups by (b2.md 1.6, R3); FEATURES.md 3.2 wrote both under Display. Fraunces on the heading computes and loads.                                                                                                                                                                                                | The matrix row and the driver read Google's categories; question 7 of FEATURES.md section 9 is where Kevin names a hand override (section 8 below).                                                                                                                                                                             |
| `assist.tailor.dialog-one-undo`, `assist.tailor.agent-deck-tailor`                                                                                                   | The count read "7 places on 5 slides" and Apply renamed 6: the logos area had placed a picture whose alt names Acme, and `slideStrings` counted the alt while `textTargets` (the write) never touches an alt; the agent row's HTTP tailor then found nothing to rename at the revision it read.                                                                                                                                                                             | `dialogs/FindReplace.tsx` `slideStrings` leaves a picture's alt out, so the count says what the write changes.                                                                                                                                                                                                                  |
| `sync.structural.concurrent`                                                                                                                                         | B deleted the slide while A moved a block on it; A's write was refused `invalid` with no text, and B3's refusal card gate (rank 8) read it as a chrome control's refusal, said one sentence and dismissed the notice, so the loser's card never showed (`sync.block.offline-replay-converges` then failed its teardown on the decks the aborted test left).                                                                                                                 | `EditorRoot.tsx` `isControlRefusal(notice, document)`: the gate holds only while every slide and block the write named is still on the document; a write whose object is gone keeps the card. `refusal-gate.test.ts` pins the four cases.                                                                                       |
| `slides.delete.two-selected-key-undo`                                                                                                                                | Two cards selected, Delete removed both (13 to 11), the snackbar's Undo brought one back (12) and the reload agreed. Not explained by a hunk of this ship; re-driven alone in run 2 (5.2).                                                                                                                                                                                                                                                                                  | none before run 2                                                                                                                                                                                                                                                                                                               |
| `tables.paste.tsv-makes-table`, `tables.paste.into-cell-spreads`                                                                                                     | FEATURES.md 2.3 item 5 (the spreadsheet paste, P1) is not landed (b3.md section 5): the paste makes a text box and a paste into a cell lands in that one cell, the product before the item.                                                                                                                                                                                                                                                                                 | none: the P1 item's rows, recorded here as the item not landed (FEATURES.md section 1), red with the facts in every run of this ship.                                                                                                                                                                                           |
| `view.live-pointers.second-browser`, `versions.show-changes-marks`, `inbox.bell-panel-toggle`, `templates.gallery.page` (and `templates.deck.read-only` not driven)  | The previous ship's parked set (`ship-7ea3db9.json`: inbox and templates parked, the three parked rows), still behind the menu model's `advanced` flags; this ship's empty list pre forgives nothing, so they read as reds here.                                                                                                                                                                                                                                            | none: standing.                                                                                                                                                                                                                                                                                                                 |

The 16 not driven rows: the 11 of ship one (the P1 controls of B3 (four table handles and bars, the group tail, the word art tail), the specimen rows, `fonts.table.takes-family`, `logos.picker.variants`, `logos.kit.find-a-logo`, and `logos.index.cached-offline`, which reads the fixture upstream on this base) and five standing (the manual paste row, the template's read only row, `assist.quota.429`, `sync.resend.idempotent`, `cost.editor-hidden.calls`), each with its reason in the ledger.

#### 5.2 Between the runs: the re-drives and the spec re-runs on 4418

The rows run 1 left red were re-driven area by area (`editor-walk-probe.mjs --core --only <areas>`, six re-drives) and spec file by spec file (`core-gate.mjs --only specs --spec <files>`, seven re-runs) on the fixed tree, each after a restart of the no watch dev server where a served module changed. What each pass read and what changed before the next, in the order it happened:

| Pass                                                           | Read                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Changed before the next pass                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| -------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| re-drives 1 to 3 (logos, assist, tables, charts, brand, fonts) | The section 5.1 fixes held (one picture a click, the kit write in the insert's commit, the flatten mode, the six valued flags, the four chooser types); left red: `logos.picker.paper-and-ink` (ink half 100 percent lit on #070707, paper half 13 percent, the inserted mark on the light deck 0.01 percent), `logos.picker.licence-words` (a `boundingBox` timeout at the AWS search), `logos.insert.logo-size` (the mark 279 px left of the slot's centre), `logos.insert.every-slide` (every check true in the observed line), `logos.insert.mono-tint` (GitHub), `assist.tailor.dialog-one-undo` (5 places against 4 renamed). | `areas/logos.mjs`: the tile halves shot inside the results group after a scroll into view with the half's own background as the ground; the lit fraction's ground from the shot's margin ring; the every slide row's blank slide made before the insert; Stripe's mono asked for through the window API. `dialogs/FindReplace.tsx`: `blockStrings` leaves a picture's alt out.                                                                                                          |
| re-drive 4 (logos, assist)                                     | `paper-and-ink`: both halves 0.00 percent lit on their own grounds and the light deck's mark 0.01 percent, with the img decoded at 554 by 480; `licence-words`: the timeout again; `logo-size`: 467,414 108 by 160 against the slot 80,218 1440 by 553; `every-slide`: every fact true; `assist.tailor.dialog-one-undo`: "5 places on 4 slides" with the alt path listed.                                                                                                                                                                                                                                                           | The hand probes of section 5.3 read the mechanisms: the appearance rule inverted against thesvg's variant names (the fix in `logo-model.ts` and `server/logos.ts`); the dialog's typographic apostrophe against the driver's straight one; a click on the preselected tile inserts it (the driver waited instead); the placement's area is the empty paragraph's column (the driver reads it); `slideStrings` had its own walk without the alt exclusion (it reads `blockStrings` now). |
| re-drive 5 (logos, assist)                                     | 39 of 40 green: `paper-and-ink` ink half lit, paper half lit with no white, the light deck's mark lit; `logo-size` centred in the paragraph's column; `every-slide` green; `assist.tailor.dialog-one-undo` "4 places on 4 slides"; left red: `licence-words`, the foot without the date at the open and the Recents group's Figma read as the preselected tile.                                                                                                                                                                                                                                                                     | `areas/logos.mjs`: the foot read after the search (the date rides with the index facts the results bring); the results group's Figma, not Recents'.                                                                                                                                                                                                                                                                                                                                     |
| re-drive 6 (logos)                                             | 31 of 31 green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| specs re-runs 1 to 3 (logos, sync, export, images)             | The logos and images files green from re-run 2 (the group read, the asset record from `describe().state.assets`); `export.print.download-pdf-follows-preview` red in all three (data-count 3 after the Include skipped slides click); `sync.structural.concurrent` red in all three (no card); `sync.block.offline-replay-converges` red once at its teardown.                                                                                                                                                                                                                                                                      | `routes/print.$deckId.tsx` announces hydration and `export.spec.ts` waits for it; `controller.tsx` shows a local refusal of a write whose slide is gone as the loser's card (`publishLocalRefusal`).                                                                                                                                                                                                                                                                                    |
| specs re-run 4 (export, sync)                                  | `export.print.download-pdf-follows-preview` green (and the whole export file); `sync.structural.concurrent` still no card: the hand probe of 5.3 read that A's release after the deletion reached A writes nothing (the gesture is cancelled with the slide), so no refusal, local or the room's, exists to show.                                                                                                                                                                                                                                                                                                                   | `sync.spec.ts`: A offline from the hold to the release, so its write is queued and meets the deleted slide at the room on the reconnect.                                                                                                                                                                                                                                                                                                                                                |
| specs re-runs 5 and 6 (sync)                                   | `sync.structural.concurrent` green ("A change was not applied / No slide "split-1"" with the mutation's details, the slide gone in both within 548 ms); `sync.block.offline-replay-converges` red at its teardown in both: its own checks green (converged 3.9 s after the reconnect, the splice offset 18 then 30), the scratch deck answering `/edit` 200 after Move to trash and Delete forever.                                                                                                                                                                                                                                 | The trace read: Move to trash at revision 5 (20:12:36.77Z), the room's checkpoint of the replayed op at 37.67Z moved the manifest to revision 6 (the trash stamp kept), the trash page's Delete forever carried 5 and was refused "baseRevision 5 is stale; … Reload the page and try again" while the page had hidden the card already; `core/lib.ts` `teardown` removes such a deck through `deck.remove` with a fresh revision; `file-store.test.ts` pins the stamp across a write.  |
| specs re-run 7 (sync)                                          | `sync.block.offline-replay-converges` red at its teardown once more: the fallback's `deck.remove` through an editor opened on the trashed deck never reached the store (the trace shows the editor's loads and no action POST), so the 404 did not come.                                                                                                                                                                                                                                                                                                                                                                            | `core/lib.ts` `teardown`: the trash page is listed again first (its second listing carries the moved revision), the action with a fresh revision after that.                                                                                                                                                                                                                                                                                                                            |
| specs re-run 8 (sync)                                          | 9 passed, 1 not driven (`sync.resend.idempotent`, standing: the driver's route abort is the fix round's); the file green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

#### 5.3 The hand probes behind the fixes

Four probes on 4418 (playwright-core over the interface audit's helpers, one scratch deck each from `/new`, trashed and deleted forever through the window API with the 404 read back), kept beside the run logs in the scratch folder and not committed:

- The Vercel tile: the ink half (`#070707`) drew `/api/logo/mark/vercel/light.svg`, whose one path fills `#000`, and the paper half (`#ffffff`) drew `dark.svg`, fill `#fff`; the inserted mark on the light deck was the `dark` twin at 554 by 480, 4.36 percent lit on the dark deck and 0 on the light. thesvg.org's own files (read through the site and the jsDelivr mirror on 2026-09-22) agree with the fixture: `vercel/light.svg` `#000`, `dark.svg` `#fff`, `github/light.svg` `#1b1f23`, `dark.svg` `#ffffff`, so thesvg names a variant after the ground it is drawn for and the appearance rule had the names as ink colours (FEATURES.md 4.4's twin sentence said the same; corrected).
- The structural race by hand (two contexts, B's Delete slide while A holds the block): at the release A's slide list had lost the slide and its stage the block; no ops POST left A, no reject notice, the snackbar once read `No slide "split-1"` from a refused `slide.update` and once nothing. A `slide.update` through the window API against the gone slide drew the card ("A change was not applied / No slide "split-1"" with Object changed and the mutation's JSON): the local refusal path shows the card; the gesture path never reaches it.
- The third probe (the dialog's halves and the light deck's mark, measured the driver's way with the toolkit's PNG decoder) gave the 0 percent readings their mechanism before the rule was turned.

#### 5.4 The memory tier, run 2 (the committed tree at `44c2cc0`, 4418)

The run of record on the memory tier, after the fixes commit and a restart of the no watch dev server (its private tmp store emptied and seeded again): `node scripts/probes/core-gate.mjs --base http://localhost:4418 --lock <scratch> --out <scratch>/gate-4418 --matrix <scratch>/gate-4418/ledger.json --parked docs/gslides-parity/focus/ship-39ae62e.json`, 20:35:47Z to 22:06:29Z (5,441 s; the load average 16 at the start, the preview deploys compressing beside it, 12 at the end): 785 rows, 760 passed, 8 failed, 17 not driven (1 manual), 0 no step, retries zero, cost rows over their ceiling 0; verdict failed. The ledger copy is `build/integrator/gate-4418-run2-core-gate.json` with its `core-matrix.md`. Of the 71 ship one rows: 56 passed, 3 failed, 12 not driven (run 1: 41, 19, 11). The cost rows: editor idle 10 function requests a minute (ceiling 12; run 1 the same), editing 64.31 (ceiling 75), the show 0 (ceiling 0), two idle tabs no list call; the store half reads zero on the file store, as in run 1, so the function requests alone are asserted and no cost row rose.

The 8 reds by mechanism:

| Rows                                                                                                                                                                | Mechanism                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Owner and what stands                                                                                                                        |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| `view.live-pointers.second-browser`, `versions.show-changes-marks`, `inbox.bell-panel-toggle`, `templates.gallery.page` (and `templates.deck.read-only` not driven) | The previous ship's parked set (`ship-7ea3db9.json`), still behind the menu model's `advanced` flags; this ship's empty list pre forgives nothing, so they read as reds, the same four as run 1, with the same observed lines (the bell panel stays open after the second click; the older version's marks 0; the second pointer not drawn within 10 s; the gallery's locator 6 s).                                                                                                                                                                                                                                             | Standing; the ship step's list renders them from the runs' `wouldParkRows` as before.                                                        |
| `tables.paste.tsv-makes-table`, `tables.paste.into-cell-spreads`                                                                                                    | FEATURES.md 2.3 item 5 (P1) is not landed (b3.md section 5): the paste makes a text box, a paste into a cell lands in the one cell; the same reading as run 1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | B3, the P1 item; the rows say so.                                                                                                            |
| `slides.delete.two-selected-key-undo`                                                                                                                               | Two cards, the Delete key, the snackbar's Undo brought one back (13 to 11 to 12), the same two cards and the same numbers as run 1, while the slides area re-driven alone read 13 to 11 to 13 twice; the key path fires the two removals without awaiting them and its Undo calls `undo()` twice in one loop (finding 17).                                                                                                                                                                                                                                                                                                      | B1 (the filmstrip); one hunk for the fix round; a standing row, not ship one's.                                                              |
| `logos.insert.every-slide` (ship one, P0)                                                                                                                           | Every fact of the row read true but one: after the insert with the check on, "title slot draws the mark false" while the new slide's footer drew it, the snackbar named the mark with Undo and Cmd+Z took the insert back in one step (512 to 514 to 515). The logos area re-driven alone on the same tree read the title slot true twice (re-drives 5 and 6). The run's title slide has 500 rows behind it; the driver reads the mark as an `img` in the sheet's mark slot, and on that slide none was drawn. Not explained by a hunk of this ship; the two re-drives and the preview runs (5.5) are the row's other readings. | B1 and B6 to read the title slide's mark after the walk's earlier rows (the brand rows toggle the mark and the kit); the row is green alone. |

The 17 not driven: the 11 of ship one (B3's P1 controls: the four table handles and bars, the group tail, the word art tail; `brand.objects.kit-colours-first`; the specimen rows; `fonts.table.takes-family`; `logos.picker.variants`; `logos.kit.find-a-logo`), `logos.index.cached-offline` (the fixture upstream on this base; driven on the outage preview, 5.5), and five standing (the manual paste row, the template's read only row, `assist.quota.429`, `sync.resend.idempotent`, `cost.editor-hidden.calls`), each with its reason in the ledger. Against run 1: `sync.structural.concurrent`, `sync.block.offline-replay-converges`, `export.print.download-pdf-follows-preview`, the eleven logos rows, `assist.tailor.dialog-one-undo` and the agent tailor row, `tables.range.size-color`, `charts.panel.no-duplicate-controls`, `brand.objects.kit-colours-first` (not driven now, the P1 item), `fonts.catalog.six-families` and `logos.picker.chrome-1280` moved to green; nothing green in run 1 moved to red but `logos.insert.every-slide`.

#### 5.5 The enforce preview, runs 1 and 2, and the outage preview

The deployment of record (`https://turboslide-e4sw7h7ay-kl01s-projects.vercel.app`, the fixture upstream, the blob tier), `core-gate.mjs --base <preview> --out <scratch>/preview/gate-<n> --matrix … --parked docs/gslides-parity/focus/ship-39ae62e.json` with the OIDC header and the agent bearer in the environment, twice in a row with no code change between the runs (the working tree at `44c2cc0` plus the two files section 8 names, unchanged from before run 1 to after run 2):

| Run                             | Window (UTC)                   | Rows                       | Passed   | Failed   | Not driven    | Cost rows over                                                                                                 | Ship one rows                                                                          |
| ------------------------------- | ------------------------------ | -------------------------- | -------- | -------- | ------------- | -------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| 1                               | 22:09:51 to 00:21:01 (7,870 s) | 785                        | 750      | 18       | 17 (1 manual) | 1 (`cost.two-tabs-idle.calls`, store list 1 against a ceiling of 0, the same reading as the sync round's ship) | 52 passed, 7 failed, 12 not driven                                                     |
| 2, the walk                     | 00:21:01 to 01:11:12 (3,011 s) | 572 probe rows             | 556      | 5        | 11            | (the cost probe runs after the specs)                                                                          | the walk's ship one rows: the same as run 1 but `logos.picker.search`, green at 281 ms |
| 2, the specs and the cost probe | from 01:11:30                  | 206 spec rows, 6 cost rows | not read | not read | not read      | not read                                                                                                       | not read                                                                               |

Run 1's walk alone: 572 probe rows, 555 passed, 6 failed, 11 not driven (3,112 s); run 2's walk differs from it in one row, `logos.picker.search` (red at 319 ms, green at 281 ms), which is flaky under 7.2 with the driver's bound as the mechanism (finding 19). Run 2's specs phase and cost probe are not readings: the preview's OIDC credential in `.turboslide/vercel-dev.env` (`VERCEL_OIDC_TOKEN`, issued 2026-09-22T13:11:25Z, twelve hours) expired at 01:11:25Z, thirteen seconds after the walk's last row, and every request from then on met the deployment protection's 302 to the sign in page (`/home` with the header 302 at 01:32Z; the first spec red at 01:14Z, the images file's uploads all red by 01:31Z). The run was stopped at 01:34:14Z at the images file (exit 143) so the chain could go on to the full check; its walk ledger is `build/integrator/preview-run2-core-walk.json` with its `core-walk.md`. A fresh credential is Kevin's (`vercel env pull`, an env command this pipeline never runs), and the specs phase of run 2 with the cost probe is the one reading this ship still owes: `<with the env> node scripts/probes/core-gate.mjs --base https://turboslide-e4sw7h7ay-kl01s-projects.vercel.app --out <scratch>/preview/gate-2b --matrix … --parked docs/gslides-parity/focus/ship-39ae62e.json --only specs,cost` on the tree of the note's commit. The ledgers: `build/integrator/preview-run1-core-gate.json` with its `core-matrix.md`. Retries zero in both walks.

The reds of run 1 by mechanism, with run 2's reading beside each where run 2 read the row (the walk's rows; a spec row's second reading is owed, above). 7.2: a row red in one run and green in the other is flaky and named as such.

| Rows                                                                                                                                                                                                                                                                                         | Run 1                                                                                  | Run 2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Mechanism                                                                                                                                                                                                                                                                                                                                     |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `view.live-pointers.second-browser`, `versions.show-changes-marks`, `inbox.bell-panel-toggle`, `inbox.settings-persist`, `inbox.notification-arrives`, `templates.gallery.page`, `templates.card.rename-and-delete`, `templates.default.use-for-new` (`templates.deck.read-only` not driven) | red                                                                                    | the walk's three (`versions.show-changes-marks`, the two inbox rows) red again; the spec rows not read                                                                                                                                                                                                                                                                                                                                                                                 | The previous ship's parked set (`ship-7ea3db9.json`: the inbox and templates features, two of the three parked rows); this ship's empty list pre forgives nothing. The three inbox rows and the three templates rows read red on the blob tier alone (green or not driven on the memory tier), as in the sync round's preview runs. Standing. |
| `tables.paste.tsv-makes-table`, `tables.paste.into-cell-spreads`                                                                                                                                                                                                                             | red                                                                                    | not read (spec rows)                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | FEATURES.md 2.3 item 5 (P1) not landed; the same reading on every base.                                                                                                                                                                                                                                                                       |
| `logos.insert.every-slide` (ship one, P0)                                                                                                                                                                                                                                                    | red, the same observed line (954 to 956 to 957)                                        | "title slot draws the mark false" with every other fact true (956 to 958 to 959 on Cmd+Z), the same reading as the memory tier's run 2 (5.4): after the walk's brand rows, whose `brand.logo.none` row leaves the kit's mark at none and the title slide without its mark, the every slide insert draws in the new slide's footer and not in the title slot; green when the logos area runs alone (re-drives 5 and 6). Finding 18.                                                     |
| `logos.picker.search` (ship one, P0)                                                                                                                                                                                                                                                         | green at 281 ms: flaky under 7.2, the bound the mechanism                              | Every fact true but the time: the results 319 ms after the last key against the driver's 300 ms bound (161 ms on 4418); the bound is FEATURES.md 4.3's "results 300 ms after the pause" read from the last key, so the preview's round trip sits inside it. The row's claim holds (one result, Figma preselected, Enter inserted at the logo size, the dialog closed); the bound is the driver's to loosen for a remote base (finding 19).                                             |
| `logos.insert.mono-tint` (ship one, P0)                                                                                                                                                                                                                                                      | red, the same 302                                                                      | The mark inserted as its tinted mono (variant mono, the twin 48.92 percent lit on ink) and AWS unmodified; the driver read the two source files' fills through `GET /decks/<id>/<sourceFile>` with `maxRedirects: 0`, which the blob tier answers 302 (the file lives on the public store), so both fill lists read empty. The driver's to follow the redirect (finding 19); the row's claim is not contradicted.                                                                      |
| `logos.index.refresh-fixture` (ship one, P0)                                                                                                                                                                                                                                                 | not read                                                                               | The refresh dropped `northwind` (9 icons, the second refresh of the fixture) and wrote the shared index, but the search right after still listed it: the instance that answered the search holds its index for up to an hour before it reads the store's version (`server/logos.ts` `INDEX_REVALIDATE_MS`), so on a deployment of several instances a refresh is adopted by the other instances late (finding 15's neighbour; finding 20). The memory tier's one instance reads green. |
| `logos.agent.search-insert` (ship one, P0)                                                                                                                                                                                                                                                   | not read                                                                               | The CLI half and the HTTP half passed; the MCP half's `tools/call` answered 404 "Unknown MCP session" (finding 16): the session is one instance's.                                                                                                                                                                                                                                                                                                                                     |
| `brand.logo.replace-every-slide`                                                                                                                                                                                                                                                             | not read                                                                               | Replace with a PNG drew the footer on every slide and the title slot, and a new slide carried it; Position > Top right then left the title slot's mark unread within 8 s (x 0 against the 553 it had), on the blob tier alone (green on 4418 in both runs). Finding 21.                                                                                                                                                                                                                |
| `versions.restore`                                                                                                                                                                                                                                                                           | not read; the sync round's preview read it green, so one red reading stands against it | Restore this version did not return the deck to the version within 35 s on the blob tier (the slide read the current text, not the second browser's), green on 4418; the sync round's preview read this row green. Run 2 says whether it is flaky.                                                                                                                                                                                                                                     |

The cost rows on the preview against the sync round's ship (`gate-preview-sync-ship.json`): editor idle 9.65 function requests a minute (9.66 then), editing 68.92 (68.93 then; the store's simple calls 83 a minute against 149 then, under the 140 ceiling now), the show 0 (0), two idle tabs advanced 19 a minute (19) with one `assets/` listing in the window on the second of two instances (the same one listing the sync ship read; a cold instance's first open lists the folder), `sync.pull.no-listing` no versions listing. Nothing rose; one row stays over its ceiling as it was.

The outage preview (`TURBOSLIDE_LOGO_UPSTREAM=down`, `https://turboslide-ccb5xs0g7-kl01s-projects.vercel.app`): `core-gate.mjs --only specs --spec logos` three times, 20:44Z to 20:50Z (`preview/down-specs-1`, `-2`, the third the folder of record), for `logos.index.cached-offline` alone; the file's other rows read there are not readings of record (the deployment answers no upstream, so `logos.cache.open-licence-only`'s AWS insert reads "thesvg.org did not answer" by design, and `logos.index.refresh-fixture` reads not driven, as the spec says for an upstream that is not the fixture).

| Pass | `logos.index.cached-offline`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1    | red at the foot: `logo.search` answered from the shared index (200, Figma, `lastError` recorded at 20:44:03Z), but the dialog's foot at the open read "Logos from thesvg.org. Brand marks belong to their owners…" without the date or the failure: the foot carries the index facts once a search has brought them (the same reading as the licence words driver's, 5.2). The spec reads the foot after a search now.                                                                                                                                                                                                                                                                          |
| 2    | red at the foot: "Logos from thesvg.org, updated 22 September 2026. Brand marks belong to their owners…", the date and no failure sentence, while the spec's own HTTP search on the bearer read `lastError` at 20:46:43Z: the failure an instance saw is held in that instance's memory (`server/logos.ts` `memoryError`, "the failure the instance saw since the index was written") and the stored index carries `lastError` only from a failed refresh, so on a deployment of several instances the browser's search can land on one that has not failed yet (finding 15). The spec drives the cached insert before the foot's read, so the row's core claim is read whatever the foot says. |
| 3    | green: the search from the cache (200, Figma, `lastError` at 20:49:27Z), the cached Figma inserted through the window API during the outage (`figma-2`, the neutral twin), the foot "Logos from thesvg.org, updated 22 September 2026. Brand marks belong to their owners; use them to name the brand, not to imply endorsement. thesvg.org did not answer at 20:49 UTC".                                                                                                                                                                                                                                                                                                                       |

`logos.agent.search-insert` on the outage preview read the MCP half red on every pass: `initialize` answered a session and `tools/call` answered 404 "Unknown MCP session <id>; initialize again" (finding 16); the CLI half and the HTTP half passed.

### 6. The findings by owner

What the runs and the probes read that a lane or the ship step owns, with the evidence and what this integrator did about it. A row named here is green on the tree unless the line says otherwise.

B6 (logos and the server routes)

1. The appearance rule read thesvg's variant names as ink colours (`chooseVariant`: on a dark deck `light`, on a light deck `dark`; `pairedVariant` and the twin sentence of FEATURES.md 4.4 the same way). thesvg.org names a variant after the ground it is drawn for: `vercel/light.svg` fills `#000`, `dark.svg` `#fff`, `github/light.svg` `#1b1f23`, `dark.svg` `#ffffff` (the site and the jsDelivr mirror on 2026-09-22; the fixture carries the files verbatim). Every tile half drew the file invisible on its ground and a light deck stored the white file as its light twin (`logos.picker.paper-and-ink`: 0 percent lit in every run). Turned in `logo-model.ts`, `server/logos.ts`, their tests and `logo-dialog.test.tsx`; FEATURES.md 4.4 corrected; b6.md section 5's line "GitHub on a dark deck inserts its `light` file" now reads the `dark` file in practice.
2. The dialog's insert placed twice when the editor's handle carried `insertLogoAsset` (5.1); fixed in `Logo.tsx`, pinned in `logo-dialog.test.tsx`.
3. `source.read` answers the active slide's source, which carries no asset record; `describe().state.assets` now carries the deck's assets so an agent or a driver reads a placed logo's record (the CLI's `logo insert --json` answers it already).

B1 (the chrome, the pickers, and the sync round's room and client)

4. The kit's `deck.set /brand` writes could not ride in a `slide.update` (`actionForMutations` and `checkSlideMutations` refused "every mutation must name the same slide"), so the every slide insert was two writes and two undo steps; both admit the kit write beside the slide's mutations.
5. `slideStrings` walked the blocks on its own and counted a picture's alt while the rename never writes one (the Tailor count said 5 places on 4 slides against 4 renamed once the logos area left an Acme picture); it reads `blockStrings` now.
6. `sync.structural.concurrent`: a release after the deletion reached A writes nothing (the gesture is cancelled with the slide; the sync round's two ships read the same) and a local refusal of a write whose slide is gone was a sentence in `error` with no card. The controller shows the card for the local refusal (`publishLocalRefusal`) and the spec holds A offline from the hold to the release so its write meets the deleted slide at the room; green in the sync file's re-runs 5 to 8. The offline form is the deterministic reading of the row; a second, timing form (A releases before the deletion reaches it) is the one production sees and is not driven.
7. The trash page's Delete forever carries the revision its listing read and hides the card before the answer; a room checkpoint landing between the listing and the click (2 s after the last op) moves the revision and the removal is refused as stale ("baseRevision 5 is stale; <id> is at revision 6. Reload the page and try again"), the card comes back and the deck stays in the trash. Read in four re-runs of the sync file at `sync.block.offline-replay-converges`'s teardown (its own checks green), with the trace of each; the shared teardown (`core/lib.ts`) now lists the trash page again and removes with the fresh revision, and `file-store.test.ts` pins that the checkpoint's write keeps the trash stamp (it does). A `Delete forever` that rebases once on the stale answer, as the editor's `invokeRebasing` does, is one hunk for the fix round; until then a seller who deletes right after trashing a freshly edited deck reads the reload sentence. The blob tier read the same: the write probe's deck on the deployment of record (`untitled-20260922-xeoh`, twelve edits, Move to trash at 20:41:04Z, Delete forever right after) still answered `/edit` 200 on production at 01:40Z, trashed at revision 15 and not removed, while the probe's own step read "Delete forever removed the deck from the trash" from the page; removed through `deck.remove` with its current revision at 01:41Z (section 8).

B7 (the server and the exports)

8. The print page is server rendered and had no hydration mark; a click on Include skipped slides before React attached its handler toggled nothing (`export.print.download-pdf-follows-preview` red in three re-runs of the file alone, green in the loaded full run). The page announces `data-hydrated` like the home page and the spec waits for it.
9. The `sharp` dependency line (b6 R8) and the `packages` asset group's size stay the ship step's (section 8).

B3 (the objects editing model)

10. The refusal card's gate (rank 8) took the reducer's refusal of a write whose slide a collaborator deleted for a chrome control's refusal and dismissed it; the gate holds only for the schema's pointer message on objects still on the document (`isControlRefusal`, `refusal-gate.test.ts`).
11. The Upload from computer chooser accepted `image/*`; it lists the four raster types now (FEATURES.md 4.7).

B4 (the matrix and the drivers)

12. The driver corrections of 5.1 and 5.2, each with its mechanism in the driver's comment: the tile halves' ground and scroll, the lit fraction's margin ring, the placement area (the empty paragraph's column, picture-place.ts), the typographic apostrophe, the preselected tile waited for, the foot read after the search, the AWS tiles polled for, the Recents group left out of the results read, the every slide row's slide made before the insert, the Wikimedia file address, the typed table's rung, the chart labels counted once, the kit plate rule as not built for the chart, Google's font categories, the count's case and key exclusions, the group read in the chrome spec, the flatten mode's control, the asset record from `describe().state`.
13. `logos.insert.mono-tint` names GitHub's mono, which the appearance rule never reaches on the fixture (GitHub has its pair); the row and the driver read Stripe's mono asked for through the window API, the note says why.

B2 (text and fonts)

14. Bricolage Grotesque and Fraunces sit under Google's categories (Sans serif, Serif), not both under Display as FEATURES.md 3.2 wrote; the row reads the catalog's rule and question 7 of FEATURES.md 9 is the ship step's to settle.

B6 and B7, read on the previews

15. The upstream failure the foot names is per instance: `server/logos.ts` holds `memoryError` for "the failure the instance saw since the index was written" and the shared index carries `lastError` only when a refresh failed, so on a deployment of several instances a search that lands on an instance that has not fetched yet reads no failure while another instance's does (`logos.index.cached-offline` on the outage preview, pass 2 of 5.5). One hunk for the fix round: a failed mark fetch records `lastError` on the shared index (throttled like `persistSoon`), or the foot reads it from the refresh route's answer.
16. The MCP HTTP handler keeps its sessions in one instance's memory (`packages/mcp/src/http.ts` `createMcpHttpHandler`, `entries = new Map`), so on a deployment of several instances an `initialize` on one and a `tools/call` on another answers 404 "Unknown MCP session; initialize again" (`logos.agent.search-insert`'s MCP half on the outage preview, every pass; the fixture preview's runs are read in 5.5). The studio's own sessions read one shared table (`sessions.ts` `redisSessionDirectory`); the MCP sessions want the same, or a stateless call.

Read on the deployment of record (5.5)

18. `logos.insert.every-slide` in a full run: the walk's brand area runs before the logos area and its `brand.logo.none` row leaves the kit's mark at none and the title slide without a drawn mark; the every slide insert then sets the kit's mark and footer, the new slide's footer draws it and the title slot does not (the memory tier's run 2 and the preview's run 1 read the same; the row is green when the logos area runs alone, re-drives 5 and 6). Whether the title slot should draw the kit's new mark after a "no logo" choice is B6's and B1's to say (FEATURES.md 4.4 says "on the title slide and in every footer"); until then the driver wants the kit's mark shown as its precondition (B4).
19. Two driver bounds read against a remote base (B4): `logos.picker.search` reads the results' time from the last key with a 300 ms bound, which the preview's round trip crosses (319 ms; the dialog's own pause is 300 ms), and `logos.insert.mono-tint` reads the source files with `maxRedirects: 0`, which the blob tier answers 302. Neither reading contradicts the row's claim; both are one line each, not changed between the two runs.
20. The held index revalidates at most once an hour per instance (`server/logos.ts` `INDEX_REVALIDATE_MS`), so a refresh that dropped a slug on the shared store is still answered with the slug by another instance's search until its hour passes (`logos.index.refresh-fixture` on the preview). One hunk for B6: a refresh answer that carries the store's version, and a search that reads the version when the answering instance is not the one that refreshed, or a shorter revalidation.
21. `brand.logo.replace-every-slide` on the blob tier: Replace with a PNG drew the footer on every slide and the title slot within 5 s, a new slide carried it, and Position > Top right then left the title slot's mark unread for 8 s (the preview's run 1; green on 4418 in both runs; run 2's reading in 5.5). B1 (the Brand kit panel) and B7 (the blob tier's asset answer) to read together: the position write moves the kit's mark and the title re-renders it from the twin the store serves.

22. `core/decks.spec.ts` leaves four decks per run on the store it runs against: the rows that import a bundle, copy a deck without notes and render a thumbnail deck create decks the file's `Scratch` does not register, so `teardownAll` never sees them (run 1 on the preview left `import-bundle-source-mudann3m`, `northwind-renewal-v4-without-notes`, `untitled-20260922-tgeh` and `untitled-20260922-hww9`, removed by hand, section 8). B4: register every deck a row creates, the copies and the imports included.

The standing row read red in both memory tier runs

17. `slides.delete.two-selected-key-undo` (the focus round's rank 22 family): two cards selected, the Delete key, the snackbar's Undo brought one of the two back (13 to 11 to 12) in run 1 and run 2 alike, the same two cards (`title-2`, `title-2-2-2`) both times, while the slides area re-driven alone read 13 to 11 to 13 twice. The key path (`Filmstrip.tsx` `removeSlides`) fires one `slide.remove` per card without awaiting them and the snackbar's Undo calls `undo()` once per card in one synchronous loop; the menu path (`EditorShell.tsx`, the batch plan) awaits each write before the snackbar and its two menu rows pass in every run. The reading is that under the full run's load the second write's entry is not yet on the history when the second `undo()` runs, so one undo is a no-op; awaiting the removals in order before the snackbar, as the menu path does, is one hunk for the fix round (B1's file; not changed after run 2 so the run of record is the tree's). Not a ship one row.

Kevin's rows, not chased: `sync.title.concurrent-both-kept` and `decks.file.open-list-search`.

### 7. The pictures

`docs/gslides-parity/features/build/integrator/shoot.mjs` (playwright-core at human speed over the interface audit's helpers, one scratch deck from `/new` trashed and deleted forever through the window API, the 404 read back; two runs, the first without the fonts pictures on a locator that matched two rows, the second whole) against the 4418 server under `.turboslide/e2e.lock`, in both appearances at 1440 by 900 and 1280 by 800, 45 files beside this note:

- `insert-menu-<appearance>-<width>.png`: the Insert menu with Logo directly under Image.
- `logo-dialog-*.png`: Insert > Logo with "figma" typed, Your brand (the GT mark on paper and on ink), the Figma tile preselected, the check with its sentence, the licence row, the foot with the index's date; `logo-inserted-light-1440.png` the mark placed at the logo size after Enter.
- `replace-image-logo-*.png`: the placed picture's right click menu with Replace image open and its Logo row.
- `tailor-find-logo-*.png`: Tools > Tailor for a customer with "Figma" in With and the Find the Figma logo button beside the mark pair.
- `format-options-tabular-*.png`: Format options on the title with the Tabular figures row and its sentence (the panel's 336 px).
- `table-caret-*.png`: a 4 by 4 table after one click on a cell: the caret in the cell, the chip Table, the frame's handles.
- `table-format-options-*.png`: the Table section first in Format options with one Height field and Distribute rows.
- `chart-edit-data-*.png`: a bar chart selected with the Edit data button under its ring; `chart-grid-*.png`: the Chart data section with the swatches and the active line's remove controls.
- `font-dropdown-*.png`: the Font dropdown with the Recent group (Geist) and Clear recent after Used; `more-fonts-*.png`: More fonts with the eight families and their licence links.

The pictures were read by this integrator (the Logo dialog, the table caret, the chart's Edit data button, Tailor's button, the Font dropdown, More fonts); the verifier's hand drive is the ship step's.

The pictures were shot on the merged tree at `11dbb5a`, before the fixes commit. The surfaces they show are unchanged by `39ae62e` but for the Vercel and GitHub tile halves (finding 1), which no picture holds (the dialog picture searches Figma, whose default draws on both grounds), and the print page's hydration mark, which draws nothing.

### 8. Deviations, and what the ship step still owes

- One commit per lane, not one commit per item: FEATURES.md section 6 asks for one commit per item; this pipeline honours it as one commit per lane with the items and their hunks named in the message body (section 1), so the fix round reverts an item by hand from the named hunks.
- Shared files ride in the first lane's commit of the merge order with the other lane's hunk named (section 1); `git blame` on `Editor.tsx` and `Selection.tsx` reads B3 for B6's lines.
- Two more commits after the day 0 integration: the seams the memory tier gate opened (`39ae62e`, section 5), and the parked list renamed after it (`44c2cc0`). The parked list stays empty; its name follows the commit whose tree the runs of record and the preview read.
- `insert.logo`'s icon is `tag`, not b1's `building-office-2` (not in the icon set); `terms` is an array on every row (b1 wrote strings, which the finder ignores).
- The appearance rule and the twin mapping were turned (finding 1), and FEATURES.md 4.4's twin sentence with them, one phrase: the document described the implementation's reading of thesvg's names, which the fixture and the site contradict.
- `sharp` is not added to `apps/studio/package.json` (b6 R8): the lockfile would move and this pipeline runs no `pnpm install`; the preview's build and routes prove the hoisted copy resolves.
- The P1 items not landed stay not landed: b2 R5 (a table takes a family; `fonts.table.takes-family` not driven), B1's specimen rows and variant controls, B6's kit Find a logo, B3's items 1 to 6 and 10. The P1 items the lanes landed as separable hunks (B3's 7, 8, 9; B1's 2.5; B2's 1.7 and 1.8; B7's URL sentence and svg branch) stay on the tree: no lane had a preview run of its own before this merge, so the P1 condition of FEATURES.md section 1 could not be read as written, and each is one hunk set that reverts alone if its row reads red (section 5 says which did).
- `apps/studio/src/editor/refusal-gate.test.ts` is named by `39ae62e`'s message as new but rides in the note's commit: the fixes commit was staged with `git add -u`, which takes tracked files alone, and the untracked test was noticed at the whole suite run (it ran there and passed). The `core/logos.spec.ts` read order of `logos.index.cached-offline` (5.5) was edited after `44c2cc0` and rides in the note's commit too; the two preview runs read the spec in that state, unchanged between them, and the deployed code is `44c2cc0`'s throughout.
- `node scripts/check.mjs` runs `pnpm install --frozen-lockfile` as its own step 1; this pipeline runs no `pnpm install` of its own, and the check's frozen install left `pnpm-lock.yaml` and `node_modules` as they were (`git status` clean of both after). Its step 19 (`pnpm format:check` over the tree) refused the gate's markdown ledgers copied into `build/integrator/`, machine written by `core-gate.mjs` and `editor-walk-probe.mjs`; `.prettierignore` gained two lines for `*-core-matrix.md` and `*-core-walk.md` under `features/build/*/`, beside the round's JSON line, and the check ran again from step 19.
- The sync round's spec and its shared teardown were edited (finding 6 and 7): the structural race holds A offline so the race is deterministic, and a deck the trash page's stale delete left is removed with a fresh revision. Both are driver changes with the mechanism in the comment; neither changes what the product does.
- The ship's parked list is empty by the task's instruction, so the gate runs pre forgive nothing: the previous ship's parked set (`ship-7ea3db9.json`: inbox and templates, three parked rows) still holds in the menu model's `advanced` flags and its rows are read in section 5 against that list, marked standing, never as this ship's reds.
- The shared Blob store: the fixture preview builds its ten mark index into `system/logo-index.json` and caches the open licence fixture marks under `system/logos/` of the one public store production shares (the store the preview and production share is the reason every scratch deck is deleted forever). Production at `88b68e7` reads neither key; the ship step's production deploy must run `POST /api/logo/refresh` with the bearer (the network upstream) before the production run, which rebuilds the index from thesvg.org and drops the fixture's slugs with their cached files at that refresh (the task's instruction on the store).
- The `packages` server asset group's size (b7's note, "recorded on the first preview deploy") is not printed by `vercel deploy`; the deploy's whole upload was 334.5 MB and the build 43 s; check step 30's local Vercel build prints the function directory sizes (section 3 once the full chain has run).
- `logo-coverage.mjs` against production (FEATURES.md 4.1, b4's request) is the ship step's after the production refresh; against the fixture preview the list would read the ten mark fixture, so it was not run here.
- Three scratch decks of the sync file's re-runs (the offline replay row's, before the teardown fix) stayed trashed on the 4418 server's private tmp store until the restart before run 2 emptied it.
- The shared Blob store was listed through production's `deck.list` with the trash included after the runs (01:39Z): six decks of this integrator's runs were still there and were trashed and deleted forever through `deck.trash` and `deck.remove` with their current revisions at 01:41Z, each answering `/edit` 404 after: the write probe's `untitled-20260922-xeoh` (finding 7's blob tier reading), four decks of run 1's `decks.spec.ts` rows that the file's scratch list does not track (`untitled-20260922-hww9` "Thumbnail deck", `untitled-20260922-tgeh` "Northwind renewal", `import-bundle-source-mudann3m`, `northwind-renewal-v4-without-notes`, 23:16Z to 23:19Z; finding 22 for B4), and `untitled-20260923-5hid` "Assist spec deck", made by run 2's first spec at 01:11:15Z, ten seconds before the credential expired. The store's 70 older decks (2026-09-10 to 2026-09-20) are earlier rounds' and Kevin's and were left alone. No deck of this ship's runs is left on any deployment.

### 9. The run ledger

Every run of this ship in the order it happened (times UTC, 2026-09-22 unless the time says 23), with its tree, its base and where its ledger is. The scratch folder's logs (`redrive-*.log`, `respecs-*.log`, `gate-4418-run2.log`, `preview-run-*.log`, `check-full.log`, `check-from-19.log`) are not committed.

| Run                                     | Tree                                      | Base                         | Window                                                                                                                                                                                                                                                                                                                                                     | Result                                                                                                                                        | Ledger                                                                  |
| --------------------------------------- | ----------------------------------------- | ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| the core gate, run 1                    | `11dbb5a`                                 | 4418, memory tier            | 15:47 to 17:24 (5,831 s)                                                                                                                                                                                                                                                                                                                                   | 785 rows: 741 passed, 28 failed, 16 not driven                                                                                                | `gate-4418-run1-core-gate.json`, `-core-matrix.md`                      |
| re-drives 1 to 6                        | the working tree of `39ae62e` as it grew  | 4418                         | 17:30 to 19:55                                                                                                                                                                                                                                                                                                                                             | the logos and assist areas green by re-drive 6 (5.2)                                                                                          | scratch                                                                 |
| specs re-runs 1 to 8                    | the same                                  | 4418                         | 17:40 to 20:28                                                                                                                                                                                                                                                                                                                                             | the logos, images, export and sync files green by re-run 8 (5.2)                                                                              | scratch                                                                 |
| the whole vitest suite                  | `44c2cc0`                                 |                              | 22:04 to 22:09                                                                                                                                                                                                                                                                                                                                             | 383 files, 4,144 passed, 3 skipped, 2 todo                                                                                                    | scratch                                                                 |
| the enforce preview, fixture upstream   | `44c2cc0`                                 | Vercel                       | deployed 20:35 to 20:39                                                                                                                                                                                                                                                                                                                                    | `https://turboslide-e4sw7h7ay-kl01s-projects.vercel.app`                                                                                      |                                                                         |
| the enforce preview, outage upstream    | `44c2cc0`                                 | Vercel                       | deployed 20:39 to 20:43                                                                                                                                                                                                                                                                                                                                    | `https://turboslide-ccb5xs0g7-kl01s-projects.vercel.app`                                                                                      |                                                                         |
| the hosted smoke                        |                                           | the fixture preview          | 20:39                                                                                                                                                                                                                                                                                                                                                      | 40 of 40                                                                                                                                      | scratch                                                                 |
| the new write probe                     |                                           | the fixture preview          | 20:39 to 20:41                                                                                                                                                                                                                                                                                                                                             | every step ok; its deck removed by hand at 01:41 (section 8)                                                                                  | scratch                                                                 |
| the logos file, three passes            | `44c2cc0` plus the spec edit              | the outage preview           | 20:44 to 20:50                                                                                                                                                                                                                                                                                                                                             | `logos.index.cached-offline` red, red, green (5.5)                                                                                            | `preview-down-specs-core-gate.json`, `-core-matrix.md` (the third pass) |
| the core gate, run 2, the run of record | `44c2cc0`                                 | 4418, memory tier            | 20:35 to 22:06 (5,441 s)                                                                                                                                                                                                                                                                                                                                   | 785 rows: 760 passed, 8 failed, 17 not driven; cost rows over 0                                                                               | `gate-4418-run2-core-gate.json`, `-core-matrix.md`                      |
| the core gate on the preview, run 1     | `44c2cc0` plus the two files of section 8 | the fixture preview          | 22:09 to 00:21 (7,870 s)                                                                                                                                                                                                                                                                                                                                   | 785 rows: 750 passed, 18 failed, 17 not driven; cost rows over 1 (as the sync ship's)                                                         | `preview-run1-core-gate.json`, `-core-matrix.md`                        |
| the core gate on the preview, run 2     | the same                                  | the fixture preview          | 00:21 to 01:34 (stopped)                                                                                                                                                                                                                                                                                                                                   | the walk: 572 rows, 556 passed, 5 failed, 11 not driven (to 01:11:12); the specs and the cost probe not read, the credential expired 01:11:25 | `preview-run2-core-walk.json`, `-core-walk.md`                          |
| `node scripts/check.mjs` in full        | `44c2cc0` plus this folder                | 4321, the check's own server | 01:34 to 01:39, steps 1 to 18 ok; step 19 refused the unformatted ledgers; `--from 19` 01:39 to 01:55 (step 19 ok, step 20 the parity audit FAIL, standing); `--from 21` 01:55 to 02:04 (step 21 FAIL, standing); `--from 22` 02:04 to 02:07 (22 to 24 ok, 25 FAIL on disk space); `--from 27` 02:07 to 02:10 (27 skipped, 28 to 30 ok, 31 FAIL, standing) | steps 1 to 19 ok, 20, 21 and 31 standing reds, 25 the machine's disk, 26 not read, 27 skipped, 32 the gate runs of 5.4 and 5.5                | scratch                                                                 |

## Features round, ship one, fix round

The integrator's fixer, 2026-09-22 (times UTC, 23:3x to 00:xx on the 23rd). One finding was assigned: F11 of VERIFICATION.md's pass 1, check step 25 failing on Docker's own disk. Everything below happened on this machine and in this tree; nothing was committed (the ship step commits) and nothing on Vercel or GitHub was touched.

### 1. The finding

Check step 25 (`docker build -f docker/render-worker.Dockerfile -t turboslide-render-worker . && docker run ... turboslide export decks/fixture/gslides --mode native --verify`, SPEC-2 11.3) failed at `COPY . .` with "no space left on device" while the host had 165 GB free; steps 26 to 31 waited behind it and the verifier ran them by `--from`. The integrator read the same in the ship's full check run (section 9 of this note).

### 2. What was read

- Docker runs in the colima VM (profile `default`, aarch64, Ubuntu 24.04, Docker 29.5.2). Its `/var/lib/docker` volume is 98 GB and read 97 GB used, 0 bytes free; the VM's root volume 19 GB with 18 GB free; the host 161 GB free. `docker system df` counted 150 images (100.8 GB, 56.2 GB reclaimable), 14 containers (5.07 GB, 4.78 GB reclaimable), 18 volumes (1.19 GB), no build cache.
- 24 untagged images and 6 exited containers were this repository's own image builds. The six containers were the classic builder's step containers left by failed `RUN pnpm install ...` steps (five of `render-worker.Dockerfile`, 2026-09-11 to 14, exit 1; one of `Dockerfile.test`, 2026-09-11, exit 134), each identified by its command (the Dockerfile's RUN line), its untagged parent image, no mounts and no ports; one of them (`wizardly_kapitsa`) was listed "Up 9 days" by `docker ps` while `inspect` read it exited on 2026-09-14 with no process. The untagged images were superseded `turboslide-render-worker` builds (command `node apps/render-worker/src/main.ts`, `TURBOSLIDE_*` environment, 2026-09-10 to 12) and the step containers' parents; every tagged image belongs to another project or is a base image.
- The Docker client is 29.6.2 without the buildx plugin, so `docker build` runs the deprecated classic builder, which reads the root `.dockerignore` (BuildKit would read `docker/render-worker.Dockerfile.dockerignore`) and prints the context it sends: 1.128 GB. Of it, `crates/turboslide-native/target` is 588 MB (gitignored; excluded by `docker/Dockerfile.test.dockerignore` since `f63dd22`, by neither render worker list) and `docs/` is 409 MB of tracked evidence pictures; the rest is about 100 MB. The classic builder stores the context and then the `COPY . .` layer on the full volume, so the step fails there before `pnpm install`.
- Nothing in the image reads `docs/`: a grep of `apps/cli/src`, `apps/render-worker/src`, `apps/studio/src` and `packages/*/src` for file reads naming `docs` finds none; the two hits in `scripts/` (`editor-depth-drive.mjs`, `build-brand.ts`) are host scripts writing evidence. The strings that name documents in messages are text.

### 3. What was done

- On the machine: the six step containers removed by id (`docker rm`, the stale "Up" one accepted without `-f`); then `docker image prune -f`, which removes untagged, unreferenced images alone and reported 6.722 GB; with the containers' layers and the parents the volume read 97 GB used to 66 GB used, 27 GB free (72 percent). After: 127 images (68.1 GB, 40.3 GB reclaimable by Docker's count), 8 containers (the gt-cloud stack's `gt-*` containers, exited five weeks ago), the 18 volumes unchanged. Nothing tagged, no named container and no volume was touched: the 40 GB Docker still counts reclaimable are tagged images of other projects (`cotcodec-*`, `ghcr.io/astral-sh/uv`, `rust`, `localstack`, `node:24-bookworm`) and dangling volumes of other projects, Kevin's to decide.
- In the tree: `.dockerignore` and `docker/render-worker.Dockerfile.dockerignore` gain `crates/*/target` and `docs` under one comment (the two lists' patterns are identical, as the root file's header says); `scripts/check.mjs`'s step 25 comment and `docker/render-worker.Dockerfile`'s header record what the context holds, what a rebuild leaves in the daemon's store (the superseded image, untagged, a few GB) and that `docker image prune` reclaims it. No command changed: step 25 stays SPEC-2 11.3's literal build and run, `--force-rm` was not added (the runner's steps are the plan's commands) and the runner prunes nothing of its own.

### 4. The rerun

`node scripts/check.mjs --only 25` on the tree with the two ignore lists, 06:42:23Z to 06:48:15Z on the 23rd (the log is `build/integrator/check-25-fix-round.txt`; `.turboslide/container/gs-native` holds the exports and their reports). The classic builder sent a 99.67 MB context (1.128 GB before), `COPY . .` passed, every layer built (LibreOffice 25.2.3.2, pdftocairo 25.03.0, Chrome for Testing from build 1217, `pnpm install --frozen-lockfile`, the 34 font files, the CLI's help and the worker's imports) and the image was tagged `turboslide-render-worker:latest`; the volume read 69 GB used, 25 GB free after. The disk failure of F11 is gone.

The step still exits 1, on the container's export and not on Docker: `turboslide export decks/fixture/gslides --mode native --verify` writes both files (28 pages, 262 shapes, 3 custGeom in each) and the verify reads FAILED in both appearances, geometry in bounds, with the blocks out of the per cell budget: `03 table` (1.008 percent, 1 block; the second pass 0.238 percent, 9 blocks), `07 styles` (0.546, 1), `12 shapes` (2.725, 1), `21 table-merge` (0.596, 3; the second pass 0.344, 7) in light, the same four plus `14 word-art` (0.123, 1) in dark; the residual names the table cells by dx 8 to 25 (`table/0/1 dx 23 dy 0 dw -5` and its neighbours). VERIFICATION-2.md's step 25 reading of round two named the same family (both tables out of the per cell 3 px budget by dx 18 to 25, `table-merge` in both themes, `table` dark), so the misses are LibreOffice's table text metrics against the renderer's, a reading for the verifier against SPEC-2 11.3's "every miss named with its budget", not a fix of the integrator's files. Check step 25 is therefore: the container verification runs again on this machine; its verify is red on the named misses.

### 5. Tests and checks

- `packages/chrome` `src/menus/__tests__` (the integrator's files, unchanged by this fix): 7 files, 124 passed, 1.56 s.
- `node --check scripts/check.mjs` parses. No TypeScript, no product code and no matrix row changed, so no gate row was re-driven; the check step touched is the one rerun above.
- The other lanes' notes carry no "Features round, ship one, fix round" heading at 06:50Z; no request to the integrator was open to answer.

### 6. Deviations

- The Docker cleanup is a change on Kevin's machine outside the repository (section 3 lists what was removed and what was left; the 40 GB Docker still counts reclaimable are other projects' tagged images and dangling volumes).
- `docs/` is outside the image's context now: the image carries no documents, and a future step that reads one inside the container has to copy it in. `docker/Dockerfile.test.dockerignore` keeps its list (the crate's target already there, docs not excluded): not this finding's image.
- The step's remaining red (the verify's per cell budget misses in the container) is reported, not fixed: it is the export's reading under LibreOffice, outside this finding and outside the integrator's files.
- `colima list` reports the profile's disk as 60 GiB while the VM's `/var/lib/docker` volume reads 98 GB; recorded as read.

## Features round, ship two

The integrator of the features round, ship two (docs/FEATURES.md section 5 the shader library, section 6 the lanes and the one merge order, section 7 the acceptance), 2026-09-24, on the shared checkout at `2be923e` with the lanes' work of B4, B1, B5 and B7. A first attempt of this step committed the four lanes and landed part of the requests before it ended without a report; this attempt read its tree and went on from it (nothing recreated, nothing reverted). The dev server port is 4418 (`TURBOSLIDE_STORE=tmp` under a private `TMPDIR`, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, two 48 character secrets, `vite.no-watch.config.ts`; stopped before the report). Git write commands ran on the branch `features/ship-two` alone, never on main and never a push. No `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran outside `scripts/check.mjs`'s own steps. No token, cookie or secret was printed. Times are UTC unless said.

### 1. The merge and its commits

The round branch `features/ship-two` was created from `2be923e` by the first attempt and carries one commit per lane in the merge order FEATURES.md section 6 gives for ship two (B4, B1, B5, B7; the lanes absent from the ship skipped), each committing the lane's files by the path list of its note with the lane's items and hunks in the message body, then this attempt's three:

| Commit    | What                                                                                                                                                                                                                                                                                                                                  |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `caae128` | B4: the 29 shader rows of the matrix (814 rows) and their drivers (`core-walk/areas/shaders.mjs`, `e2e/core/shaders.spec.ts`, the export, chrome and lib additions), `core-matrix.mjs` with `shaders` parkable and `ROW_FEATURE`, the README and FOCUS renders.                                                                       |
| `ff177aa` | B1: `dialogs/ShaderGallery.tsx` and `.css`, the Background dialog's Shader row, the Download dialog's report rows, the parked controls test, the `InsertMaterial.tsx` shim (deleted by this attempt), the smoke and its two runs.                                                                                                     |
| `e52624e` | B5: the document shape (`motion`, `controls`, `palette`; `source.backend: 'client'`, `frameKey`), the eleven controls, the kit palette, the frame key and the capture, `store/frames.ts`, the client capturer `viewer/shader-frame.ts`, the one live mount, the Shader section, the 125 stills, the `shader.*` handlers, the notices. |
| `3691ef3` | B7: the hosted capture's 30 s bound and sentence, the shader ids on the studio's dispatcher and window transport lists, the orphan prune behind the response, the export's wait and report row, the frame in the PDF, both PowerPoints and the web page, `vite.deploy.config.ts`.                                                     |
| `e2c6a7a` | The integrator's day 0 requests landed and the seams closed (section 2), `pnpm generate:contracts` once (10 of 14 files).                                                                                                                                                                                                             |
| `4a9a12f` | The ship's parked list before the runs, `docs/gslides-parity/focus/ship-e2c6a7a.json`, `parkedFeatures` and `parkedRows` empty (7.2), named after the commit the enforce preview is built from.                                                                                                                                       |
| `f3c1b45` | The gallery's stills reach the client bundle: `packages/materials/src/previews.ts` `shaderPreviewUrl` keeps `../previews/` and `.webp` inline in the `new URL` template, the one shape Vite's build turns into a glob of assets; the first preview answered 404 for every still (section 4).                                          |
| `2f64bd8` | The seams the local gate read (section 5.1): the shader's Format options tail (`toolbar-tails.ts`, `editor-shell.ts`), the capturer's stale scan after a server side write (`controller.tsx`), one WebGL probe per page (`shader-frame.ts`), the show's Options read by menu item (`shaders.mjs`), the pictures script.               |
| `811fe11` | The shader drivers move Amplitude, the control the featured entry uses (`shaders.mjs`, `shaders.spec.ts`), and the spec's frame key pattern accepts the `sha256:` prefix; the tree of the two runs of record.                                                                                                                         |
| `5c71a96` | The brand facts follow the six shader actions (`facts.json` 192 actions, 169 MCP tools, 176 HTTP paths; the brand manifest and the home twin), read by the full check's step 5.                                                                                                                                                       |
| `240e2bb` | This note's section, the twenty pictures, the ledgers of every run and the sweep log.                                                                                                                                                                                                                                                 |
| `1f7af31` | The pictures script under prettier (the check's step 19).                                                                                                                                                                                                                                                                             |
| `44b0da4` | The full check's readings in this note.                                                                                                                                                                                                                                                                                               |

### 2. The requests answered

Every request under the lanes' "Features round, ship two" headings, by owner, with where it landed. The first attempt landed B5's R1 to R5 and the model.ts half of B1's R1; this attempt landed the rest.

| Request                  | Where it landed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| B1 R1 (model.ts)         | `insert.material` is `insert.shader`, Shader, unflagged, `terms` shader, material, animation, gradient, motion, the effect `block.insert` kept; P1: `MenuSetting` gains `playShaders`, View gains the Play shaders submenu between Mode and Full screen (`view.playShaders.on`, `.show`, `.off`, three toggles over one setting, `turboslide`, the cube icon, the finder terms); `editor-shell.ts` `STORED_SETTINGS` gains `playShaders` and `DEFAULT_SETTINGS` reads `playShaders: 'show'`. `menu-model.test.ts`: the row table reads `insert.shader`, the twenty additions list and the two parked lists follow, `OTHER_ROWS` carries the four View ids, two new cases (Insert > Shader in the default view found by "shader" and "animation"; the submenu's three toggles with In the show only checked). `editor-shell.test.ts` pins the stored default and its round trip.                                                                                                                                                                                    |
| B1 R2 (the picker)       | `DIALOG_IDS` `shaderGallery`, `InsertPicker` `'icon' \| 'shader'`, `PICKER_OF['insert.shader']`, `planFor`'s `insert.shader` case, `EditorShell.tsx`'s `PICKER_DIALOG`, lazy `ShaderGalleryDialog` and `dialogNode`; `palette-data.ts` `BLOCK_ROW.material` `insert.shader` and `insertLabel` reads Shader for a material (5.1, the Cmd+K entry); `dialogs/InsertMaterial.tsx` deleted; `editor-walk-probe.mjs` drives Insert > Shader through `dialog.shader` and `dialog.shader.tile.*`; `editor-shell.test.ts` 1000, `editor-shell-render.test.tsx` (the parked list and the gallery case: the dialog named Shader, the first card `paper:liquid-metal`, the write with `preset: 'diamond'`), `palette-data.test.ts` and `insert-menu.test.tsx` (the Shader entry in the default view) updated.                                                                                                                                                                                                                                                                 |
| B1 R3 and B7 R4          | `SHADER_GALLERY` lives in `menus/strings.ts` (the title, the sentence, the search field, the chips, the empty state, the tile docs, the alt, the Background row's words, the one failure sentence); `dialogs/ShaderGallery.tsx` imports and re-exports it for its readers, `dialogs/Background.tsx` imports it from strings; `DIALOGS.insertMaterial` left. The server's `RENDER_ERROR_SENTENCE` (`apps/studio/src/server/shader-frames.ts`) is `SHADER_GALLERY.placeFailed`, so the dialog's fallback and the server's answer are one string; `shader-frames.test.ts` keeps the literal.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| B1 R6 and B7 R1          | `ExportMenu.tsx` `ExportProgress` gains `rows?: ReadonlyArray<{ id, text }>`; `controller.tsx` copies the route's rows at the three publish sites: the sync path's poll (`runSyncExport`'s callback carries the poll's `rows` beside the line), the queued path's poll (`poll.rows`) and the batched path's progress (`progress.rows`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| B4 (the integrator)      | The model rows above; the README and FOCUS renders were current (`what-works.mjs --check`, no matrix edit in this step); the empty list committed before the runs and `--emit-parked` left to the ship step as b4.md 192 says; the two `tsc -b` errors b4.md named in B7's tests were gone on the committed tree (exit 0 whole tree).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| B5 R1 (actions.ts)       | The six `shader.*` ids in `ACTION_IDS` before the round three block and their entries (the text of `b5/actions-shader.patch.ts`), `material.capture` gains `frameKey` (B7 R2's schema half); `actions-gs3.test.ts` counts 192; `pnpm generate:contracts` once.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| B5 R2 (format-sections)  | `FormatSectionId` and `FORMAT_SECTIONS` gain `shader` (a P0 section, no `advanced` flag), `leadingFormatSection` reads a material, a material's fields route to `shader` (`/caption`, `/captionSize` to text, `/height` to size, `/asset` to none), `OWN_SECTION_PATHS` gains the recipe paths.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| B5 R3 (FormatOptions)    | The `shader` case mounts `ShaderSection` with `settings={{ advancedTools }}` and `renderGeneratedAdvanced('shader')`; the filter `selected?.type === 'material' && !many`; `onChangeShader` passed from the shell as `openDialog('shaderGallery')`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| B5 R4 (editor-shell.ts)  | The `insert.shader` branch of `insertBlockPlan` through `shaderBlockOf` with the first featured entry, labelled Shader (the palette's and the window API's direct insert; the row opens the gallery).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| B5 R5 (place-insert)     | `PlacedKind` gains `material`, `INSERT_MIN_SIZE.material` `[240, 135]`, `wantsPlacement` accepts a material; `place-insert.test.ts` gains the shader case and the `wantsPlacement` line; `apps/studio/src/server/actions.ts` passes `placeInsert` for `shader.insert` unguarded now the kind exists (B7's `Object.hasOwn` guard and its unused imports dropped).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| B5 R6 (Editor.tsx)       | The `MaterialMount` call passes `selected={ids}`, `palette={shaderPalette}` (`shaderPaletteOfDeck(doc.deck)`, memoised on the deck) and `scale={k}` (the sheet's fit scale the stage already computes).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| B5 R7 (Selection.tsx)    | The chip table reads `material: 'Shader'`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| B5 R8 (controller.tsx)   | One `createShaderFrameCapturer` per editor: `document` is `latest().document`; `write` waits `idle()`, runs `shader.frame` through `runDeckActionDetailed` at `reportedRevision()` (the acknowledged revision, the asset writes' base), adopts a created deck, and settles through `settleOwnWrite` so the tab holds the frame at once on the blob tier; a `ConflictError`, a `name` of ConflictError or a status 409 answers `conflict`; `captureHosted` runs `shader.capture` the same way; `afterCommit(mutations)` after this tab's own room commit (`commitAs`, so undo and redo ride it) and after a draft's local commit; `scheduleStale()` at `start()` for a stored deck when the principal can write; `dispose()` at `stop()`; never from `adoptExternal`. The F2 ids (`SERVER_SIDE_WINDOW_ACTIONS_F2`) are registered on the window transport, the mutating ones announcing as `logo.insert` does, so `shader.list`, `shader.insert`, `shader.set`, `shader.frame`, `shader.capture` and `shader.render` run through `window.turboslide.studio.invoke`. |
| B5 R9 (Inspector)        | Round one's `MaterialSection` left `Inspector.tsx` (the import, the target, the section), `inspector/material.tsx` and `material.css` deleted, the `./inspector/material` export removed; `materials-sections.test.tsx` drops the four cases and inverts the Inspector case (the intake in the Asset section, no Material section); `MATERIAL_RECIPE_PATHS` stays so the Block section draws no recipe row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| B5 R10 (block-css.ts)    | The `.material-label` rule dropped.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| B5 R11 (B7)              | Landed by B7 in `3691ef3` (`PACKAGES_PATTERN` `materials/previews/*`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| B5 R12 (optional)        | Not taken: `SHADER_WORDS` stays in `inspector/shader.tsx` and the ten sentences in `controls.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| B7 R2 (B5's commitFrame) | `packages/materials/src/actions.ts` `commitFrame`: an `AssetExistsError` or a Blob exists error on the put is success, the first writer's file stands and the record and the block are written (5.5).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| B7 R3                    | B5 R1 above.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| B7 R5, R6                | Information for B4 and B5; nothing to land.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| B1 2.5 (Slideshow.tsx)   | The show's Options menu gains `present.options.playShaders` with the three `menuitemradio` rows over the stored setting, read once on the client and written on a pick, as the ready hunk reads.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| B4 R5 seam (shaders.mjs) | The show's Options read in `core-walk/areas/shaders.mjs` matched `[data-control^="present.options."]`, which `Menu.tsx` never draws (a row is `data-control="menu.<id>"` with `data-menu-item="<id>"`, b1.md R5's last bullet); the reader takes `[data-menu-item^="present.options."]` too. Committed with the note; the local gate's run 1 read the row with the old reader (section 5).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

Beyond the requests, one seam of the tree: `packages/materials/src/previews.ts`'s `new URL(`${SHADER_PREVIEWS_DIR}${file}`, import.meta.url)` was not a shape Vite's build rewrites, so the first enforce preview served no still (section 4); `f3c1b45` inlines the folder and the extension around the one variable.

### 3. The gates on the tree

Run from the repository root unless said, on the tree of `e2c6a7a` before its commit (the requests landed) and again where the row says so on `f3c1b45`.

| Gate                                                     | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| -------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm generate:contracts`                                | Once; 10 of 14 files written (cli.json, mcp-tools.json, describe.json, openapi.json, manifest.json, llms-full.txt, docs/grammar.md, the three skill references), committed in `e2c6a7a`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `node_modules/.bin/tsc -b`                               | Exit 0 on the whole tree, the e2e files included (54.7 s cold; the two errors the first run showed were this attempt's own, `document` for `doc` in Editor.tsx and a dropped fixture import in a test, fixed in the same minute). The errors B1, B4, B5 and B7 named in each other's in progress files (export.spec.ts 1991, agent-actions.test.ts 77, shader-frames.test.ts 453) are not on the committed tree. Exit 0 again on `packages/materials` and `packages/chrome` after `f3c1b45`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `node_modules/.bin/vitest run` (every changed package)   | chrome 80 files, 748 passed, 2 todo (after five test updates of this attempt: the palette lists gain the Shader entry in the default view, the menu model's effect spelling); viewer 43 files, 431; render 18 files, 380; schema 25 files, 583; materials 6 files, 38; store 21 files, 259 passed, 1 skipped; export 21 files, 154; apps/studio 72 files, 546; the scripts project 12 files, 113. The named tests of FEATURES.md 7.3 for this ship green inside them: `parked-controls.test.ts` (the gallery and the section surfaces), `materials/src/controls.test.ts`, `capture.test.ts`, `store/src/frames.test.ts`, the schema validators, `menu-model.test.ts` (Shader where Material was, `view.playShaders` with three values), `no-action-id-snackbar.test.ts`. After `f3c1b45`: materials 38, the gallery and section tests 19, `build-shader-previews.mjs --check` 125 stills present.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `node scripts/check.mjs --greps`                         | Exit 0: innerHTML 27 call sites in the allowlist, overwrite: true 25 in the allowlist.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `node docs/readme/what-works.mjs --check`                | README.md is current (B4's render of `caae128` stands; no matrix edit in this step).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `turboslide lint --chrome` (step 18's six pages on 4418) | `node apps/cli/bin/turboslide.mjs lint --chrome` over 1440, 1280 and 390 in both appearances, under the e2e lock: `/deck/gt-brand` 24 audits, `/edit/gt-brand` 18, `/new` 18, `/decks` 6, `/home` 6, `/no-such-page` 6, 0 with findings, 0 states unapplied (the deck page without `--states`, as the check's step runs it; a first pass gave it the editor states and it refused on `menubar.file`, which the viewer page has no reason to draw).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| prettier                                                 | `--check` over the 124 files of the ship (the tracked changes since `2be923e` plus this attempt's, the webp stills, the snapshot and the licence files aside): 17 with drift, `--write` on them (eight of the lanes' files, nine of this attempt's), then `--check` clean; `previews.ts` and `shaders.mjs` checked after their edits.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `node scripts/check.mjs` in full                         | Run in pieces, because the runner stops at the first red step and the machine carried two other pipelines through the night (the hotfix worktree's gates and cost probes on its own previews, a `Turboslide-vector` checkout's own check chain on 4321, a puppeteer Chrome; load averages 24 to 49 over the passes). The first pass (03:21:43Z to 03:27:11Z, the tree of `811fe11`): steps 1 to 4 ok (the frozen install 1.4 s, the route tree 4.0 s, the generated files tracked 8.2 s, `tsc -b` 21.1 s); step 5, `pnpm test`, red on `packages/theme/src/brand.test.ts` (2 of 4,265: the brand facts at 186 actions and 163 MCP tools against the tree's 192 and 169), answered by the facts commit. The second (`--from 5`, 03:31Z to 03:35Z): step 5 red on one timing flake, `packages/agent/src/generate/contracts.test.ts` "are deterministic" at 6,922 ms against its 5 s timeout under the workspace run's workers (1 of 4,265; the file passes alone in 2.5 to 4.5 s). The third (`--from 5`, 03:37Z to 03:43Z): step 5 red on twenty timeouts of 5 to 38 s across unrelated packages under the load (the chrome's render tests, the pulse's, the fonts' raster comparison); step 5 stands as read three ways, and every changed package's suite passed alone on the same tree (the vitest row above). The fourth (`--from 6`, 03:44Z to 03:53Z): steps 6 to 17 ok (the sheet 26.8 s, the lint 22.8 s, the build 9.5 s, the viewer spec 38.9 s); step 18, the chrome lint on the check's 4321 server, read `/deck/gt-brand` 24, `/edit/gt-brand` 18, `/new` 18 and `/decks` 6 audits with 0 findings and then `net::ERR_CONNECTION_REFUSED` on `/home` from its own server, which had died under the load (the six pages read 0 findings on 4418, the lint row above). The fifth (`--from 19`, 03:53Z to 04:00Z): step 19 red on `pictures-ship-two.mjs`'s formatting after its cleanup edit, committed formatted (`1f7af31`). The sixth (`--from 19`, 04:01Z to 04:19Z): step 19 ok in 279.8 s; step 20, the parity audit, red as it stands: 892 pass, 39 fail, 235 skipped in 814 s under the load (ship one read 3,127, 40 and 397 on a quiet machine; the two infrastructure rows red and the `firstOpen`, `rows:scratch`, `effects:scratch` and `retiredKeys:scratch` sections carrying the fails), its rewritten `verification-3/parity-audit.json` restored before any commit as the earlier ships did. The seventh (`--only 21,…,31`, 04:19Z): step 21 could not start its server, 4321 held by the `Turboslide-vector` checkout's own `check.mjs --only 21`. The eighth (`--only 22,23,24,25,27,28,29,30`, 04:21Z to 04:31Z): 22 the native export round trip ok in 252.1 s, 23 the fonts build check ok in 92.8 s, 24 the canvas fidelity ok in 195.6 s, 25 skipped (no Docker daemon answers; the verifier's container step), 27 skipped (no production preview on 4344), 28 the audit ok (0 advisories at high or above), 29 the brand, home assets and definitions checks ok in 6.5 s, 30 the Vercel build with `check-vercel-output.mjs` ok in 20.3 s. The ninth (`--only 21,26,31`, 04:34:46Z once 4321 freed, the load at 10): step 21, the older studio suites, red as it stands, 18 failed, 14 did not run, 40 passed in 8.8 min (ship one's 17 of 58); step 26, the round three suites, red as it stands, 43 passed, 4 failed, 2 skipped, 5 did not run in 4.7 min (ship one's 43, 4 and 5, the four standing files); step 31, the perf budget, red as it stands, 113 of 151 budgets met on 4321 (ship one's exact reading). The suites' five scratch deck folders under `decks/` were removed after the runs and the tree read clean. Step 32, the gate against 4321, is the two runs of record of 5.3 and the memory tier run of 5.1, as ship one recorded. Standing reds: 20 (the sync ship's), with 21, 26 and 31 as the ninth pass reads them; 25 the machine's. |
| the core gate on 4418 (the memory tier)                  | Run 1 at `f3c1b45` (19:52:29Z to 21:42:31Z, 6,601 s): 814 rows, 752 passed, 34 failed, 28 not driven (1 manual), 0 no step, retries zero, cost rows over their ceiling 0; verdict failed (5.1). Two re drives of the shaders area and the spec files on the restarted server at `2f64bd8` and with the Amplitude drivers (5.2). Not every row is green on the memory tier: the ship two rows red there are the seams of section 6, sixteen of them read again on the deployment of record.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

### 4. The previews and their probes

Two enforce previews were deployed from the repository root with the recorded command of VERIFICATION.md plus the logo upstream FEATURES.md 4.9 and 7.2 name for a preview, the two secrets minted by `openssl rand -hex 32` into the command and never printed: `vercel deploy --yes --archive=tgz -e TURBOSLIDE_AUTHORIZE=enforce -e TURBOSLIDE_ASSIST=fixture -e TURBOSLIDE_LOGO_UPSTREAM=fixture -e TURBOSLIDE_MAIL=off -e TURBOSLIDE_REALTIME=blob -e TURBOSLIDE_PUBLIC_STORE_HOST=ggmycvj7j6224ay5.public.blob.vercel-storage.com -e TURBOSLIDE_SESSION_SECRET=<hex> -e TURBOSLIDE_DOWNLOAD_SECRET=<hex>`. `origin/main` was fetched before each and still read `2be923e` (the hotfix worktree had not pushed), so the branch deployed as it stood. Nothing on Vercel was created, changed or deleted beyond the two deployments.

- The first, `https://turboslide-nneo3yypv-kl01s-projects.vercel.app`, from the tree of `4a9a12f` (19:48:46Z to 19:50:43Z, the upload 347.5 MB, the build 47 s). Its hosted smoke (`node scripts/hosted-smoke.mjs <preview> --token-env TURBOSLIDE_TOKEN`, 19:52:52Z to 19:53:13Z) passed 40 of 40 and its write probe (`node scripts/probes/new-write-probe.mjs --base <preview>`, 19:53:23Z to 19:54:15Z) every step, the scratch deck trashed and deleted forever. The pictures of section 7 were first taken on it and read the gallery's cards as empty plates: a probe of the dialog (`thumb-probe.mjs`, the scratchpad) read every card's still as the plate with `data-thumb="none"` and the browser's requests for `/previews/<name>.webp` answering 404, while the dev server served the same files through `/@fs/` with 320 px decoded. The cause is the URL form in `previews.ts` (section 1, `f3c1b45`). Superseded before any gate run; not a deployment of record.
- The deployment of record, `https://turboslide-8q880rwic-kl01s-projects.vercel.app`, from `f3c1b45` (20:04:44Z to 20:06:40Z, the build 44 s). Before the gate runs: the same probe read four of four stills decoded and no failed response; the hosted smoke (20:06:54Z) 40 of 40; the write probe (20:07:14Z) every step ok, the deck `untitled-20260924-mnuh` trashed and deleted forever. The preview sits behind Vercel Authentication and every probe sent `VERCEL_OIDC_TOKEN` from `.turboslide/vercel-dev.env` (expiring 2026-09-25T03:02Z, not refreshed) as `x-vercel-trusted-oidc-idp-token`; the agent bearer travelled as `TURBOSLIDE_TOKEN` read from the production row of `~/.config/turboslide/hosts.json` (the preview's origin has no row).

### 5. The rows by run

#### 5.1 The core gate on 4418, the memory tier, run 1 (the tree of `f3c1b45`)

`node scripts/probes/core-gate.mjs --base http://localhost:4418 --lock <scratch> --out <scratch>/gate-4418 --matrix <scratch>/gate-4418/ledger.json --parked docs/gslides-parity/focus/ship-e2c6a7a.json`, 19:52:29Z to 21:42:31Z (6,601 s): 814 rows, 752 passed, 34 failed, 28 not driven (1 manual), 0 no step, retries zero, cost rows over their ceiling 0; verdict failed. The ledger copies are `ship-two-gate-4418-run1-core-gate.json` with its `core-matrix.md`, the walk's `core-walk.json` and `.md`, and the cost probe's JSON in this folder. The cost rows on the memory tier: `cost.editor-idle.calls`, `cost.editor-editing.calls`, `cost.two-tabs-idle.calls` and `cost.show.calls` passed, `sync.pull.no-listing` passed, `cost.editor-hidden.calls` not driven (headless Chromium reports no hidden page); the store half reads zero on the tmp store, so the function requests alone were asserted, and none rose against docs/SYNC.md 6.1.

The 34 reds and 28 not driven, by mechanism:

- The environment of this run's dev server, which was started without `TURBOSLIDE_ASSIST=fixture` and `TURBOSLIDE_LOGO_UPSTREAM=fixture` (the preview carries both): the four assist rows (`assist.rewrite.card-accept-undo`, `assist.notes.draft`, `assist.free-ask.fallback-sentence`, `assist.mark.chip-and-history`), `assist.agent.propose-accept`, and eleven logos rows (`logos.picker.search`, `.paper-and-ink`, `.recents`, `.licence-words`, `.chrome-1280`, `logos.insert.one-click-asset`, `.logo-size`, `.mono-tint`, `.every-slide`, `logos.replace-image.row`, `logos.cache.open-licence-only`, `logos.agent.search-insert`). The server was restarted with both variables after the run and these files re driven (5.2).
- Ship one's parked list, which this step's empty list does not carry (section 8): `view.live-pointers.second-browser`, `versions.show-changes-marks`, `inbox.bell-panel-toggle`, `templates.gallery.page`, `templates.deck.read-only`; the eight P1 controls of ship one not driven (`tables.seam.row-drag`, `tables.edge.add-row-column`, `tables.heads.select-row-column`, `tables.bar.row-column-buttons`, `brand.objects.kit-colours-first`, `arrange.group.tail-text-controls`, `wordart.tail.fill-outline`, `fonts.table.takes-family`, `fonts.picker.specimen-rows`, `logos.tailor.find-customer-logo`, `logos.picker.variants`, `logos.kit.find-a-logo`); `assist.quota.429`, `sync.resend.idempotent`, `logos.route.mark-headers`, `logos.index.refresh-fixture`, `logos.index.cached-offline` and `logos.export.pdf-pptx-crisp` not driven as their drivers say on a checkout.
- The ship two rows, 29: 8 passed, 14 failed, 7 not driven, read below.

| Row                                      | Run 1 on 4418 | What happened                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `shaders.insert.gallery-thumbnails`      | passed        | title Shader, the sentence, 17 cards with 17 decoded stills 4 ms after the open, 0 canvas, "metal" narrows to Liquid metal, the Metal chip narrows to one.                                                                                                                                                                                                                                                                            |
| `shaders.insert.selected-free-rectangle` | failed        | Liquid metal landed at 646,129 480 by 272 (preset diamond), the move handle, eight resize handles, the rotate handle, the ring, the chip Shader, no session, overlapping no text box of three; the slide's kind read title then content. The clause is the row's alone: an object insert through `insertBlockPlan` converts a fixed kind slide first (SPEC-2 1.6), as a table's does, and no tables row asserts the kind (finding 1). |
| `shaders.insert.words`                   | failed        | The Insert row Shader, the chip Shader, the alt "The liquid metal shader", the sheet clean, the show clean; the section absent (the tail seam, 5.2) and the viewer's second page read threw "Target page, context or browser has been closed" (B4's driver, b4.md).                                                                                                                                                                   |
| `shaders.panel.section-groups`           | passed        | The chrome spec at 1440 and 1280 (its opener reaches the panel).                                                                                                                                                                                                                                                                                                                                                                      |
| `shaders.panel.slider-live-undo`         | not driven    | "no Shader section in Format options": the walk's opener clicks `toolbar.formatOptions`, which a selected material block did not draw (the parked other tail); 5.2 after the tail seam.                                                                                                                                                                                                                                               |
| `shaders.panel.preset-tiles`             | not driven    | The same.                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `shaders.panel.control-sentences`        | not driven    | The same.                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `shaders.panel.kit-colours`              | not driven    | The same.                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `shaders.panel.one-home`                 | failed        | Pictures and materials draws Pictures and materials and Pictures with the switch on (no Material section); the Background dialog's link opened Format options with no Shader section: the covering material is a picture block and the section's filter takes a material block alone (finding 2).                                                                                                                                     |
| `shaders.frame.auto-capture`             | failed        | No frame after the recipe change: the change went through `shader.set`, a server side write the capturer did not hear (the seam of `2f64bd8`); 5.2.                                                                                                                                                                                                                                                                                   |
| `shaders.frame.box-aspect`               | failed        | No frame after the resize; the same seam.                                                                                                                                                                                                                                                                                                                                                                                             |
| `shaders.frame.reuse-and-prune`          | failed        | No frame at Strength 1.2; the same seam.                                                                                                                                                                                                                                                                                                                                                                                              |
| `shaders.frame.one-capturer`             | failed        | An equality in the spec's second context read; 5.2.                                                                                                                                                                                                                                                                                                                                                                                   |
| `shaders.background.place-answers`       | passed        | Liquid metal chosen, Place, the ground changed after 3,361 ms (the covering picture over the asset), the button read "Placing, 2 s".                                                                                                                                                                                                                                                                                                  |
| `shaders.export.pdf-frame`               | passed        | The export spec.                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `shaders.export.pptx-frame`              | passed        | The export spec, both modes.                                                                                                                                                                                                                                                                                                                                                                                                          |
| `shaders.export.html-frame`              | passed        | The export spec.                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `shaders.export.missing-frame-row`       | failed        | "the export started within 800 ms of the change": the spec's own timing clause; 5.2.                                                                                                                                                                                                                                                                                                                                                  |
| `shaders.perf.one-context`               | failed        | With the second block selected, two canvases after 2,675 ms and no frame image on the first: the first block had no frame yet (the capture seam above) and a frameless block mounts a still (5.5's "before the first frame the block shows the live canvas"); 5.2.                                                                                                                                                                    |
| `shaders.perf.hidden-pauses`             | passed        | The spec with its hidden tab fallback.                                                                                                                                                                                                                                                                                                                                                                                                |
| `shaders.perf.editor-frame`              | failed        | The measurement row: the longest animation frame 158.4 ms over 5 s with one shader on the stage (583 frames; renderer "ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (LLVM 10.0.0)), SwiftShader driver)"), over the 150 ms of docs/performance.md 3.1 on the runner's software renderer; recorded, never holding the ship (7.2).                                                                                                   |
| `shaders.agent.list-insert-set-render`   | passed        | The six actions over the window API.                                                                                                                                                                                                                                                                                                                                                                                                  |
| `shaders.background.add-to-theme`        | not driven    | P1, not built (`dialog.background.shader.addToTheme`).                                                                                                                                                                                                                                                                                                                                                                                |
| `shaders.library.glyph-engines-render`   | not driven    | P1, not built (`dialog.shader.engine.glyph`; the catalog lists none of the three engines).                                                                                                                                                                                                                                                                                                                                            |
| `shaders.frame.scrubber-capture`         | not driven    | P1, not built (`formatOptions.shader.frame.scrubber`).                                                                                                                                                                                                                                                                                                                                                                                |
| `shaders.view.play-setting`              | failed        | The three rows with In the show only checked, Off stored and read after a reload, the default back; the show's Options menu "lists no Play shaders row" through the driver's `data-control` read of rows `Menu.tsx` draws as `data-menu-item` (the reader seam of `2f64bd8`); 5.2.                                                                                                                                                    |
| `shaders.show.plays-when-on`             | failed        | P1, the present layer not built; the spec asserts rather than reads not built ("one canvas in the show"); parks `present` per the row.                                                                                                                                                                                                                                                                                                |
| `shaders.show.frame-when-off`            | failed        | P1, the same ("the frame is shown").                                                                                                                                                                                                                                                                                                                                                                                                  |
| `shaders.insert.gallery-hover-live`      | passed        | One canvas after the hover, one at 400 ms, at most one while moving, none after leaving.                                                                                                                                                                                                                                                                                                                                              |

#### 5.2 The re drives on 4418 after the seams

The dev server was restarted at 21:44Z with `TURBOSLIDE_ASSIST=fixture` and `TURBOSLIDE_LOGO_UPSTREAM=fixture` beside the run 1 variables (a fresh tmp store), serving the tree of `2f64bd8` (the material tail, the capturer's stale scan after a server side write, the WebGL probe, the show reader). Two passes, each the walk's shaders area (`editor-walk-probe.mjs --core --only shaders`, under the e2e lock) then spec files through `core-gate.mjs --only specs --spec …` with a scratch lock; the ledgers are `ship-two-4418-redrive1-*` and `ship-two-4418-redrive2-*` in this folder.

Pass 1 (21:46Z to 22:03Z; the Strength drivers as B4 wrote them; the spec files assist, chrome, logos and shaders): the environment reds of run 1 read green, every one: `assist.rewrite.card-accept-undo`, `assist.notes.draft`, `assist.free-ask.fallback-sentence`, `assist.mark.chip-and-history`, `logos.picker.recents`, `logos.picker.chrome-1280`, `logos.route.mark-headers`, `logos.index.refresh-dry-run`, `logos.cache.open-licence-only`, `logos.agent.search-insert`, with `shaders.panel.section-groups` and the four other chrome rows (27 spec rows: 18 passed, 6 failed, 3 not driven). The walk's shaders area read the tail seam closed (`shaders.insert.words` reads the section "Shader") and `shaders.view.play-setting` still red on the old reader the running walk had loaded before the fix (it read green in pass 2). The slider row read a drag that moved no value, no write on the release, and its Cmd+Z undoing the insert, which left the three panel rows after it and the perf row without a block; the diagnosis on the deployment of record read the Strength slider `disabled`, by design (finding 3), and the drivers were moved to Amplitude (`2f64bd8`'s follower, the drivers' commit).

Pass 2 (22:03Z to 22:16Z; the Amplitude drivers; the shaders spec file alone): the walk's area 4 passed (`gallery-thumbnails`, `view.play-setting`, `gallery-hover-live`, and the words row's five reads), 6 failed, 4 not driven; the spec file 10 rows: 2 passed, 7 failed, 1 not driven. What the frame rows read with a control that moves a uniform:

| Row                                            | Pass 2 on 4418 | What happened                                                                                                                                                                                                                                                                                                                                                                   |
| ---------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `shaders.frame.auto-capture`                   | failed         | The frame `frame-091c1b413302364f` came 758 ms after the insert, the setup's read 18 ms; Amplitude 1.15 through the section; a new frame `frame-8524c8bd005242b0` 910 ms after the change with the client's source; the one assertion left red was the spec's key pattern, bare 64 hex against B5's `sha256:` prefix, the schema's own spelling. The drivers' commit widens it. |
| `shaders.frame.box-aspect`                     | failed         | The south east handle dragged, `/pos` 600 by 152, the frame `frame-7c4c17e8962f5b27` after 1,369 ms at 3200 by 810 (wanted about 811); the viewer against the editor at rest differed by 82 on one channel against the row's 48 (finding 16).                                                                                                                                   |
| `shaders.frame.reuse-and-prune`                | failed         | Amplitude 3.2 to 1.2 to 3.2: frames `091c…` to `8524…` (1,558 ms) to a read of `daffa7…` 24 ms after the return, the assets 5 to 5 and the block naming `091c…` at the end; the read 24 ms after the return caught a transient, the reuse itself held (finding 16).                                                                                                             |
| `shaders.frame.one-capturer`                   | failed         | The second context's read expected null and found `frame-091c…` after 20 s (finding 16).                                                                                                                                                                                                                                                                                        |
| `shaders.perf.hidden-pauses`                   | passed         | 118 frames a second visible; the driver set `document.hidden` and dispatched `visibilitychange` (headless Chromium kept the page visible): 0 frames in 2 s; resumed 116 in 1.5 s; reduced motion one canvas, two clips 900 ms apart equal.                                                                                                                                      |
| `shaders.perf.editor-frame`                    | failed         | 425.7 ms longest animation frame over 5 s on SwiftShader during the pass (the measurement; 158.4 ms in run 1, under 150 ms in pass 1).                                                                                                                                                                                                                                          |
| `shaders.agent.list-insert-set-render`         | passed         | `shader.list` 200, `shader.insert` 200 (gem smoke, brand-blue, `motion.play` show, placed at 560, 530), `shader.set` 200, `shader.render` 350,337 bytes in 13,590 ms at 3200 by 1800.                                                                                                                                                                                           |
| `shaders.library.glyph-engines-render`         | not driven     | P1, 17 entries, none of the three engines.                                                                                                                                                                                                                                                                                                                                      |
| `shaders.show.plays-when-on`, `frame-when-off` | failed         | P1, the present layer absent: 0 canvas in the show, the frame not drawn by the spec's read (finding 13).                                                                                                                                                                                                                                                                        |

The walk's slider row in pass 2 read the Amplitude range at 3.2 during and after the drag with no write; the same drag path (`moveHuman`, the pointer down at the thumb, fourteen moves, the release), replayed on the deployment of record by a scratch script, moved the range to 5.6 and, under `prefers-reduced-motion: reduce`, to 8, and a plain drag there stored `controls.amplitude` 4.7 with `u_distortion` moved, a frame 24 ms later and a re capture 3 s after; so the control, the write and the capturer hold and the local row's still range is that server's moment, read again by the preview's runs. `shaders.background.place-answers` read 5,847 ms in pass 1 and 15,300 ms in pass 2 (the hosted capture on the checkout's render worker, 3,361 ms in run 1), over the row's 5 s (finding 15).

#### 5.3 The two runs of record on the deployment of record

`node scripts/probes/core-gate.mjs --base https://turboslide-2lbzoh8o7-kl01s-projects.vercel.app --out <scratch>/preview/gate-<n> --matrix … --parked docs/gslides-parity/focus/ship-e2c6a7a.json` with `VERCEL_OIDC_TOKEN` and `TURBOSLIDE_TOKEN` in the environment, twice in a row with no change between the runs (the drivers' commit `811fe11` was the tree for both; the deployment is `2f64bd8`'s build), 7.2's flakiness check. A first start of run 1 at 22:17Z was stopped after eight minutes for the spec's frame key pattern (the drivers' commit) and is not a reading; its scratch decks were swept (section 8). The ledgers: `ship-two-preview-run1-*` and `ship-two-preview-run2-*` in this folder.

Run 1: 22:19:41Z to 00:52:45Z (9,184 s): 814 rows, 755 passed, 40 failed, 19 not driven (1 manual), 0 no step, retries zero, cost rows over their ceiling 0; verdict failed. The cost rows: `sync.pull.no-listing` passed; `cost.editor-idle.calls` 8.99 function requests a minute (ceiling 12; production 9.66) with the store settled 63.65 s after the state was ready; `cost.editor-hidden.calls` not driven (headless Chromium reports no hidden page); `cost.editor-editing.calls` 71.28 a minute (ceiling 75; production 68.53) over 214 requests in 3 minutes; `cost.two-tabs-idle.calls` passed, the store advancing 18 a minute; `cost.show.calls` 0 in the window (ceiling 0). The two standing rows of Kevin's: `sync.title.concurrent-both-kept` failed, `decks.file.open-list-search` failed (green in run 2); neither chased.

Run 2: 00:52:45Z to 03:20:22Z (8,857 s): 814 rows, 758 passed, 36 failed, 20 not driven (1 manual), 0 no step, retries zero, cost rows over their ceiling 2; verdict failed. The cost rows: `sync.pull.no-listing` passed; `cost.editor-idle.calls` failed on its store half, simple 43 a minute over 40 and advanced 13 over 11, with the function requests at 9.66 a minute (ceiling 12); `cost.editor-hidden.calls` not driven; `cost.editor-editing.calls` passed; `cost.two-tabs-idle.calls` failed on one store listing in the window against none allowed; `cost.show.calls` 0 in the window. The `VERCEL_OIDC_TOKEN` in `.turboslide/vercel-dev.env` outlived the prompt's 03:02Z through the last three cost rows without a refusal (no 401 in either run's log; the two " 401" matches are a row number and a field name), so it was not refreshed. A third reading of the two cost rows after the runs is in 5.4.

The flakiness check (7.2): 18 rows differ between the runs. Two are ship two's: `shaders.export.html-frame` (red in run 1, green in run 2; the mechanism is the spec's sampler over the hosted web page's JPEG data URI, finding 23, in neither forgiven class, so the row is flaky under docs/FOCUS.md 6.2 until a rerun after a named change reads it green twice) and `shaders.perf.editor-frame` (241.7 ms then under 150 ms; the measurement row records and never holds the ship). The sixteen others: `assist.mark.chip-and-history`, `assist.notes.draft` (red then green) and `assist.quota.429` (red then not driven; the fixture assistant on the blob tier); `cost.editor-idle.calls` and `cost.two-tabs-idle.calls` (green then red on the store half, a listing among them, the recorded blob tier class); `decks.card.make-a-copy`, `decks.card.rename-enter`, `export.png.current-slide`, `share.dialog.co-edit-from-copied-link`, `share.dialog.you-label` (green then red), `images.background.upload-picture`, `versions.undo-restore`, `templates.save.as-template`, `templates.save.same-name-replaces` (red then green): the return round's measured blob tier timing (a listing, a copy, a restore, a second browser bound), the first recorded class; `fonts.picker.recent-group` (red then green); `decks.file.open-list-search` (Kevin's standing row). Their mechanisms are in the two runs' `core-matrix.md`.

Red or not driven in both runs, 48 rows: the sixteen ship two rows of the table below plus its three P1 not driven; ship one's parked list carried by this step's empty list (`view.live-pointers.second-browser`, `versions.show-changes-marks`, `inbox.bell-panel-toggle`, `inbox.notification-arrives`, `inbox.settings-persist`, `templates.gallery.page`, `templates.card.rename-and-delete`, `templates.default.use-for-new`, `templates.deck.read-only`, the eight P1 controls `tables.seam.row-drag`, `tables.edge.add-row-column`, `tables.heads.select-row-column`, `tables.bar.row-column-buttons`, `brand.objects.kit-colours-first`, `arrange.group.tail-text-controls`, `wordart.tail.fill-outline`, `fonts.table.takes-family`, and `fonts.picker.specimen-rows`, `logos.picker.variants`, `logos.kit.find-a-logo`); `images.insert.upload-while-pending`, `versions.restore` and `sync.block.offline-replay-converges` (the blob tier class, red twice this time); `sync.title.concurrent-both-kept` (standing); `logos.index.refresh-fixture` (failed twice; ship one's fix round read it on the fixture upstream and the hotfix's logo service is the seam under it) and `logos.index.cached-offline` (not driven on the fixture upstream, as its driver says); `sync.resend.idempotent`, `cost.editor-hidden.calls` and the manual row `text.clipboard.paste-without-formatting`.

What the ship step reads from this for the list of record (7.2): of the sixteen ship two rows red in both runs, fourteen carry `parks` in the matrix (`insert.shader` for the insert, frame and perf rows, `formatOptions.shader` and its `.strength`, `.preset` and `.color` for the panel rows, `view.playShaders` and `formatOptions.shader.play` for the show rows), so the mechanical `wouldParkRows` parks their controls and not the feature. Two carry none: `shaders.export.pdf-frame`, an export row by `ROW_FEATURE`, which red in both runs blocks the ship under 7.2 (a red export row never parks the picker), and `shaders.agent.list-insert-set-render`, a shaders row without `parks`, which red in both runs parks the feature whole. Both are named seams (finding 23, the PDF sampler over the hosted file; finding 24, `shader.render`'s 500 on the deployment) and sit in the fix round between this step and the production run; the drivers' seams of findings 16, 20, 22 and 23 and the contract seams of findings 2, 17 and 19 go with them, and a row whose seam is the driver's alone reads green there without a product change.

The cost rows against docs/SYNC.md 6.1 and the production table: the function requests a minute, the half the production table reads, held in every reading of this ship (idle 8.99, 9.66 and 9.65 against the ceiling 12 and production's 9.66; editing 71.28 and under in run 2 against 75 and production's 68.53; the show 0 against 0). The store half of the idle row read simple 43 and advanced 13 a minute in run 2 and simple 47 in the third reading against the ceilings 40 and 11, under in run 1; the two tabs row one listing in run 2's window against none, under in run 1 and in the third reading. No route of this ship adds a poll (the capturer writes once per recipe change behind the seller's commits and only for a deck with a shader, the prune runs behind a frame write's response, the gallery and the section open no stream), the probe's deck holds no shader, and the store counters are the instance's; the mechanism is not read here and the store half's rise on the idle row is for Kevin and the ship step to weigh against the sync round's readings before the production run.

The ship two rows by run, with what run 1 read (run 2's differences named in the last column):

| Row                                      | Run 1      | Run 2      | What happened in run 1                                                                                                                                                                                                                                                                                        |
| ---------------------------------------- | ---------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `shaders.insert.gallery-thumbnails`      | passed     | passed     | title Shader, the sentence, 17 cards with 17 decoded stills 4 ms after the open, 0 canvas, "metal" narrows to Liquid metal, the Metal chip narrows to one.                                                                                                                                                    |
| `shaders.insert.selected-free-rectangle` | failed     | failed     | Liquid metal at 646,129 480 by 272 with the eight handles, the ring, the chip Shader, no overlap of three text boxes; the kind read title then content (finding 1).                                                                                                                                           |
| `shaders.insert.words`                   | failed     | failed     | Insert row Shader, chip Shader, section "Shader", alt "The liquid metal shader", the sheet and the show clean; the viewer's second page read threw (finding 4).                                                                                                                                               |
| `shaders.panel.section-groups`           | passed     | passed     | At 1440 and 1280: the nine groups in order (Shader, Preset, Colors, Form, Light and texture, Orientation, Motion, Dither, Advanced), 44 fields, none past the 320 px panel, no horizontal scroller.                                                                                                           |
| `shaders.panel.slider-live-undo`         | failed     | failed     | The mount at rest; the drag read the value 3.2 with 0.0 percent of pixels changed and no write on the release; Cmd+Z left 3.2: the range sits below the viewport after the section's scroll (finding 16).                                                                                                     |
| `shaders.panel.preset-tiles`             | failed     | failed     | Ten tiles in sentence case against ten presets, Diamond pressed; the click wrote diamond to ink-paper (the revision moved) and the stage's canvas read unchanged 936 ms later (finding 18).                                                                                                                   |
| `shaders.panel.control-sentences`        | failed     | failed     | 11 of 11 controls, 8 with their sentence; Strength, Detail and Grain, disabled on liquid metal, showed no tooltip (finding 17).                                                                                                                                                                               |
| `shaders.panel.kit-colours`              | failed     | failed     | Six swatches and Custom; `brand.set /colors/dark/primary` ok; the shader's colour moved 0.0 and the frame key stayed 6 s later (finding 19).                                                                                                                                                                  |
| `shaders.panel.one-home`                 | failed     | failed     | Pictures and materials with no Material section; the Background dialog's link opened Format options with no Shader section for the covering picture (finding 2).                                                                                                                                              |
| `shaders.frame.auto-capture`             | failed     | failed     | The frame came 2,597 ms after the insert and a new one 2,050 ms after the Amplitude change with the client's source; the spec's id derivation kept the `sha256:` prefix inside `frame-<16 hex>` (finding 20).                                                                                                 |
| `shaders.frame.box-aspect`               | failed     | failed     | The handle dragged to 600 by 152, the frame after 1,381 ms at 3200 by 810; the viewer against the editor at rest 82 on one channel against 48 (finding 21).                                                                                                                                                   |
| `shaders.frame.reuse-and-prune`          | failed     | failed     | Amplitude 3.2 to 1.2 to 3.2: the 1.2 key's frame came back in 5 ms (the asset reused) and the 3.2 frame in 2,073 ms, the assets 5 to 5, the block naming the first; the prune left one frame and the deck's own `opener-prototemplate` material asset, which the spec counted as a second frame (finding 22). |
| `shaders.frame.one-capturer`             | failed     | failed     | The second browser's Amplitude 0.8 captured `frame-9c2803b9467812f2` and the first page sent no `shader.frame` request; the same count of two (finding 22).                                                                                                                                                   |
| `shaders.background.place-answers`       | passed     | passed     | Choose, Place, the ground changed within the row's 5 s with the seconds on the button.                                                                                                                                                                                                                        |
| `shaders.export.pdf-frame`               | failed     | failed     | The frame in 2,519 ms; the PDF's largest image 3200 by 1734 FlateDecode among four image objects, no label text; the spec's sampler read nothing from it (finding 23).                                                                                                                                        |
| `shaders.export.pptx-frame`              | passed     | passed     | The picture `ts:split-4#material` with the recipe JSON in `descr`, the media 3200 by 1814.                                                                                                                                                                                                                    |
| `shaders.export.html-frame`              | failed     | passed     | 751,987 bytes in 1,647 ms, 0 canvas, no label words, the frame inlined as a JPEG data URI at 3200 natural; the spec's sampler read nothing from it (finding 23).                                                                                                                                              |
| `shaders.export.missing-frame-row`       | passed     | passed     | The anchor moved to 7000, the export OK 537 ms after the change; the dialog read "1 shader had no frame; the export waited 2 s for it" among its rows.                                                                                                                                                        |
| `shaders.perf.one-context`               | failed     | failed     | With the second block selected two canvases and no frame image on the first (finding 6).                                                                                                                                                                                                                      |
| `shaders.perf.hidden-pauses`             | passed     | passed     | 117 frames a second visible; the driver set `document.hidden` and dispatched `visibilitychange` (the headless browser kept the page visible): 0 frames in 2 s; resumed 151 in 1.5 s; reduced motion one canvas, two clips 900 ms apart identical.                                                             |
| `shaders.perf.editor-frame`              | failed     | passed     | The measurement: 241.7 ms longest animation frame over 5 s (573 frames, SwiftShader); recorded (7.2).                                                                                                                                                                                                         |
| `shaders.agent.list-insert-set-render`   | failed     | failed     | `shader.list` 200, `shader.insert` 200 (gem smoke at 560, 530), `shader.set` 200; `shader.render` 500 in 153 ms with no bytes on the deployment, where the checkout answered 350,337 bytes in 13.6 s (finding 24).                                                                                            |
| `shaders.background.add-to-theme`        | not driven | not driven | P1, not built.                                                                                                                                                                                                                                                                                                |
| `shaders.library.glyph-engines-render`   | not driven | not driven | P1, not built.                                                                                                                                                                                                                                                                                                |
| `shaders.frame.scrubber-capture`         | not driven | not driven | P1, not built.                                                                                                                                                                                                                                                                                                |
| `shaders.view.play-setting`              | passed     | passed     | The three rows with In the show only checked, Off stored and read after a reload, the show's Options menu listing Play shaders, the default back.                                                                                                                                                             |
| `shaders.show.plays-when-on`             | failed     | failed     | P1, the present layer absent: 0 canvas in the show (finding 13).                                                                                                                                                                                                                                              |
| `shaders.show.frame-when-off`            | failed     | failed     | P1, the same: the spec read no frame drawn.                                                                                                                                                                                                                                                                   |
| `shaders.insert.gallery-hover-live`      | passed     | passed     | One canvas after the hover, one at 400 ms, at most one while moving, none after leaving.                                                                                                                                                                                                                      |

#### 5.4 A third reading of the two cost rows

`sync-cost-probe.mjs --base <preview> --row <id>` for the two rows run 2 read over, at 2026-09-25T03:21:37Z after the runs with nothing else of this step on the deployment: `cost.editor-idle.calls` failed (store simple 47 a minute over 40 | function requests 9.65 a minute (ceiling 12; 29 in 3 min) | the store settled 2.48 s after the state was ready (1 sync.status read(s) before the window) | store simple 47 a minute (ceiling 40; 3 of the heads the probe's own sync.status reads, which the ceiling carries, b3.md R7 b) | store advanced 10 a minute (ceiling 11) | instances answering sync.status 1); `cost.two-tabs-idle.calls` passed (the store settled 2.35 s after the state was ready (1 sync.status read(s) before the window) | store advanced 18 a minute (ceiling 30) | store list 0 (ceiling 0) | instances answering sync.status 2). The readings are informational beside the two runs of record; the store half of the idle row and the listing of the two tabs row are the blob tier's own (the pulse and the prunes the sync round measured), and no route of this ship adds a poll: the capturer writes once per recipe change behind the seller's commits, the prune runs behind a frame write's response once a minute per deck at most, the gallery and the section open no stream.

### 6. The findings by owner

Each names the file, what was read and who lands the change. A finding this step closed is marked so; the others are the fix round's.

1. B4, `core-walk/areas/shaders.mjs` (`shaders.insert.selected-free-rectangle`): the clause "the slide keeps its kind" is the row's alone. An object insert through `insertBlockPlan` on a fixed kind slide converts it to a canvas first (SPEC-2 1.6, the `slide.toCanvas` prefix `convertThenCommit` measures), as a table's and a chart's do, and no tables or charts row asserts the kind; the row read title then content on every run while every other clause passed (the free rectangle, the eight handles, the chip Shader, no overlap). Either the row drops the clause or the round decides that a shader insert alone keeps a fixed kind, which is a product change beyond this ship.
2. B5 with B1, `inspector/shader.tsx`, `FormatOptions.tsx`, `dialogs/Background.tsx` (`shaders.panel.one-home`, the link half): the Background dialog's Shader options selects the covering material, which is a picture block whose asset is a material (the ground `slide.setBackgroundMaterial` writes until the P1 slot lands), and the Shader section's filter takes a material block alone (`selected?.type === 'material'`), so the panel opens with no Shader section. Round one's Material section had a picture target for this case; the Shader section wants one (the asset's recipe read, the writes through `asset.dither` and a re capture), or the link opens the panel at Dither until it has one.
3. B4 with B5, `core-walk/areas/shaders.mjs`, `e2e/core/shaders.spec.ts` (`shaders.panel.slider-live-undo`, `shaders.frame.auto-capture`, `shaders.frame.reuse-and-prune`, the rows after them): the drivers moved Strength on the featured entry, liquid metal, whose uniforms (`u_repetition`, `u_softness`, `u_shiftRed`, `u_shiftBlue`, `u_distortion`, `u_contour`, `u_angle`, `u_shape`) carry none of the Strength keys (`intensity`, `contrast`, `bloom`, `outerGlow`, `innerGlow`, `highlights`, `glow`, `midIntensity`; Glyphfield's `paperShaderControls.ts` scales the same list and gives Strength its visible effect there through a CSS `contrast()` filter the port does not carry). The section disables a control that maps to nothing ("not used by this shader", `controlApplies`), so the walk's drag moved no value, its release wrote nothing, its Cmd+Z undid the insert itself, and the three panel rows after it and the perf row found no block; the spec's Strength change moved no uniform, so no frame key changed and no frame followed. Closed by this step in the drivers: the walk's slider row and the spec's `changeRecipe`, `reuse-and-prune` and the not built id read Amplitude (liquid metal's `u_distortion`); the matrix rows' words still say Strength (7.1 verbatim) and are B4's to amend, or B5 gives Strength a mapping on the featured entry (`u_contour` reads as the effect's strength on liquid metal) so the rows' words hold.
4. B4, `core-walk/areas/shaders.mjs` (`shaders.insert.words`): the viewer's second page read throws "page.evaluate: Target page, context or browser has been closed" on every run (b4.md names it open); the other five reads pass, the section "Shader" included since the tail seam.
5. B5, `inspector/shader.tsx`: the component root repeats the section head's id, `data-control="formatOptions.shader"` on both the head button and the `.ts-shader` root, two elements with one id on the page; the drivers take the first and the lint passed on pages with no shader selected. One id per element is the shape of `model.ts`'s ids; the root wants its own (`formatOptions.shader.body`) with the drivers following.
6. B5 with B4, `MaterialMount.tsx` (`shaders.perf.one-context`): a block with no frame yet mounts a still at speed 0 (5.5: before the first frame the editor shows the live canvas), so two frameless shader blocks are two canvases until their frames land; the row counts canvases 2.7 s after the second insert, inside the first capture's time on the runner's software renderer. The row wants to wait for the frames (`waitFrame`) before it counts, or the rule wants the frameless unselected block to draw the plate.
7. B7 with B4, `e2e/core/export.spec.ts` (`shaders.export.missing-frame-row`): "the export started within 800 ms of the change" failed on the memory tier; the spec's own window from the recipe change to the export's start, with the Strength change of finding 3 behind it (no frame was ever pending). Read again on the preview runs with the Amplitude change.
8. B5, `scripts/build-shader-previews.mjs` (the gallery): the preset tiles under a card render in each preset's own colours (blue and orange tiles under gem smoke) while the dialog's sentence says the previews are black and white; the cards are. Either the tiles render in ink and paper too or the sentence names the tiles.
9. B5, `packages/materials/src/previews.ts`: the still URL's form (section 1, `f3c1b45`); closed by this step.
10. B5, `packages/viewer/src/shader-frame.ts` `clientCanCapture`: a WebGL probe per capture leaked a context (closed by this step, `2f64bd8`).
11. The integrator's own, `apps/studio/src/editor/controller.tsx`: the capturer heard this tab's local commits alone and not its server side writes (closed, `2f64bd8`); the shader's toolbar tail (closed, `2f64bd8`).
12. B4, `core-walk/areas/shaders.mjs` (`shaders.view.play-setting`): the show's Options read (closed by this step).
13. B4 with B7, `e2e/core/shaders.spec.ts` (`shaders.show.plays-when-on`, `shaders.show.frame-when-off`): the two P1 show rows assert ("one canvas in the show", "the frame is shown") instead of reading not built on the absent present layer, so they count as failed where the other P1 rows count as not driven; their `parks` name the present layer's controls either way.
14. The measurement row `shaders.perf.editor-frame`: 158.4 ms longest animation frame over 5 s with one shader on the stage on the runner's software renderer (SwiftShader, 583 frames), over the 150 ms of docs/performance.md 3.1; recorded for the round after's first shader item (7.2).
15. B7, `apps/studio/src/server/actions.ts` (`shaders.background.place-answers`): the hosted capture on the memory tier answered in 3.4 s in the run of record and 5.8 s on the re-drive while the pictures ran beside it, over the row's 5 s; the 30 s bound and the sentence hold. Read again on the preview.
16. B4, `core-walk/areas/shaders.mjs` `sliderOf` and the rows that drag a range (`shaders.panel.slider-live-undo` first): `openShaderSection` scrolls the section's top into view and reads the range's rect from there; the Form group sits under the Shader, Preset and Colors groups, and on the deployment of record the Amplitude range's box read y 1106 in the 900 px viewport after that scroll, so the pointer down, the moves and the release land outside the page and nothing moves ("the value read 3.2 with 0.0 percent of pixels changed", no write, in every run). The same drag with the range scrolled into view first moved it to 5.6 and held it after the release (the scratch probe, 23:13Z). The reader wants `scrollIntoViewIfNeeded` on the range before it reads the thumb; `shaders.panel.kit-colours` and `shaders.panel.preset-tiles` read the section's upper groups and are not hit by this.
17. B5, `inspector/shader.tsx` (`shaders.panel.control-sentences`): the tooltip sits on the range input through `tipProps`, and a disabled input takes no pointer events, so a control the material does not use (Strength, Detail and Grain on liquid metal) shows no tooltip at all where the row, and a seller, expects "how strong the effect is; not used by this shader"; the row read 8 of 11. The label span carries the same tip, so the wrapper is the place for it, or the disabled input keeps `pointer-events`.
18. B5 with B4, `MaterialMount.tsx` and the preset row (`shaders.panel.preset-tiles`): the preset write landed (diamond to ink-paper, the revision moved) and the row read the stage's canvas unchanged 936 ms later under reduced motion; a scratch probe's clip caught the filmstrip card instead of the stage, so the stage's timing after a preset change at speed 0 is unread here. The row's 500 ms window against the remount after a commit is the thing to read on the fix round, with the stage clip.
19. B5, `presets.ts` `shaderPaletteOfDeck` (`shaders.panel.kit-colours`): after `brand.set /colors/dark/primary '#0b3d91'` the shader's mean colour moved 0.0 and the frame key stayed `sha256:4de0e…` 6 s later; on the walk's deck the kit record the write makes does not reach the palette, so the row's re capture never comes. The six swatches read (text, background, caption, hint, primary, accent) and Custom.
20. B4, `e2e/core/shaders.spec.ts` (`shaders.frame.auto-capture`): with the key pattern widened, the spec derives the asset id as `frame-` plus the first sixteen characters of the key, which keeps `sha256:` inside ("frame-sha256:8524c8bd0" against B5's `frame-8524c8bd005242b0`); the id takes the sixteen hex after the prefix (`frames.ts` `frameAssetId`). Every other clause of the row passed on the deployment of record: the frame 2,597 ms after the insert, the new frame 2,050 ms after the Amplitude change with `backend: 'client'` and the renderer string.
21. B4 with B5, `e2e/core/shaders.spec.ts` (`shaders.frame.box-aspect`): the frame at the box's aspect (3200 by 810 for 600 by 152) came in 1,381 ms; the row's comparison of the viewer's picture against the editor at rest read 82 on one channel against its 48 on both tiers, the mount at anchor and the hosted or client still differing by more than the row allows on the software renderer; the tolerance, or the sample points, are the row's to read again.
22. B4, `e2e/core/shaders.spec.ts` (`shaders.frame.reuse-and-prune`, `shaders.frame.one-capturer`): the spec counts every asset with `source.kind: 'material'` as a frame, and the seeded deck carries its own `opener-prototemplate` material picture, so "one frame asset for the block" reads 2 after the prune and in the second browser's read while the block names one frame; the reuse (the 1.2 key's frame back in 5 ms) and the one capturer rule (no `shader.frame` request from the first page) held. The count wants the `frame-` id prefix.
23. B4 with B7, `e2e/core/export.spec.ts`, `lib.ts` (`shaders.export.pdf-frame`, `shaders.export.html-frame`): on the deployment of record both files carried the frame (the PDF's largest image 3200 by 1734 FlateDecode among four image objects and no label text; the web page's frame inlined as a JPEG data URI at 3200 natural, 0 canvas, no label words) and the spec's samplers (`largestPdfImage` re wrapping the image as a PNG, `sampleClip` over the data URI) read nothing ("samples unread"), where the same rows passed on the memory tier with the checkout's render worker; the hosted files' encoding (the predictor or the JPEG) is what the samplers do not read.
24. B7 with B5, `apps/studio/src/server/actions.ts`, `packages/materials/src/actions.ts` (`shaders.agent.list-insert-set-render`): `shader.render` answered 500 in 153 ms with no bytes on the deployment of record, where the checkout answered a 350,337 byte PNG in 13.6 s; the three other actions answered 200. A 153 ms refusal is before any browser launch: the render worker's queue or its browser on the function (the capture route B7 bounded works there: `shaders.background.place-answers` placed its ground). The function's log has the cause; this step reads no Vercel log.

### 7. The pictures

`docs/gslides-parity/features/build/integrator/pictures-ship-two.mjs` (playwright-core, one Chromium, one context per width and appearance with the chrome's own `ts-chrome-appearance` record set before the page runs, each context making its own `/new` deck through the window API and trashing and deleting it forever before it closes; `--only <names>` re shoots named surfaces) took every new surface of the ship on the deployment of record at 1440 and 1280 in both appearances, twenty files `<surface>-<width>-<appearance>.png` in this folder:

| Surface                     | What the picture shows                                                                                                                                                                                                                                                                                            |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `gallery`                   | Insert > Shader: the dialog titled Shader, the sentence "Previews are in black and white. The shader takes your brand kit’s colours on the slide", the search field focused, the six chips with All pressed, the cards two a row in the featured order with their stills decoded and the preset tiles under each. |
| `shader-section`            | The liquid metal diamond inserted by the first card, selected on the stage with the chip Shader and eight handles, Format options open at the Shader section: the name, Change, the still's state, the Preset tiles with Diamond pressed, the six kit swatches and Custom, the Form group under them.             |
| `background-shader`         | Slide > Change background with the Shader row's Choose open: the compact two column gallery under the row, Place beside it.                                                                                                                                                                                       |
| `view-play-shaders`         | View > Play shaders open: On, In the show only (checked), Off.                                                                                                                                                                                                                                                    |
| `show-options-play-shaders` | The show (`/deck/<id>?present=1`) with its Options menu open at Play shaders: the three rows, In the show only checked, the shader's frame on the slide.                                                                                                                                                          |

The `gallery`, `shader-section` and `background-shader` files are from the deployment of record (20:08Z); the first preview's copies drew empty plates for every still (section 4) and were replaced. The `view-play-shaders` and `show-options-play-shaders` files are from the first preview (19:56Z to 20:02Z), whose View menu and show read the same code. The Download dialog's report row (`dialog.download.report.shaders`) has no picture: it draws while an export waits for a pending frame, which the row `shaders.export.missing-frame-row` measures and this folder's script does not stage.

### 8. Deviations, and what the ship step still owes

- A first attempt of this step ended without a report after committing the four lanes and landing part of the requests (B5 R1 to R5, the model.ts half of B1 R1); this attempt kept its branch and commits and went on from its working tree, as the prompt for a second attempt says.
- The ship's parked list was committed empty before the runs (`ship-e2c6a7a.json`, the prompt's rule) rather than carried forward from `ship-f1afe1e.json` as b4.md 192 suggests; the gate therefore drove ship one's ten parked rows as ordinary rows, and their readings are in section 5 for the ship step, which writes the list of record from the two preview runs' `wouldParkRows` with the previous list carried forward where its rows did not read green, and runs `--emit-parked` on it before the production deploy (7.2). `parked-controls.ts` was not touched by this step.
- Two enforce previews were deployed, not one: the first (`turboslide-nneo3yypv`) served no gallery still (section 4) and was superseded by `f3c1b45` before any gate run; the two runs of record, the smoke, the write probe and the pictures of the gallery, the section and the Background row are on the second (`turboslide-8q880rwic`). Two of the pictures' surfaces were taken on the first, whose code for them is the same.
- The integrator edited lane files to resolve seams, none reverted: `packages/materials/src/previews.ts` (B5, the URL form), `packages/materials/src/actions.ts` `commitFrame` (B5, B7's R2), `scripts/probes/core-walk/areas/shaders.mjs` (B4, the show's Options read), `apps/studio/src/server/actions.ts` (B7, the `placeInsert` guard), `apps/studio/src/components/Slideshow.tsx` (B1's ready hunk), and prettier `--write` on eight lane files (b1.md, b7.md, their drive scripts, `download.ts`, `shader-frames.test.ts`, `background-shader.test.tsx`, `shader-gallery.test.tsx`, `report.ts`, `report.test.ts`, `scene/shaders.ts`, `scene/shaders.test.ts`).
- `turboslide lint --chrome`'s first pass gave `/deck/gt-brand` the editor states and it refused on `menubar.file`; the deck page was run again without `--states`, as `scripts/check.mjs` step 18 runs it (section 3).
- The pictures script's first cleanup called `deck.remove` without `confirm: true`, so its twelve scratch decks stayed in the shared store's trash while the deck page's 404 read as a removal; the store listing of 20:11Z (the sweep script in the scratchpad, `deck.list` with the trash through the preview with the bearer) showed them and each was trashed and deleted forever at 20:12Z (`trash 200, remove 200; /edit answers 404`), the store back at 71 decks (55 live, 16 trashed); the script carries the flag now. The live `untitled-20260924-melp` "Acme renewal deck v3" written at 20:11Z is not this step's (this step's local gate writes a tmp store; the hotfix worktree runs its own gates on the shared store) and was left alone.
- `scripts/check.mjs` runs `pnpm install --frozen-lockfile` as its own step 1; this step ran no `pnpm install` of its own. Its first pass stopped at step 5 (`pnpm test`, the whole workspace: 2 failed of 4,265) on `packages/theme/src/brand.test.ts`, whose facts read 186 actions and 163 MCP tools against the tree's 192 and 169 after B5's six actions: the brand facts were rebuilt (`node scripts/build-brand.ts --facts`, then `node scripts/build-home-assets.ts` for the home twin with the committed pictures kept) and committed, and the chain resumed from step 5 (section 3).
- The preview's run 1 was started once at 22:17Z and stopped after eight minutes for the drivers' commit (the spec's frame key pattern), then started again at 22:19:41Z from `811fe11` as the run of record; the stopped start's scratch decks were swept with the runs' (below).
- The runs' leftover decks: the store listing after the runs (03:22Z, 97 decks against the baseline's 71) held 27 decks new since the baseline; 23 were this step's (two probe decks whose cleanups had failed silently, four probes' "Untitled presentation" decks, the two chrome spec decks of the stopped start, and the specs' fixture decks of both runs: `Thumbnail deck`, `Northwind renewal`, `import-bundle-source-*`, `northwind-renewal-v*-without-notes`, `gt-brand-*`, `Brand spec deck`, `Assist spec deck`, the write probe's typed title) and each was trashed and deleted forever (`trash 200, remove 200; /edit answers 404`, `ship-two-sweep-after-runs.log` in this folder), with the cost reading's own `untitled-20260925-m8kl` after it; four were not and were left alone: `untitled-20260925-v8t9` "LMAo" (00:11Z) and `untitled-20260925-3gb4` "this is google slides (better)" (03:18Z), human titles written on the shared store while the runs went, and `untitled-20260925-pc8u` "Acme renewal deck v3" (00:26Z), a walk's deck in a window where this step's runs were in their specs phases, the hotfix worktree's. The store closed at 73 decks (57 live, 16 trashed): the baseline's 71 less the hotfix's `untitled-20260924-melp`, which left on its own, plus those three.
- The `VERCEL_OIDC_TOKEN` was not refreshed: no run met a 401 (the token outlived the prompt's 03:02Z through run 2's last cost rows and the third reading at 03:21Z to 03:28Z).
- The ship step still owes: the list of record and `--emit-parked` (above), the production deploy and its one run after the alias moves, the release note in `docs/updates.md` and the README's "Advanced tools" section rewritten from the runs, the verifier's hand drive, and the measurement row's number and the show's screen sharing frame (5.6) in the ship note.

### 9. The run ledger

| Run                                                                       | Origin                               | Tree                                | Started              | Rows                                                                                              | Ledger                      |
| ------------------------------------------------------------------------- | ------------------------------------ | ----------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------- | --------------------------- |
| the core gate on 4418, run 1                                              | http://localhost:4418 (memory)       | `f3c1b45`                           | 2026-09-24T19:52:29Z | 814: 752 passed, 34 failed, 28 not driven                                                         | `ship-two-gate-4418-run1-*` |
| the re drive, pass 1 (shaders walk; assist, chrome, logos, shaders specs) | http://localhost:4418 (memory)       | `2f64bd8`                           | 2026-09-24T21:46:52Z | walk 14 shaders rows; specs 27: 18 passed, 6 failed, 3 not driven                                 | `ship-two-4418-redrive1-*`  |
| the re drive, pass 2 (shaders walk and spec, Amplitude)                   | http://localhost:4418 (memory)       | `2f64bd8` plus the drivers          | 2026-09-24T22:03:24Z | walk 14 shaders rows: 4 passed, 6 failed, 4 not driven; spec 10: 2 passed, 7 failed, 1 not driven | `ship-two-4418-redrive2-*`  |
| the preview, run 1 of record                                              | turboslide-2lbzoh8o7 (blob, enforce) | `811fe11` on the build of `2f64bd8` | 2026-09-24T22:19:41Z | 814: 755 passed, 40 failed, 19 not driven; cost over 0                                            | `ship-two-preview-run1-*`   |
| the preview, run 2 of record                                              | the same                             | the same                            | 2026-09-25T00:52:45Z | 814: 758 passed, 36 failed, 20 not driven; cost over 2                                            | `ship-two-preview-run2-*`   |
| the two cost rows, a third reading                                        | the same                             | the same                            | 2026-09-25T03:21:37Z | 5.4                                                                                               | the scratchpad              |

Not runs of record: the first start of the preview's run 1 (22:17Z, stopped at eight minutes for the drivers' commit); the pictures, the hosted smoke and the write probe on each of the three previews (section 4); the hand probes of section 6 (the scratchpad's `thumb-probe.mjs`, `section-probe.mjs`, `slider-probe.mjs`, `drag-mimic.mjs`, `panel-probe.mjs`, `sweep.mjs`). Retries zero in every run.

## Features round, ship two, fix round

The integrator's fixer, 2026-09-25 (times UTC, 14:05Z to 14:25Z). One finding was assigned, VERIFICATION.md "Features round, ship two, pass 1" F.1 sixth bullet and F.10 item 4 (severity 2, `cost.editor-idle.calls` with `cost.two-tabs-idle.calls`): the store half of the idle row over its ceiling in the verifier's clean reading on the enforce preview (simple 53 against 40) and one listing on the two tabs row, with the function requests under their ceilings in every reading. Its fix as written is the ship step's, not a code change: read the cost rows again on production after the rebase onto `origin/main`, whose pulse cadence this branch does not carry, and weigh the store half against the P0 ceiling before the production run. Everything below happened on this machine and in this tree at `3bd34f8` (the tree as the verifier left it: `VERIFICATION.md`'s pass 1 section uncommitted and the 64 pictures untracked under `focus/verification/`; B4's and B7's fixers editing `e2e/core/lib.ts` and `vite.deploy.config.ts` beside this fix); no product file, no lane file, no driver and no matrix row changed by this fix; nothing was committed (the ship step commits) and no git write command ran; nothing on Vercel or GitHub was created, changed or deleted; no dev server was started, no lock folder made (a deployment base takes none), and no token, cookie or secret printed. `packages/store/src/pulse.ts`, `packages/realtime/src/blob.ts` and `room.ts` were read and not edited, as the round's prompt says.

### 1. What was read

- The branch against `origin/main`. `origin/main` is at `da00b5e` (2026-09-25 08:12Z), eight commits past the merge base `2be923e`: the ship one hotfix (`f89b376` the logo routes' busy window and the pulse's roster across instances, `bb63032` the logo index snapshot, `1c292da`, `c9d4054`, `3c1f18a`, `88d8e21` the production table) with `bb9c28a` (the chrome hairline) and `da00b5e` (the template rows skip on production; the gate puts Blank back after its drivers). The branch is fourteen commits past the base. The files changed on both sides are two, `apps/studio/e2e/core/lib.ts` (the branch's export and shader helpers; `origin/main`'s `isProductionBase` at 900) and `docs/gslides-parity/focus/core-matrix.json` (the branch's 29 shader rows; `origin/main`'s one changed row, `chrome.separators.once`, no id added or removed). A three way merge of each in the scratchpad (`git merge-file`, no repository write) is clean, for the committed `lib.ts` and for B4's fixer's working copy of it at 14:17Z alike; the merged matrix parses at 814 rows with 814 distinct ids, and one branch commit touches either file (`caae128`, B4's rows and drivers), so the ship step's rebase has one commit to replay over them and no conflict to resolve on this reading. The hotfix's files (`pulse.ts`, `blob.ts`, `channel.ts`, `logos.ts`, `logo-index.ts`) are untouched on the branch: the branch's only store or realtime change is the new `packages/store/src/frames.ts` with its test.
- Production. `vercel inspect https://turboslide.vercel.app` (read only, 14:08Z): `dpl_4hTgBSidwHSte78caHrmsdsRYfdq` (`turboslide-1dq2jhyp5-kl01s-projects.vercel.app`), target production, Ready, created 08:12Z, its build log opening on `Commit: da00b5e`. Production serves `origin/main`'s head, so the hotfix's pulse is on production and a cost reading there today is the reading the finding asks for, before the ship's own code joins it.
- The mechanism, from the diff `2be923e..origin/main`. Before the hotfix, the blob channel counted a roster row as this instance's own when this instance's presence route had set it; on a deployment a tab's presence POST and its stream land on whichever instances are warm, so the instance holding a lone tab's stream read the tab's own row as company whenever the POST had committed elsewhere, and `pollTickMs` ticked at the active pace (`HOSTED_POLL_MS` 2,000 ms, 30 heads a minute) instead of the quiet one (`HOSTED_POLL_QUIET_MS` 10,000 ms, 6 a minute; `blob.ts` 491 to 492 pass both). That is the shape of the preview readings: the integrator's run 2 sampled `head` 35 on one instance with the probe's own 3 (simple 43), the third reading 47, the verifier's clean reading heads 46, 34 and 23 (simple 53); run 1 read 27 over two instances and the verifier's first phase 31, and the readings before this ship on the same code (ship one's preview 31 and 30, its production 31, the sync ship's production 28) sit in the same band, the instances' routing deciding where in it a window lands. The hotfix's `pollCompany` (`pulse.ts`) matches a roster row to the stream's own client id (`channel.ts` `SubscribeOptions.clientId`, set by the stream route), so a lone tab is alone on every instance and ticks at the quiet pace; the hotfix's production run of record read the idle row's store at simple 13, advanced 9 (`gate-production-features-one-hotfix.json`).
- The two tabs row's one listing. Run 2's sample and the verifier's carry `lists.assets` 1: `ensureAssets` (`blob-store.ts` 2905) pulls `assets/` on an instance's first open of the deck, the sync round's measured first open listing (`sync.pull.no-listing`'s own text names it), and a second browser on a second instance is that first open. Nothing of this ship lists on those rows: the orphan prune of 5.5 (`packages/store/src/frames.ts` `pruneFrameAssets` over `assets/`, run by `server/shader-frames.ts`) is scheduled behind a `shader.frame` or `shader.capture` response alone (`server/actions.ts` `FRAME_WRITE_IDS`, once a minute per deck per instance), and the cost probe's deck holds no shader; the logo cache and the exports ride the existing records (docs/SYNC.md 4). No route of the branch adds a timer.

### 2. The production reading

`node scripts/probes/core-gate.mjs --base https://turboslide.vercel.app --only cost --out <scratch>/fix-two/production-cost-fix --matrix <scratch>/fix-two/production-cost-fix/ledger.json --parked docs/gslides-parity/focus/ship-e2c6a7a.json`, detached from the checkout at 14:09:46Z with the bearer read by the probe from the production row of `~/.config/turboslide/hosts.json` (production is not behind Vercel Authentication; the development OIDC credential in `.turboslide/vercel-dev.env` was read for its expiry alone, 2026-09-26 01:57Z, refreshed by another pass) and polled through its log. The rows in the order the probe drives them, read by id from each row's JSON as it landed; this fix was returned at 14:25Z with the last two rows not yet reached (the harness closed the fix before the gate ended), so their readings are in the gate's own files when the process ends, about 14:35Z (`production-cost-fix/ledger.json`, `cost-probe.json` and the row files under `cost-probe/`), and are not claimed here. The probe's decks are each trashed and deleted forever by the row's own teardown (`/edit` and `/deck` 404 in the log for the four rows read); the store carries nothing of the rows read.

| Row                         | Production, this reading (the hotfix's pulse, `da00b5e`)                                                                                                                                                                                | The hotfix's production run of record (`3c1f18a`, 2026-09-25 06:56Z) | The preview readings of ship two (`811fe11` on `2f64bd8`, the pulse before the hotfix)                                                                                         | Ceiling                              |
| --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------ |
| `sync.pull.no-listing`      | passed at 14:10:51Z: 10 commits landed, `list` 0 in the window, `lists.versions` 0                                                                                                                                                      | passed                                                               | passed                                                                                                                                                                         | `lists.versions` 0                   |
| `cost.editor-idle.calls`    | passed at 14:14:24Z: function 9.99 a minute (30 in 3 min); store simple 11 (3 of the heads the probe's own), advanced 9; `head` 11 in every sample, `get` 0, `list` 0; one instance; the store settled 2.32 s after the state was ready | function 9.66; simple 13, advanced 9                                 | function 8.99, 9.66, 9.65, 9.66; simple 27 (run 1, two instances), 43 (run 2), 47 (the third reading), 31 and 53 (the verifier's two); advanced 13 in run 2, 9 to 10 otherwise | function 12; simple 40, advanced 11  |
| `cost.editor-hidden.calls`  | not driven at 14:18:04Z by its rule (`visibilityState` read visible after a second page was brought to the front); the visible tab's samples read `head` 26 to 30 with the second page open, the active pace with company               | not driven by its rule                                               | not driven by its rule                                                                                                                                                         | function 8                           |
| `cost.editor-editing.calls` | passed at 14:23Z: function 69.95 a minute (210 in 3 min); store simple 80 (`head` 62, `get` 18), advanced 66 (`put` 64, `list` 2: `assets` 2, `snapshots` 1); two instances; the store settled 2.76 s after the state was ready         | function 68.94; simple 126, advanced 74                              | function 71.28, 74.59 and under; simple 137 to 143, advanced 49 to 65                                                                                                          | function 75; simple 140, advanced 85 |
| `cost.two-tabs-idle.calls`  | not reached at the return; read the row's JSON                                                                                                                                                                                          | advanced 3, `list` 0                                                 | `list` 0, 1, 0, 0, 1; advanced 15 to 19                                                                                                                                        | `list` 0; advanced 30                |
| `cost.show.calls`           | not reached at the return; read the row's JSON                                                                                                                                                                                          | 0 function; the page's store calls 0 (the rerun under Blank)         | 0 function; the page's 0 store calls after a 107 s settle (the verifier's clean reading)                                                                                       | 0; 0                                 |

The weighing the finding asks for, on the rows read: the editing row holds with room (function 69.95 against 75, simple 80 against 140, advanced 66 against 85); the idle row's store half on production with the hotfix's pulse is simple 11 against 40 and advanced 9 against 11, the quiet pace (`head` 11 a minute, of which 3 the probe's own and 6 the tick), where every reading on the pre hotfix preview sat between 27 and 53; the function requests are 9.99 against 12 (30 in the window against 29 in ship one's and the hotfix's readings; the one more request was not attributed by this fix). Nothing of this ship is on production yet, so the reading measures the pulse alone, which is what the verifier attributed the red to; the rebased preview's two runs measure the ship's code under that pulse.

### 3. For the ship step

1. Fetch `origin` and rebase `features/ship-two` onto `origin/main` before the enforce preview (the round's prompt); the two shared files merge clean on this reading (section 1) and `caae128` is the one commit to replay over them. After the rebase run `node scripts/probes/core-matrix.mjs` (814 rows) and `node docs/readme/what-works.mjs --check` (`origin/main` changed neither `docs/FOCUS.md` nor the README, so both should read current), and `node_modules/.bin/tsc -b` once, since `da00b5e`'s `lib.ts` hunk lands beside the branch's and B4's fix round edits of the same file.
2. The rebased preview's two runs are the reading of record for the store half (docs/SYNC.md 6.1: a measure row holds the ship only when over its ceiling on the preview): with the hotfix's pulse under the ship's code, expect the idle row's store at the production shape above (simple about 11 to 13, `head` at the quiet pace, one instance) and not the 27 to 53 of the pre hotfix preview; a reading over 40 on the rebased preview is a new mechanism, not the pulse's, and is read from the sample's `head`, `get` and `lists` by folder before the production run. The two tabs row's `list` 1, when it comes, is the second instance's first open pull of `assets/` (section 1), the recorded class of docs/FEATURES.md 7.2 (the blob tier's timing) when red once.
3. Read the cost rows on production once more in the production run of record, as 7.2 says, and put the store halves in the production table beside the function requests; the table of section 2, completed from the gate's files, is the baseline for that comparison.
4. `da00b5e`'s gate reads `template.list` after its drivers on every deployment base and puts Blank back; the rebased gate carries it, so the production run's templates rows skip with their reason and the preview run's readings stand for them (hotfix.md item 13).

### 4. Tests and checks

- `packages/chrome` `src/menus/__tests__` (the integrator's files, unchanged by this fix): 7 files, 127 passed, 1.06 s at 14:14Z. Beside them, the packages the mechanism lives in, unchanged by the branch and by this fix: `packages/store` 21 files, 259 passed, 1 skipped (11.6 s); `packages/realtime` 10 files, 138 passed (11.7 s); `apps/studio` `server/shader-frames.test.ts` and `server/agent-actions.test.ts` 17 passed (the prune's schedule).
- No TypeScript, no product code, no driver and no matrix row changed, so no other gate row was re driven; the rows this fix touched are the cost rows, re read on production in section 2.
- The other lanes' notes carry no "Features round, ship two, fix round" heading at 14:19Z (B4's and B7's fixers were editing their files at the time); no request to the integrator was open to answer (the day 0 requests are all in this note's ship two section 2). A request they write after this return is the ship step's to read.

### 5. Deviations

- The reading of section 2 is on production before the rebase, not after it: production serves `origin/main`'s head and the ship's code is not on it, so it is the baseline the finding asks for (the pulse the branch does not carry, measured alone) and not the production run of record, which the ship step runs after the alias moves. Nothing of the ship's code was measured on the blob tier by this fix; the rebased preview's two runs do that.
- The gate was still running at the return (two rows of six unread, the two tabs row and the show row): the harness closed the fix at 14:25Z, fifteen minutes into a run that takes about twenty five. The process is detached and finishes on its own with each row's teardown; its files are the scratchpad's, not `build/integrator/`'s (the ship step's ledgers are the runs of record and this is a reading beside them), and the ship step reads them before it writes the production table's baseline.
- The vector round's verifier was running its own gate on the shared Blob store (a fixture preview and a localhost server) throughout this fix; the store counters are per instance and per deck, so its decks do not enter this reading, and its processes were not touched.
