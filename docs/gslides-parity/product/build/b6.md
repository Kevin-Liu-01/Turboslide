# B6 The assist and the agent surface

The note of builder B6 of the product round (2026-09-19, times in UTC). The binding specification is `docs/PRODUCT.md` (section 6 the assist, section 5 the tailoring pass, section 7 the lanes and the control ids, section 8 the rows); `docs/RETURN.md` section 8, `docs/FOCUS.md` section 6 and `AMENDMENTS.md` A1 stand under it; `docs/gslides-parity/product/audit-assist.md` is the evidence. The dev server is port 4416. No git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran. No token, cookie or secret is printed here.

## Product round

### 0. Requests, written first so the other lanes can land them while this lane builds

Every request names the file, its owner under PRODUCT.md section 7, and the exact change. This lane's own files compile and pass their tests without them; the rows they unblock are named.

**R1 (the integrator, `packages/chrome/src/menus/model.ts`; unblocks `assist.entry.title-row`, `assist.panel.first-line-and-cards`, `assist.tailor.dialog-one-undo`, `assist.finder.ask-row`).** Three rows, exactly as PRODUCT.md 6.1 and section 5 name them:

- In `TITLE_ROW_ITEMS`, between `title.presence` and `title.comments`:
  ```ts
  now('title.assist', 'Assist', panel('Assist'), {
    turboslide: true,
    icon: 'sparkles',
    key: shortcut('Cmd+J'),
    when: 'write',
    doc: 'Tailor the deck for a customer, ask for a shorter slide or for speaker notes; nothing changes until you accept',
  }),
  ```
- In `TOOLS.items`, before `tools.checkSlides`:
  ```ts
  now('tools.tailor', 'Tailor for a customer', dialog('Tailor for a customer'), {
    turboslide: true,
    icon: 'sparkles',
    dividerBefore: true,
    when: 'write',
    doc: 'Replaces the customer name everywhere, swaps the pictures named after the old one and skips slides, as one change',
  }),
  now('tools.assist', 'Assist', panel('Assist'), {
    turboslide: true,
    key: shortcut('Cmd+J'),
    when: 'write',
    doc: 'Tailor the deck for a customer, ask for a shorter slide or for speaker notes; nothing changes until you accept',
  }),
  ```
- No `terms` field is needed on `MenuItem`: the seller's words live in `packages/chrome/src/menus/finder.ts` (`SELLER_TERMS`, keyed by item id; this lane's file), and `finder.ts` reads an optional `terms` array off an item when one exists, so adding the field later costs nothing. The parks rule of 8.1 (`assist` parks `title.assist`, `tools.assist`, `tools.tailor` and the Ask row) needs no `advanced` flag today; B4's `parks` on the rows names these ids.

**R2 (B1, `packages/chrome/src/editor-shell.ts` and `EditorShell.tsx`, `TitleRow.tsx`; unblocks the same rows).**

- `editor-shell.ts`: `PANEL_IDS` gains `'assist'` and `panelIdOfTitle` gains `case 'Assist': return 'assist';`; `DIALOG_IDS` gains `'tailor'` and `dialogIdOf` gains `case 'Tailor for a customer': return 'tailor';`.
- `EditorShell.tsx`: a lazy dialog `const TailorDialog = lazyDialog(() => import('./dialogs/Tailor').then((m) => m.TailorDialog));` with `case 'tailor': return <TailorDialog.Component />;` in the dialog switch; in the panel switch:
  ```tsx
  case 'assist':
    return (
      <AssistPanel
        document={input.document}
        slideId={input.slideId}
        revision={input.revision}
        dispatch={input.dispatch}
        canWrite={input.capabilities === undefined || input.capabilities.includes('write')}
        restricted={input.access?.record?.generalAccess?.mode === 'restricted'}
        initialPrompt={assistPrompt}
        onTailor={() => openDialog('tailor')}
        say={say}
        onClose={closePanel}
      />
    );
  ```
  with `import { AssistPanel } from './panels/Assist';`, a `const [assistPrompt, setAssistPrompt] = useState<string | undefined>(undefined);` and the ToolFinder given `onAsk={(phrase) => { setAssistPrompt(phrase); setToolFinderOpen(false); openPanel('assist'); }}`. `AssistPanel`'s props are `AssistPanelProps` in `packages/chrome/src/panels/Assist.tsx`; `dispatch` is the shell's `EditorDispatch` and the panel runs `assist.propose` and `assist.accept` through it (the controller's window handlers post to `/api/assist` and commit; section 2 below).
- `TitleRow.tsx`: the Assist button between the presence slot and the comments slot, a `ToolButton` like the comments glyph, `control="title.assist"`, `menuItem="title.assist"`, `icon="sparkles"`, the label `Assist` beside the glyph, `pressed={shell.panel === 'assist'}`, drawn only while `isPresent(itemById('title.assist'), shell.menuContext)` (guard with `findItem` until R1 lands), the click `shell.panel === 'assist' ? shell.closePanel() : shell.runItem(item)`.

**R3 (the integrator, `apps/studio/src/server/actions.ts`; unblocks `deck_assist_propose` and `deck_assist_accept` over `/api/actions` and MCP; `/api/assist` serves them without it).** After `registerStoreActions(dispatcher, storeDeps);` in `deckDispatcher`:

```ts
registerAssistActions(dispatcher, { store: deckStore, deckId });
```

with `import { registerAssistActions } from './assist';`. The module reads the model configuration and the card secret itself.

**R4 (B7, `apps/studio/src/server/flags.ts`, `ratelimit.ts`, `packages/schema/src/access.ts`, `firewall/rules.json`, `apps/studio/vite.deploy.config.ts`, `apps/studio/package.json`).** As PRODUCT.md 6.3 names them: the `assist` flag in `FLAG_NAMES`, `FLAG_DEFAULTS` (on), `FLAG_OFF_MEANS` and `FLAG_REFUSALS` ("The assistant is off on this Turboslide"); the two quota rows `assistCallsPerMinutePerDeck` (anonymous 6, account 20, agent 60, `perDeck: true`, the sentence "Too many assistant requests. Try again in a minute") and `assistCallsPerDay` (anonymous `TURBOSLIDE_ASSIST_DAY_CAP` default 20, account 300, agent 1000); WAF rule R22 for `/api/assist`; the route's function directory with a 60 s `maxDuration`; `@anthropic-ai/sdk` on `apps/studio`. The route `apps/studio/src/routes/api/assist.ts` exists on the tree from this lane (the task prompt named it as this lane's; PRODUCT.md 7 names B7): it reads the flag through `flagOn('assist')` and the two quotas through `assertQuota` only when `QUOTAS` holds the row, so it works before and after R4; B7 takes the file over and removes the guards when the rows exist. The model call in `apps/studio/src/server/assist.ts` (`anthropicModel`) is a `fetch` to the Messages API behind the `ModelClient` type because the SDK is not installed on this tree and this lane may not add a dependency; once `@anthropic-ai/sdk` is on `apps/studio`, swapping the body of `anthropicModel` for `client.messages.create` is one function with the same request shape (model `claude-opus-5`, `output_config: { effort: 'low', format: { type: 'json_schema', schema } }`, `max_tokens` 4000, `fallbacks: 'default'` with the beta header `server-side-fallback-2026-07-01`, `cache_control` on the system block).

**R5 (the integrator or B7, `apps/studio/src/server/room.ts` and `apps/studio/src/routes/api/decks.$deckId.ops.ts`; the Version history half of `assist.mark.chip-and-history`).** A page's op is attributed to the session's principal (`room.ts` `authorOf(identity)`), so the seller's Accept, which commits through the editor's own `commit` (one undo step, the seller's lease and room), is recorded under the seller. The row asks that Version history list the assistant's revisions as their own rows named Assistant. The change: `OpsPost`'s edit entry gains an optional `author` of `authorSchema` restricted to `kind: 'agent'`; the admission keeps it as the entry's author when `kind === 'agent'` and `name === ASSISTANT_NAME` (`@turboslide/store/store`), with `principalId` set to the session's principal id so the audit trail keeps who accepted; every other value is ignored and the session's author stands, so a page can hand a write to the assistant and never to another person. `packages/realtime/client/room-client.ts` `apply(mutations, label, flush)` then takes an optional `{ author }` the controller's `acceptAssist` passes. Until this lands, Change history lists the write as "Assist: <sentence>" under the seller, the blocks carry `ext.assist` and the chip word, and the row is recorded not driven for its Version history check.

**R6 (B3, `packages/viewer/src/Selection.tsx` `blockDisplayName`, or `Editor.tsx`'s `chip`; the chip half of `assist.mark.chip-and-history`).** One line: when `assistMark(block.ext)` (`@turboslide/schema/ext`) is set on the selected block, the chip reads `Assistant` (the block's plain name otherwise). The mark is written on Accept and cleared by the seller's next edit of the block (the controller's `withAssistClear`, section 2).

**R7 (the integrator, `packages/chrome/src/menus/strings.ts`).** The assist's and the tailor's strings live in `packages/chrome/src/panels/assist-strings.ts` (this lane's folder) as `ASSIST` and `TAILOR`; the integrator may move them under `strings.ts` at the merge and re-export, or leave them.

**R8 (the integrator, `apps/studio/src/editor/EditorRoot.tsx`, two lines).** `lastEditBy` and the external banner's author read `authorLabel`; swap both to `authorDisplay` from `@turboslide/store/store` so an agent's write reads "Assistant" on the title row and the banner (`audit-assist` 15).

**R9 (B4, the matrix).** The Ask row's control id is `palette.finder.assist.ask` (the palette prefixes every row with `palette.`; the entry id is `finder.assist.ask`). The panel's card rows are `panel.assist.card.<n>` with `<n>` from 1; a card's buttons are `panel.assist.card.<n>.accept`, `.dismiss`, `.change`; the Tailor dialog's ids are the ones of 7.1. `panel.assist.starter.tailor` opens the Tailor dialog through `onTailor`. The propose over HTTP is `POST /api/assist?deck=<id>` with `{ action: 'assist.propose', input }` and the bearer, or `POST /api/actions/assist.propose?deck=<id>` after R3.

**R10 (the integrator, `apps/studio/src/editor/shell-bridge.tsx`, one line; the Undo button of `assist.outside-write.snackbar`, `assist.tailor.agent-deck-tailor` and the card's accept).** The controller's snackbar (`controller.say`) is the viewer shell's `say(msg)`, which takes no action, so the Undo of an outside write reaches the seller only through the editor shell's `say(text, action)`. The controller gained `attachEditorShell(api)`; in `ShellBridge`, after `api.current = editorShell;`, add `controller.attachEditorShell(editorShell);`. Without it every sentence of section 2 shows without its Undo button and the controller falls back to the plain snackbar.

**R11 (the integrator, the shared header and the count pins).** `packages/schema/src/actions.ts` gains `'P1'` in `Milestone` and `MILESTONES` (the product round's label for the three assist actions; `packages/agent/src/http/manifest.ts`'s order list reads it after `GS3`), and `packages/schema/src/actions-gs3.test.ts` pins `ACTION_IDS` at 169, which every lane's actions move (183 on this tree with B5a's and B5b's); the integrator updates the pin at the merge. The three ids sit before `presence.list` so `GS3_ACTION_IDS` keeps its 64.

### 1. What landed

Every file below is this lane's under PRODUCT.md section 7 or a new file beside one; the controller's edits are the parts section 7 gives B6 (the outside write snackbar and the `ext.assist` mark) plus the three window handlers the panel and the dialog need.

| Item (PRODUCT.md)             | Files                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 5, 6.2 the actions            | `packages/schema/src/actions.ts` (`deck.tailor`, `assist.propose`, `assist.accept`; the schemas `tailorInputSchema`, `assistCardSchema`, `assistProposeInputSchema`; `ASSIST_INTENTS`; the `P1` milestone), `packages/agent/generated/**`, `skills/*/references/*.md`, `docs/grammar.md` (regenerated with `node packages/agent/src/generate/main.ts`; the generation read the whole table as it stood, B5a's and B5b's in progress actions included, and the integrator regenerates at the merge) | The tailoring pass and the assist's two actions on every transport, CLI usages `turboslide tailor …`, `turboslide assist propose …`, `turboslide assist accept < card.json`, MCP tools `deck_tailor`, `deck_assist_propose`, `deck_assist_accept`                                                                                                                                                                                                                                      |
| 5 the tailoring pass          | `apps/cli/src/store-actions.ts` (`tailorPlan`, `tailorCount`, `deckTailor`, the `deck.tailor` registration; `textTargets` exported), `apps/cli/src/commands/tailor.test.ts` (6 tests)                                                                                                                                                                                                                                                                                                              | One write: the replacements over every visible text and the notes through `replaceInText` (never inside a link address or the GT mark), a second pair over the first pair's result, the pictures whose alt names the old customer swapped for the asset, the skips; a logo without `replaceAlt` refused with the reason until the brand kit gives the deck a logo slot                                                                                                                 |
| 5 the Tailor dialog           | `packages/chrome/src/dialogs/Tailor.tsx`, `panels/assist-strings.ts` (`TAILOR`), `__tests__/tailor-dialog.test.tsx` (3 tests)                                                                                                                                                                                                                                                                                                                                                                      | The live count ("5 places on 3 slides") over `slideStrings` (the same texts Find and replace reads), the logo step (`dialog.tailor.logo.everySlide` drawn disabled with its tooltip until the kit lands; `dialog.tailor.logo.replaceAlt` with a file chooser that runs `asset.add` first), the skip rows `dialog.tailor.skip.<slideId>`, Apply as one `deck.tailor`; the controller labels the commit "Tailor for <name>" and says "Tailored for <name>" with Undo                     |
| 6.1 the panel                 | `packages/chrome/src/panels/Assist.tsx`, `Assist.css`, `assist-model.ts` (the changed words by a word LCS, the ids), `assist-strings.ts` (`ASSIST`), `__tests__/assist-panel.test.tsx` (6 tests)                                                                                                                                                                                                                                                                                                   | The first line (the restricted clause when the deck is restricted), the slide line, the three starters, the `role="log"` region of asks, cards and sentences, a card with the sentence, the rows (before struck through, after with the changed words underlined), Accept, Dismiss, Change the ask, the composer (Enter sends, Shift Enter breaks), the busy and error lines, the viewer's sentence, the switch's sentence on a 503, the finder's phrase in the box                    |
| 6.1 the mark                  | `packages/schema/src/ext.ts` (`assistMark`, `withAssistMark`, `withoutAssistMark`), `apps/studio/src/editor/assist-accept.ts` (`planAcceptedCard`, `withAssistClear`, `assistTargets`), `assist-accept.test.ts` (3 tests), `controller.tsx` (`commit` clears the mark of an edited block; `acceptAssist` writes the plan as one commit labelled "Assist: <sentence>")                                                                                                                              | `ext.assist = { at, runId, card }` on every written block or slide field, cleared by the seller's next edit of that block in the same write; the chip word and the Version history row are R6 and R5                                                                                                                                                                                                                                                                                   |
| 6.1 the author label          | `packages/store/src/store.ts` (`ASSISTANT_NAME`, `authorDisplay`), `packages/chrome/src/dispatch.ts` (`authorName` reads Assistant, `authorRunId` keeps `agent:<runId>` for a tooltip), `__tests__/palette-data.test.ts` (the pin)                                                                                                                                                                                                                                                                 | Change history, the Versions list and the palette read Assistant for an agent author; the CLI's `authorLabel` keeps the run id                                                                                                                                                                                                                                                                                                                                                         |
| 6.1 outside writes            | `controller.tsx` (`announceAgentWrite`, `sayWithAction`, `attachEditorShell`, `beforeRemote`), `apps/studio/src/editor/auto-title.ts` (the docblock; the rule runs unchanged over the document before the remote op)                                                                                                                                                                                                                                                                               | A live write by an agent author raises "Assistant changed slide N: <before> became <after>" with Undo (the inverse over the document before the op, committed as this seller: one entry on the seller's stack, the seller's own last edit stays); the auto title follows a write to the first title whoever wrote it                                                                                                                                                                   |
| 6.1 Search the menus          | `packages/chrome/src/menus/finder.ts` (`SELLER_TERMS`, the doc sentence in the terms, an optional `terms` array on an item), `palette-data.ts` (`SUBSEQUENCE_MAX_NEEDLE`: no subsequence match for a needle of four characters or more), `ToolFinder.tsx` (`askEntry`, `assistPresent`, the Ask row `finder.assist.ask` drawn as `palette.finder.assist.ask`, present only while `title.assist` is in the model and drawn), `__tests__/tool-finder.test.ts`, `palette-data.test.ts`                | "hide slide" lists Skip slide first, "rename the customer" lists Find and replace, "logo" lists Replace image > Upload from computer and no Long dash dot, "bigger text" Increase font size, "talk track" Show speaker notes, "send a pdf" PDF Document; a phrase that matches nothing offers "Ask the assistant: <phrase>" whose Enter opens the panel with the phrase (`onAsk`, R2)                                                                                                  |
| 6.2, 6.3, 6.4 the server half | `apps/studio/src/server/assist.ts` (the prompt, the model client over the Messages API, the fixture switch, the answer validator, the HMAC signature, `proposeCards`, `acceptPlan`, `registerAssistActions`, `logAssistLine`), `assist-fixtures.ts` (`fixtureModel`, `shorterText`, `notesFor`), `assist.test.ts` (19 tests)                                                                                                                                                                       | `claude-opus-5`, effort low, `output_config.format` with a JSON schema whose target enum is the slide's texts, `max_tokens` 4,000, the system block cached and constant, `fallbacks: 'default'` under `server-side-fallback-2026-07-01`; the deck text fenced in the user turn; the budget cut by whole slides with "The assistant read the first N slides"; a card signed over deck id, mutations, base revision and expiry, ten minutes; the log line per call without the deck text |
| 6.3 the route                 | `apps/studio/src/routes/api/assist.ts` (the binding onto B7's `handleAssistRequest`; section 4 records the overwrite), the window handler `assistPropose` in `controller.tsx` (`fetch('/api/assist')` with the session, the error's status kept for the panel)                                                                                                                                                                                                                                     | `POST /api/assist?deck=<id>` `{ action, input }` from the page with its cookies or an agent with the bearer                                                                                                                                                                                                                                                                                                                                                                            |
| 6.2 the agent surface         | `packages/mcp/src/prompts.ts` (`DECK_ASSIST_PROMPT`, `deckAssistPrompt`), `server.ts` (listed and served), `prompts.test.ts`, `server.test.ts` (the prompt list pin), `skills/turboslide-studio/references/assist.md`, `SKILL.md`                                                                                                                                                                                                                                                                  | The `deck_assist` prompt tells an agent the two actions, the tailoring pass and the card rule                                                                                                                                                                                                                                                                                                                                                                                          |

The window transport: `invoke('assist.propose', input)` posts to the route with the page's session; `invoke('assist.accept', { card })` commits the plan through the editor's history (one undo step); `invoke('deck.tailor', input)` commits one entry labelled "Tailor for <name>". The three ids are on `describe().actions` as every window action is.

### 2. The control ids on this tree

`panel.assist`, `panel.assist.close`, `panel.assist.firstLine`, `panel.assist.slide`, `panel.assist.starters`, `panel.assist.starter.tailor`, `.shorter`, `.notes`, `panel.assist.log`, `panel.assist.ask`, `panel.assist.sentence`, `panel.assist.accepted`, `panel.assist.card.<n>`, `panel.assist.card.<n>.row.<slideId>`, `panel.assist.card.<n>.accept`, `.dismiss`, `.change`, `panel.assist.busy`, `panel.assist.error`, `panel.assist.prompt`, `panel.assist.send`, `panel.assist.viewer`, `panel.assist.off`; `dialog.tailor`, `dialog.tailor.from`, `dialog.tailor.to`, `dialog.tailor.count`, `dialog.tailor.logo.everySlide` (disabled on purpose: not driven, never broken), `dialog.tailor.logo.replaceAlt`, `dialog.tailor.logo.file`, `dialog.tailor.skip`, `dialog.tailor.skip.<slideId>`, `dialog.tailor.apply`, `dialog.tailor.error`; `palette.finder.assist.ask`; `toolFinder.frame`. `title.assist`, `tools.assist` and `tools.tailor` are the model rows of R1.

### 3. Tests and checks

- `node_modules/.bin/tsc -b apps/studio/tsconfig.json`: clean for this lane's files; the errors on the tree are other lanes' in progress work (`packages/schema/src/layouts.test.ts` `subtitle-body`, B3; `packages/fonts/src/catalog.test.ts` `font.list`, B5a).
- vitest, per package from its folder: `apps/studio` 68 files, 449 tests pass (`assist.test.ts` 19, `assist-accept.test.ts` 3); `apps/cli` 22 files pass (`tailor.test.ts` 6); `packages/mcp` 44 pass; `packages/chrome` 71 files, 650 of 652 pass, the two red tests other lanes': `editor-shell-render.test.tsx` "closes on Esc with focus back on the title" (B1's `MenuBar.tsx` now returns focus to the stage, PRODUCT.md rank 29) and, before this lane's pin moved, `palette-data.test.ts` "author and note" (now pins Assistant); `packages/store`: `template-index.test.ts` red is B5b's; `packages/schema`: the 169 pin (R11) and B5b's `template.rename` without `baseRevision`; `packages/agent`: B5b's `template.*` examples without `baseRevision`.
- `node_modules/.bin/eslint` over this lane's files: clean. `node_modules/.bin/prettier --write` over them.
- `node packages/agent/src/generate/main.ts` wrote 10 of 14 files; `node_modules/.bin/tsr generate --config apps/studio/tsr.config.json` added the route to `routeTree.gen.ts`.

### 4. Deviations and the one collision

- The route file `apps/studio/src/routes/api/assist.ts` is B7's under PRODUCT.md 7; the lane prompt named it as B6's. This lane wrote it at 04:26Z on the 20th and the write reported the file as existing: B7 had created it minutes before, and this lane's version replaced theirs (an untracked file, so nothing to diff back). B7's logic lives in `server/assist-route.ts` (`handleAssistRequest`, 10 tests) and was untouched; at 04:40Z this lane rewrote the route as the binding onto it with the real seams (`requestIdentity`, `decideFor`, `flagOn`, `checkAssistQuotas`, `authorOf`, the deck dispatcher with `registerAssistActions` registered when it lacks the handlers, `refuseCrossSite`, `refuseNonJson`, `readJsonBody`), which is what B7's note section 2 describes. B7 reviews and owns it from here.
- The model call is a `fetch` to the Messages API rather than `@anthropic-ai/sdk` (R4): the SDK is on `apps/studio/package.json` now (B7) but not installed on this tree, and this lane may not run `pnpm install`. `anthropicModel` is one function with the SDK's request shape; the swap is its body.
- The Accept write from the page is recorded under the seller's session (R5), with `ext.assist` on the blocks and the label "Assist: <sentence>" in Change history; the server side accept (HTTP, MCP, the CLI) is recorded under the author Assistant with the note "Assist: <sentence>", as `assist.test.ts` "proposes over the store, accepts as the assistant" pins.
- `SELLER_TERMS` is a table in `finder.ts` keyed by item id rather than a `terms` field on `MenuItem` (R1): the same behaviour, no shared file edit; `finder.ts` also reads a `terms` array off an item when one exists.
- The three actions carry the milestone `P1` and sit before `presence.list` (R11).
- `apps/studio/src/editor/assist-accept.ts` is a new file beside `auto-title.ts` (not named in section 7): the pure half of Accept shared by the page and the server, so the re base and the marks are one implementation; its test names it.

### 5. The drive

Every run was against this lane's dev server on 4416 (`apps/studio`, the tmp store under the scratchpad, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, `TURBOSLIDE_ASSIST=fixture` so the model route answered from `server/assist-fixtures.ts`), under `.turboslide/e2e.lock`, on 2026-09-20 UTC. The probes and their logs sit in the session scratchpad (`scratchpad/b6/`): `assist-probe.mjs` (HTTP and finder rows), `assist-probe-page.mjs` (the window transport rows), `assist-probe-*.log`, `walk-assist-*.log`, `spec-assist-*.log`.

- Run 1, 04:31:30Z to 04:38:20Z: the server had `TURBOSLIDE_TOKEN` set, so every cookieless HTTP call from the probe met the bearer rule, and the probe read the deck id from the URL before `/new` redirected (`No deck new`). Its HTTP rows are void; the finder row passed. B4's walk `--only assist` and `core/assist.spec.ts` ran in the same chain with the same fault.
- Run 2, 04:39:48Z to 04:45:45Z: the server restarted without the token; the scratch probe, B4's `editor-walk-probe.mjs --core --only assist` (28 steps: 18 ok, 5 failed, 5 not driven, 0 console errors) and `core/assist.spec.ts` (1 failed in teardown, 4 skipped).
- Run 3, 04:52:58Z to 04:53:10Z: `assist-probe-page.mjs`, the assist actions through `window.turboslide.studio.invoke` on the open editor (6 steps: 5 ok, 1 failed).

Per row, as observed:

| Row                                                                                                              | Outcome                                                         | What happened                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| assist.tailor.agent-deck-tailor                                                                                  | failed                                                          | `deck.tailor` over HTTP answered 200 (`replacements: 2`, the slide skipped, revision 2 to 3 in the scratch probe; 14 to 15 in walk row 23, Acme 3 to 0 and put back). The open editor showed the change and the snackbar read "Assistant changed slide 1: Acme pricing at a glance became Globex pricing at a glance" within the poll. No Undo button: the snackbar's action needs `attachEditorShell` (R10).                                                                                                                                                                                                               |
| assist.outside-write.snackbar                                                                                    | failed                                                          | `text.replaceAll` as `agent:assist-probe` answered 200 while the editor was open; the seller's own edit stayed; the snackbar named the change after 156 ms (walk row 24); Undo false (R10). The deck title stayed "Untitled presentation" through both agent writes.                                                                                                                                                                                                                                                                                                                                                        |
| assist.agent.propose-accept                                                                                      | not driven                                                      | Cookieless `POST /api/assist?deck=<id>` answered 403 `{"error":"forbidden"}`: B7's `handleAssistRequest` asks for the `comment` capability and the cookieless localhost identity is denied. `POST /api/actions/assist.propose` answered 501 "lands in P1" (R3). Walk row 27 the same. The tampered card refusal ran in `assist.test.ts` only.                                                                                                                                                                                                                                                                               |
| assist.finder.terms                                                                                              | passed, one gap                                                 | "hide slide" gave Skip slide first (Unskip slide in run 2, the tailor step had skipped the slide); "rename the customer" gave Find and replace; "logo" gave Replace image; "bigger text" gave Increase font size; no Long dash dot. Walk row 25 fails on `palette.menu:tools.tailor` (R1).                                                                                                                                                                                                                                                                                                                                  |
| assist.finder.ask-row                                                                                            | not driven                                                      | `title.assist` is not in the model (R1), so `assistPresent` is false and the empty state read "Nothing matches" (walk row 26).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| assist.entry.title-row, assist.panel.first-line-and-cards, assist.tailor.dialog-one-undo, assist.viewer.disabled | not driven                                                      | The panel, the dialog and the entry wait on R1 and R2; the spec's four panel rows skipped for the panel not mounted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| assist.rewrite.card-accept-undo                                                                                  | passed through the window transport, the history label not read | `invoke('assist.propose', { intent: 'shorter' })` answered card `card-acffbedb` "Slide 1: the text is 23 words shorter", one row, the revision unchanged (2). `invoke('assist.accept', { card })` answered `{ revision: 3 }`; the block read "Acme pays for every seat it uses and nothing else." with `ext.assist { at, runId: 'card-acffbedb', card: 'card-acffbedb' }`. One Cmd+Z put the 33 word text back and the mark went with it (describe().state.revision stayed 3). The Change history label "Assist: <sentence>" was not read: `describe().state` has no history field, and the panel's accepted line needs R2. |
| assist.mark.chip-and-history                                                                                     | mark half passed                                                | Redo brought the mark back; the seller's next `block.set` on the block cleared `ext.assist` in the same write (`withAssistClear`). The chip word (R6) and the Version history row (R5) not driven.                                                                                                                                                                                                                                                                                                                                                                                                                          |
| assist.free-ask.fallback-sentence                                                                                | passed through the window transport                             | `invoke('assist.propose', { intent: 'ask', prompt: 'add a video' })` answered `{ cards: [], sentence: "I can make this slide shorter or write its speaker notes. Tailor for a customer is under Tools" }`, the revision unchanged.                                                                                                                                                                                                                                                                                                                                                                                          |
| assist.notes.draft                                                                                               | passed through the window transport                             | The notes card "Slide 1: speaker notes, 34 words"; accept wrote the slide's notes ("Start with the point of slide 1, Slide 1. Say Acme pays for ..."), revision 4 to 5.                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| assist.quota.429                                                                                                 | not driven                                                      | The memory limiter counts per instance (B7's `ratelimit.test.ts` covers the rows). The spec's run 2 failure was its teardown: the 404 check on `untitled-20260920-1ipd` was cut by "Test ended".                                                                                                                                                                                                                                                                                                                                                                                                                            |

Scratch decks: `untitled-20260920-qrr2`, `-tlcp`, `-k445`, `-1ipd` and `-x2h9` were trashed and removed with `deck.trash` and `deck.remove { confirm: true, baseRevision }`; each `GET /edit/<id>` answers 404 and `deck.list { includeTrashed: true }` lists `gt-brand` alone. The first probe's cleanup omitted `baseRevision` on `deck.remove` (400) and left two of them; fixed in the probe and removed by hand at 04:50Z. The dev server on 4416 was stopped before the report.
