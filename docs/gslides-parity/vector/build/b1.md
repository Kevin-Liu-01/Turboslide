# B1, the chrome: the Shape grids and the icons

## Vector round

Builder B1 of the vector round (docs/VECTOR.md sections 2.6, 2.7 items S6 and S7, section 3, 4.4 the crop sentence, 4.8 item V6 the `svgVector` flag, 5.1, 5.4 and 6.3), started 2026-09-24 on the shared worktree `/Users/kevinliu/repos/Turboslide-vector` at `2be923e` on the branch `vector/round`, while B2 to B5 land beside it. The dev server port is 4421 (`TURBOSLIDE_STORE=tmp`, `TURBOSLIDE_REALTIME=memory`, `TURBOSLIDE_LOCAL_OPEN=1`, two fake secrets of 40 characters; stopped before the report). No git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran. No token, cookie or secret was printed. Times are PDT.

This file carries the requests (section 1, written first so the owners can act), the items landed with their files and hunks (section 2), the rows this lane covers and what happened to each (section 3), the commands run (section 4) and the decisions (section 5).

### 1. Requests

Each names the file, the owner and the one change, written so it can be applied as it stands. The icon names every request uses exist in `packages/chrome/src/icons.tsx` from this lane (section 2.1), so the model typechecks the moment the rows land.

R1 (the integrator, `packages/chrome/src/menus/model.ts`): the seven Insert > Shape rows (VECTOR.md 2.6), the icons of 3.2 and 3.3, the flags leaving, the `rasterPictureSelected` predicate with its selection fact, and the crop `disabledReason` (B3's request through this lane, 4.4). Six edits.

R1.1, Insert > Shape (1395 to 1451): the `sub('insert.shape', 'Shape', [...], { icon: 'box' })` block becomes the text below, whole. The three named rows keep their ids and hoist out of the `insert.shape.shapes` container, which leaves; `insert.shape.gallery` is relabelled from All shapes to Shapes (Google's row, so `turboslide` leaves it and the completeness test over `__fixtures__/google-menus.json` finds Insert > Shape > Shapes as before); `insert.shape.arrows` becomes a plain row with `shapeGrid('arrows')` and its child `insert.shape.arrows.arrow` leaves (a plate wins over children, `Menu.tsx` 626 to 650, so the child never drew; Insert > Line > Arrow holds the arrow line); the four category rows lose `advanced`, gain their icons and keep their docs. The long comment above the block stays; append one sentence to it: "The vector round (docs/VECTOR.md 2.6, 3.2; vector/build/b1.md R1): the three named rows hoist out of the Shapes container with their glyph icons, and Shapes (Google's row, once All shapes), Arrows, Callouts and Equation follow them in the default view as glyph grids now that the geometry interpreter draws every preset."

```ts
    sub(
      'insert.shape',
      'Shape',
      [
        now('insert.shape.shapes.rectangle', 'Rectangle', action('block.insert'), {
          turboslide: true,
          icon: 'shape-rect',
          doc: 'Click to place a 240 by 160 rectangle, or drag to draw one',
        }),
        now('insert.shape.shapes.rounded', 'Rounded rectangle', action('block.insert'), {
          turboslide: true,
          icon: 'shape-round-rect',
          doc: 'Click to place a rounded rectangle, or drag to draw one',
        }),
        now('insert.shape.shapes.ellipse', 'Ellipse', action('block.insert'), {
          turboslide: true,
          icon: 'shape-ellipse',
          doc: 'Click to place an ellipse, or drag to draw one',
        }),
        now('insert.shape.gallery', 'Shapes', shapeGrid('shapes'), {
          icon: 'squares-2x2',
          dividerBefore: true,
          doc: 'Every shape preset; a tile places a shape, or drag to draw one',
        }),
        now('insert.shape.arrows', 'Arrows', shapeGrid('arrows'), {
          icon: 'arrow-long-right',
          doc: 'Block arrows; a tile places one, or drag to draw one',
        }),
        now('insert.shape.callouts', 'Callouts', shapeGrid('callouts'), {
          icon: 'chat-bubble-left',
          doc: 'A shape with a pointer; set the pointer under Format options',
        }),
        now('insert.shape.equation', 'Equation', shapeGrid('equation'), {
          icon: 'variable',
          doc: 'Plus, minus, multiply, divide, equal and not equal',
        }),
      ],
      { icon: 'box' },
    ),
```

R1.2, the Insert rows of VECTOR.md 3.2, each one `icon` added to the row's options object (the line numbers are the tree at `2be923e`):

| Row (line)                                                                                                   | Add                                                                                                        |
| ------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| `insert.image.upload` (1349)                                                                                 | `icon: 'arrow-up-tray'`                                                                                    |
| `insert.image.logo` (1353)                                                                                   | `icon: 'tag'`                                                                                              |
| `insert.image.byUrl` (1367)                                                                                  | `{ icon: 'link' }` as its options object                                                                   |
| `insert.image.fromThisPresentation` (1368 to 1372)                                                           | `icon: 'document-duplicate'`                                                                               |
| `replaceImageItems(prefix)` (663 to 690): `.upload`                                                          | `icon: 'photo'` becomes `icon: 'arrow-up-tray'`                                                            |
| `replaceImageItems`: `.byUrl`, `.logo`, `.fromThisPresentation`                                              | `icon: 'link'`, `icon: 'tag'`, `icon: 'document-duplicate'`                                                |
| `insert.chart.bar`, `.column`, `.line`, `.pie` (1471 to 1482)                                                | `icon: 'chart-bars'`, `icon: 'chart-bar'`, `icon: 'chart-line'`, `icon: 'chart-pie'`                       |
| `insert.wordArt` (1492)                                                                                      | `icon: 'word-art'`                                                                                         |
| `insert.line.line`, `.arrow` (1507, 1508)                                                                    | `{ icon: 'line-line' }`, `{ icon: 'line-arrow' }` as their options objects                                 |
| `insert.line.rule`, `.elbowConnector`, `.curvedConnector`, `.curve`, `.polyline`, `.scribble` (1509 to 1536) | `icon: 'line-rule'`, `'line-elbow'`, `'line-curved'`, `'line-curve'`, `'line-polyline'`, `'line-scribble'` |
| `insert.specialCharacters` (1542)                                                                            | `icon: 'language'`                                                                                         |
| `insert.slideNumbers` (1571)                                                                                 | `{ icon: 'hashtag' }` as its options object                                                                |
| `insert.shader` (ship two's row, after the rebase)                                                           | the icon ship two's hunk names, else `cube`; the integrator resolves at the rebase                         |

R1.3, the Format rows of VECTOR.md 3.3, the same way:

| Row (line)                                                                  | Add                                                                                       |
| --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| `format.text.bold` (1602)                                                   | `icon: 'bold'`                                                                            |
| `format.alignIndent.left`, `.center`, `.right`, `.justified` (1693 to 1705) | `icon: 'bars-3-bottom-left'`, `'bars-3-center-left'`, `'bars-3-bottom-right'`, `'bars-3'` |
| `format.image.maskImage` (1884 to 1888)                                     | `advanced: true` leaves; `icon: 'mask'`                                                   |
| `format.image.replaceImage` (1890 to 1894)                                  | `icon: 'arrow-path'`                                                                      |
| `format.image.addCaption` (1896)                                            | `icon: 'bars-2'`                                                                          |
| `format.image.useOnEverySlide` (1907)                                       | `icon: 'slide'`                                                                           |
| `format.image.imageOptions` (1920)                                          | `icon: 'adjustments'`                                                                     |
| `format.bordersLines` (1931; the `sub` gains an options object)             | `{ icon: 'line-weight' }`                                                                 |
| `format.bordersLines.borderColor` (1932)                                    | `icon: 'swatch'`                                                                          |
| `format.bordersLines.borderWeight` (1936)                                   | `icon: 'line-weight'`                                                                     |
| `format.bordersLines.borderDash` (1941 to 1945)                             | `icon: 'line-dash'`                                                                       |
| `format.bordersLines.lineStart`, `.lineEnd` (1947 to 1957)                  | `icon: 'line-start'`, `icon: 'line-end'`                                                  |
| `format.textFitting` (1986)                                                 | `icon: 'arrows-pointing-in'`                                                              |
| `format.dropShadow` (1991)                                                  | `icon: 'shadow'`                                                                          |
| `format.changeShape` (1997 to 2002)                                         | `advanced: true` leaves; `icon: 'square-2-stack'`                                         |
| `format.editData` (2004)                                                    | `icon: 'table-cells'`                                                                     |
| `format.chartType` (2019; the `sub`'s options object)                       | `icon: 'chart-bar'`                                                                       |

R1.4, the selection fact (`MenuContext['selection']`, 3066 to 3110): after `imageEdited?: boolean;` add

```ts
    /** the selected picture draws a vector (svg) asset (docs/VECTOR.md 4.4): crop is refused with the sentence */
    vector?: boolean;
```

R1.5, the predicate: `'rasterPictureSelected'` joins the `MenuPredicate` union after `'imageEdited'` (399) with the doc comment `/** a picture whose asset is a raster: crop applies (docs/VECTOR.md 4.4) */`, and `evaluate` (3260) gains

```ts
    case 'rasterPictureSelected':
      return selection.block === 'image' && selection.vector !== true;
```

R1.6, the crop row and the reason that varies with the state. `disabledReason` (464) is one string for every disabled state, and Crop image has two: no picture selected (the plain tooltip today) and an svg picture (the sentence). So `MenuItem.disabledReason` takes a function form beside the string:

```ts
  /** the tooltip sentence while the predicate says no; a function answers per state, undefined for the doc */
  disabledReason?: string | ((ctx: MenuContext) => string | undefined);
```

and `tooltipDoc` (3398 to 3403) reads it:

```ts
if (item.status === 'now' && !evaluate(item.enabled, ctx)) {
  const reason =
    typeof item.disabledReason === 'function' ? item.disabledReason(ctx) : item.disabledReason;
  if (reason !== undefined) return reason;
}
```

The one other reader of a `MenuItem`'s `disabledReason` is `EditorShell.tsx` 1204 (`if (item.disabledReason !== undefined) say(item.disabledReason)`), which becomes `const reason = typeof item.disabledReason === 'function' ? item.disabledReason(menuContext) : item.disabledReason; if (reason !== undefined) say(reason);` with the shell's context in scope there (the `TailControl.disabledReason` of `toolbar-tails.ts` stays a string; `ToolbarHead.tsx` 38 and `ToolbarTail.tsx` 955 read that type). Then `format.image.cropImage` (1877 to 1881) becomes

```ts
        now('format.image.cropImage', 'Crop image', client('cropMode'), {
          enabled: 'rasterPictureSelected',
          disabledReason: (ctx) =>
            ctx.selection.vector === true ? FORMAT_WORDS.picture.svgCrop : undefined,
          icon: 'viewfinder-circle',
          doc: 'Drag the handles to crop; press Enter to finish',
        }),
```

with `FORMAT as FORMAT_WORDS` joining the existing `import { STUB_PREFIX, stubClause } from './strings.ts';` (model.ts has its own `const FORMAT: Menu`, so the words take the alias; `FORMAT.picture.svgCrop` is the sentence "An SVG picture cannot be cropped. Resize it instead", landed in this lane's `menus/strings.ts`, section 2.5, so the row, the toolbar's crop path and B3's viewer refusal say one thing; B3 copies the string into the viewer, which cannot import the chrome). If the integrator prefers no function form, the fallback is `disabledReason: FORMAT.picture.svgCrop` alone, which then also reads on the row while no picture is selected; this lane's test `menu-model.test.ts` asserts the sentence on an svg picture and the plain doc with nothing selected, and reads red on the fallback for the second assertion.

R2 (the integrator, `packages/chrome/src/editor-shell.ts`, `buildMenuContext` 1304 to 1410): the `vector` fact of R1.4, beside `imageEdited` (1401):

```ts
    ...(vectorPictureOf(input.document.deck, block) ? { vector: true } : {}),
```

with the reader, beside `imageEditedOf` (1289):

```ts
/** True when the picture's asset draws a vector (docs/VECTOR.md 4.1, 4.4): `vectorOf` answers its file. */
export function vectorPictureOf(deck: Deck, block: Block | undefined): boolean {
  if (block?.type !== 'shot' && block?.type !== 'picture') return false;
  const asset = deck.assets[block.asset];
  return asset !== undefined && vectorOf(asset) !== undefined;
}
```

and `import { vectorOf } from '@turboslide/schema/assets';` once B3's `vectorOf` has landed (VECTOR.md 4.1; merge order 5.3, B3 before B1). Until then the fact is absent and every picture reads as raster, which is what production does today. One more line in the same file, optional: `shapePickPlan` (2356 to 2368) writes `shape.set { kind }` and the store drops the old preset's adjust values on its own (`apps/cli/src/store-actions.ts` 3187 to 3194), while the inspector's plate writes `adjust: null` explicitly (`inspector/shape.tsx` 60); adding `adjust: null` to the plan's input makes the three plates write the same input, which is what the row `shapes.change-shape.plate` quotes ("writes `shape.set { kind: 'octagon', adjust: null }`"). B5's driver should read the block after the pick (no `adjust` on it, eight vertices in the path) rather than the action's input, so either form passes.

R3 (the integrator, `packages/chrome/src/__tests__/editor-shell-render.test.tsx` 561 to 571, `editor-shell.ts` 1583 and 2585, `__tests__/editor-shell.test.ts` 986): the leftovers of the container and of the Arrows child. The render test walks `clickMenuPath(gallery.container, 'insert', 'insert.shape', 'insert.shape.shapes')` through the container that leaves; it becomes `clickMenuPath(gallery.container, 'insert', 'insert.shape')` and the `data-menu-item^="insert.shape.shapes."` read then lists the three named rows directly under Insert > Shape. Lines 583 to 597 (the toolbar Shape button's seven rows in order) pass unchanged. `INSERT_TOOLS` (1583) carries `'insert.shape.arrows.arrow': { kind: 'line', line: 'arrow' }` and `menuActionPlan` (2585) a `case 'insert.shape.arrows.arrow':`, both for a row that never drew and now leaves; both lines go, and with them the entry `'insert.shape.arrows.arrow': { kind: 'tool', tool: { kind: 'line', line: 'arrow' } }` of the test's map at 986 (`itemById` throws on the unknown id otherwise). With R1 and these three edits applied to a copy of the chrome package under this lane's scratchpad, the whole chrome suite reads 77 files green (section 4).

R4 (B5, `scripts/probes/editor-walk-probe.mjs` 1462 and 1936, `scripts/probes/core-walk/areas/shapes.mjs`, `apps/studio/e2e/objects.spec.ts` 283 to 291, and the new rows): the paths through the container, and the ids this lane's rows expose.

- `insertByTool(..., ['insert.shape', 'insert.shape.shapes', 'insert.shape.shapes.pick.rect'], ...)` at 1462 and 1936 hovers `insert.shape.shapes`, which no longer exists; the path becomes `['insert.shape', 'insert.shape.shapes.rectangle']` (the named row arms the draw tool as before) or, through the gallery, `['insert.shape', 'insert.shape.gallery', 'insert.shape.gallery.pick.rect']`. `objects.spec.ts` 283 clicks `[data-menu-item="insert.shape.shapes"]` before the named row: drop that click. `areas/shapes.mjs` 294 to 356 (`shapes.insert.named-rows`) already tolerates the container's absence (`hoverRow('insert.shape.shapes', ...).catch(() => undefined)`), filters the menu rows by the `insert.shape.shapes.` prefix and asserts no plate under it, so it keeps working; extend it to read the seven ids in order as VECTOR.md 6.1 says.
- The plates. The menu's Shapes, Arrows, Callouts and Equation rows open the `ShapePicker` with the row's id as its control: the grid is `[data-control="insert.shape.<row>.grid"]` (`role="grid"`, `aria-label` the category's label) and a tile `[data-control="insert.shape.<row>.pick.<preset>"]` (`role="gridcell"`, `aria-label` the preset's label, `data-shape` the preset id, `.is-active` on the pointed or keyed tile, `.is-picked` on the current one), with `<row>` one of `gallery`, `arrows`, `callouts`, `equation`. The toolbar Shape button's dropdown (`#ts-menu-toolbar\.insertShape`) lists the same seven `[data-menu-item]` rows and its four grid rows open the same plates. Change shape: the toolbar's `toolbar.changeShape` opens `[data-control="toolbar.changeShape.grid"]` with tiles `toolbar.changeShape.pick.<preset>`; the right click menu's row opens `format.changeShape.grid` and `format.changeShape.pick.<preset>`; the Format options Shape section's button `formatOptions.shape.change` opens `formatOptions.shape.change.grid` and `.pick.<preset>`. Mask image: `format.image.maskImage.grid` and `format.image.maskImage.pick.<preset>` from the menu row and from the Crop button's arrow (`toolbar.cropImage`'s arrow names `format.image.maskImage`). Every tile draws one `<path>` from `shapePath(id, 48, 36)` in an `svg[viewBox="-2 -2 52 40"]`; its `d` is the glyph a driver compares with the schema's `shapePath`.
- The icons. A menu row's icon is `.ts-menu-ic > svg[viewBox="0 0 20 20"][width="16"][height="16"]`, filled Heroicons carrying `fill="currentColor"` on the `<svg>`, a drawn line glyph carrying `fill="none" stroke="currentColor"` on its `<path>` (`stroke-width` 1.5, or 1, 2 and 3 on the Line weight glyph, `stroke-dasharray="3 2"` on the Border dash glyph). A row's glyph is read from `[data-menu-item="<id>"] .ts-menu-ic svg path` (`d` non empty; the three named rows differ from each other; the eight line kinds are eight distinct stroked paths). The toolbar dropdown rows carry the same slot.
- The crop refusal on an svg picture (VECTOR.md 4.4; b3.md R2): once R1.6 lands and B3's `vectorOf` reaches `buildMenuContext` (R2), the Format > Image > Crop image row is disabled with the sentence in its tooltip, and the toolbar Crop button (`toolbar.cropImage`, the main half of the split) is `aria-disabled` with the sentence as its tooltip while the Mask arrow beside it stays live (`ToolbarTail.tsx`, section 2.4); a click on the button's path speaks the sentence through the snackbar as well. The sentence is `FORMAT.picture.svgCrop` in `menus/strings.ts`, "An SVG picture cannot be cropped. Resize it instead", the same bytes as the viewer's `SVG_CROP_SENTENCE` (`packages/viewer/src/picture-place.ts` 315, B3's copy).
- The toolbar Shape button's plates (VECTOR.md 2.6): a pick in a plate the dropdown opens closes the dropdown (`ToolbarTail.tsx`, section 2.4), so a driver's next click lands on the sheet; before this a pick armed the tool and left the dropdown and its plate open, and the sheet click hit a tile (this lane's drive, section 3). The four shape plates take their grid's own width (`Pickers.css`), 342 px; the menu's 320 px cap clipped the eighth column before.
- The Download dialog (VECTOR.md 4.6, 6.2): `export.run` carries `svgVector: false` only while `export.svg.vector` is parked (`isParked('export.svg.vector', settings)`, `parked-controls.ts`), and no `svgVector` key otherwise, so the header's default (true) applies.

R5 (B3, `packages/viewer/src/Editor.tsx`, the crop refusal): the sentence is `FORMAT.picture.svgCrop` in `packages/chrome/src/menus/strings.ts` (this lane; section 2.5), verbatim "An SVG picture cannot be cropped. Resize it instead", so the viewer's own copy (the viewer cannot import the chrome) says the same words; a test in this lane pins the string so a change on either side reads red.

R6 (B4 and the integrator's header of `packages/schema/src/actions.ts`, `export.run` 2675 to 2700): `svgVector?: boolean`, default true, as VECTOR.md 4.6 states; `dialogs/Download.tsx` in this lane sends `svgVector: false` only while the control is parked (R4's last bullet) and sends no key otherwise, so the dialog typechecks before and after the header takes the field (the dispatch input is `Record<string, unknown>` at the call site).

R7 (the theme sprite twin, `packages/theme`): nothing. None of this round's names is drawn by the sheet, so `sprite.svg`, `sprite-ids.json` and `ICON_NAMES` gain nothing; the twin rule is the test of section 2.2 (every name in both `PATHS` and `sprite-ids.json` carries the same `d` strings).

### 2. The items landed, their files and their hunks

Each item is one revertable hunk set; the line numbers are the files as they stand at the return. Every file below is this lane's alone (VECTOR.md 5.1).

2.1 The icon table (VECTOR.md 3.1, 3.4; the rows `menus.icons.*`, `shapes.icons.named-rows`). `packages/chrome/src/icons.tsx`: the header gains the round's paragraph (21 to 31) and the import of the schema's `shapePath` (33); `IconName` gains `bars-3-bottom-left` (110; its paths were in the table under `sidebar` and `text` as `BARS_3_BOTTOM_LEFT`, and 3.3 names the row's icon by its Heroicon name, so the name joins with the same paths at 220), the three Heroicons `arrow-long-right`, `chat-bubble-left`, `chart-pie` (161 to 163, copied verbatim from heroicons 2.2.0 `20/solid` on disk under the Prototemplate checkout, byte equal to the `squares-2x2` the table already carried), and the twenty drawn names (165 to 184). `IconPath` (193 to 200) gains `stroke?: true`, `width?`, `dash?` and `translate?`, exported. The entries (975 to 1063): the Heroicons as the source writes them; the shape glyphs `shapePath(id, 16, 12)` at `translate: [2, 4]`, filled, so the row draws the outline the picker strokes and B2's interpreter feeds both; the line kinds as 3.1 writes them, with two departures recorded in section 5 (the arrow's stroke stops at its head's base; Line start and Line end the same); `line-curve` the Catmull-Rom string computed with `primitives.ts`'s formula and `px` writer, written as a literal since the chrome does not depend on the render package's blocks; `chart-bars`, `chart-line`, `word-art` (width 2), `line-weight` (widths 1, 2, 3), `line-dash` (`3 2`), `mask` and `shadow` (the back square written as the L its front square leaves visible, section 5). `ICON_NAMES` (1065) and `iconPaths` (1068) are exported for the tests and the contact sheet; `Icon` (1077 to 1109) draws a stroked entry as `fill="none" stroke="currentColor"` at its width with round caps and joins, its dash array and its translate, and a filled entry as before. 144 names in the table.

2.2 The icons test (VECTOR.md 6.3). `packages/chrome/src/__tests__/icons.test.ts` (new): every name has paths with a non empty `d`; every name the theme sprite also draws (`packages/theme/assets/sprite-ids.json`, `sprite.svg`, read from disk) carries the sprite's `d` strings in order, and none of the round's names is in the sprite; the drawn glyphs are non empty, the three shape glyphs equal `shapePath(id, 16, 12)` and differ, the eight line kinds are eight distinct stroked paths, the arrow kinds carry one filled head, the weights and the dash are as written; the rendered markup of every name is one `svg[viewBox="0 0 20 20"][width="16"][height="16"]` with as many `<path>` elements as entries, no `<use>` and no `<img>`, the stroke attributes on a stroked glyph and none on a filled one. 4 cases, green.

2.3 The shape picker (VECTOR.md 2.6). `packages/chrome/src/pickers/ShapePicker.tsx`: the docblock (20 to 33), the grid named after its category when it draws one (87; `aria-label` and the tooltip name read Arrows on the Arrows plate, Shapes on the four category plate), `data-category` on the grid (97). `Glyph` keeps one `<path>` from `shapePath(id, 48, 36)`, which B2's interpreter now answers for every preset. `Pickers.css` unchanged. `__tests__/pickers.test.tsx`: the grid's name and category (46 to 59), and the new case at 62 to 95: 99 tiles in Shapes with 94 distinct glyphs (the five ECMA twins named, section 3), the rectangle's the box, Arrows 26, Callouts 4 and Equation 6 distinct, every glyph one stroked path at 1.5 with no fill. 6 cases, green.

2.4 The toolbar shape tail and the crop sentence (VECTOR.md 2.6 item S7, 4.4). `packages/chrome/src/menus/toolbar-tails.ts`: `SHAPE_TAIL`'s Change shape loses `advanced: true` (the docblock 368 to 377 says why), so it leads the tail in the default view with the divider and the fill follows without one; `menus/__tests__/toolbar-tails.test.ts` 81 to 88 asserts no control of the tail carries the flag and Change shape carries the divider and the dropdown. `packages/chrome/src/ToolbarTail.tsx`: `vectorPictureSelected(context)` (263 to 272) reads `selection.vector` through a widened type, so the file typechecks before and after R1.4 lands; the crop path (891 to 899) says `FORMAT.picture.svgCrop` through the snackbar on an svg picture and enters crop mode otherwise; the Crop split button's main half takes a control whose `enabled` is the existing `never` predicate with the sentence as its `disabledReason` on an svg picture (b3.md R2's read of `aria-disabled`; the Mask arrow beside it stays live), and the imports at 46 and 48. The dropdown of the Insert shape button: its rows are as they stand (its "first visible row is a static submenu" rule no longer fires once the container leaves, and the seven rows list directly), and its `Menu` now passes `onPicked: () => setPlate(null)` to the shell's `renderDynamicSubmenu`, so a pick in a plate the dropdown opens closes the dropdown (before this the dropdown and its plate stayed open after a pick and the next click on the sheet landed on a tile; this lane's drive, section 3). `packages/chrome/src/pickers/Pickers.css`: `.ts-menu.is-dynamic:has(.ts-shapes) { max-width: none }` (the menu's 320 px cap clipped the grids' eighth column, 342 px wide; the plate measures its own width for its position).

2.5 The sentence. `packages/chrome/src/menus/strings.ts` 705: `FORMAT.picture.svgCrop`, "An SVG picture cannot be cropped. Resize it instead", pinned by the menu-model test (section 2.7) so B3's copy in the viewer and this one cannot drift unnoticed.

2.6 The Download dialog (VECTOR.md 4.6, 6.2). `packages/chrome/src/dialogs/Download.tsx`: `isParked` imported (13), `settings` read from the shell (214), and the `export.run` input carries `svgVector: false` only while `export.svg.vector` is parked (280 to 283), no key otherwise. `__tests__/download-dialog.test.tsx`: `isParked` spied through `vi.mock` so one case parks the control (25 to 30), and the two cases at 260 to 287 (no key by default; `svgVector: false` and nothing else changed while parked). 10 cases, green.

2.7 The menu model test (VECTOR.md 6.3). `packages/chrome/src/menus/__tests__/menu-model.test.ts`, written against R1 and proven on a copy of the chrome package with R1 applied (section 4): the rows table drops the container and the Arrows child (264 to 275); `reasonOf` (86 to 108) reads a function valued `disabledReason` on an svg picture for the two word checks (1603, 1787); the return round's cases now expect the seven rows in both contexts, the galleries, Mask image and Change shape in the default view and in Search the menus with the switch off, and Change shape leading the shape tail; the new describe at 2977 to 3160: the seven rows in order with their labels, icons, effects, no children and the divider before Shapes; Google's four labels under Shape and no `turboslide` on Shapes; every Insert row of 3.2 and every Format row of 3.3 with its icon, the word rows without one, the eight line kinds eight different glyphs; Change shape and Mask image unflagged, on the shape's and the picture's right click menus and on the tail; Crop image enabled on a raster picture, disabled on an svg picture with the sentence, disabled with its own doc when nothing is selected, and the other picture rows enabled on an svg picture. 64 cases: 53 green on the tree as it stands, 11 waiting on R1 (section 3); 64 green with R1 applied.

2.8 The request patches. `docs/gslides-parity/vector/build/b1/model.ts.patch` (R1, 451 lines, `git apply` or `patch -p1` from the worktree root; it applies cleanly to `model.ts` at `2be923e` and reproduces the copy the tests ran on) and `b1/r3.patch` (R3, the three files, 4 hunks). Generated by `scratchpad/vector/apply-r1.mjs` over a copy, never by an edit of the integrator's files.

2.9 The pictures. `docs/gslides-parity/vector/build/b1/icons-sheet-light.html` and `-dark.html` (every glyph of the table drawn by the chrome's `Icon` through Vite's SSR loader, `scratchpad/vector/icon-sheet.mjs`: the Insert and Format rows of 3.2 and 3.3 as menu rows at 16 px with a 64 px twin, then the whole table), their PNGs at 1440, and the menu and plate pictures from 4421 named in section 3.

### 3. The rows

This lane's rows (VECTOR.md 5.4): `shapes.insert.grid-shapes`, `.grid-arrows`, `.grid-callouts`, `.grid-equation`, `shapes.insert.named-rows`, `shapes.icons.named-rows`, `shapes.change-shape.plate`, `shapes.mask-image.plate`, `menus.icons.insert-rows`, `menus.icons.format-rows`, `menus.icons.one-family`. What happened to each on 4421, driven by hand at human speed through `scratchpad/vector/b1-shots.mjs` (playwright-core over Chrome for Testing, a pointer that moves in steps, 1440 by 900 at 2x), in the default view and with Tools > Advanced tools on, light and dark, while `model.ts` stood at `2be923e` (R1 not yet applied; the integrator's day 0 request):

- `shapes.insert.named-rows`: the toolbar Shape button's dropdown lists `insert.shape.shapes.rectangle`, `.rounded`, `.ellipse` in the default view and the seven ids in order with the switch on (`toolbar-shape-default-*.png`, `toolbar-shape-switch-on-*.png`; the log's `toolbar Shape rows` lines); the menu bar's Insert > Shape lists the three under the Shapes container as the tree stands (`insert-shape-default-*.png`, `insert-shape-switch-on-*.png`). The seven rows in both surfaces are R1's; proven on the R1 copy by `menu-model.test.ts` and `editor-shell-render.test.tsx` (section 4). Not driven on the seven-row tree.
- `shapes.icons.named-rows`, `menus.icons.insert-rows`, `menus.icons.format-rows`: the rows draw no icon on this tree (`insert-line-*.png`, `format-image-*.png`: the line kinds and the image rows as plain text, Kevin's screenshot state), since the `icon` fields are R1's; every glyph the rows will draw is in the table and drawn by the chrome's `Icon` on the contact sheets (`icons-sheet-light.png`, `icons-sheet-dark.png`: the Insert and Format rows of 3.2 and 3.3 at 16 px in a menu row with a 64 px twin, then all 144 names). `icons.test.ts` pins the table; `menu-model.test.ts` pins every row's name on the R1 copy. Not driven; the glyphs are on the sheets.
- `menus.icons.one-family`: `icons.test.ts` renders every name and asserts one `svg[viewBox="0 0 20 20"][width="16"][height="16"]`, no `<use>`, no `<img>`; the eight line kinds are eight distinct stroked paths (section 2.2). The rendered menus carry only what `Icon` draws (`Menu.tsx` 605 to 611). Green in the unit test; the walk's read over the deployment is B5's driver.
- `shapes.insert.grid-shapes`: with the switch on, the toolbar dropdown's All shapes row opened the plate with 99 tiles, every tile its own outline from B2's interpreter (`toolbar-shape-gallery-switch-on-*.png`); the plate clipped its eighth column (the menu's 320 px cap) and a pick of the hexagon tile armed the tool but left the dropdown and the plate open, so the placing click landed on a tile (`shape-context-switch-on-*.png` of the first run: the tile under the sheet point highlighted, no shape on the sheet). Both fixed in this lane (section 2.4; Pickers.css) and re-driven on the restarted server: the readings of that run follow this list. `pickers.test.tsx`: 99 tiles, 94 distinct (decision 6), the rectangle's the box, Enter picks (the keyboard case of the round two test). Request to B5: the row's numbers read 100 and 95; the tree has 99 and 94 with the five ECMA twins named in decision 6.
- `shapes.insert.grid-arrows`, `.grid-callouts`, `.grid-equation`: 26, 4 and 6 tiles with as many distinct glyphs (`pickers.test.tsx`); the menu rows are parked on this tree until R1 (the switch-on menu drive found `insert.shape.arrows`, `.callouts` and `.equation` under the Shape submenu of the dropdown, `toolbar-shape-switch-on-*.png`). The plates re-driven from the dropdown on the restarted server: the readings follow.
- `shapes.change-shape.plate`: the shape tail carries Change shape in the default view since this lane's `toolbar-tails.ts` hunk (the unit tests); on the server the hexagon pick of the first run placed nothing (above), so the tail's plate and the right click menu's row were not reached in that run; re-driven on the restarted server, readings below.
- `shapes.mask-image.plate`: not driven by this lane (the row needs a picture placed; the Mask row is `advanced` on this tree until R1, and the picker it opens is the same `ShapePicker` the Change shape plate draws, with `format.image.maskImage` as its control); B3's drive reached `format.image.maskImage.pick.ellipse` only with the switch on (b3.md).

The re-drive on the restarted server (19:09 to 19:10, both appearances with the switch on, `scratchpad/vector/shots3.log`), after the two fixes of section 2.4 and the width rule:

- From the menu bar (Insert > Shape > All shapes, Arrows, Callouts, Equation as the tree names them): the Shapes plate opened with 99 tiles and 94 distinct glyph `d` strings, all eight columns drawn (`insert-shape-gallery-switch-on-light.png`, `-dark.png`); Arrows 26 tiles, 26 distinct; Callouts 4 and 4; Equation 6 and 6 (`insert-shape-arrows-*`, `-callouts-*`, `-equation-*`). The glyph `d` strings are `shapePath(id, 48, 36)` (the picker) and the counts are `pickers.test.tsx`'s.
- From the toolbar Shape button: the seven rows in order (`toolbar-shape-switch-on-*.png`); the All shapes row's plate (`toolbar-shape-gallery-switch-on-*.png`); a click on the hexagon tile closed the dropdown, and one click on the sheet at (800, 450) placed a hexagon whose path is `M0,79.5 L40,0 H199.5 L239,79.5 L199.5,159 H40 Z` (six vertices at 240 by 160 less the stroke, `data-shape="hexagon"`).
- One click on the placed hexagon selected it (the ring, the eight handles, the rotation stem, the Shape chip); the shape tail's Change shape opened `toolbar.changeShape.grid` with the hexagon tile ringed (`is-picked` true; `change-shape-switch-on-light.png`, `-dark.png`), the four categories at full width. The right click menu on the shape is `shape-context-switch-on-*.png`. A pick of Octagon and Cmd+Z (the rest of `shapes.change-shape.plate`) were not driven by this lane; the plate's pick path is `shapePickPlan` (`shape.set { kind }`, R2's note) and `pickers.test.tsx` drives the pick.
- So on this lane's server `shapes.insert.grid-shapes`, `.grid-arrows`, `.grid-callouts` and `.grid-equation` open their plates with the counts above and a tile places its shape; `shapes.insert.named-rows` reads the seven ids in the toolbar and waits for R1 in the menu bar; `shapes.change-shape.plate` opens ringed; the icon rows wait for R1; `shapes.mask-image.plate` was not driven.

The tree typechecks whole (`node_modules/.bin/tsc -b`, exit 0, twice: at 18:44 and 18:52 with every lane's in progress files, the second after this lane's last hunks).

### 4. The commands

All from `/Users/kevinliu/repos/Turboslide-vector` unless said; every one exited 0 unless said.

- `node_modules/.bin/tsc -b packages/chrome`: clean after every hunk of section 2 (the last run after the `bars-3-bottom-left` entry).
- `cd packages/chrome && ../../node_modules/.bin/vitest run`: 77 files, 724 passed, 11 failed, 2 todo on the tree as it stands; the 11 are the cases of section 2.7 that read `model.ts` before R1 (section 3 names them). `icons.test.ts` 4 of 4, `pickers.test.tsx` 6 of 6, `download-dialog.test.tsx` 10 of 10, `toolbar-tails.test.ts` 13 of 13 green on their own.
- The proof of R1 and R3 without an edit of the integrator's files: `packages/chrome` copied to `scratchpad/vector/chrome-check` (its `node_modules` a symlink to the worktree's, the tsconfig `references` stripped, the tooling tsconfigs copied beside it, `packages/theme` symlinked for the sprite test), `node scratchpad/vector/apply-r1.mjs <copy>/src/menus/model.ts` (50 exact substring hunks, each anchor required once), the three R3 edits by `sed`, then `vitest run` in the copy: 77 files, 735 passed, 2 todo, 0 failed. The same copy is the source of `b1/model.ts.patch` and `b1/r3.patch` (`diff -u`); `patch -p1` of `model.ts.patch` over a fresh `model.ts` reproduces the copy byte for byte (`cmp`).
- `node scratchpad/vector/icon-sheet.mjs`: the two contact sheets (Vite's SSR loader over `icons.tsx`, React's server renderer through the studio's `require`), 144 glyphs.
- The dev server: from `apps/studio`, `TURBOSLIDE_STORE=tmp TURBOSLIDE_REALTIME=memory TURBOSLIDE_LOCAL_OPEN=1 TURBOSLIDE_SESSION_SECRET=<40 characters> TURBOSLIDE_DOWNLOAD_SECRET=<40 characters> node_modules/.bin/vite dev --port 4421 --strictPort --config vite.no-watch.config.ts` (the studio's own binary; the root has none), `GET /new` 200; stopped before the return.
- The pictures: `node scratchpad/vector/b1-shots.mjs --sheets --menus http://localhost:4421` under `.turboslide/e2e.lock` (taken with the `until mkdir` loop after B4's drive released it, released with `rmdir` when the run ended; section 3 carries the log's readings).
- `node_modules/.bin/tsc -b` over the whole tree: see section 3's closing note (the other lanes' files were in progress while it ran).
- No git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build` ran; no token, cookie or secret was printed; nothing on Vercel or GitHub changed; no deck was created on a deployment.

### 5. Decisions

1. The three named rows keep their ids and hoist; `insert.shape.gallery` reads Shapes without `turboslide` (Google's row); the Arrows child leaves (VECTOR.md 2.6). The `dividerBefore` sits on Shapes so the divider separates the three named rows from the four grids, as 2.6 draws it.
2. `disabledReason` takes a function form (R1.6) rather than a string that would read the svg sentence on Crop image while nothing is selected; the fallback is named in R1.6 and the test tells the two apart.
3. The crop refusal in the toolbar reads the selection fact `vector` through a widened type (`vectorPictureSelected`), never the asset directly: B3's `vectorOf` reaches the chrome once, in `buildMenuContext` (R2), so the row, the button and the viewer agree on what an svg picture is; until R2 lands the button enters crop mode as today.
4. The icon table's drawn glyphs depart from VECTOR.md 3.1 in three places, each so the glyph reads at 16 px: the arrow's stroke stops at its head's base (`M3,16 L13.2,7.25`, the head at (17, 4) as computed) so its round cap stays under the head and so the eight line kinds are eight distinct stroked paths (`line-line` and `line-arrow` would otherwise share `M3,16 L17,4`); Line start and Line end stop their rule at the head's base for the same reason; `shadow`'s back square is written as the L its front square leaves visible, since the glyph draws in one colour and a full square behind an open square reads as two overlapping squares.
5. `bars-3-bottom-left` joins the table under its Heroicon name with the paths `sidebar` and `text` already carried, because 3.3 names the Left row's icon by that name and `BARS_3_BOTTOM_LEFT` is that symbol; the sprite twin test is unaffected (the sprite has no such id).
6. The Shapes plate holds 99 tiles with 94 distinct outlines, not the 100 and 95 of row `shapes.insert.grid-shapes`: the table has 99 Shapes presets (the audit counted 99 too), and the five flowchart presets whose ECMA geometry is another preset's (Process is `rect`'s, Alternate process `roundRect`'s, Connector `ellipse`'s, Extract `triangle`'s, Decision `diamond`'s) draw the same outline by definition; `pickers.test.tsx` pins 99 and 94 with the five pairs, and section 3 asks B5 to write the row as 99 tiles and 94 distinct.
7. The `line-curve` glyph is a literal computed with `catmullRomPath`'s formula and the `px` writer of `packages/render` rather than an import: the chrome does not depend on the render package's block modules and `primitives.ts` is not an export of it.
8. R1 is delivered as a proven patch (section 2.8) beside the prose, so the integrator applies one file and runs the tests that are already green on it, instead of typing fifty edits from a table.
