# Turboslide round five, build status

Kept by the integrator (`docs/gslides-parity/MILESTONES-5.md`, "Integrator"). One heading per builder once the lanes land: what landed at merge 1 and at merge 2, what was cut, the open requests with where they went, and the deviations recorded. Section numbers refer to `docs/gslides-parity/SPEC-5.md` unless prefixed SPEC, SPEC-2, SPEC-3, SPEC-4 or A (`SPEC-5-amendments.md`). Started on day 0, 2026-09-15, over `main` at `d5a1be5` (`BASE`); nothing is committed until the ship step. Full notes: `docs/gslides-parity/build-5/integrator.md` (day 0: the line citation map, the reassignments, the seam file by file, the commands, the deviations) and the builders' `b1.md` to `b7.md` as they land. Kevin's directives of the round: every Google Slides feature supported with full agent queryability and editability, on local decks first; anything on the canvas draggable and movable the way Google Slides moves it, backgrounds and shaders included, with text handled the same way; multiplayer with the collaboration features; pictures as slide backgrounds with dithers over them; more font options; the revision staleness of a tab against its own edits and the resize scaling fixed and reworked; the product built for the sales team, which is most of the organisation.

## Day 0

The integrator's day 0 (MILESTONES-5 "The order", day 0; `build-5/integrator.md` sections 1 to 8):

| Item                            | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `BASE` named                    | `d5a1be5`; SPEC-5's citations were written at `d5d7f07` with round four in the working tree; of the cited files four changed between the two (`controller.tsx` new, `blob-store.ts`, `canvas.ts`, `check.mjs`, six lines of `render/slide.ts`), the rest are byte identical                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| The line citation map (18)      | `integrator.md` section 2: every citation re-read on `BASE` and after the day 0 seam, with the current line of each construct (`later(` rows 19 lines down, `OMITTED_MENUS` 121, the action table's rows, `Scene`, the post process, `entryKindSchema`, `renderSlide`, the step table, `reduce.ts`'s stale message at 573, `syncTtlMs` at 420, 446 and 662, `resizeBox` at 792)                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Day 0 amendments                | `integrator.md` section 3: the round four names (the editor split under `apps/studio/src/editor/`, the filmstrip files, the home components, `shapes/definitions.ts`, `perf-budget.mjs`, `thumbs.ts`); `store-actions.ts` does not split handlers by area, so the lanes' handler modules are new files under `apps/cli/src/actions/`; `menu-model.test.ts` counts the `file.email` container (22 Later over `allItems()` at `BASE`, 3 at the round's end with 2 leaves); the new directories `schema/validate/`, `export/check/`, `chrome/panels/`, `packages/spelling/`; the shared files (`render-block.ts`, the exhaustive maps); B7's sync files and the resize model B4 inherits from `hotfix-2.md` and `hotfix-3.md`                                                                         |
| The schema seam (1.2, 1.3, 1.6) | every field optional with its absent meaning: the page, the language, the theme records, the custom layouts, the media map, the transition and animations, the guide colours, the placeholder kinds, the list and indent fields, the font family, the group path, the per edge cell borders, the four dither patterns and the angle, the media role and record, the preferences, the building blocks, the import report, the export options and report fields, the seven validator codes with six empty family modules (`page.ts` bounds the guides by the page), the three block types drawn as plain boxes, the catalog entries; `normalizeMotion` on the two reducer sites; `Deck.media` beside `Deck.assets` (deviation 1)                                                                     |
| The action table (13)           | 51 rows in `ACTION_IDS` (220 in all), each with input, output, CLI usage, MCP name and example, answering `NotImplementedError` with milestone `GS5` from a fresh dispatcher (`packages/agent/src/__tests__/gs5-seam.test.ts`); the widened inputs of 13 on 17 existing rows; `NO_REVISION_WRITES` gains the seven; `actions-gs5.test.ts` pins the ids, groups, transports and words                                                                                                                                                                                                                                                                                                                                                                                                               |
| The dispatch seam               | `apps/cli/src/actions/deps.ts` and twelve empty lane modules (`motion`, `media`, `import`, `templates`, `page`, `print`, `prefs`, `spelling`, `equation`, `theme`, `chat`, `font`), spread by `registerLaneActions` at the end of `registerStoreActions` and again in the hosted dispatcher with `hosted: { deckId, request, origin }`; `apps/cli/package.json` exports `./actions/*`                                                                                                                                                                                                                                                                                                                                                                                                              |
| The chrome seam (14, 15)        | `MenuSetting` and `MenuClientHandler` extended, `Menu.setting?`; the rows still Later unchanged with `GS5_PLANNED_EFFECTS` (38 rows with their lane, `toolbar.font` included), `GS5_DIALOG_TITLES` (14), `GS5_PANEL_TITLES` (6); `GS5_KEY_ROWS` (22) in `keys.ts`; `ROUND_FIVE` in `menus/strings.ts` walked by the default view words test; `BLOCK_ICONS`, the picker role words; `menus/__tests__/gs5-seam.test.ts`                                                                                                                                                                                                                                                                                                                                                                              |
| The render and export seam      | `render/motion.ts` (`compileMotion` empty, `motionCss` empty), `render/theme-css.ts` (`themeCss` empty), the four new dither patterns as Bayer 8 in `effects/dither.ts`, the dither key with the optional angle; `Scene` with seven optional fields set in `extract.ts`, `scene/media.ts`, `scene/equations.ts`, `ooxml/media.ts` and `ooxml/math.ts` as identities called from `pptx/build.ts` after the alt text and before the grouping, `check/{motion,media,equations,odf,svg}.ts` answering ok, the three sections on `ExportCheck`; `fontkit` attached to the exporter                                                                                                                                                                                                                      |
| The identity and realtime seam  | `PrincipalRecord.preferences?` parsed through `preferencesSchema`; the `chat` entry kind, `ChatMessage`, `Entry.chat?`, `CHAT_TEXT_MAX_CODE_POINTS` 4000                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| The editor seam                 | `EditorMode` with `mode: 'theme'` in the snapshot (`editorMode`, `setEditorMode`) and `GS5_WINDOW_SLOTS` above the `on(...)` table in `controller.tsx`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| The font seam (A5, B7)          | `packages/schema/src/fonts.ts` (`FONT_IDS`, 26 ids, `DEFAULT_FONT_ID`, the categories and licences), `Typography.family`, the `font` handler module, `font.list` as `deck_list_fonts` and `turboslide fonts list`; the toolbar's Font row exists as `toolbar.font` with `enabled: 'never'` and its planned effect `client('fontPicker')`                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Catalog entries                 | `@xmldom/xmldom` 0.9.12, `temml` 0.13.5, `nspell` 2.1.5 with `@types/nspell` 2.1.6, `dictionary-en` 4.0.0, `-en-gb` 3.0.0, `-fr` 3.0.0, `-es` 4.0.0, `-pt` 4.0.0, `-pt-pt` 2.0.0, `-nl` 2.0.0, `fontkit` 2.0.4, `web-vitals` 6.2.2, attached to `import`, `render`, `spelling`, `export` and `studio`; `pnpm install` once (23 packages added, frozen install "Already up to date" afterwards); `node scripts/check.mjs --audit`: 0 advisories at high or above                                                                                                                                                                                                                                                                                                                                    |
| `packages/spelling`             | scaffolded (`package.json`, `tsconfig.json`, `vitest.config.ts`, `src/index.ts` with the seven dictionary tags, `index.test.ts`), referenced from the root tsconfig; B5 replaces the placeholder                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `scripts/check.mjs`             | steps 32 to 37 as gated stubs: each names its command and prints "not yet; waiting for <files>" with exit 0 until every file its lane lands exists (`--list` prints 37 with the pending counts 4, 6, 6, 7, 7, 3); step 37 needs the node-server build on 4321                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `pnpm generate:contracts`       | once: 11 of 14 contract files rewritten (`openapi.json` 63,004 to 82,130 lines from the block union and the slide schema every slide answering action inlines; the served MCP list stays under its budgets after `compactOutputSchema` weighs the definitions a property reaches)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `facts.json`                    | regenerated through `node scripts/build-brand.ts --facts` and `node scripts/build-home-assets.ts` (no count typed by hand): 220 actions, 194 MCP tools, 201 HTTP paths, 37 check steps; `--check` of the home assets passes; the README and `docs/brand.md` stay for the ship step                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `AGENTS.md`                     | the round five sections: the day 0 seam pattern, the handler modules, the dev server exception with the ports 4351 to 4358, the shared file rule, the catalog entries, the acceptance lines for steps 32 to 37 and the probe, the seams, the `file.email` container answer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Gates                           | `node_modules/.bin/tsc -b` exit 0; schema 509, chrome 501, export, studio home 26, agent 295, mcp 42, theme 79 vitest passed; `node scripts/check.mjs --only 1,2` green (0.3 s, 0.6 s); `node scripts/check.mjs --only 4,5,6` green in 103.5 s (19.7 s; 77.6 s with 290 files, 3,093 tests passed and 2 skipped; 6.2 s with the bundle check, the largest client chunk 579,662 B under 600,000, and the greps); `node scripts/check.mjs --only 32,33,34,35,36,37` six "not yet" lines, exit 0; prettier clean over every changed file                                                                                                                                                                                                                                                              |
| The `/new` boot probe           | 27 of 27 on a dev server on 4321 with `playwright.config.ts`'s environment on the file store (`untitled-20260915-6otg`: twelve edits each acknowledged at the next revision, the slide switch on one stream and one client id, the reload with zero collaborator marks and one roster row, the second tab as exactly one other participant, the finding 33 keystroke kept, trashed and deleted forever; `decks/` as found; the server stopped; the e2e lock taken and released)                                                                                                                                                                                                                                                                                                                    |
| Files                           | 71 tracked files changed and 46 new (the twelve handler modules and `deps.ts`, the six validator modules, the five check modules, the four export identities, the two render modules, the seven schema modules, the six spelling files, the three seam tests, this file and `integrator.md`); the generated contracts, `docs/grammar.md`, the skills references and `pnpm-lock.yaml` regenerated                                                                                                                                                                                                                                                                                                                                                                                                   |
| Deviations recorded             | `integrator.md` section 7: `Deck.media` beside `Deck.assets` (the union moved 96 reader sites in 40 files across every lane), `SpotlightBlock.picture` for the spec's `placeholder`, the custom template id guards, the dither angle kept out of the variant key, the guide colour key `x:800`, the unannotated `placeholder`, `DECK_SET_ROOTS`, the planned effects and key rows tables in place of flipped rows, the Font row already present, the export option rows as data, `text/spelling` as B5's lint rule, the MCP weight fix, the CLI USAGE flags, the regenerated facts, the contracts' growth, step 3 on an uncommitted tree, the gated steps, the four "new" Later rows left to their lanes, the request path for `SERVER_SIDE_WINDOW_ACTIONS`, the headers and the dialog registries |

The verifier's day 0 (the baseline run against the round four ship deploy, `verification-5/BASELINE.md` and `baseline-2026-09-15.json`) is the verifier's to record under `verification-5/` and in VERIFICATION-5.md; not the integrator's. The integrator's tree did not touch `packages/chrome/src/tokens.css`, the Perfect export's bytes for the GT deck (the post process rewrites are identities and the variant keys are unchanged) or round four's budgets.

## Merge 1

Day 2, 2026-09-15 (MILESTONES-5 "Integrator" Delivers item 2; `build-5/integrator.md` section 9): the tree at merge 1 is `BASE` plus the day 0 files, the seven lanes' day 1 and the requests of `b1.md` to `b7.md` that fall in the integrator's files. Shared checkout, no branch, nothing committed (ruling 1): 180 tracked files changed and 105 untracked entries against `d5a1be5`. Every lane's exports are the names other lanes read from merge 1; the signatures are in each `b<n>.md`.

| Lane                                   | Landed at merge 1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Exports (read from merge 1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Requests answered by the integrator                                                                                                                                                                                                                                                                                                                                                | Open (owner)                                                                                                                                                                                                                                                                                                                                                                              |
| -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1 motion (`b1.md`)                    | `schema/motion.ts` a leaf with the schedule types widened (`slideId`, `durationMs`, `box`), `validate/motion.ts` filled, `render/motion.ts` `compileMotion` with the 1.3 semantics, new `render/motion-css.ts` (the transition and effect rules, the 64 frame Dissolve as one bit PNG masks), the extractor's `paragraphCountOf` counting list items, `decks/fixture/motion` (11 slides, two media assets from ffmpeg), 77 tests with the snapshots pinned                                                                                                                                                                                                                       | `@turboslide/schema/motion`: `SWITCH_MS`, `FLY_EFFECTS`, `effectClass`, `effectiveTransition`, `slideHasMotion`, `nextAnimationId`, `ANIMATION_CHOICES_IN_ORDER`, `MOTION_ATTRS`, `MOTION_CLASSES`, `PARAGRAPH_CARRIER_TYPES`, `blockParagraphCount`, `motionTargets`, `motionEffectSchema`, `emptySchedule(slideId, transition)`, `MotionEffect.durationMs`, `.box`; `@turboslide/render/motion`: `compileMotion(slide, blocks, paragraphs, mediaLength?)`, `stepCount`, `motionBlocks`, `deckMediaLength`, `paragraphNodes`, `countParagraphs`, `isFlyEffect`; `@turboslide/render/motion-css`: `motionCss(schedule, page)`, `MOTION_BASE_CSS`, `MOTION_PERSPECTIVE_PX`, `BAYER8_RANKS`, `DISSOLVE_FRAMES`, `dissolveFramePng`; `packages/export/src/scene/extract.ts` `paragraphCountOf`; the show's contract of `b1.md` (the attributes and classes)                                                                                                                                         | R2 the `./motion-css` export; R3 the contracts regenerated (`deck_motion_schedule` carries the three fields); R4 (optional, `@turboslide/effects` on render) not taken, the pinned test guards the copy                                                                                                                                                                            | R1 `data-block` under `blockAttrs                                                                                                                                                                                                                                                                                                                                                         |     | motion`and`data-motion`on the slide root (B4,`render/slide.ts`, `blocks/context.ts`; MILESTONES-5 names merge 1, not landed); R5 the `playMedia`row rule (B2, day 4); R6`odp/motion.ts` reading the schedule (B4, day 5) |
| B2 media (`b2.md`)                     | `packages/store/src/media/` (the sniff, the four parsers, `mediaInfo`, 58 tests within 20 ms of ffprobe), `schema/blocks/media.ts` runtime (the one extension table, the caps, `effectivePlayback`, `playbackProblem`), `validate/media.ts` filled, the content type tables (blob store, assets route, headers, upload, bundle scan refusing a mislabelled container), `fixtures/media` (13 accepted, 9 refused, `facts.json`, `generate.mjs`)                                                                                                                                                                                                                                   | `@turboslide/schema/blocks/media`: `MEDIA_MIME_BY_EXTENSION`, `mediaMimeOfName`, `mediaKindsOfMime`, `mediaKindOfMime`, `MEDIA_BYTES_MAX`, `DECK_MEDIA_BYTES_MAX`, `mediaAssetFile`, `mediaAssetForBlock`, `posterOf`, `mediaBytesOf`, `effectivePlayback`, `playbackProblem`, `formatDuration`, `parseDuration`, `mediaTitle`; `@turboslide/schema/validate/media`: `validateMedia`, `mediaBlocksOf`; `@turboslide/store/media/{sniff,info,isobmff,ebml,mp3,wav}`: `sniffMedia`, `mediaInfo`, `isMediaRefusal`, `playabilityOf`, `mediaFormatOfName`, `mediaFormatProblem`, `parseIsobmff`, `parseEbml`, `parseMp3`, `parseWav`, the sentences; `@turboslide/store/bundle`: `MEDIA_ASSET_KINDS`, the widened `AssetKind`; `apps/studio/src/server/headers.ts` `INLINE_TYPES`, `upload.ts` `MEDIA_UPLOAD_CONTENT_TYPES`                                                                                                                                                                          | R1 the six `./media/*` exports of `packages/store`; R2 `@turboslide/store` as a dependency of `packages/headless` (no cycle; the store imports the schema alone)                                                                                                                                                                                                                   | day 3: `SERVER_SIDE_WINDOW_ACTIONS` for the four media ids, the CSP and Permissions-Policy rows, `api/x.upload.media` (integrator, on request); the sniff re-export in `headless/capture/shared.ts` (B2)                                                                                                                                                                                  |
| B3 import (`b3.md`)                    | `packages/import/src/pptx/{xml,units,package,theme,inherit,report}.ts` over fixture `01-text.pptx` (33,924 bytes, generated by `scripts/pptx-fixtures.py` in the fonts venv, byte identical on a second run), 74 tests; `export/ooxml/groups.ts` admits `mc:AlternateContent` and answers each group's id (`groups.test.ts`); `chrome/dialogs/upload-accept.ts` with the two Upload tabs accepting `.pptx`                                                                                                                                                                                                                                                                       | `packages/import/src/pptx/package.ts`: `openPackage`, `PptxPackage`, `validatePackage`, `readPresentation`, `refuseBySignature`, `sniffPackage`, `PackageRefusal`, the constants; `xml.ts`: `parseXml`, the walkers, `alternateBranch`, `effectiveChildren`; `units.ts`: `sheetMapping`, `emuToPx`, `boxToPx`, `szToPx`, `angleOf`, `percentOf`, `lineWidthPx`, `presetOf`; `theme.ts`: `readScheme`, `resolveColor`, `readFill`, `snapColor`, `themeRecordColors`, `SCHEME_RECORD_KEYS`; `inherit.ts`: `slideChain`, `placeholderOf`, `inheritedXfrm`, `levelStyle`, `paragraphStyle`, `bodyProps`; `report.ts`: `ImportReportBuilder`, `ROW_CODES`, `fidelityOf`; `@turboslide/export` `ooxml/groups.ts` `GroupResult.groups[].id`; `@turboslide/chrome/dialogs/upload-accept`: `UPLOAD_ACCEPT`, `PPTX_MIME`, `BUNDLE_MIME`, `isPptxFile`                                                                                                                                                      | B3-1 `@turboslide/store` and `@turboslide/export` as dependencies of `packages/import` with the tsconfig references; B3-2 `import-report.ts` widened (`source.producer`, `.sections`, `.embeddedFonts`, `.modifyVerifier`, `theme.scheme`, `validation.lines`, all optional); B3-3 and B3-6 recorded; B3-5 waits for day 5                                                         | B3-4 the `ThemeRecord.colors` keys (B6); the test only `__tests__/unzip.ts` retires against the store's `readZip` (B3)                                                                                                                                                                                                                                                                    |
| B4 the page (`b4.md`)                  | the page sweep: 126 sheet constant sites read `deckPage`, `grid(page)`, `geometry(page)` with no behaviour change across every package; `scaleCanvas`; the `shapePath` seam (the box path behind the final signature, 135 presets tested); the stage root's `data-page` and `--ts-sheet-w`/`--ts-sheet-h`; `decks/fixture/page-4-3` and `page-16-10`; `compare-to-shoot` and `canvas-fidelity` read the deck's page; the `block-css.ts` sheet literals fixed (canvas-fidelity on 4:3 from 24.6 percent to 0.019)                                                                                                                                                                 | `@turboslide/schema/render`: `contentBox`, `pageBox`, `pageCentre`, `pageAspect`, `pageInches`, `PAGE_CAP`, `presetOfPage`, `boxInsideSheet`, `coversPage`; `@turboslide/schema/canvas`: `scaleCanvas`, `scaleFactor`, `scalePosition`, `scaleGuides`, `SCALE_CANVAS_MODES`, the page parameters on `toCanvas`, `fromCanvas`, `applyGuides`, `picturePos`, `guideCentre`; `@turboslide/schema/freeform` and `diagrams` page parameters; `@turboslide/schema/shapes/geometry`: `shapePath`, `textInset`, `sites`, `boxPath`, `boxSites`, `pathData`, `PathCommand`; `@turboslide/theme/tokens`: `grid(page)`, `columnWidths`; `@turboslide/render/geometry`: `geometry(page)`, `chipsOf`, `colsWidths`, `slotBoxes`; `render/stage`: `sheetSizeStyle`, `pageAttr`; `render/print`: `printPagePt`, `printCss`; `@turboslide/export/units`: `pageIn`, `pageEmu`, `layoutName`, `isPageAspect`, `scenePage`; the viewer, chrome, headless, lint, studio and CLI page parameters of `b4.md` section 2 | R6 the `deck.page` sentence in `agent/generate/grammar.ts` and "the sheet (the deck's page, 1600 by 900 by default)" in `rules.ts`, `catalog.ts`, `position.ts`, regenerated; R7 no move                                                                                                                                                                                           | R1 the Add guide rows, `--ts-page-aspect` on the shell root, `toViewerDeck` page, `EditorRoot.tsx:1980` (integrator, day 3 with Page setup); R2 the material size literal (integrator and B2, merge 2, deviation 6 below); R3 the two tone screen (B2, B6); R4 `page` on `LiveClone` and `Stage` in the filmstrip, the presenter and the slideshow (B1, day 3); R5 the pointer clamp (B5) |
| B5 preferences (`b5.md`)               | `schema/preferences.ts` with the pointer reads and writes, `normalizePreferences`, the localStorage mirror, the one time migration, the `PreferencesStore` contract (21 tests); `identity/principal.ts` `principalPreferencesStore`; `apps/cli/src/actions/prefs.ts` (`prefs.get`, `prefs.set`) and `commands/prefs.ts`; `packages/spelling/src/index.ts` as the dictionary catalog; `scripts/check-dictionaries.mjs` and the seven dictionaries under `apps/studio/public/dictionaries/` (3.4 MB gzip, 12.7 MB inflated, budgets per tag)                                                                                                                                       | `@turboslide/schema/preferences`: `getPreference`, `setPreference`, `setPreferences`, `PreferenceError`, `normalizePreferences`, `PREFERENCES_STORAGE`, `preferencesMirror`, `migrateLegacySettings`, `PreferencesStore`, `memoryPreferencesStore`, `applyPreferenceWrite` (with the day 0 record); `@turboslide/identity/principal`: `preferencesOf`, `withPreferences`, `principalPreferencesStore`; `@turboslide/cli/actions/prefs`: `PrefsLaneDeps`, `registerPrefsActions`, `noPreferencesStore`; `apps/cli/src/commands/prefs.ts`: `prefs`, `PREFS_USAGE`, `parsePrefsValue`; `@turboslide/spelling/index`: `DICTIONARY_TAGS`, `DICTIONARY_PACKAGES`, `DICTIONARY_LICENCES`, `GPL_ONLY_DICTIONARY_TAGS`, `DICTIONARY_FILE_NAMES`, `DICTIONARY_GZIP_BUDGETS`, `dictionaryUrls`, `dictionaryTagFor`, the manifest types                                                                                                                                                                      | R1 `LaneDeps.preferences`, `localPreferencesStore` in `records/principal.ts` with the record passthrough, `storeDeps` composing it, the hosted `preferencesStoreFor(facts)`; R2 the `prefs` route and USAGE; R3 `SERVER_SIDE_WINDOW_ACTIONS_GS5` with the `prefs.set` mirror write; R7 no change; the acceptance line ran (`prefs set /units cm`, `prefs get /units` answers `cm`) | R4 the shell's preferences state (integrator, day 3 with the dialog); R5 `Cache-Control` on `/dictionaries/**` (B2's `headers.ts` or the integrator's route rules, day 4 with the Worker); R6 the spelling subpaths (integrator, day 4)                                                                                                                                                   |
| B6 theme (`b6.md`)                     | `packages/theme/src/themes.ts` (the specs per id, the frame variables, `stageFrame`, `sheetRootAttributes`), `sheet.css` reading seven frame variables (byte identical pixels), `render/theme-css.ts` filled (the scoped stylesheet per appearance, the derived tokens, the frame, the corner slot, the counter, the type levels), the three validator families (theme, layout, equation) with 35 tests                                                                                                                                                                                                                                                                          | `@turboslide/theme/themes` (and through `./theme`): `THEME_IDS`, `DEFAULT_THEME_ID`, `THEME_LABELS`, `isThemeId`, `ThemeSpec`, `THEME_SPECS`, `themeSpec`, `tokensFor`, `FRAME_VARIABLES`, `frameVariables`, `crossOffsetOf`, `GT_FRAME_HTML`, `stageFrame`, `sheetRootAttributes`, `SHEET_ATTRIBUTE`, `THEME_BASE_ATTRIBUTE`, `TYPE_LEVEL_SELECTORS`; `@turboslide/schema/validate/theme`: `THEME_COLOR_SLOTS`, `THEME_DERIVED_TOKENS`, `THEME_COLOR_KEYS`, `isThemeColorKey`, `validateTheme`; `validate/layout`: `validateLayouts`; `validate/equation`: `isMathMlElement`, `validateEquations`; `@turboslide/render/theme-css`: `themeCss`, `themeCssScope`, `THEME_CSS_SCOPE`, `THEME_CSS_STYLE_ID`, `hasThemeEdits`, `fontStack`; `render/theme-node`: `themeFolder`, `themePaths`, `LoadThemeOptions.theme`                                                                                                                                                                               | R1 the `./themes` export of `packages/theme`; R2 (the integrator's half) `render/thumb.ts` reads `stageFrame(deck.theme)` from `@turboslide/theme/themes`                                                                                                                                                                                                                          | R2 (B4's half) `stageFrame` and `sheetRootAttributes` in `stage.ts` and `slide.ts`, not landed; R3 `#rrggbbaa` on the alpha slots (integrator, day 5); R4 `fontFamilyName` (B7); R5 `LiveClone` `base` prop (B2, day 5); `theme-css.ts` moves to `@turboslide/theme/themes` (B6)                                                                                                          |
| B7 sync and fonts (`b7.md`)            | the font catalog (`packages/fonts/src/catalog.ts`, the generated `catalog-files.ts`, `catalog-node.ts`, `scripts/fetch-fonts.mjs`; 25 families fetched at google/fonts `1ac2012`, 72 woff2 and 25 LICENSE files, 14.6 MB), `render/fonts.ts` (`usedFontIds`, the `@font-face` rules, the `--ts-font-<id>` variables), `font.list` and `turboslide fonts list`, `THIRD_PARTY_NOTICES.md`'s catalog section, `scripts/probes/sync-stress-probe.mjs` with the `BASE` baseline (16 steps, 13 ok, the reload defect, `b7/sync-stress-base-2026-09-15.json`)                                                                                                                           | `@turboslide/fonts/catalog`: `FONT_CATALOG`, `FONT_SOURCES`, `catalogFont`, `fontFamilyStack`, `fontFamilyVariable`, `fontAssetPath`, `fontsInCategory`, `weightsOf`, `catalogSummary`, `GOOGLE_FONTS_COMMIT`; `catalog-files`: `CATALOG_FILES`, `CatalogFile`; `catalog-node`: `fontFileUrl`, `fontFileBytes`, `fontFileDataUri`, `fontFilesWithBytes`; `@turboslide/fonts/assets/*`; `@turboslide/render/fonts`: `usedFontIds`, `fontFaceRules`, `fontVariablesRule`, `fontsCss`, `deckFontsCss`, `fontSrcUnder`, `fontFaceCount`; `@turboslide/cli/actions/font`: `fontList`, `registerFontActions`; the CSS contract (`font-family: var(--ts-font-<id>, inherit)` on the block, the property on the sheet root)                                                                                                                                                                                                                                                                              | R1 the `font-family` declaration in `typographyDeclarations` with `typography.test.ts`; R2 the `./fonts` export of `packages/render`; R3 `font.list` in `SERVER_SIDE_WINDOW_ACTIONS_GS5`; R4 the `fonts list` USAGE line; R9 recorded (the regeneration changed no `font.list` row)                                                                                                | R5 `LoadThemeOptions.fonts` (B6, day 3); R6 the standalone faces (B1, day 6); R7 the runtime file glob and the `/fonts/<id>/<file>` route (integrator, day 3); R8 `a:latin typeface` (B5, day 5); the walk probe's three picture resize rows (B4, A8 row 1)                                                                                                                               |
| Integrator (`integrator.md` section 9) | the requests above in the integrator's files: five `package.json` files and two `tsconfig.json` files, `pnpm install` once (three workspace edges, the lockfile +9 lines), `pnpm generate:contracts` once (6 of 14 files: `mcp-tools.json`, `openapi.json`, `llms-full.txt`, `docs/grammar.md`, two skill references), `typography.ts`, `import-report.ts`, `rules.ts`, `catalog.ts`, `position.ts`, `grammar.ts`, `actions/deps.ts`, `records/principal.ts`, `write.ts`, `store-actions.ts`, `cli.ts`, `server/actions.ts`, `server/agent-actions.ts`, `editor/controller.tsx`, `render/thumb.ts`, two new tests (`schema/typography.test.ts`, `cli/records/principal.test.ts`) |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |                                                                                                                                                                                                                                                                                                                                                                                    | the day 3 rows above; `SERVER_SIDE_WINDOW_ACTIONS_GS5` grows as each lane's server side ids land                                                                                                                                                                                                                                                                                          |

Gates on the merged tree (every command from the repository root unless a `cd` is shown; the e2e lock taken at 07:28:11 and released at 07:31:55; the dev server on 4321 started for the probe alone and stopped):

| Gate                                                                                                                                                                                | Result                                                                                                                                                                                                                                                                                                                                                                  |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm install`                                                                                                                                                                      | once, for the three changed dependency lists; "Already up to date" for packages, the workspace links `import → store`, `import → export`, `headless → store` made, `pnpm-lock.yaml` +245 lines against `BASE` (+236 at day 0)                                                                                                                                           |
| `pnpm generate:contracts`, then `node packages/agent/src/generate/main.ts --check`                                                                                                  | wrote 6 of 14 files; "every committed contract is current" (the stale `MotionSchedule` of B1's day 1 and the sweep cleared; step 3 as written fails on an uncommitted tree as in every round, day 0 item 16)                                                                                                                                                            |
| `node_modules/.bin/tsc -b`                                                                                                                                                          | exit 0 over the whole tree, 18.3 s wall                                                                                                                                                                                                                                                                                                                                 |
| `node scripts/check.mjs --only 1,2`                                                                                                                                                 | ok in 0.3 s and 0.9 s                                                                                                                                                                                                                                                                                                                                                   |
| `node scripts/check.mjs --only 4,5,6`                                                                                                                                               | all three passed in 84.7 s: step 4 ok (0.4 s, incremental); step 5 `pnpm test` ok in 78.1 s with 324 files, 3,421 tests passed and 2 skipped (every package's suite; 290 files and 3,093 tests at day 0); step 6 ok in 6.2 s, the largest client chunk `index-B9Q-dc8_.js` 580,375 B under 600,000, 25 innerHTML call sites and 13 `overwrite: true` in the allowlists  |
| `node apps/cli/bin/turboslide.mjs render all --deck decks/gt-brand --theme light,dark --scale 1 --out .turboslide/render --json`                                                    | 170 records, 0 with page errors, 17 s                                                                                                                                                                                                                                                                                                                                   |
| `node scripts/compare-to-shoot.mjs --deck decks/gt-brand --render .turboslide/render --shoot /Users/kevinliu/repos/Prototemplate/deck --max-mismatch 0.005`                         | 170 pairs, 170 compared, 0 skipped as escapes, 0 over budget; worst non escape 0.408 percent, mean 0.016 percent, budget 0.50: the same line as `BASE` and the six round four runs, so no pixel moved                                                                                                                                                                   |
| `node scripts/canvas-fidelity.mjs --deck decks/gt-brand --deck decks/templates/gt-brand --deck decks/templates/blank --max-mismatch 0.005 --out .turboslide/canvas-fidelity-merge1` | 171 slides converted, 342 pairs, 0 over budget; worst 0.262 percent (`gt-brand/directions` dark), mean 0.004 percent, 82.1 s: unchanged from B4's day 1 run                                                                                                                                                                                                             |
| `node scripts/probes/new-write-probe.mjs --base http://localhost:4321`                                                                                                              | 27 of 27 rows ok on a dev server started from `apps/studio` as `node_modules/.bin/vite dev --port 4321 --strictPort` with `playwright.config.ts`'s environment on the file store (`untitled-20260915-1170` created, edited at the next revision each time, reloaded with one roster row, the finding 33 keystroke kept, trashed and deleted forever; `decks/` as found) |
| `git diff --exit-code d5a1be5 -- packages/chrome/src/tokens.css`                                                                                                                    | empty                                                                                                                                                                                                                                                                                                                                                                   |
| `node apps/cli/bin/turboslide.mjs prefs set /units cm --deck decks/fixture/page-16-10 --author merge1` then `prefs get /units`                                                      | the record with `units: cm`, then `{ "path": "/units", "value": "cm" }` (B5's acceptance line, request 1); the principal file written under `.turboslide/principals/` removed afterwards                                                                                                                                                                                |
| `node_modules/.bin/prettier --check` over the 24 files the merge edited                                                                                                             | clean                                                                                                                                                                                                                                                                                                                                                                   |
| `node scripts/check.mjs --list`                                                                                                                                                     | 37 rows; 32 to 36 still gated (2, 4, 4, 6, 6 files pending); 37 no longer gated because B7's files exist, so it runs against the node-server build from here and reads the sync stress probe's `BASE` defect (exit 1) until B7's engine work lands (deviation 8)                                                                                                        |

Deviations and decisions at merge 1 (`integrator.md` section 9.4): `font.list` served rather than answered in the page (the catalog's file table stays out of a client chunk 19.6 KB under its ceiling); the `prefs.set` window handler writes the localStorage mirror now and the shell's state waits for B5's dialog; `readLocalPrincipal` drops a malformed preferences member alone; `storeDeps` answers `LaneDeps` and `registerStoreActions` takes it so the record flows with its type; B3-2 landed at merge 1 (wanted by day 4) with the contracts regenerated once; B4's R2 deferred to merge 2; B1's optional R4 not taken; step 37 ungated by B7's files; the prose of R6 keeps the default's numbers beside the name. Not this merge's: B1's `data-block` request and B6's `stageFrame` request to B4 (`render/slide.ts`, `stage.ts`, `blocks/context.ts`, B4's files) are open at merge 1 and named above.

## Merge 2

Day 8, 2026-09-15 (MILESTONES-5 "Integrator" Delivers items 3 and 4; `build-5/integrator.md` section 10): the tree at merge 2 is merge 1 plus every lane's days 3 to 7 as delivered (`b1.md`, `b2.md`, `b3.md`, `b5.md`, `b6.md`, `b7.md`; B4 delivered no report past day 2 and its days 3 to 7 surfaces are absent, see its heading), the requests those reports name answered in the integrator's files or with the smallest edit in a lane's file with a note, and the integrator's own engineering of item 3. Shared checkout, no branch, nothing committed. Preview deployment of the merged tree from the repository root: `https://turboslide-1c19vi1gj-kl01s-projects.vercel.app` (`vercel deploy --yes --archive=tgz`, "Build Completed in /vercel/output [47s]", four pages prerendered: `/home`, `/help/training`, `/help/updates`, `/decks/templates`), behind Vercel Authentication like every preview of this project, so every probe carries `VERCEL_OIDC_TOKEN` from `vercel env pull`.

### B1 motion (`b1.md`)

- Landed: the schedule and the compiler of merge 1 plus the renderer's transition and effect rules, the export's `--motion keep|drop` with the renumber, the transition and the timing after the hidden title, `checkMotion` in the check, the standalone build's motion runtime (`--motion`, `--autoplay <ms>`, `--loop`), the viewer deck's compiled `motion` from `buildViewerDeck`, the `motion.play` window row, the filmstrip's motion glyph, the Motion panel (`MotionPanel` lazy).
- Cut: nothing named.
- Open requests: none left in the integrator's files.
- Deviations: none recorded beyond the day 0 schedule widening.

### B2 media (`b2.md`)

- Landed: the media block renderer and the picture effects, the intake (`media.insert` over file, URL, upload and YouTube through the oEmbed title), the playback and poster actions, the camera and screens dialogs, the presigned upload path, the export's `--media embed|poster|url` and `checkMedia`, the content type defaults in the OOXML clean pass, the dither worker's preview frames, the media sections of the inspector, the access record's `assetKey` rotation, the five media ids on the server side list.
- Cut: the Search YouTube tab stays drawn and disabled (SPEC-5 17 row 3); `view.presentOnScreen` on the server answers `{ supported: false }`.
- Open requests: R7 (`onMediaFile` on the viewer's drop path) is B4's and did not land; the Share dialog's interim sentence was never present, so nothing to remove.
- Deviations: `Deck.media` stays a second map (B2's call at merge 1); the duplicate `camera` discriminator in `MediaAssetSource` was removed at merge 2 (integrator.md 10.4 item 1); the media Range 206 and media grant smoke rows are not in `hosted-smoke.mjs` (the smoke deck carries no media asset).

### B3 import, templates, building blocks, nested groups (`b3.md`)

- Landed: the PowerPoint reader over the five fixtures with the python oracle, `import.pptx`, `theme.import`, `slide.import --file` and `--template`, the Upload tabs accepting `.pptx`, the bundle route's `.pptx` branch, the Import report dialog on the first open, the templates page (`/decks/templates`, prerendered) with the templates index and the building blocks pane, the nested group paths in `block.group` and `block.ungroup`, the `mc:AlternateContent` group rewrite, `docs/import-pptx.md`.
- Cut: nothing named.
- Open requests: B3-5 (`IMPORT_PPTX` retired), B3-7, B3-9 and B3-10 answered at merge 2; the templates gate of step 34 lints every template but `gt-brand` (27 findings that predate the round) and `blank`.
- Deviations: the expected documents of the import fixtures are `canonicalJson` bytes and now sit in `.prettierignore` (the merge's formatter pass had reflowed them, restored with `canonicalJson` and never touched again).

### B4 the page, print, ODP, SVG, the shape interpreter, the card (`b4.md` at its day 2 state)

- Landed: the page sweep and the `shapePath` seam of days 1 and 2 (merge 1); during day 8 the shape interpreter under the seam (`shapes/definitions.ts`, `ids.ts`, `geometry.ts` filled) and `packages/render/src/print-layout.ts`, without a report and without their tests.
- Cut, as delivered: the Page setup dialog and `page-setup.spec.ts`, the print layouts dialog, the ODP writer (`export-odp.ts`), the SVG writer (`svg/write.ts`), the card render path in `server/thumbs.ts`, the `og:image` line, `onMediaFile`, the `InlineText` autocorrect props, `data-block` and `stageFrame`.
- Open requests: every request other lanes put to B4 (b2.md R1, R7; b5.md; b6.md) stands open; the integrator's B4 R2 of merge 1 (the card path) has no surface to land on.
- Deviations: `file.download.odp`, `file.download.svg` and `file.pageSetup` stay Later (the Later count reads 5 leaf rows and 6 over `allItems()`, against 14.2's 2 and 3); check steps 32 and 36 answer `not yet`; the interpreter changed the text rectangle of the roundRect preset and five test files pin the day one seam: `packages/schema/src/shapes.test.ts`, `packages/schema/src/shapes/geometry.test.ts`, `packages/schema/src/diagrams.test.ts` (12 `connect` rows), `packages/render/src/__tests__/canvas.test.ts` (12 snapshots), `packages/export/src/gslides-fixture.test.ts` (the padded shape row: `tIns + bIns` 203,606 EMU against 121,920). Each is B4's to update; step 5 of the merged tree carries them.

### B5 preferences, spelling, autocorrect, dictionary, chat, accessibility (`b5.md`)

- Landed: the Preferences dialog over the mirror and the record, the spelling engine over the seven dictionaries with `spelling.check`, `spelling.replace` and the Spelling and grammar dialog, the `text/spelling` rule over an injected checker, autocorrect (`text.autocorrect`), the personal dictionary, Tools > Dictionary through `/api/define` with `TURBOSLIDE_DICTIONARY`, the chat panel over the room channel, the Accessibility menu under screen reader support with the verbalize region, the guides dialog with colours, indentation options, restart numbering, prefix and suffix, delete versions, the two help routes (`/help/training`, `/help/updates`, prerendered), the drawing box's recogniser.
- Cut: nothing named; the hosted `version.delete` refuses (no store level version removal exists).
- Open requests: R4, R9, R11 and the ports answered at merge 2; the `InlineText` autocorrect props are B4's and did not land.
- Deviations: the chat port is composed in the hosted dispatcher over the room channel because B7's `admitChat` never landed; `packages/agent/src/__tests__/fixtures.test.ts` waives the planted slide for `text/spelling`.

### B6 theme mode, layouts, equations, the crate patterns (`b6.md`)

- Landed: the theme mode (`ThemeMode`, the theme css style, the layouts and placeholders, `theme.*` and `layout.*` actions), the imported themes, the equation block with Temml (the toolbar, the inspector section, `equation.*` actions, the OOXML `a14:m` wrapper with the Fallback picture and `checkEquations`), the validator's `r:id` and `a14:m` rules (SPEC-5 8.3), the dither patterns in the crate, the theme and equation validators.
- Cut: the `equation/parse` lint rule (R7).
- Open requests: `data-block` and `stageFrame` (B4) did not land; the allowlist row for `equation.ts` landed at merge 2; `native.yml` written for the crate patterns (untracked).
- Deviations: none beyond R7.

### B7 the sync engine and the font catalog (`b7.md`)

- Landed: the sync engine rework (the rebase, the second person step, the `movedSentence` wording, `attachChrome` and `setShowState` on the controller, the transport pick), the font catalog with the picker (`FontField` in the toolbar, `MoreFonts`), the fonts route and the runtime glob, the viewer's font stylesheet, the standalone build's `@font-face` css, `docs/sync.md`, `docs/fonts.md`, the sync stress probe as a smoke row.
- Cut: `admitChat` (b5.md R21).
- Open requests: C1 to C6, E1, F1, F2, F3 applied through `integrator-lines.py`; R20 (the wording) and R22 (`docs/README.md`) answered.
- Deviations: none recorded.

### Integrator (`integrator.md` section 10)

- Landed: the SPEC-5 14.1 flips with their effects and the 14.2 clauses retired (`DOWNLOAD_FORMATS` kept, B4), the Google key rows moved out of `OMITTED_SHORTCUTS`, the access key matching, the hero gate behind `HERO_LIVE`, `api/vitals.ts` with the sample and the limiter, `VitalsReporter`, `vitals-report.mjs`, the INP row, the WebSocket flag with the client transport and the dev sidecar on 4322, `lighthouse.yml` and `native.yml` (untracked), the cold first byte gate (1,200 ms on the deployment profile) and the function directory gate (200 MB in `check-vercel-output.mjs`), the Starred view of the home page (B5's 7.7 row on the integrator's side), the real commands of steps 32 to 37 with `--list` at 37, the hosted smoke rows of 16.8 (`/decks/templates`, `/help/training`, `/help/updates` twice, `/api/vitals`, the PowerPoint import, the chat, the sync probe), the tooltip and layout shift audit page lists, the perf budget routes, the prerender list, the `IMPORT_PPTX` retirement, `facts.json` and the home assets regenerated from the tree, `docs/README.md`, `docs/hosting.md` section 13, `docs/performance.md` 3.7 and 8, AGENTS.md's round five sections.
- Cut: the Nitro upgrade route on the node-server preset (no resolvable WebSocket server in the studio; a new dependency is a request), the two-tone screen pass on the live hero frame.
- Merge fixes in other lanes' areas, each with a note in the file: `packages/schema/src/assets.ts` (the duplicate `camera` discriminator), `packages/export/src/pptx/text.ts` (`catalogFace`, `registerBoxLink`), `packages/agent/src/__tests__/fixtures.test.ts`, `scripts/gslides-parity-audit.mjs` (the bar reads `visibleMenus`), the formatter pass over 84 lane files and three round four records, the import fixtures restored and ignored.
- Deviations: `integrator.md` 10.5.

### Gates at merge 2

The full table with times is `build-5/integrator.md` 10.3. In short, at 14:32: `tsc -b` clean; contracts current and uncommitted (step 3); steps 1, 2, 4, 6 to 19 green, 5 carrying B4's five files and four load timeouts that pass alone, 20 fixed and re-queued with 21 to 37 behind the e2e lock (`scratchpad/check-chain2.log` of the session); the first preview's fetch smoke 39 of 39 with the trusted sources token and the bearer rows skipped; the vitals route exercised on the preview (three 204s, a p75 of 120 ms over three samples, 413 on an oversize body); the editor walk probe at 228 rows ok and 0 failed and still running, the full smoke with `--sync-probe` and the deployment perf run queued behind it; the second deployment of the final tree at `https://turboslide-ho9rltx7u-kl01s-projects.vercel.app`; `tokens.css` unchanged against `BASE`; the tree formatted.
