# The return round, B1 the chrome and the menu model

Notes of builder B1 of the return round (docs/RETURN.md section 4, the menu model half of sections 2 and 3, section 6's B1 rows), in the shape of the focus round's lane notes. The binding specification is `docs/RETURN.md`; `docs/FOCUS.md` and `docs/gslides-parity/focus/AMENDMENTS.md` A1 stand under it; the audits under `docs/gslides-parity/return/audit-*.md` are the evidence. Section numbers refer to `docs/RETURN.md` unless prefixed FOCUS, SPEC, SPEC-2, SPEC-3 or SPEC-4. Nothing here ran a git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build`. Scratch files live under `.turboslide/b1-return/` (the dev server log, the runner, the vitest log), never in the repository's tracked tree; the evidence of this round is under `docs/gslides-parity/return/build/b1/`.

## Return round

### 0. Owns

The lane prompt's list, read against section 6's table (binding where the two differ):

- `packages/chrome/src/TitleRow.tsx`, `TitleRow.css`; `EditorShell.css` (the notes slot rule); `EditorToolbar.css` (`.ts-tb-sep`); `presence/presence.css`; `Menu.tsx` (`placeMenu`, `returnFocusTo`); `tokens.css` (`--pt-hair-on-ink`, `--pt-plate-on-ink`, `--pt-plate-on-ink-open`, `--pt-ctl-h`); `menus/model.ts` (the `advanced` flags of every returning row and context entry; the Themes panel's stub); `menus/toolbar-tails.ts` (`toolbar.highlightColor`; `parkedTail` for the shape, line, table, chart and group tails); `inspector/format-sections.ts` (the Table, Chart data and Shape sections' flags); `EditorShell.tsx` (`runBinding` `key.commit`, the Viewing mode's toolbar, the comments glyph, the Comments display rows); `useEditorKeys.ts` (the Enter path; untouched, see 1.4); the Themes panel stub (`ThemesPanel.tsx`); `menus/__tests__/menu-component.test.tsx` (the `placeMenu` end alignment case); `default-view-words.test.ts`, `menu-model.test.ts` and the tests of the files above.
- `ToolButton.css` where the split button needs it: not touched (4.1 says it is not touched, and the wrapper's `.pt-ib.is-solid` chain outranks it without an edit).
- Read where a name is loose: "the first write naming with B7" (the prompt) is the deck's name of 2.18, which section 6 gives to B2 (`autoTitleMutations`, `controller.tsx`; the tree carries B2's `apps/studio/src/editor/auto-title.ts`). The title row reads `input.document.deck.title` and needs no change for it; `decks.name.follows-heading` (B4) measures the name in the title row, and this lane changed nothing there.
- Edited because my flag changes broke their assertions, and recorded here rather than left red (the focus round's b1 did the same): `__tests__/ContextMenu.test.tsx`, `sidebar-filmstrip.test.tsx`, `format-options-round-two.test.tsx`, `versions-panel-switch.test.tsx`, `shortcuts-dialog.test.tsx`, `tool-finder.test.ts`, `editor-shell-render.test.tsx`, `presence-slot.test.tsx`, `insert-menu.test.tsx`, `palette-data.test.ts`, `menus/__tests__/toolbar-tails.test.ts`. Every change asserts the returned row in the default view and the still parked rows behind the switch; nothing was deleted, and each edit names the RETURN.md section it follows.
- New: `__tests__/title-row-slideshow.test.tsx` (the split button's structure, ARIA and keys, `key.commit`, the comments toggle, the slots, Viewing mode); `docs/gslides-parity/return/build/b1/shoot-chrome.mjs` (the drive of section 5 below).
- Not edited, requested below: `editor-shell.ts` (B2), `Editor.tsx` (B3), `packages/lint/src/chrome.ts` and `.prettierignore` (the integrator), `ThemesPanel.css` (the integrator), `scripts/probes/core-walk/areas/*` (B4).

### 1. What landed

#### 1.1 The Slideshow split button as one control (4.1)

`TitleRow.tsx` and `TitleRow.css`, `Menu.tsx`, `tokens.css`, `EditorShell.tsx`:

- Structure. The wrapper `span.ts-title-slideshow` owns the box: `box-sizing: border-box; height: var(--pt-ctl-h); border: 1px solid var(--pt-ink); border-radius: 8px; background: var(--pt-ink); color: var(--pt-paper); overflow: hidden; display: inline-flex; align-items: stretch`. The halves are transparent inside it through the chain `.ts-title-slideshow > .pt-ib.is-solid { height: 30px; border: 0; border-radius: 0; background: transparent; color: inherit; flex-direction: row; gap: 6px }` (specificity 0,3,0 over `ToolButton.css`'s 0,2,0, which the audit found winning over the plain class names); the chevron half adds its own class to the chain (`.ts-title-slideshow > .pt-ib.is-solid.ts-title-present-arrow`, 0,4,0) so its `border-left: 1px solid var(--pt-hair-on-ink)` outranks the `border: 0`. The play glyph comes before the word (the markup already had that order; the row reversal came from `ToolButton.css`).
- Geometry. The label half `padding: 0 10px 0 12px`, a 12 px glyph, the word in `.pt-lb`; the chevron half 28 px wide, `padding: 0`, a 14 px chevron. At 900 px the word leaves and the label half is `0 8px`.
- Tokens (`tokens.css` `:root` and `[data-theme='dark']`): `--pt-hair-on-ink` `rgba(255, 255, 255, 0.26)` / `rgba(7, 7, 7, 0.26)`; `--pt-plate-on-ink` `.16`; `--pt-plate-on-ink-open` `.24`; `--pt-ctl-h: 32px`. Each is a paper tint over ink with no colour (rule 6 of section 1); the composites are in the token file's comment and measured in section 5.
- States. Hover per half `background: var(--pt-plate-on-ink); color: inherit` (no inversion); the open chevron `[aria-expanded='true']` on `--pt-plate-on-ink-open`; focus `outline: 1px solid var(--pt-paper); outline-offset: -3px` on the half that has it.
- ARIA. The wrapper `role="group" aria-label="Slideshow"`; the label half a plain button with the text Slideshow (no `aria-haspopup`); the chevron half `aria-label="Presentation options" aria-haspopup="menu" aria-expanded`, and `aria-controls="ts-menu-slideshow"` only while the menu is mounted (`SLIDESHOW_MENU_ID`, exported from `TitleRow.tsx`). `returnFocusTo` follows the half that opened the menu (`openedBy` state: `'label' | 'arrow' | null`).
- Keyboard. ArrowDown on the label half (no modifier) opens the menu with the first row focused, as on the chevron; Escape returns focus to the label. Enter: the mechanism of the drive (`return-drive.md` rows 7 to 12) is fixed at its owner, `EditorShell.tsx` `runBinding` `key.commit`: the binding answers true only while a crop is open, read as the overlay's `.ts-crop-frame` being in the document (`Overlay.tsx` draws it only in crop mode), so a bare Enter with no crop is left to the focused element and the browser activates the button. `useEditorKeys.ts` is untouched, as 4.1 expected. Tab from the label to the chevron is B3's `Editor.tsx` branch (4.1); the drive of section 5 reads it as it stands.
- The menu. `placeMenu` (`Menu.tsx`) gains `align?: 'start' | 'end'` for `placement: 'below'` (`left = anchor.right - size.width` for `end`), threaded through `MenuProps`, `ListProps` and the root `MenuList`; the Slideshow menu is anchored to the wrapper with `align="end"` so the plate's right edge meets the control's right edge instead of clamping at the viewport. The unit case of 4.1 (`menu-component.test.tsx`: anchor 1209 to 1342, size 220 by 96, viewport 1440 by 900, `below` with `end`, expected left 1122 and top 40) passes; the same test shows the chevron anchored start alignment clamping at 1212, the placement audit-chrome row 12 measured.

#### 1.2 The separators (4.2)

- `EditorShell.css`: the notes slot draws no line (its `border-top` is gone); `NotesPane.css` keeps the pane's one `--pt-hair` top edge with the handle on it. The header comment of the sheet states the law.
- `EditorToolbar.css` `.ts-tb-sep`: the token stays `--pt-hair`, 1 by 20 px, `margin: 0 4px` in the 4 px gap row (8 px of air each side); the rule gained the comment of 4.2 item 2 and no value changed (`chrome.separators.toolbar-dividers` is "works" with today's geometry). `--pt-hair-soft` was not adopted, as 4.2 item 2 says.
- `presence.css` `.ts-presence-rule`: 1 by 24 px in its own grid track with the 8 px and 7 px tracks as its air; no value changed (a `margin: 0 4px` would break the slot's 184 px sum of fixed tracks), the comment says why.
- `tokens.css`: the light `--pt-hair` stays at `.18` (question 3's default). Every boundary is read in both appearances in section 5.

#### 1.3 The right cluster (4.3)

- `TitleRow.css`: `.ts-title-share` on `--pt-ctl-h` with `border-radius: 8px` (question 1's default, the pair on one corner); `.ts-title-slot` on `--pt-ctl-h`; `.ts-title-inbox-slot.is-empty { display: none }` (question 9's default: the parked plate leaves no 60 px hole; the slot keeps its width whenever `title.inbox` is present, and `TitleRow.tsx` already marks it `is-empty`).
- `presence.css`: `.ts-presence-more.is-empty { opacity: 0; pointer-events: none }` so no empty `+N` box is drawn while nobody else is present (`PresenceSlot.tsx` already sets the class at `more === 0`).
- `TitleRow.tsx`: the comments glyph toggles (`shell.panel === 'comments' ? shell.closePanel() : shell.runItem(comments)`) with `aria-pressed` following the panel.

#### 1.4 The shell (2.14 item 6, 2.16, 4.1)

`EditorShell.tsx`: `key.commit` as in 1.1; View > Mode > Viewing draws no toolbar (`mode === 'viewing' ? null : <div className="ts-toolbar">`), and `EditorShell.css` zeroes the row from the root's `data-edit-mode` (`.pt-viewer.is-editor[data-edit-mode='viewing'] { --pt-tool-h: 0px }` and `> .ts-toolbar { display: none }`), so the body moves up 40 px and `--ts-top-h` (the overlay filmstrip's offset) follows; View > Comments > Show all comments opens the Comments panel and Hide comments closes it when it is the open panel (`runToggle` `case 'comments'`; Expand and Minimize change the markers alone). `useEditorKeys.ts` is untouched.

#### 1.5 The Themes panel (2.13)

`ThemesPanel.tsx`: the Import theme stub (the disabled button at the foot, `data-control="themes.import"`) is not drawn while the GT theme is the one theme; the two tiles stay. The words stay in `strings.ts` for the round that adds themes; `ThemesPanel.css` keeps the `.ts-themes-foot` and `.ts-themes-import` rules unused (a request below).

#### 1.6 The menu model (sections 2 and 3), `menus/model.ts`

The `advanced` flag left every returning row and the `parked()` wrapper left every returning container (52 edits, applied by an exact match script and formatted by prettier): `title.presence.goTo`; `file.new.templateGallery`, `file.open`, `file.importSlides`, `file.download.jpg`, `.png`, `.zip`, `file.versionHistory.showChanges`, `file.details`; `edit.selectNone`; `view.showRuler`, `view.guides` (whole; `view.guides.edit` stays a Later stub), `view.snapTo` (both), `view.comments` (four), `view.livePointers` (two), `view.showFilmstrip`, `view.mode` (three), `view.fullScreen`, `view.appearance` (three); `insert.shape` (the container and its three named rows; `insert.shape.gallery`, `insert.shape.arrows`, `insert.shape.callouts`, `insert.shape.equation` keep their own flags), `insert.table`, `insert.chart` (whole), `insert.diagram`, `insert.wordArt`, `insert.line` (the container, Line, Arrow, `insert.line.elbowConnector`, `insert.line.curvedConnector`; Rule, Curve, Polyline and Scribble keep their flags), `insert.slideNumbers`; `format.text.superscript`, `.subscript`, `format.text.capitalization` (whole), `format.alignIndent.justified`, `format.spacing.addBefore`, `.addAfter`, `.custom`, `format.table` (whole), `format.bordersLines` (whole), `format.editData`, `format.chartType` (whole); `slide.changeTheme`; `arrange.distribute` (whole), `arrange.rotate` (whole), `arrange.group`, `arrange.ungroup`, `arrange.regroup`; `tools.checkSlides`; `help.improve`; the toolbar's `toolbar.paintFormat`, `toolbar.insertShape`, `toolbar.insertLine`, `toolbar.theme`, `toolbar.hideMenus`.

The context entries of FOCUS 3.4 were parked on the item, not per entry, so the same edits return `arrange.rotate`, `arrange.distribute`, `arrange.group`, `arrange.ungroup` on `textBlock`, `shape` and `line`, `view.guides` on `emptyCanvas`, `image` (covering the sheet) and the `guide` target (Delete guide), `slide.changeTheme` on the card and the canvas, `format.text.superscript`, `.subscript`, `.capitalization` on `textSelection`, the eleven `format.table.*` rows on `tableCell` and `cellRange`, `format.editData` and `format.chartType` on `chart`, `format.bordersLines.lineStart` and `.lineEnd` on `line`. The toolbar (3.2) and the Format menu (3.3) hold the orders section 3 gives without a move: the head reads Search the menus, New slide, Undo, Redo, Print, Paint format, Zoom; the default tail Select, Text box, Image, Shape, Line, Comment, Background, Layout, Theme, then the Hide the menus chevron (Transition stays a hidden Later stub). Search the menus (`finder.ts`) and the shortcuts dialog (`ShortcutsDialog.tsx`) read `isPresent`, so the returned rows and their chords are listed with the switch off and the parked ones with it on alone (asserted in `menu-model.test.ts`, `shortcuts-dialog.test.tsx`, `tool-finder.test.ts`).

What keeps the flag, by RETURN.md's word: `title.presence.follow`, `title.presence.me`, `title.account` (whole), `title.inbox` (2.16, section 8); `file.makeCopy.selected`, `file.share.publish`, `file.download.txt`, `file.download.html` (2.17, section 8); `view.gridView`, `view.showSections` (section 8); `insert.image.byUrl`, `insert.image.fromThisPresentation` and the two under Replace image (3.4); `insert.shape.gallery`, `.arrows`, `.callouts`, `.equation` (2.9); `insert.line.rule`, `.curve`, `.polyline`, `.scribble` (section 8); `insert.specialCharacters` (section 8); `insert.icon`, `insert.material` (2.10); `format.bulletsNumbering.listOptions` (2.15); `format.image.maskImage` (2.9), `format.image.dither` (3.4); `format.altText`, `format.dropShadow` (questions 6 and 7, see 2.4 below); `format.changeShape` (2.9); `format.editHtml`; `tools.notificationSettings` (2.16), `tools.activityDashboard`, `tools.spelling`, `tools.advanced` (section 8); `extensions.agentAccess`, `extensions.embedInSite` (section 8). The Later stubs stay Later.

#### 1.7 The toolbar tails, `menus/toolbar-tails.ts`

`toolbar.highlightColor` lost its flag (3.2); `SHAPE_TAIL`, `LINE_TAIL`, `TABLE_TAIL`, `CHART_TAIL` and `GROUP_TAIL` left `parkedTail` (only `toolbar.changeShape` keeps its own flag on the shape tail, with the galleries); `toolbar.pointer` of the tail end lost its flag with the live pointers (2.16). What stays: the text tail's fill and border controls (`fillAndBorder('text')`, question 8), the image tail's frame controls and Dither (3.4), `OTHER_TAIL` whole (icons and materials, 2.10). `ToolbarTail.tsx`'s fallback to the default tail for a tail parked whole now reaches the other tail alone.

#### 1.8 Format options, `inspector/format-sections.ts`

The Table, Chart data and Shape sections lost their flags with their features (2.2, 2.4, 2.5); Dither, Drop shadow and Alt text keep theirs (2.15, 3.4, questions 6 and 7).

### 2. Deviations and readings recorded

1. `key.commit` reads the crop state from the DOM (`.ts-crop-frame`) rather than from a typed handle flag. 4.1 names two mechanisms ("the handle exposes whether one is, or `exitCrop` returns a boolean"); both need `editor-shell.ts` (B2, the `EditorHandle` type) and `Editor.tsx` (B3, the handle at 3118), so the DOM read lands the fix inside this lane's file today, with the same precedent the switch already has (`key.comment.enter` reads `[data-control="comment.card"]`). Requests R1 and R4 ask for the typed flag; the DOM read leaves with it.
2. `EditorShell.css` gained two rules beyond the notes slot rule (the Viewing mode's `--pt-tool-h: 0px` and the hidden toolbar): section 6 gives B1 "the Viewing mode's toolbar" in `EditorShell.tsx`, and the grid row's height has no other home. The toolbar is also not rendered in Viewing, so a driver that reads the element's presence (audit-surface row 56 read "toolbar true") and one that reads its box both see it gone.
3. The Themes panel's stub is removed from the render rather than hidden by a rule: "hidden in the panel" (2.13) is what a seller sees either way, and a disabled row drawn with `display: none` would still be a control the tooltip audit's `--advanced` pass could expect. `ThemesPanel.css` keeps two unused rules (R9).
4. `format.altText` stays parked though question 6's default reads "return": the row `formatting.alt-text.write-undo` is "added to section 5's count by B4 when Kevin answers", and rule 1 of section 1 returns a feature only once every interaction is a row; one flag on the item and one on the Alt text section leave when the row exists (R6). `format.dropShadow` stays parked by question 7's default.
5. The rows whose return is conditional on a not driven matrix row are unflagged here: `view.snapTo.grid` (`arrange.snap.grid-effect`), `file.versionHistory.showChanges` (`versions.show-changes-marks`), `toolbar.hideMenus` (`view.hide-menus-chevron`), `insert.slideNumbers` (`slides.numbers.apply`), `file.download.jpg` and `.png` (`export.jpg.current-slide`, `export.png.current-slide`, B7's signed url), the table tail's fill and border and merge controls and the Distribute rows (`tables.*` with `parks`). Section 1 rule 2 says a red row with `parks` "keeps those ids behind the `advanced` flag for that ship", rendered from the run by the merge owner; the alternative (keeping the flags until the rows pass) would leave the rows drivable only with the switch on. The merge owner re-parks from the run; the ledger test in `menu-model.test.ts` ("the return round: the flags") names every id so a re-park is one deliberate edit of the test and the model together.
6. `.ts-presence-rule` and `.ts-tb-sep` changed no value (1.2): the geometry 4.2 item 2 asks for is today's, and the presence rule's air is its grid tracks.
7. The `title.slideshow` label half carries no `aria-haspopup` though ArrowDown opens the menu: 4.1's ARIA names the chevron as the menu button and the pattern is accepted there.
8. `GROUP_TAIL` and `toolbar.pointer` are unflagged on section 6's word ("the shape, line, table, chart and group tails") and 2.16's return of the live pointers; neither is named in section 3.2's tail list.
9. The `+N` chip keeps its 32 px track and its DOM (`opacity: 0`), so the presence slot's 184 px and the roster's opener are unchanged; a `display: none` would move the own chip's rule when the plate returns.

### 3. Tests

- `packages/chrome`: `node_modules/.bin/vitest run` from `packages/chrome`: 65 files, 605 passed, 1 failed. The one failure is `src/__tests__/editor-shell.test.ts` ("the cover title before it converts (RETURN.md 2.14 item 1) ... plan block.set on the field object as on a block", expected 44 to be 52), a B2 test over B2's in progress `editor-shell.ts`; not this lane's file. Earlier in the session the same file failed on `tailKindOf` for a table by one click (the RETURN 2.4 fix 2 in progress), and for eleven minutes `editor-shell.ts` did not parse (a duplicate `const selection` at 1283 and 1319, saved 18:02:16, fixed 18:06:32), which took every jsdom test that imports `EditorShell` and the dev server's editor page down with it; nothing of this lane's changed in that window.
- `node_modules/.bin/tsc -b packages/chrome/tsconfig.json`: exit 0 before B2's save at 18:02; at 18:06 it reported three errors in `editor-shell.ts` (1283, 1319 the redeclared `selection`; 1385 `EditorSelection | null | undefined` not assignable) and none in this lane's files; the final state is in section 6.
- New and changed tests of this lane, all green: `title-row-slideshow.test.tsx` (the group and the two halves, `aria-controls` only while mounted, the chevron's open and close with focus back, ArrowDown on the label with focus back to the label, the label's click and a row's run, Enter and Space not prevented with no crop and Enter consumed with `.ts-crop-frame` present, the comments toggle with `aria-pressed`, the empty inbox slot and `+N` chip, the cluster's order, Viewing without the toolbar and Editing and Commenting with it); `menu-component.test.tsx` (the `placeMenu` end alignment case of 4.1, the start alignment clamp, the margin); `menu-model.test.ts` (the cycle 2 block rewritten as the return: Insert > Shape and Line with the named rows and the connectors, the parked kinds, the Format menu's returned rows and its parked ones, the tails and the two toolbar buttons, Search the menus off and on, the chord table; a new ledger describe naming every returned id, every parked id and the Later stubs); `toolbar-tails.test.ts`; `default-view-words.test.ts` unchanged and green (the returned rows' words were already in the model).
- `node_modules/.bin/prettier --check` over every touched file: clean.

### 4. Requests

Numbered, for the owner named; the integrator or the owner makes them.

- R1 (B2, `packages/chrome/src/editor-shell.ts` `EditorHandle`): add `cropOpen?: () => boolean` (or let `exitCrop` return whether a crop was open) so `EditorShell.tsx` `key.commit` reads the handle instead of `.ts-crop-frame` (deviation 1). One line in `EditorShell.tsx` follows: `if (current.editor?.cropOpen?.() && current.editor.exitCrop)`.
- R2 (B2): `src/__tests__/editor-shell.test.ts` is red on B2's own in progress `editor-shell.ts` (section 3); nothing for B1 to do, recorded so the merge owner reads the chrome suite's one failure as B2's.
- R3 (B3, `packages/viewer/src/Editor.tsx` 3241 to 3257): `chrome.split.tab-order`, the Tab branch returning first for a chrome control (4.1). The drive of section 5 read Tab from the Slideshow half reaching the chevron with nothing selected and after a click on the title; the click's selection was not verified by the drive (its chip read came back null, the selector was a guess), so the row with a selection is B4's to measure.
- R4 (B3, `Editor.tsx` 3118): implement R1's `cropOpen` on the stage's handle (`crop !== null`).
- R5 (B4, `scripts/probes/core-walk/areas/chrome.mjs`, `view.mjs`, `formatting.mjs`): the readers of `docs/gslides-parity/return/build/b1/shoot-chrome.mjs` are the audit's (`decodePng`, `segmentsAlong`, `runsAlongColumn`, `runsAlongRow`, `boundaryPoints`, `seamsFromShot`, `splitSeamFromShot`) with two additions the rows can use: `splitSeamFromShot` returns `dividerContrast` (the mid height divider's contrast against the ink ground; the floor of 4.4 is 1.7:1, measured 2.17 light and 1.83 dark) and `cornerFromShot` reads the wrapper's top row at its left corner. The chrome rows' criteria as this lane read them: `chrome.split.one-box` from the wrapper's computed box (32 tall, 8 px on the four outer corners), the halves (30 tall, 0 px corners) and the pixels (one 1 px run at mid height, none 1 and 2 px under the top edge); `chrome.split.chevron-menu-aligned` from `#ts-menu-slideshow`'s right edge within 1 px of `.ts-title-slideshow`'s right edge and its top at the control's bottom plus 2; `chrome.split.aria` from the attributes of 1.1; `chrome.split.enter-chevron` and `.enter-label` from a window `keydown` listener reading `defaultPrevented` after the document's (the shoot script's `window.__b1keys`); `chrome.cluster.gaps-heights` from `.ts-title-r`'s children with a width above 0 (the inbox slot is `display: none` and reads as hidden); `chrome.comments-glyph.toggle` from `[data-control="panel.comments"]` and the glyph's `aria-pressed`; `view.mode.viewing-hides-toolbar` from `.ts-toolbar` absent and `data-edit-mode="viewing"` on `.pt-viewer` with `.pt-main`'s top 40 px higher; `view.comments.show-all-panel` from the panel after `menu.view.comments.showAll` and its absence after `menu.view.comments.hide`; `formatting.theme.import-hidden` from `[data-control="themes.import"]` absent in the open Themes panel.
- R6 (B4, then B1): when Kevin answers question 6 and `formatting.alt-text.write-undo` joins the matrix, drop the flag of `format.altText` in `model.ts` and of the `altText` section in `format-sections.ts` (two lines; deviation 4).
- R7 (the merge owner): the re-parking of deviation 5 from the run, if a row with `parks` is red, is one flag per id in `model.ts` or `toolbar-tails.ts` plus its line in the ledger test of `menu-model.test.ts`.
- R8 (the integrator, `.prettierignore`): add `docs/gslides-parity/return/build/*/**/*.json` beside the `return/audit-*/**/*.json` line, so `shoot-chrome-run.json` and the lanes' evidence JSON never fail step 19; until then this lane formats its JSON with prettier after each run.
- R9 (the integrator, `packages/chrome/src/ThemesPanel.css`): drop the unused `.ts-themes-foot` and `.ts-themes-import` rules (1.5).
- R10 (the integrator, `packages/lint/src/chrome.ts`; B4 runs the lint): `turboslide lint --chrome`'s border role check reads every visible border as `--pt-hair`, `--pt-hair-soft` or `--pt-edge`, or ink near an active surface (`SHELL_CHROME.tokens`, `activeNear`). The split button now draws its one divider in `--pt-hair-on-ink` and its outline in `--pt-ink` on the wrapper, which is not itself `.is-solid`. Add `--pt-hair-on-ink` to `tokens.hair` and `.ts-title-slideshow` to `TURBOSLIDE_CHROME.active` so the lint reads the control as the one solid call to action it is; without it the lint reports two border role findings on the title row. This lane did not run the lint (B4's row, 4.2 item 5).

### 5. The drive on 4401

The lane's dev server: `TURBOSLIDE_STORE=tmp TURBOSLIDE_REALTIME=memory TURBOSLIDE_LOCAL_OPEN=1 ... node_modules/.bin/vite dev --port 4401 --strictPort -c vite.no-watch.config.ts` from `apps/studio` (the no watch config, so another lane's save cannot remount the page mid run), stopped before this lane returned. The runs held `.turboslide/e2e.lock` (taken 18:09:47 after a 35 s wait on another lane's run, released after the walk).

#### 5.1 The shoot (`build/b1/shoot-chrome.mjs`, the table in `shoot-chrome-run.json`)

Run 1 at 18:09:47 (the lock taken after 35 s), headless Chromium 1440 by 900 at human speed, one draft from `/new`, both appearances through View > Appearance with the switch off (the row is in the default view now), the pictures `build/b1/<appearance>-1x-01` to `09` (the editor, the title row, the menu bar, the toolbar, the toolbar to sheet boundary, the filmstrip edge, the notes divider, the bottom bar, the right cluster), `light-10` to `17` (the hovered label, the hovered chevron, the open menu, the focused chevron and label, the Comments panel, Viewing mode, the Themes panel) and `<appearance>-18` and `19` at 900 px. 31 rows, 29 works, 2 broken; the two are the draft's first write (below) and the 2x context that opened `/edit/null` because of it. Run 2 (the script patched to open a draft in the 2x context when no deck exists and to read `.ts-select-chip` and the active element after the double click) waited for `.turboslide/e2e.lock` from 18:17:53 behind B3's walk of shapes, lines and arrange on 4403 and was stopped before it took the lock when this lane had to return; so the 2x pictures of section 4 are not taken and the first write's mechanism is not read. Both are one run of `shoot-chrome.mjs --base <origin>` away (about four minutes), on the lane's server or on the enforce preview, and the merge owner or B4 can run it as it stands.

What the pixels and the computed styles read, the same in run 1 and run 2 unless a row says otherwise:

| Rule                                                            | Read on 4401                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 4.1 one box (`chrome.split.one-box`)                            | the wrapper 132.9 by 32, `border-radius` 8 px on the four corners, `border` 1 px ink, ground ink; the label half 102.9 by 30 with 0 px corners, `padding 0 10px 0 12px`, `flex-direction row`, `gap 6px`; the chevron half 28 by 30 with 0 px corners and `border-left 1px --pt-hair-on-ink`; the title row crop's pixels at mid height read one 1 px run at x 1313 and the same 1 px run 1, 2 and 4 px under the top edge (no notch); the corner row reads the anti aliased 8 px arc (paper, three steps, ink) and no square corner                                               |
| 4.1 the divider's contrast (4.4, at least 1.7:1)                | light `#474747` on `#070707`, 2.17:1; dark `#b5b5b4` on `#f2f2f0`, 1.83:1 (the token comment's computed composites, to the pixel)                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 4.1 hover (`chrome.split.hover-no-inversion`)                   | the hovered label half `rgba(255, 255, 255, 0.16)` on the ink wrapper, the paper text kept on both halves, the chevron half transparent; the hovered chevron half the same the other way round; nothing inverts (`light-10`, `light-11`)                                                                                                                                                                                                                                                                                                                                           |
| 4.1 focus                                                       | Tab onto the chevron half: `:focus-visible` true, `outline 1px rgb(255, 255, 255) -3px`; Tab from the comments glyph onto the label half: the same ring on the label (`light-13`, `light-14`)                                                                                                                                                                                                                                                                                                                                                                                      |
| 4.1 the menu (`chrome.split.chevron-menu-aligned`)              | after the click on the chevron: rows Presenter view and Start from beginning, the plate's right edge 1342 against the control's 1342.4, its top 40 against the control's bottom 37.5 (the 2 px gap plus the half pixel), `aria-expanded true`, `aria-controls ts-menu-slideshow`; after Escape `aria-expanded false`, no `aria-controls`, focus on `button[present.arrow]` (`light-12`)                                                                                                                                                                                            |
| 4.1 ArrowDown (`chrome.split.arrow-down-label`)                 | the menu opens with `div[menu.title.slideshow.presenterView]{menuitem}` focused; Escape puts the focus on `button[present.open]`                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 4.1 Enter (`chrome.split.enter-chevron`, `.enter-label`)        | a window listener after the document's read `defaultPrevented false` for Enter on `present.arrow` (the menu opened) and on `present.open` (the show opened, `present.show` in the document, Escape left it); with a click on the title before it the chevron's Enter read the same (the click's selection was not verified, the chip read came back null on a guessed selector; run 2 reads `.ts-select-chip`)                                                                                                                                                                     |
| 4.1 Space (`chrome.split.space-both`)                           | the menu by Space on the chevron, the show by Space on the label                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 4.1 Tab (`chrome.split.tab-order`, B3)                          | Tab from the label half landed on `button[present.arrow]` with nothing selected and after the click on the title (the same caveat on the selection)                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 4.1 the 900 px collapse (`chrome.split.collapse-900`)           | the wrapper 58.0 by 32, the label half 28.0 with `padding 0 8px` and the word `display none`, the chevron half 28, Share 32.0 by 32, in both appearances (`light-18`, `dark-18`)                                                                                                                                                                                                                                                                                                                                                                                                   |
| 4.1 ARIA (`chrome.split.aria`)                                  | the wrapper `span role=group aria-label=Slideshow`; the label `button` with the text Slideshow and no `aria-haspopup`; the chevron `button aria-label="Presentation options" aria-haspopup=menu aria-expanded=false` and no `aria-controls` while closed                                                                                                                                                                                                                                                                                                                           |
| 4.2 the seams (`chrome.separators.once`)                        | at 1440 in both appearances every scanned seam holds one 1 px run in `--pt-hair` (light `#d2d2d2` 1.51:1, dark `#3a3a3a` 1.77:1): title to menu bar (y 43), toolbar to sheet and to filmstrip (y 111), stage to notes pane (y 804: one line where the audit read two), notes pane to bottom bar and filmstrip to bottom bar (y 868), filmstrip to sheet at mid height and upper (x 255); menu bar to toolbar none, as the law says; the right panel's seam was not scanned (the Comments panel was closed when the whole editor was shot; run 2 the same, a B4 row detail)         |
| 4.2 at 900 px                                                   | title to menu bar and toolbar to sheet 1 px in both appearances; the filmstrip and the notes pane leave with their lines (the vertical scans read none); the two bottom bar scans landed on the tmp store's notice box drawn in the bottom bar on this server ("Edits are kept on this server instance only ...", a 2 px paper frame at y 862 and its hairline at 864) and are not evidence for that seam, which production has no box over                                                                                                                                        |
| 4.2 the toolbar dividers (`chrome.separators.toolbar-dividers`) | two `.ts-tb-sep`, 1 by 20 at y 81.5, `--pt-hair`, `margin 0 4px`, 8 px to the neighbour on the open side                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 4.3 the cluster (`chrome.cluster.gaps-heights`)                 | the children with a width: the presence slot 184 by 32, the comments slot 32 by 32, the split button 132.9 by 32, Share 77.6 by 32, every gap 8 px, the right inset 12 px, the inbox slot `display none` (hidden); Share `border-radius 8px` on the four corners, `border 1px --pt-hair`; the `+N` chip `opacity 0` at 32 by 24; at 900 the same gaps with the split 58 and Share 32                                                                                                                                                                                               |
| 4.3 the comments glyph (`chrome.comments-glyph.toggle`)         | the first click opened `panel.comments` with `aria-pressed true`, the second closed it with `aria-pressed false` (`light-15`)                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2.16 View > Comments (`view.comments.show-all-panel`)           | Show all comments opened the panel, Hide comments closed it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2.14 item 6 (`view.mode.viewing-hides-toolbar`)                 | Viewing: `data-edit-mode viewing`, no `.ts-toolbar`, `--pt-tool-h 0px` on the root, `.pt-main` at y 72 from 112; Editing: the toolbar back, `.pt-main` at 112 (`light-16`)                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2.13 (`formatting.theme.import-hidden`)                         | the toolbar Theme button opened Themes with four radio tiles and no `themes.import` control; the second click closed it (`light-17`)                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| sections 2 and 3, the menus and the toolbar                     | 31 returned rows present in the default view (Insert > Shape, Line, Table, Chart, Diagram, Word art, Slide numbers; Format > Borders & lines, Table, Text; Arrange > Distribute, Rotate, Group, Ungroup, Regroup; Slide > Change theme; View > Appearance, Mode, Guides, Snap to, Comments, Live pointers, Show ruler, Show filmstrip, Full screen; File > Open, Import slides, Details; Edit > Select none; Tools > Check slides; Help > Help Turboslide improve); the toolbar draws Paint format, Insert shape, Insert line, Theme and the Hide the menus chevron, no Transition |

The draft's first write (row 1, not this lane's row): the double click on the heading run of `/new`, the typed title and Escape left the page on `/new` at revision 0 and the heading at "Click to add title" in run 1; the tree carries B2's uncommitted `InlineText.tsx`, `typing-key.ts`, `controller.tsx` and `auto-title.ts` and B3's `Editor.tsx`, so this lane records the observation for B2 and B4 (`decks.name.follows-heading`, `text.title.double-click-enters`) and drew the chrome on the draft, which is the same editor page. Run 2, which reads what the double click focused, did not run (above).

#### 5.2 The walk probe

`node scripts/probes/editor-walk-probe.mjs --core --base http://localhost:4401 --only surface --json .turboslide/b1-return/walk-surface.json` under the same lock: `surface.menus.open-close-escape` ok (9 menus: file 12 rows, edit 11, view 12, insert 12, format 10, slide 8, arrange, tools, help; each closed by Escape, 0 console errors; Extensions stays undrawn with both rows parked), with the walk's own setup and cleanup rows (13 decks steps) ok: 14 steps, 14 ok, 0 failed, 0 not driven, 38 s. The probe exited 1 on the 432 rows a `--only surface` run has no step for, the matrix wide accounting of a narrowed run, not a failure of a driven row.

### 6. The matrix rows this lane covers

The chrome rows (feature `chrome`, unparkable): `chrome.split.one-box`, `chrome.split.hover-no-inversion`, `chrome.split.click-show`, `chrome.split.chevron-menu-aligned`, `chrome.split.enter-chevron`, `chrome.split.enter-label`, `chrome.split.space-both`, `chrome.split.arrow-down-label`, `chrome.split.tab-order` (with B3), `chrome.split.aria`, `chrome.split.collapse-900`, `chrome.separators.once`, `chrome.separators.toolbar-dividers`, `chrome.cluster.gaps-heights`, `chrome.comments-glyph.toggle`; the view rows `view.mode.viewing-hides-toolbar`, `view.comments.show-all-panel`; `formatting.theme.import-hidden`; `present.slideshow.from-beginning` (existing, moved to works by audit-chrome row 14); the flag changes are measured by every returning row of section 5 of RETURN.md, and the surface rows `surface.menus.open-close-escape`, `surface.advanced.off-by-default`, `surface.advanced.on-shows-parked`, `surface.parked-shortcut-unbound`, `surface.parked-blocks-render`, `surface.parked-block-core-rows` and `surface.cleanup` read the switch over the new default view. B4 owns the rows and their drivers; the criteria this lane read them by are R5.

### 7. The gates at the end of the lane's work

- `node_modules/.bin/tsc -b packages/chrome/tsconfig.json`: exit 0 (18:15, after B2's `editor-shell.ts` parsed again).
- `node_modules/.bin/vitest run` in `packages/chrome`: 65 files, 609 passed, 0 failed (18:17; the one B2 failure of section 3 had been fixed by B2 by then).
- `node_modules/.bin/prettier --check` over every file this lane touched: clean; the shoot script and the note are formatted; `shoot-chrome-run.json` is formatted by hand after each run until R8 lands.
- Not run by this lane: `scripts/check.mjs` whole (the chain is the merge owner's), `turboslide lint --chrome` (B4's row, R10 names what it will report), the core gate (B4 and the merge owner), any Playwright spec (none of this lane's rows lives in a spec), anything on a deployment (no scratch deck of this lane exists on the preview or on production; the drive ran on the tmp store of 4401 and the draft it drew died with the server).
- The dev server on 4401 was stopped before this note closed; `.turboslide/e2e.lock` was released after each run (the timestamps are in `.turboslide/b1-return/drive.log` and `drive2.log`).
