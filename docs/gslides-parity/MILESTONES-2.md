# Google Slides parity build round two

Companion to `docs/gslides-parity/SPEC-2.md` (the addendum this round builds; section numbers below refer to it unless prefixed SPEC, which is `docs/gslides-parity/SPEC.md`) in the shape of `docs/gslides-parity/MILESTONES.md`. One round, six builders with disjoint file ownership, an integrator, a verifier, a fixer round and a ship step. Revised 2026-09-12 after the review in `docs/gslides-parity/build-2/review.md` (SPEC-2 section 0 rows 0.40 to 0.68) and amended the same day for Kevin's directive (3), the canvas (SPEC-2 sections 1 and 6, rows 0.69 to 0.95), then revised after the canvas review (`docs/gslides-parity/build-2/review-canvas.md`, rows 0.96 to 0.108): B4 builds the canvas, B1 the conversion and the guides, B2 the measurer and the picture object, B3 the ruler, guides and zoom rows. This document follows those rows. Every acceptance command runs from the repository root at `/Users/kevinliu/repos/Turboslide` on a machine with Node 24, pnpm 11 through corepack, the Chrome for Testing binary `playwright-core` installs, `pdftoppm` (poppler), Python 3 with fontTools in `.turboslide/venv` for the fonts build, Docker for the verifier's container pass, and the Prototemplate checkout at `/Users/kevinliu/repos/Prototemplate` for `pnpm check` steps 7, 8 and 12. Estimates are judgments from round one's measured hours (`BUILD-STATUS.md`); "agent-hours" counts builders and the integrator together.

Rules (AGENTS.md is the contract; the round one rules stand):

- Shared checkout, disjoint ownership per the "Owns" lists below. A builder edits only the files named in their row. A change needed elsewhere is a request in the builder's own file `docs/gslides-parity/build-2/<key>.md` and the integrator makes it or reassigns it. Never edit another builder's files, never revert another builder's work, never run git write commands (read only git is fine).
- Never run `pnpm install`, `pnpm add` or `pnpm exec` (implicit install); call binaries as `node_modules/.bin/<tool>` or `node <script>`. Never run `pnpm build` or `vite build` (the integrator builds); typecheck with `node_modules/.bin/tsc -b` and run vitest per package (`cd <package> && ../../node_modules/.bin/vitest run`). A dev server is `TURBOSLIDE_STORE=tmp node_modules/.bin/vite dev --port <port>` from `apps/studio` on the port the builder's row names, always with the tmp store so no spec writes `decks/`; this is the round's written exception to AGENTS.md's one port rule, which the integrator records in AGENTS.md's dev server section on day 1 (SPEC-2 0.43); stop it before returning; never touch 4321, 3005 or another builder's port. Playwright runs against the builder's own server with `PLAYWRIGHT_BASE_URL=http://localhost:<port>` (the integrator's day 1 change to `playwright.config.ts` skips `webServer` when it is set) and never overlap on the shared machine: take `.turboslide/e2e.lock` with `mkdir` before `playwright test`, `rmdir` it after, and wait while it exists. Docker is the verifier's. `pnpm generate:contracts` is B1's and the integrator's.
- Copy rules (Kevin): plain technical English, sentence case, no em dashes, no metaphors, no trailing periods on headings, full sentences in body; labels are Google's words; agent vocabulary never appears in the default view or its tooltips.
- Mimic Google's structure, labels, positions, shortcuts and behaviours; never copy Google's icons, logos or artwork; icons are Heroicons 20 solid from the theme sprite; shape glyphs in pickers are drawn from `shapes.ts` itself.
- Every new behaviour has a unit test or an e2e spec; never weaken an existing test to pass; the Perfect export of the GT deck stays pixel identical (compare-to-shoot at 0.5 percent); the migration test keeps the GT deck and the templates unchanged; every new field is optional at schema version 1.
- Report honestly with the commands run and their results in `docs/gslides-parity/build-2/<key>.md`; durable notes go into the repo, never only into a scratchpad.

Baseline: `main` at `a65b313`. The working tree is not clean: it carries the stopped first attempt's additive edits (B1's `packages/schema/src/{blocks,catalog,deck,export,mutations,position,render,text,typography}.ts`, `blocks/table.ts`, the new `blocks/chart.ts`, `shapes.ts` and `shapes/`, `packages/fonts/assets/InterVariable-Italic.woff2`; B3's `packages/chrome/src/menus/{model,keys,strings,toolbar-tails}.ts`, `Menu.tsx`, `MenuBar.tsx`, `Dialog.css`, `editor-shell.ts`, `editor-shell-context.ts`, the new `pickers/`, `dialogs/InsertTable.tsx` deleted), nothing committed, plus this document, SPEC-2 and `build-2/review.md`, and an untracked `.github/` that stays untracked and unpushed. Each builder continues from those edits in their own files and removes what SPEC-2 0.69 retires (`SlideBase.objects`, `BlockSlot` `'objects'`, the `'objects'` catalog place, the objects rows of the menu model); `git status` and `git diff` show exactly what is there. Branch names (the integrator creates them; builders work in the shared checkout on their own files): `gs2/b1-document`, `gs2/b2-render-export`, `gs2/b3-chrome`, `gs2/b4-canvas`, `gs2/b5-charts-tables`, `gs2/b6-hosted`, `gs2/integration`.

## The order

```
Day 1                      Day 2                          Day 3, 4, 5             Day 6             Day 7             Day 8
B1 fields, canvas.ts,   ─► merge 1 ─► B1 geometry interpreter ─► merge 1b ─┐
   picture, guides,        (B1 + B3    B2 measurer, picture block,          ├──► merge 2 ──► verifier ──► fixer round ──► ship
   actions                  day 1,     render and export                    │    (integrator)  (11.1 to 11.8)  (each in own files)
B3 rows, tails, keys,       integrator B4 the canvas (sections 1 and 6),   │
   types, stubs             day 1)        text, draw tools, canvas walk     │
integrator: playwright                 B5 charts, tables                    │
   config, AGENTS.md, exports          B6 hosted and hygiene                ┘
                                       B3 panels, dialogs, pickers, rulers rows
```

- Day 1: B1 lands the schema fields of section 2 (with `valign`, `padding`, `alt`, `connect` and `level` 1 to 9), the `picture` block (SPEC-2 2.6.4), `Deck.guides` (2.10), `packages/schema/src/canvas.ts` with `toCanvas` and `fromCanvas` over a `CanvasBoxes` measurement (1.2), the `shapes.ts` table (ids, labels, categories, `prstGeom` names, guide names, the committed definitions file) with `shapePath`, `textInset` and `sites` as rectangle stubs until day 2, the 36 actions (`slide.toCanvas` and `deck.guides` included), the fixture deck and the fonts build, and removes `SlideBase.objects` and the `'objects'` slot from the working tree (0.69); B3 lands the menu model rows of section 4 with `GS2_ACTION_IDS`, the View rows flipped (Show ruler, the four Guides rows, Zoom, Snap to) and Center on page writing `to: 'sheet'`, the toolbar tails, the keys, the strings, the `editor-shell.ts` type additions (the `EditorHandle` methods of the seams list, `MenuSetting` `showRuler` and `showGuides`), the `PANEL_IDS` and `DIALOG_IDS` entries and the two typed stubs `table-tools.ts` and `chart-tools.ts` (SPEC-2 0.49); the integrator lands `playwright.config.ts` reading `PLAYWRIGHT_BASE_URL`, the AGENTS.md dev server exception, and the `./shapes`, `./diagrams` and `./canvas` exports of `packages/schema/package.json`, `./measure-dom` of `packages/render/package.json` and `./batch` of `packages/export/package.json`. The integrator merges (merge 1) so every other builder types against the new schema and the model.
- Day 2: B1 lands the ECMA geometry interpreter (`packages/schema/src/shapes/geometry.ts`, SPEC-2 0.57) and the real `shapePath`, `textInset` and `sites`; the integrator merges it as merge 1b before B2 renders shapes and B3 draws the picker glyphs. B2 lands `packages/render/src/measure-dom.ts` and `packages/headless/src/measure.ts` `measureCanvas` first (day 2, merge 1c the same evening), because B1's CLI conversion and B4's editor conversion both call them; B2, B4, B5 and B6 start against merge 1 the same day.
- Days 2 to 5: B2, B4, B5 and B6 build in parallel against merge 1 (merge 1b for shapes, merge 1c for the measurer); B3 continues on the panels, dialogs, pickers and the Zoom box; B5 fills the two tool plan files B3 stubbed; B4 lands the canvas in the order of its deliverables (the conversion and the first write first, so B1's `withCanvas` and the editor agree by day 3). The integrator wires the `on(...)` table for the 36 actions as they land, keeps `pnpm generate:contracts` current and makes the requests.
- Day 6: merge 2, `pnpm check` on the merged tree, the verifier's pass including the container verification and the canvas walk rerun.
- Day 7: the fixer round in each builder's files, the second verifier pass.
- Day 8: the ship step.

## The seams every builder types against

Fixed by SPEC-2 so the parallel builders need no negotiation; B1 and B3 land them on day 1, B2 the measurer on day 2.

- The document: `pos.rotate`, `pos.flip`, `pos.group`, the `Run` marks, `Typography` fields, `ShapeBlock` kinds and fields including `connect` (2.4.7), `TextBlock.outline`, `shadow`, `background` (a colour only, 0.74), `shot.trim`, `mask`, `adjust`, `frame`, the new `PictureBlock` (`type: 'picture'`, 2.6.4), `TableBlock.spans`, `cells` (border `weight` 0 allowed), `TableRow.height`, `ChartBlock`, `PlainBlock.marker`, `preset`, `PlainItem.level` (1 to 9), `autofit` (on heading, paragraph, text, box and shape; `grow` needs `pos`), `valign` and four sided `padding` (2.2.18, 2.2.19), `BlockBase.alt`, `Deck.guides` (2.10) (section 2); no `SlideBase.objects` and no `'objects'` slot (0.69); `toCanvas`, `fromCanvas`, `CanvasBoxes` (with `prompted: BlockId[]`, 0.97), `GrammarRecord`, `CANVAS_GROUP` (`'plate'`) from `@turboslide/schema/canvas` (1.2; B1, day 1, over a `CanvasBoxes` type B1 defines so B2's measurer fills it); `SlideBase.grammar?: GrammarRecord` as the conversion record, never `ext.grammar` (0.99); `PictureBlock.side?` (0.105); `block.flip` with `about` (0.102); `boundingBox(pos)` from `@turboslide/schema/freeform` (0.107); `SHAPE_PRESETS`, `SHAPE_PRESET_IDS`, `shapePath`, `textInset`, `sites`, `lineEndPath`, `LINE_ENDS`, `DASHES`, `BULLET_PRESETS`, `NUMBER_PRESETS` exported from `@turboslide/schema/shapes` and `@turboslide/schema/text` (the three geometry functions are rectangle stubs at merge 1 and real at merge 1b); `followConnectors` from `@turboslide/schema/connect`; `DIAGRAM_TEMPLATES` from `@turboslide/schema/diagrams` (B5, after merge 1; the DiagramPanel is its only consumer).
- The measurer (B2, day 2, merge 1c): `measureCanvasBoxes(root: Element, stage: Element): CanvasBoxes`, `awaitSheetReady(root: Element): Promise<void>` (self contained: fonts, image decodes, the dither draw, two frames; 0.97) and `CANVAS_SELECTORS` from `@turboslide/render/measure-dom` (browser safe, no `node:` import, no React); `measureCanvas(page): Promise<CanvasBoxes>` from `@turboslide/headless/measure` (1.3), over a page rendered with `prompts: true`. B4's `packages/viewer/src/canvas-measure.ts` (rendering with `prompts: true` and awaiting `awaitSheetReady`) and B1's `withCanvas(deps, slide, mutations)` in `store-actions.ts` call them and nothing else measures for a conversion; `withCanvas` reads `deps.measureCanvas(deck, slide): Promise<CanvasBoxes>`, which the CLI and the MCP server bind to headless over one sheet page per action call and the integrator binds to the render worker facade of `apps/studio/src/server/render.ts` for the hosted http transport (0.104).
- The renderer's DOM: `.free[data-free]` wrappers carrying `data-rotate`, `data-flip`, `data-group` and `aria-label` from `alt`; `img.picture-img` inside the picture object's wrapper with `data-recipe` (carrying `side`) and `data-live` when its asset is a material, and `.ts-chips` inside that wrapper (`inset: 0; z-index: 1`) when the object covers the sheet at the bottom of the stack (1.4); `.ts-sheet .slide { isolation: isolate }` so object z-indexes stay inside the slide (0.98); `.free > .material-fig` and `.free > .material` at the object's box (0.105); the `.prompt` child marking a prompted box (0.97); `data-shape` on the svg; `.shape-text[data-run]`; `<i>`, `<u>`, `<s>`, `<sup>`, `<sub>`, `<span style="color">`, `<mark>` for the marks; `.para` margins; the `.ts-measure` class the measurer sets on the sheet root to drop transforms, used in the order SPEC-2 1.5 fixes (on for measuring and element screenshots, off for the sheet screenshot); `.slide-bg` (a colour); `data-trim`, `data-mask` on the picture frame; `.chart` svg with `data-run` on the title. No `.objects` layer.
- The chrome contract (`packages/chrome/src/editor-shell.ts`, landed by B3 on day 1 so B4 and B5 type against it from merge 1): `EditorSelection` gains `group?: string`, `cells?: { r0: number; c0: number; r1: number; c1: number }`, `range?: [number, number]`, `marks?: RunMarks`, `imageEdited?: boolean`, `coversSheet?: boolean` (a picture object at the bottom of the stack covering the sheet; the `image` context menu appends the slide rows, 0.100), `positioned?: boolean` (true on a canvas slide; the Arrange predicates read `objectSelected`, which is any top level block on any kind, 4.1), `outlined?: boolean`, `canvas?: boolean` (the slide is on the freeform layout); `EditorShellInput.editor` (`EditorHandle`) gains `rotate`, `flip`, `group`, `ungroup`, `regroup`, `cropMode`, `exitCrop`, `mask`, `insertText`, `wordArt`, `tableCommand(plan)`, `chartData`, `previewBackground`, `selectNone`, `applyAutofit`, and for the canvas `toCanvas()`, `zoomTo(zoom: number | 'fit', center?: { x: number; y: number })`, `zoomStep(direction: 1 | -1)`, `addGuide(axis: 'x' | 'y', at?: number)`, `clearGuides()`, `centerOnPage(axis: 'x' | 'y')` (all optional until the integrator wires them); `EditorShellInput.guides?: { x: number[]; y: number[] }` (the deck's, for the rulers and the overlay); `MenuSetting` gains `showRuler` and `showGuides` and `STORED_SETTINGS` stores them; `TailKind` gains `chart` and `group`; `MenuEffect` `submenu.dynamic` gains `'shapes' | 'bulletPresets' | 'numberPresets' | 'tableGrid' | 'lineEnds' | 'dashes'`; `MenuClientHandler` gains `cropMode`, `wordArt`, `borderColorPicker`, `borderWeightPicker`, `selectNone` (`zoomIn` and `zoomOut` exist and now step the ladder of 0.81); `ContextTarget` gains `guide`; `PANEL_IDS` gains `'diagram'`; `DIALOG_IDS` gains `'background'`, `'customSpacing'`, `'specialCharacters'` (`'insertTable'` leaves with its dialog after merge 1); `DrawTool` becomes `{ kind: 'text' } | { kind: 'shape'; shape: ShapeKind } | { kind: 'line'; line: 'line' | 'arrow' | 'rule' | 'elbow' | 'curved' | 'curve' | 'polyline' | 'scribble' } | { kind: 'table'; columns; rows } | { kind: 'chart'; chart: ChartKind } | { kind: 'wordArt'; text: string }`. `packages/chrome/src/menus/model.ts` gains `GS2_ACTION_IDS` (the 36 ids of section 3) in `MenuActionId` the way `GS1_ACTION_IDS` did in round one, so B3's rows type check before B1's ids exist; the integrator collapses it at merge 1 when `ActionId` carries them.
- The viewer's contract (`packages/viewer/src/Editor.tsx`, B4): `EditorHandle` (the stage's) gains `toCanvas`, `zoomTo`, `zoomStep`, `rotate`, `flip`, `group`, `ungroup`, `regroup`, `cropMode`, `exitCrop`, `centerOnPage`; `EditorContextTarget` gains `'shape' | 'line' | 'group' | 'chart' | 'cellRange' | 'guide'`; `EditorOverlayView` gains `rotation` (the live angle), `sizeReadout`, `rulers` (on, the scale and the selection extent), `deckGuides` and `draggingGuide` (axis, position, the inch readout); `EditorProps` gains `guides`, `onGuides(mutation)`, `showRuler`, `showGuides`, `snapGuides`, `snapGrid`, `onZoom(zoom, center?)`, `onCanvasConvert?` (a notice hook, no write). The integrator passes them from the route; the chrome's `Overlay.tsx`, `Rulers.tsx` and `DeckGuides.tsx` (B4; the chrome's guides module is `DeckGuides.tsx` because the viewer's `Guides.tsx` is the snap lines, 0.108) draw them.
- The scene (`packages/export/src/scene/types.ts`): `SceneStyle` gains `italic`, `underline`, `baseline`, `highlight`; `SceneText`, `SceneRect`, `SceneSegment`, `SceneRaster`, `SceneTable` gain `rotate?`, `flip?`, `group?` (the user tag as `g:<tag>`), `shadow?`, `dash?`, `alt?`; `SceneText` and `SceneRect` gain `valign?` and `padding?` (four numbers); `SceneRect` gains `preset`, `adjust`; `SceneSegment` gains `kind`, `points`, `bend`, `startEnd`, `endEnd`, `connect?` (the target's object name and site for `stCxn` and `endCxn`); `SceneTable` gains `spans`, `cellStyles`, `border`; a `SceneChart` entry per chart block; `Scene.background` (a colour, or the picture object's raster when it covers the sheet at the bottom of the stack, 1.5).
- The store: `documentAtRevision(revision)` on every `DeckStore` (exists on the file store; the Blob store reads the version record's `snapshot` key, SPEC-2 0.40); `export.run` with `batch` and `merge` (section 8.1); the batch module `@turboslide/export/batch` (`planBatches`, `assetHashes`, `isStale`, `mergeParts`, the `ExportPlan` type; B6's files inside B2's package, SPEC-2 0.48).
- The lint entry: `@turboslide/lint/run-client` (section 8.3).
- The tool plans (B3 lands the two files as typed stubs on day 1; B5 owns and fills them from merge 1; the signatures do not move): `packages/chrome/src/table-tools.ts` exports `type TableCommandId = 'insertRowsAbove' | 'insertRowsBelow' | 'insertColumnsLeft' | 'insertColumnsRight' | 'deleteRows' | 'deleteColumns' | 'distributeRows' | 'distributeColumns' | 'merge' | 'unmerge' | 'cellFill' | 'cellBorder' | 'tableBorder'`, `type TablePlan = { action: MenuActionId; input: Record<string, unknown> } | { refused: string }` and `tablePlan(block: TableBlock, selection: { cell?: [number, number]; cells?: EditorSelection['cells'] }, command: TableCommandId, options?: { count?: number; fill?: Color | null; border?: CellBorder | null; total?: number }): TablePlan`; `packages/chrome/src/chart-tools.ts` exports `chartPlaceholder(kind: ChartKind): Pick<ChartBlock, 'categories' | 'series'>`, `type ChartGridEdit = { kind: 'cell'; row: number; column: number; value: number } | { kind: 'addSeries' } | { kind: 'addCategory' } | { kind: 'removeSeries'; index: number } | { kind: 'removeCategory'; index: number } | { kind: 'renameSeries'; index: number; name: string } | { kind: 'renameCategory'; index: number; name: string } | { kind: 'seriesColor'; index: number; color: Color }`, `chartDataPlan(block: ChartBlock, edit: ChartGridEdit): { action: 'chart.setData'; input: Record<string, unknown> }`, `chartKindPlan(block: ChartBlock, kind: ChartKind): { action: 'chart.setKind'; input: Record<string, unknown>; dropped: string[] }` and `chartOptionPlan(block: ChartBlock, field: 'legend' | 'numberFormat' | 'labels' | 'title', value: unknown): { action: 'block.set'; input: Record<string, unknown> }`. The stubs throw `new Error('table-tools lands at merge 1')` so a call before B5 fills them fails loudly.

## B1 Document, actions, fonts build, CLI, MCP, lint rules, fixture deck

Estimate: 72 agent-hours (the ECMA geometry interpreter is about 12 of them, `connect` and `followConnectors` about 4, the canvas conversion, the picture block, the guides and the measured store actions about 10). The fields, the table, the conversion and the actions land on day 1 because every other builder reads them; the interpreter lands on day 2 (merge 1b).

### Inputs

Sections 0 (with 0.40 to 0.95), 1 (1.2, 1.4 the validator and the catalog, 1.6 the first write), 2 (every field, with 2.2.18, 2.2.19, 2.4.7, 2.6.4, 2.10), 3 (every action), 7.1, 7.2, 11.2, 11.4, 11.5 (schema, lint, fonts, cli, agent rows), 11.8 item 4; SPEC 7.1 to 7.5; R05 A5, A10, C1 to C7, E1 to E3; R09 A5; R11 A6; ECMA-376 Part 1 `presetShapeDefinitions.xml` and its formula language.

### Owns

`packages/schema/src/{deck,blocks,text,position,typography,color,catalog,reduce,mutations,validate,actions,rules,export,apply-layout,layouts,freeform,annotate,ids,render,fixtures}.ts`, new `packages/schema/src/canvas.ts` and `canvas.test.ts`, `packages/schema/src/rules.json`, `packages/schema/src/blocks/{table,composite,material}.ts`, new `packages/schema/src/blocks/chart.ts`, new `packages/schema/src/shapes.ts`, new `packages/schema/src/shapes/**` (`presetShapeDefinitions.xml` as committed from the standard, `geometry.ts` the interpreter, `geometry.test.ts`), new `packages/schema/src/connect.ts`, `packages/schema/src/**/*.test.ts` except `diagrams.test.ts`, new `packages/schema/src/__fixtures__/gslides-r1/**` (the fixture deck as committed at `a65b313`, for the migration test); `packages/lint/src/{context,contracts,lint-static,run,text,chrome}.ts`, `packages/lint/src/static/**`, `packages/lint/src/rendered/**`, `packages/lint/src/fixtures/deck.ts`, `packages/lint/src/lint.test.ts`, `packages/lint/fixtures/index.json` (generated); `packages/fonts/**` (`assets/InterVariable-Italic.woff2` new, `export/*.ttf` and `fonts.json` rebuilt, `src/*`), `scripts/build-fonts.py`, `scripts/requirements.txt`, `THIRD_PARTY_NOTICES.md`; `apps/cli/src/commands/{block,slide,slides,text,fonts,export,deck,info,validate,lint}.ts`, new `apps/cli/src/commands/{table,chart,shape,line,diagram}.ts`, `apps/cli/src/commands/*.test.ts` (new `canvas.test.ts` included), new `apps/cli/src/commands/__fixtures__/canvas-walk/**`, `apps/cli/src/{cli,store-actions,deck-files,args,write}.ts`; `packages/mcp/src/**`; `packages/agent/src/generate/**`, `packages/agent/src/__tests__/{coverage,fixtures,skills}.test.ts`, `packages/agent/src/http/manifest.ts`, `packages/agent/generated/**` (regenerated); `decks/fixture/gslides/**` (the 20 new slides of SPEC-2 11.2, `deck.json`'s `guides` and `assets/`); `docs/grammar.md` (regenerated).

### Delivers

1. Day 1: every field of section 2 in the schema with its validator rule, reducer path and catalog entry (`NATIVE_BLOCK_TYPES` gains `chart`; `allowedIn` loses `'objects'` and `credit` becomes `['plate', 'content']`; `LINK_REFUSED_BLOCK_TYPES` unchanged), including `valign`, four sided `padding`, `BlockBase.alt`, `ShapeBlock.connect`, `PlainItem.level` 1 to 9, border `weight` 0, `autofit` on heading, paragraph, text, box and shape with `grow` refused without `pos` (SPEC-2 0.41), the `picture` block (2.6.4, `PictureBlock` in `blocks.ts` with optional `side`, its catalog entry, `blockAssetRefs` reading `/asset`), `SlideBase.background` and `Deck.defaults.background` as a colour only (0.74), `Deck.guides` (2.10) with its validator rule; the removal of `SlideBase.objects`, `slideObjects`, `BlockPlace` `'objects'`, `BLOCK_SLOTS` `'objects'` and the objects branch of `slideBlocks` from the working tree (0.69); the content layouts' `make` writing `autofit: 'shrink'` on the heading, paragraph, text and box blocks of a new content slide; `packages/schema/src/canvas.ts`: `toCanvas`, `fromCanvas`, `CanvasBoxes`, `GrammarRecord` exactly as 1.2 states for every kind (the picture object at `0, 0, 1600, 900`, the plate box with its padding and the `plate` group, the closing's mark, the title's `heading` and `lead` ids, the statement's centred `big`, `autofit: 'shrink'`, `template` from `derivedLayout`, `grammar` with `kind` and `fields` as the first class field `SlideBase.grammar?: GrammarRecord` with its schema, outside `extPointers` (0.99), one line boxes for the ids `CanvasBoxes.prompted` names (0.97), `side` on the picture object (0.105)), with `Freeform.tsx`'s `toFreeform` and `toGrammar` reduced to calls (a request to B4); `boundingBox`, `offSheet` and `boxesOverlap` in `freeform.ts` over the rotated box (0.107); `applyLayout` reading a canvas source (the picture object as the picture, the plate box dropped, SPEC 5.5's two new rows); `shapes.ts` with the 135 preset rows (id, label, category, `prstGeom`, guide names) over the committed definitions file, `shapePath`, `textInset` and `sites` as rectangle stubs, the ten line ends and the six dashes; `connect.ts` with `followConnectors`; the mark span rule in `text.ts` with `Run` extended; `blocks/chart.ts`; the 36 actions of section 3 in `actions.ts` with inputs, outputs, CLI usages, MCP names and parsing examples (`slide.toCanvas`, `deck.guides`, `block.autofit` with `apply`, `line.set` with `connect`, `block.setAlt` writing the block or the asset, `text.list` levels to 9, `view.zoom` with `center` and the scroll in its output, `block.align` defaulting `to` to `sheet` for one block, `block.flip` with `about` (0.102), `view.zoom` clamped to 1600 (0.101), `slide.setBackground` and `deck.setBackground` as a colour only); `Milestone` `GS2`; `pnpm generate:contracts` run and its outputs committed at the ship step.
2. Day 1: the fixture deck's 20 new slides (11.2's total is 27: `canvas-title` (with the autofit text boxes and the padded shape) and `canvas-opener`, with the `pos` the editor's walk records, or the CLI's until it does; the two background slides; the attached connector on `lines`; the `shrink` heading block on `autofit`; both list forms on `bullets`; the `chart/size` plant on `chart-bar`), `deck.json`'s `guides: { x: [800], y: [450] }`, and the two-tone fixture asset; the fixture as committed at `a65b313` copied under `__fixtures__/gslides-r1/`.
3. Day 1: the Inter italic build (7.1): the source file, `plan()` with the italic twins, `BUILD_VERSION` 2, the licence check over both files, `fonts.json` with 34 faces, `exportFace` with `italic`, `inter.css` and `inter.ts` with the italic descriptor; `turboslide fonts build --check` exit 0.
4. Lint: `text/overflow` (2, rendered, its finding carrying the fix `fix.run` applies), `chart/size` (2, static, the legibility rule of SPEC-2 0.60), `color/semantic-icons-only` over runs (1), `layout/freeform` with the sentence "This slide is arranged by hand; Apply layout re-flows it" (1.4, 0.76), `freeform/off-sheet` at its table severity 3 for a box wholly outside the sheet and at 2 through the severity override for a box crossing an edge, with the two sentences of section 10 (0.96), `freeform/overlap` with its plain word message, both over the rotated bounding box (0.107), the picture object and a textless box skipped as the lower block, the copy and type rules over shape text, `asset/twin-or-border` reading `frame` and the picture object; no rule fires for a conversion or an object count (0.52); the fixture slides planted for every new rule; the GT deck's count unchanged.
5. The CLI handlers for every new action in `store-actions.ts` (one implementation for the file, tmp and Blob stores) and their commands: `slide to-canvas <slideIds>` and `withCanvas(deps, slide, mutations)`, which precedes every canvas write on a slide that is not on the freeform layout (`block.set` with a `/pos` path, `block.move` with `z`, `block.insert` with a positioned block, `block.align`, `block.distribute`, `block.order`, `block.rotate`, `block.flip`, `block.group`, `block.duplicate`, `block.crop`, `diagram.insert`) with the conversion's `slide.replace` in the same write, measuring through `deps.measureCanvas` (bound to `@turboslide/headless` `measureCanvas` on a 1x sheet page in the CLI and the MCP server, one page per action call; the studio binds the render worker facade; 1.3, 1.6, 0.73, 0.104), the measured cost of one canvas write per transport recorded in `build-2/b1.md`; `slide set-layout --type freeform` on a grammar content slide running the same conversion; `deck guides --add-vertical`, `--add-horizontal`, `--remove-vertical`, `--remove-horizontal`, `--set`, `--clear`; `block insert --slot main --pos`; `block autofit --apply` running `measureSlide` of `@turboslide/headless` on the slide and writing the fit in the same write (0.64); `line set --connect-start`, `--connect-end`, `--detach`; `block set /valign`, `/padding`, `/alt`; `followConnectors` called from the handlers of `block.set /pos`, `block.rotate`, `block.flip`, `block.align`, `block.distribute` and `slide.update` so a moved shape carries its connectors on every transport; the `--json` output of every read carrying the new fields (`slide.get` with `pos` on every object after a conversion, `deck info` with `canvas`, `charts` and `guides`, `slide list` with `canvas` and `objects`); `export txt` with object texts in reading order, chart data and bullet glyphs.
6. MCP: every new action as a tool (`deck_*` names of section 3, `deck_slide_to_canvas` and `deck_set_guides` included), `DESTRUCTIVE_ACTIONS` gaining `block.resetImage`, `table.deleteRows`, `table.deleteColumns`, `table.merge`, `text.case`, `chart.setKind`; the stdio server serving them.
7. Day 2: `packages/schema/src/shapes/geometry.ts`, the interpreter for the definitions file's `avLst`, `gdLst`, `ahLst`, `cxnLst`, `rect` and `pathLst` (the operators and built in variables SPEC-2 2.3 lists), replacing the day 1 stubs so `shapePath`, `textInset` and `sites` are exact for all 135 presets; `geometry.test.ts` checks the operators against worked values from the standard and `shapes.test.ts` evaluates every preset at three sizes; `THIRD_PARTY_NOTICES.md` records the file, its sha256 and the standard's terms. The integrator merges it as merge 1b.
8. `canvas.test.ts` (schema) and `apps/cli/src/commands/canvas.test.ts` as 11.5 states them: the conversion of every kind over recorded boxes, `fromCanvas` restoring the kind byte for byte, `applyLayout` re-flowing a converted slide into every layout, the CLI's `slide to-canvas` equal to the walk's recording (11.8 item 4), `block set /pos` on a grammar slide converting in one revision, `slide to-canvas` with two ids opening one browser page (0.104), the fresh Title slide converting with one line boxes (0.97), a converted slide validating with no issue (0.99), a reroute in `connect.test.ts` (0.103), `deck guides`.

### Acceptance

```
cd packages/schema && ../../node_modules/.bin/vitest run
cd packages/lint && ../../node_modules/.bin/vitest run
cd packages/fonts && ../../node_modules/.bin/vitest run
cd apps/cli && ../../node_modules/.bin/vitest run
cd packages/mcp && ../../node_modules/.bin/vitest run
cd packages/agent && ../../node_modules/.bin/vitest run          # coverage.test.ts names the 36 actions
pnpm generate:contracts && git diff --exit-code -- packages/agent/generated skills/*/references docs/grammar.md packages/schema/src/rules.json packages/lint/fixtures/index.json ':(literal)apps/studio/src/routes/openapi[.]json.ts'
node apps/cli/bin/turboslide.mjs validate decks/gt-brand && node apps/cli/bin/turboslide.mjs validate decks/fixture/gslides
node apps/cli/bin/turboslide.mjs lint all --json > .turboslide/lint.json          # the GT count is not higher than at a65b313
node apps/cli/bin/turboslide.mjs fonts build --check
node scripts/compare-to-shoot.mjs --deck decks/gt-brand --render .turboslide/render --shoot /Users/kevinliu/repos/Prototemplate/deck --max-mismatch 0.005
```

Plus, in a temp deck copied from `decks/fixture/gslides`: `slide to-canvas <a title slide>` (the output's `pos` equals the committed `canvas-title` slide's), `block insert <slide> --slot main --pos 200,200,480,64 < text.json`, `block set <a grammar slide>#<block> /pos '{"x":137,"y":129,"w":600,"h":80}'` (one revision, the slide converted), `block rotate --by 90`, `block flip --axis h`, `block group`, `block ungroup`, `block regroup`, `block order <slide>#picture --move back`, `deck guides --add-vertical 800` then `--clear`, `text style --range 0:4 --italic --color red`, `text list --marker bullet --level 4`, `text spacing --line 1.5`, `text columns 2`, `text indent --in`, `text case --range 0:5 upper`, `text insert --at 2 "→"`, `chart set-data`, `chart set-kind pie`, `table merge`, `table unmerge`, `table insert-rows`, `table delete-columns`, `table distribute rows`, `table cell-style --border-weight 0`, `shape set --kind hexagon --dash dot`, `line set --end fillArrow`, `line set --connect-end <box>:2` followed by `block set <box> /pos` and a check that the connector's end moved, `block crop`, `block mask ellipse`, `block adjust`, `block reset-image`, `block alt` on a shape, `block shadow --on`, `block autofit shrink --apply` on a text box whose text overflows (the size steps down in the same revision), `block set /valign middle`, `block set /padding '{"top":8,"right":16,"bottom":8,"left":16}'`, `slide background --color plate`, `deck background --color plate`, `diagram insert --kind process --count 4` each exit 0 and the deck validates after each; `migrations.test.ts` asserts the GT deck, both templates and `__fixtures__/gslides-r1` unchanged by `migrate`.

## B2 Renderer and export

Estimate: 70 agent-hours. The measurer lands on day 2 (merge 1c), because B1's CLI conversion and B4's editor conversion call it; the rest after merge 1, shapes after merge 1b.

### Inputs

Sections 1.3 to 1.5, 2 (the Renderer, Perfect and Editable text columns; 2.6.4), 7.1 (the export faces), 7.2 (`renderRuns` and the measurer), 11.3, 11.4 (the fidelity step), 11.5 (render, export and headless rows); SPEC 7.3, 7.6; `docs/pptx.md`; `docs/export-verification.md`; the pptxgenjs 4.0.1 declarations named in section 13.

### Owns

`packages/render/src/**` (`slide.ts`, `block-css.ts`, `text.ts`, `print.ts`, `standalone.ts`, `runtime.ts`, `deck.ts`, `thumb.ts`, `geometry.ts`, `blocks/*` including new `blocks/chart.ts`, `blocks/shape-text.ts` and `blocks/picture.ts`, new `measure-dom.ts`, `dia/*`, `__tests__/**`), `packages/theme/src/gt-ink-paper/{sheet,stage}.css` (the marks, `.para` spacing, `.slide-bg`, the picture frame), `packages/export/src/**` except `packages/export/src/batch/**` (B6) (`scene/*` including the measurement order of 1.5 in `extract.ts` and `extract.test.ts`, `pptx/*` including new `pptx/chart.ts`, `pptx/shapes.ts`, `pptx/pictures.ts` for trim, mask and adjust through sharp and the picture object as `slide.background`, `ooxml/*` including the nesting of `groups.ts`, `avLst` and `numCol` writers, `verify/*` with the chart region rule, `pdf/*`, `report.ts`, `check.ts` with `italicRuns`, `rotated`, `groups`, `charts`, `custGeom`, `numCol`), `packages/headless/src/**` (new `canvas-measure.test.ts`; `measure.ts` gains `measureCanvas`), new `scripts/canvas-fidelity.mjs`, `apps/render-worker/**`, `docker/render-worker.Dockerfile` (the 34 faces), `docs/pptx.md`, `docs/export-verification.md`.

### Delivers

1. Day 2 (merge 1c): `packages/render/src/measure-dom.ts` with `measureCanvasBoxes(root, stage): CanvasBoxes` and `CANVAS_SELECTORS` (1.3: `[data-block]` through the `.free[data-free]` wrapper, `img.opener-img, img.mood-img` as `picture`, `[data-slot="plate"]` as `plate`, the title's `svg[data-raster="mark"]` and the closing's `svg.mark` as `mark`; integers in sheet px relative to the stage; `prompted` for the boxes that came from a prompt) and `awaitSheetReady(root)` (fonts, every image decoded, the dither draw moved here from `ready.ts`, two frames; `ready.ts` `waitForReady` evaluates it, 0.97), browser safe; `packages/headless/src/measure.ts` `measureCanvas(page)` evaluating it on a 1x sheet page rendered with `prompts: true`; `packages/headless/src/canvas-measure.test.ts` asserting the fixture's `canvas-title` and `canvas-opener` measure to their committed `pos`, that the fresh Title slide measures at the prompts' boxes with no zero height, that a figure slide measures after its twins decode, and that every selector resolves on every kind.
2. Renderer: no `.objects` layer (0.69); the `picture` block (1.4, 2.6.4: `img.picture-img` at object-fit cover with the twins, `object-position`, `alt`, `data-recipe` and `data-live` for a material asset, `clip-path` and `filter` for mask and adjust, the `.ts-chips` inside its wrapper at `inset: 0; z-index: 1` when it covers the sheet at the bottom of the stack, the recipe with the block's `side` and `pictureRecipeAttr`'s `plate` parameter optional, 0.105); `.ts-sheet .slide { isolation: isolate }` in stage.css so object z-indexes stay inside the slide and the frame, wordmark and counter paint over every object (0.98; step 12 proves no grammar pixel moves); `.free > .material-fig` and `.free > .material` at the object's box with the caption inside it (0.105); the deck default background layer not drawn on an unconverted picture kind (0.108); the plate scoped declarations of `block-css.ts` repeated on the block's own classes under `.free` (the `.big`, `p`, `.title`, `.credit` and `.mark` rules of lines 19 to 26) so a converted plate renders as the plate did; rotation and flip transforms with the data attributes; the marks (`renderRuns` in the order of 7.2); `.para` spacing, columns, indent, justify; bullet glyphs and numerals per level and preset with nine levels cycling, and the round one `numbered: true` rendering untouched (SPEC-2 0.59); shape presets from `shapePath` with the text layer and the adjust guides; the line kinds, decorations and dashes; word art outline; drop shadow; the slide background colour layer, drawn only when set; trim, mask, adjust and frame on pictures; the chart svg in the dia grammar; merged cells, cell styles, row heights, table border colour and dash, no rule at border weight 0; `valign` and four sided `padding` on box, shape and text (2.2.18, 2.2.19); `aria-label` from `alt` on the `.free` wrapper; every new case in the thumbnails, the print document and the standalone build; the `.ts-measure` class contract of 1.5. An unconverted slide renders byte for byte as at `a65b313` (the snapshot tests and compare-to-shoot prove it).
3. `scripts/canvas-fidelity.mjs` (check step 24, SPEC-2 0.95): every slide of the named decks rendered, converted through `toCanvas` over `measureCanvas`, rendered again and compared with the comparator of `compare-to-shoot.mjs` in both themes; diff PNGs under `.turboslide/canvas-fidelity/`; exit 1 naming the first slide over budget. B2 runs it on every render change and fixes the CSS repetition until every slide of the GT deck and the templates is under 0.5 percent.
4. Perfect: by construction, plus the invisible runs of rotated blocks with `rotate`, the runs inside shape text, the chart title run; `perfect: true` on the fixture, the converted `canvas-title` and `canvas-opener` included.
5. Editable text: the measurer reading the new styles, transforms, groups, presets, lines, tables, charts and the background in the order of 1.5 (`.ts-measure` on, measure and element screenshots of rotated blocks, off, the flatten sheet screenshot), with `extract.test.ts` covering the rotated icon case; the picture object as a raster of kind `shot`, written as `slide.background = { data }` when it covers the sheet at the bottom of the stack so a converted Section header exports as an unconverted one; the plate box as a `SceneRect` and the `plate` group as one `p:grpSp`; `pptx/build.ts` writing `rotate`, `flipH`, `flipV`, `shadow`, `dashType`, the arrow heads with the substitution residual, `addShape(preset)` with the ECMA name and `addText` with `shape`, the `prst()` cast helper for `bentConnector3`, `curvedConnector3` and `custGeom` points, `outline`, `italic`, `underline`, `strike`, `superscript`, `subscript`, `highlight`, `bullet` with `characterCode` and `numberType` (the `zerodigit` residual), `indentLevel` to 8, `paraSpaceBefore`, `paraSpaceAfter`, `align: 'justify'`, `valign`, a four number `margin`, `rowspan`, `colspan`, per cell fill and border with `type: 'none'` at weight 0, `altText` from a block's `alt` on shapes, charts, connectors and text boxes, `addChart` per kind, the picture trimmed and masked through sharp and adjusted with `ImageProps.transparency`, the picture frame as a grouped rectangle, `slide.background` for a colour; the post-process writing `avLst`, `numCol`, `p:cxnSp` with `stCxn` and `endCxn` for attached connectors, and nested groups with `groups.ts` matching `p:sp`, `p:pic`, `p:graphicFrame` and `p:cxnSp`, the outer group first (0.67); the verify loop treating chart boxes as picture regions; `export check` with the new counts (`italicRuns`, `rotated`, `groups`, `charts`, `custGeom`, `numCol`, `connectors`).
6. PDF: one page per fixture slide (11.2's total) under the gate; TXT unchanged (B1 owns `export.text`).
7. The render worker image with the 34 faces (the Dockerfile; the verifier builds it).
8. Snapshot tests for every new render case in both themes, a converted slide of every kind beside its original included, the converted opener asserting the wordmark and counter boxes uncovered and a material object at a box off 16:9 (0.98, 0.105); the export tests of 11.5.
9. `packages/headless/src/ready.ts` waiting on the italic descriptor; the standalone build size under `turboslide build --budget 16` recorded in `docs/gslides-parity/build-2/b2.md` (SPEC-2 0.66).

### Acceptance

```
cd packages/render && ../../node_modules/.bin/vitest run
cd packages/export && ../../node_modules/.bin/vitest run
cd packages/headless && ../../node_modules/.bin/vitest run
cd apps/render-worker && ../../node_modules/.bin/vitest run
node apps/cli/bin/turboslide.mjs render all --theme light,dark --scale 1 --out .turboslide/render --json
node scripts/compare-to-shoot.mjs --deck decks/gt-brand --render .turboslide/render --shoot /Users/kevinliu/repos/Prototemplate/deck --max-mismatch 0.005
node scripts/canvas-fidelity.mjs --deck decks/gt-brand --deck decks/templates/gt-brand --deck decks/templates/blank --max-mismatch 0.005
node apps/cli/bin/turboslide.mjs export decks/fixture/gslides --mode flatten --out .turboslide/gs-flatten && node apps/cli/bin/turboslide.mjs export check .turboslide/gs-flatten
node apps/cli/bin/turboslide.mjs export decks/fixture/gslides --mode native --out .turboslide/gs-native && node apps/cli/bin/turboslide.mjs export check .turboslide/gs-native
node apps/cli/bin/turboslide.mjs export decks/fixture/gslides --format pdf --verify --out .turboslide/gs-pdf
```

The flatten report is `perfect: true`; the native check reports one `a:tbl` with a merged cell, three chart parts, the group counts including one nested row group inside a user group and the `plate` group of `canvas-opener`, the rotated shapes, 17 italic runs or more, one `numCol`, one `avLst`, one connector with `stCxn` and `endCxn`, and `slide.background` on `canvas-opener`; the PDF report has one page per fixture slide (11.2's total) under the gate; the GT deck's render is unchanged against the shoot; the fidelity script exits 0.

## B3 Chrome shell

Estimate: 62 agent-hours. The menu model rows (the View rows of the canvas included), the tails, the keys, the strings, the `editor-shell.ts` types and the two tool plan stubs land on day 1; the panels, dialogs, pickers, the Zoom box and the filmstrip handle after merge 1.

### Inputs

Sections 0, 4 (the View, Center on page and Arrange rows of 4.1, the `guide` and `emptyCanvas` targets of 4.3), 5, 6.1 rows 23, 25, 27, 29 to 31 (the rows whose surface is a menu row, the Zoom box or a setting), 6.2 (the picker and dialog surfaces), 8.6, 9, 10, 12; SPEC 2, 3, 12, 13; R01 Insert, Format, Arrange, View; R02 sections 4.2 to 4.6, 6 (the zoom facts); R05 A5, A10, A11, B1 to B8, B16, C1 to C7; R08 A5, A6, A9, A10, A11, A14, A20.

### Owns

`packages/chrome/src/menus/{model,toolbar-tails,keys,strings,finder}.ts`, `packages/chrome/src/menus/__fixtures__/**`, `packages/chrome/src/menus/__tests__/**`; `packages/chrome/src/{EditorShell,FormatOptions,Inspector,InspectorControl,ToolbarHead,ToolbarTail,Toolbar,ToolButton,Palette,ToolFinder,ShortcutsDialog,Menu,MenuBar,Panel,ThemesPanel,LayoutGrid,Snackbar,TitleRow,BottomBar,Tooltip,Seg,Dialog,ViewerShell,StatusChip,DeckName,ThemeButton,HelpCard,ExportMenu,ExportReportCard,InsertMenu,Toast,ConnectCard,IconPicker,AssetPicker,LintPanel,VersionsPanel,HistoryPanel,SourceDrawer,TwinStage,PicturesPanel,Progress,GtMark,Sidebar,ListRow,Thumb,ThumbShot,SidebarFilter,PreviewLayer}.tsx` and their CSS (the filmstrip files move to B3 from the first draft's B4, whose scope is the canvas), `packages/chrome/src/{editor-shell,editor-shell-context,useEditorKeys,useShellKeys,palette-data,dispatch,shell-data,shell-context,icons}.ts*`, `packages/chrome/src/{table-tools,chart-tools}.ts` on day 1 only (the typed stubs; B5 owns both from merge 1), new `packages/chrome/src/__tests__/use-editor-keys.test.tsx`, `packages/chrome/src/inspector/**` except `chart.tsx`, `chart.css`, `table.tsx`, `table.css` (B5), `packages/chrome/src/dialogs/**` (the round one dialogs; new `Background.tsx`, `CustomSpacing.tsx`, `SpecialCharacters.tsx`, `special-characters-data.ts`, `WordArtBar.tsx`; `InsertTable.tsx` deleted with its test), new `packages/chrome/src/pickers/{ShapePicker,PresetPicker,LineEndPicker,DashList,ColorPlate,WeightList}.tsx` and CSS (B5 owns `pickers/TableGrid.tsx` and `pickers/DiagramPicker.tsx`), `packages/chrome/src/tokens.css`, `packages/chrome/src/__tests__/**` except the files B4 and B5 own (`packages/chrome/src/__tests__/{thumb-component,sidebar-filmstrip}.test.tsx` are B3's), `packages/theme/assets/{sprite.svg,sprite-ids.json}`, `packages/theme/src/sprite.ts`, `packages/theme/scripts/**`; `apps/studio/e2e/{editor,undo,filmstrip}.spec.ts` (the filmstrip and shell specs; `filmstrip.spec.ts` extended with the Change background row).

### Delivers

1. Day 1: every row of section 4.1 in `model.ts` with its effect, key, predicate and Google label (`google` field where Google's spelling differs), including the canvas rows: `view.showRuler` Now with `altLabel` Hide ruler, `view.guides.show`, `view.guides.addVertical`, `view.guides.addHorizontal`, `view.guides.clear` Now (`view.guides.edit` Later with its clause), `view.guides.delete` context only, `view.snapTo.*` and `view.zoom.*` as they stand with `zoomIn` and `zoomOut` stepping the ladder of 0.81, `arrange.centerOnPage.*` writing `to: 'sheet'` and enabled by `objectSelected` (0.80), the Align, Distribute, Order, Rotate, Group, Ungroup and Regroup rows enabled on every slide kind through `objectSelected`, `twoOrMore`, `threeOrMore` and the order predicates (`twoOrMoreFreeform`, `threeOrMoreFreeform`, `freeformBlockSelected` and `GRAMMAR_LINES_UP` removed), `edit.selectNone` Now (SPEC-2 0.61), `hasBorderField` on the two border rows (0.62), the new predicates and `MenuContext` fields (`guides`, the `showRuler` and `showGuides` settings), `GS2_ACTION_IDS` mirroring section 3 (36 ids), the dynamic submenu kinds, the context menu targets of 4.3 (`shape`, `line`, `group`, `chart`, `cellRange`, `guide`; `emptyCanvas` with Guides ▸ after Comment) and their rows; the `editor-shell.ts` type additions of the seams list (the canvas methods of `EditorHandle`, `EditorShellInput.guides`, `MenuSetting` `showRuler` and `showGuides` in `STORED_SETTINGS` and `DEFAULT_SETTINGS`, `ContextTarget` `guide`), `PANEL_IDS` `'diagram'` and the three new `DIALOG_IDS`; `table-tools.ts` and `chart-tools.ts` as typed stubs with the seams' signatures; the tails of 4.2 in `toolbar-tails.ts` (`chart` and `group` tail kinds, the group tail's four controls applied to every member, 0.102; the new ops; Border color and Border weight enabled on word art); the `image` context menu's appended slide rows under `selection.coversSheet` and `emptyCanvas` opening from the workspace (0.100); the chords of section 9 in `keys.ts` with the conflicts resolved as written (Google's rotate keys with the Cmd+Option alias note, Cmd+scroll, Space+drag marked `turboslide: true`, Shift+arrows 10 px) and the shortcuts dialog's greyed rows reduced to section 9's list; the strings of section 10 in `strings.ts` with the canvas strings ("480 × 64", "6.67 in", "3 objects", "This slide is arranged by hand; Apply layout re-flows it", Show ruler, Hide ruler, Delete guide) and `round`, `convert`, `conversion`, `freeform`, `measure` in `FORBIDDEN_DEFAULT_VIEW_WORDS`, every section 12 clause free of process words and every round one clause the extended test catches rewritten (0.53); the tests extended (`menu-model`, `shortcuts`, `toolbar-tails`, `default-view-words` over the Later clauses, the lint rules' labels and the fixture decks' finding messages; the View rows Now; Center on page's `to: 'sheet'`; the Arrange rows enabled on a grammar slide with an object selected).
2. Format options with the sections and controls of section 5 (`format-sections.ts` routes; the Size & rotation dial and flip buttons and the Position fields on every object of every slide kind and on a group over its union (a Width or Height edit scaling the members, the flip toggling them, and Colour, Text and Drop shadow over every member, 0.102), the measured box shown on a grammar slide and the first commit converting through the route; the Text section's marks buttons and swatches; Text fitting on heading, paragraph, text, box and shape with the third radio disabled without `pos` and its note, four sided Padding, Vertical alignment; Drop shadow; Adjustments; Picture with the crop, mask and reset buttons for a `shot` and a `picture`; Line with Detach for an attached end; Shape; the List marker, preset and level controls to 9; Alt text on every block writing `block.setAlt`); `Custom spacing` and `Background` dialogs (Background: Color writes the fill, Choose inserts the picture object through the editor's handle, Reset to theme, Add to theme, 0.74); the `Insert special characters` dialog with the data table; the word art bar mounted over the canvas; the shape picker (the four category grids from `shapePath(48, 36)`, real glyphs after merge 1b), the bullet and numbered preset grids, the line end picker, the dash list, the colour plate and weight list anchored to a menu row (decision 0.27), the plate and list writing `outline.color` and `outline.width` on word art.
3. The Zoom box (`ToolbarHead.tsx` `ZoomBox`, 6.1 row 27): Fit and the presets, a typed value clamped to 25 to 1600 (0.101), the arrow's list; `zoomIn` and `zoomOut` client handlers stepping the ladder 25, 50, 75, 100, 125, 150, 200, 300, 400, 800, 1600 from the effective zoom (the Fit factor rounded to the nearest step) through `editor.zoomStep`, Cmd+0 through `view.zoom.100`; the box reads the effective percent while a pinch zooms; the toolbar's Snap to and ruler state reflected in the View menu checks.
4. `editor-shell.ts` `menuActionPlan` for every flipped row (the plans call B5's `table-tools.ts` for the table rows and `chart-tools.ts` for the chart rows; the View guide rows call `deck.guides` with the centre positions; Center on page calls `block.align` with `to: 'sheet'`); `insertBlockPlan` placing every insert as a positioned block centred on the sheet on every slide kind (the editor's handle converts, 1.6) and never into a slot; the draw tool intents for every shape preset and line kind; `buildMenuContext` from the extended selection (`objectSelected` true for a title or statement slide's field and a plate's block); `tailKindOf` for charts and groups; the retired sentence "Use the toolbar control to pick a value" removed.
5. `useEditorKeys` binding section 9's chords with the routing rules (the text chords inside a run through the InlineText, the rotate keys with a block selected and no caret, Tab in a list item, the zoom chords everywhere in the editor), and handling Cmd+[ and Cmd+] before the focused field early return in every focus state with `preventDefault` always and the indent only when a list item or text block is selected (SPEC-2 0.55); `use-editor-keys.test.tsx` asserts it with focus in the notes pane, the filmstrip and a Format options field.
6. `Sidebar.tsx` exposing `selectAll()` and `selectNone()` on its handle (8.6), the Cut path without a snackbar and the filmstrip's Change background row on every slide kind; `filmstrip.spec.ts` extended as 11.6 says.
7. The icons the new rows need (Heroicons 20 solid names) in `icons.tsx` and the theme sprite through `packages/theme/scripts/add-icon.ts`, `sprite.ts` regenerated.

### Acceptance

```
cd packages/chrome && ../../node_modules/.bin/vitest run
node_modules/.bin/tsc -b packages/chrome
[server 4432, TURBOSLIDE_STORE=tmp] node apps/cli/bin/turboslide.mjs lint --chrome --url http://localhost:4432/new --widths 1440,1280,390 --themes light,dark --states editorGrid,editorMenu,editorPanel
[server 4432, TURBOSLIDE_STORE=tmp] node apps/cli/bin/turboslide.mjs lint --chrome --url http://localhost:4432/edit/gt-brand --widths 1440,1280,390 --themes light,dark --states editorGrid,editorMenu,editorPanel
[server 4432, TURBOSLIDE_STORE=tmp] node scripts/tooltip-audit.mjs --base http://localhost:4432 --strict
[server 4432, TURBOSLIDE_STORE=tmp; take .turboslide/e2e.lock] PLAYWRIGHT_BASE_URL=http://localhost:4432 node_modules/.bin/playwright test apps/studio/e2e/filmstrip.spec.ts apps/studio/e2e/editor.spec.ts apps/studio/e2e/undo.spec.ts
```

0 chrome lint findings, 0 tooltip misses, 0 tooltips or labels containing an action id, a JSON pointer or a word of section 10's list in the default view; `menu-model.test.ts` counts the rows flipped (section 4.1, the View rows included) and asserts every remaining Later row has a clause from section 12.

## B4 The canvas, text and the draw tools

Estimate: 112 agent-hours, days 2 to 6 (the long pole of the round). Starts after merge 1 and types against B2's measurer from merge 1c; uses B3's pickers as props the route passes. Scope: SPEC-2 section 1 (the conversion in the editor and the first write) and section 6 in full (every behaviour of 6.1, the surfaces of 6.2), plus the text editing and the draw tools of the first draft.

### Inputs

Sections 1 (all), 2.1, 2.2, 2.4, 2.5, 2.6.4 (the gestures), 6 (all), 7.2 (the editable run), 9, 10 (the readouts, the accessibility of the handles, rulers and guides), 11.5 (viewer and chrome rows), 11.6 (`canvas`, `objects`, `text-styles`), 11.8; SPEC 4.3, 6.4, 10.2; `docs/freeform.md`; R05 A1, A5, A10, B7, B11, B15, C1 to C7; R09 A1 to A3; R02 sections 6 and 12; R08 A5 to A14, A20; R04 B3, B7.

### Owns

`packages/viewer/src/**` except `packages/viewer/src/present/**` (the present branches are untouched this round): `packages/viewer/src/{Editor,Gestures,Freeform,InlineText,Selection,Marquee,Guides,GridView,BookView,Frame,LiveClone,MaterialMount,Stage,Sheet,SlideView}.tsx` and their CSS (`BookView.css`, `Editor.css`, `GridView.css`, `Guides.css`, `InlineText.css`, `LiveClone.css`, `Marquee.css`, `Sheet.css`, `Stage.css`), `packages/viewer/src/{clipboard,keys,snap,model,theme,dither,hash}.ts`, `packages/viewer/src/modules.d.ts`, new `packages/viewer/src/{canvas-measure,rotate,crop,zoom,rulers-model,marks}.ts`, `packages/viewer/src/__tests__/**` (the existing `clipboard`, `dither`, `editor-keys`, `freeform`, `gestures`, `grid-view`, `hash`, `inline-text`, `keys`, `marquee`, `selection`, `sheet`, `snap-freeform`, `snap` tests; new `canvas`, `rotate`, `crop`, `zoom` tests); `packages/chrome/src/{Overlay,ContextMenu,NotesPane}.tsx` and their CSS (`Overlay.css`, `ContextMenu.css`, `NotesPane.css`), new `packages/chrome/src/{Rulers,DeckGuides}.tsx` and `{Rulers,DeckGuides}.css` (the chrome's `DeckGuides.tsx` draws the deck's guides and their menu; the viewer's `Guides.tsx` stays the snap guide lines; 0.108), `packages/chrome/src/__tests__/{overlay-component,ContextMenu,NotesPane}.test.tsx`, new `packages/chrome/src/__tests__/{rulers,deck-guides}.test.tsx`; `apps/studio/e2e/{canvas,objects,text-styles}.spec.ts` (all three new).

### Delivers

1. The conversion in the editor (1.2, 1.3, 1.6): `canvas-measure.ts` `measureForCanvas(document, slide, theme)` rendering the slide into a hidden 1x sheet with `prompts: true`, awaiting `awaitSheetReady` and measuring with `measureCanvasBoxes` (0.97); `Freeform.tsx` `toFreeform` and `toGrammar` as calls to `toCanvas` and `fromCanvas`; the first canvas gesture on a slide that is not a canvas measuring, converting and committing one `slide.update` of `[slide.replace, ...the gesture's mutations]`; the list of what converts and what does not exactly as 1.6; the selection surviving (`heading`, `lead`, `big` keep their ids); `EditorHandle.toCanvas()`; the stage's own measurement kept for hover, selection and gestures.
2. Selection on every slide kind (6.1 rows 1 to 6): click, the caret surface, Shift and Cmd click, the marquee from empty sheet and from the stage outside the sheet (the workspace, which also deselects on a click and opens the empty canvas menu on a right-click; `selection.coversSheet` for a covering picture object, 0.100), Tab and Shift+Tab in z order on a canvas and in document order otherwise (0.83), Cmd+A with the canvas owning focus, Esc, click on empty, the hover outline; a title or statement slide's field and a plate's block as objects; the multi selection chip "3 objects".
3. Move and nudge (rows 7, 8): the drag of the frame or the body with the snap lines of other objects' bounding box edges and centres (a rotated object by its bounding box, 0.107), the sheet's edges and centre, the deck's guides and the GT lines under `snapGuides`, the 8 px grid under `snapGrid`, nothing under both off; the equal spacing guides; Shift constraining to an axis; Cmd+drag suppressing snaps; the arrows 1 px and Shift+arrows 10 px on every slide kind (0.87); one `slide.update` per gesture with `followConnectors`.
4. Resize and rotate (rows 9 to 12): the eight handles with Shift aspect and Option about the centre and the four cursors; the size chip; the rotation handle with the angle chip and the 15 degree Shift snap, the rotated ring and handles drawn from `pos`, Google's rotate keys and the Cmd+Option alias (0.78), `block.rotate` and `block.flip` from the handle; a group's rotation as one `block.rotate` with `about: 'selection'`, a group's resize scaling every member about the union and a group's flip as one `block.flip` with `about: 'selection'` (0.102); a line's end handles, which snap to the connection sites under the pointer and set or remove `connect` (a reroute, 0.103); a text object re-flowing in its new box with autofit after the commit; `MaterialMount` re-mounting a material block's or a picture object's shader at the new box, at any ratio, after every commit (0.94, 0.105); the editor's stage lifting the sheet's clip while editing so an object past the edge shows on the workspace (`Stage.css`, 0.96).
5. Z order, groups, duplicate, clipboard, delete (rows 13 to 17): Cmd+Up, Cmd+Down and the Shift forms on every slide kind with one stack per canvas; group selection, the double click into a member, the union ring and "Group" chip, every gesture over members, the remembered ungrouped set for Regroup, `block.group`, `block.ungroup`, `block.regroup` from the handle; Cmd+D and Option+drag duplicating at the drop (0.79); paste keeping `pos` across slides and offsetting on the source slide (0.84); Delete and Backspace over every selected object, the picture object included.
6. Text editing and crop (rows 18, 19): `InlineText` with the marks (Cmd+I, Cmd+U, Cmd+Shift+X, Cmd+., Cmd+, over a range or the word at the caret; `runsFromNode` reading the elements; the editable HTML rewritten from the runs under the rule of 7.2 and SPEC-2 0.54: at a burst boundary only, only when the serialized runs differ from the canonical string, the selection recorded as plain text offsets and restored, bold moved onto the same path and `execCommand('bold')` removed), the pressed state exposed to the toolbar through the selection's `marks`, Tab and Shift+Tab list levels to 9, Cmd+[ and Cmd+] inside a run preventing the default (0.55), Enter twice leaving a list, `text.style` and `text.list` writes at the burst boundary; the Capitalization writes over the range; autofit after each commit on positioned blocks and on grammar placeholders with `shrink` (0.41), and the `apply` path of `block.autofit` on the window transport measuring at once (0.64); the converted title, statement and plate texts editing the same way; crop mode on every picture object and shot (the double click, the black handles, the pan, Enter, Esc and click away, the chip sentence, the mask outline; `block.crop` and `block.mask` from the handle).
7. Connectors and the draw tools (rows 20, 33; 6.2): the draw tools per shape preset and line kind (the click default, the drag, Shift and Option, the point tools, the scribble sampling), a connector's ends snapping to the target's connection sites (`sites()`) and recording `connect` (SPEC-2 2.4.7, 0.65), `followConnectors` called at every gesture end and from the Size & rotation and Position fields so attached connectors travel in the same write, the word art bar's insert, the chart and table default placements, the special characters insert at the caret (`text.insert`) or into a new text box; every insert as a positioned object on every slide kind, converting first.
8. Zoom and pan (rows 27, 28): `zoom.ts` with the ladder to 1600, the clamp and the centre math (0.101); `EditorHandle.zoomTo` and `zoomStep`; Cmd+scroll and the pinch (`wheel` with `ctrlKey`) zooming about the pointer with the default prevented over the stage; the stage scrolling while zoomed; Space+drag panning with the `grab` cursor; `view.zoom` with `center` through the route.
9. Rulers, guides and snap (rows 29 to 31): `Rulers.tsx` in inches at the stage scale (0.82) with the ticks, numerals, the selection's shaded extent, the pointer hairline and the drag out that creates a guide; `DeckGuides.tsx` drawing the deck's guides in the overlay, dragging one with the inch readout, the guide's right-click menu (Delete guide, Edit guides greyed); `rulers-model.ts` for the tick math; the deck's guides as snap lines under `snapGuides`; `EditorProps.guides`, `onGuides`, `showRuler`, `showGuides`, `snapGuides`, `snapGrid` read from the route.
10. Format painter over objects (row 32) carrying run marks and the new block fields (0.37); the Overlay's readouts (size, angle, guide position), `role="group"` on a group's ring, the rotation handle as a `slider`, the crop handles focusable, every handle with the Tooltip primitive (section 10).
11. The right-click targets (row 25): `EditorContextTarget` gaining `shape`, `line`, `group`, `chart`, `cellRange` and `guide`; `ContextMenu.tsx` drawing every target of 4.3 on every slide kind and the guide's menu.
12. `canvas.spec.ts` (the canvas walk of 11.8 with its step 8, recording `.turboslide/canvas-walk/<slideId>.json` for B1's CLI test), `objects.spec.ts` and `text-styles.spec.ts` (11.6).

### Acceptance

```
cd packages/viewer && ../../node_modules/.bin/vitest run
cd packages/chrome && ../../node_modules/.bin/vitest run Overlay ContextMenu NotesPane rulers deck-guides
node_modules/.bin/tsc -b packages/viewer packages/chrome
[server 4434, TURBOSLIDE_STORE=tmp; take .turboslide/e2e.lock] PLAYWRIGHT_BASE_URL=http://localhost:4434 node_modules/.bin/playwright test apps/studio/e2e/canvas.spec.ts apps/studio/e2e/objects.spec.ts apps/studio/e2e/text-styles.spec.ts
node scripts/compare-to-shoot.mjs --deck decks/gt-brand --render .turboslide/render --shoot /Users/kevinliu/repos/Prototemplate/deck --max-mismatch 0.005   # after the walk: the committed deck is untouched
```

The walk's recordings under `.turboslide/canvas-walk/` are handed to B1 (a request in `build-2/b4.md`) so `apps/cli/src/commands/canvas.test.ts` and the committed `__fixtures__/canvas-walk/` assert the CLI's conversion equals the editor's.

## B5 Charts, diagrams and tables

Estimate: 40 agent-hours. Starts after merge 1.

### Inputs

Sections 0.24 to 0.26, 2.7, 2.8, 4.2 (the table and chart tails), 4.3 (`tableCell`, `cellRange`, `chart`), 5 (Chart data, Table, the hover grid, the Diagram panel), 6 (the hover grid); R11 A1 to A7, A10; R05 A6 to A8.

### Owns

New `packages/schema/src/diagrams.ts` and `packages/schema/src/diagrams.test.ts`; new `packages/chrome/src/inspector/{chart,table}.tsx` and `.css`; new `packages/chrome/src/pickers/{TableGrid,DiagramPicker}.tsx` and `.css`; new `packages/chrome/src/DiagramPanel.tsx` and `.css`; `packages/chrome/src/{table-tools,chart-tools}.ts` from merge 1 (B3 lands them as typed stubs on day 1 with the signatures the seams list fixes; B5 fills the bodies and does not change the exports); new `packages/chrome/src/__tests__/{table-tools,chart-tools,chart-grid,table-section,table-grid,diagram-panel}.test.*`; `apps/studio/e2e/{tables,charts}.spec.ts` (new).

### Delivers

1. `diagrams.ts`: the six templates with their counts and three styles, `make(kind, count, style, box, group)` returning grouped shape, text and line blocks in the dia stroke grammar; the test of 11.5.
2. `table-tools.ts`: pure plans from a cell or a range for every Format > Table and context row (insert and delete with counts, distribute, merge, unmerge, cell style including `weight: 0`, table border) as the action inputs of section 3, behind the `tablePlan` signature of the seams list; `chart-tools.ts`: the placeholder data per kind, the data grid's edits as `chart.setData`, the kind change with its dropped series note, legend and number format writes, behind `chartPlaceholder`, `chartDataPlan`, `chartKindPlan` and `chartOptionPlan`.
3. The Table section of Format options (section 5) and the Chart data section with the data grid (a `grid` with the accessibility rules of section 10); the Insert > Table hover grid plate (`TableGrid.tsx`, mounted by B3's dynamic submenu); the Diagram panel in the right slot (type tiles, the count control named per type, the style tiles, a preview drawn from `diagrams.ts`, Insert running `diagram.insert`, which lands the group as positioned objects centred on the sheet and converts a grammar slide first, SPEC-2 1.6).
4. The chart tail's dropdowns (kind, legend, number format) and the table tail's Merge and Unmerge buttons as the ops B3's tail lists (`chart-tools.ts` supplies the handlers B3's `ToolbarTail.tsx` calls by op name).
5. `tables.spec.ts` and `charts.spec.ts` (11.6).

### Acceptance

```
cd packages/schema && ../../node_modules/.bin/vitest run diagrams
cd packages/chrome && ../../node_modules/.bin/vitest run table chart picker diagram
[server 4435, TURBOSLIDE_STORE=tmp; take .turboslide/e2e.lock] PLAYWRIGHT_BASE_URL=http://localhost:4435 node_modules/.bin/playwright test apps/studio/e2e/tables.spec.ts apps/studio/e2e/charts.spec.ts
```

## B6 Hosted export batching, Blob snapshots, lint entry, eslint, e2e budgets, hygiene

Estimate: 44 agent-hours. Starts after merge 1.

### Inputs

Sections 0.28 to 0.35, 0.40, 0.43 to 0.45, 0.48, 0.56, 8, 11.1 (step 6), 11.5 (store and export batch rows), 11.6 (`hygiene`, `export-batch`); `docs/hosting.md`, `docs/hosting-chromium.md`, `docs/hosting-diagnosis.md`; `apps/studio/src/server/export-sync.ts` and `download.ts` as they stand; `apps/studio/vite.deploy.config.ts` (the limits, read only); `packages/store/src/blob-store.ts` `pull()` and `write()`.

### Owns

`apps/studio/src/server/{export-sync,download,lint}.ts`, new `apps/studio/src/server/{export-batch,lint-rendered}.ts` (the adapter, no test of its own), new `packages/export/src/batch/{plan,merge}.ts` and `{plan,merge}.test.ts` (inside B2's package; ownership is per file, SPEC-2 0.48), `apps/studio/src/routes/api/export.$deckId.ts`, `apps/studio/src/routes/new.tsx`; `packages/store/src/**` (`blob-store.ts`, `blob-fake.ts`, `blob-vercel.ts`, `hosted.ts`, `versions.ts`, `store.ts`, new `snapshots.ts`, their tests; the file, tmp, templates, bundle and zip modules untouched); new `packages/lint/src/run-client.ts`; `tooling/eslint-config/**` including new `tooling/eslint-config/baseline.json`, new `scripts/lint-packages.mjs`, `scripts/check-client-bundle.mjs`; `apps/studio/e2e/{ten-tasks,text-editing}.spec.ts` (the budgets; handed over from the integrator and B4 for this round), new `apps/studio/e2e/{hygiene,export-batch}.spec.ts`, `scripts/hosted-smoke.mjs`, `docs/hosting.md`, `docs/hosting-chromium.md`.

### Delivers

1. The batched Perfect export (8.1): `planBatches`, `assetHashes`, `isStale` and `mergeParts` in `@turboslide/export/batch` with `EXPORT_BATCH_SIZE` derived from the named constants (240 s of work per batch over 2.6 s per slide times 1.5, so 60; SPEC-2 0.44) and `TURBOSLIDE_EXPORT_BATCH` as the override; `startBatchedExport` (pruning jobs older than 24 h, recording the asset hashes), `exportBatch` (idempotent, `overwrite: true`, answering `stale` for a moved revision or asset), `mergeExport` (streaming parts to disk, building from paths, answering `peakMb`) and `cancelBatchedExport` as server functions in `export-batch.ts`, the http form on `api/export.$deckId.ts` (`?batch=`, `?merge=`, `?cancel=`), the stored parts under `exports/<deckId>/<jobId>/parts/`, the `jsonBody` answer; the client function `runBatchedExport(deckId, input, onProgress)` in `download.ts` the Download dialog calls, with the one retry per batch and the `pagehide` cancel through `fetch` with `keepalive` (0.45) (B3 draws the progress; the integrator wires `EditorExport.progress`).
2. The Blob snapshots (8.2, SPEC-2 0.40): `snapshots/<md5>.json` keyed by the md5 of the `deck.json` bytes about to be pushed, stored with `overwrite: false` before `deck.json` flips; the version record carrying `snapshot: <md5>`; the snapshot path in `pull()` proven by the etag with one `head()`; `documentAtRevision` through the record's key with the replay fallback; retention keeping the last 50 records' and every named version's snapshots; `deck.info`'s `snapshots` count on the Blob store; `hosted.test.ts` extended with the five cases of 11.5 including the two writer race.
3. `run-client.ts` and the studio's server lint split (8.3); `check-client-bundle.mjs` failing on `node:` builtins in the client output.
4. `scripts/lint-packages.mjs` and the eslint config changes (8.4): `--changed` passing the changed files to eslint, the per package counts, `tooling/eslint-config/baseline.json` measured on `main` at `a65b313` and the exit rule against it (SPEC-2 0.56); the files this round touches eslint clean at the end of the round (each builder fixes their own; B6 reports the per package counts and the baseline in `build-2/b6.md`).
5. The e2e budgets (8.5) in the two specs; the route's `selectSoon` change is a request to the integrator.
6. `hygiene.spec.ts` (Help > Help Turboslide improve, the undoable first write, Select all and Cut with the filmstrip focused, Select none clearing a block selection) and `export-batch.spec.ts` (9 batches of 3 over 11.2's total and one merge); `hosted-smoke.mjs` with the two batch export row and a snapshot row (`deck.info` reports `snapshots` on the preview).

### Acceptance

```
cd packages/store && ../../node_modules/.bin/vitest run
cd packages/export && ../../node_modules/.bin/vitest run batch
node scripts/lint-packages.mjs --changed          # exit 0 on the changed files
node scripts/lint-packages.mjs                    # per package counts; exit 0 while no package is above tooling/eslint-config/baseline.json
[server 4436, TURBOSLIDE_STORE=tmp, TURBOSLIDE_EXPORT_BATCH=3; take .turboslide/e2e.lock] PLAYWRIGHT_BASE_URL=http://localhost:4436 node_modules/.bin/playwright test apps/studio/e2e/export-batch.spec.ts apps/studio/e2e/hygiene.spec.ts
[server 4436, TURBOSLIDE_STORE=tmp; take .turboslide/e2e.lock] PLAYWRIGHT_BASE_URL=http://localhost:4436 node_modules/.bin/playwright test apps/studio/e2e/ten-tasks.spec.ts apps/studio/e2e/text-editing.spec.ts   # three runs while `node apps/cli/bin/turboslide.mjs export decks/gt-brand --mode flatten --out .turboslide/load` runs on the same machine
```

The bundle check is the integrator's step 6 once the tree builds; B6 asserts it with `node scripts/check-client-bundle.mjs apps/studio/dist` on the integrator's build output at merge 2.

## Integrator

Estimate: 48 agent-hours across the eight days.

### Owns

`apps/studio/src/routes/edit.$deckId.tsx` and `edit.$deckId.css` (the `on(...)` table for the 36 actions, the `EditorHandle` wiring, the pickers and dialogs mounted, the crop and word art modes, the canvas wiring of SPEC-2 1.6, the zoom, pan, rulers and guides state, the Download dialog's batch progress, the draft's history hand off, `selectSoon`, the filmstrip's `selectAll` and the Cut path), `apps/studio/src/server/{actions,agent-actions,contracts,sessions,auth,tokens,decks,write,root,thumbs,render,bundle,bundle-core,health,warm,json,hosting-plugin}.ts`, `apps/studio/src/components/**`, `apps/studio/src/routes/{__root,index,decks.index,decks.trash,print.$deckId,present.$deckId,deck.$deckId,embed.$deckId}.tsx`, `apps/studio/vite.config.ts`, `apps/studio/vite.deploy.config.ts`, `packages/theme/src/tokens.ts` (the `--pt-` tokens the rulers and guides need arrive as requests from B4, 0.108), `playwright.config.ts` (day 1: `PLAYWRIGHT_BASE_URL`, SPEC-2 0.43), `apps/studio/e2e/gslides-actions.spec.ts` (the 36 actions on every transport), `apps/cli/e2e/**`, `scripts/check.mjs` (steps 6, 20, 21, 22, 23, 24), root `package.json` (`lint`), `turbo.json`, every `packages/*/package.json` and `apps/*/package.json` (exports and dependencies; `packages/lint/package.json` gains `./run-client`, `packages/schema/package.json` `./shapes`, `./diagrams` and `./canvas`, `packages/render/package.json` `./measure-dom`, `packages/export/package.json` `./batch`), `pnpm-workspace.yaml`, `AGENTS.md`, `README.md`, `docs/README.md`, `docs/freeform.md`, `docs/spec/SPEC.md` (the amendments of section 2.9), `skills/*/SKILL.md`, new `docs/gslides-parity/BUILD-STATUS-2.md`, the conflict resolution on any shared line, the branches and merges, `pnpm install`, `pnpm build`, `pnpm generate:contracts` when B1 is not running it.

### Delivers

1. Day 1, before merge 1: `playwright.config.ts` reading `PLAYWRIGHT_BASE_URL` for `baseURL` and defining no `webServer` when it is set; AGENTS.md's dev server section recording this round's per builder port exception and the `.turboslide/e2e.lock` rule; the package exports above. Merge 1 (B1 and B3's day 1 files) with `pnpm check` steps 1 to 6 green, `GS2_ACTION_IDS` collapsed into `ActionId`, and `pnpm generate:contracts` current; merge 1b (B1's day 2 interpreter) before B2 renders shapes; merge 1c (B2's measurer) the evening of day 2.
2. The `on(...)` handlers for every action of section 3 and the `EditorHandle` methods of the seams list: `slide.toCanvas` and the canvas writes running the store actions with the editor's commit so the conversion travels in one `slide.update` (SPEC-2 1.6, 0.73; `convertedLayout` in the route becomes the measured conversion for the freeform target); `deps.measureCanvas` for the hosted http transport bound to the render worker facade of `server/render.ts` (a `measureCanvas` method beside `render.slide`, the worker over HTTP or the CLI child process, never Chromium in the actions function, 0.104); `deck.guides` with the guides passed to the stage and the rulers; `view.zoom` with `center` publishing the zoom and the scroll and reporting both; `selectNone`, `applyAutofit` measuring through the editor for `block.autofit` with `apply`, the connector follow on every move path; every menu effect of kind `action`, `toggle` (`showRuler`, `showGuides`, `snapGuides`, `snapGrid` persisted per browser) and `client` (`zoomIn`, `zoomOut` through `editor.zoomStep`) resolving; the crop and word art modes mounted; `Rulers` and `Guides` mounted around the stage from the settings; the Background dialog's Choose inserting the picture object through the handle; the Download dialog driving `runBatchedExport` when the play list is over the batch size on a hosted studio; the draft's history kept across the hand off (0.29); `selectSoon` until the filmstrip renders the id (8.5); the filmstrip's Select all, Select none and the Cut path without a snackbar (0.30, 0.61); `SERVER_SIDE_WINDOW_ACTIONS` for `block.setAlt` when it uploads nothing (it does not), for `diagram.insert` when the template needs an asset (it does not), and never for `slide.toCanvas` on the window transport (the editor measures itself, 1.3); `export.run` with `batch` and `merge` on the http route.
3. Merge 2 with every builder's branch, conflicts resolved in the parity chain order (schema, render and export, chrome, viewer, charts and tables, hosted).
4. `gslides-actions.spec.ts` extended to the 36 actions (`slide.toCanvas` asserting one `slide.replace` and `pos` on every object of a Title slide; `deck.guides` asserting the deck field); check steps 22 to 24; step 21's spec list with `canvas.spec.ts`; step 6's bundle check.
5. `pnpm check` 24 of 24 on the merged tree; contracts current; `AGENTS.md`'s deviations list with section 2.9 (the eleven items) and every builder's recorded deviation; `docs/freeform.md` amended (rotation, the canvas as the one form of an arranged slide, the measured conversion on every transport, the `layout/freeform` sentence, the two severities of `freeform/off-sheet`, 0.96); the four skills' prose naming the canvas, the conversion, the marks, the shape presets and the 36 actions; `BUILD-STATUS-2.md` with one heading per builder, what landed, what was cut, and the open requests.

### Acceptance

```
node scripts/check.mjs   # 24 of 24
pnpm generate:contracts && git diff --exit-code -- packages/agent/generated skills/*/references docs/grammar.md packages/schema/src/rules.json packages/lint/fixtures/index.json ':(literal)apps/studio/src/routes/openapi[.]json.ts'
[server 4390] node_modules/.bin/playwright test apps/studio/e2e/gslides-actions.spec.ts
```

## Verifier

Estimate: 30 agent-hours, days 6 and 7.

### Owns

`scripts/gslides-parity-audit.mjs`, `scripts/tooltip-audit.mjs`, new `docs/gslides-parity/VERIFICATION-2.md`, new `docs/gslides-parity/verification-2/**` (`parity-audit.json`, `parity-audit-preview.json`, the container verification reports and PNG diffs, the ten task counts, screenshots of every new surface on the preview, the export and PDF reports, the chrome lint and tooltip audit outputs, the budgeted specs' three runs under load, the eslint per package counts).

### Delivers

1. The parity audit extended to every row of section 4 (the flipped rows, the dynamic submenus' children, the new tails after selecting a shape, a line, a chart, a group and a cell range, the new context menu targets, the section 9 chords, the picker picks writing their actions), the section 10 words in the new surfaces, and the tooltip audit walking the pickers and dialogs; exit 0 against `vite preview` and against the preview deployment under the token.
2. The container verification of 11.3 in the rebuilt `turboslide-render-worker` image, recorded per slide.
3. Every builder's acceptance rerun on the merged tree with the numbers (the fixture deck at 11.2's total in both modes and in the container); the canvas walk of SPEC-2 11.8 rerun on the merged tree and on the preview, its `pos` recordings compared with the CLI's conversion on a local copy, and the fidelity script's worst slide and its diff PNG; the ten tasks driven by hand on the preview with counts against SPEC 11.2 (unchanged targets); the budgeted specs three times under load; the merge's `peakMb` for the fixture deck and the GT deck against the 3009 MB function (SPEC-2 0.44); the eslint per package counts against the baseline.
4. One Perfect and one Editable text export of the fixture deck and one PDF recorded with the LibreOffice renders; the italic, rotated, grouped, merged and chart slides opened in PowerPoint where a copy is available (the manual checklist of `docs/export-verification.md`), else listed for Kevin.
5. VERIFICATION-2.md in the shape of VERIFICATION.md: the 24 check steps with their numbers, the audit totals per menu, the container results, the task counts, the merge's peak memory, the eslint baseline, the blockers, the deviations recorded, and the list for Kevin (the unverified Google facts of section 13, the Cmd+, chord, the unbound two key chord of Select none, Format > Text > Font staying Inter, the title, statement and picture kinds' heading, lead and big fields keeping the grammar's fixed sizes because no block carries `autofit` there (SPEC-2 0.41), any preset whose formulas the interpreter could not evaluate, which is a build failure rather than an approximation and should be empty, and the canvas facts SPEC-2 section 13 lists as unverified: the size readout, the ruler drag out, Align with one object, the paste position, Cmd+A with no caret, the pinch, the Cmd+Option rotate alias, the marquee's crossed objects, the undimmed overflow on the workspace, font sizes under a group resize, the rotated snap; and the canvas decisions that scope or extend Google (0.106): the theme elements (the wordmark, the counter, the rails, the chips, the paper ground) staying fixed until Slide > Edit theme as Google's master elements are, the workspace and the covering picture's appended menu rows as the escape Google never needs (0.100), the editor and CLI `pos` identity holding in Chromium only (0.104), and the severity 2 `freeform/off-sheet` for an object crossing the slide's edge (0.96)).

### Acceptance

```
[server 4321] node scripts/gslides-parity-audit.mjs --base http://localhost:4321 --out docs/gslides-parity/verification-2/parity-audit.json   # exit 0
node scripts/gslides-parity-audit.mjs --base <preview origin> --out docs/gslides-parity/verification-2/parity-audit-preview.json
docker build -f docker/render-worker.Dockerfile -t turboslide-render-worker . && docker run --rm -v "$PWD/.turboslide/container:/work" turboslide-render-worker turboslide export decks/fixture/gslides --mode native --verify --out /work/gs-native
```

## Fixer round

Estimate: 24 agent-hours, day 7.

The verifier's VERIFICATION-2.md lists every miss with the builder who owns the file. Each builder fixes only in their own files; the integrator merges in the same order as merge 2; the verifier reruns the audit, the e2e specs of step 21 and `pnpm check`. A miss that needs a design change (a Google fact the audit shows we read wrong, a preset whose path cannot be drawn exactly, a pptxgenjs option that does not exist) is written into VERIFICATION-2.md as a deviation with the reason and left for Kevin, not patched around. The fixer round ends when the audit exits 0 and `pnpm check` is 24 of 24, or when the remaining misses are all recorded deviations.

## Ship step

Estimate: 8 agent-hours.

1. `pnpm check` 24 of 24 on `gs2/integration` with `main` merged in; `git log` shows one commit per builder plus the integration commits; no conflict markers (`git grep -n '<<<<<<<'` empty); the contracts and the fonts committed.
2. A preview deployment of `gs2/integration`; `node scripts/hosted-smoke.mjs --base <preview>` green including the batch export row; the parity audit against the preview exit 0; one batched Perfect export of the GT deck from the preview's Download dialog (two batches at the derived size) answers `perfect: true` in under 5 minutes of wall time with `peakMb` recorded; one PDF downloads with one page per slide.
3. Kevin's manual checks on the preview: the GT deck's opener photograph dragged past the edge and sent back behind a new text box, Change background from the photograph's right-click menu, the Title slide's heading dragged and rotated, a shader material resized, a text box on the Title slide, a rotated and grouped shape, italic in a heading, a chart, a merged table, the special characters picker, the ruler and a guide, zoom by pinch, 1600 typed and pan, the wording of section 10 on screen, the Later stubs of section 12 reading as intended.
4. Merge to `main`, push, production deploy; `hosted-smoke.mjs` against production green; a production `deck.info` on a deck written after the deploy reports `snapshots: 1`.
5. The verifier appends the production table to VERIFICATION-2.md.

## What ships in this round

- The canvas: every slide is a canvas (SPEC-2 section 1); the first drag, resize, rotate, nudge, duplicate or insert on a title, statement, section header, caption, closing or grammar content slide converts it losslessly to the freeform layout in the same write, measured by one function in the editor and from the CLI; the photograph of a picture kind becomes a picture object at the bottom of the stack, the plate a group, the layout's texts objects; untouched slides stay byte identical and the conversion loses no pixels (check step 24); every canvas behaviour of section 6 on every object: selection, move with live guides, nudge, resize with readouts, rotate, flip, order, group, duplicate, copy and paste, delete, text editing in place, crop, connectors that follow, align, distribute, Center on page, the Format options fields, zoom to 1600 and pan, rulers, guides, snap to, the right-click menus, undo across it all, and the format painter; `slide.toCanvas`, `deck.guides` and every canvas write on the CLI, MCP, HTTP and window transports.
- Text: italic, underline, strikethrough, superscript, subscript, run colour and highlight through the mark span rule and the Inter italic build; justified alignment; line and paragraph spacing; columns; indent; bullets and numbering with Google's presets and nine levels beside the untouched ruled list; capitalization; the special characters picker; paint format with marks; autofit as explicit writes on text boxes, on the layouts' placeholders and on converted placeholders, applied on every transport.
- Objects: rotation and flip with the handle, the keys and the section; groups with Regroup; 135 shape presets in four categories with text, drawn exactly from the standard's definitions; seven line kinds, ten line decorations, six dashes; connectors that attach to shapes and follow them; word art with its border controls; drop shadow; the slide background as a colour fill and the picture object; crop with a trim box, mask, adjustments, reset, frames; vertical alignment and four sided padding; alt text on every object.
- Tables: merge and unmerge, per cell fill and border, table border colour and dash, row heights and Distribute rows, insert and delete with counts, the hover grid.
- Charts and diagrams: the chart block with the data grid and native `addChart`; the Diagram panel over six template families.
- Hosted and hygiene: batched Perfect exports sized from the measured 800 s budget with retention, idempotent batches and pinned assets; immutable snapshots keyed by the body's hash with the two writer race tested; the client safe lint entry; eslint per package against a committed baseline; e2e budgets that hold under load with serialised Playwright runs; Help Turboslide improve; the undoable first write; Select all, Select none and Cut with the filmstrip focused.
- The acceptance: 36 actions on every transport, 24 check steps, the parity audit over every flipped row, the canvas walk over one slide of every kind of the GT deck with the CLI's conversion equal to the editor's, the fixture deck at 11.2's total (27 slides) in both modes and in the container, VERIFICATION-2.md.

## What is round three

In the order of section 12: transitions and animations; audio and video; comments with a display name; the view token and Stop sharing; Insert > Templates and Building blocks with the sales starter deck; PPTX import; Page setup; the Edit guides dialog; handouts; ODP and SVG; Preferences with the ruler's unit; Show changes; Reflection and Recolor; the spell check card; Restart numbering and prefix and suffix; Indentation options and the ruler's indent markers; nested groups; Edit points; the cell border chord; the drawing box of the special characters dialog; the theme elements as objects (Slide > Edit theme). Each is a Later stub this round with the clause of section 12, and no clause names a round. Select none, bullet levels 4 to 9 and connector attachment left this list for round two (SPEC-2 0.61, 0.58, 0.65); rulers, guides, zoom and pan left it for Kevin's directive (3) (SPEC-2 0.77).
