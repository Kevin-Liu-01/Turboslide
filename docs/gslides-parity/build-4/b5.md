# B5 notes, round four: README, repository metadata and documents

The documents builder of the Turboslide round four (`docs/gslides-parity/MILESTONES-4.md`, "B5
README, repository metadata and documents"; `docs/gslides-parity/SPEC-4.md` sections 0.25, 0.26,
0.52, 1, 2.2, 4 and 5), in the shared working tree over `main` at `d5d7f07` (`BASE`) after merge 1,
on 2026-09-14. Section numbers refer to SPEC-4 unless prefixed. Every command ran from
`/Users/kevinliu/repos/Turboslide` with Node 24.13.0. No git write command ran, no `pnpm install`,
no build, no dev server (port 4345 stays unused: B5 runs no server). Nothing is committed; the
ship step commits. Appended across stages.

## 1. Day 2 (2026-09-14)

### 1.1 What landed

MILESTONES-4 B5 "Delivers" items 1 to 3, written against the merge 1 tree; item 3 (the README) was
written now because the README workflow of 2026-09-13 finished with its commit `28cb63b` and
round three's `1178478` amended the file after it, so nothing else owns `README.md` (0.52). B1's
day 2 and day 3 outputs (the twins, the card, the wordmarks, the README lockup PNGs, the previews
and `facts.json`) landed between 10:37 and 10:39 while this stage ran, and the documents were
re-read against them before returning (section 1.2 items 1 and 5).

- `docs/brand.md` (new, 18 sections): the mark's construction with the numbers recomputed from
  `packages/theme/src/brand.ts` on this tree (52 of 64 cells at N = 8, 610 of 1,024 at N = 32,
  2,400 of 4,096 at N = 64, the 208 unit path, `D_MAX` 0.625), the size table, the tile with
  `mark-geometry.json`'s rows, the wordmark and lockups, the GT mark, the icon set as built with
  the bytes of `brand-manifest.json`, the head tags, the card, the token table, the where table,
  the app chrome table with its state per surface, the selection colour as an editor affordance
  (ruling 1), the CLI banner, the accessibility record (the WCAG 2.2 URLs read on 2026-09-14 with
  the criterion text as the pages state it, the contrast table recomputed with `contrastRatio`),
  light and dark, the build script and its records, the repository description and topics strings
  with the `gh repo edit` commands and the current values read from GitHub, the social preview file
  with GitHub's stated size rule (read 2026-09-14), the two alternates with their generators, what
  stays for round five, sources.
- `docs/performance.md` (new, 9 sections): the definitions, the today, after, never table re-read
  on the merge 1 tree with the owner and the change that flips each "after" row, the budget tables
  of section 4 with the verifier's day 0 production numbers in the today column, the rows added
  this round, how to run check steps 30 and 31 by hand and against a preview (both fake secrets
  named beside the start command, as the integrator asked), the bundle diet with owners and
  state, the two definitions round three moved (the verifier's requests 1 to 4 recorded as open),
  the day 0 baseline in short (the verifier's finding 2 on the JavaScript sizes included), what
  stays for round five, sources.
- `docs/hosting.md`: line 341's `runSyncExport` now names `apps/studio/src/editor/controller.tsx`
  (b3.md R5); pointers in sections 2, 4, 6 and 8 to the new section 12, "Round four: the CDN
  rules, the thumbnail cache, the seed twins and the native addon" (12.1 the `routeRules` and the
  root redirect with step 30, 12.2 the thumbnail cache on Blob with its retention and the signed
  URL rule, 12.3 the seed twins out of the bundle, 12.4 the native addon in the function, 12.5 the
  contracts routes, 12.6 the check on a checkout with the two secrets and the hosted smoke rows),
  each item with its owner and its state on 2026-09-14.
- `docs/native.md`: the layout block names the committed outputs and `BUILD-RECORD.json`; the
  building section's git ignore sentence describes the day 0 rule; "What runs where" rewritten
  from the tree (a checkout with and without the addon, the hosted studio on TypeScript today and
  what flips it, the worker, the Docker image, the round three patterns); a new section "Round
  four: the committed outputs and the CI rebuild rule"; "Open items" rewritten (the closed M5
  items named as closed, the round four items as open until B4 lands them).
- `packages/native/README.md`: the committed outputs, the build record rule and the pointer to
  `docs/native.md` (the prose only; B4 writes `BUILD-RECORD.json`).
- The four skills' `SKILL.md` prose: one sentence each naming `/home` as the product page and
  `/llms.txt` as the agent guide where the skill names the studio's pages (api "Discovery" item 3,
  studio "Connect" item 1, create "Before writing" item 2, verify "Evidence" item 6). The
  `references/` tables are untouched.
- `docs/README.md`: rows for the round four documents (`SPEC-4`, `MILESTONES-4`, `BUILD-STATUS-4`,
  with `VERIFICATION-4.md` named as the verifier's day 7 file), `brand.md` and `performance.md`.
- `README.md`: the stacked lockup as a `<picture>` under the title (the dark twin as the default
  source for GitHub's light ground, the light twin under `prefers-color-scheme: dark`; 1.13 and R01
  6.9 item 33); the first sentence now equals `SITE.description` with the subject in front; the
  link row gains `/home`, `/llms.txt`, `docs/brand.md` and `docs/performance.md`; the contents list
  gains the two new sections; the title row bullet names the Turboslide mark and `/decks`; the
  round three section's opening sentence names presence, comments, share roles, optional
  accounts, security and dithered backgrounds as features rather than as a round; the `/decks`
  bullet calls it the files page beside `/home`; the MCP tool count is 146 and the HTTP bullet
  names `/llms.txt` and `/home`; the check step sentence reads 31 steps with the three new ones
  named; the parity audit sentence reads 3,369 rows with its file; the hosting section gains the
  realtime tiers, the icon set and `/home` bullets and `docs/performance.md`; "The identity" and
  "Why it is fast" sections before Getting started (the eight rows of 2.2 item 7 with the numbers,
  their sources and dates, and the closing sentence with the verifier's day 0 numbers); the
  getting started block reads 31 steps and gains `pnpm build:brand`; the architecture paragraphs
  of `packages/theme` and `packages/chrome` name the brand module and the brand sheet; the
  documentation index gains round three, round four, `brand.md`, `performance.md` and
  `security.md`; the fifteen markdown images are `<img>` tags (section 1.2 item 3).
- `docs/freeform.md` line 130: the `on(...)` table's file is `apps/studio/src/editor/controller.tsx`
  (b3.md R5, forwarded to B5 by the integrator in BUILD-STATUS-4; the file is in nobody's row and
  the edit is that one pointer).

### 1.2 Readings and deviations, recorded

1. The README hero's PNGs (`docs/readme/brand/lockup-stacked-dark.png`, `lockup-stacked-light.png`)
   are B1's day 3 output through `build-brand.ts --readme`. They did not exist when this stage
   started (the README was written first, as the task said, with the `<picture>` naming the two
   paths SPEC-4 section 5 fixes) and landed at 10:39 while the notes were being written. Read back
   with sharp before returning: 832 by 580 px each (a 2x raster of a 416 by 290 CSS px lockup),
   7,236 and 7,202 bytes, the dark file's ground `#070707` and the light file's `#ffffff`, so the
   `<picture>`'s assignment of item 2 holds and its `width` attribute was set to 416. MILESTONES-4's
   link check reads markdown links only; the two paths were checked by `ls`.
2. Which twin is the default (b1.md 1.2 item 6 left the pick to B5): the `<img>` source is the dark
   twin (the ink ground with the paper mark and word) because GitHub's default ground is light
   and an ink hero reads on white (1.13; R01 6.9 item 33), and the `<source>` under
   `prefers-color-scheme: dark` is the light twin for a reader on GitHub's dark theme. SPEC-4
   section 5 words the media attribute the other way round ("`lockup-stacked-light.png` for
   `prefers-color-scheme: light`"); 1.13's reasoning was followed and the sentence is recorded
   here as the reading.
3. The B5 acceptance grep `grep -nP '\x{2014}|!|Built on Rust'` flags every markdown image
   (`![alt](src)`), which the acceptance note "the `!` outside code spans" did not foresee. Rather
   than argue the reading, the README's fifteen images are `<img alt src>` tags (GitHub renders
   them the same; the alt texts are unchanged, with `&` written as `&amp;`), and the literal
   command is empty on every touched document. The three documents that named the forbidden
   phrase in order to forbid it now describe it without writing it, for the same reason.
4. The README's first sentence: SPEC-4 section 5 says "the first sentence unchanged (it is
   `SITE.description`'s source)" and MILESTONES-4 asks that it equal `SITE.description`'s source,
   but on `BASE` the sentence was "Turboslide is an agent native slides editor with Google Slides'
   structure and behaviours in the Prototemplate look.", while `site.ts` carries R05 6.10's
   sentence. The README now opens with `SITE.description` behind "Turboslide is" and keeps the
   Prototemplate clause as its second sentence, so the head, the manifest, the card and the README
   say one thing.
5. `facts.json` (0.25) was B1's day 5 item and did not exist when the documents were written, so
   every count in the README and `docs/brand.md` was read from the tree by the method 0.25 names,
   on 2026-09-14; the file then landed at 10:39 and every count agrees with it (169, 146, 21, 135,
   17, 31, 3,369). One definition differs: `facts.json` counts 153 HTTP paths, every path of
   `openapi.json`, where the tree count below took the 149 under `/api/actions/`; the README now
   states both ("153 HTTP paths in the OpenAPI document, 149 of them under `/api/actions/`") with
   `facts.json` as its source. The counts as read from the tree: 169 actions
   (`packages/agent/generated/manifest.json` `actionCount`), 146 MCP tools
   (`mcp-tools.json` `tools`), 149 `/api/actions/` paths (`openapi.json`; 153 paths in all),
   21 layouts (`LAYOUTS`), 135 shape presets (`SHAPE_PRESETS`; `PRESET_DEFINITIONS` holds 138
   entries, the three extra being the legacy rectangle, rounded rectangle and ellipse presets the
   picker does not list), 17 materials (`MATERIALS`), 31 check steps (`check.mjs --list`),
   3,369 parity audit rows (`verification-3/parity-audit.json`: 2,980 pass, 5 fail, 384 skip).
   The README's older "89 MCP tools" and "over 2,100 rows" were pre round three numbers. Request
   R2 asks B1 to compare `facts.json` with these when it lands.
6. The tense rule (0.26, PP 4) was applied to the merge 1 tree, since that is the tree these
   documents were written on. Sentences that must flip to the present tense when a builder's
   change lands before the ship step are listed in section 4 as the ship step checklist for
   these files; none of them is written in the present tense today. In particular the README says
   "The hosted studio runs the TypeScript stages today" and names the `/openapi.json`
   placeholder, the Blob thumbnail cache and the preload changes as round four changes.
7. `docs/native.md` "What runs where" was to be rewritten "from the preview's `describeBackends`
   record" (SPEC-4 section 5); no preview of this round exists yet, so the section states what
   the tree does today with the record marked "not yet taken", and the glibc line waits for
   `hosted-smoke.mjs` on the preview.
8. The README carries no image badges. SPEC-4 0.52 says the badges are refreshed; the file had
   none on `BASE`, and a shields style badge is an external image in a service's colours on the one
   surface that must stay paper and ink, so the link row under the lockup does the badges' job.
9. `docs/freeform.md` is in no builder's row; the one pointer edit was made because the integrator
   forwarded b3.md R5 to B5 in BUILD-STATUS-4 "B5" and "Open requests". Nothing else in that file
   changed.
10. The panel text contrast: SPEC-4 1.12 and proposal 3 give `--pt-panel-text` on `--pt-panel-ink`
    as 14.41:1; `contrastRatio` on the 0.87 white composited over `#101010` (`#e0e0e0`) gives
    14.69:1. The table in `docs/brand.md` carries the computed value and names the difference.
11. GitHub's repository metadata as read on 2026-09-14 (`gh repo view`): the description is the
    pre round sentence, 20 topics are set, and the homepage is the pre hosting round address
    `https://studio-delta-six-40.vercel.app`. `docs/brand.md` section 15 carries the exact new
    strings and three `gh repo edit` commands (the description with the homepage, the topics to
    remove, the four to add); the ship step runs them (ruling 2), and the social preview upload
    is Kevin's.
12. The "Routes load before the click" row uses the verifier's day 0 numbers (544 ms for a one
    slide deck and 598 ms for the GT deck, production, 2026-09-14, BASELINE.md 3.1) instead of
    R04's 399 and 723 ms, because 0.26 says the verifier's numbers replace R04's when they exist
    and the day 0 run is on the current tree. The closing sentence's numbers are the same run's.
13. Other builders' files appeared in the working tree while this stage ran (`home.tsx`,
    `home.css`, `components/home/`, `build-home-assets.ts`, `PresenterPage.tsx`,
    `-access-page.tsx`, `Filmstrip.tsx`, `wordmark.svg`, `outline-wordmark.py`); none is recorded
    in a `b2.md` or `b4.md` yet, and `b1.md` and `b3.md` did not grow. The documents name the
    state at the moment of writing and section 4 lists what to re-read.
14. Inherited sentences in `docs/hosting.md` (lines 353, 465 and 587) and `docs/native.md` (line 235) carry an "X, not Y" clause from earlier rounds; they were left as written, since the
    round's copy rule binds the text this round writes and the sentences are other builders'
    records.

### 1.3 Requests

R1 (B1): met before this stage returned. The README lockup PNGs exist at the two paths SPEC-4
section 5 names with the grounds the `<picture>` expects (section 1.2 item 1), so no request
stands; if `--readme` ever changes their size, the `width="416"` attribute in `README.md` follows
(the natural CSS width of the 2x raster).

R2 (B1): met before this stage returned. `facts.json` landed and B5 compared it with the README
and `docs/brand.md` (section 1.2 item 5): every count agrees, and the README names the file as its
source. If `--facts` is re-run after the merge 2 tree changes a count, `b1.md` names the new
number and B5 follows in the fixer round.

R3 (the integrator, the ship step): run the three `gh repo edit` commands of `docs/brand.md`
section 15 (the description and the homepage, the topics to remove, the topics to add) after
Kevin confirms the strings; the social preview upload is Kevin's step and the file is
`apps/studio/public/og/turboslide.png` once B1's day 2 produces it.

R4 (B2, for `apps/studio/src/components/home/copy.ts`): 2.2 item 5 says the two cards round
three changed are rewritten from the ship tree's README by B2 and confirmed by B5. The README's
sentences to confirm against are its "The viewer and sharing" and "Home and files" sections as
amended today (the `/decks` bullet now reads "`/decks` lists your presentations (the files page;
`/home` is the product page)"). For the speed rows (2.2 item 7) the numbers and sources B5 used in
the README are: renders within 0.5 percent (check step 12) and 0.262 percent over 342 pairs
(step 24); 169 actions, 146 MCP tools, 149 HTTP paths (`packages/agent/generated/`, 2026-09-14);
20, 23 and 52 ms per screen and 0 disagreeing cells (`docs/native.md`, 2026-09-10); 0.003 percent
over 170 pages, about 15.5 MiB per theme (`docs/pptx.md`, `docs/HOSTED-STATUS.md`); 187.9 to
203.1 s for the GT deck as one call (`docs/hosting.md` 7, 2026-09-11) and 239.5 s batched at
784.2 MiB (`VERIFICATION-2.md`, 2026-09-12); 800 s and 3,009 MB (`vite.deploy.config.ts`); 544 and
598 ms for `decks->edit` warm (BASELINE.md 3.1, 2026-09-14); 239 to 385 ms, 112 to 299 ms and 164
to 292 ms (`docs/hosting.md` 7, 2026-09-11); the closing sentence's `/decks` 0.29 to 0.45 s or 4.7
to 7.1 s, `/new` ready 823 / 750 ms, the first thumbnail 4.3 s, 3,049 to 3,187 KB (BASELINE.md).
If the page's rows carry other numbers, one of us is wrong and the verifier should hear which.

R5 (the integrator, AGENTS.md, merge 2): two notes for the round four paragraph. The README's
images are `<img>` tags because the B5 acceptance grep counts a markdown image's `!` (section 1.2
item 3), so a future README edit adds an image as a tag too; and `docs/freeform.md` line 130 was
edited by B5 on the integrator's forwarding of b3.md R5.

R6 (the verifier, day 6): the README lockup on GitHub's light and dark grounds (MILESTONES-4 B5
acceptance) is ready to shoot; the `<picture>` and its two files are in place (section 1.2 item 1).
The four inherited sentences of section 1.2 item 14 are not this round's defects.

## 2. Commands run and their results

| Command                                                                                                                                                                                                                                                                      | Result                                                                                                                                                                                                                                                                                                                     |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `git status --short`, `git log --oneline -5`, `git log --since=2026-09-13 -- README.md docs/readme`                                                                                                                                                                          | the merge 1 tree; the README workflow's commit `28cb63b` (2026-09-13) and round three's `1178478` are the last README changes, so the README is free (0.52)                                                                                                                                                                |
| `node scripts/check.mjs --list`                                                                                                                                                                                                                                              | 31 lines (steps 29 `[brand]`, 30 `[vercel]`, 31 `[node-server]`)                                                                                                                                                                                                                                                           |
| `node -e` over `packages/agent/generated/{manifest,mcp-tools,openapi}.json` and `verification-3/parity-audit.json`; `node --experimental-strip-types -e` over `packages/schema/src/layouts.ts`, `shapes.ts`, `shapes/definitions.ts`, `packages/materials/src/catalog.ts`    | 169 actions; 146 tools; 149 of 153 paths under `/api/actions/`; parity 2,980 pass, 5 fail, 384 skip; `LAYOUTS` 21; `SHAPE_PRESETS` 135 and `PRESET_DEFINITIONS` 138; `MATERIALS` 17 (section 1.2 item 5)                                                                                                                   |
| `node --experimental-strip-types -e` over `packages/theme/src/brand.ts` (`contrastRatio`, `litCount`, `markBits`, `solidUnits`, `markPath`, `markBlocks`)                                                                                                                    | the contrast table of `docs/brand.md` section 12 (20.14, 17.97, 10.88, 10.59, 18.64, 14.69, 5.83, 5.68, 4.45, 4.54, 3.25, 6.20, and the select and guide ratios 4.51, 4.47, 3.58, 5.63, 4.62, 4.36, 3.77, 5.34); 52 cells at N = 8, 610 at 32, 2,400 at 64; 208 units; the path and the block glyph                        |
| `gh repo view Kevin-Liu-01/Turboslide --json description,repositoryTopics,homepageUrl,url` (read only)                                                                                                                                                                       | the current description, 20 topics and the stale homepage (section 1.2 item 11)                                                                                                                                                                                                                                            |
| WebFetch of the two WCAG 2.2 Understanding pages and GitHub's social media preview page                                                                                                                                                                                      | the criterion texts and the size rule quoted in `docs/brand.md` sections 12 and 15, read 2026-09-14                                                                                                                                                                                                                        |
| `node -e` converting the README's `![alt](src)` lines to `<img alt src>`                                                                                                                                                                                                     | 15 images converted; `grep -c '^<img alt=' README.md` is 15                                                                                                                                                                                                                                                                |
| `node_modules/.bin/prettier --write` then `--check` on `README.md docs/README.md docs/brand.md docs/performance.md docs/hosting.md docs/native.md docs/freeform.md packages/native/README.md skills/turboslide-{api,create,studio,verify}/SKILL.md`                          | `--write` reformatted the tables of `README.md`, `docs/README.md`, `docs/brand.md`, `docs/performance.md` and `docs/hosting.md`; `--check`: "All matched files use Prettier code style!"                                                                                                                                   |
| `grep -nP '\x{2014}\|!\|Built on Rust' README.md docs/brand.md docs/performance.md docs/hosting.md docs/native.md`                                                                                                                                                           | empty (exit 1) after section 1.2 item 3; the first run had flagged the fifteen images and three sentences that quoted the forbidden phrase                                                                                                                                                                                 |
| a case insensitive `grep -niE` for every word of `METAPHOR_WORDS` (`packages/theme/src/copy.ts`) over every touched file, this note included; `grep -niP 'built on rust'` over the same with this note excluded (its quoted acceptance commands carry the phrase, section 3) | empty (exit 1) both                                                                                                                                                                                                                                                                                                        |
| the MILESTONES-4 B5 link check (`node -e` over the markdown links of `README.md`, `docs/brand.md`, `docs/performance.md`)                                                                                                                                                    | exit 0; the `<picture>` sources are HTML attributes and are named in section 1.2 item 1                                                                                                                                                                                                                                    |
| `grep -n immutable apps/studio/src/server/thumbs.ts`                                                                                                                                                                                                                         | line 269: `public, max-age=31536000, immutable` when the revision is in the URL (the README's "cached for a year" sentence)                                                                                                                                                                                                |
| `ls docs/readme/brand apps/studio/public/brand apps/studio/public/og`                                                                                                                                                                                                        | absent at the start of the stage; at its end the two lockup PNGs, the eight twins and the card exist (B1's days 2 and 3, landed 10:37 to 10:39), read back with sharp: the lockups 832 by 580 with grounds `#070707` and `#ffffff`, the hero twins 1600 by 900 with two colours each, the card 1200 by 630 at 29,818 bytes |

## 3. The acceptance of MILESTONES-4 B5, as run

```
node_modules/.bin/prettier --check README.md docs/README.md docs/brand.md docs/performance.md docs/hosting.md docs/native.md   # clean
grep -nP '\x{2014}|!|Built on Rust' README.md docs/brand.md docs/performance.md docs/hosting.md docs/native.md   # empty
node -e "for (const f of ['README.md','docs/brand.md','docs/performance.md']) { ... existsSync ... }"   # exit 0
```

Plus: every number in the README's "Why it is fast" names its source and date (section 1.3 R4 has
the list); the README's first sentence is `SITE.description` (section 1.2 item 4); the `<picture>`
on GitHub's grounds is the verifier's screenshot (the two files exist and their grounds were read
back, section 1.2 item 1).

## 4. The ship step checklist for these documents

Written on the merge 1 tree; each line flips to the present tense, or its number is replaced, only
when the named change is on the tree the round ships from (0.26). B5 re-reads `b1.md` to `b4.md`
and BUILD-STATUS-4 at the fixer round and edits these files alone:

- `README.md` "Why it is fast": the `/openapi.json` placeholder sentence (B4's contracts routes,
  3.11); "The hosted studio runs the TypeScript stages today" (only if `hosted-smoke.mjs` reports
  `native` on the preview, 0.38); the preload sentence ("are round four changes", B3's 0.39 and
  0.16); the Blob thumbnail cache sentence (B4's 0.31); the closing sentence's numbers (the
  verifier's day 6 run replaces the day 0 run); the `decks->edit` numbers of the preload row (the
  same run); the check step sentence if a step is renamed.
- `README.md` "Home and files": add the app bar's word as the link to `/home` once B3 mounts
  `AppBarBrand` (b1.md R5).
- `docs/performance.md` section 2 (every "after" row's state), section 3 (the today columns stay
  the day 0 numbers; the verifier's day 6 and production tables are VERIFICATION-4's), section 5
  (the diet rows' states), section 6 (the verifier's requests as the integrator settles them).
- `docs/hosting.md` section 12 (the state sentences of 12.1 to 12.5); section 8's `/openapi.json`
  item once the bundled files serve on production.
- `docs/native.md` "What runs where" (the hosted row and the worker row), "Round four" (the glibc
  record, the nested `.gitignore` line, `BUILD-RECORD.json`'s run id) and "Open items".
- `docs/brand.md` sections 3, 5, 6, 9 and 11 (the state lines of the wordmarks, the card and the
  twins, the app chrome mounts, the banner) and section 15 once the ship step ran the commands.
- `docs/README.md` and the README index: the `VERIFICATION-4.md` row when the verifier writes it.

## 5. Next

The fixer round: the checklist of section 4 against the merge 2 tree and VERIFICATION-4's misses
in these files; the counts against a re-run `facts.json` (R2); the state lines of `docs/brand.md`
once `b1.md`'s day 2 and day 3 sections record B1's own numbers for the twins, the card and the
previews.
