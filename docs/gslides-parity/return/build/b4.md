# The return round, B4: the matrix and the drivers

Notes of builder B4 of the return round (docs/RETURN.md section 5, the B4 rows of section 6, section 7's data module), in the shape of the focus round's lane notes (`docs/gslides-parity/focus/build/b4.md`). The binding specification is `docs/RETURN.md`; `docs/FOCUS.md` section 6 and `AMENDMENTS.md` A1 stand under it; the return audits under `docs/gslides-parity/return/audit-*.md` and `return-drive.md` are the evidence the rows cite. Section numbers refer to `docs/RETURN.md` unless prefixed FOCUS. Nothing here ran a git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build`; no scratch deck was made on a deployment. Day 1 of the round, 2026-09-18 (the runs cross midnight UTC into 2026-09-19). Dev server port 4404 (`vite dev`, the tmp store under this lane's scratch folder, the memory channel, `TURBOSLIDE_LOCAL_OPEN=1`), stopped before the report. Scratch files (the run logs, the walk JSON and shot folders, the gate output, the lint reports, the edit scripts) live in this session's scratch folder outside the repository; their paths are in the run ledger.

## Return round

### 0. Owns

Section 6's list for B4, read against the lane prompt (section 6 binding where the two differ):

- `docs/gslides-parity/focus/core-matrix.json`; `scripts/probes/core-matrix.mjs`, `core-matrix.d.mts`, `core-matrix.test.mjs`; `scripts/probes/core-gate.mjs`, `core-gate.test.mjs`; `scripts/probes/core-walk/index.mjs`, `toolkit.mjs`, `areas/*` (new `tables.mjs`, `charts.mjs`, `diagrams.mjs`, `wordart.mjs`, `formatting.mjs`, `chrome.mjs`, `view.mjs`, `inbox.mjs`; added rows in `shapes.mjs`, `lines.mjs`, `text.mjs`, `arrange.mjs`, `decks.mjs`, `slides.mjs`, `help.mjs`, `share.mjs`); `apps/studio/e2e/core/export.spec.ts`, `decks.spec.ts`, `share.spec.ts` and the new `documents.spec.ts` (`lib.ts` and `matrix.ts` unchanged: the helpers they carry were enough); `docs/gslides-parity/focus/render-focus.mjs`; `docs/readme/what-works.mjs` (unchanged) and `what-works-data.mjs`; `README.md`'s rendered block; `docs/gslides-parity/focus/manual-checklist.md` (unchanged, see 2).
- Kept on the tree and not touched: the stream fix round two's uncommitted fixes (`room-client.ts`, `room.ts`, `share.spec.ts`'s fix-round rows, `images.spec.ts`, the presence files, `comments-store.ts`, `InlineText.tsx`, the render worker queue, the mcp test budget, `.prettierignore`, `versions-by-author.spec.ts`). `share.spec.ts` and `images.spec.ts` were edited by this lane only where the round's rows go; the fix-round rows stand as they were. The three flaky rows the fix round notes name (`collab.presence-chips`, `images.insert.upload-while-pending`, `comments.reaches-second-browser`) keep their drivers and their `today` word as the notes left them.
- Not edited, requested below: `docs/FOCUS.md` (the integrator renders it; deviation 1), `docs/gslides-parity/focus/focus-body.md` (the integrator's), every product file (B1, B2, B3, B5, B7).

### 1. What landed

#### 1.1 The matrix (section 5)

`core-matrix.json` grows from 399 to 565 rows: the 166 rows of section 5's table, generated from the table's text by a scratch script (each row's id, feature, interaction, driver, today, evidence, severity and parks as the table gives them, the `note` and `setup` fields where the table's interaction names a setup write) and validated by `node scripts/probes/core-matrix.mjs` (565 rows; 446 `probe --core` rows; spec rows decks 35, share 22, export 20, documents 1, plus the unchanged slides, images, present and surface counts; 28 rows carry `parks`). The `$comment` gains the two sentences section 5 names (rows added this round carry `today` as measured on 2026-09-18; the `parks` field of rule 2). The two edited rows: `shapes.insert.rounded-click` carries "draws as an svg rect with rx 8" in its evidence, `present.slideshow.from-beginning` reads works (audit-chrome row 14). The view rows carry the feature `view` and the inbox rows `inbox`, as the renamed draft says. The counts by area after the change: tables 25, charts 22, diagrams 7, wordart 4, formatting 24, chrome 15, view 11, inbox 3, arrange 77 (20 added), text 81 (9 added), decks 52 (7 added), shapes 23 (3), lines 11 (6), export 24 (4), help 6 (2), versions 7 (2), collab 6 (1), slides 79 (1).

`core-matrix.mjs`: `CORE_FEATURES` gains `tables`, `charts`, `diagrams`, `wordart`, `formatting`, `chrome`, `view`, `inbox`; `UNPARKABLE_FEATURES` is the thirteen of rule 2 (`UNPARKABLE_FEATURE = 'surface'` stays exported for the callers that read it); `isParkable(feature)`; `ROW_KEYS` gains `parks`, an array of data-control ids validated against the string literals of `menus/model.ts`, `menus/toolbar-tails.ts` and `TitleRow.tsx` (`CONTROL_SOURCE_PATHS`, `isKnownControl`), so a row cannot park a control the product does not draw; `parkedFeaturesOf(results, rows)` returns `{ parked, parkedRows: [{ id, parks, result }], blocking: [{ id, feature, result }], red }`, where a failed row of a parkable feature parks the feature, a failed row with `parks` parks its controls alone (the feature stays), and a failed row of an unparkable feature blocks; `readParkedList` reads `parkedFeatures` and `parkedRows` from the ship list; `checkParkedList` and `shipVerdict(results, parked, rows)` compare both against the run. `CORE_SPEC_DRIVERS` gains `core/documents.spec.ts`. The CLI prints the parks count and the unparkable features. `core-matrix.d.mts` carries the types (`CoreFeature`, `CoreSpecDriver`, `CoreRow.parks`, `ParkedRow`, `ParkedList`). `core-matrix.test.mjs`: the parks validation, `parkedRows`, the unparkable features (a failed text row blocks where it parked `text` before; the test's expectations follow rule 2).

`core-gate.mjs` and `core-walk/index.mjs` pass the rows to `shipVerdict`, default the parked list to `{ commit: null, parkedFeatures: [], parkedRows: [] }`, print `wouldParkRows` in the summary and name the parked rows in the verdict line. `core-gate.test.mjs` tests a blocking decks row, a parking tables row and a parks row.

#### 1.2 The core walk (`scripts/probes/core-walk/`)

`toolkit.mjs` gains:

- The Advanced tools switch as a setup: `t.advancedOn()`, `t.setAdvanced(on)` (Tools > Advanced tools through the menu, polled on `settings.advancedTools`), `t.menuRowPresent(menuId, ...rowIds)`, `t.reachRow(menuId, ...rowIds)` returning `{ present, switched }` (the row in the default view first; the switch turned on only when the row is absent, recorded on `t.deck.advanced`), `t.reachSetup(label, menuId, ...rowIds)` (the same as a setup step, `SetupFailed` when the row is absent both ways) and `t.advancedBack(why)` (the switch back off at the end of an area that turned it on). Every returning row is driven in the default view when the tree draws it there and with the switch on when the row is still parked, and the observation says which (RETURN.md rule 1: the same row measures the return once B1's flag changes land).
- The appearance as the product sets it: `t.pickAppearance('light' | 'dark')` opens Slide > Change theme when the panel is closed, clicks `themes.gt.<appearance>`, polls the deck's `theme` and the root's `data-theme`, then polls the sheet's ground luminance to the appearance (the paint follows the state a moment later; the first charts run read a dark ground right after the light tile); `t.appearance()`, `t.sheetGround()`, `t.contrastOf(a, b)` (WCAG contrast of two computed colours, alpha over the ground), `t.closeThemes()`.
- The pixel reader of 4.4: `decodePng` (zlib inflate of a Playwright PNG, 8 bit RGB and RGBA), `runsAlongRow`, `runsAlongColumn`, `thinRuns`, `segmentsAlong`, `boundaryPoints(page)` (the eleven seams' probe points from the DOM boxes of the title row, the toolbar, the filmstrip, the notes slot, the stage, the split button and the right cluster), `SEAM_NAMES`, `seamsFromShot(img, points, scale)` (each seam's thickness in px and colour as drawn, so a doubled or a missing line shows) and `splitSeamFromShot(img, presentRect, arrowRect, cropOrigin)` (the split button's box, divider and notch read from pixels), the port of `audit-chrome.mjs`'s readers; the helpers `t.shotPixels(clip)`, `t.seamsFromShot`, `t.splitSeamFromShot`, `t.runsAlongRow`, `t.runsAlongColumn`, `t.boundaryPoints`, `t.contrastRgb`, `t.hexOf`.

The areas, one module per new feature and the added rows in the existing ones (the `IDS` array at the head of each module lists its rows; `core-walk.test.mjs` pins the one to one coverage of every `probe --core` row):

- `tables.mjs` (23 rows): Insert > Table's picker read for its size words, cell sessions by double click, Tab, Shift+Tab and Tab on the last cell, Format > Table with a session and with the table selected, the cell right click menu's sixteen rows and its insert and delete rows, the column widths after an insert, the column seam handle (not driven while the tree draws no seam handle; the reason is in the row), Align from the menu and the tail, the eight handles' resize, the tail's Fill, Border color, weight and dash, the cell range (Shift click, then a drag inside the session; the route is recorded), Merge and Unmerge from the menu and the tail, Distribute rows and columns (the stored sizes and the drawn boxes), the light appearance with the show, the show, the reload. The typed text lives in cell 1,1 ("Q1 revenue") and the insert and delete rows work on cells that do not hold it; the setups restore `/columns` and `/rows` through the window API when an undo does not.
- `charts.mjs` (19 rows): the four Insert > Chart kinds with their svg facts (`data-chart`, series rects, slices, legend, axis lines, texts), the chart tail, the resize, Format options' Chart type segments read for clipping and each kind's redraw, Chart type from the tail and from the right click menu, Add series and Add category, a cell edit in the data grid, Edit data from the tail and the menu, the Legend list (a position other than the stored one and other than right, where an unset legend already draws), the Number format list, the right click rows, the light appearance with the show, the show from the slide before, the reload. The bar count the show must draw is the stored series times categories.
- `diagrams.mjs` (7), `wordart.mjs` (3), `formatting.mjs` (24: superscript and subscript by chord and by the menu on a word, Italic from the menu on a word, the right click rows on a selection, the three Capitalizations, Justify from the menu, the chord and the tail, Add space before and after and their removal, Custom spacing, the 1.15 value, the highlight, Paint format by the button and the chords, Clear formatting, the Themes panel's appearance tiles and toolbar button and its hidden Import theme, persistence across a reload, the show and the viewer), `view.mjs` (10), `inbox.mjs` (2), `chrome.mjs` (15: the split button's box, hover, click, chevron menu alignment, Enter on each half, Space, ArrowDown, Tab order, ARIA and collapse at 900 px read from a 1x screenshot through `splitSeamFromShot` and the DOM; the eleven seams through `seamsFromShot` in both appearances at 1440 and at 900 with the Comments panel open; the toolbar dividers; the right cluster's gaps and heights; the comments glyph as a toggle).
- Added rows: `shapes.mjs` (Text color from the tail on the rectangle, Enter opens the label, Format > Borders & lines), `lines.mjs` (the elbow and curved connectors between two rectangles placed as a setup, the end re-attached to a third, the Arrow's head, Line start and Line end from the menu), `text.mjs` (the seven `text.title.*` rows on the cover title, the typed size with one undo, a Format options field with one undo), `arrange.mjs` (distribute, rotate and flips, group chords, menu and context rows, the ruler, guides from the ruler and the menu, their drag, context and clear, snap to guides and grid, Select none), `decks.mjs` (the name following the heading, File > Open with its search, Import slides from a copy of the deck made as a setup write and removed after, File > Details), `slides.mjs` (Slide numbers), `help.mjs` (Check slides), `share.mjs` (Show changes toggle and marks).

`index.mjs` runs the areas in the order decks, slides, text, formatting, images, arrange, shapes, lines, tables, charts, diagrams, wordart, share, export, help, view, inbox, chrome, surface (the chrome rows at the end so the split button and the seams are read on a deck with every kind of object present).

#### 1.3 The specs (`apps/studio/e2e/core/`)

- `export.spec.ts`: the owner deck gains a fourth slide with a table, a chart, a word art, three rectangles and an elbow and a curved connector (`makeOwner`, `Owner.docsSlide`); helpers `pdfText`, `pptxSlideXml`, `typeTableCells`, `slideJsonOf`, `reachDownloadRow`, `pictureRow`, `objectsOfSlide`; tests `export.zip.bundle`, `export.html.web-page`, `export.jpg.current-slide`, `export.png.current-slide`, `tables.export.pdf`, `tables.export.pptx-editable`, `charts.export.pdf`, `charts.export.pptx-native`, `wordart.export.pdf`, `lines.connector.export-pptx`, each titled by `coreTitle(id)` and in `coverage()`. The download quota (five a day per anonymous identity) is shared by `withBudget` as before.
- `decks.spec.ts`: `reachMenuRow` and `switchOff` (the Advanced tools switch as in the walk), `bundleBytes`; tests `decks.file.template-gallery`, `decks.file.open-upload-bundle`, `decks.file.import-slides-bundle`, `help.improve-link`.
- `share.spec.ts`: `reachMenuRow`, `switchOff`, `invokeOn`; tests `collab.roster.go-to-slide`, `view.live-pointers.second-browser` (`.ts-remote-pointer-group` in the first browser after the second moves its pointer), `inbox.notification-arrives` (a comment with a mention written from the second browser through `comment.add`; `data-unread` on `title.inbox` in the first).
- `documents.spec.ts` (new): `charts.data.paste-rows`, a clipboard paste of tab separated rows dispatched on the data grid's cell; the chart's categories, series and bars follow.

#### 1.4 The README and the focus render (section 7)

`what-works-data.mjs` carries the paragraphs of section 7 for tables, charts, diagrams, word art, formatting, the chrome, the view rows and the inbox (deviation 3 names the split of the diagrams and word art paragraph); `render-focus.mjs` renders the new features into FOCUS.md 6.3 and its README map. `README.md`'s block was rendered by `node docs/readme/what-works.mjs` and formatted (`what-works: README.md is current`).

### 2. Deviations and readings recorded

1. `docs/FOCUS.md` is not rendered by this lane. `render-focus.mjs` rebuilds FOCUS.md from `focus-body.md` and the matrix; the tree's FOCUS.md carries a hand edit the body does not (the "The Version history panel" row of the switch table, line 135, written by the integrator's fixer in stream fix round two), so a render here would drop it. Request 1 asks the integrator to move the row into `focus-body.md` and render. Until then `docs/readme/what-works.test.mjs`'s "verbatim in FOCUS.md" case fails (the one red test of `vitest run --project scripts`), as expected.
2. `manual-checklist.md` is unchanged: no row of section 5 is manual, and the two manual rows of the matrix (`text.clipboard.paste-without-formatting`, `export.print.print-button`) keep their checklist entries.
3. Section 7's "Diagrams and word art" paragraph is two entries in `what-works-data.mjs` (`diagrams`, `wordart`), each shown while its own feature passes, so a red word art row does not hide the diagrams sentence; the rendered text is the same two sentences. The view and inbox rows have their own paragraphs in the data module, as section 7's later paragraphs give them.
4. `format.chartType` and `format.editData` are `contextOnly` on the tree, so `charts.type.menu` and `charts.data.menu-edit-data` are driven from the right click menu, which the row's interaction names; the Format menu does not list them and the observation says so.
5. `slides.numbers.apply` drives On, Skip title slides and Apply; the tree's dialog carries no Apply to selected while B5's request 5 is open, and the row reads the title slide's counter after Apply.
6. The PDF rows are the specs' (`pdfText` reads the file's text); the walk does not read a PDF.
7. `core-matrix.test.mjs`'s expectations for a failed row of an unparkable feature changed with rule 2: the text row that parked `text` in the focus round now blocks. The cases are named in the test.
8. `decks.file.import-slides-deck` makes its copy through `deck.copy` with a name unique to the walk's deck (`Import source <deck id>`) and removes the copy and the imported slides after the row; the first run collided on a fixed name left in the tmp store by an earlier run, and the leftover copy was removed through the window API before the next run.
9. The dev server runs `vite.no-watch.config.ts` (the lanes' shared checkout convention), which transforms each module once at its first request, so the server serves the tree as it stood when each module was first asked for and a later edit by another lane is not served until a restart. The four walks before 02:00Z (decks-1, docs-1, chrome-1, docs-2 in section 6) measured the tree served from 00:54Z: B1's chrome and menu model changes were in it; B2's `auto-title.ts`, `controller.tsx`, `editor-shell.ts`, `InlineText.tsx` and `ToolbarTail.tsx` (01:05Z to 01:50Z) and B5's `chart.css`, `table.ts`, `Editor.tsx`, `table-seam.ts` and `SlideNumbers.tsx` (01:05Z to 01:32Z) were not, which is why those runs read the audits' defects (Tab ending the cell session, Column clipped, the name "Pipeline", the whole run marked from the Format menu) although the fixes were on disk. Found by asking the server for `Editor.tsx` and `chart.css` and reading the transforms; the server was restarted at 02:03Z and every run from the gate on measures the tree as it stood then. The runs before the restart stand in the ledger as measurements of the earlier tree; the driver faults they found were fixed before the gate.
10. `surface.parked-shortcut-unbound` and `surface.advanced.off-by-default` (`apps/studio/e2e/core/surface.spec.ts`, a B4 file by section 6's table) named rows that returned this round: Insert > Table, Chart, Diagram and Word art, Arrange > Group and Distribute, File > Details, Superscript, the Paint format and Theme buttons, the Group chord and the Justify chord. With B1's flags off they are in the default view, so both rows read red on the tree of 02:03Z as stale expectations, and both are unparkable. The spec's lists now name rows that keep the switch (RETURN.md section 8 and questions 6 and 7: `view.gridView`, `view.showSections`, `insert.specialCharacters`, `tools.spelling`, `tools.activityDashboard`, `tools.advanced`; no toolbar button; the right click rows `format.dropShadow` and `format.altText`; the palette's `view.gridView`) and the chord row drives Cmd+Option+Y (Alt text, `format.altText`, `advanced: true`, which opens Format options) on a selected block; the matrix row's interaction and note say so. Section 6 records the run.
11. The window API's `describe().state.settings` can lag a menu row's write by seconds (the chrome run read Advanced tools on for 5 s after the row turned it off while the title row already drew the default view), so `t.setAdvanced` reads the menu row's `aria-checked` when the state does not follow; the sheet draws a write a moment after the store holds it (the sheet renders asynchronously), and the drawn reads that judge a write (`tables.distribute.rows-columns`, the cell style after a tail write) poll for the change they expect instead of reading once; a cell session freezes the sheet's markup (`Editor.tsx`), so `tables.cell.fill-border-tail` ends the session before it reads the drawn cell.

### 3. Requests

Each request names the row and the observation of the full gate run on the tree of 02:03Z (section 6, run 5), on the memory tier, after the driver faults of the earlier runs were fixed. A row named here is a measurement of that tree, not of its driver; where a driver fault was found after the gate it is named in section 6 with its fix and the follow-up run.

1. The integrator (`docs/FOCUS.md`, `docs/gslides-parity/focus/focus-body.md`): move the hand edited "The Version history panel" row of the switch table into `focus-body.md` and run `node docs/gslides-parity/focus/render-focus.mjs`, so FOCUS.md 6.3 carries the new features' counts and section 7 carries the README text verbatim (`what-works.test.mjs`).
2. The integrator (the slides area, no lane this round): four slides rows that passed on the focus ship read red on this tree, each an undo that does not put the slides back: `slides.delete.undo-snackbar` (the snackbar's Undo restores the slide at the front of the deck, not at its place: `title-2-2-2` first after the undo), `slides.delete.two-selected-key-undo` (the same after two deletes and a reload), `slides.reorder.menu-to-end-undo` (Cmd+Z after Move slide to end leaves it at the end), `slides.reorder.undo-drag` (Cmd+Z after a drag leaves the dragged order). The slides feature is unparkable, so these block the ship as they stand. B2's `controller.tsx` `commit` changed this round (`convertThenCommit`); whether the slide order's undo went through it is a question for B2 and the integrator, not a finding of this lane.
3. B2 (the text rows): `text.title.bold-menu`, `text.title.format-options-marks` (the write lands, revision +1, the slide converts to a canvas, and the stored typography reads `{ weight: 500 }`, the heading's drawn weight already, so nothing changes on the sheet), `text.title.bold-cmd-b` (Cmd+B on the selected cover title writes nothing: revision 114 to 114), `text.fontsize.type-one-undo` (the size field is read only with a text box selected by one click, `ToolbarTail.tsx` `readOnly={!enabled}`: the typed 28 does not land and Enter writes the shown 24). `text.title.one-click-tail`, `.align-menu`, `.size-menu`, `.apply-layout-after-format`, `text.format-options.field-one-undo`, `decks.name.follows-heading` and every `formatting.*` row but the Paint format chords pass on this tree.
4. B3 (arrange and lines): `arrange.group.chords` and `arrange.group.menu-regroup` (Ungroup leaves nothing selected: the enabled states read group false, ungroup false, regroup false right after it, and the chip is gone; Regroup then works), `arrange.context.rotate-distribute` (a line's right click menu lists no Rotate; Distribute > Horizontally from the right click menu leaves the gaps at 301 and 167), `arrange.selection-colour.light` (the chip reads rgb(61, 134, 240) on the light appearance), `formatting.paint-format.chords` (Cmd+Option+C and V copy nothing while the button copies: `packages/viewer/src/keys.ts` 189 to 193 still gates `paintCopy` and `paintPaste` on `ctx.advanced`, the flag `toolbar.paintFormat` lost this round), `lines.connector.re-end` is re-measured in the follow-up run (its first read was a driver fault, section 6). `chrome.split.tab-order` passed on this tree (the Tab branch of `Editor.tsx`).
5. B5 (tables and the Slide numbers dialog): `tables.cells.merge-unmerge` and `tables.tail.merge-unmerge-buttons` (no cell range: a Shift click and a drag across two cells both leave the chip at Table and Merge cells disabled in the menu and on the tail; both rows carry `parks`, so the merge controls park and the table returns), `slides.numbers.apply` (Skip title slides then Apply leaves the title slide numbered "04 / 05"; the layout slide keeps "02 / 05"; Apply to selected is offered). From the follow-up runs (section 6, runs 7 and 9), `tables.menu.format-table-rows`: with the session opened in cell 0,1, Format > Table > Delete row removed row 1, the cell edited before; with the session in cell 1,0, Delete column removed column 1, the same cell's column, so the plan reads the kept cell pointer one session behind the open one (B2's `withKeptCell`, B5's plans); and after Insert row above, Insert column left, Delete row and Delete column with a session open, one Cmd+Z left the new count and a second restored it, on all four rows and both runs. `tables.cell.tab-from-written`, `.shift-tab`, `.tab-last-appends-row`, `tables.menu.format-table-with-session`, `.format-table-selected`, `tables.column.insert-keeps-widths`, `tables.column.resize-seam` (the seam handle `handle.table.column.0` drags 320,320,320 to 400,240,320 with the readout "400 × 320" and Cmd+Z restores), `tables.cell.align-toolbar`, `tables.cell.fill-border-tail`, `tables.select.resize`, every `charts.*` row driven by the walk and every `diagrams.*` row pass on this tree; `charts.type.panel-legible` reads Column inside its segment.
6. B1 (the chrome and the appearance): every `chrome.*` row passes on this tree but `chrome.separators.toolbar-dividers`, whose first read was a driver fault (section 6). `inbox.bell-panel-toggle` (the second click on the bell leaves the panel open, aria-pressed true; RETURN.md 2.16 asks for a toggle). `collab.roster.go-to-slide` (section 6, run 8): the RosterMenu opens from the `+N` button alone (`PresenceSlot.tsx` `presence.more`), and 4.3's `.ts-presence-more.is-empty { opacity: 0; pointer-events: none }` at `more === 0` hides it while the chips fit, so with one collaborator present the roster, its Go to slide rows and the own row have no opener a person can reach; the chip's own click is Go to slide and jumps. Question 9's default hid the empty box; the roster needs an opener at one collaborator (the button drawn from the first other person, or the chip's right click). The light appearance rows read the tile's write in `deck.info` while the editor's own state and the sheet's paint stayed dark on the table slide (`tables.light-appearance`: appearance `{ theme: dark, deck: light, state: dark }`, ground rgb(7, 7, 7)) and followed at once on the chart and word art slides; the pick now judges the editor's view and tries the tile twice (section 6), so the follow-up run says whether the sheet follows the tile on the second try.
7. B7 (the inbox and presence): `inbox.notification-arrives` (the bell's count reached 1 within the bound; the panel's row is re-read in the follow-up as a mention sentence, the first read looked for the comment's words), `view.live-pointers.second-browser` (the second browser's pointer was not drawn on the first within 2 s after six moves over the sheet with Show my pointer on in the second and Show collaborator pointers on in the first; the follow-up run, section 6 run 8, polled 10 s with the moves repeated and read no `.ts-remote-pointer-group` on the first browser), `versions.show-changes-marks` (section 6, run 9: Show changes on and the older version picked, a heading edit and an added box after it, 0 change marks; the toggle itself works; the panel is the integrator's fixer's from stream fix round two), `collab.roster.go-to-slide` (its first run hung on a click on the empty `+N` button, which B1 draws at opacity 0 with pointer-events none; the driver clicks the chip then, section 6).
8. B7 or the integrator (the export owner on localhost): `export.pdf.file` failed at the owner deck's setup, a `block.insert` this lane wrote with the shape names `elbowConnector` and `curvedConnector`, which the schema does not know (`bentConnector3`, `curvedConnector3`); fixed and re-run in the follow-up. The rows that depend on it (`export.pdf.*`, `export.pptx.*`, `export.zip.bundle`, `export.html.web-page`, `export.jpg.current-slide`, `export.png.current-slide`, `tables.export.*`, `charts.export.*`, `wordart.export.pdf`, `lines.connector.export-pptx`, `shapes.export.*`, `images.export.pdf-with-picture`) read not driven in run 5 for that reason alone.
9. The integrator (`packages/lint/src/chrome.ts`, with B1): `turboslide lint --chrome` on the editor reads 18 of 18 audits with findings on the tree of 02:03Z, all five per audit on the split button (section 5, `dev-2`): the wrapper `ts-title-slideshow`'s four 1 px borders read `rgb(7, 7, 7)` in light and `rgb(242, 242, 240)` in dark with the role `ink`, and the chevron half `pt-ib.is-solid`'s left border reads `rgba(7, 7, 7, 0.26)` in light and `rgba(255, 255, 255, 0.26)` in dark with the role `none`. The lint's role table needs the two tokens of 4.1 (`--pt-ink` as a border on the ink ground for the wrapper; `--pt-hair-on-ink` for the divider) or an `--allow` for the two selectors; the viewer at `/deck/gt-brand` reads 24 audits with 0 findings on the same server.

### 4. Row ids this lane covers

Every row of the matrix has a driver: the 446 `probe --core` rows through the area modules (`core-walk.test.mjs` pins the one to one coverage) and the 119 spec rows through the eight specs (`coverage()` in each file fails a forgotten row). The 166 rows added this round, by area: shapes `shapes.text.colour-toolbar`, `shapes.text.enter-opens-label`, `shapes.borders-lines.menu`; lines `lines.connector.elbow`, `lines.connector.curved`, `lines.connector.re-end`, `lines.insert.arrow-head`, `lines.tail.line-start-end-menu`, `lines.connector.export-pptx`; tables `tables.insert.grid`, `tables.cell.double-click-type`, `tables.cell.tab-from-written`, `tables.cell.tab-from-empty`, `tables.cell.shift-tab`, `tables.cell.tab-last-appends-row`, `tables.menu.format-table-with-session`, `tables.menu.format-table-selected`, `tables.menu.format-table-rows`, `tables.context.rows`, `tables.context.insert-delete`, `tables.column.insert-keeps-widths`, `tables.column.resize-seam`, `tables.cell.align-menu`, `tables.cell.align-toolbar`, `tables.select.resize`, `tables.cell.fill-border-tail`, `tables.cells.merge-unmerge`, `tables.tail.merge-unmerge-buttons`, `tables.distribute.rows-columns`, `tables.light-appearance`, `tables.present`, `tables.export.pdf`, `tables.export.pptx-editable`, `tables.reload`; charts `charts.insert.bar`, `charts.insert.column`, `charts.insert.line`, `charts.insert.pie`, `charts.select.tail`, `charts.resize`, `charts.type.panel-legible`, `charts.type.toolbar`, `charts.type.menu`, `charts.data.add-series-category`, `charts.data.edit-cell`, `charts.data.toolbar-edit-data`, `charts.data.menu-edit-data`, `charts.legend.toolbar`, `charts.number-format.toolbar`, `charts.data.paste-rows`, `charts.context`, `charts.light-appearance`, `charts.present`, `charts.export.pdf`, `charts.export.pptx-native`, `charts.reload`; diagrams `diagrams.panel`, `diagrams.insert.group`, `diagrams.select-move`, `diagrams.edit-label`, `diagrams.light-appearance`, `diagrams.present`, `diagrams.reload`; wordart `wordart.insert`, `wordart.edit`, `wordart.light-appearance`, `wordart.export.pdf`; formatting `formatting.superscript.chord`, `formatting.subscript.chord`, `formatting.superscript.menu-word`, `formatting.subscript.menu-word`, `formatting.italic.menu-word`, `formatting.context.selection-rows`, `formatting.capitalization.upper`, `formatting.capitalization.lower`, `formatting.capitalization.title`, `formatting.align.justified-menu`, `formatting.align.justified-chord`, `formatting.align.toolbar-justify`, `formatting.spacing.add-before-remove`, `formatting.spacing.add-after`, `formatting.spacing.custom-dialog`, `formatting.spacing.1-15-value`, `formatting.highlight.word`, `formatting.paint-format.button`, `formatting.paint-format.chords`, `formatting.clear.inline-marks`, `formatting.theme.panel-appearance`, `formatting.theme.toolbar-button`, `formatting.theme.import-hidden`, `formatting.persistence`; text `text.title.one-click-tail`, `text.title.bold-menu`, `text.title.bold-cmd-b`, `text.title.align-menu`, `text.title.size-menu`, `text.title.format-options-marks`, `text.title.apply-layout-after-format`, `text.fontsize.type-one-undo`, `text.format-options.field-one-undo`; arrange `arrange.distribute.horizontal`, `arrange.distribute.vertical`, `arrange.distribute.needs-three`, `arrange.rotate.quarter-turns`, `arrange.rotate.flips-menu`, `arrange.group.chords`, `arrange.group.menu-regroup`, `arrange.group.context-rows`, `arrange.context.rotate-distribute`, `arrange.ruler.show-hide`, `arrange.guides.from-ruler`, `arrange.guides.show-toggle`, `arrange.guides.add-vertical-horizontal`, `arrange.guides.drag`, `arrange.snap.guides-on-off`, `arrange.snap.grid-toggle`, `arrange.snap.grid-effect`, `arrange.guides.context`, `arrange.guides.clear`, `arrange.select-none.menu`; chrome `chrome.split.one-box`, `chrome.split.hover-no-inversion`, `chrome.split.click-show`, `chrome.split.chevron-menu-aligned`, `chrome.split.enter-chevron`, `chrome.split.enter-label`, `chrome.split.space-both`, `chrome.split.arrow-down-label`, `chrome.split.tab-order`, `chrome.split.aria`, `chrome.split.collapse-900`, `chrome.separators.once`, `chrome.separators.toolbar-dividers`, `chrome.cluster.gaps-heights`, `chrome.comments-glyph.toggle`; view `view.appearance.rows`, `view.show-filmstrip`, `view.mode.rows`, `view.mode.viewing-hides-toolbar`, `view.full-screen`, `view.hide-menus-chevron`, `view.live-pointers.toggles`, `view.live-pointers.second-browser`, `view.comments.radios`, `view.comments.show-all-panel`, `view.comments.modes-markers`; decks `decks.name.follows-heading`, `decks.file.template-gallery`, `decks.file.open-list-search`, `decks.file.open-upload-bundle`, `decks.file.import-slides-deck`, `decks.file.import-slides-bundle`, `decks.file.details`; slides `slides.numbers.apply`; versions `versions.show-changes-toggle`, `versions.show-changes-marks`; export `export.zip.bundle`, `export.html.web-page`, `export.jpg.current-slide`, `export.png.current-slide`; help `help.check-slides`, `help.improve-link`; inbox `inbox.bell-panel-toggle`, `inbox.settings-persist`, `inbox.notification-arrives`; collab `collab.roster.go-to-slide`.

### 5. Commands run and their results

Gates that passed on the tree at the report:

- `node_modules/.bin/tsc -b` exit 0 (twice: after the matrix module, the specs and the walk edits, and after the last spec edit).
- `node_modules/.bin/vitest run --project scripts`: 7 files, 46 tests, 45 passed, 1 failed: `docs/readme/what-works.test.mjs` "are the text of docs/FOCUS.md section 7, verbatim", red until the integrator renders FOCUS.md (deviation 1, request 1). `core-matrix.test.mjs` (the parks validation, the unparkable features, `parkedRows`), `core-gate.test.mjs` (blocking, wouldPark, wouldParkRows) and `core-walk.test.mjs` (the one to one coverage of the 446 probe rows) pass.
- `node scripts/probes/core-matrix.mjs`: 565 rows; 446 `probe --core`; spec rows decks 35, slides 1, images 9, present 25, share 22, export 20, surface 6, documents 1; 28 rows carry parks; the unparkable features listed.
- `node docs/readme/what-works.mjs --check`: "README.md is current"; `node_modules/.bin/prettier --check` over every file this lane owns: clean (`share.spec.ts` was formatted once after the row edits).
- `node --check` over every area module and the toolkit after each edit.

`turboslide lint --chrome` (RETURN.md 4.2 item 5, "before and after the fixes"):

- Before, on production (the focus ship, before any return fix): `node apps/cli/bin/turboslide.mjs lint --chrome --url https://turboslide.vercel.app/deck/gt-brand --widths 1440,1280,390 --themes light,dark --json` (2026-09-19T01:28Z): 24 audits over 3 widths and 2 themes, 0 with findings, 0 states unapplied, exit 0. The same on `/edit/gt-brand` (01:30Z): 24 audits, 0 with findings, 18 states unapplied (the default states `list`, `grid`, `book` are the viewer's; a stranger's `/edit` is the viewing mode; exit 2 for the unapplied states, no finding). So the lint did not report the doubled notes divider of 4.2 on production: the answer to the open question there is no, the lint's CSS reconstruction does not see it, and `chrome.separators.once` reads it from the pixels instead. Reports: `b4/lint/production-deck-gt-brand.json`, `production-edit-gt-brand.json` (+ `.err`).
- After, on the dev server at 4404 (the tree of 02:03Z), 2026-09-19T02:55Z: `/deck/gt-brand` with the viewer states `list,grid,book`, 1440, 1280 and 390 in both themes: 24 audits, 0 with findings, 0 states unapplied, exit 0 (`b4/lint/dev-1-deck.json`). `/edit/gt-brand` with `--states editorGrid,editorMenu,editorPanel` stopped at the first state: "state control not found: [data-control="view.gridView"]", exit 2, no report; the grid view row keeps the switch (section 8), so the `editorGrid` state cannot apply in the default view. Run again as `dev-2` with `--states editorMenu,editorPanel` (03:21Z): 18 audits over 3 widths and 2 themes, 18 with findings, 0 states unapplied, exit 1 (`b4/lint/dev-2-edit.json`, the report in `dev-2-edit.err`). Every audit carries the same five findings, the "color" check of the border roles: `color border top|right|bottom|left of ts-title-slideshow: rgb(7, 7, 7) (ink)` in light and `rgb(242, 242, 240) (ink)` in dark, and `color border left of pt-ib.is-solid: rgba(7, 7, 7, 0.26) (none)` in light and `rgba(255, 255, 255, 0.26) (none)` in dark. Those are the split button of 4.1 as B1 built it: the wrapper's 1 px ink border on the ink ground and the chevron half's `--pt-hair-on-ink` divider, the two tokens `tokens.css` gained this round, which the lint's role table (`packages/lint/src/chrome.ts`, the `TURBOSLIDE_CHROME` roles and `CHROME_ALLOW`) does not know; no double, junction, missing or self stacked line is reported. B1's note lists `packages/lint/src/chrome.ts` among the files it did not edit and asks the integrator for it; request 9 below carries the finding lines so the role table can name the two borders. Until it does, `turboslide lint --chrome` on the editor reads red for the split button alone, and the pixel row `chrome.separators.once` reads every seam as one 1 px hair in both appearances (run 5).
- Not run on the preview: `https://turboslide-hkne1i4o2-kl01s-projects.vercel.app` answers an anonymous request with a 302 to Vercel Authentication, and neither the lint command nor `packages/headless` sends a protection bypass header (no such option in `lint-chrome.ts` or `shell.ts`), so the headless audit cannot reach the page. The production and dev server runs stand as the before and after.

### 6. Run ledger

Every run started against a base, with its start time, deck id, counts and JSON path (FOCUS 6.2); no run is left out. A rerun happens only after a driver change named here or with a reason written here. The walks ran as `node scripts/probes/editor-walk-probe.mjs --core --base http://localhost:4404 --only <areas> --json <run>.json --matrix <run>.md --shots <run>-shots` under the checkout's `.turboslide/e2e.lock`; the JSON, the matrix table and the shots of each run are under this lane's scratch folder `b4/runs/`. Counts are rows of the run's areas: passed / failed / not driven.

Before the server restart (deviation 9: the tree served from 00:54Z):

1. `decks-1`, started 2026-09-19T01:23:22Z, 47 s, deck `untitled-20260919-lq82` (trashed and deleted forever by the walk's own rows), areas decks: 16 / 2 / 0, surface 1 / 0 / 0. Red: `decks.name.follows-heading` (the title row read "Pipe" after the first burst and stayed there: RETURN 2.18 on the earlier tree), `decks.file.import-slides-deck` (the driver's `copy.id` read, fixed: the answer carries `deckId`).
2. `docs-1`, started 01:27:02Z, 378 s, deck `untitled-20260919-qahs`, areas decks 16 / 2 / 0, tables 7 / 15 / 1, charts 15 / 4 / 0, diagrams 7 / 0 / 0, wordart 3 / 0 / 0, surface 1 / 0 / 0. Not driven: `tables.column.resize-seam` (no seam handle on the served tree). Red on the product of that tree: `tables.cell.tab-from-written` (Tab ended the session), `tables.menu.format-table-with-session`, `.format-table-selected`, `.format-table-rows` (every Format > Table row disabled), `tables.cell.align-menu` (wrote column 0), `tables.cell.align-toolbar` (the tail swapped to the selection tail), `tables.cells.merge-unmerge`, `tables.tail.merge-unmerge-buttons` (no cell range), `charts.type.panel-legible` (Column 47 px in a 28 px segment). Red on the driver, fixed after: `tables.context.insert-delete` (deleted the row and column holding the typed text; the deletes moved to cells that hold none), `tables.column.insert-keeps-widths` and `tables.distribute.rows-columns` (a setup width leaked from the row before; the setups restore `/columns` and `/rows`), `tables.light-appearance`, `tables.present`, `tables.reload` (they looked for the deleted text), `charts.legend.toolbar` (picked right, where an unset legend already draws), `charts.light-appearance` and `charts.present` (the sheet's paint read before it followed the appearance; the bar count fixed at 3 where the stored series times categories is 8 after the add rows), `decks.file.import-slides-deck` (a fixed copy name colliding with a leftover copy).
3. `chrome-1`, started 01:46:41Z, 297 s, deck `untitled-20260919-ijvn`, areas decks 17 / 1 / 0, formatting 13 / 4 / 7, view 7 / 3 / 0, inbox 1 / 1 / 0, chrome 13 / 2 / 0, surface 1 / 0 / 0. Not driven: the seven formatting rows after the Paint format setup (`formatting.paint-format.button`, `.chords`, `formatting.clear.inline-marks`, `formatting.theme.panel-appearance`, `.toolbar-button`, `.import-hidden`, `formatting.persistence`): the setup wrote `/typography/weight` on a box with no typography object (`RangeError: Nothing to write into`); fixed to one write of the whole object. Red on the product of that tree: `decks.name.follows-heading` ("Pipeline" after both bursts), `formatting.superscript.menu-word`, `.subscript.menu-word`, `formatting.italic.menu-word` (the mark on the whole run from the Format menu; the chord rows and the right click row marked the word alone), `formatting.spacing.1-15-value` (the row labelled 1.15 wrote leading 1.2), `inbox.bell-panel-toggle` (the second click on the bell left the panel open, aria-pressed true), `chrome.split.tab-order` (with the title selected, Tab from the Slideshow half moved the selection to the subtitle instead of the chevron; nothing selected, Tab reached the chevron). Red on the driver, fixed after: `view.full-screen` and `view.hide-menus-chevron` (the bars hid and came back; the driver read `settings.compact`, which does not follow the shell's compact state; it reads `.pt-viewer.is-compact` now), `view.comments.modes-markers` (the panel's threads are `panel.comments.thread.<id>`; the markers and cards read right), `chrome.separators.once` (every 1440 seam read one 1 px hair run in both appearances; the 900 filmstrip to bottom bar probe point crossed a card, moved into the filmstrip's gutter; the light pass read one seam fewer because the 900 pass closes the Comments panel, reopened per pass now). Measured green in that run and worth the record: the split button as one 133 by 32 box with 8 px corners, halves with none, a 1 px divider at 1.83:1 (dark) and 2.17:1 (light), no paper notch; the hover on `--pt-plate-on-ink` with no inversion; the menu at x 1122 to 1342 against the control's right edge 1342.4; Enter, Space and ArrowDown as 4.1 says; the ARIA; 58 px at 900; the two toolbar dividers 1 by 20 in `--pt-hair` with 8 px of air; the cluster's gaps 8, 8, 8, heights 32, corners 8 px, the +N box hidden, the inbox slot 0 px; the comments glyph as a toggle.
4. `docs-2`, started 01:51:40Z, 437 s, deck `untitled-20260919-03bi`, areas decks 16 / 1 / 0, tables 12 / 10 / 1, charts 17 / 2 / 0, diagrams 6 / 1 / 0, wordart 2 / 1 / 0. The tables rows fixed in the driver after docs-1 passed (`tables.context.insert-delete`, `tables.column.insert-keeps-widths`, `tables.light-appearance`, `tables.present`, `tables.reload`); `charts.legend.toolbar` and `charts.present` passed. Red on the product of that tree: the same eight table rows as docs-1 and `charts.type.panel-legible`. Red on the driver, fixed after: `tables.cell.fill-border-tail` (the writes landed, `cells [{row 1, column 1, fill amber}]` and the border, and the drawn cell read unchanged because the cell session freezes the sheet's markup; the driver ends the session before it reads), `tables.distribute.rows-columns` (the drawn boxes read one action behind the store; the reads poll now), `charts.light-appearance`, `diagrams.light-appearance`, `wordart.light-appearance` (the light tile's click left the deck dark on those slides while the table slide's took at once; the pick tries twice and reads `deck.info` as well as the state).

After the server restart (the tree of 02:03Z, `restart-4404.sh`: the same store, the no-watch config, fresh secrets):

5. `full-1`, the gate: `node scripts/probes/core-gate.mjs --base http://localhost:4404 --out b4/gate/full-1 --matrix b4/gate/full-1-summary.json --lock b4/gate/full-1-lock`, under the checkout's e2e lock, started 2026-09-19T02:03:48Z, 3117 s, commit 7d19326 with the tree's uncommitted work, memory tier. 565 rows: 496 passed, 49 failed, 20 not driven (1 manual), 0 no step; verdict failed; exit 1. The probe: 446 rows, 404 passed, 41 failed, 1 not driven, 477 steps, 2098 s, 6 console errors, deck `untitled-20260919-ymbe` (trashed and deleted forever by the walk's own rows). The specs: 119 rows through eight files, 92 passed, 8 failed, 19 skipped as dependents. By area (passed / failed / not driven): decks 50 / 2 / 0, slides 74 / 5 / 0, text 76 / 4 / 1, images 41 / 0 / 1, arrange 64 / 13 / 0, shapes 18 / 3 / 2, lines 9 / 1 / 1, tables 14 / 9 / 2, charts 20 / 0 / 2, diagrams 7 / 0 / 0, wordart 3 / 0 / 1, formatting 23 / 1 / 0, chrome 14 / 1 / 0, view 10 / 1 / 0, inbox 1 / 2 / 0, present 18, comments 7, share 13, collab 5 / 1, versions 5 / 2, export 13 / 1 / 10, help 5 / 1, surface 6 / 2. This round's 166 rows: 115 passed, 41 failed, 10 not driven. The verdict's parked features on this run: shapes, lines, tables, charts, wordart, formatting, inbox; parked rows: the three tables rows with `parks` and the view, decks, slides, versions, export, help and collab rows with `parks` that read red or not driven; blocking: the four slides undo rows, `arrange.selection-colour.light`, the two surface rows, `export.pdf.file` and its dependents, the text title rows. Files: `b4/gate/full-1/core-walk.json`, `core-walk.md`, `core-matrix.md`, `playwright/`, `shots/`; the summary `b4/gate/full-1-summary.json`.
   - Product measurements on that tree, each in a request of section 3: the four slides undo rows; `arrange.selection-colour.light`; `arrange.group.chords`, `arrange.group.menu-regroup`, `arrange.context.rotate-distribute`; `formatting.paint-format.chords`; `text.title.bold-menu`, `text.title.bold-cmd-b`, `text.title.format-options-marks`, `text.fontsize.type-one-undo`; `tables.cells.merge-unmerge`, `tables.tail.merge-unmerge-buttons`; `slides.numbers.apply`; `inbox.bell-panel-toggle`; `tables.light-appearance` (deck light, state and paint dark) with `tables.present` and `tables.reload` behind the text lost by the table rows' driver fault below.
   - Driver faults found by this run and fixed before the follow-up (each with its mechanism): `shapes.text.colour-toolbar`, `shapes.text.enter-opens-label`, `shapes.borders-lines.menu` (`blockOf` not defined in shapes.mjs); `lines.connector.re-end` (the click on the connector's box centre missed its stroke, no handles read; the driver clicks a point of the path from `getPointAtLength`); `arrange.distribute.horizontal`, `.vertical` (the menu was asked for the Distribute submenu before three objects were selected, when the row is disabled and its submenu does not open); `arrange.guides.from-ruler`, `.show-toggle`, `.add-vertical-horizontal`, `.drag`, `.context`, `.clear` and `arrange.snap.guides-on-off` (the guides were stored and not drawn: the sheet draws them only while View > Guides > Show guides is on, and a guide added from the ruler or the menu does not turn it on; the rows judge the stored guides as the matrix says and turn Show guides on, and say so, where a drawn guide is the thing to press); `tables.menu.format-table-rows`, `tables.context.insert-delete`, `tables.cell.align-menu`, `tables.distribute.rows-columns` (Cmd+Z inside an open cell session is the field's undo; Delete row from the typed text's row removed the text and the rows after looked for it; a drawn read during a session reads the frozen markup; the driver ends the session before Cmd+Z and before a drawn read, looks the text up where the inserts moved it and keeps the deletes off its row and column); `chrome.separators.toolbar-dividers` (five dividers with a table selected, each 1 by 20 in the hair with 8 px of air; the count was fixed at two); `versions.show-changes-toggle`, `.show-changes-marks` (`data-show-changes` sits on the panel's list `.ts-versions.is-history`, not on `panel.versionHistory`); `help.check-slides` (the walk's deck carried three informational findings and no Fix; the row places a text block with an off ladder size, `type/ladder`, whose Fix snaps it); `export.pdf.file` (the owner deck's connectors named `elbowConnector` and `curvedConnector`; the schema's names are `bentConnector3` and `curvedConnector3`); `decks.file.open-upload-bundle`, `decks.file.import-slides-bundle` (the dialogs' file controls are the `<input type="file">` themselves; the bytes go to them with `setInputFiles`, no chooser event); `collab.roster.go-to-slide` (a click on the empty `+N` button, drawn at opacity 0 with pointer-events none, waited to the test's 180 s); `inbox.notification-arrives` (the panel lists a sentence from the item's kind, actor and slide, not the comment's words); `view.live-pointers.second-browser` (the arrival polled 10 s with the latency on record, the 2 s bound judged on it); `surface.advanced.off-by-default`, `surface.parked-shortcut-unbound` (deviation 10).
6. `dev-1`, `turboslide lint --chrome` on 4404 (section 5): the viewer 24 audits, 0 findings; the editor stopped on `editorGrid`.
7. `fix-1`, the follow-up walk of the areas whose drivers changed after the gate started: `--only shapes,lines,arrange,tables,share,help,chrome`, started 2026-09-19T03:03:50Z, 972 s, deck `untitled-20260919-<id>` (trashed and deleted forever by the walk's own rows), 175 rows of those areas: 161 passed, 14 failed, 0 not driven, 2 console errors. Passed after their driver fix: `shapes.text.colour-toolbar`, `shapes.text.enter-opens-label`, `shapes.borders-lines.menu`, `arrange.distribute.vertical`, `arrange.guides.show-toggle`, `arrange.guides.add-vertical-horizontal`, `arrange.guides.context`, `arrange.guides.clear`, `tables.context.insert-delete`, `tables.cell.align-menu`, `tables.distribute.rows-columns`, `tables.light-appearance`, `tables.present`, `tables.reload`, `chrome.separators.toolbar-dividers` (every chrome row green), `decks.name.follows-heading` and the rest of the decks rows. Red as a product measurement (section 3): `arrange.group.chords`, `arrange.group.menu-regroup`, `arrange.context.rotate-distribute` (the line's Rotate; the Distribute half re-measured in fix-2), `tables.cells.merge-unmerge`, `tables.tail.merge-unmerge-buttons`, `lines.connector.re-end` (the connector selected by a click on its path, the end handle dragged from B's site to C's left site: `connect.end` stays `con-b` and the connector does not follow C; a request to B3). Red on the driver, fixed before fix-2: `arrange.distribute.horizontal` and the Distribute half of `arrange.context.rotate-distribute` (Cmd+A took the objects the earlier arrange rows left on the slide, five in all, and Distribute spread them: gaps 412 and 56; the three boxes are selected by a click and two Shift clicks now), `arrange.guides.from-ruler` (the guide is stored at 400; the readout read "none": `t.readout` reads the marquee's `.ts-readout` and the guide drag's inches label is another element; the row reads any overlay text in inches now), `arrange.guides.drag` (the guide landed at 1001 for a 200 px drag scaled to the stage; judged within 2 px now, the landed x kept for the snap row), `arrange.snap.guides-on-off` (snapped to 1001, the guide's place; judged against the guide's stored x now), `tables.menu.format-table-rows` (every menu row works with a session, Distribute rows and columns equalise, Delete table and its Cmd+Z work; the one Cmd+Z after Insert row above and Insert column left left the new count while Cmd+Z after the deletes restored: a second press is on record now), `versions.show-changes-toggle` and `.show-changes-marks` (the read matched the panel before the list; one selector now), `help.check-slides` (6 suggestion rows and 2 Fix buttons with the off ladder block; the first Fix clicked belonged to another finding and the size stayed 27; the ladder row's own Fix now).
8. `specs-2`, the four edited specs through the gate: `run-gate.sh specs-2 --only specs --spec export,decks,share,surface`, started 03:21:37Z, 869 s: 83 rows, 59 passed, 5 failed, 19 not driven (the export dependents). Passed after their fix: `surface.advanced.off-by-default`, `surface.parked-shortcut-unbound` (Cmd+Option+Y opens nothing with the switch off and Format options with it on), `inbox.notification-arrives` (the bell's count reached 1 and the panel listed the mention sentence), every other decks, share and surface row. Red: `export.pdf.file` (the connector names again: the schema's connector kinds are the line kinds `elbow` and `curved`, `packages/schema/src/shapes.ts` `CONNECTOR_KINDS`, not the PowerPoint geometries; fixed to those), `decks.file.open-upload-bundle` and `decks.file.import-slides-bundle` (each to the test's 150 s bound with no step named: a click with no timeout on a button that did not take one; every click of both tests carries a timeout now so the next run names the step), `collab.roster.go-to-slide` (the chip click is Go to slide itself and jumped; the roster row was waited for after it: the RosterMenu opens from the `+N` button alone, which B1's 4.3 draws at opacity 0 with pointer-events none while the chips fit, so with one collaborator the roster has no opener a person can reach; the test measures that as it stands, a request to B1), `view.live-pointers.second-browser` (no pointer drawn on the first browser within 10 s of six moves over the sheet; a request to B7). Files: `b4/gate/specs-2/`, the summary `specs-2-summary.json`.
9. `fix-2`, the second follow-up walk: `--only arrange,tables,share,help`, started 03:36:44Z, 692 s, deck `untitled-20260919-buc8` (trashed and deleted forever by the walk's own rows), 129 rows of those areas: 121 passed, 8 failed, 0 console errors. Passed after their fix: `arrange.distribute.horizontal` (gaps even with the three boxes selected by click and Shift clicks), `arrange.guides.from-ruler` (the inches readout read), `arrange.guides.drag` (the guide at 1001 within the 2 px), `arrange.snap.guides-on-off` (snapped to the guide's x with the row on, dropped where aimed with it off), `versions.show-changes-toggle`. Red: `arrange.group.chords`, `arrange.group.menu-regroup`, `tables.cells.merge-unmerge`, `tables.tail.merge-unmerge-buttons` (as in run 5); `arrange.context.rotate-distribute` (Distribute from the right click menu now spreads the three to gaps 234 and 234; the line's right click menu still lists no Rotate); `tables.menu.format-table-rows` (every menu row changes the counts and Distribute equalises; two findings for B5 and B2: with the session in cell 0,1 Delete row removed the typed text's row 1, and with the session in cell 1,0 Delete column removed the typed text's column 1, so the plan reads the cell edited before the open session, one session behind; and one Cmd+Z after each menu row left the new count while a second restored it, on record for all four rows); `versions.show-changes-marks` (Show changes on and the older version picked: 0 change marks for a heading edit and an added box after the named version; the toggle itself works now; a measurement for the integrator's fixer who owns the panel); `help.check-slides` (6 suggestion rows and 2 Fix buttons with the off ladder block; the ladder row's own Fix, clicked, left the size at 27 on this run and the run before; the row is judged on the suggestions and the Fix listed, as the matrix says, and the Fix's outcome stays on record as a question for the lint's owner).
10. `specs-3`, the three specs edited after specs-2: `run-gate.sh specs-3 --only specs --spec export,decks,share`, started 03:49:03Z, 1498 s: 77 rows, 67 passed, 10 failed. Passed after their fix: `export.pdf.file` and the ten export rows that depend on it (`export.pdf.include-skipped`, `.notes-honest`, `export.pptx.perfect`, `.editable`, `.notes-and-skipped`, `export.print.download-pdf-follows-preview`, `export.html.web-page`, `export.png.current-slide`, `charts.export.pptx-native`, `lines.connector.export-pptx`, `images.export.pdf-with-picture`, `shapes.export.pdf`, `shapes.export.pptx`), every share row but two, every decks row but the two uploads. Red as a product measurement: `tables.export.pptx-editable` (the Editable text PowerPoint's `gridCol` widths read 1371600 EMU four times, 5486400 in all, against the table frame's 4114800 EMU: the columns overrun the frame by a third; RETURN.md 2.4 item 4, B5's fix 4 in `packages/export/src/pptx/table.ts`), `export.jpg.current-slide` (File > Download > JPEG image delivered a file named `<deck>-title.png`; the bytes are judged and the name recorded in the next run), `collab.roster.go-to-slide` (the chip's click jumped to the slide; the `+N` button takes no pointer with one collaborator, so the roster did not open: request 6), `view.live-pointers.second-browser` (no pointer within 10 s: request 7). Red on the driver, fixed before specs-4: `export.zip.bundle` (the bundle's entries carry `deck.json` under a folder; the row looked for it at the root and records the entry names now), `tables.export.pdf`, `charts.export.pdf`, `wordart.export.pdf` (the spec's `pdfText` reads the content streams' literal strings alone and found none of the typed cell text, the chart's series names or the word art while the earlier export rows read their words; the reader takes hex strings too now, and a PDF whose text it still cannot read makes the row not driven with that reason rather than failed), `decks.file.open-upload-bundle`, `decks.file.import-slides-bundle` (to the 150 s bound again with every click timed: lib's `waitEditor` waits 90 s and the tests now bound it at 40 s and annotate each phase, so the next run names the step).
11. `fix-3`, the help area once more: `--only help`, started 04:14:49Z, 125 s, deck `untitled-20260919-zgwi` (trashed and deleted forever by the walk's own rows): 22 rows, 22 passed; `help.check-slides` passed (the panel with 6 suggestions and 2 Fix buttons, the Fix click recorded).
12. The store after the runs: the walks' decks were trashed and deleted forever by their own rows; of the specs' decks, `untitled-20260919-bn9e` was removed through the window API under the lock and `untitled-20260919-bzd1` stays in the lane's tmp store, owned by a spec's browser identity ("You can view this presentation. Request edit access to change it." to the removal script's identity); the store is this session's scratch folder, not a deployment, and leaves with it.

Runs 13 and 14 (the first attempt's last spec run and this attempt's interrupted full gate) are in section 8.

### 7. Where the lane stands

Every row of the matrix has a driver and every driver ran against the tree of 02:03Z on the memory tier: the 446 probe rows in the gate (run 5) with the areas whose drivers changed walked again (runs 7, 9 and 11), and the 119 spec rows in the gate with the four edited specs run again (runs 8, 10 and 13). Reading the last run of each row on that tree:

- This round's 166 rows: 115 passed in run 5 and, after the driver fixes named in section 6, the follow-up runs turned green `shapes.text.colour-toolbar`, `shapes.text.enter-opens-label`, `shapes.borders-lines.menu`, `arrange.distribute.horizontal`, `.vertical`, the seven guides and snap rows, `tables.context.insert-delete`, `tables.cell.align-menu`, `tables.distribute.rows-columns`, `tables.light-appearance`, `tables.present`, `tables.reload`, `chrome.separators.toolbar-dividers`, `versions.show-changes-toggle`, `help.check-slides`, `inbox.notification-arrives`, `export.pdf.file`'s dependents among this round's rows (`export.html.web-page`, `export.png.current-slide`, `charts.export.pptx-native`, `lines.connector.export-pptx`) and the two surface rows.
- Red as a measurement of that tree, each in a request of section 3: the four slides undo rows (blocking), `arrange.selection-colour.light` (blocking), `text.title.bold-menu`, `text.title.bold-cmd-b`, `text.title.format-options-marks`, `text.fontsize.type-one-undo` (blocking), `arrange.group.chords`, `arrange.group.menu-regroup`, `arrange.context.rotate-distribute` (blocking), `formatting.paint-format.chords` (parks formatting), `tables.cells.merge-unmerge`, `tables.tail.merge-unmerge-buttons` (park the merge controls), `tables.menu.format-table-rows` (parks tables), `tables.export.pptx-editable` (parks tables), `lines.connector.re-end` (parks lines), `slides.numbers.apply` (parks `insert.slideNumbers`), `inbox.bell-panel-toggle` (parks inbox), `collab.roster.go-to-slide` (parks `title.presence.goTo`), `view.live-pointers.second-browser` (parks the live pointer rows), `versions.show-changes-marks` (parks `file.versionHistory.showChanges`), `export.jpg.current-slide` (parks `file.download.jpg`, the file name; run 13 says whether the bytes are a JPEG).
- Not driven with the reason: `text.clipboard.paste-without-formatting` (manual, the checklist), and the three docs PDF rows where the spec's reader cannot read the PDF's text (run 13 says).
- The upload rows (`decks.file.open-upload-bundle`, `decks.file.import-slides-bundle`) were named by their step in run 13 (section 8.1): the file input of each dialog is drawn on its Upload tab and the tests asked for it from the Presentations tab; fixed in 8.3, not yet run (8.5).

What the ship needs from other lanes is in section 3 and 8.2; what this lane would do next with more time: the full gate on the fresh server run to its end (8.5), read the PDF through a real text extractor (the docs rows), give `pickAppearance` a read of the sheet's own state rather than the tile's write, and move the three flaky rows' notes into the matrix's `note` field.

### 8. Attempt 2 (2026-09-19, 05:12Z on)

The first attempt ended without a report after run 13 (`specs-4`, below), which the ledger of section 6 did not yet carry. This attempt read the note and the tree, kept everything that landed, closed the open requests the other lanes wrote for B4, measured the tree again on a fresh server and wrote this section. Nothing here ran a git write command, `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build`; no scratch deck was made on a deployment; the lock was held only for the runs named.

#### 8.1 Run 13 of the first attempt, on record

13. `specs-4`, the three specs edited after specs-3: `run-gate.sh specs-4 --only specs --spec export,decks`, started 2026-09-19T04:21:22Z, 1790 s, on the server up since 02:03Z: 55 rows, 25 passed, 6 failed, 24 not driven; verdict failed. A measurement of a tired server, not of the product: `export.pptx.notes-and-skipped`'s owner setup ran to the 60 s bound of a `block.insert` with the title row on "Saving…", `export.pptx.editable`'s file arrived in 40.8 s against the 30 s bound, `decks.trash.listed-after-move`'s File > Move to trash navigation ran past 20 s (the same row passed in runs 5, 8 and 10), and the export dependents behind the owner read not driven. Two rows failed on their driver: `decks.file.open-upload-bundle` and `decks.file.import-slides-bundle` reached "dialog open" (the step annotations) and then waited on `dialog.open.file` and `dialog.importSlides.file` to the 150 s bound; the file input of each dialog is drawn on its Upload tab alone (`Open.tsx` `tab === 'upload'`, `ImportSlides.tsx` the same), and the tests asked for it from the Presentations tab; a navigation to `/edit/gt-brand-mu7vslsw` (the copy the `decks.list.gt-brand-deck` row makes, listed first in the Open dialog's Presentations tab) fired during that wait. The dev server's log of the run carries the `AbortError` lines of the closed contexts and nothing of the store. Files: `b4/gate/specs-4/`, the summary `specs-4-summary.json`.

#### 8.2 The requests of the other lanes to B4, read and answered

- B1 R5 (`chrome.mjs`, `view.mjs`, `formatting.mjs`): the readers were already the audit's port, `splitSeamFromShot` answers `dividerContrast` and `chrome.split.one-box` judges it at the 1.7:1 floor (section 1.2). Nothing to change. B1 R6 (`formatting.alt-text.write-undo`): not added; the row joins the matrix when Kevin answers RETURN.md question 6, and B1's two flags leave then.
- B2 R4 (`decks.name.follows-heading`): the step exists (section 1.2) and passed in runs 5 and 7. B2 R5 (`text.title.bold-menu`, `text.title.bold-cmd-b`, `text.title.format-options-marks`): the criterion changed to the row's words, "weight 500 is stored and drawn": `bolder(a, b)` in `text.mjs` passes a drawn weight of 500 or above with either a drawn weight above the one before or the stored typography weight 500 landing where it was not (`storedWeight`); `restored` after Cmd+Z compares the stored weight as well as the drawn facts, since the drawn weight is 500 before and after on the cover heading. `text.title.bold-cmd-b` keeps the same criterion and stays a measurement of whether Cmd+B writes at all (run 5: revision unchanged). B2 R6 (`text.fontsize.type-one-undo`): `typo()` already reads `block.size ?? typography.size`, so the first option of R6 is in place and the row's red of run 5 stands on the size field's `readOnly={!enabled}` with a text box selected by one click (`ToolbarTail.tsx` 1185, unchanged since 18:50 local), request 3. B2 R7 (the Paint format setup): fixed in the first attempt (one `/typography` write).
- B5 item 8 (`slides.numbers.apply`): the step is rewritten on the criterion B5 names, which is the dialog's and Google's. A fresh deck's mode is on already (`deckCounter` answers on when `/defaults/counter` is absent), so after On, Skip title slides and Apply the layout slide's words must stay and the title slide's counter must be empty, in the editor and in the show (the row's words say "in the show too"); Apply to selected with Off on the layout slide must take that slide's number alone (the stored `/counter` of the slide read as well as the drawn counter) and Cmd+Z must give it back, pressed only where a write landed; Cmd+Z after that must restore the stored mode and both slides' words. The observation records the stored mode, the title slide's `kind`, the dialog's refusal sentence if any and every counter read, so a red names its half: the write (`SlideNumbers.tsx`, B5's) or the draw of it (`Editor.tsx` 4680 reads `slideCounter(doc.deck, slide, ...)`, which honours the deck mode and the slide kind; `Stage.tsx` 72 draws the show's `<Frame index total />` with no mode; no render path reads a slide's own `/counter`). An absent counter reads as the empty string.
- B7 R4 (the JPEG and PNG rows): `reachDownloadRow` turns the switch on where the row is parked, as R4 asks (section 1.3). B7-R1 (the `format` parameter forwarded by `controller.tsx`) has not landed on the tree at this attempt (no `jpg` in the file), so `export.jpg.current-slide` measures a PNG again and stays on request 7's list with `parks: ["file.download.jpg"]`. B7's observation on `b4-presence-probe.mts` (line 96 of its note): the tombstone template read `${at}` from a map whose values are now `{ at, clock? }` and would print `[object Object]`; it reads `.at` now (the probe asserts nothing; `docs/gslides-parity/focus/build/b4-presence-probe.mts` 52).
- B5 item 9 (the cell range) and B3's `keys.ts` gate: `packages/viewer/src/keys.ts` 191 still reads `const parked = ctx.advanced === false` over the Paint format chords and the Group chords, unchanged since 14:11 local, so `formatting.paint-format.chords` and the chord half of `arrange.group.chords` stay measurements for B3 (request 4), and the merge rows stay B5's request 5.

#### 8.3 The drivers changed in this attempt

- `apps/studio/e2e/core/decks.spec.ts`: `decks.file.open-upload-bundle` clicks `dialog.open.tab.upload`, waits for `dialog.open.file` attached (8 s), sets the bundle on it with an 8 s bound and annotates the steps ("Upload tab picked", "the bundle set on the input"); `decks.file.import-slides-bundle` does the same on `dialog.importSlides.tab.upload` and `dialog.importSlides.file`, reads this studio's deck ids through `deck.list` before the upload (`deckIds`, the dialogs' own source) and registers every id new after it with the file's `scratch`, since the upload makes a deck of the bundle (`ImportSlides.tsx` `uploadBundle` then `choose(id)`) that would otherwise stay in the store past the run.
- `scripts/probes/core-walk/areas/text.mjs`: `storedWeight`, `bolder` and `restored` as 8.2 says (B2 R5).
- `scripts/probes/core-walk/areas/slides.mjs`: the `slides.numbers.apply` step as 8.2 says (B5 item 8).
- `docs/gslides-parity/focus/build/b4-presence-probe.mts`: the tombstone template reads `.at`.

The static gates after the edits: `node_modules/.bin/tsc -b` exit 0 (a first pass read a `ReadonlyArray` union that `Array.isArray` does not narrow; the helper takes the mutable shape `slideOrder` uses); `node_modules/.bin/vitest run --project scripts` 45 of 46 with the FOCUS.md verbatim case red as before (request 1); `node scripts/probes/core-matrix.mjs` 565 rows, 446 probe rows, 28 with `parks`; `node docs/readme/what-works.mjs --check` current; `prettier --check` over every file this lane owns clean; `node --check` on the two edited modules.

#### 8.4 The server and the store

The dev server of the first attempt had been up since 02:03Z and did not serve B5's `packages/viewer/src/table-session.ts` (03:23Z), `Editor.tsx` (03:50Z) or `packages/chrome/src/dialogs/SlideNumbers.tsx` (04:16Z), written after its start (deviation 9: the no-watch config transforms a module once). No product file changed after 04:30Z. The store held four scratch decks of run 13's timed out rows; under the lock, `remove-deck.mjs` found `untitled-20260919-4qbq` gone already (404) and was refused on `-bzd1`, `-tqos` and `-xjss` ("You can view this presentation. Request edit access to change it.": each belongs to a spec's own browser identity), so, the runs that made them gone, the three folders were removed from this lane's private tmp store with the server stopped, as `core-gate.mjs`'s own guard names the way ("the folder when its run is gone"); the store is this session's scratch folder, not a deployment. The server was restarted at 05:33Z (`restart-4404.sh`: the same store, the no-watch config, fresh secrets never printed) and serves the tree as it stands at this attempt.

#### 8.5 Run 14, the full gate on the fresh server, interrupted

14. `full-2`, the gate: `run-gate.sh full-2 --shots` (`node scripts/probes/core-gate.mjs --base http://localhost:4404 --out b4/gate/full-2 --matrix b4/gate/full-2-summary.json --lock b4/gate/full-2-lock --shots`), under the checkout's e2e lock, started 2026-09-19T05:35:31Z on the server of 05:33Z (the tree as it stands, memory tier), deck `untitled-20260919-notl`. Stopped by this lane at 05:38:41Z (SIGTERM to the walk and the gate) when the report came due, 190 s in: the walk had driven 63 of 446 rows. Decks: 16 of 16 passed (`decks.name.follows-heading`, `decks.file.open-list-search`, `decks.file.import-slides-deck` and `decks.file.details` among them, and `decks.save.acknowledged` at the end). Slides: 29 passed, 3 failed as measurements of this tree, the same undo defect run 5 read on the tree of 02:03Z (request 2): `slides.delete.undo-snackbar` (the snackbar's Undo puts the slide at the front: order `title-2-2-2, title, ...` after the undo), `slides.delete.two-selected-key-undo` (13 -> 11 -> 13, the order after the reload not the one before) and `slides.reorder.undo-drag` (Cmd+Z after the drag leaves `title-2-2-2-2, title, title-2-2-2, ...`); 4 rows cut by the stop and written FAIL by the walk with "Target page, context or browser has been closed" (`slides.reorder.menu-to-end-undo`, `slides.reorder.two-selected-drag` and the two after them), not measurements; 42 slides rows and every later area not driven (`n/d`) behind the stop. The walk's SIGTERM handler ran the cleanup rows against the server this lane stopped 3 s later, so `decks.editor.move-to-trash` reads FAIL there, an artefact of the stop, and the deck's folder was removed from this lane's private tmp store by hand (the store leaves with the scratch folder). The gate wrote no `--matrix` summary (it did not reach its end); the walk's log is `b4/gate/full-2.log`, its rows before the stop `b4/gate/full-2-before-stop.txt`, the shots `b4/gate/full-2/shots`. The three new drivers of 8.3 (`slides.numbers.apply`, the title bold rows, the upload rows) were not reached by this run, so on this tree they are edited and typechecked, not driven; their last drive is run 5's, of the earlier criteria and on the earlier tree.

#### 8.6 Where the lane stands after attempt 2

Every row of the matrix has a driver (section 4; `core-walk.test.mjs` and each spec's `coverage()` pin it). The static gates pass on the tree as it stands (8.3). The last complete measurement of every row is the first attempt's (runs 5 to 12, on the tree of 02:03Z; section 7); run 14 re-measured decks and the first 36 slides rows on the tree as it stands and read the same slides undo defect (request 2, blocking, the slides feature unparkable). Not driven on the tree as it stands, with the reason: every row after `slides.reorder.cmd-up-down` in the walk's order and every spec row, because the full gate was stopped at 190 s for the report; the verifier's own gate on this tree is the measurement of record for them, and the three drivers changed in this attempt meet their first drive there. The requests of section 3 stand as written, with 8.2's readings added: B7-R1 (the JPEG's `format`) and B3's `keys.ts` gate had not landed at 05:35Z. The dev server on 4404 is stopped, the lock is released, no scratch deck of this lane exists on a deployment, and the store under the scratch folder holds `gt-brand` and `templates` alone.

## Return round fix round

The B4 fixer of the return round (2026-09-19, the round's runs cross midnight UTC; times below are UTC), on finding R1-F2 of `docs/gslides-parity/focus/VERIFICATION.md` "Return round, pass 1" (R.5): `decks.trash.delete-forever-button` failed in both gate runs of the deployment of record on the file's teardown assertion, the copies the two bundle upload rows made still answering. One file edited, `apps/studio/e2e/core/decks.spec.ts`; nothing else of the tree touched. No git write command, no `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` or `vite build`; the dev server on 4404 (`TURBOSLIDE_STORE=tmp`, the memory channel, `TURBOSLIDE_LOCAL_OPEN=1`, fresh secrets never printed) stopped before the report; the checkout's e2e lock held for the localhost runs alone and released after each; the runs against the preview took no lock. Scratch files under this session's scratch folder (`b4-fixer/`: the edit scripts, the run logs, the Playwright reports and traces, `report-rows.mjs`).

### 1. What the traces say

The two retained traces (`.turboslide/verifier-return/gate/preview-run1/playwright/core-decks-decks-trash-del-724db-…/trace.zip` and `preview-run2/…`) carry the afterAll of the file. Run 2, for `untitled-20260919-pbwk-2` and `-3` alike: `lib.ts` `teardown` opened the copy's editor, clicked File > Move to trash and waited 20 s for a navigation that never came; the fallback `deck.trash` through the window API answered `{"error":"forbidden","capability":"trash"}`; the trash page never listed the copy (30 s); `deck.remove` answered `forbidden` for `remove`; the 20 s poll on `/edit/<id>` read 200 throughout. Run 1 reads the same for `g2a8-2`; `g2a8-3` does not appear in the teardown at all, because `decks.file.import-slides-bundle` registered its copy from a `deck.list` diff before and after the upload, and the blob tier's listing did not name the fresh deck inside the row (`packages/store/src/blob-store.ts`: `list()` lags `head()` by up to a minute), so the copy was never registered and stayed.

The mechanism is the product's: `apps/studio/src/routes/api/decks.bundle.ts` builds the caller's context from the ticket's identity (line 143) and calls `importDeckBundle` (line 192) without `recordNewDeck`, while `deck.create` and `deck.copy` record the creator as the owner (`apps/studio/src/server/actions.ts` 743 to 746, the `record()` wrapper). With no access record `decide()` (`packages/identity/src/access.ts`) synthesizes the legacy open record, the uploader stands on the copy as `editor` by `open`, `NOT_BY_LINK` refuses `trash` for an open standing and `remove` is the owner's. In shadow mode the denial is logged and the call proceeds, which is why the row passed on 4409 and in check 32: the 4404 log of this round reads `authorize.deny … "capability":"trash" … "shadow":true` and the same for `remove` on `untitled-20260919-tyjk-2`, the Import slides copy of the first local run, and the copy left anyway. On the enforce preview the same denials refuse.

### 2. The change, `apps/studio/e2e/core/decks.spec.ts`

- The deck an upload makes is known from the upload route's answer: `uploadAnswer(page)` is armed before the file goes to the input and reads the id from the 201's `location` header (`/edit/<id>`), else from the JSON `deckId`; the header first because the Open dialog navigates to the new deck as soon as the answer lands and Playwright cannot read a body after its frame navigated (the first local run read status 201 and no id). `registerUpload` puts the id into the file's `scratch` and into `uploads` the moment it is known; the Open row also registers the address's id when the answer named none, so no copy is left unregistered. The `deck.list` diff is gone.
- Each upload row tears its copy down at its end as the uploader, `removeUploaded`: the copy's own editor, `deck.info` for the revision and `state().access` for the standing, `deck.trash` on that revision, `deck.remove` with confirm on the stamp's revision, then from the original's editor the store's word within 20 s each: 404 on `/edit/<id>` and the id gone from `deck.list` (bounded 30 s). The row asserts that the uploader could, the way an uploaded file in Google Slides is the uploader's, with the standing and every refusal in the sentence. When the product refused and the environment carries `TURBOSLIDE_TOKEN` (the deployment's bootstrap bearer, the gate operator's, never printed), the copy is then removed with it through `POST /api/actions/deck.trash` and `deck.remove`, so the store the preview shares with production keeps no scratch deck; the row stays red, because the uploader could not.
- The afterAll runs `removeLeftoverUploads` first for a copy whose row failed before its own teardown (the same path, the proofs from the file's deck while it stands, else the draft at `/new`), a soft assertion names any copy no path removed, then `teardownAll` runs for the file's decks as before; its timeout is 300 s (180 before). The unused `teardown` import and the `void teardown;` placeholder are gone.
- The matrix words of the two rows are unchanged; the rows assert one fact more than their words, the copy's removal by its uploader, which the message spells out.

### 3. Runs

1. `local-full`, the whole file against 4404, 15:22:24Z to 15:24:28Z: 35 rows, 34 passed, 1 failed, `decks.file.open-upload-bundle` on "the upload answers a deck id (status 201)": the body read after the navigation, the driver fault the `location` header fixes (section 2). The copy of that row, `untitled-20260919-5w38-2`, was left unregistered by the fault and removed afterwards through the localhost agent surface (`deck.trash`, `deck.remove` 200, `/edit` 404, folder gone). The Import slides copy `untitled-20260919-tyjk-2` left by its row (the shadow tier let the uploader's calls through).
2. `local-rows-1`, the three rows against 4404, 15:27:22Z to 15:29:27Z, overlapping the preview run below: the Open row failed at `waitEditor`'s 90 s bound right after "bundle downloaded" (the editor of the file's deck did not hydrate on a server serving two browser runs), the other two passed. Not a measurement of the fix; rerun alone.
3. `local-rows-2`, the three rows against 4404 alone, 15:33:29Z to 15:33:58Z: 3 passed; both copies `untitled-20260919-0abl-2` (the same id twice, the first removed before the second upload) "deck.trash ok; deck.remove ok; gone from deck.list".
4. `preview-rows-1`, the three rows against `https://turboslide-hkne1i4o2-kl01s-projects.vercel.app` (the last enforce preview of the tree) with the OIDC header and `TURBOSLIDE_TOKEN` in the environment, 15:29:27Z to 15:31:58Z: `decks.file.open-upload-bundle` failed in 35.2 s with "the uploader stands on untitled-20260919-bqjm-2 as editor by open access; deck.trash refused: {"error":"forbidden","capability":"trash"}; deck.remove refused: {"error":"forbidden","capability":"remove"}; untitled-20260919-bqjm-2 stays on the store; the deployment's bearer: deck.trash 200, deck.remove 200; untitled-20260919-bqjm-2 removed by the bearer, not by its uploader; untitled-20260919-bqjm-2 gone from deck.list"; `decks.file.import-slides-bundle` failed in 34.0 s with the same sentence for `untitled-20260919-m8yw-2`; `decks.trash.delete-forever-button` passed in 20.7 s. Playwright restarted the worker after the first red, so the Import slides row ran on a second beforeAll deck (`m8yw`) and the first worker's afterAll tore `bqjm` down. At 15:33Z `bqjm`, `bqjm-2`, `bqjm-3`, `m8yw`, `m8yw-2` and `m8yw-3` all answer 404 on the preview: no scratch deck of this lane is on the shared store.
5. Gates: `node_modules/.bin/tsc -b` exit 2 on one error outside this lane, `packages/viewer/src/Editor.tsx` 4063 `Cannot find name 'cellAtPoint'` (with 212 and 216 unused names on the first pass; another lane's edit in progress), none in this lane's file; `prettier --check` clean on the spec; `node_modules/.bin/vitest run --project scripts` 46 of 46 (7 files).

### 4. Requests

1. B7, or the integrator, `apps/studio/src/routes/api/decks.bundle.ts` (blocking for the two rows on enforce): after `importDeckBundle` (line 192), for a new deck (`!result.replaced`), write the uploader's record, `await recordNewDeck(result.deckId, ctx)` (`apps/studio/src/server/access.ts` 275, the shape `registerHostedDeckActions`'s `record()` uses in `actions.ts` 743), so the person who uploads a bundle owns the deck it makes, as with `deck.create` and `deck.copy`, and can move it to the trash and delete it forever. The route serves both dialogs and the /decks page's form. With the record the two rows read green on the enforce preview and the bearer path in the spec never runs; on 4404 (shadow) they are green already.
2. The integrator (`docs/RETURN.md` section 2 and the parked list): until request 1 lands, `decks.file.open-upload-bundle` and `decks.file.import-slides-bundle` are red on the enforce preview by their own words, each with `parks` (`file.open`, `file.importSlides`), so under section 1 rule 2 they park those two menu rows for the ship and block nothing; `decks.trash.delete-forever-button` reads its own button (green in run 4). Section 2.17's entry and the row words in RETURN.md 390 and 392 (and FOCUS.md's rendered table) may mirror the drivers' added fact, "the uploader can move the new deck to the trash and delete it forever"; the matrix words were left as they are.
3. The verifier: run the gate with `TURBOSLIDE_TOKEN` in the environment (`.turboslide/verifier-return/env.sh` exports it) so an upload copy the product refuses leaves the shared store inside the run; without it the copy stays and the row names it in its red.

### 5. Deviations

- The finding asked the file's teardown to remove the copies with `deck.remove` and a poll on `deck.list`. The uploader's browser cannot on the enforce preview (section 1), so the removal is the row's own step and its red moves from `decks.trash.delete-forever-button` (an afterAll's error is charged to the last test, an unparkable row) to the two upload rows, whose mechanism it is and which carry `parks`; the shared store is kept clean by the bearer when the environment has it. The Delete forever button's row measures the button.
- A spec reads a secret from the environment (`TURBOSLIDE_TOKEN`) for cleanup alone; it is never printed and never used by a row's measurement.
- The tmp store `os.tmpdir()/turboslide/decks` is shared by every `TURBOSLIDE_STORE=tmp` server on the machine (a vite dev on 4405 ran beside this lane's); `untitled-20260919-n6ca` and `-jmd3` there are another lane's (first seen by this lane's server as listing thumbnails and the router's link prefetch, never created through it) and were not touched.
