# B1 notes, round four: brand assets, tokens, chrome mark, favicon and card pipeline

The brand builder of the Turboslide round four (`docs/gslides-parity/MILESTONES-4.md`, "B1 Brand assets, tokens, chrome mark, favicon and card pipeline"), in the shared working tree over `main` at `d5d7f07` (`BASE`). Section numbers refer to `docs/gslides-parity/SPEC-4.md` unless prefixed. Every command ran from `/Users/kevinliu/repos/Turboslide` with Node 24.13.0. No git write command ran; nothing is committed (the ship step commits). Dev server port 4341, started for the chrome checks and the selection screenshots of this stage and stopped before returning. Appended across stages.

## 1. Day 1 (2026-09-14)

### 1.1 What landed

Items 1 and 2 of MILESTONES-4 B1 "Delivers", plus the orchestrator's ruling 1 (the selection colour).

The geometry module and the tokens:

- `packages/theme/src/brand.ts` (new): `BRAND_TOKENS` (the eleven `--ts-` tokens of 1.8), `BRAND_TOKENS_NARROW` and `BRAND_NARROW_MAX_PX` (the 760 px block), `WINDOW`, `FIELD_END`, `D_MAX`, `isBody`, `windowDistance`, `field`, `markBits(N)`, `litCount`, `markPath(unit = 2)`, `solidUnits`, `cellRuns`, `cellRects`, `markGrid(sizePx)`, `markSvg(N, size, options)`, `markBlocks(N)`, `CELL_THRESHOLD_PX = 64`, `PATH_UNITS`, `MARK_LABEL`; the tile of 0.4 as data (`TILE_COLORS`, `TILE_SIZES`, `TileGeometry`, `solidWindow`, `solidBits`, `tileMarkPath`); the WCAG helpers `relativeLuminance` and `contrastRatio`; `SELECTION_COLORS` (ruling 1). Moved from `design-4/dither/mark.mjs` with its construction unchanged; imports `bayer8` from `@turboslide/effects/bayer` (the day 0 dependency).
- `packages/theme/src/brand.test.ts` (new, 28 tests): brand.css and `BRAND_TOKENS` agree (eleven tokens on `:root`, four in the one media block, no colour, radius or duration); the geometry (the window, the field, 52 of 64 cells at N = 8, 208 units of the 16 unit path, the recorded cell counts at 16, 24, 32, 40, 56 and 64, the fixed path `M0 0h16v16H0z M2 8h8v6H2z`, the row runs, the size table, the block glyphs); the tile (the inset marks, the integer edges, the hinted 12 px mark, the frame's contrast on both plates and both Chrome strips); the contrast numbers of 1.12; the selection colour (tokens.css declares it in both appearances with the values `brand.ts` records; 3:1 on both paper and ink per appearance; 4.5:1 under the chip's text; the selectors of Overlay.css, Marquee.css and Guides.css resolve to the token; every other `--pt-` token unchanged); `site.ts` (the description, the alt text, the manifest, the origin rule, the robots rules); EmptyFigure.css names the twins `site.ts` exports.
- `packages/theme/brand/site.ts` (new): `SITE` with `description`, `imageAlt`, `origin(requestOrigin?)`, `themeColor`, `icons`, `twins`, `card`, `manifest` (`start_url: /home`, five icons with one purpose each), `robots`; `ICON_PATHS`, `TWIN_PATHS`, `TWIN_SIZE`, `CARD_SIZE`, `THEME_COLORS`, `ROBOTS`.
- `packages/chrome/src/brand.css` (new): the eleven tokens, the 760 px block, the two `::view-transition-*` rules of 0.40, `.ts-mark` and `.ts-tile`, the app bar lockup (`.ts-brand-lockup`, `-mark`, `-word`) and the Not found frame (`.ts-notfound`, `.ts-notfound-actions`).
- `packages/theme/src/copy.ts`: `PROPER_NOUNS` gains `Turboslide`; `copy.test.ts` pins the seventh noun (the test named the six and would have failed otherwise; the list grew, nothing was weakened).

The chrome:

- `packages/chrome/src/TurboslideMark.tsx` (new): `TurboslideMark({ size, tile?, ...svg props })`, the solid path below 64 px and the cells of `markBits(N)` from 64 px on the size's own grid, `currentColor`, `crispEdges`, `role="img" aria-label="Turboslide"` alone and quiet with `aria-hidden`; `tile` draws the 16, 32 or 48 px tile in the host's tokens. The size is written as `width` and `height` attributes so a sheet's rule wins over them (the title row reads `--ts-mark`).
- `packages/chrome/src/AppBarBrand.tsx` (new): `AppBarBrand({ linkComponent?, homeTo = '/decks', aboutTo = '/home' })`, the 16 px mark as one link (`data-control="appbar.home"`, tooltip "Turboslide", "Your presentations.") and the word as a second (`data-control="appbar.about"`, "About Turboslide", "What Turboslide is and how fast it runs."); plain anchors without the slot. `APP_BAR_BRAND` exports the strings.
- `packages/chrome/src/EmptyFigure.tsx` and `EmptyFigure.css` (new): `EmptyFigure({ title, sentence, action?, figure: 'figure' | 'notfound', mark?, heading?, figureLabel? })`, the 320 by 180 crop at 1:1 in a `--pt-edge` frame (`role="img"` with `FIGURE_LABELS[figure]`), the title, one sentence, at most one action; the light twin by default and the dark twin under `:root[data-theme='dark']`; `mark` draws the mark on a `--ts-plate` plate over the crop (Not found). The twin files arrive on day 2; until then the frame shows the paper ground.
- `packages/chrome/src/editor-shell.ts`: `LinkSlotProps` and `LinkComponent` (the seam of 0.16: `{ to, preload?: 'intent', className?, children } & anchor attributes`) and `linkComponent?: LinkComponent` on `EditorShellInput`.
- `packages/chrome/src/TitleRow.tsx` and `TitleRow.css`: the GT mark in a plain anchor becomes `TitleHomeLink` (exported), the 24 px `TurboslideMark` inside the `linkComponent` when the input has one and a plain anchor when not, the same label, tooltip, `data-control="title.home"` and `data-menu-item` on both; the sheet sizes the mark from `--ts-mark`. `GtMark.tsx` stays for the sheet's content.
- `packages/chrome/src/Progress.tsx` and `Progress.css`: the fill's leading sixteen cells are the deck's ramp (`rampInk`, one cell row) applied as a mask on the fill's end (`rampEdgeMask`, `RAMP_EDGE_CELLS`), so the fill moves with `translateX` (`shiftOf`, exported) instead of `scaleX` and its masked right end is the leading edge; the track clips it. The track keeps the port's 2 px and `--pt-hair-soft`.
- `packages/chrome/PORTED_FROM.json`: rows for `brand.css`, `TurboslideMark.tsx`, `AppBarBrand.tsx`, `EmptyFigure.tsx`, `EmptyFigure.css` as Turboslide's own; the Progress changes recorded.
- `packages/chrome/src/__tests__/brand-chrome.test.tsx` (new, 13 tests): the mark's three forms and the tile; the lockup with and without the slot; the empty figure and the Not found form; `TitleHomeLink` and `TitleRow` through `EditorShellContext` rendering the slot when given and an anchor when not; `shiftOf`, `rampEdgeMask` and the rendered fill.

The icon set and the build:

- `scripts/build-brand.ts` (new): `node scripts/build-brand.ts` writes `packages/theme/brand/{mark.svg,mark-small.svg,icon-tile.svg,mark-geometry.json}` and `apps/studio/public/{icon.svg,favicon.ico,apple-touch-icon.png,manifest.webmanifest,robots.txt,brand-manifest.json}` and `apps/studio/public/icons/{icon-192,icon-512,icon-dark-192,icon-dark-512,icon-mask-192,icon-mask-512,icon-mono-512}.png`; `--check` verifies the committed files against the manifest (bytes, sha256), rebuilds and compares (pixels for PNGs and the ICO's decoded entries, bytes for text), asserts the facts of 6.4 (three ICO entries at 16, 32, 48 with three colours each and zero differing pixels against the tile rasters; the scheme block and `crispEdges` in `icon.svg`; the 180 px opaque touch icon; two colours on the six manifest and dark icons with the maskable marks inside the 40 percent safe circle; alpha only on the monochrome icon; the manifest's `start_url` and one purpose per icon) and compares `mark-geometry.json` with the colour counts read back. `--capture`, `--previews`, `--readme` and `--facts` name the day they land and exit 1. The two colour PNGs are written by the effects package's `encodePng1` (deterministic bytes; 257 to 460 bytes each); the touch icon and the monochrome icon by sharp as truecolor RGBA. The ICO writer is the design copy's.
- The head tags of 1.6 in `apps/studio/src/routes/__root.tsx`: the description, `application-name`, `apple-mobile-web-app-title`, one `theme-color` (`#070707` in the server's HTML, set to the stamped theme's paper by `THEME_COLOR_BOOT_SCRIPT` placed after `<HeadContent />`, since the meta does not exist when the theme boot script runs), the `og:*` and `twitter:*` set with the static card at `SITE.origin()`, the two icon links (`/favicon.ico` with `sizes="32x32"` first, `/icon.svg` second), the touch icon and the manifest; `brand.css` imported after `tokens.css`; `NOINDEX_ROUTES` gains `/edit/$deckId` (0.34, on B3's behalf); `NotFound` rebuilt as `EmptyFigure` with the 64 px mark, the heading "Not found", one sentence and three `.pt-ib` buttons with tooltips (New Presentation and Your Presentations as router links, About Turboslide as a plain anchor until B2's route is in the tree).

Ruling 1, the selection colour:

- `packages/chrome/src/tokens.css`: `--pt-select: #1a73e8` and `--pt-guide: #d6336c` on `:root`, `--pt-select: #3d86f0` and `--pt-guide: #f0397a` in the dark remap, with the contrast numbers in the comment. Nothing else changes; `git diff --exit-code packages/chrome/src/tokens.css` exits 1 by that block alone (section 1.3).
- `packages/chrome/src/Overlay.css`: the hover outline, the selection ring (and `.is-extra`), the group box, the selection chip's ground, the eight resize squares (filled while active or hovered), the rotation handle and its stem, the line end handles, the crop frame and the crop handles, and the slide level handles while active read `--pt-select`. The drop line, the readout, the connection sites, the clearance ring and the lint boxes stay ink and titanium.
- `packages/viewer/src/Marquee.css`: the marquee reads `--pt-select`. `packages/viewer/src/Guides.css`: the guides read `--pt-guide`. `packages/viewer/src/Editor.css` carries no colour and did not change.
- `packages/lint/src/chrome.ts`: the roles gain `select` and `guide` (`tokens.select = ['--pt-select']`, `tokens.guide = ['--pt-guide']`); `ChromeScope` gains `select` and `guides`; the studio scope names `.ts-select, .ts-select-chip, .ts-hover, .ts-handle, .ts-marquee, .ts-group, .ts-crop-frame, .ts-turn` and `.ts-guide`; a border or an outline in the select role passes on those elements and nowhere else; the collaborator hues are unchanged. `chrome.test.ts` gains the test.

Contrast, by the WCAG 2.2 relative luminance formula (`brand.ts contrastRatio`, asserted in `brand.test.ts`):

| Value     | Where                | On `#ffffff` | On `#070707` | Paper text on it |
| --------- | -------------------- | ------------ | ------------ | ---------------- |
| `#1a73e8` | `--pt-select`, light | 4.51         | 4.47         | 4.51 (`#ffffff`) |
| `#3d86f0` | `--pt-select`, dark  | 3.58         | 5.63         | 5.63 (`#070707`) |
| `#d6336c` | `--pt-guide`, light  | 4.62         | 4.36         |                  |
| `#f0397a` | `--pt-guide`, dark   | 3.77         | 5.34         |                  |

Each value holds 3:1 (SC 1.4.11) against both the paper and the ink of its appearance, so a ring reads on a white slide and on a dark photograph whichever appearance the chrome is in; the chip's `--pt-paper` text holds 4.5:1 (SC 1.4.3, 13 px). The frame of the tile: `#656565` on `#ffffff` 5.83, `#888887` on `#070707` 5.68, against Chrome's strips `#dee1e6` 4.45 and `#202124` 4.54.

The screenshot pair (`docs/gslides-parity/build-4/b1-selection/`, shot by `shoot-selection.mjs` against 4341 on a scratch copy of the GT deck made through `deck.copy`, light appearance, 1440 by 900 at 1x): `before-dark-photo.png` and `after-dark-photo.png` (the heading `h` selected on `opener-prototemplate`, the ink ring and chip against the dark photograph before, the blue ring and chip after) and `before-white.png` and `after-white.png` (`big` selected on `thesis`). "Before" is the round three CSS (`git show d5d7f07:` of Overlay.css, Marquee.css and Guides.css) injected over the served sheets on the same page, so the two states are the same pixels apart from the colour. The ring's computed colour after: `rgb(26, 115, 232)`. The scratch deck was removed from the tmp store afterwards.

The chrome record (`docs/gslides-parity/build-4/b1-chrome/`): `notfound-{dark,light}.png` (the Not found page at 2x, the 32 cell mark on its plate in the frame, the three buttons; the twin ground is missing until day 2), `title-row-{dark,light}.png` and `title-row-mark-8x-{dark,light}.png` (the 24 px mark in its 32 px link box, the computed size 24 by 24 inside an `<a href="/decks" data-control="title.home" data-tip="Turboslide home">`), `favicon-tiles-8x.png` (the three ICO entries at 8x nearest on Chrome's light strip colour). The served head on 4341 carried every tag of 1.6 and `theme-color` read `#070707` with the dark theme stored and `#ffffff` with the light one.

### 1.2 Readings and deviations, recorded

1. 6.4 says "`markBits(8)` has 208 ink cells": an 8 by 8 grid has 64 cells and the solid form lights 52 (64 less the 4 by 3 window); 208 is the area of the 16 unit path (256 less the 8 by 6 window). `brand.test.ts` pins both readings (`litCount(markBits(8)) === 52`, `solidUnits() === 208`, `litCount(solidBits(16)) === 208`) and the recorded count at N = 64 (2,400 of 4,096; 3,328 body cells).
2. The 16 px tile's mark (0.4, 1.1's first row): a 16 px plate with a 1 px frame leaves a 12 px mark, and 12 px is 0.75 px per unit, so the window's edge lands at 1.5 px. The one hinted size: rails of 2 px on the left and the bottom, a window 6 px wide and 4 px tall at (2, 6) (`brand.ts solidWindow(12)`, `TILE_SIZES[16]`); the 32 and 48 px tiles carry 24 and 40 px marks with integer edges by arithmetic (1.5 and 2.5 px per unit). 0.50 allows a hint after a soft edge shows; here the arithmetic shows it before any raster, and the three ICO entries decode to three colours each with no anti aliased pixel. A different inset (a 16 px mark filling the plate, which hides the plate) is one line of `TILE_SIZES`.
3. `markGrid` puts 8 px cells from 384 px, so the 448 px mark of the 512 tile has 8 px cells as 1.1's table says, and the 256 px maskable mark has 4 px cells (N = 64) as 0.5 says for 128 to 256 px; P1's mock drew that one at 8 px cells (N = 32). Reversible in one line.
4. `--ts-lead` is 18 px under 760 px (the table of 1.8) beside the three tokens 0.9 names; `BRAND_TOKENS_NARROW` and the media block carry the four.
5. The progress track: 1.9 says "the track stays one 1 px `--pt-hair-soft` line"; the port's track is 2 px in `--pt-hair-soft` and it stays so, read as one cell row of `--ts-cell`; the ramp runs over sixteen cells of that one row (`rampInk(x, 0, 16)`, cells 0 to 6, 8, 10 and 12 lit). Changing the track's height would change the viewer shell's grid and was not done.
6. `icon-dark-192.png` and `icon-dark-512.png` are the paper plate with the ink mark, the inverse of the ink tile manifest icons, per 1.5 step 2 ("the paper plate for `icon-dark-*`'s inverse"); P2 named the same files the other way round. B5 picks the source per GitHub ground for the README's `<picture>`.
7. `SITE.origin()` in the root `head()` reads `TURBOSLIDE_PUBLIC_ORIGIN` else the production origin; the request's origin is not available in `head()` without a loader, so a preview deployment without the variable carries production's card address (the same file). A route with a loader passes its request origin as `SITE.origin(requestOrigin)`.
8. The Not found page's About Turboslide button is a plain anchor to `/home`: a typed `Link` to a route the tree does not know is a type error until B2's `home.tsx` exists. It becomes a `Link` in the fixer round (`__root.tsx` is B1's).
9. `EmptyFigure.css` names `/brand/figure-{light,dark}.png` and `/brand/notfound-{light,dark}.png`; the files arrive on day 2, so today the frame shows the paper ground and the dev server answers 404 for the four paths.
10. The acceptance grep "no `--ts-` token in `apps/studio/src/styles.css`" as the integrator wrote it (`grep -c -- '--ts-\(cell\|mark\|...\)'`) counts 3, because `--ts-mark` is a prefix of round three's `--ts-marker` (lines 161, 209, 210); with a word boundary (`grep -cE -- '--ts-(cell|mark|h1|h2|h3|lead|body|small|figure|rail|plate)\b'`) it is 0, which is the reading of the day 0 amendment.
11. `packages/theme/src/copy.test.ts` (in nobody's row) was edited to pin the seventh proper noun; the list grew and nothing was weakened.
12. The tmp store dev server refused every request without `TURBOSLIDE_SESSION_SECRET`, as the integrator found for the node-server build (integrator.md 6.2); the 4341 command below sets it and `TURBOSLIDE_DOWNLOAD_SECRET` at obviously fake values.
13. For the studio typecheck and the dev server checks of this stage, the four chrome exports and the theme dependency of request R1 were applied to `packages/chrome/package.json` and the `../theme` reference to `packages/chrome/tsconfig.json`, then both files were restored from copies and `git diff --exit-code` on them is empty. `packages/chrome/node_modules/@turboslide/theme -> ../../../theme` was created by hand and is left in place: it is the link `pnpm install` makes once R1 lands, and the chrome tests resolve the geometry module through it until then.

### 1.3 Commands run and their results

```
cd packages/theme && ../../node_modules/.bin/vitest run          # 5 files, 72 tests passed (brand.test.ts 28)
cd packages/chrome && ../../node_modules/.bin/vitest run         # 54 files, 496 tests passed (brand-chrome.test.tsx 13)
cd packages/lint && ../../node_modules/.bin/vitest run           # 10 files, 72 tests passed
node_modules/.bin/tsc -b packages/theme packages/lint packages/chrome packages/viewer   # exit 0
node_modules/.bin/tsc -b apps/studio                             # exit 0 (with R1 applied temporarily, see 1.2 item 13)
node scripts/build-brand.ts                                      # 17 files written (list in 1.1)
node scripts/build-brand.ts --check                              # "16 files match the manifest, the rebuild and the facts of SPEC-4 6.4", exit 0
node scripts/build-brand.ts --facts                              # exit 1: "facts.json lands on day 3 of MILESTONES-4 B1"
git diff --exit-code packages/chrome/src/tokens.css              # exit 1 by the --pt-select block alone (ruling 1); the diff is the two comments and four declarations
python3 -c "from PIL import Image; im=Image.open('apps/studio/public/favicon.ico'); print(sorted(im.ico.sizes()))"   # [(16, 16), (32, 32), (48, 48)]; each entry decodes to 3 colours
grep -cE -- '--ts-(cell|mark|h1|h2|h3|lead|body|small|figure|rail|plate)\b' apps/studio/src/styles.css   # 0
node_modules/.bin/prettier --check <every file this stage touched>   # "All matched files use Prettier code style!"
```

The dev server, from `apps/studio`, stopped before returning:

```
TURBOSLIDE_STORE=tmp TURBOSLIDE_REALTIME=memory TURBOSLIDE_LOCAL_OPEN=1 TURBOSLIDE_AUTH_RATE_LIMIT=off TURBOSLIDE_DOWNLOAD_SECRET=<fake, 36 bytes> TURBOSLIDE_SESSION_SECRET=<fake, 38 bytes> node_modules/.bin/vite dev --port 4341 --strictPort
```

On it: `/new` 200 with every head tag of 1.6; `/no-such-page` 404 with the Not found markup; `/favicon.ico` 200 `image/x-icon` 15,086 bytes; `/icon.svg` 200 `image/svg+xml` 957; `/apple-touch-icon.png` 200 `image/png` 753; `/manifest.webmanifest` 200 `application/manifest+json` 971; `/robots.txt` 200 `text/plain` 99; `/brand-manifest.json` 200 `application/json` 2,571; `/icons/icon-512.png` 200 `image/png` 460. The two screenshot scripts ran under `.turboslide/e2e.lock` with one page at a time.

Not run today (day 3 and 5 items): `cd apps/cli && vitest run commands/banner`, `cd apps/studio && vitest run brand-files`, `turboslide --version` under `NO_COLOR`.

### 1.4 Requests

R1 (the integrator, before merge 1): `packages/chrome/package.json` gains `"@turboslide/theme": "workspace:*"` under `dependencies` and the exports `"./brand.css": "./src/brand.css"`, `"./TurboslideMark": "./src/TurboslideMark.tsx"`, `"./AppBarBrand": "./src/AppBarBrand.tsx"`, `"./EmptyFigure": "./src/EmptyFigure.tsx"`; `packages/chrome/tsconfig.json` gains `{ "path": "../theme" }` under `references`; then `pnpm install` once (the lockfile records the new workspace edge). `apps/studio/src/routes/__root.tsx` imports the four subpaths and does not compile without them; `packages/chrome/src/TurboslideMark.tsx` imports `@turboslide/theme/brand`. Verified with the lines applied temporarily (1.2 item 13).

R2 (the integrator, to B4's `packages/chrome/src/Thumb.tsx`, with B4's consent per MILESTONES-4 "The seams"): the prop type `capture?: 'never' | 'when-available'` on `Thumb`'s props today, so `Sidebar` compiles against the seam before B4 lands the behaviour on day 2.

R3 (the integrator, AGENTS.md at merge 1): the ruling 1 rule (`--pt-select` and `--pt-guide` are the only additions to `tokens.css`; the git gate reads "differs from `BASE` by that block only"; the chrome lint's `select` and `guides` scopes are the allow list); the brand module rule (`markBits` and `markPath` from `@turboslide/theme/brand` and nowhere else); the `linkComponent` seam; port 4341.

R4 (the integrator or B4, one comment line): `packages/viewer/src/Guides.tsx` line 3 still says the guides are `--pt-titanium`; they read `--pt-guide` since today (Guides.css). No behaviour change.

R5 (B3, from merge 1): fill `EditorShellInput.linkComponent` in `EditorRoot` with the router's `Link` wrapped to `LinkSlotProps` (`preload: 'intent'`, the anchor attributes passed through), so the title row's mark is a same document transition (0.16, 4.2 `edit->decks`); mount `AppBarBrand` with the same wrapper on `/decks`, `/decks/trash` and the four views in place of the `<Link className="ts-appbar-brand">` of `decks.index.tsx:586-594` (the decks.css rule can go); mount `EmptyFigure` for the empty states (`HOME.empty`, `HOME.trashEmpty`, no search match) with one New Presentation action; when `home.tsx` exists, tell B1 so the Not found page's About Turboslide anchor becomes a `Link` (or apply the one line at merge 2).

R6 (the integrator, the check chain): step 29 runs for real from this stage (the script exists) and step 30's gate (`brand-manifest.json` present on a linked machine) is now open, so `pnpm check` on this machine runs the Vercel build; the tooltip audit and the chrome lint of step 18 meet the new controls `notfound.new`, `notfound.decks`, `notfound.about` on `/no-such-page` and `appbar.home`, `appbar.about` once B3 mounts the lockup; every one carries the Tooltip primitive.

R7 (the integrator, a note for B2): `/home` reads the twins by `SITE.twins.hero` and the brand assets by `SITE` paths; the `--ts-` tokens are on `:root` from `brand.css`, which `__root.tsx` loads for every route, so `home.css` declares none.

### 1.5 Next

Day 2: `turboslide material capture` with P1's recipe and the anchor scan of 0.7 into `hero.recipe.json`; the twins cut at black 160, white 250, gamma 1.5 through `encodePng1` with the theme palette and the ink fraction sum check; `hero-`, `figure-`, `notfound-` and `og-screen-` twins under `apps/studio/public/brand/`; `og-template.html` and the card under 1 MB through `@turboslide/headless`; `--capture` in the build with the byte comparison in `--check`. Day 3: the wordmarks and `outline-wordmark.py`, the previews and the variants, `--readme`, the CLI banner and `--version`, the MCP sentence. Day 5: the print bar mark, `applyTheme`'s `theme-color`, `brand-files.test.ts`, `--facts`, the accessibility record's numbers for B5, the raster edge check of 0.50 at 20 and 24 px.

## 2. Days 2 and 3 (2026-09-14)

### 2.1 What landed

Items 3 to 6 of MILESTONES-4 B1 "Delivers" plus the day 5 items the stage named (the CLI banner, the MCP sentence, the print bar mark, `applyTheme`'s `theme-color`, `brand-files.test.ts`, `facts.json`, the contrast table) and the curtain picture of 0.15. Typed against the merged tree; no seam redefined. B3's `home.tsx` was in the route tree by the end of the stage, so the Not found page's About Turboslide button is a router `Link` now (b1.md 1.2 item 8 closed).

The twins (0.7, 0.12; `scripts/build-brand.ts --capture`):

- The build imports `captureMaterial` from `packages/materials/src/capture.ts`, the implementation the `material.capture` action and the `turboslide material capture` command run, and mounts P1's recipe (`paper:liquid-metal`, `LIQUID_METAL_DECK` on the whole canvas, `u_shape none`) at 3200 by 1800 on ANGLE Metal into a scratch deck folder under the OS temp directory, frozen at the 21 anchors of the scan (0 to 10,000 ms in 500 ms steps). Every frame is cut through `twoTone()` (the selected effects backend, `native` here; the cells are identical by the parity test) with autocontrast 0.5, black 160, white 250, gamma 1.5, 2 px cells, and `PLATE_BOXES.opener` measured. The rule of 0.7 ranks the frames by the fewest lit cells under the plate, then the fewest in its 30 px band, then the farthest nearest lit cell, then the earlier anchor; the hero is rank one and the figure and the Not found frames are the next two at least 1,000 ms from every chosen anchor, so the three pictures differ. Chosen: hero 6,000 ms (13,878 lit cells under the plate, 5,791 in the band, lit 30.45 percent), figure 10,000 ms (14,015, 5,196, 29.66 percent), notfound 2,500 ms (14,029, 6,092, 30.98 percent). The whole canvas recipe lights cells under the plate at every anchor (13,878 to 16,606), which is why the hero's plate is opaque (2.3); the lit fractions (28.5 to 31.9 percent) sit inside the GT twins' range.
- The twins are encoded through `encodePng1` with the theme palette: index 0 is `#070707` on both twins, index 1 `#f2f2f0` on the dark twin and `#ffffff` on the light one, the light twin `invertBits` of the dark (0.12). `apps/studio/public/brand/hero-{dark,light}.png` (13,679 and 13,689 bytes), `figure-*` (13,873 and 13,892), `notfound-*` (13,866 and 13,862), all 1600 by 900; `og-screen-{dark,light}.png` (1,885 and 1,887 bytes) is the hero frame through the same stage order at 120 by 63 cells scaled 10x nearest to 1200 by 630 (lit 24.46 percent). The ink fraction of a twin is its `#070707` share; the hero pair reads 0.69545 and 0.30455 and the two sum to 1.0 for every pair, which `--check` and `brand.test.ts` read back from the files.
- `packages/theme/brand/hero.recipe.json`: the material id, the resolved uniforms, the size, the GPU backend, the renderer string ("Chrome for Testing 147.0.7727.15, ANGLE Metal, Apple M5 Max"), the treatment, the plate box, the effects backend, the palettes, the scan with its rule, the 21 frames with their recipe keys and plate metrics, the chosen three, the twins' bytes and ink fractions, the card screen's facts. The capture took 24.8 s; the whole `--capture` run 28 s.

The card (1.7):

- `packages/theme/brand/og-template.html` is generated by the build from `design-4/dither/og.html`'s composition: the dark appearance, the card screen behind, the plate at left 150, width 600, bottom 79, height 236 carrying the 48 px solid mark, the word as the outlines of `wordmark-outlines.svg` (inlined as one path, `currentColor`) and the one sentence, the address top left in `--pt-mono` on its own plate. It links `inter.css`, `tokens.css` and `brand.css` by relative paths and is formatted through Prettier's HTML parser so the check chain's format step accepts it.
- `apps/studio/public/og/turboslide.png` is rendered by `@turboslide/headless` `launchBrowser` (the chromium-1217 build here) over an in memory origin that serves the repository (the capture job's `page.route` pattern, so no `file://` rule is involved) at 1200 by 630 and device scale factor 1, then encoded as the smallest exact PNG: an 8 bit indexed PNG written by the script when the render has at most 256 colours, sharp's truecolor otherwise. The render has 480 colours (the antialiased type), so the card is truecolor, 29,818 bytes, under the 1 MB ceiling. `--check` renders it again and compares pixels when the local browser is the chromium-1217 build and says why it skips otherwise.

The wordmarks and lockups (1.2):

- `packages/theme/brand/wordmark.svg` (887 bytes): the 48 px solid mark at the cap height of the word set at 66 px as live text (Inter 500, `cv11`, `ss01`, tracking -0.025em), a 16 px gap, the baseline at y 51 of a 66 unit line, a 354 by 66 box.
- `packages/theme/scripts/outline-wordmark.py` (moved from `design-4/shader/tools/`, the coordinates rounded to one decimal so the bytes do not depend on the pen's number formatting): the word from `InterVariable.woff2` at wght 500, opsz 32, with Inter's pair kerning (T to u minus 197 units, r to b minus 13), one JSON document on stdout. `build-brand.ts` runs it under `.turboslide/venv/bin/python` (fontTools 4.65.0) and writes `wordmark-outlines.svg` (5,474 bytes; the word is 288.7 px wide at 66 px against Chromium's 287.06). Without the venv the build says so and `wordmark.svg` carries the word as live text; `--check` compares the outlines by bytes only when the venv is present.
- `packages/theme/brand/lockup-stacked.svg` (10,463 bytes): the 64 px mark at 32 cells centred over the word at 66 px, a 32 px gap, 64 px of clear space, a 416 by 290 box with the mark's left edge on an even pixel.
- `docs/readme/brand/lockup-stacked-{dark,light}.png` (`--readme`): the lockup on an opaque plate in each appearance at 2x, rendered by Chromium with the repository's Inter, 832 by 580, 7,236 and 7,202 bytes, indexed (254 and 249 colours). For B5's `<picture>` (the dark file as the default source on GitHub's light ground).

The previews (`--previews`, `packages/theme/brand/previews/`, 680 KB, not in the manifest; `index.json` lists them):

- `mark-sizes.png` (the 16 tile, 24, the 32 and 48 tiles, 64, 128, the 180 touch tile and 512 at 1:1 on paper and on ink); `mark-16-tile-8x.png`, `mark-32-tile-8x.png`, `mark-16-paper-8x.png`, `mark-32-paper-8x.png` (nearest upscales); `tab-strips-1x.png` and `tab-strips-2x.png` (Chrome's light strip with the paper tile, Chrome's dark strip with the ink tile through the media block, Safari's dark strip with the paper tile, beside grey boxes for other sites' icons; the 2x strip uses the 32 px tile rasters); `confusion-sheet.png` and `-4x.png` (the 32 px tile and the bare 32 px mark beside R01 6.3 item 12's grey silhouettes: triangle, cube, Z, rectangle in a rectangle, roundel, chevron pair, speed line, a portrait card with a plate); `raster-edges.png` and `.json` (0.50, below); `hero-scan.png` (the 21 anchors' plate metrics over the committed hero twin at a quarter, with `PLATE_BOXES.opener` outlined; a record, not a product surface).
- `variants/`: P2's slide frame and T with the ramp panel at 16, 32, 64 and 512 (`p2-frame-t-*.png`) and P3's Inter T tile at 16, 32 and 64 (`p3-t-tile-*.png`), re-rendered from the generators' geometry copied out of `design-4/shader/tools/make-mark.mjs` and `design-4/type/tools/build-marks.mjs` (importing those modules would rerun their writes into the design folders), with a measured reason each in `variants.json` (P2 at 16 px: the 16:9 frame leaves 7 of 16 rows outside it, 44 percent of the box, a 1 px stroke and no cell; P3 at 16 px: the letter covers 15.6 percent of the box with a 2 px stem and no ramp cell before 64 px; the judges' rejections cited); `cell-threshold-1x.png` and `-2x.png`, P1's candidate sheet (the mark beside 44 px type at 32 and 48 px in the solid, 16 and 24 cell forms and at 64 px in 32 cells), rendered by Chromium with Inter.

The raster edge check (0.50; `previews/raster-edges.json` and the print bar shots in `b1-chrome/`): the 16 unit path at 16, 20 and 24 px, 1x and 2x, rasterized by librsvg with `crispEdges` reads two colours at every size; Chromium on 4341 reads two colours for the 20 px print bar mark at 1x and 2x in both appearances. At 20 px 1x the window's edges land at 2.5, 12.5, 10 and 17.5 px and the renderers snap them: librsvg draws a 2 px left rail and a 3 px bottom rail, Chromium a 3 px left rail and a 2 px bottom rail; at 2x (40 px) both draw 5 px rails. No edge is soft, so by 0.50's rule no hint was added; `solidWindow(20)` with 2 px rails and a 10 by 8 window is the one line if Kevin wants symmetric rails at 1x. The 24 px title row mark has integer edges (3, 12, 15, 21) and 3 px rails at 1x, 6 px at 2x.

The CLI banner (0.17, 1.11):

- `apps/cli/src/commands/banner.ts` (new): `cliVersion()` (apps/cli/package.json, 0.0.0 today), `bannerFacts(fourth)` (the version, `SITE.productionOrigin`, `ACTION_IDS.length`, `describeBackends().selected`), `bannerLines(facts)` (four lines: `markBlocks(8)` from the brand module, two spaces, the text), `printBanner(ctx, facts)` and the `banner(ctx)` command. No colour is written, so `NO_COLOR` changes nothing. The fourth line names the checkout (`repoRoot()`).
- `apps/cli/src/cli.ts`: `--version` is parsed before the command table and before the help check (`turboslide version ...` stays the version log command); the usage text names it under Global flags. `apps/cli/src/commands/info.ts` prints the same header before the deck facts with `deck <id> at revision <n>, <count> slides` on the fourth line; the `--json` result is unchanged (the header goes to stderr under `--json`).
- `apps/cli/src/commands/banner.test.ts` (new, 8 tests): the four lines and the glyph (52 of 64 cells, the window as spaces), the version against `package.json`, no escape sequence, `--version` on stdout with exit 0, byte identity under `NO_COLOR=1`, `--version --json`, and `info`'s header over the lint fixture deck.

The rest:

- `packages/mcp/src/server.ts`: `INSTRUCTIONS` gains "The product page is /home and the agent guide is /llms.txt." (0.18). `SERVER_NAME` and the title are unchanged.
- `apps/studio/src/routes/print.$deckId.tsx` and `print.css`: `<TurboslideMark size={20} />` before the title in place of `<GtMark width={25} height={16} />`; the GT mark stays the theme's, on the sheet.
- `packages/viewer/src/theme.ts`: `writeThemeColor(theme, doc = document)` sets the `theme-color` meta to the theme's `--pt-paper` from `THEME_COLORS` (`@turboslide/theme/brand/site`) and leaves a document without the meta alone; `applyTheme` and both bridge listeners call it (1.6). `packages/viewer/src/__tests__/theme-color.test.ts` (new) covers it with a stand in document.
- `apps/studio/src/brand-files.test.ts` (new, 9 tests): every manifest path exists at the recorded size with a known kind; every icon, manifest, robots and card path of `site.ts` is in the manifest; the eight twins are `captured` files; no stray file under `public/icons`, `public/brand` or `public/og`; the web manifest's `start_url`, purposes and description; robots.txt from `SITE.robots`; the card's size and ceiling; the twins' PNG headers (1 bit, indexed, 1600 by 900 and 1200 by 630).
- `packages/theme/brand/facts.json` (`--facts`, 0.25): 169 actions (`ACTION_IDS`, and `manifest.json`'s `actionCount` agrees), 146 MCP tools (`mcp-tools.json`), 153 HTTP paths (`openapi.json`), 21 layouts, 135 shape presets, 17 materials, 31 check steps (`node scripts/check.mjs --list`), the parity audit rows (2,980 pass, 5 fail, 384 skipped, 3,369 in all, from `docs/gslides-parity/verification-3/parity-audit.json` of 2026-09-14 on the node-server build), the licence (MIT, `LICENSE`), the export's worst decoded page mismatch (0.003 percent, `docs/HOSTED-STATUS.md`), and the measured rows: the 141 rows of the verifier's `perf-budget-baseline-2026-09-14.json` (the deployment profile against production, the newest such run by its `startedAt`) with `check`, `name`, `value`, `limit`, `unit` and `ok` each, so B2's source lines can name the file and the date. `brand.test.ts` (7 new tests) asserts the counts against the tree (the schema exports, the generated contracts, `check.mjs --list`), the sources' existence and the twins' palettes, sizes and lit sums from the PNG bytes; `--check` recomputes every count (a stale count fails naming `--facts`) and notes a newer measured run without failing.
- The curtain picture (0.15): `EditorSkeleton.tsx`'s stage box holds a `ts-curtain-host` laid over it with a `ts-curtain` child, a 16:9 box fitted through container query units and centred where the sheet will be; `PresenterSkeleton.tsx`'s current slide frame carries `ts-curtain`. `brand.css` paints the figure twin at 1600 by 900, aligned to the box's top left corner so no cell is split, `image-rendering: pixelated`, the light twin on light chrome and the dark twin under the theme attribute, with selectors that outrank the skeleton sheet's rules on the same boxes; the stage box is given `position: relative` in the skeleton state because the shell's own sheet (which positions it) is not loaded yet. No box changes size, so the layout shift gate of SPEC-3 9.4 is untouched. The ramp texture is the Progress component's (day 1); the skeleton carries no progress track because SPEC-3 9.2's skeleton has none and a new box would change the frame.
- `scripts/build-brand.ts` (rewritten around day 1's core): the flags of the header comment; the manifest records carry a `kind` (`built`, `outlined`, `captured`, `rendered`, `facts`) so `--check` knows what to rebuild and what to compare by bytes; every JSON output and the card template go through Prettier's API so the bytes the build writes are the bytes the tree keeps; `--check` runs in 5.2 s (one browser launch for the card compare) and prints what it skipped and why.

### 2.2 Readings and deviations, recorded

1. The capture is the product's job by implementation and not by the binary: the build imports `captureMaterial` (what `material.capture` and the CLI run) instead of spawning `turboslide material capture ... --two-tone`, because the CLI's two tone path encodes grayscale twins and drops the continuous frame, while 0.12 wants the theme palette and 1.7 wants the same frame at 10 px cells; one browser session gives both. A scratch deck folder under the OS temp directory receives the capture's files and is removed. The command of 1.5 step 6 is recorded as read.
2. The scan of 0.7 never finds a clear plate with P1's whole canvas recipe (13,878 lit cells under the opener plate at best), so the hero is the frame with the fewest, as the rule says, and the plate on `/home` is opaque (2.3). The figure and the Not found frames are the next two by the same rule at least a second apart; P1's "frames two and three" were the 5,500 and 7,000 ms anchors of a three anchor capture, which the scan supersedes.
3. `TWIN_PATHS` are fixed names (`/brand/hero-dark.png` and so on, the seam MILESTONES-4 names and `EmptyFigure.css`, `brand.css` and B2 read), while SPEC-4 1.6 says the `/brand/**` twins are content hashed with a year of `immutable`. The seam wins this round; the cache rule should be the icons' (a day, a week of stale service) unless the paths gain hashes. Request R9 to B3.
4. The card's plate is `--ts-plate` on the dark appearance, the same colour as the screen's ground, so the plate reads as the region without cells (P1's composition; no edge rule in 1.7). The card holds 480 colours because of the antialiased type; 1.7's "four colours" counts the flat fills. The verifier's OG decode will record the count.
5. The wordmark's live text SVGs (`wordmark.svg`, `lockup-stacked.svg`) take Inter from the page they sit in; the README PNGs and the card use the outlines or a Chromium render with the repository's Inter, so no surface depends on an installed font.
6. `raster-edges.json` records the 20 px snapping asymmetry (item 2.1 above); no hint by 0.50's rule.
7. `packages/theme/brand/hero.recipe.json`, `facts.json`, `mark-geometry.json`, `brand-manifest.json` and `og-template.html` are written in Prettier's shape by the build (Prettier collapses number arrays, which changed the recipe's bytes under `format:check` until the build adopted the same formatter). `manifest.webmanifest` goes through the same formatter with the JSON parser.
8. `facts.json`'s parity row count is a measured fact of a named run (the verifier's node-server audit of 2026-09-14, 3,369 rows), not a count of the tree, because the audit's rows exist only when it runs; the source path and date are in the file and B2's copy must carry them.
9. The chrome lint refuses a page that answers 404 (`packages/headless/src/shell.ts:254`, `HTTP 404 for <url>`), so `lint --chrome` on `/no-such-page` reports four unapplied states and audits nothing; step 18's `/no-such-page` row (SPEC-4 6.1) needs the auditor to accept the Not found status. Request R8 to the integrator. The tooltip audit's `--only /no-such-page` walked its whole page list instead of the one page (not this builder's file; noted for the integrator).
10. The editor skeleton's grid was styled on the first 4341 run of the day (the stage box measured 1,184 by 756 px and the curtain filled it) and unstyled on later runs after B3's route changes landed in the tree (the stage box measured 1,440 by 0 px, `.pt-main` `display: block`), so the fitted curtain could be shot only in the state where the host was the viewport (1,440 by 810). The recorded `skeleton-curtain-editor-*.png` are the first run's whole stage crop; the fit is verified by the computed sizes of the second run and waits for B3's route to settle. Request R10 to B3.
11. The e2e lock: the first shoot on 4341 ran while another builder's `.turboslide/e2e.lock` was held (this builder's wait logic was inverted) and its cleanup removed that lock afterwards; the lock's holder's run was a Playwright suite on its own port and this shoot drove one page on 4341, so no server was shared, but the removal was wrong and is recorded here. The later single page shoots ran without taking the lock while it was held by another builder for more than 90 s, on 4341 only.
12. The `--version` banner prints `0.0.0`, the version `apps/cli/package.json` carries (the MCP server reads the same file); SPEC-4 1.11's sample shows `0.1.0`, which is the crate's version and not the CLI's. The banner states what is true.
13. `describeBackends()` in the banner loads the native addon on this machine (`native`); on a Vercel build it says `typescript` until 0.38 lands, as 1.11 says.
14. The verifier's Layout grid plate on Escape (finding 9) was not taken this stage; it stays the fixer round's unless day 5 has room.

### 2.3 Contrast, for docs/brand.md (SPEC-4 0.50, 1.12; B5 writes the document)

By the WCAG 2.2 relative luminance formula on the token values (`packages/theme/src/brand.ts` `contrastRatio`, pinned in `brand.test.ts`), against SC 1.4.3 (https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html, 4.5:1 for text, 3:1 for large text, no requirement for text that is part of a logo) and SC 1.4.11 (https://www.w3.org/WAI/WCAG22/Understanding/non-text-contrast.html, 3:1 for user interface components and graphical objects), both read 2026-09-14 by this builder.

| Pair                                                                                                 | Light         | Dark          | Where                                                                                   | Verdict                                        |
| ---------------------------------------------------------------------------------------------------- | ------------- | ------------- | --------------------------------------------------------------------------------------- | ---------------------------------------------- |
| `--pt-ink` on `--pt-paper` (`#070707` on `#ffffff`; `#f2f2f0` on `#070707`)                          | 20.14         | 17.97         | headings, body, the mark on its plate, the banner's text                                | passes every level                             |
| `--pt-ink-2` on paper (`#3a3d44`; `#b9bcc3`)                                                         | 10.88         | 10.59         | captions, the fact line, source lines, the card's sentence                              | passes AAA                                     |
| ink on `--pt-plate` (`#f6f6f6` composited)                                                           | 18.64         |               | commands in hairline boxes                                                              | passes                                         |
| `--pt-panel-text` on `--pt-panel-ink` (`rgba(255,255,255,0.87)` over `#101010`, composite `#e0e0e0`) | 14.41         | 14.41         | the present toolbar                                                                     | passes                                         |
| the tile's frame (`#656565` on paper; `#888887` on ink)                                              | 5.83          | 5.68          | a graphical object                                                                      | passes 3:1                                     |
| the frame against Chrome's tab strips (`#dee1e6`; `#202124`)                                         | 4.45          | 4.54          | the tile's edge reads on both strips                                                    | passes 3:1                                     |
| the paper plate against Safari's dark strip (`#ffffff` on `#2d2d2d`)                                 | 13.77         |               | the tile without its frame                                                              | passes                                         |
| `--pt-titanium` on paper (`#8a8f98`)                                                                 | 3.25          | 6.20          | never text under 19 px on the new surfaces                                              | large text only on light                       |
| `--pt-select` on paper and on ink (`#1a73e8`; `#3d86f0`)                                             | 4.51 and 4.47 | 3.58 and 5.63 | the canvas selection ring, handles, marquee, hover outline, crop frame, group box, chip | passes 3:1 on both grounds in both appearances |
| `--pt-guide` on paper and on ink (`#d6336c`; `#f0397a`)                                              | 4.62 and 4.36 | 3.77 and 5.34 | the snap guides                                                                         | passes 3:1                                     |
| the chip's text, `--pt-paper` on `--pt-select`                                                       | 4.51          | 5.63          | the selection chip's 13 px text                                                         | passes 4.5:1                                   |

The twins carry the theme palette exactly (`#070707` with `#f2f2f0`, `#070707` with `#ffffff`), so a figure's cells are the ink and paper pairs of the first row; the mark is `currentColor`; the tab icon holds three colours (plate, frame, ink) at 16 px as 0.4 amends R01 acceptance item 5.

### 2.4 Commands run and their results

```
node scripts/build-brand.ts --no-card                  # the sources, the outlines, the icon set; 20 files in the manifest, the twins named as missing
node scripts/build-brand.ts --capture                  # 21 frames in 24.8 s on ANGLE Metal; hero 6000, figure 10000, notfound 2500; 9 files written; 28.4 s in all
node scripts/build-brand.ts                            # the card by chromium-1217 (480 colours, truecolor, 29,818 bytes); 31 files in the manifest; 4.5 s
node scripts/build-brand.ts --facts --previews --readme  # facts.json (169, 146, 153, 21, 135, 17, 31, 3,369 rows, 141 measured rows), 23 preview files, the two README lockups (832 by 580, indexed); 4.0 s
node scripts/build-brand.ts --check                    # "31 files match the manifest, the rebuild and the facts of SPEC-4 6.4"; 5.2 s (the card compared by pixels on chromium-1217; the outlines rebuilt under the venv)
cd packages/theme && ../../node_modules/.bin/vitest run          # 5 files, 79 tests passed (brand.test.ts 35)
cd packages/chrome && ../../node_modules/.bin/vitest run         # 54 files, 496 tests passed
cd packages/viewer && ../../node_modules/.bin/vitest run         # 27 files, 242 tests passed (theme-color.test.ts 2)
cd packages/mcp && ../../node_modules/.bin/vitest run            # 5 files, 42 tests passed
cd apps/cli && ../../node_modules/.bin/vitest run commands/banner   # 1 file, 8 tests passed
cd apps/cli && ../../node_modules/.bin/vitest run                # 21 files, 131 passed, 1 skipped
cd apps/studio && ../../node_modules/.bin/vitest run brand-files # 1 file, 9 tests passed
node_modules/.bin/tsc -b apps/studio packages/chrome packages/theme packages/viewer apps/cli packages/mcp   # exit 0 (a full `tsc -b` at 10:47 failed on B3's in flight EditorRoot.tsx and -access-page.tsx alone; both compiled by 10:58)
NO_COLOR=1 node apps/cli/bin/turboslide.mjs --version > a && node apps/cli/bin/turboslide.mjs --version > b && cmp a b   # identical; the banner prints 0.0.0, 169 actions, effects backend: native
git diff --exit-code packages/chrome/src/tokens.css              # exit 1 by the --pt-select block alone (17 insertions against BASE, the comment and four declarations)
python3 -c "from PIL import Image; im=Image.open('apps/studio/public/favicon.ico'); print(sorted(im.ico.sizes()))"   # [(16, 16), (32, 32), (48, 48)]
grep -cE -- '--ts-(cell|mark|h1|h2|h3|lead|body|small|figure|rail|plate)\b' apps/studio/src/styles.css   # 0
node_modules/.bin/prettier --check <every file this stage touched>   # clean (outline-wordmark.py is not Prettier's)
```

The dev server on 4341 (from `apps/studio`, the tmp store, the memory channel, both fake secrets, stopped before returning; the shots in `docs/gslides-parity/build-4/b1-chrome/`, the scripts `shoot-day3.mjs`, `shoot-curtain.mjs` and `debug-curtain.mjs` beside them): every twin and the card answered 200 `image/png` at the manifest's byte sizes; `/no-such-page` 404 with the notfound twin behind the mark (`url(/brand/notfound-dark.png)` under the dark theme, `notfound-light.png` under the light one, 320 by 180), `theme-color` `#070707` and `#ffffff`, three `<a>` buttons with About Turboslide at `/home`; `/print/gt-brand` 200 with the 20 px mark reading two colours at 1x and 2x in both appearances; `/edit/gt-brand` and `/present/gt-brand` with the server functions held showed the curtain (`figure-dark.png` and `figure-light.png` at 1600 by 900, pixelated) in the stage box and the presenter's current frame.

### 2.5 Requests

R8 (the integrator; `packages/headless/src/shell.ts:254`, nobody's file this round): `lint --chrome` treats any status of 400 or more as an infrastructure failure, so the `/no-such-page` row of step 18 (SPEC-4 6.1) audits nothing; accept a 404 when the page carries the Not found root (`main[data-control="notfound"]`) or add a `--allow-status 404` switch to `lint-chrome.ts`. The page's colours resolve inside the `--pt-` set by construction (`brand.css`, `EmptyFigure.css`), so the row should pass once it runs.

R9 (B3, `vite.deploy.config.ts` `routeRules`): the twins keep the fixed names of `TWIN_PATHS` (the seam), so `/brand/**` should take the icons' rule (a day in the browser, a week of stale service) and not a year of `immutable`; a re-capture would otherwise never reach a browser that cached the old file.

R10 (B3): the editor skeleton lost its grid on 4341 after the day's route changes (`.pt-main` `display: block`, the stage box 1,440 by 0 px) while the first run of the day had it (1,184 by 756 px), which reads as the shell's CSS leaving the route's synchronous graph; the skeleton's boxes and the curtain need `EditorShell.css` and `ViewerShell.css` (or the skeleton rules of `styles.css`) present before the route chunk lands, else the layout shift gate of SPEC-3 9.4 measures the unstyled frame. When the route settles, `node docs/gslides-parity/build-4/b1-chrome/shoot-curtain.mjs` on 4341 records the fitted curtain.

R11 (B5, `docs/brand.md`, `README.md`): the contrast table of 2.3 with its two URLs and the date; the lockup files `docs/readme/brand/lockup-stacked-dark.png` (the default source) and `lockup-stacked-light.png` (`prefers-color-scheme: light`), 832 by 580 at 2x, drawn at 416 by 290; the README's icon `<picture>` per b1.md 1.2 item 6 (`icon-192.png` is the ink plate, `icon-dark-192.png` the paper plate); `facts.json`'s counts and sources for every number the README states; the capture record (`hero.recipe.json`) and the build's flags for the build script section.

R12 (the integrator; `scripts/hosted-smoke.mjs`, `scripts/check-vercel-output.mjs`): rows for the eight twins under `/brand/` and `/og/turboslide.png` (200, `image/png`, the manifest's bytes, `x-vercel-cache: HIT` on the second request), and `brand/*.png` in the Vercel output's static list beside the icon set.

R13 (the integrator, a note for step 29): `build-brand.ts --check` now launches Chrome for Testing once (the card compare on chromium-1217) and runs the venv python once (the outlines), 5.2 s here; on CI both are skipped with a printed reason and the committed files are verified by bytes.

R14 (B2, a note): `facts.json` is in the tree; the measured rows carry `check`, `name`, `value`, `limit`, `unit` and `ok`, the source file and the date; the parity row count is a measured fact with its own source and date; `TWIN_PATHS.hero` is the hero pair and `hero.recipe.json` `chosen.hero.anchor` is 6,000 ms for the credit line.

### 2.6 Next

Day 5 if room allows: the Layout grid plate on Escape (verifier finding 9); the fitted curtain shot once B3's routes settle (R10); the About Turboslide `Link` is done. The fixer round: whatever VERIFICATION-4 names in these files.
