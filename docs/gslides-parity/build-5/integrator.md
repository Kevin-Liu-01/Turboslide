# Integrator notes, round five, day 0

The integrator of the Turboslide round five (`docs/gslides-parity/MILESTONES-5.md`, "Integrator"; `SPEC-5-amendments.md` A6 to A8), day 0 on 2026-09-15 over `main` at `d5a1be5` in the shared working tree. Nothing is committed: the ship step commits on `main` by an explicit path list (the orchestrator's ruling 1). Section numbers refer to `docs/gslides-parity/SPEC-5.md` unless prefixed SPEC, SPEC-2, SPEC-3, SPEC-4 or A (the amendments). Every command ran from `/Users/kevinliu/repos/Turboslide` with Node 24.13.0 and pnpm 11.15.1; no git write command ran; `pnpm install` ran once (section 5) and `pnpm generate:contracts` once (section 6). The untracked `.github/` (the gh token lacks the workflow scope), the nine mockup PNGs under `design-4/*/previews/`, the eight walk shots under `build-4/hotfix-4/shots/` and the verifier's `verification-5/` (written this morning, `BASELINE.md`) were not touched. No token, cookie or secret was printed.

## 1. BASE

`BASE` is `d5a1be5`, "Turboslide round five: the handoffs from the hotfix afternoon", the head of `main` after the round four ship (`43707c3`), the round five amendments (`0830115`), hotfix 2 (`cb646e9`, `02e92c6`), the poll cadence (`720ccb4`), hotfix 3 (`1e78132`, `6679c54`, `04d95c2`), the round four fix round and production table (`2ee1db4`, `30ff754`), and hotfix 4 (`e2e6307`, `ecbf715`). SPEC-5 was written against `d5d7f07` (the round three hotfix) while round four was in the working tree. Between the two, of the files SPEC-5 18 cites, four changed and one is new (`git diff --stat d5d7f07 d5a1be5 -- <cited paths>`): `apps/studio/src/editor/controller.tsx` (new at round four's merge 1, 3,399 lines), `packages/store/src/blob-store.ts` (+307, hotfix 2 and the deck index), `packages/schema/src/canvas.ts` (+226, hotfix 3's `resizeBox`), `scripts/check.mjs` (+199, steps 29 to 31), `packages/render/src/slide.ts` (6 lines, hotfix 4's `renderMultiline` on the title lead). Every other cited file (`menus/model.ts`, `present/strings.ts`, `actions.ts`, `store-actions.ts`, `store/templates.ts`, `pptx/shapes.ts`, `scene/types.ts`, `pptx/build.ts`, `ooxml/groups.ts`, `realtime/protocol.ts`, `menus/keys.ts`, `menus/strings.ts`) is byte identical between `d5d7f07` and `BASE`, so its citations hold as written. `packages/chrome/src/tokens.css` is untouched by this day; the gate `git diff --exit-code packages/chrome/src/tokens.css` against `BASE` is empty.

## 2. The line citation map (SPEC-5 18)

Every code citation of SPEC-5 18, read at `d5d7f07` with `git show` and re-read on `BASE`; the "on BASE" column is the line before this day's edits (the day 0 seam moves some of them again, the third column says where).

| Citation at `d5d7f07`                                                                                                                                         | On `BASE` (`d5a1be5`)                                                    | After day 0                                                                                                                                                                                                                                     |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `menus/model.ts` the 21 `later(` rows at 687, 724, 832, 841, 858, 901, 909, 927, 1067, 1204, 1205, 1352, 1353, 1491, 1558, 1563, 1832, 1834, 2026, 2052, 2086 | the same lines (the file is identical)                                   | unchanged rows, each 19 lines further down (the `MenuSetting` and `MenuClientHandler` unions grew by 8 lines and `Menu.setting?` with the helper comments by 11 above them); `OMITTED_MENUS` moves 121 lines under the three new tables         |
| `menus/model.ts` the clause constants at 536 to 547                                                                                                           | 536 to 547                                                               | 555 to 566                                                                                                                                                                                                                                      |
| `menus/model.ts` the helpers `now`, `later`, `omit`, `sub` and the effect builders at 485 to 529                                                              | 485 to 529                                                               | 504 to 548                                                                                                                                                                                                                                      |
| `menus/model.ts` `OMITTED_MENUS`                                                                                                                              | 2254                                                                     | 2375, under the new `GS5_PLANNED_EFFECTS`, `GS5_DIALOG_TITLES` and `GS5_PANEL_TITLES`                                                                                                                                                           |
| `present/strings.ts` `captionsStub` 22, `autoPlayStub` 30, `penStub` 32, `downloadStub` 36                                                                    | the same lines                                                           | unchanged (B1 retires three of them by day 7; `captionsStub` stays, 0.15)                                                                                                                                                                       |
| `actions.ts` `ACTION_GROUPS` 119, `Milestone` 92, `DECK_TEMPLATES` 567, `slide.import` 1478, `picture.dither` 4721                                            | the same lines                                                           | `Milestone` 138, `ACTION_GROUPS` 170, `ACTION_IDS` 220 (the 51 ids at 393 to 443 after `slide.setBackgroundMaterial`), `DECK_TEMPLATES` 693, `slide.import` 1613, `picture.dither` 5072, the round five rows from 5240 (`motion.setTransition`) |
| `apps/cli/src/store-actions.ts` (the checkout dispatcher)                                                                                                     | `registerStoreActions` at 3491                                           | 3506; `registerLaneActions` at 3790 (section 4)                                                                                                                                                                                                 |
| `packages/store/src/templates.ts` 50 to 77 (`TemplateArchetype`, `TemplateRecord`)                                                                            | 50 to 77 (`StaleRevisionError` at 587)                                   | unchanged; B3's record grows there from merge 1                                                                                                                                                                                                 |
| `packages/export/src/pptx/shapes.ts` `objectName`                                                                                                             | 42                                                                       | unchanged                                                                                                                                                                                                                                       |
| `packages/export/src/scene/types.ts` `Scene`                                                                                                                  | 372                                                                      | 412 (`SceneMedia` and `SceneEquation` above it; the seven optional fields at its end)                                                                                                                                                           |
| `packages/export/src/pptx/build.ts` the post process (`groupShapes` call)                                                                                     | 639                                                                      | 652; `rewriteMedia` at 647 and `rewriteEquations` at 649 in Editable text mode (the order of 2.4)                                                                                                                                               |
| `packages/export/src/ooxml/groups.ts` `groupShapes`                                                                                                           | 153                                                                      | unchanged (B3's ids and the `mc:AlternateContent` regex land at merge 1)                                                                                                                                                                        |
| `packages/realtime/src/protocol.ts` `entryKindSchema`                                                                                                         | 97                                                                       | 106, `z.enum(ENTRY_KINDS)` with `chat` (section 4)                                                                                                                                                                                              |
| `packages/render/src/slide.ts` `renderSlide`                                                                                                                  | 144 (143 at `d5d7f07`; hotfix 4's one line import above it)              | 145                                                                                                                                                                                                                                             |
| `scripts/check.mjs` (the step table)                                                                                                                          | `const steps` at 283 (143 at `d5d7f07`; round four's constants above it) | 375, with steps 32 to 37 at its end                                                                                                                                                                                                             |
| `packages/schema/src/reduce.ts:532` "baseRevision N is stale" (A1)                                                                                            | 532                                                                      | 573 (the `DECK_SET_ROOTS` comment, `isMediaAsset` and the two `normalizeMotion` call sites above it)                                                                                                                                            |
| `packages/store/src/blob-store.ts` `syncTtlMs` 750 ms (A3 item 2)                                                                                             | 420, 446, 662 (319, 345, 561 at `d5d7f07`)                               | unchanged; B7's day 3 removes it for documents and manifests                                                                                                                                                                                    |
| `packages/schema/src/canvas.ts` `resizeBox` (A4)                                                                                                              | 792 (absent at `d5d7f07`)                                                | 792                                                                                                                                                                                                                                             |

The tree listing SPEC-5 18 read for the ownership map of 12.6 holds, with the round four names section 3 records.

## 3. Reassignments and the day 0 amendments (MILESTONES-5 "Day 0 amendments")

1. The file names round four gave. The editor split is `apps/studio/src/routes/edit.$deckId.tsx` (the route alone), `apps/studio/src/routes/-edit-search.ts`, `apps/studio/src/editor/controller.tsx` (`createEditorController`, the write path, the `on(...)` table), `apps/studio/src/editor/EditorRoot.tsx` and `apps/studio/src/editor/shell-bridge.tsx`, plus hotfix 2's `apps/studio/src/editor/client-ids.ts` and `apps/studio/src/routes/-hash-guard.ts`. The filmstrip is `packages/chrome/src/Filmstrip.tsx` with `Thumb.tsx`, `ThumbShot.tsx` and `Sidebar.tsx`. The home components are `apps/studio/src/routes/home.tsx` and `home.css` and `apps/studio/src/components/home/` (`HomeHero.tsx`, `HomeNav.tsx`, `HomeEditor.tsx`, `HomeCards.tsx`, `HomeCompare.tsx`, `HomeAgents.tsx`, `HomePipeline.tsx`, `HomeSpeed.tsx`, `HomeFacts.tsx`, `HomeFooter.tsx`, `HomeLink.tsx`, `Shot.tsx`, `SpriteIcon.tsx`, `copy.ts`, `facts.ts`, the generated `facts-data.ts`, `shots.ts` and `shots.json`, `home-meta.ts`). The shape definitions are `packages/schema/src/shapes/definitions.ts`, `shapes/ids.ts` and `shapes/presetShapeDefinitions.xml` with the generator `packages/schema/scripts/build-definitions.mjs`; B4's interpreter is the new `packages/schema/src/shapes/geometry.ts` reading `definitions.ts`. The perf script is `scripts/perf-budget.mjs`; the card render path is `apps/studio/src/server/thumbs.ts`. Every round five "Owns" row names these files under those names.
2. `apps/cli/src/store-actions.ts` does not split handlers by area: it is one 3,764 line module whose `registerStoreActions` registers every handler (`apps/cli/src/commands/*.ts` are the CLI's flag parsers, not handler modules). The per lane modules therefore land as new files under `apps/cli/src/actions/`: `motion.ts` (B1), `media.ts` (B2), `import.ts` and `templates.ts` (B3), `page.ts` and `print.ts` (B4), `prefs.ts`, `spelling.ts` and `chat.ts` (B5), `equation.ts` and `theme.ts` (B6), `font.ts` (B7), with `deps.ts` (`LaneDeps`, the integrator's), each exporting `register<Lane>Actions(dispatcher, deps)` and spread by `registerLaneActions` in `store-actions.ts` and again in the hosted `deckDispatcher` with `deps.hosted` (section 4). The modules are reached by the editor page through `store-actions.ts`, so they stay free of `node:` imports; a Node only need is a dependency on `LaneDeps`, requested from the integrator. B1's "`apps/cli/src/actions/motion.ts` or the split the integrator names" resolves to the former. B5's "dictionary, accessibility, chat, version and remaining rows handler modules" map onto `spelling.ts` (spelling, dictionary), `prefs.ts` (prefs, autocorrect, version.delete, the remaining rows) and `chat.ts`; `spelling.ignore`, `accessibility.verbalize` and `motion.play` are window only and live in the controller's `on(...)` table.
3. `menu-model.test.ts` counts the `file.email` container. `allItems()` walks containers (`walkItems`), so the Later count at `BASE` reads 22 over `allItems()` (21 leaf rows plus the `file.email` container, whose status derives from its Later child) and the round's end state reads 3 over `allItems()` (`file.email`, `file.email.collaborators`, `tools.activityDashboard.viewers`) and 2 over leaf rows. SPEC-5 14.2's "Later count at 2" is the leaf count; the assertion the last flip lands names both numbers (`allItems().filter(later)` 3 with one container, the leaves 2), and 16.1's audit row reads the leaf count.
4. Files a round five lane needs that round four did not land, reassigned without changing the owner of the behaviour: `packages/schema/src/validate/` did not exist (the validator is one file), so the six family modules of 1.2 are new files under it, integrator on day 0 and their owners afterwards (`motion.ts` B1, `media.ts` B2, `page.ts` B4, `equation.ts`, `theme.ts`, `layout.ts` B6); `packages/export/src/check/` did not exist, so the five check modules are new files there; `packages/chrome/src/panels/` does not exist, so B1's `MotionPanel.tsx`, B3's `TemplatesPane.tsx` and `BuildingBlocksPane.tsx` and B5's `SpellCheck.tsx`, `Chat.tsx` and `Dictionary.tsx` create it; `packages/schema/src/building-blocks.ts` (B3, merge 1) and `packages/schema/src/import-report.ts` (the `ImportReport` shape of 0.29; the integrator's, with B3 requesting widenings) landed on day 0 because the action table types against them; `packages/spelling/` did not exist, so the integrator scaffolded it (`package.json`, `tsconfig.json`, `vitest.config.ts`, `src/index.ts` with the seven dictionary tags and `index.test.ts`) so the one `pnpm install` attaches `nspell` and the dictionaries; B5 replaces the placeholder. `packages/render/src/blocks/render-block.ts`'s switch belongs to no lane's "Owns" row and is the integrator's shared file for the round: the three new cases draw a plain box, and B2 and B6 replace the case bodies by a one line request each. `packages/chrome/src/inspector/sections.ts` `BLOCK_ICONS`, `packages/chrome/src/AssetPicker.tsx` and `inspector/asset.tsx` (the role word tables) and `packages/import/src/ids.ts` `STEMS` are exhaustive maps over `BlockType` and `AssetRole`, so they gained the three types and the `media` role on day 0 (the glyphs `play`, `user-group` and `code` are placeholders until B2 and B6 name theirs).
5. B7's files, read from `build-4/hotfix-2.md` and `hotfix-3.md` (A6, A8). The sync client module is `packages/realtime/client/room-client.ts` (hotfix 2's `incoming`, `appliedAtSeq`, `applyEntry`, `drain`, `take`, the `resync` position, the checkpoint rule, `OpenOptions.retire`, `stop`) with `room-client.test.ts`, plus `apps/studio/src/editor/client-ids.ts` (`partitionRoster`, the id memory) and its test; the write queue and rebase live in `controller.tsx` (`draftEnqueue`, `replayDraftQueue`, `rejectDraftQueue`, `draftCommit`, `commitAs`, `StaleBaseError`, `invokeRebasing`, `checkBase`, `reportedRevision`) and `EditorRoot.tsx` (the shell's `revision`, `pagehide`, the `presence` filter), which stay the integrator's files with B7's lines landing as same day requests; the server side is `apps/studio/src/server/room.ts` (`admitOnBlob`, `retireClients`) and `apps/studio/src/routes/api/decks.$deckId.stream.ts` (the retire list), B7's for the sync rules; the document read path of `packages/store/src/blob-store.ts` (`sync` at 662, the manifest read, the 750 ms `syncTtlMs` at 420 and 446; B2 owns the media entries, `putAssetFile`, `cacheControlMaxAge`, the keyed prefix and the index of the same file, so each edits only its own functions and says so); the presence self filter across `packages/chrome/src/presence/**` (`PresenceSlot.tsx`, `RosterMenu.tsx`, `FilmstripMarks.tsx`, `RemoteCursors.tsx`, `remote-caret.ts`, `FollowingPlate.tsx`, `Announcer.tsx`, `presence-model.ts`, `roster-hook.ts`); the poll cadence of `docs/sessions-polling.md` (`apps/studio/src/components/useStudioSession.ts` and `apps/studio/src/server/sessions.ts`, the `RETRY_MS` backoff and the visibility gate, A8 rows 7 and 8); and `scripts/probes/new-write-probe.mjs`, which B7 keeps green beside the new `sync-stress-probe.mjs`. The resize model B4 inherits is hotfix 3's `packages/schema/src/canvas.ts` `resizeBox` with `RESIZE_HANDLES`, `ResizeModifiers`, `ResizeKind`, `ASPECT_LOCKED_KINDS`, `locksAspect`, `resizeKindOf`, `RESIZE_MIN_SIZE`, `rotateVector` and `rotatedBoxCorners` (`canvas.test.ts`), the gesture path in `packages/viewer/src/snap.ts` `snapResize`, `Gestures.tsx` `freeGesture` and `nudgeMutation`, `rotate.ts` `unrotateDelta`, `Editor.tsx` `withAutofit`, `Freeform.tsx` `measureBoxes` and `Editor.css`, the renderer's `blocks/primitives.ts`, `blocks/dia.ts` and `block-css.ts` rules (B4 owns the two viewer files and B2 the picture renderer; the block CSS lines are requests), `packages/chrome/src/inspector/geometry.tsx` (the Width and Height fields over the model) and `apps/studio/e2e/resize.spec.ts`, which joins check step 32 (A4) and needs its hosted teardown (A8 row 5). A8 row 1 (the shot frame 12 px past its box) is B4's in `blocks/figures.ts`'s shot renderer by request to B2's picture file where the two meet. A8 row 6 (the report only CSP header's `upgrade-insecure-requests`, the `script-src` eval report) is the integrator's in `apps/studio/src/server/headers.ts` with B2, who owns the file this round, by request. A8 row 10 (the Insert menu rows for the mark, the code panel and the logo plates) is B3's rows landed by the integrator in `model.ts` on request.
6. Ownership additions this day names: `packages/schema/src/fonts.ts` (`FONT_IDS`, the integrator's; B7 requests membership changes), `packages/schema/src/import-report.ts` (the integrator's), `packages/schema/src/motion.ts` (B1's from day 1, the `MotionSchedule` types included), `packages/schema/src/blocks/media.ts` (B2's), `blocks/equation.ts` (B6's), `preferences.ts` (B5's), `building-blocks.ts` (B3's), `packages/render/src/motion.ts` (B1's), `theme-css.ts` (B6's), `packages/export/src/scene/media.ts`, `ooxml/media.ts`, `check/media.ts` (B2's), `scene/equations.ts`, `ooxml/math.ts`, `check/equations.ts` (B6's), `check/motion.ts` (B1's), `check/odf.ts`, `check/svg.ts` (B4's), `apps/cli/src/actions/<lane>.ts` per lane and `deps.ts` (the integrator's), `packages/mcp/src/tools.ts` (the integrator's agent surface; section 7 item 12).

## 4. The day 0 seam, file by file (1.6)

The schema (`packages/schema/src`), every field optional with the meaning of an absent field in its doc comment:

- `deck.ts`: `THEMES` gains `ts-plate`; `DEFAULT_LANGUAGE`; `CustomLayoutId` (`custom-<slug>`), `customLayoutIdSchema`, `isCustomLayoutId`; `Deck.page`, `.language`, `.themeEdits`, `.importedThemes`, `.customLayouts` and `.media` (item 7.1 below); `SlideBase.template` widened to a custom id, `.transition`, `.animations`; `DeckGuides.colors`; `THEME_MARK_KINDS`, `THEME_FONT_ROLES`, `THEME_TYPE_LEVELS`, `ThemeEdits`, `IMPORTED_THEMES_MAX` 5, `ThemeRecord`, `CustomLayout` with their schemas; `GUIDE_MAX` at the page cap with the page itself bounded in `validate/page.ts`; the slide and deck schemas annotated for the inspector.
- `render.ts`: `PAGE_PRESETS`, `PAGE_PRESET_LABELS`, `Page`, `PAGE_PRESET_SIZES`, `DEFAULT_PAGE`, `PAGE_MIN_PX` 120, `PAGE_MAX_PX` 6720, `PAGE_PX_PER_INCH` 120, `pageSchema`, `deckPage(deck)` (the identity B4's sweep reads from), `isDefaultPage`.
- `motion.ts` (new; 1.3 verbatim plus the schedule types): `TRANSITION_KINDS`, `ANIMATION_EFFECTS`, `ANIMATION_TRIGGERS`, `FLY_DIRECTIONS`, `ENTRANCE_EFFECTS`, `EXIT_EFFECTS`, `DURATION_MS`, `ANIMATIONS_MAX` 200, `SlideTransition`, `Animation`, `transitionSchema`, `animationIdSchema`, `animationSchema`, `animationsSchema`, `MotionEffect`, `MotionStep`, `MotionSchedule`, `motionStepSchema`, `motionScheduleSchema`, `emptySchedule`, `MOTION_LABELS`, `ANIMATION_LABELS_IN_ORDER`, `animationLabel`, `normalizeMotion`.
- `blocks/media.ts` (new): `MEDIA_KINDS`, `PLAYBACK_STARTS` with Google's labels, `YOUTUBE_ID_PATTERN`, `MediaSource`, `MediaPlayback`, `MediaFields`, `SPOTLIGHT_SHAPES`, `SpotlightFields`, `mediaAssetRefSchema` (the empty reference for a fresh block), `mediaSourceSchema`, `mediaPlaybackSchema`, `mediaFieldsShape`, `spotlightFieldsShape`, `DEFAULT_PLAYBACK`, `isYoutubeSource`.
- `blocks/equation.ts` (new): `EQUATION_DISPLAYS`, `EQUATION_SYMBOL_GROUPS` with Google's labels, `EquationSymbol`, `EquationFields`, `EQUATION_PLACEHOLDER_TEX`, `equationFieldsShape`; B6 adds `GOOGLE_ALIASES` and `EQUATION_SYMBOLS` on day 3.
- `blocks.ts`: `BlockBase.placeholder` (`PLACEHOLDER_KINDS`, `PLACEHOLDER_LABELS`; not annotated, item 7.6), `PlainBlock.start`, `.prefix`, `.suffix` with their fields, `RECOLOR_PRESETS`, `ShotReflection`, `ShotAdjust.reflection` and `.recolor` in the shared picture tools' `adjust` schema, `MediaBlock`, `SpotlightBlock`, `EquationBlock`, the three schemas composed from the field shapes like the table and chart blocks, `BLOCK_TYPES` and `BLOCK_SCHEMAS` with the three, `GS5_BLOCK_TYPES`.
- `blocks/table.ts`: `CellEdgeBorder`, `CELL_BORDER_EDGES` (Google's nine), `CellBorder` per edge (`top`, `bottom`, `left`, `right` override the one border), `cellEdgeBorderSchema`, `cellBorderSchema` extended.
- `blocks/dither.ts`: `DITHER_PATTERNS` with `floyd-steinberg`, `atkinson`, `halftone-dot`, `halftone-line`; `DITHER_PATTERNS_GS5`; `DITHER_ANGLE`; `PictureDither.angle`; `ResolvedDither` keeps `angle` optional and `resolveDither` copies it only when written (item 7.4).
- `typography.ts`: `firstLine`, `hanging`, `family` (`FONT_IDS`). `fonts.ts` (new): `FONT_IDS` (the 26 of A5), `DEFAULT_FONT_ID`, `FONT_CATEGORIES`, `FONT_LICENCES`, `isFontId`.
- `position.ts`: `GROUP_PATH_PATTERN`, `groupPathSchema` on `group`, `groupSegments`, `outerGroup`, `innerGroupPath`.
- `assets.ts`: `ASSET_ROLES` gains `media`; `TWIN_VARIANT_SIZE` [320, 180]; `MEDIA_ASSET_KINDS`, `MEDIA_MIMES`, `MEDIA_EXTENSIONS`, `MediaAssetSource`, `MediaAsset`, `DeckAsset`, `mediaAssetSourceSchema`, `mediaAssetSchema`, `deckAssetSchema`, `isMediaAsset`, `isPictureAsset`, `pictureAssets`, `pictureAssetOf`.
- `preferences.ts` (new; 7.1 verbatim): `PREFERENCE_UNITS` with Google's labels, `AUTOFIT_PREFERENCES`, `SVG_TEXT_MODES`, `Substitution`, `Preferences`, `LANGUAGE_TAG_PATTERN`, `languageTagSchema`, `preferencesSchema`, `DEFAULT_SUBSTITUTIONS` (the twelve of R10 2.2, no em dash row), `defaultPreferences`, `PREFERENCES_DEFAULTS`.
- `building-blocks.ts` (new): `TEMPLATE_CATEGORIES` with labels, `BUILDING_BLOCK_CATEGORIES` with labels (the nine of 0.25), `BUILDING_BLOCK_BOX`, `TemplateIndexEntry`, `BuildingBlock`, their schemas, `buildingBlockIdSchema`, `buildingBlockSummarySchema`. `import-report.ts` (new): `IMPORT_ROW_STATUSES`, `IMPORT_THEME_MODES`, `IMPORT_SHEET_MODES`, `ImportReportRow`, `ImportReport`, their schemas, `importSummarySentence`.
- `export.ts`: `EXPORT_FORMATS` with `odp`; `MOTION_EXPORT_MODES`, `MEDIA_EXPORT_MODES`, `PRINT_LAYOUTS` with Google's labels and `PRINT_SLIDES_PER_PAGE`, `PAPERS`, `ORIENTATIONS`, `PRINT_ORDERS`, `AUTOPLAY_INTERVALS_MS`; `ExportReportRow`, `ExportMotionSummary`, `ExportCellEntry`, `ExportCheckSection` with schemas; `ExportOption.id` and `EXPORT_OPTIONS` gain the eight rows of 1.2; `ExportReport` gains `page`, `motion`, `media`, `equations`, `layout`, `paper`, `orientation`, `pages`, `cells`, `truncatedNotes`; `ExportCheck` gains `container`, `odf`, `motion`, `media`, `equations`, `svg`.
- `validate.ts`: `IssueCode` gains the seven codes, `GS5_ISSUE_CODES`; `validateDeck` calls the six family modules over the parsed document; `validate/{motion,media,equation,page,theme,layout}.ts` (new) each answer `[]` but `page.ts`, which bounds the guides by `deckPage(deck)`.
- `catalog.ts`: the `media`, `spotlight` and `equation` entries (`export: 'mixed'`, group `figure`; the media block's `assetPaths` names the poster alone, the source names `deck.media` and validate/media.ts checks it).
- `reduce.ts`: `normalizeMotion` on `block.remove` and `slide.replace`; `asset.set` and `asset.remove` route a media record into `deck.media` by `kind`; `DECK_SET_ROOTS` admits `language`, `page`, `themeEdits`, `importedThemes`, `customLayouts`. `diff.ts` diffs `deck.media` through the two asset ops. `mutations.ts` `asset.set` carries a `DeckAsset`.
- `actions.ts` (13): `Milestone` and `MILESTONES` gain `GS5`; `ActionGroup` and `ACTION_GROUPS` gain `motion`, `media`, `theme`, `import`; `ACTION_IDS` the 51 ids (220 in all; `GS5_ACTION_IDS`; `GS3_ACTION_IDS` sliced to its 64); `NO_REVISION_WRITES` gains `prefs.set`, `dictionary.add`, `dictionary.remove`, `chat.send`, `chat.clear`, `version.delete`, `import.pptx`; `viewStateSchema` gains `step` and `steps`; `slideListRow.template` widened; the widened rows `deck.create` (`from` any template id), `deck.set` (`/language`, the four records refused with the reason in the description), `deck.info` (`page` with inches and EMU, `language`), `deck.guides` (`colors`), `slide.import` (`sourceFile`, `slideIndexes`, `sourceTemplateId`, `keepTheme`, `after` defaulting to the last slide, one source), `render.slide` (`svg`, `text`), `view.goto` and `view.present` (`step`), `export.run` (`odp`, `motion`, `media`, `autoplay`, `loop`, `layout`, `paper`, `orientation`, `order`, `hideBackground`), `export.check` (`page`, `libreoffice`), `build.run` (`motion`, `autoplay`, `loop`, `media`), `block.adjust` (`reflection`, `recolor`), `text.list` (`start`, `prefix`, `suffix`), `text.indent` (`firstLine`, `hanging`, alone or beside `by`/`to`), `table.cellStyle` (`edges`); `line.set` already carried `points` at `BASE` (B4's request row is void). The 51 new rows each with input, output, CLI usage, MCP name and example; `font.list`'s MCP name `deck_list_fonts` is this day's (A5 names none).
- `actions-gs5.test.ts` (new) pins the ids, groups, transports, MCP names, CLI words, the baseRevision rule and the widened inputs; `actions-gs3.test.ts`'s table count reads 220 with the reason. `package.json` exports the twelve new subpaths.

The identity, the realtime protocol, the dispatchers and the surfaces:

- `packages/identity/src/principal.ts`: `PrincipalRecord.preferences?: Preferences`, parsed through `preferencesSchema` in `parsePrincipalRecord` (a malformed record is refused as an unknown key is).
- `packages/realtime/src/channel.ts` and `protocol.ts`: `EntryKind` gains `chat`, `ChatMessage`, `Entry.chat?`; `ENTRY_KINDS`, `CHAT_TEXT_MAX_CODE_POINTS` 4000, `chatMessageSchema`; `hasPayload` and the three entry schemas carry `chat`.
- `apps/cli/src/actions/deps.ts` and the twelve lane modules; `store-actions.ts` `registerLaneActions` spread at the end of `registerStoreActions` in the merge 2 order; `apps/studio/src/server/actions.ts` spreads them again after `registerAdminActions` with `hosted: { deckId, request, origin }`; `apps/cli/package.json` exports `./actions/*`.
- `packages/render/src/motion.ts` (new): `compileMotion` answering `emptySchedule(slide.transition)`, `motionCss` answering `''`, `stepCount`, `ParagraphCounter`; `theme-css.ts` (new): `themeCss` answering `''`, `themeCssScope`, `hasThemeEdits`; `blocks/render-block.ts` draws `media`, `spotlight` and `equation` as `renderSeamPlaceholder` (a `div.block-<type>[data-later]` with the description); `package.json` exports `./motion` and `./theme-css`. `packages/effects/src/dither.ts` `thresholdAt` draws the four new patterns as Bayer 8 until B6's stages. `packages/render/src/dither-key.ts` `PictureDitherLike.angle`, its own pattern list with the eight ids, `ResolvedDither` with `angle` optional, the key unchanged when `angle` is absent.
- `packages/export/src/scene/types.ts`: `SceneMedia`, `SceneEquation`, `SceneLine.baseline`, the seven optional `Scene` fields; `scene/extract.ts` sets them from `deckPage`, `DEFAULT_LANGUAGE`, `compileMotion` with `paragraphCountOf` (the distinct `SceneLine.paragraph` values per block), `sceneMedia`, `sceneEquations`, `themeCss`; `scene/media.ts`, `scene/equations.ts`, `ooxml/media.ts` (`rewriteMedia`), `ooxml/math.ts` (`rewriteEquations`, `mathmlToOmml`) as identities; `pptx/build.ts` calls the two rewrites after the alt text and before the grouping in Editable text mode; `check/{motion,media,equations,odf,svg}.ts` answer `{ ok: true, lines: [] }`; `check.ts` runs the three PPTX sections and carries them on `ExportCheck`; `package.json` exports the nine new subpaths.
- `packages/chrome/src/menus/model.ts`: `MenuSetting` gains `screenReader`, `braille`, `speakAloud`, `equationToolbar`, `starred`; `MenuClientHandler` gains `themeMode`, `dictate`, `fontPicker`; `Menu.setting?`; `GS5_PLANNED_EFFECTS` (38 rows: every row of 14.1 that flips this round with the effect it takes and its lane, `toolbar.font` included), `GS5_DIALOG_TITLES` (14), `GS5_PANEL_TITLES` (6). `keys.ts`: `PlannedKeyRow`, `GS5_KEY_ROWS` (22 rows: the Google rows that leave `OMITTED_SHORTCUTS`, the Turboslide additions). `strings.ts`: `ROUND_FIVE` (the sentences of 15). `__tests__/gs5-seam.test.ts` (new) pins them; `default-view-words.test.ts` walks `ROUND_FIVE`.
- `apps/studio/src/editor/controller.tsx`: `EditorMode`, `EditorSnapshot.editorMode` (`edit`), `setEditorMode`, `GS5_WINDOW_SLOTS`, the slot comment above `invoke`.
- `scripts/check.mjs`: steps 32 to 37 through `gated(cmd, files, needs)` with the file lists `STEP_32_FILES` to `STEP_37_FILES`, the `pending` runner branch and the `--list` tag; `perfectReport` and `TEMPLATES_GATE` helpers. `--list` prints 37.
- `packages/spelling/` scaffolded; `tsconfig.json` references it.
- `AGENTS.md`: the round five dev server exception with the ports 4351 to 4358, the catalog entries, the ownership and shared file rule, the acceptance lines, the seams.
- `packages/mcp/src/tools.ts` `compactOutputSchema` weighs a served property with the definitions it reaches (item 7.12).
- `apps/cli/src/cli.ts` USAGE names the round five export flags and the `.odp` and `.svg` check forms (item 7.13).
- `packages/theme/brand/facts.json` and `apps/studio/src/components/home/facts-data.ts` regenerated through the generators (item 7.14).

## 5. Dependencies

`pnpm-workspace.yaml` catalog, each pinned exactly, `pnpm install` once (23 packages added, the lockfile +236 lines; pnpm appended `web-vitals@6.2.2` to `minimumReleaseAgeExclude` as it did for zod), `node scripts/check.mjs --audit` afterwards: 0 advisories at high or above, 0 open.

| Package                                                                                                                     | Version                                         | Attached to                                   | Source of the version                                       |
| --------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- | --------------------------------------------- | ----------------------------------------------------------- |
| `@xmldom/xmldom`                                                                                                            | 0.9.12                                          | `packages/import` (B3's reader)               | SPEC-5 0.26                                                 |
| `temml`                                                                                                                     | 0.13.5                                          | `packages/render` (B6's equation block)       | SPEC-5 0.43                                                 |
| `nspell`                                                                                                                    | 2.1.5                                           | `packages/spelling` (B5)                      | SPEC-5 0.35                                                 |
| `@types/nspell`                                                                                                             | 2.1.6                                           | `packages/spelling` devDependencies           | the newest release on 2026-09-15 (nspell ships no types)    |
| `dictionary-en`, `dictionary-en-gb`, `dictionary-fr`, `dictionary-es`, `dictionary-pt`, `dictionary-pt-pt`, `dictionary-nl` | 4.0.0, 3.0.0, 3.0.0, 4.0.0, 4.0.0, 2.0.0, 2.0.0 | `packages/spelling`                           | the newest releases on 2026-09-15 (SPEC-5 names no version) |
| `fontkit`                                                                                                                   | 2.0.4                                           | `packages/export` (B4's SVG outline mode)     | SPEC-5 0.33                                                 |
| `web-vitals`                                                                                                                | 6.2.2                                           | `apps/studio` (the integrator's `api/vitals`) | the newest release on 2026-09-15 (SPEC-5 names no version)  |

No `allowBuilds` entry was needed (none of them has an install script). One workspace edge joined: `@turboslide/spelling` depends on `@turboslide/schema`. B6's `Temml-Local.css` and the Latin Modern Math font are files under `packages/render` or `packages/theme`, not catalog entries (MILESTONES-5 B6 "Requests"); the fonts venv (`.turboslide/venv`, present on this machine), `ffmpeg` (present), `cargo` with `wasm-bindgen` (present), `soffice` (absent) and Docker (present, 249 GB free) are the toolchain facts for the lanes.

## 6. Commands run and their results

| Command                                                                                                                                                                            | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `node_modules/.bin/tsc -b` (after every batch of the seam)                                                                                                                         | exit 0 each time once the batch settled; 19.1 s wall on the first full pass with 153 errors from the first asset union attempt (item 7.1), 0 on every later pass                                                                                                                                                                                                                                                                                                                                                                                                 |
| `cd packages/schema && ../../node_modules/.bin/vitest run`                                                                                                                         | 25 files, 509 tests passed (three pinned counts extended: the dither annotate groups with `angle`, the GS3 table at 220, the catalog's media `make`)                                                                                                                                                                                                                                                                                                                                                                                                             |
| `cd packages/chrome && ../../node_modules/.bin/vitest run`                                                                                                                         | 55 files, 501 tests passed after two fixes (`placeholder` left unannotated so the inspector generator's field list holds; the seam test allows the one row already Now)                                                                                                                                                                                                                                                                                                                                                                                          |
| `cd packages/mcp && ../../node_modules/.bin/vitest run`                                                                                                                            | 5 files, 42 passed (after the weight fix of item 7.12; before it `deck_theme_get`'s served outputSchema was 326,038 bytes against the 32 KB budget)                                                                                                                                                                                                                                                                                                                                                                                                              |
| `cd packages/agent && ../../node_modules/.bin/vitest run`                                                                                                                          | 13 files, 295 passed (`coverage.test.ts` with `GS5` landed and the seven no base ids; `gs5-seam.test.ts` new)                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `cd packages/theme && ../../node_modules/.bin/vitest run`, `cd apps/studio && ../../node_modules/.bin/vitest run src/components/home`, `node scripts/build-home-assets.ts --check` | 79, 26 passed; the check reads 48 files and `facts-data.ts` matching `facts.json` (item 7.14)                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `cd apps/cli && ../../node_modules/.bin/vitest run src/commands/export.test.ts`                                                                                                    | 4 passed (the USAGE names every generated export flag, item 7.13)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `pnpm install`                                                                                                                                                                     | once; +23 packages, 22 added, 3.1 s; `pnpm install --frozen-lockfile` (step 1) "Already up to date" afterwards                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `node scripts/check.mjs --audit`                                                                                                                                                   | 0 advisory(ies) at high or above, 0 accepted, 0 open, 0 expired                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `pnpm generate:contracts`                                                                                                                                                          | wrote 11 of 14 files (`cli.json`, `mcp-tools.json`, `describe.json`, `openapi.json`, `manifest.json`, `llms-full.txt`, `docs/grammar.md`, the four skills' references); `openapi.json` grew from 63,004 to 82,130 lines because every action that answers a slide inlines the slide schema and the block union grew (item 7.15)                                                                                                                                                                                                                                  |
| `node scripts/build-brand.ts --facts`, `node scripts/build-home-assets.ts`                                                                                                         | `facts.json`: 220 actions, 194 MCP tools, 201 HTTP paths, 21 layouts, 135 shape presets, 17 materials, 37 check steps, 3,369 parity rows, 190 measured rows from `verification-4/perf-budget-production-ship-2026-09-14.json`; `facts-data.ts` rewritten (10 lines), `brand-manifest.json` 2 lines (item 7.14)                                                                                                                                                                                                                                                   |
| `node scripts/check.mjs --list`                                                                                                                                                    | 37 rows; 32 to 37 tagged `[not yet: N file(s) pending]`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `node scripts/check.mjs --only 1,2`                                                                                                                                                | ok in 0.3 s and 0.9 s                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `node scripts/check.mjs --only 32,33,34,35,36,37`                                                                                                                                  | six "not yet; waiting for …; the command is: …" lines, exit 0 in 0.0 s (no server started)                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `node scripts/check.mjs --only 4,5,6` (first run)                                                                                                                                  | 4 ok (0.4 s); 5 FAIL after 77.4 s with 11 tests in 4 files (the six no base examples in `coverage.test.ts`, the export USAGE flags, the MCP served budget on `deck_theme_get`, the three `facts.json` counts), every one fixed above and re-run alone green                                                                                                                                                                                                                                                                                                      |
| `node scripts/check.mjs --only 4,5,6` (second run, after the fixes)                                                                                                                | all three passed in 103.5 s: step 4 `pnpm exec tsc -b` ok in 19.7 s; step 5 `pnpm test` ok in 77.6 s with 290 files, 3,093 tests passed and 2 skipped; step 6 (`pnpm build`, the client bundle check, the greps) ok in 6.2 s, the largest client chunk `index-*.js` 579,662 B under the 600,000 ceiling, 25 innerHTML call sites and 13 `overwrite: true` in the allowlists                                                                                                                                                                                      |
| `node_modules/.bin/prettier --check` over the 104 files this day changed or created                                                                                                | clean (the generated files were not formatted by hand)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `node scripts/probes/new-write-probe.mjs --base http://localhost:4321`                                                                                                             | 27 of 27 rows ok on a dev server started from `apps/studio` as `node_modules/.bin/vite dev --port 4321 --strictPort` with `playwright.config.ts`'s environment on the file store (`untitled-20260915-6otg`: twelve edits each at the next revision, the slide switch on one stream, the reload with zero collaborator marks and one roster row, the second tab as one other participant, the finding 33 keystroke kept, trashed and deleted forever); run under `.turboslide/e2e.lock` (free, taken at once, released); the server stopped and `decks/` as found |

## 7. Deviations and decisions, recorded for the lanes, the verifier and Kevin

1. `Deck.media` beside `Deck.assets` (SPEC-5 0.16, 1.2 say one map holding pictures and media as a union). The union was tried first: with `Deck.assets: Record<AssetId, Asset | MediaAsset>` `tsc -b` reported 153 errors, about 130 of them `twins`, `alt`, `variants`, `treatment` and `Asset` narrowings at 96 picture reader sites in 40 files across every lane (`store`, `render`, `export`, `headless`, `materials`, `lint`, `chrome`, `viewer`, `cli`, `studio`). Moving every reader on day 0 would have put the integrator's hand in every lane's files before day 1 and made merge 1 a conflict on each. The record therefore lives in its own map with the same ids: `MediaBlock.source.asset` and `MediaAsset.poster` name ids, `asset.set` and `asset.remove` carry both records and the reducer routes by `kind` (`MUTATION_OPS` unchanged, 1.1), `diffDecks` reads both maps, the bundle scan and the standalone build read `deck.media` explicitly (B2), and `validate/media.ts` checks a media source against `deck.media` (the catalog entry's `assetPaths` names the poster alone so the picture `reference` check never fires on a media source). `DeckAsset`, `isMediaAsset`, `isPictureAsset`, `pictureAssets` and `pictureAssetOf` exist so B2 can fold the two maps at merge 1 with the reader list if B2 prefers the union; that is B2's call by request.
2. `SpotlightBlock.placeholder` (1.2) is `picture`: `placeholder` on every block is the custom layout's placeholder kind (R03 4.1, `BlockBase.placeholder`) and one name cannot carry both. The catalog entry's `assetPaths` reads `/picture`.
3. `SlideBase.template` widens to `LayoutId | CustomLayoutId` with `custom-<slug>` ids, so `isLayoutId` guards the built in readers: `layouts.ts` `derivedLayout` reads a custom id as `blank`, `canvas.ts` `toCanvas` and `fromCanvas`, `render/slide.ts`'s block context and `store-actions.ts`'s `slide.toCanvas` rows take the built in id alone. B6's Apply layout on a custom layout lands over these guards.
4. `PictureDither.angle` is optional on the resolved record too (`ResolvedDither = Required<Omit<PictureDither, 'angle'>> & { angle?: number }` in the schema and the same shape in `render/dither-key.ts`), and `resolveDither` copies it only when written, so the sha256 variant key of every existing dither is byte identical to `BASE` and no committed twin or `Asset.variants` record moves (0.7). The four new patterns draw as Bayer 8 in `effects/dither.ts` until B6's stages (day 6); `render/dither-key.ts` spells the eight pattern ids itself because the module stays out of the schema's runtime graph (SPEC-4 3.12).
5. `DeckGuides.colors` is `Record<string, Color>` keyed `x:800` or `y:450` (1.2 writes `Record<number, Color>`): a guide has an axis and a position and no stable index once one is removed. B5's Edit guides dialog reads and writes the key; `deck.guides` gains `colors` with `null` clearing one.
6. `BlockBase.placeholder` is not annotated for the inspector: annotating it put a Placeholder control on every block's inspector (`inspector-generate.test.ts` pins the field list), while the placeholder belongs to the theme mode alone (9.2); B6's `theme-mode.tsx` draws its own control.
7. The reducer's `DECK_SET_ROOTS` admits `language`, `page`, `themeEdits`, `importedThemes` and `customLayouts` so `deck.setPageSize`, `theme.*`, `layout.*` and `theme.import` write their records as `deck.set` mutations (1.1: no new mutation op), while the `deck.set` action's pointer regex stays closed to the four records (0.30, 0.44) and admits `/language` (7.1).
8. A Later row carries no effect (`menu-model.test.ts` "gives every now item an effect, every later item a stub clause and no effect"), so "the rows still later with their final effects" is `GS5_PLANNED_EFFECTS`: every row of 14.1 with the effect it takes and its lane, the rows themselves unchanged. The same reading gives `GS5_KEY_ROWS` for the key rows (the Google rows stay in `OMITTED_SHORTCUTS`, whose greyed set `shortcuts.test.ts` pins), and `GS5_DIALOG_TITLES` and `GS5_PANEL_TITLES` for the titles. A lane flips a row in one edit and moves the count tables and the greyed set in the tests it names.
9. `toolbar.font` exists at `BASE` in `packages/chrome/src/menus/toolbar-tails.ts` (line 150) as a Now control with `enabled: 'never'` and `disabledReason: FONT_DOC` (`menus/__tests__/toolbar-tails.test.ts:63` pins `never`); A5's "Font dropdown row as later" is satisfied by that row plus its planned effect `client('fontPicker')`. B7 flips `enabled`, wires the picker and the More fonts dialog, and moves the test.
10. `EXPORT_OPTIONS` gained the eight rows of 1.2 as data (`motion`, `media`, `layout`, `paper`, `orientation`, `order`, `hideBackground`, `autoplay`); no surface reads the list today except the CLI help and the skills, so the Download dialog draws nothing new until B4 and B1 wire their rows.
11. `text/spelling` (1.2 "IssueCode … `text/spelling` (lint)") is a lint rule id, not a validator code: `RULE_IDS` lives in `packages/lint/src/contracts.ts` and the generator refuses a rule with neither a planted fixture slide nor a test, so the row lands with B5's `packages/lint/src/static/spelling.ts` and its fixture (day 4). The seven validator codes landed.
12. `packages/mcp/src/tools.ts` `compactOutputSchema` now weighs a property together with the definitions it reaches through `$ref` (transitively): `theme.get`'s `customLayouts` and the `layout.*` outputs reference the recursive Block union through `#/definitions/…`, a few hundred bytes of property carrying 300 KB of definitions, and the served list's 32 KB per tool budget (VERIFICATION-2 finding 16) read the fragment alone. The full schemas stay in the committed contracts; `tools.test.ts` passes with 194 tools.
13. `apps/cli/src/cli.ts` USAGE names the round five export flags (`--motion`, `--media`, `--autoplay`, `--loop`, `--layout`, `--paper`, `--orientation`, `--order`, `--hide-background`, `export odp`, `export html`) and the `.odp` and `.svg` check forms (`--page`, `--libreoffice`), each line saying which lane lands it, because `export.test.ts` asserts the help names every generated flag. The branches behind them are B1's, B2's and B4's.
14. `facts.json` and `facts-data.ts` were regenerated through `build-brand.ts --facts` and `build-home-assets.ts` (no count typed by hand, the orchestrator's ruling 5): `brand.test.ts` compares the file with the tree in step 5, so the day 0 tree carries 220 actions, 194 MCP tools, 201 HTTP paths and 37 check steps. The generator adopted the newest perf run (`verification-4/perf-budget-production-ship-2026-09-14.json`, 190 rows) and the parity audit of 2026-09-15 (2,979 pass, 5 fail, 385 skip) as the tree holds them; the verifier's `verification-5/` files are outside its search folders until the ship step moves them. The README and `docs/brand.md` stay for the ship step.
15. The generated contracts grew: `openapi.json` 63,004 to 82,130 lines and `mcp-tools.json` accordingly, because the slide schema every slide answering action inlines gained three block types and the typography, table and dither fields. The served MCP list stays under its budgets (item 12); the static `/openapi.json` route serves the file as before. A `$ref` based OpenAPI is a round item for Kevin's list, not this round's.
16. Step 3 (`generate:contracts` then `git diff --exit-code`) fails as written on the day 0 tree because the regenerated files are uncommitted, as in every round; it passes by regeneration and diff (`node packages/agent/src/generate/main.ts --check` reads "every committed contract is current" against the working tree). Step 29's `build-brand.ts --check` was not run this day (it launches Chrome for the card compare); its facts branch reads the regenerated file.
17. `check.mjs` steps 32 to 37 are gated on files, not on the verifier's rows: a step runs once every file its lane lands exists and is otherwise `pending`, printed with its command and exit 0. The file lists (`STEP_32_FILES` to `STEP_37_FILES`) are the integrator's; a lane whose file name differs from the list asks for the list to move. Step 37 needs the node-server build (the probe) and runs the fonts tests beside it; B7's two specs are B7's acceptance on 4358, not the step.
18. `GS3_ACTION_IDS` is now `ACTION_IDS.slice(indexOf('presence.list'), indexOf('motion.setTransition'))` so it stays the 64 rows of round three; `actions-gs3.test.ts` reads the table at 220 with the reason in its title.
19. The `media` catalog entry's `make` writes `source: { asset: '' }` (the empty reference of the figure layouts) so the palette's insert parses and the validator skips the reference; B2's Insert audio and Insert video dialogs replace the empty reference with the stored record.
20. The four `later` rows SPEC-5 14.1 lists as "new" (`block.animate`, `insert.equation`, `view.equationToolbar`, `tools.accessibilitySettings.speakAloud`) do not exist at `BASE` and are not added on day 0: a new row moves the count tables of `menu-model.test.ts` and the parity audit's fixture; each lane adds its row with its flip. `view.motion` exists as an Omit row.
21. A request path the lanes will need and the integrator answers the same day: `apps/studio/src/server/agent-actions.ts` `SERVER_SIDE_WINDOW_ACTIONS` for the round five ids whose window form must run on the server (the template and building block readers, `import.pptx`, `theme.import` from a file, `media.insert` with a file, url or upload, `media.info`, `media.list`, `media.poster`, `prefs.*`, `dictionary.*`, `version.delete`, `font.list`); `apps/studio/src/server/headers.ts`'s CSP and Permissions-Policy rows (B2 owns the file; A8 row 6 is the integrator's); `packages/chrome/src/lib/lazyDialog.ts` and `EditorShell.tsx`'s dialog and panel registries for the fourteen titles and six panels; `apps/studio/src/routes` for `/decks/templates`, `/help/training`, `/help/updates`, `/api/define`, `/api/vitals`, `/og/deck/<id>.png`; `NOINDEX_ROUTES`, the prerender list and the perf budget's route list.

## 8. For day 1 and merge 1

- B4's page sweep (6.1) reads `deckPage`, `DEFAULT_PAGE` and `pageSchema` from `@turboslide/schema/render`; `grid(page)` and `geometry(page)` are B4's in `packages/theme/src/tokens.ts` and `packages/render/src/geometry.ts`; `validate/page.ts` already bounds the guides by the page and `GUIDE_MAX` in `deck.ts` is the page cap.
- B1's `compileMotion` replaces the body of `packages/render/src/motion.ts` under the same signature; `paragraphCountOf` in `scene/extract.ts` is the exporter's count (1.3); the reducer's two `normalizeMotion` call sites are in and `reduce.test.ts` gains B1's assertions.
- B2's `MediaAsset` runtime reads `deck.media` (item 7.1); the media block's `source.asset` check is `validate/media.ts`'s.
- B3's `package.ts`, `xml.ts`, `theme.ts` and `inherit.ts` land under `packages/import/src/pptx/` with `@xmldom/xmldom` already attached; `ImportReport` is `@turboslide/schema/import-report` (widenings by request); `TemplateIndexEntry` and `BuildingBlock` are `@turboslide/schema/building-blocks`.
- B5's `Preferences` is `@turboslide/schema/preferences` with the defaults and the twelve rows; `PrincipalRecord.preferences` parses through it; `packages/spelling/src/index.ts` is the placeholder to replace.
- B6's `themeCss` replaces the body of `packages/render/src/theme-css.ts`; `THEME_IDS` and `tokensFor(id)` are B6's in `packages/theme/src/themes.ts`; `THEMES` in `deck.ts` already names `ts-plate`.
- B7's `FONT_IDS` is `@turboslide/schema/fonts`; the catalog's test asserts the two lists agree; `Typography.family` parses today.
- Merge 1 runs `tsc -b`, `pnpm test`, `check --only 1,2` and `--only 4,5,6`, the `/new` boot probe on 4321, `compare-to-shoot` at 0.5 percent and `canvas-fidelity` unchanged, then `pnpm generate:contracts` once.

## 9. Merge 1 (day 2, 2026-09-15)

MILESTONES-5 "Integrator" Delivers item 2 over the shared checkout: `BASE` `d5a1be5` plus the day 0 files, the seven lanes' day 1 (`b1.md` to `b7.md`) and the requests below. No branch, no commit, no git write command; `pnpm install` ran once because three dependency lists changed; `pnpm generate:contracts` ran once at the end of the edits. The tree afterwards: 180 tracked files changed and 105 untracked entries against `BASE`; `git rev-parse HEAD` still `d5a1be5`, nothing staged. `BUILD-STATUS-5.md` "Merge 1" carries each lane's exports and the gate table; this section carries the request by request record, the edits file by file, the collisions read and the decisions.

### 9.1 The requests, one row each

| From | Request                                                                                                                              | To               | State at merge 1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------ | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1   | R1 `RenderOptions.motion`, `data-block` under `blockAttrs \|\| motion`, `data-motion` on the slide root                              | B4               | open; `render/slide.ts` and `blocks/context.ts` are B4's and carry neither at merge 1 (MILESTONES-5 "The seams" names merge 1); B4 lands it on day 3 with the sweep's follow up and the GT deck's bytes hold (no motion in it)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| B1   | R2 `"./motion-css"` export of `packages/render`                                                                                      | integrator       | applied (`packages/render/package.json`); `@turboslide/render/motion` keeps its re-export                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| B1   | R3 the contracts pick up `MotionSchedule.slideId`, `effects[].durationMs`, `effects[].box`                                           | integrator       | applied by the one regeneration (`deck_motion_schedule` and `motion.compile` outputs in `mcp-tools.json`, `openapi.json`, `llms-full.txt`); `contracts.test.ts` green again                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| B1   | R4 (optional) `@turboslide/effects` as a dependency of `packages/render` so `motion-css.ts` imports `bayer8`                         | integrator       | not taken this merge: the output is byte identical either way and `motion-css.test.ts` pins the copy; can land at a quiet merge if B1 still wants it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| B1   | R5 the `playMedia` row rule in `media.setPlayback`                                                                                   | B2               | open, day 4                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| B1   | R6 `odp/motion.ts` reading `durationMs`, `SWITCH_MS`, `paragraph`, `hiddenAtStart`                                                   | B4               | open, day 5                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| B2   | R1 the `./media/{sniff,info,isobmff,ebml,mp3,wav}` exports of `packages/store`                                                       | integrator       | applied (`packages/store/package.json`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| B2   | R2 `@turboslide/store` as a dependency of `packages/headless`                                                                        | integrator       | applied (`package.json` and the `../store` tsconfig reference); no cycle: the store depends on the schema, `@vercel/blob` and zod alone                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| B3   | B3-1 `@turboslide/store` and `@turboslide/export` as dependencies of `packages/import`                                               | integrator       | applied (`package.json`, the two tsconfig references, the one `pnpm install`); no cycle: neither package names `@turboslide/import`, and the chain `import → export → headless → store` is acyclic                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| B3   | B3-2 `import-report.ts` widenings                                                                                                    | integrator       | applied now rather than on day 4 (every field optional, so nothing B3 wrote moves): `source.producer?`, `source.sections?`, `source.embeddedFonts?`, `source.modifyVerifier?`, `theme.scheme?: { colors: Record<string, HexColor>; fonts: { major?, minor? } }`, `validation.lines?`; B3's `report.ts` can now pass the producer through instead of stripping it                                                                                                                                                                                                                                                                                                                                                                                                                        |
| B3   | B3-3 the third Upload tab; B3-6 step 34's file list                                                                                  | integrator       | recorded, no action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| B3   | B3-4 the `ThemeRecord.colors` keys                                                                                                   | B6               | open                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| B3   | B3-5 retire `IMPORT_PPTX` from `menus/strings.ts` and the default view words test                                                    | integrator       | day 5, not before, as asked                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| B4   | R1 the Add guide rows on `guideCentre(page)`, `--ts-page-aspect` on the shell root, `toViewerDeck` page, `EditorRoot.tsx:1980`       | integrator       | day 3 with the Page setup dialog, as B4 asked                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| B4   | R2 the material source `size: [3200, 1800]` literal widened to any `2W by 2H` page                                                   | integrator       | deferred to merge 2 (B4 allowed either): the literal pins `materials/capture.ts` `FRAME_SIZE` and `mount.ts` `maxPixelCount` (B2's files) and B2's day 3 intake decides the screen with R3, so the schema, the capture and the contracts move together once                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| B4   | R3 the two tone screen `W/2 by H/2` on another page                                                                                  | B2, B6           | open                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| B4   | R4 `page` on `LiveClone` and `Stage` in `Filmstrip.tsx`, `Thumb.tsx`, `PresenterPage.tsx`, `Slideshow.tsx`                           | B1               | open, day 3                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| B4   | R5 the pointer clamp to `PAGE_CAP` in `realtime/protocol.ts`                                                                         | B5               | open                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| B4   | R6 one sentence naming `deck.page` in `agent/generate/grammar.ts`; "1600 by 900" reworded in `rules.ts`, `catalog.ts`, `position.ts` | integrator       | applied: the grammar's first sentence now says the sheet is 1600 by 900 px by default and that `deck.page` (File > Page setup, `deck.setPageSize`) sets another size; the freeform sentence, `freeform/off-sheet`'s rule text, the freeform catalog doc and `positionSchema`'s help read "the sheet (the deck's page, 1600 by 900 by default)"; `docs/grammar.md` and the two skill references regenerated (`rules.json` holds ids alone, unchanged)                                                                                                                                                                                                                                                                                                                                    |
| B4   | R7 `STEP_32_FILES`                                                                                                                   | integrator       | no move, as B4 said                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| B5   | R1 the caller's preferences record on both dispatchers                                                                               | integrator       | applied: `LaneDeps.preferences?: PreferencesStore` (`actions/deps.ts`); `records/principal.ts` gains `LocalPrincipalRecord.preferences?`, the passthrough in `readLocalPrincipal` and `localPreferencesStore(stateDir, principalId, now?)`; `write.ts` gains `stateDirOf(ctx, store)` (the helper B5 suggested lifting) and `storeDeps` answers `LaneDeps` with `preferences: localPreferencesStore(stateDirOf(ctx, store), principalIdOf(ctx.author))`; `registerStoreActions` takes `LaneDeps`; `server/actions.ts` gains `preferencesStoreFor(facts)` (`principalPreferencesStore(principalStore(), principalId)` unless the caller is `NO_PRINCIPAL` or not a principal id) spread into `registerLaneActions`; `records/principal.test.ts` (5 tests); the acceptance line ran (9.3) |
| B5   | R2 the `prefs` route in `cli.ts`                                                                                                     | integrator       | applied (the import, the table row, the USAGE line under `account`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| B5   | R3 `prefs.get` and `prefs.set` server side, the window handler writing the mirror and the shell state                                | integrator       | the server side half applied: `SERVER_SIDE_WINDOW_ACTIONS_GS5` in `agent-actions.ts` and a loop in `controller.tsx` after the GS3 one, where `prefs.set` writes the answer into `preferencesMirror(window.localStorage)`; the shell state half is R4 (day 3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| B5   | R4 the shell's preferences state, the migration on mount, the reconcile                                                              | integrator       | day 3 with the dialog, as asked (B5 names the lines then)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| B5   | R5 `Cache-Control: public, max-age=31536000, immutable` on `/dictionaries/**`                                                        | B2 or integrator | deferred to day 4 with the Worker's `?v=` fetch (B5: `max-age=60` is harmless until then); the integrator will take the route rule if B2 does not take the header                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| B5   | R6 the `./engine`, `./walk`, `./node` subpaths of `packages/spelling`; R7 `STEP_35_FILES`                                            | integrator       | R6 day 4; R7 no change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| B6   | R1 `"./themes"` export of `packages/theme`                                                                                           | integrator       | applied; `render/theme-css.ts` still imports `@turboslide/theme/theme` (node:fs) and B6 moves it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| B6   | R2 `stageFrame(deck.theme)` and `sheetRootAttributes` in `stage.ts` and `slide.ts`; the same for `thumb.ts`                          | B4; integrator   | the integrator's half applied: `render/thumb.ts` imports `stageFrame` from `@turboslide/theme/themes` (browser safe) and emits `stageFrame(deck.theme)`, byte identical to `FRAME_HTML` for both ids; B4's half open                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| B6   | R3 `#rrggbbaa` on the four alpha slots                                                                                               | integrator       | day 5, as asked                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| B6   | R4 `fontFamilyName(id)`; R5 `LiveClone` `base` prop                                                                                  | B7; B2           | open                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| B7   | R1 `font-family:var(--ts-font-<id>, inherit)` in `typographyDeclarations`                                                            | integrator       | applied after the indent line with the comment naming the definer; `typography.test.ts` (new, 2 tests) pins the property and the absence without a family                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| B7   | R2 `"./fonts"` export of `packages/render`                                                                                           | integrator       | applied                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| B7   | R3 `font.list` on the window transport                                                                                               | integrator       | applied as a row of `SERVER_SIDE_WINDOW_ACTIONS_GS5` (deviation 1 below) rather than `on('font.list', fontList)`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| B7   | R4 the `fonts list [--json]` USAGE line                                                                                              | integrator       | applied under `fonts build`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| B7   | R5 `LoadThemeOptions.fonts`; R6 the standalone faces; R8 `a:latin typeface`                                                          | B6; B1; B5       | open (days 3, 6, 5)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| B7   | R7 the runtime file glob and a served `/fonts/<id>/<file>` route                                                                     | integrator       | day 3 (hosting), as asked                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| B7   | R9 no regeneration needed for `font.list`                                                                                            | integrator       | recorded; the regeneration ran for B1's and B3-2's schema changes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

### 9.2 The edits, file by file

- `packages/render/package.json`: `./motion-css`, `./fonts`. `packages/store/package.json`: the six `./media/*` subpaths. `packages/theme/package.json`: `./themes`. `packages/headless/package.json` and `tsconfig.json`: `@turboslide/store`. `packages/import/package.json` and `tsconfig.json`: `@turboslide/store`, `@turboslide/export`. `pnpm-lock.yaml`: the three workspace edges (+9 lines over day 0).
- `packages/schema/src/typography.ts`: the `family` declaration; `typography.test.ts` new. `import-report.ts`: the six optional widenings with `hexColorSchema` from `color.ts`. `rules.ts`, `catalog.ts`, `position.ts`: the sheet prose. `packages/agent/src/generate/grammar.ts`: the `deck.page` sentence and the freeform sentence.
- `apps/cli/src/actions/deps.ts`: `preferences?`. `apps/cli/src/records/principal.ts`: the member, the passthrough, `localPreferencesStore`; `principal.test.ts` new. `apps/cli/src/write.ts`: `stateDirOf`, `storeDeps` as `LaneDeps` with the record, `recordDeps` on the helper. `apps/cli/src/store-actions.ts`: `registerStoreActions(dispatcher, deps: LaneDeps)`. `apps/cli/src/cli.ts`: the `prefs` route and two USAGE lines.
- `apps/studio/src/server/actions.ts`: `preferencesStoreFor(facts)` and the spread. `apps/studio/src/server/agent-actions.ts`: `SERVER_SIDE_WINDOW_ACTIONS_GS5` (`prefs.get`, `prefs.set`, `font.list`) joined to `SERVER_SIDE_WINDOW_ACTIONS`. `apps/studio/src/editor/controller.tsx`: the import and the round five loop with the `prefs.set` mirror write.
- `packages/render/src/thumb.ts`: `stageFrame(deck.theme)`.
- Generated: `packages/agent/generated/{mcp-tools,openapi}.json`, `llms-full.txt`, `docs/grammar.md`, `skills/turboslide-create/references/grammar.md`, `skills/turboslide-verify/references/verification.md`.
- `docs/gslides-parity/BUILD-STATUS-5.md` "Merge 1" and this section.

### 9.3 Commands run and their results

| Command                                                                                                                                                        | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `node_modules/.bin/tsc -b` (before any edit)                                                                                                                   | exit 0 (the lanes' in progress errors of the morning were cleared by their owners before the merge started)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `pnpm install`                                                                                                                                                 | once; "Already up to date" for packages, the three workspace links made (`packages/import/node_modules/@turboslide/{store,export}`, `packages/headless/node_modules/@turboslide/store`), lockfile +245 lines against `BASE`                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `node_modules/.bin/tsc -b` (after the edits)                                                                                                                   | exit 0, 18.3 s wall                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `pnpm generate:contracts`; `node packages/agent/src/generate/main.ts --check`                                                                                  | wrote 6 of 14 files (`mcp-tools.json`, `openapi.json`, `llms-full.txt`, `docs/grammar.md`, `turboslide-create/references/grammar.md`, `turboslide-verify/references/verification.md`); "every committed contract is current"                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `node_modules/.bin/prettier --check` over the 24 edited files                                                                                                  | clean                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `cd packages/schema && vitest run typography freeform actions-gs5`                                                                                             | 3 files, 27 passed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `cd apps/cli && vitest run records/principal actions/prefs commands/prefs commands/export cli`                                                                 | 6 files, 29 passed (the USAGE names every generated flag and the two new lines)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `cd packages/agent && vitest run contracts coverage gs5-seam`                                                                                                  | 3 files, 248 passed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `cd packages/render && vitest run theme-css`; `cd apps/studio && vitest run src/server`                                                                        | 12 passed; 27 files, 186 passed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `node scripts/check.mjs --only 1,2`                                                                                                                            | ok in 0.3 s and 0.9 s                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `node scripts/check.mjs --only 4,5,6`                                                                                                                          | all three passed in 84.7 s: 4 ok 0.4 s; 5 `pnpm test` ok 78.1 s, 324 files, 3,421 passed, 2 skipped; 6 ok 6.2 s, largest client chunk 580,375 B under 600,000, greps in the allowlists                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `node apps/cli/bin/turboslide.mjs prefs set /units cm --deck decks/fixture/page-16-10 --author merge1 --json`, then `prefs get /units`                         | the validated record with `units: cm`; `{ "path": "/units", "value": "cm" }`; the file `.turboslide/principals/local-merge1.json` carried `preferences.units` and `lastSeenAt` and was removed afterwards so the state folder is as found                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| the Chrome gates under `.turboslide/e2e.lock` (taken 07:28:11 with `until mkdir`, released 07:31:55 with `rmdir`; scratch `merge1-chrome-gates.sh` and `.log`) | `render all` 170 records, 0 page errors (17 s); `compare-to-shoot` 170 pairs, 0 over budget, worst 0.408 percent, mean 0.016 (the `BASE` line); `canvas-fidelity` on the three default decks 342 pairs, 0 over budget, worst 0.262 percent (`gt-brand/directions` dark), mean 0.004, 82.1 s (B4's day 1 numbers); the dev server on 4321 from `apps/studio` with `playwright.config.ts`'s environment on the file store (an own auth database under `.turboslide/`, removed afterwards), `/new` and `/decks` warmed, `new-write-probe.mjs` 27 of 27 rows ok (`untitled-20260915-1170` trashed and deleted forever), the server stopped (no listener on 4321), `decks/` as found |
| `git diff --exit-code d5a1be5 -- packages/chrome/src/tokens.css`                                                                                               | empty                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `node scripts/check.mjs --list`                                                                                                                                | 37 rows; 32 to 36 `[not yet]` with 2, 4, 4, 6, 6 files pending; 37 ungated (deviation 8)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

### 9.4 Collisions read and decisions taken

1. `font.list` runs server side (`SERVER_SIDE_WINDOW_ACTIONS_GS5`), not as `on('font.list', () => fontList())` in the page: `@turboslide/fonts/catalog` pulls the generated `catalog-files.ts` (74 rows with two digests each) into the editor's graph, and the largest client chunk sits at 580,375 B against a 600,000 B ceiling; B7's picker reads the catalog module directly as `b7.md` says, so the window row's round trip costs nothing it needs.
2. The `prefs.set` window handler writes the answered record into the localStorage mirror at once (R10 3.4's first half); the shell's `preferences` state, the mount migration and the `prefs.get` reconcile are B5's request 4 with the dialog (day 3), where B5 names the lines.
3. `readLocalPrincipal` carries a stored `preferences` member through `normalizePreferences` (an older record fills its absent members) and drops a malformed member alone, keeping the CLI's rule that a malformed file reads as fresh for the record while the member follows the schema's rule; every write path of `record-actions.ts` (`account.setName`, `account.setAvatar`, the pointer) reads and writes the whole record, so the member survives them (pinned in `principal.test.ts`).
4. `storeDeps` answers `LaneDeps` and `registerStoreActions` takes `LaneDeps`, so the preferences store flows into `registerLaneActions` with its type instead of riding an untyped field; every existing caller passes a `StoreActionDeps` and still typechecks (the extra members are optional).
5. B3-2 landed at merge 1 (wanted by day 4): all six fields optional, one regeneration, and B3 can drop the `producer` strip in `report.ts` on day 3.
6. B4's R2 deferred to merge 2: widening `[3200, 1800]` alone leaves `capture.ts` writing that size, so the schema, B2's capture and mount and the contracts move together when B2's intake and the two tone screen (R3) land.
7. B1's R4 (the `@turboslide/effects` edge on render) not taken; the pinned `BAYER8_RANKS` copy guards the output.
8. Check step 37 is no longer gated: `STEP_37_FILES` are all in the tree after B7's day 1 (`sync-stress-probe.mjs`, the catalog files), so `pnpm check` runs the probe against the node-server build on 4321 from here. B7's baseline (`b7.md` section 7) shows the probe exiting 1 on `BASE`'s reload defect (a burst typed right before a reload is lost), so step 37 reads as a failure until B7's day 3 to 7 engine work lands; that is the gate doing its job (A3 item 7) and the step is not weakened or re-gated.
9. The R6 prose keeps the default's numbers beside the name ("the sheet (the deck's page, 1600 by 900 by default)") so `docs/grammar.md` still states the default a reader needs while naming `deck.page`.
10. Two lanes in one file, read and left alone: `packages/theme/src/tokens.test.ts` (B4's `grid(page)` and `--ts-sheet-w` cases, B6's `resolve()` and frame variable case; disjoint, 94 passing); `packages/render/src/{block-css.ts,blocks/context.ts,blocks/picture.ts}` edited by B4 under the sweep rule and recorded in `b4.md` section 6 (B2's picture file: the one fallback line); `packages/schema/src/reduce.test.ts` with B1's appended block; `THIRD_PARTY_NOTICES.md` with B7's catalog section. No builder's work was reverted and no builder's file was edited by the integrator beyond the requests above.
11. Two seam rows MILESTONES-5 names for merge 1 are open with their owner: B1's `data-block` under `blockAttrs || motion` with `data-motion` on the slide root, and B6's `stageFrame` and `sheetRootAttributes` on `renderStage` and `renderSlide` (both B4's `render/slide.ts`, `stage.ts`, `blocks/context.ts`). Neither moves a GT deck pixel when it lands; B4 is asked to land them on day 3 and to run `compare-to-shoot` after.

### 9.5 For day 3

- The integrator's day 3 rows from the requests: B4 R1 (with Page setup), B5 R4 (with the Preferences dialog), B7 R7 (the fonts route and the runtime glob), B2's `SERVER_SIDE_WINDOW_ACTIONS` media ids and CSP rows on request, and every `on(...)` row a lane names.
- `SERVER_SIDE_WINDOW_ACTIONS_GS5` is the list to grow; a lane names the id and whether the page needs the answer written somewhere (the mirror pattern of `prefs.set`).
- Every lane rebases its reading on this tree: the new subpaths (`@turboslide/render/motion-css`, `@turboslide/render/fonts`, `@turboslide/theme/themes`, `@turboslide/store/media/*`), the `LaneDeps.preferences` field, the `import-report.ts` fields, and `stateDirOf` in `apps/cli/src/write.ts`.

## 10. Merge 2 (day 8, 2026-09-15)

MILESTONES-5 "Integrator" Delivers items 3 and 4 and the "Ship step" list, over the shared tree at `BASE` `d5a1be5` with the day 0 and merge 1 files, the six builder reports (`b1.md`, `b2.md`, `b3.md`, `b5.md`, `b6.md`, `b7.md`; no B4 report reached the merge, `b4.md` stands at its day 2 state) and every lane's files as they stood on the day. Merge order as the brief names it: B4, B1, B2, B6, B3, B5, B7. Nothing is committed; the tree, the preview deployment and this note are the deliverable. Section numbers refer to SPEC-5 unless prefixed.

### 10.1 The tree at merge 2

- B4: the page sweep and the `shapePath` seam of days 1 and 2 stand, and the shape interpreter landed under the seam during the day (`packages/schema/src/shapes/definitions.ts`, `ids.ts`, `geometry.ts` filled; `packages/render/src/print-layout.ts`). Not present at the merge: `apps/studio/e2e/page-setup.spec.ts`, `packages/export/src/export-odp.ts`, `packages/export/src/svg/write.ts`, the card render path in `apps/studio/src/server/thumbs.ts`, the `og:image` line in `routes/deck.$deckId.tsx`, `onMediaFile` on the viewer's drop path (b2.md R7), the `InlineText` autocorrect props (b5.md), the `data-block` and `stageFrame` requests (b6.md). Check steps 32 and 36 read `not yet` on those files, and the three menu rows that need them (`file.download.odp`, `file.download.svg`, `file.pageSetup`) stay Later (10.5).
- B1, B2, B3, B5, B6, B7: every file their reports name exists; their requests to the integrator are answered in 10.2.

### 10.2 The requests, one row each

| Lane       | Request                                                                                                                                                                   | Where it went                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1         | the window rows and the export options of motion                                                                                                                          | `controller.tsx`: `motion.play` as a window only row; `toViewerDeck` carries `withMotion(...)` and `page: deckPage(deck)`; `viewState()` answers `step` and `steps`. `apps/studio/src/server/decks.ts` `buildViewerDeck` attaches `motion` (`compileMotion` over `motionTargets`, `blockParagraphCount` and `deckMediaLength` when a slide has motion). `packages/export/src/export-pptx.ts` takes `motion?: MotionExportMode` into `buildPptx` and `themeReport` returns `built.motion`; `apps/cli/src/commands/export.ts` `--motion keep\|drop`; `commands/build.ts` `--motion`, `--autoplay <ms>`, `--loop` with `renderStandaloneFile` taking `runtime: standaloneRuntimeSource(title)` and `motionScript: standaloneMotionSource()`. `packages/export/src/check.ts` carries the round five lines. `packages/viewer/src/GridView.tsx` and `.css` draw the `.pt-thumb-motion` glyph when `motionOf(slide)` answers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| B2         | R3 the media ids, R4 the hosted intake, R6 the inspector and the dialogs, R8 `blockAdjust`, the window rows, the export and build media options, the worker frames        | `agent-actions.ts` `SERVER_SIDE_WINDOW_ACTIONS_GS5` gains `media.insert`, `media.setPlayback`, `media.poster`, `media.info`, `media.list`. `server/actions.ts` `hostedLaneDeps` composes `media: nodeMediaIntake(...)` with `media.assetKey` from the deck's access record. `FormatOptions.tsx` and `inspector/format-sections.ts`: the `textFitting` section of a media block is `MediaPlaybackSection` (`hasMediaPlayback`, `mediaSectionTitle`); `EditorShell.tsx` registers `InsertAudio`, `InsertVideo`, `Camera` and `DisplayOptions` as lazy dialogs and their cases. `store-actions.ts` `blockAdjust` loops `reflection` and `recolor` beside the three sliders. `controller.tsx`: `camera.capture` and `view.presentOnScreen` as window only rows. `commands/export.ts` `--media embed\|poster\|url`, `commands/build.ts` media data URIs (`--media`). `apps/studio/src/workers/dither.worker.ts` and `packages/viewer/src/dither.ts` carry `preview`, `pattern` and `fallback` on frames and replies. `apps/cli/src/records/access.ts` rotates `assetKey` on `setGeneralAccess(restricted)`, `revokeLink`, `rotateLink` and `remove`. The Share dialog's interim sentence: never present in `Share.tsx` at merge 2, so nothing to remove.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| B3         | B3-5 `IMPORT_PPTX`, B3-7 `LaneDeps.imports`, B3-9 the ids, B3-10 `slide.import` routing, the Upload tabs and the bundle route, the templates gate, the nested group paths | `menus/strings.ts` drops `IMPORT_PPTX` and `default-view-words.test.ts` its row. `actions/deps.ts` `LaneDeps.imports?: ImportLaneDeps`; `write.ts` `storeDeps` composes `importLaneDeps`; `hostedLaneDeps` composes it with `allowPaths: false` and `readUpload`. `SERVER_SIDE_WINDOW_ACTIONS_GS5` gains `import.pptx`, `theme.import`, `template.list`, `template.slides`, `buildingBlock.list`, `buildingBlock.insert`. `store-actions.ts` `slide.applyLayout` takes `SlideApplyAnyLayoutInput` and routes `custom-<slug>` ids to `slideApplyCustomLayout`; `blockGroup` uses `groupPathOnGroup`, `blockUngroup` `innerGroupPath`. `routes/api/decks.bundle.ts` gains the `.pptx` branch (`importPptxIntoFolder` then `pushDeckDir`, 201 with `editUrl`); `EditorRoot.tsx` `uploadBundleFile` posts a `.pptx` with `x-turboslide-file-name` and stashes the report for the `ImportReport` dialog on mount; `EditorShellInput.uploadBundle` widened to `Promise<{ id; report? }>`. `EditorShell.tsx` registers `ImportTheme`, `ImportReport`, `TemplatesPane` and `BuildingBlocksPane`. `server/decks.ts` `isTemplateId` accepts every id of `readTemplateIndex`. `scripts/check.mjs` step 34 lints every template of `templates.json` except `gt-brand` (27 static findings that predate the round) and `blank` with `--layers static`. `vite.deploy.config.ts` prerenders `/decks/templates`. `hosted-smoke.mjs` `pptxImportRow` and the `/decks/templates` row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| B5         | R4 the Preferences dialog and the mirror, R9 the CLI routes, R11 the rule row, the ports, the window rows, the dialogs and panels, the help routes, the audit page lists  | `EditorShell.tsx` holds the preferences state (`preferencesMirror(localStorage)`, `writePref`) and derives `effectiveSettings`; `EditorRoot.tsx` `useMirroredPreferences()`; `controller.tsx` `prefs.set` dispatches `ts-preferences`. `cli.ts` routes `spelling`, `dictionary`, `chat`, `text autocorrect`, `version delete` (USAGE heading reworded, 10.4). `LaneDeps` gains typed `spelling`, `define`, `dictionaryFile`, `versions`, `chat`; `storeDeps` composes `spelling`, `dictionaryFile`, `versions` (removes the version file); `hostedLaneDeps` composes `documentSpelling({ corrections: CORRECTIONS, ignore: [...PROPER_NOUNS, ...PRODUCT_TOKENS], refs: deckTextRefs, personal })`, `defineWord`, a `versions.remove` that refuses, and the chat port over `roomFor` (`authorize(ctx, deckId, 'comment')`, `checkChatMessage`, `checkQuota('chatMessagesPerMinutePerDeck')`, `chatEntry`, `appendWithRetry`, `chatMessagesOf` with `REPLAY_MAX_ENTRIES`, `logChatMessage`). `SERVER_SIDE_WINDOW_ACTIONS_GS5` gains `text.autocorrect`, `spelling.check`, `spelling.replace`, `dictionary.*`, `chat.*`, `version.delete`; `spelling.ignore` and `accessibility.verbalize` are window only rows. `EditorShell.tsx` registers `Preferences`, `PersonalDictionary`, `Guides`, `IndentationOptions`, `RestartNumbering`, `PrefixSuffix`, `DeleteVersions`, `SpellCheck`, `Chat` and `Dictionary`, the toggles `spellcheck`, `screenReader`, `braille`, `speakAloud`, `starred` through `writePref`, `<VerbalizeRegion on={preferences.accessibility.screenReader} />` and the `ACCESSIBILITY` menu under `screenReader` (`menus/model.ts` `visibleMenus` gates by `menu.setting`). `describe().state.language`; `NotesPane` takes `language`, `preferences`, `dictate`. `vite.deploy.config.ts` prerenders `/help/training` and `/help/updates`; `tooltip-audit.mjs` and `layout-shift-audit.mjs` list the three routes; `hosted-smoke.mjs` `chatRow`, `/help/training`, `helpUpdatesRows`, `/api/vitals`. `docs/hosting.md` section 13 documents `TURBOSLIDE_DICTIONARY`. The Starred view of the home page (7.7; b5.md section 2 "the home page's Starred view reads `preferences.starred`"): `apps/studio/src/routes/decks.index.tsx` gains the `home.starred` toggle beside the search (Google's word, `HOME.starred`), the list filtered to the mirror's `starred` ids read after hydration and on `ts-preferences`, and its empty figure; `decks.css` the pill; the strings in `menus/strings.ts`. `packages/agent/src/__tests__/fixtures.test.ts` waives the planted slide for `text/spelling` (10.4). |
| B6         | the theme mode, the theme css, the equation surfaces, the ids, the allowlist row, `native.yml`                                                                            | `EditorRoot.tsx` mounts `<ThemeMode …>` when `snap.editorMode === 'theme'`, writes `<style id={THEME_CSS_STYLE_ID}>` from the deck's theme edits, stamps `data-editor-mode` and `--ts-page-aspect` on the root; `EditorShell.tsx` passes `renderThemed`, `dispatch`, `revision`, `onImportTheme` to `ThemesPanel`, mounts `EquationToolbar` in the toolbar with `equationWrite: SectionWrite`, and `FormatOptions.tsx` gains the `'equation'` section (`EquationSection`); `controller.tsx` `themeMode` client handler. `SERVER_SIDE_WINDOW_ACTIONS_GS5` gains `equation.*`, `theme.*`, `layout.*`, `deck.setPageSize`. `scripts/check.mjs` `INNER_HTML_ALLOW` gains `packages/render/src/blocks/equation.ts` (Temml's MathML on the block's own element). `.github/workflows/native.yml` written with the crate patterns' note (untracked, 10.6). The `equation/parse` lint rule (b6.md R7) did not land: the `IssueCode` exists from day 0, no rule module was delivered.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| B7         | `integrator-lines.py` C1 to C6, E1, F1, F2, F3; the `movedSentence` wording; the fonts glob and the viewer's stylesheet; the transport pick; `docs/README.md` rows        | The script applied once (idempotent, refused on a second run as designed): `controller.tsx` `attachChrome`, `setShowState`, the room client's transport pick (`init.payload.room?.ws === true` picks `wsTransport` with the SSE transport as `fallback`, else SSE), `EditorRoot.tsx` E1 and F1, `ToolbarTail.tsx` `FontField`, the chrome `package.json` exports. `StaleBaseError` and `packages/schema/src/reduce.ts` carry "The presentation moved to revision N while this change was on its way" (the two tests that matched `/stale/` now match `/moved to revision/`, 10.4). `vite.deploy.config.ts` `PACKAGES_PATTERN` adds `fonts/assets/*/*` and `theme/src/ts-plate/*.css`; `apps/studio/src/components/DeckViewer.tsx` links `fontsStylesheetHref([...deck.fonts])`; `packages/viewer/src/model.ts` `ViewerDeck.fonts?: readonly FontId[]`; `buildViewerDeck` sets `fonts: usedFontIds(deck, slides)`; `apps/cli/src/deps/render.ts` adds the deck's `@font-face` css and `fontSrc` to the standalone build (`apps/cli/package.json` gains `@turboslide/viewer`, `tsconfig.json` the reference; `pnpm install` answered "Already up to date"). `hosted-smoke.mjs` `--sync-probe` runs `scripts/probes/sync-stress-probe.mjs` as a row. `docs/README.md` gains the `sync.md` and `fonts.md` rows (and `import-pptx.md`, B3's). `THIRD_PARTY_NOTICES.md`'s catalog section kept where B7 put it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Integrator | Delivers item 3                                                                                                                                                           | `menus/model.ts`: every 14.1 row whose surface exists flipped to `now` with its effect (the `ACCESSIBILITY` menu, `LANGUAGE_ROWS`, `PLACEHOLDER_ROWS`, `menuOf` falling back to the holder of a nested id), the 14.2 clauses retired except `DOWNLOAD_FORMATS`; `keys.ts`: the Google rows moved out of `OMITTED_SHORTCUTS` with the chords of the fixture, three extra bindings, `assignAccessKeys` as an augmenting path matching with an alphabet fallback; `menu-model.test.ts`, `gs5-seam.test.ts`, `shortcuts.test.ts`, `default-view-words.test.ts`, `ContextMenu.test.tsx`, `editor-shell-render.test.tsx`, `tool-finder.test.ts` updated (chrome: 65 files, 564 tests). `scripts/check.mjs` steps 32 to 37 carry the literal commands of 16.7 behind `gated(...)`, `--list` prints 37. `HomeHero.tsx` and `home.css`: `HERO_LIVE`, `HERO_RECIPE`, `heroMayGoLive`, `useLiveHero` (an idle mount of `@turboslide/materials/mount`), the `.ts-product-hero-live` crossfade. `routes/api/vitals.ts`, `components/VitalsReporter.tsx` (mounted in `__root.tsx`), `scripts/vitals-report.mjs`; `perf-budget.mjs` gains the three routes on both profiles, the async `vitals()` reader and `vitals: { inp: 200 }` on the deployment profile. The two gates of 16.7's deferred engineering row: `check-vercel-output.mjs` fails a function directory over 200 MB (`FUNCTION_GATE_BYTES`; the 250 MB cap stays the platform's line) and `perf-budget.mjs` gates the worst cold first byte of `/new` and `/decks` at 1,200 ms on the deployment profile (reported on the node-server build, where a fresh instance is not forced). `packages/realtime/src/client/ws.ts` (`wsTransport`, `wsUrl`, `parseWsMessage`, `WS_SIDECAR_PORT`; export `./client/ws`), `scripts/ws-sidecar.mjs`, `server/write.ts` `room.ws` under `TURBOSLIDE_REALTIME_WS=1` off Vercel. `.github/workflows/lighthouse.yml` and `native.yml` (untracked). `build-brand.ts --facts` and `build-home-assets.ts` regenerated (220 actions, 194 MCP tools, 201 HTTP paths, 37 check steps). `pnpm generate:contracts` run twice (after the registrations landed the tools moved).                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

### 10.3 Commands run and their results

Every command from the repository root unless a `cd` is shown; the e2e lock taken by each Playwright chain and released by the same script. The state at the time of this note (14:32 on 2026-09-15); the runs still in flight are named as such with their logs, and the ship step reads their end.

| Command                                                                                                                                                                                            | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `node_modules/.bin/tsc -b`                                                                                                                                                                         | exit 0 over the whole tree after the last edit (14:30)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `pnpm generate:contracts` then `node packages/agent/src/generate/main.ts --check`                                                                                                                  | wrote 3 of 14 files on the second run (`cli.json`, `mcp-tools.json`, `openapi.json`: the lane registrations moved the implemented tools); "every committed contract is current"; step 3 fails as written until the ship step commits them                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `node scripts/check.mjs --only 4,5,6` (before the fixes)                                                                                                                                           | step 5 failed with 35 tests in 11 files; the duplicate `camera` discriminator, the stale contracts, the `text/spelling` fixture entry, the `catalogFace` bold rule and the `rIdundefined` relationships were fixed from that list (10.4)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `node scripts/check.mjs` (steps 1 to 3), `--from 4`, `--from 6`                                                                                                                                    | 1 ok 0.3 s, 2 ok 1.1 s, 3 FAIL (the uncommitted contracts, recorded); 4 ok 27.8 s; 5 FAIL 109.5 s (B4's five files, 10.5 item 2, and four suites that time out under load and pass alone: `apps/cli/src/commands/canvas.test.ts`, `commands/mcp.test.ts`, `packages/mcp/src/tools.test.ts`, `packages/spelling/src/engine.test.ts`); 6 ok 2.2 s after the two allowlist rows (28 innerHTML sites, 16 `overwrite: true`; client 14.2 MB, server 15.6 MB); 7 ok 3.1 s; 8 ok; 9 ok 1.4 s; 10 ok 22.8 s (170 records); 11 ok; 12 ok (`compare-to-shoot` on the GT deck at the 0.5 percent budget); 13 to 18 ok; 19 ok 35.6 s after the formatter pass; 20 FAIL 49.8 s (the verifier's audit read `MENUS` as the bar, fixed in 10.4 item 9)                     |
| `node scripts/check.mjs --from 20` (queued behind the e2e lock, `scratchpad/check-chain2.log`)                                                                                                     | in flight at the time of this note; steps 32 and 36 answer `not yet` on B4's three missing files by construction; the ship step reads its end                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `vercel deploy --yes --archive=tgz` (the merged tree at 14:00)                                                                                                                                     | `https://turboslide-1c19vi1gj-kl01s-projects.vercel.app`, "Build Completed in /vercel/output [47s]", 4 pages prerendered (`/home`, `/help/training`, `/help/updates`, `/decks/templates`); the deployment every probe below measures                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `vercel deploy --yes --archive=tgz` (the final tree at 14:31, after the Starred view, the gates and the audit edit)                                                                                | https://turboslide-ho9rltx7u-kl01s-projects.vercel.app                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `node scripts/hosted-smoke.mjs --base <preview>` (no token)                                                                                                                                        | 1 of 39: every request answered 302 (Vercel Authentication), the reason `VERCEL_OIDC_TOKEN` rides on every probe                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `vercel env pull <scratch>/.env.oidc --yes`, then the smoke with the token in the environment                                                                                                      | 39 of 39 passed against the first preview: the eight page rows plus `/decks/templates` (200, the gallery heading), `/help/training` (200, 64,362 chars), `/api/vitals` (200, the endpoint's description), `/help/updates` twice (200 MISS then HIT), the asset, `/api/agent` 401, the three header rows, the attachment, the cross site POST, the unsigned thumbnail, the CSP endpoint, the fifteen `cdn` rows, the two thumbnail rows; skipped: the bearer rows (`effects backend`, `template copy`, `pptx import`, `chat message`; the pulled development environment holds no `TURBOSLIDE_TOKEN`), the share and publish token rows, the private document and twin rows, the restricted row, the sync probe (run with `--sync-probe` in the lock chain) |
| `POST /api/vitals` three times, `GET /api/vitals?report=1`, an oversize body, `node scripts/vitals-report.mjs --base <preview>`                                                                    | 204, 204, 204; `{"samples":3,"routes":{"/edit":{"INP":{"p75":120,"count":3,"good":3}}}}`; 413; the report table with the 200 ms ceiling column                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `node scripts/probes/editor-walk-probe.mjs --base <preview> --json docs/gslides-parity/verification-5/walk-merge2.json` (under the lock)                                                           | in flight: 228 rows ok and 0 failed at the time of this note (`scratchpad/preview-chain.log`); the JSON lands at the end of the run                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `node scripts/hosted-smoke.mjs --base <preview> --sync-probe` and `node scripts/perf-budget.mjs --base <preview> --profile deployment --runs 3 --write --json .turboslide/perf-budget-merge2.json` | queued behind the walk probe in the same lock chain (`scratchpad/smoke-full.log`, `perf-deployment.log`); the scratch deck of `--write` is named in the perf JSON and is the ship step's to remove with `deck.trash` and `deck.remove` if this session ends first                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `git diff --exit-code d5a1be5 -- packages/chrome/src/tokens.css`                                                                                                                                   | empty                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `pnpm format:check`                                                                                                                                                                                | clean on the whole tree after the pass of 10.4 item 7                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `node scripts/build-brand.ts --facts`, `node scripts/build-home-assets.ts --check`                                                                                                                 | 220 actions, 194 MCP tools, 201 HTTP paths, 37 check steps; the home assets current                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `node scripts/check.mjs --list`                                                                                                                                                                    | 37 rows; 32 (`page-setup.spec.ts` pending) and 36 (`export-odp.ts`, `svg/write.ts` pending) gated                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| the fixture export probe (`exportPptx` native over `decks/fixture/gslides`)                                                                                                                        | `passed: true`, 157 parts, 186 relationships after `registerBoxLink`; the padded shape's insets read `lIns 223571 tIns 67368 rIns 223876 bIns 136238` (B4's interpreter, 10.5 item 2)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

### 10.4 Collisions read and decisions taken

1. `packages/schema/src/assets.ts` (the integrator's schema file): `MediaAssetSource` and `mediaAssetSourceSchema` carried the discriminator `camera` twice (B2's `{ kind: 'camera'; at }` beside the day 0 `camera | recording` member), and zod refuses a discriminated union with a duplicate value at module load, so every deck parse that reached the media schema failed (the motion, media, templates, equation, import and spelling suites all showed "Duplicate discriminator value 'camera'"). The duplicate member is removed and the picture `AssetSource` gains `{ kind: 'camera'; at: string }` with its strict object, which is where a camera capture as a picture lands.
2. `packages/export/src/pptx/text.ts` `catalogFace`: B7's catalog made the export set's own instances (`GT Inter`, `Inter ...`) resolve to a catalog face, which dropped the bold rule in `report.test.ts`; the function answers `null` for them with a merge 2 note.
3. `packages/export/src/pptx/text.ts` `addSceneText`: B6's validator (SPEC-5 8.3, `ooxml/validate.ts`) names every `r:id` a slide part writes, and found `r:id="rIdundefined"` on the two text boxes of the fixture's `links` slide that carry a block link (SPEC 7.2.7): pptxgenjs 4.0.1 writes the box's `a:hlinkClick` from `options.hyperlink._rId` but creates hyperlink relationships for the runs alone, so the file was invalid at `BASE` and nobody checked. `registerBoxLink` follows `createHyperlinkRels` exactly (the next id over the three tables, a `slide` row for a jump, a `dummy` row for a URL) and writes the id back; a slide without the tables keeps the runs' links and the residual says so. The fixture export is valid again (157 parts, 186 relationships, `passed: true`); the flatten (Perfect) path is untouched because its texts are invisible and carry no box link.
4. `packages/agent/src/__tests__/fixtures.test.ts`: `text/spelling` is a static rule that runs over an injected checker (b5.md item 12, R11), so the fixture index lints the fixture deck without a dictionary and records the test file alone; the assertion that a static rule has a planted slide is waived for `INJECTED_CHECKER_RULES`.
5. `apps/cli/src/cli.ts` USAGE heading: the round five heading carried the word `gslides`, which `cli.test.ts` forbids in user facing text; it reads "Round five (Google Slides parity, SPEC-5; …)". `apps/cli/src/commands/writes.test.ts` and `packages/store/src/file-store.test.ts` matched `/stale/` in the conflict message that B7's wording rule replaced; they match `/moved to revision/`.
6. `pnpm generate:contracts` ran twice: once after the schema and menu work, once more after the lane registrations landed in the dispatchers (the tools' implemented flags moved `cli.json`, `mcp-tools.json` and `openapi.json`); `--check` answers "every committed contract is current" and step 3 fails as written until the ship step commits them.
7. Step 19: `prettier --write` over the 84 changed files that failed `--check` (every lane's; formatting only), then the 17 expected documents of the import fixtures restored to `canonicalJson` bytes with a small script (their test compares the string) and the folder added to `.prettierignore` with a note; three committed round four records (`build-4/hotfix-4/walk-preview.json`, `walk-production.json`, `verification-4/production-ship/production-selection-record.json`) were unformatted at `BASE` and are formatted now.
8. Step 6's greps: `packages/render/src/blocks/equation.ts` (Temml's MathML on the block's element) joined `INNER_HTML_ALLOW` and `apps/studio/src/routes/api/vitals.ts` (the day file mirror) joined `OVERWRITE_ALLOW`, each with its reason in `scripts/check.mjs`.
9. Step 20: the verifier's `scripts/gslides-parity-audit.mjs` (dated 2026-09-14) reads `MENUS` as the bar and so expected the Accessibility menu in the bar and tried to open `menubar.accessibility`; the bar draws `visibleMenus(ctx)` (SPEC-5 7.5). The smallest edit in the verifier's file: `BAR_MENUS = visibleMenus(DEFAULT_MENU_CONTEXT)` for the walks and the first open row, `visibleMenus(ctx, [menu])` for the per menu presence rows, with a note naming the merge.
10. B7's `integrator-lines.py` re-applied its inserts on a second run when the anchor text remained inside the inserted text; `HomeHero.tsx`, `home.css`, `__root.tsx` and `packages/realtime/package.json` were restored from `git show HEAD:<path>` (read only git) and the lines applied once; the controller's duplicate `wsTransport` import removed.
11. `apps/studio/src/server/decks.ts` `isTemplateId` accepts every id of `readTemplateIndex(join(repoRoot(), 'decks'))` rather than the two literal ids, so B3's templates open from the templates page.
12. The chat port is composed in the hosted dispatcher directly over the room channel (`roomFor`, `appendWithRetry`, `chatEntry`, `chatMessagesOf`) because B7's `admitChat` (b5.md R21) never landed; the checkout answers through the same `LaneDeps.chat` shape over the file store's room.
13. The hosted `versions.remove` refuses with a sentence (no store level version removal exists in `DeckStore`); the checkout's `storeDeps.versions.remove` removes the version file. `reauthenticated` is the agent or `identity.session.fresh`.

### 10.5 Deviations, recorded for the verifier and Kevin

1. B4's days 3 to 7 did not reach the merge: no Page setup dialog, no print layouts dialog, no ODP writer, no SVG writer, no card render path in `server/thumbs.ts`, no `og:image` line, no `onMediaFile` on the viewer's drop path, no `InlineText` autocorrect props, no `data-block` or `stageFrame` change. Check steps 32 and 36 answer `not yet` (1 and 2 files pending) and `menu-model.test.ts` asserts the Later count at 5 leaf rows and 6 over `allItems()` (`file.download.odp`, `file.download.svg`, `file.pageSetup` beside the two of 14.2 and the `file.email` container), a deviation from 14.2's 2 and 3.
2. B4's shape interpreter landed under the seam without its tests: `packages/schema/src/shapes.test.ts` ("the geometry stubs of day one", `textInset` now answers the preset's `rect` guides), `shapes/geometry.test.ts` (the box path expectation), `diagrams.test.ts` (12 rows: the `connect` codes moved with the sites), `packages/render/src/__tests__/canvas.test.ts` (12 snapshots), and `packages/export/src/gslides-fixture.test.ts` "the padded shape" (the roundRect text rectangle moved in, so `tIns + bIns` reads 203,606 EMU against the pinned 121,920) fail on the merged tree; each is B4's to update with the interpreter. `apps/cli/src/commands/canvas.test.ts`, `commands/mcp.test.ts`, `packages/mcp/src/tools.test.ts` and `packages/spelling/src/engine.test.ts` time out under `pnpm test`'s load and pass alone (recorded in BUILD-STATUS-5).
3. The Nitro upgrade route `apps/studio/src/routes/api/decks.$deckId.ws.ts` is not written (neither `crossws` nor `ws` resolves in the studio and a new dependency is a request); the flag, the client transport with the SSE fallback and the dev sidecar on 4322 are, so a checkout runs the socket path end to end and a node-server deployment falls back to SSE.
4. The hero gate is in place with `HERO_LIVE = false`: the second `perf-budget.mjs` measurement against the preview is the number that flips it, and the two-tone screen pass is not applied to the live frame.
5. The `equation/parse` lint rule (b6.md R7) did not land; the `IssueCode` exists from day 0.
6. The Share dialog's interim sentence (B2) was never present in `Share.tsx`, so item 4's removal had nothing to remove.
7. The media Range 206 row and the media grant row of 16.8 are not in `hosted-smoke.mjs`: the smoke deck carries no media asset, and a row that uploads one is B2's presigned path, which the preview cannot exercise without a bearer.
8. The templates gate (step 34) lints every template except `gt-brand` (27 static findings that predate the round) and `blank`.
9. The preview of merge 2 sits behind Vercel Authentication: every probe carries `VERCEL_OIDC_TOKEN` from `vercel env pull` (the development token of the linked project, kept in the session's scratch folder, never printed); the pulled environment holds no `TURBOSLIDE_TOKEN`, so the smoke's bearer rows are skipped.
10. `.github/workflows/lighthouse.yml` and `native.yml` are written and untracked: adding them needs a token with the workflow scope (`gh auth refresh -s workflow`), which is Kevin's.

### 10.6 For the ship step

- Commit the regenerated contracts (step 3), the formatted round four records and the two workflows (with the workflow scope).
- B4: the five test files of 10.5 item 2 and the surfaces of item 1; the three Later rows flip with their files and `menu-model.test.ts` moves to 2 and 3.
- The scratch deck of the perf run is removed after the run (10.3); a preview that outlives the session keeps nothing else.
- `HERO_LIVE`: read the deployment profile's `/home` rows of 10.3 and flip the flag if the largest chunk stays under 600 KB with the shader mounted.

## 11. Fix round (day 9, 2026-09-15)

The integrator's fixer pass over `VERIFICATION-5.md` section 13, on the shared tree at `BASE` `d5a1be5` plus every lane's uncommitted files, from 16:50 to 17:40 PDT. Nothing is committed and no git write command ran; `pnpm install`, `pnpm add`, `pnpm exec`, `pnpm build` and `vite build` did not run outside `scripts/check.mjs`. No token, cookie or secret was printed (the agent bearer of `~/.config/turboslide/hosts.json` and the `VERCEL_OIDC_TOKEN` of `vercel env pull` were read in process for the hosted probes and never written to a note). The findings this pass owns are 2, 7, 8, 9, 10, 11 and 12 of the verifier's numbering (the brief's 1 to 8); each is below with the files, the reason and the commands. Where a fix sat in another lane's file it is a seam the integrator resolved (MILESTONES-5 "Fixer round": the integrator may edit any file to resolve a seam) and is named here so the lane reads it.

### 11.1 Finding 11 (severity 3): `instanceof BlobExistsError` in the deployed bundle, and the cached rejected `ready()`

- `packages/store/src/blob-store.ts`: two guards beside the classes, `isBlobExistsError(error)` and `isBlobPreconditionError(error)`, true for an instance of the class or for any `Error` whose `name` is the class name (the deployed server bundle carries more than one copy of the module; production's own `seedOnce` threw one copy's class past the other copy's `catch` at 16:04 and 16:13). Every `instanceof` on the two classes in the store now goes through the guards: `fetchMissingTwins`, `pushDeckDir`, `putSnapshot`, the thumbnail and media puts, the index writes, `stamp`, `create`; `packages/store/src/migrate.ts`, `comments-store.ts` and `apps/studio/src/server/bundle-core.ts` too; `access-store.ts`'s `isBlobConflict` is now written over the two guards (it carried the name test alone before). `ready()` no longer caches a rejection: the promise is dropped on rejection and logged (`blob: ready failed and will be retried on the next call`), so an instance that met a transient Blob failure or the duplicated class while seeding answers the failure to the request that met it and seeds again on the next one, instead of answering 500 to every route for its life.
- Tests: `packages/store/src/hosted.test.ts` gains "the blob collection's ready" with two rows: a `BlobExistsError` thrown by another copy of the module (an `Error` with the name and no `instanceof`) during the seed leaves `ready()` resolved and the deck seeded; a rejected `ready()` (a transient put failure on `deck.json`) is retried on the next call and the deck is listed. `cd packages/store && vitest run`: 22 files, 216 tests passed.
- Production at 17:10 PDT, read only (`GET` on the four `gt-brand` routes and `/new`; `POST /api/actions/deck.info?deck=gt-brand` with the bearer, three times): `/deck`, `/edit`, `/present`, `/print/gt-brand` and `/new` answer 500 and `deck.info` answers 500 with `{"error":{"name":"BlobExistsError",…"decks/gt-brand/assets/…"}}` on every request, over seven instance ids (`z9f88`, `r92bb`, `64cz4`, `z26bj`, `54fzj`, `htfg8`, `vg7qr`; `/decks` alone 200). An hour after the verifier stopped its requests the poisoned instances have not recycled: production is down for every deck route until it is redeployed. The redeploy is the ship step's or Kevin's (the orchestrator's ruling 1: only the ship step commits and ships); the fixed code is in the tree and the preview deployment of 11.8 runs it. Kevin should know that the seed deck stands at the repository's revision 25 (the verifier's finding 0) and that production answered 500 on its deck routes from 16:13 PDT until the redeploy.

### 11.2 Finding 2 (severity 3): the Temml font file is not in the deployment

- `apps/studio/vite.deploy.config.ts` `PACKAGES_PATTERN` gains `render/assets/*`: `Temml.woff2`, `latinmodern-math.woff2` and the two licence files travel as the `packages` server asset group and materialize under `TURBOSLIDE_PACKAGES_DIR/render/assets/`, where `equationFontPaths` (render/theme-node.ts) reads them.
- `packages/render/src/theme-node.ts` `woff2DataUri`: the read failure names the fix (the checkout's `packages/render/assets`, or the `TURBOSLIDE_PACKAGES_DIR` folder of a bundled server through the `render/assets/*` glob) instead of the bare ENOENT the merge 2 preview answered.
- `scripts/check-vercel-output.mjs`: one row per function directory, "carries the packages runtime files", looks for `render/assets/Temml.woff2`, `latinmodern-math.woff2`, the sprite, `InterVariable.woff2` and `calibration.json` as a file of that name or as the asset key inside a bundled chunk; `node scripts/check.mjs --only 30` answers 46 of 46 assertions (8 functions, 5 of 5 files each; the fonts sit at `functions/__server.func/_virtual/Temml.woff2.mjs` and beside it).
- `packages/render/assets/` (5 files, 405 KB) is untracked: the ship step commits it (11.9). The hosted export itself is read on the preview of 11.8.

### 11.3 Finding 12 (severity 2): no media resolver in the render context

- `packages/render/src/slide.ts` `renderSlide`: the context carries `media: (id) => deck.media?.[id]` and, when the new `RenderOptions.mediaSrc` is given, `mediaUrl`. No caller in the tree passed the resolver (not the editor, the show, the export capture or the standalone build), so every stored clip rendered with `data-src=""` and the show's controller had nothing to mount; the fix sits in `renderSlide` rather than in EditorRoot.tsx so every path reads the record. The GT deck has no media block, so no rendered byte of it changes (ruling 6).
- Test: `packages/render/src/__tests__/media-context.test.ts` (new): the motion fixture's media slide renders `data-src="/decks/motion/assets/bars-1s.e5f1f192.webm"`, the duration and the kind on both poster roots with no warning; `mediaSrc` rewrites the clip's URL and leaves the picture twins; a missing id warns once (`media#clip: media gone is not in the deck`) and leaves `data-src` empty. `cd packages/render && vitest run`: 25 files pass; the one failing file is B4's `__tests__/canvas.test.ts` (12 snapshots, the verifier's finding 4), unchanged by this pass.

### 11.4 Finding 7 (severity 2): the five Google shortcut rows still in `OMITTED_SHORTCUTS`

- `packages/chrome/src/menus/keys.ts`: `animation-preview`, `play-video`, `video-play-pause`, `video-rewind` and `video-forward` leave `OMITTED_SHORTCUTS` (22 rows now: 10 later, 12 omit). Five `extra` bindings: `key.present.mediaToggle` K, `key.present.mediaRewind` U, `key.present.mediaForward` O in the `present` scope under Google's "Video player" group; `key.motion.preview` Enter (editor scope) and `key.media.play` Enter (canvas scope) under Common actions, in one `SHARED_CHORDS` focus group with `key.commit` (Enter leaves crop mode, continues a waiting preview, or plays the selected media, by which is in front; each handler answers false when its surface is not). Static reading as the audit's round five row performs it: 18 planned Google rows, 0 still omitted, 18 of 18 bound.
- Handlers. `packages/chrome/src/EditorShell.tsx` `runBinding`: `key.motion.preview` calls `advanceMotionPreview()` when `motionPreview()?.waiting` is true; `key.media.play` toggles the selected audio or video block (`hasMediaPlayback`) through a media controller created over the slide root in front (`createMediaController`, mounted over the slide's poster roots, `toggle(blockId)`; Enter again pauses; torn down with the shell). `packages/viewer/src/present/presentKeys.ts`: `mediaToggle`, `mediaRewind`, `mediaForward` for K, U and O with `MEDIA_SEEK_MS` 10,000 and three rows in `presentKeyRows`; `apps/studio/src/components/Slideshow.tsx` dispatches them on the playing media of the current slide, else its first poster root, without pausing Auto-play. `packages/viewer/src/present/media-controller.ts` gains `seek(blockId, deltaMs)` (clamped to Start at and End at or the file's duration, reports the position; a YouTube player is left alone) and `motionLayer.ts`'s `MediaControllerLike` the optional `toggle`, `seek` and `stateOf`. Seam edits in B1's and B2's files, named here for them.
- Tests: `shortcuts.test.ts` pins the five rows to Google's keys, scopes and the Enter dispatch order (`key.commit`, `key.motion.preview`, `key.media.play`) and keeps K, U and O out of the editor map; `present-keys.test.ts` gains the three letters (a blank slide, a field and a modifier keep their rules) and the rows, and `k` leaves its swallowed list for `x` (it is Google's row now); `media-controller.test.ts` gains the seek row. `cd packages/chrome && vitest run --maxWorkers=2`: 65 files, 565 tests; `cd packages/viewer && vitest run src/present`: 56 tests; `cd apps/studio && vitest run`: 44 files, 288 tests.

### 11.5 Finding 10 (severity 2): the largest client chunk over 600 KB

- Cause, read by grepping the merge 2 entry chunk against the `BASE` build: the menu model and `keys.ts` (absent from the entry at `BASE`) rode it through `routes/help.training.tsx` and `help.updates.tsx`, whose `head` called `helpTitle` from `HelpPage.tsx` (with it the key table, the menu model, the home shell and the markdown reader), and the two documents' raw markdown rode with the `head`; `web-vitals/attribution` (15.6 KB) rode through `VitalsReporter` in `__root.tsx`.
- Fixes: `apps/studio/src/components/help/help-title.ts` (new) reads the first heading with the reader's rule and no import of the reader (`help-title.test.ts` pins it against `markdownTitle(parseMarkdown())` on both documents and a fenced case); `apps/studio/src/components/help/help-titles.ts` is generated by `scripts/build-home-assets.ts --help-titles` from the documents' first headings and asserted by `--check` (check step 29), and the two routes' `head` read `HELP_TITLES`, so the raw markdown lives in the split component chunk alone; `apps/studio/src/components/vitals-client.ts` (new) holds the reporter's body and the web-vitals import, loaded through `import()` on a sampled page alone (`VitalsReporter.tsx` keeps `sampled` and the mount).
- Numbers (`node scripts/check.mjs --only 6`, `pnpm build` then `check-client-bundle`): the entry `index-*.js` 725,388 B at merge 2, 602,125 B after the help title split, 599,048 B with the reader out, 586,751 B with the generated titles and the vitals client out (`BASE` 579,024); the new chunks `vitals-client` 14,436 B, `HelpPage` 5,036 B, `help.training` 7,597 B, `help.updates` 3,734 B. `check-client-bundle` still reports rather than asserts the ceiling (no `vendor-*.js` in the output; SPEC-4 0.27 and build-4/integrator.md section 17, unchanged this pass). Step 29 `[brand]` ok after the generator change.
- Not closed here: the `js decoded` rows of the perf budget (`/edit` 2,560 KB against 1,953; `/decks` 2,475 against 586; `/home` 778 against 586; `/decks/templates` 1,189; the help pages 709) were misses at `BASE` too on every route but the three new ones, and the entry cut above is the part this lane owns; the rest is each route's chunk graph (the shape interpreter in the `Frame` chunk, the editor). Recorded as a deviation for pass 2 with the baseline beside each number (VERIFICATION-5 section 8), not patched around.

### 11.6 Findings 8 and 9 (severity 2): Docker, and step 3

- Docker: not code. The VM disk is full (`docker system df`: 150 images, 100.8 GB, 56.23 GB reclaimable) and ruling 3 forbids a prune; the commands for Kevin are VERIFICATION-5 6.1. Nothing was run against Docker in this pass.
- Step 3: `node packages/agent/src/generate/main.ts --check` answers "every committed contract is current" on this tree; the diff is the ten uncommitted generated files and the step passes once the ship step commits them (11.9). No action or contract changed in this pass.

### 11.7 Finding 0 (the verifier's, severity 3): the seed deck removed and production poisoned

- The walk's `PROTECTED` set is the verifier's own change in `verification-5/scripts/`. What this lane adds is 11.1: the guards and the non-cached `ready()` remove the failure mode the incident exposed, and production's state at 17:10 PDT (every deck route 500) is recorded there for the ship step, which must redeploy production before any smoke against it is read, and for Kevin (the seed deck at revision 25; revisions 26 to 32 lost).

### 11.8 The preview deployment of the fixed tree

`vercel deploy --yes --archive=tgz` from the linked root at 17:15 PDT: `https://turboslide-eqsuvip5s-kl01s-projects.vercel.app` ("Build Completed in /vercel/output [42s]", the four prerendered pages). Behind Vercel Authentication like the merge 2 previews, so every probe carried the `VERCEL_OIDC_TOKEN` of `vercel env pull` (the scratchpad, never printed) and the agent bearer of `~/.config/turboslide/hosts.json`, both read in process. The probe (`<scratch>/probe-preview.mjs`, a `PROTECTED` set with `gt-brand` and `gt-brand-mu05h5vq`, one scratch deck `v5-fix-<rand>`):

| Row                                                             | Result                                                                                                                                                         |
| --------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/deck/gt-brand`, `/edit/gt-brand`, `/new`, `/decks`            | 200 (183,215; 296,109; 75,065; 71,370 B) on fresh instances of the fixed store code                                                                            |
| `deck.info gt-brand` (read only)                                | 200, revision 25 (the repository's, VERIFICATION-5 finding 0)                                                                                                  |
| `deck.create v5-fix-56aki` from `blank`                         | 200 (the first run passed no `from` and was refused 400 at `/from`; nothing created)                                                                           |
| `export.run pptx native` on the scratch deck                    | 200 in 8,134 ms with the report (`fontSet exact 4.001+gt.2`, the files)                                                                                        |
| `export.run pdf`                                                | 200 in 618 ms with the report                                                                                                                                  |
| `GET /api/render/title?deck=v5-fix-56aki&w=320`                 | 200 `image/png` 1,736 B                                                                                                                                        |
| `deck.trash` then `deck.remove` (`confirm: true`, the revision) | 200, 200; `/deck/v5-fix-56aki` 404 afterwards (the first cleanup attempt sent no `baseRevision` and was refused 400 twice; the deck was removed on the second) |

The three rows the merge 2 preview failed with `ENOENT … Temml.woff2` (finding 2) pass on this deployment; the four routes and `deck.info` pass on instances that booted against the restored store with the guards of 11.1 in place. Not run against this preview in this pass: the hosted smoke and the perf run (the verifier's lock chain still held `.turboslide/e2e.lock` with its Chromium at 17:20, taken 15:21, three chain scripts alive; a Playwright or probe run beside it would overlap, so the quick editor walk of this pass is recorded as not driven, see 11.10).

### 11.9 For the ship step

- Commit `packages/render/assets/` (`Temml.woff2`, `latinmodern-math.woff2`, `TEMML-LICENSE.txt`, `GUST-FONT-LICENSE.txt`, `README-Latin-Modern-Math.txt`), the new files of this pass (`packages/render/src/__tests__/media-context.test.ts`, `apps/studio/src/components/help/help-title.ts`, `help-title.test.ts`, the generated `help-titles.ts`, `apps/studio/src/components/vitals-client.ts`) and the regenerated contracts (step 3).
- Redeploy production before any smoke against it is read (11.1): every deck route of production answers 500 at 17:10 PDT from the instances poisoned at 16:13; the fixed store code heals an instance on its next request and never caches the rejection again.
- Tell Kevin: the seed deck on production stands at the repository's revision 25 and its revisions 26 to 32 are lost (the verifier's finding 0); production's deck routes were down from 16:13 PDT until the redeploy.
- `node scripts/build-home-assets.ts --check` is part of step 29 and now asserts `help-titles.ts`; a change to the first heading of `docs/training.md` or `docs/updates.md` needs `node scripts/build-home-assets.ts --help-titles`.

### 11.10 Commands run and their results

| Command                                                                                             | Result                                                                                                                                                                                                                                                                                                   |
| --------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `node_modules/.bin/tsc -b`                                                                          | exit 0 on every project but `packages/realtime` (`src/blob.ts` 599 and `src/blob.test.ts` `presenceShards`: B7's in progress chat and presence shard work, VERIFICATION-5 findings 3 and 14; not this lane's)                                                                                            |
| `cd packages/store && vitest run`                                                                   | 22 files, 216 tests passed (the two new `ready` rows among them)                                                                                                                                                                                                                                         |
| `cd packages/render && vitest run`                                                                  | 25 files passed, 463 tests; `__tests__/canvas.test.ts` 12 snapshots failed (B4, finding 4, unchanged)                                                                                                                                                                                                    |
| `cd packages/chrome && vitest run --maxWorkers=2`                                                   | 65 files, 565 tests passed                                                                                                                                                                                                                                                                               |
| `cd packages/viewer && vitest run src/present`                                                      | 6 files, 56 tests passed                                                                                                                                                                                                                                                                                 |
| `cd apps/studio && vitest run`                                                                      | 44 files, 288 tests passed                                                                                                                                                                                                                                                                               |
| `node packages/agent/src/generate/main.ts --check`                                                  | "every committed contract is current"                                                                                                                                                                                                                                                                    |
| `node scripts/check.mjs --only 6` (three runs)                                                      | ok; the entry chunk 602,125 then 599,048 then 586,751 B (ceiling 600,000; reported, not asserted, no vendor chunk)                                                                                                                                                                                       |
| `node scripts/check.mjs --only 29`                                                                  | ok (`build-brand --check`, `build-home-assets --check` with the titles row, the definitions, the native record)                                                                                                                                                                                          |
| `node scripts/check.mjs --only 30`                                                                  | ok, `check-vercel-output` 46 of 46 (the eight packages rows among them)                                                                                                                                                                                                                                  |
| `node scripts/build-home-assets.ts --help-titles`; `--check`                                        | wrote `help-titles.ts`; every file matches                                                                                                                                                                                                                                                               |
| the static shortcut reading (`node --input-type=module` over `menus/keys.ts`)                       | `OMITTED_SHORTCUTS` 22 rows, 0 of the 18 planned Google rows omitted, 18 of 18 bound                                                                                                                                                                                                                     |
| `vercel deploy --yes --archive=tgz`; `vercel env pull <scratch>/.env.oidc --yes`; the probe of 11.8 | the table of 11.8                                                                                                                                                                                                                                                                                        |
| production, read only, 17:10 PDT                                                                    | 11.1: five routes 500, `deck.info` 500 `BlobExistsError`, seven instance ids                                                                                                                                                                                                                             |
| `scripts/probes/editor-walk-probe.mjs --quick` on 4321                                              | not driven: `.turboslide/e2e.lock` held by the verifier's chain (`run-chain.sh`, `run-dev-phase.sh`, `run-walk2.sh` alive with Chromium, taken 15:21) for the whole pass; the surfaces this pass changes are covered by the unit rows above and the preview table, and the walk is the verifier's pass 2 |
| `node_modules/.bin/prettier --write <every touched file>`                                           | formatted; the generated module through the generator's own pass                                                                                                                                                                                                                                         |
