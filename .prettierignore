pnpm-lock.yaml
**/dist
**/.output
**/.tanstack
.turbo
.turboslide
decks
**/routeTree.gen.ts
packages/agent/generated
packages/theme/assets
packages/fonts/assets

# Generated by pnpm generate:contracts (M5): stableJson output, compared byte for byte by check step 3
packages/schema/src/rules.json
packages/lint/fixtures/index.json

# The parity audit and the verifier write their evidence here; a regenerated report must not fail check step 19
docs/gslides-parity/verification/**/*.json
docs/gslides-parity/verification-2/**/*.json

# Round two of the Google Slides parity: the round one fixture deck frozen byte for byte at a65b313
# (packages/schema/src/migrations.test.ts reads it; decks/ is ignored above for the same reason), and
# the fonts manifest scripts/build-fonts.py writes, compared by `turboslide fonts build --check`
packages/schema/src/__fixtures__/gslides-r1
packages/fonts/export/fonts.json

# Round four (gslides-parity SPEC-4 0.38; the integrator at merge 2): the wasm module is wasm-bindgen's
# output, committed as a build product whose bytes packages/native/BUILD-RECORD.json pins, so the
# formatter never touches it (check step 29 compares the sha256, step 19 would otherwise reflow the glue)
packages/native/wasm/

# The focus round (docs/FOCUS.md 6; the integrator's fixer, cycle 3 stream fix round two, VERIFICATION C2-F15):
# the core gate, the probes, the layout shift audit and the lanes' audit scripts write their evidence under these
# folders as they produce it, and a regenerated file must not fail check step 19 (the rule the two folders above
# already carry). The hand written notes beside them (VERIFICATION.md, the audit-*.md reports, build/*.md) stay
# under the formatter.
docs/gslides-parity/focus/verification/**/*.json
docs/gslides-parity/focus/verification/*-layout-shift-*.md
docs/gslides-parity/focus/audit-*/**/*.json
docs/gslides-parity/return/audit-*/**/*.json
# The return round's lanes write their run JSON under their evidence folders (build/b1/, build/b3/,
# build/b2-evidence/, build/integrator/); the notes beside them stay under the formatter (b1.md R8)
docs/gslides-parity/return/build/*/**/*.json
# The synthesizer's drive evidence of the return round (RETURN.md: return-drive.md cites them): the run
# JSON and the deck's built page, written by return-drive.mjs and question-pictures.mjs
docs/gslides-parity/return/return-drive/*.json
docs/gslides-parity/return/return-drive/*.html
docs/gslides-parity/build-4/hotfix-4/*.json
docs/gslides-parity/verification-4/production-ship/*.json
# The sync and costs round: the lanes' run JSON under their evidence folders (build/b1/, build/b4/,
# build/b5/, build/integrator/) and the hosting checks' run JSON; the notes beside them stay under
# the formatter (build/b4.md, b5.md R9, b6.md R5)
docs/gslides-parity/sync/build/*/**/*.json
docs/gslides-parity/sync/hosting/**/*.json
# The same rule for the run JSON of the discovery audits committed at 0b4f459 (the sync, hosting and
# features rounds) and of the product round's lanes: evidence a script wrote, never reflowed by hand
docs/gslides-parity/sync/audit-*/**/*.json
docs/gslides-parity/features/audit-*/**/*.json
docs/gslides-parity/product/audit-*/**/*.json
docs/gslides-parity/product/build/*/**/*.json
# The features round: the lanes' run JSON under their evidence folders (build/b1/ to build/b4/,
# build/integrator/); the notes beside them stay under the formatter (build/b4.md section 6)
docs/gslides-parity/features/build/*/**/*.json
# The features round's gate ledgers beside the run JSON (core-matrix.md, core-walk.md, written by
# core-gate.mjs and editor-walk-probe.mjs); the notes beside them stay under the formatter
docs/gslides-parity/features/build/*/**/*-core-matrix.md
docs/gslides-parity/features/build/*/**/*-core-walk.md
